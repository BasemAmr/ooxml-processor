/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: validators for the opc-core-properties namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

export type ValidationCode = 'missing-required' | 'invalid-value' | 'facet-violation';
export interface ValidationIssue { readonly path: string; readonly code: ValidationCode; readonly message: string; }
export type ValidationResult = readonly ValidationIssue[];
function issue(path: string, code: ValidationCode, message: string): ValidationIssue { return { path, code, message }; }
function invalidEnum(path: string, value: unknown, values: ReadonlySet<string>): ValidationIssue { return issue(path, 'invalid-value', `value ${String(value)} is not in the enumeration {${[...values].join(', ')}}`); }
const BUILTIN_GROUPS = {
  string: new Set(['xsd:ID', 'xsd:IDREF', 'xsd:NCName', 'xsd:NMTOKEN', 'xsd:anyURI', 'xsd:base64Binary', 'xsd:date', 'xsd:dateTime', 'xsd:duration', 'xsd:hexBinary', 'xsd:language', 'xsd:string', 'xsd:time', 'xsd:token']),
  boolean: new Set(['xsd:boolean']),
  number: new Set(['xsd:byte', 'xsd:decimal', 'xsd:double', 'xsd:float', 'xsd:int', 'xsd:integer', 'xsd:long', 'xsd:nonNegativeInteger', 'xsd:positiveInteger', 'xsd:unsignedByte', 'xsd:unsignedInt', 'xsd:unsignedLong', 'xsd:unsignedShort']),
};
function validBuiltin(value: unknown, name: string): boolean { if (BUILTIN_GROUPS.string.has(name)) return typeof value === 'string'; if (BUILTIN_GROUPS.boolean.has(name)) return typeof value === 'boolean'; if (BUILTIN_GROUPS.number.has(name)) return typeof value === 'number' && Number.isFinite(value); return false; }

export function validateCT_CoreProperties(value: unknown, path = 'CT_CoreProperties'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['category'] === undefined) {
  } else {
    if (!validBuiltin(v['category'], 'xsd:string')) issues.push(issue(path + '.category', 'invalid-value', 'invalid xsd:string'));
  }
  if (v['contentStatus'] === undefined) {
  } else {
    if (!validBuiltin(v['contentStatus'], 'xsd:string')) issues.push(issue(path + '.contentStatus', 'invalid-value', 'invalid xsd:string'));
  }
  if (v['created'] === undefined) {
  } else {
    if (typeof v['created'] !== 'string') issues.push(issue(path + '.created', 'invalid-value', 'expected a string'));
  }
  if (v['creator'] === undefined) {
  } else {
    if (typeof v['creator'] !== 'string') issues.push(issue(path + '.creator', 'invalid-value', 'expected a string'));
  }
  if (v['description'] === undefined) {
  } else {
    if (typeof v['description'] !== 'string') issues.push(issue(path + '.description', 'invalid-value', 'expected a string'));
  }
  if (v['identifier'] === undefined) {
  } else {
    if (typeof v['identifier'] !== 'string') issues.push(issue(path + '.identifier', 'invalid-value', 'expected a string'));
  }
  if (v['keywords'] === undefined) {
  } else {
    issues.push(...validateCT_Keywords(v['keywords'], path + '.keywords'));
  }
  if (v['language'] === undefined) {
  } else {
    if (typeof v['language'] !== 'string') issues.push(issue(path + '.language', 'invalid-value', 'expected a string'));
  }
  if (v['lastModifiedBy'] === undefined) {
  } else {
    if (!validBuiltin(v['lastModifiedBy'], 'xsd:string')) issues.push(issue(path + '.lastModifiedBy', 'invalid-value', 'invalid xsd:string'));
  }
  if (v['lastPrinted'] === undefined) {
  } else {
    if (!validBuiltin(v['lastPrinted'], 'xsd:dateTime')) issues.push(issue(path + '.lastPrinted', 'invalid-value', 'invalid xsd:dateTime'));
  }
  if (v['modified'] === undefined) {
  } else {
    if (typeof v['modified'] !== 'string') issues.push(issue(path + '.modified', 'invalid-value', 'expected a string'));
  }
  if (v['revision'] === undefined) {
  } else {
    if (!validBuiltin(v['revision'], 'xsd:string')) issues.push(issue(path + '.revision', 'invalid-value', 'invalid xsd:string'));
  }
  if (v['subject'] === undefined) {
  } else {
    if (typeof v['subject'] !== 'string') issues.push(issue(path + '.subject', 'invalid-value', 'expected a string'));
  }
  if (v['title'] === undefined) {
  } else {
    if (typeof v['title'] !== 'string') issues.push(issue(path + '.title', 'invalid-value', 'expected a string'));
  }
  if (v['version'] === undefined) {
  } else {
    if (!validBuiltin(v['version'], 'xsd:string')) issues.push(issue(path + '.version', 'invalid-value', 'invalid xsd:string'));
  }
  return issues;
}

export function validateCT_Keyword(value: unknown, path = 'CT_Keyword'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (!validBuiltin(v['$value'], 'xsd:string')) issues.push(issue(path + '.$value', 'invalid-value', 'invalid xsd:string'));
  if (v['lang'] === undefined) {
  } else {
    if (typeof v['lang'] !== 'string') issues.push(issue(path + '.lang', 'invalid-value', 'expected a string'));
  }
  return issues;
}

export function validateCT_Keywords(value: unknown, path = 'CT_Keywords'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (!Array.isArray(v['value'])) {
  } else {
    for (let i = 0; i < (v['value'] as readonly unknown[]).length; i += 1) {
      issues.push(...validateCT_Keyword((v['value'] as readonly unknown[])[i], path + '.value[' + i + ']'));
    }
  }
  if (v['lang'] === undefined) {
  } else {
    if (typeof v['lang'] !== 'string') issues.push(issue(path + '.lang', 'invalid-value', 'expected a string'));
  }
  return issues;
}
