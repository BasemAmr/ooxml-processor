/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: the vml-powerpoint namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import type { XmlAttr } from '../../runtime/index.js';

// Complex types.

/**
 * **Transitional only.** The writer refuses to emit this into a package of the other dialect.
 *
 * Defined at `transitional/vml-presentationDrawing.xsd:8`.
 */
export interface CT_Empty {
  /** Unrecognized attributes, in source order. See ADR 0009. */
  readonly $unknownAttrs?: readonly XmlAttr[] | undefined;
}

/**
 * **Transitional only.** The writer refuses to emit this into a package of the other dialect.
 *
 * Defined at `transitional/vml-presentationDrawing.xsd:9`.
 */
export interface CT_Rel {
  readonly id?: string | undefined;
  /** Unrecognized attributes, in source order. See ADR 0009. */
  readonly $unknownAttrs?: readonly XmlAttr[] | undefined;
}
