/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: writers for the bibliography namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  PositionedRawQueue,
  type WriteContext,
  type XmlSink,
  uriFor,
} from '../../runtime/index.js';
import type {
  CT_AuthorType,
  CT_NameListType,
  CT_NameOrCorporateType,
  CT_NameType,
  CT_PersonType,
  CT_SourceType,
  CT_Sources,
} from './types.js';

// XML writers. Element names are supplied by the owning particle.

/** Write a `CT_AuthorType`; the caller supplies its element local name. */
export function writeCT_AuthorType(s: XmlSink, value: CT_AuthorType, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'bibliography'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_content of value['content']) {
    if (v_content.kind === 'Artist') writeCT_NameType(s, v_content.value, ctx, 'Artist');
    if (v_content.kind === 'Author') writeCT_NameOrCorporateType(s, v_content.value, ctx, 'Author');
    if (v_content.kind === 'BookAuthor') writeCT_NameType(s, v_content.value, ctx, 'BookAuthor');
    if (v_content.kind === 'Compiler') writeCT_NameType(s, v_content.value, ctx, 'Compiler');
    if (v_content.kind === 'Composer') writeCT_NameType(s, v_content.value, ctx, 'Composer');
    if (v_content.kind === 'Conductor') writeCT_NameType(s, v_content.value, ctx, 'Conductor');
    if (v_content.kind === 'Counsel') writeCT_NameType(s, v_content.value, ctx, 'Counsel');
    if (v_content.kind === 'Director') writeCT_NameType(s, v_content.value, ctx, 'Director');
    if (v_content.kind === 'Editor') writeCT_NameType(s, v_content.value, ctx, 'Editor');
    if (v_content.kind === 'Interviewee') writeCT_NameType(s, v_content.value, ctx, 'Interviewee');
    if (v_content.kind === 'Interviewer') writeCT_NameType(s, v_content.value, ctx, 'Interviewer');
    if (v_content.kind === 'Inventor') writeCT_NameType(s, v_content.value, ctx, 'Inventor');
    if (v_content.kind === 'Performer') writeCT_NameOrCorporateType(s, v_content.value, ctx, 'Performer');
    if (v_content.kind === 'ProducerName') writeCT_NameType(s, v_content.value, ctx, 'ProducerName');
    if (v_content.kind === 'Translator') writeCT_NameType(s, v_content.value, ctx, 'Translator');
    if (v_content.kind === 'Writer') writeCT_NameType(s, v_content.value, ctx, 'Writer');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  s.endElement();
}

/** Write a `CT_NameListType`; the caller supplies its element local name. */
export function writeCT_NameListType(s: XmlSink, value: CT_NameListType, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'bibliography'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['Person'].length; idx++) {
    const v_Person = value['Person'][idx]!;
    writeCT_PersonType(s, v_Person, ctx, 'Person');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NameOrCorporateType`; the caller supplies its element local name. */
export function writeCT_NameOrCorporateType(s: XmlSink, value: CT_NameOrCorporateType, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'bibliography'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'NameList') writeCT_NameListType(s, value['content'].value, ctx, 'NameList');
    if (value['content'].kind === 'Corporate') s.text(String(value['content'].value));
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NameType`; the caller supplies its element local name. */
export function writeCT_NameType(s: XmlSink, value: CT_NameType, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'bibliography'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['NameList'] !== undefined) writeCT_NameListType(s, value['NameList'], ctx, 'NameList');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_PersonType`; the caller supplies its element local name. */
export function writeCT_PersonType(s: XmlSink, value: CT_PersonType, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'bibliography'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['Last'].length; idx++) {
    const v_Last = value['Last'][idx];
    if (v_Last !== undefined) s.text(String(v_Last));
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  for (let idx = 0; idx < value['First'].length; idx++) {
    const v_First = value['First'][idx];
    if (v_First !== undefined) s.text(String(v_First));
    $q.flush(s, 1, idx);
  }
  $q.flush(s, 1);
  for (let idx = 0; idx < value['Middle'].length; idx++) {
    const v_Middle = value['Middle'][idx];
    if (v_Middle !== undefined) s.text(String(v_Middle));
    $q.flush(s, 2, idx);
  }
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Sources`; the caller supplies its element local name. */
export function writeCT_Sources(s: XmlSink, value: CT_Sources, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'bibliography'), localName);
  if (value['SelectedStyle'] !== undefined) s.attr(null, 'SelectedStyle', String(value['SelectedStyle']));
  if (value['StyleName'] !== undefined) s.attr(null, 'StyleName', String(value['StyleName']));
  if (value['URI'] !== undefined) s.attr(null, 'URI', String(value['URI']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['Source'].length; idx++) {
    const v_Source = value['Source'][idx]!;
    writeCT_SourceType(s, v_Source, ctx, 'Source');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SourceType`; the caller supplies its element local name. */
export function writeCT_SourceType(s: XmlSink, value: CT_SourceType, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'bibliography'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_content of value['content']) {
    if (v_content.kind === 'AbbreviatedCaseNumber') s.text(String(v_content.value));
    if (v_content.kind === 'AlbumTitle') s.text(String(v_content.value));
    if (v_content.kind === 'Author') writeCT_AuthorType(s, v_content.value, ctx, 'Author');
    if (v_content.kind === 'BookTitle') s.text(String(v_content.value));
    if (v_content.kind === 'Broadcaster') s.text(String(v_content.value));
    if (v_content.kind === 'BroadcastTitle') s.text(String(v_content.value));
    if (v_content.kind === 'CaseNumber') s.text(String(v_content.value));
    if (v_content.kind === 'ChapterNumber') s.text(String(v_content.value));
    if (v_content.kind === 'City') s.text(String(v_content.value));
    if (v_content.kind === 'Comments') s.text(String(v_content.value));
    if (v_content.kind === 'ConferenceName') s.text(String(v_content.value));
    if (v_content.kind === 'CountryRegion') s.text(String(v_content.value));
    if (v_content.kind === 'Court') s.text(String(v_content.value));
    if (v_content.kind === 'Day') s.text(String(v_content.value));
    if (v_content.kind === 'DayAccessed') s.text(String(v_content.value));
    if (v_content.kind === 'Department') s.text(String(v_content.value));
    if (v_content.kind === 'Distributor') s.text(String(v_content.value));
    if (v_content.kind === 'Edition') s.text(String(v_content.value));
    if (v_content.kind === 'Guid') s.text(String(v_content.value));
    if (v_content.kind === 'Institution') s.text(String(v_content.value));
    if (v_content.kind === 'InternetSiteTitle') s.text(String(v_content.value));
    if (v_content.kind === 'Issue') s.text(String(v_content.value));
    if (v_content.kind === 'JournalName') s.text(String(v_content.value));
    if (v_content.kind === 'LCID') s.text(String(v_content.value));
    if (v_content.kind === 'Medium') s.text(String(v_content.value));
    if (v_content.kind === 'Month') s.text(String(v_content.value));
    if (v_content.kind === 'MonthAccessed') s.text(String(v_content.value));
    if (v_content.kind === 'NumberVolumes') s.text(String(v_content.value));
    if (v_content.kind === 'Pages') s.text(String(v_content.value));
    if (v_content.kind === 'PatentNumber') s.text(String(v_content.value));
    if (v_content.kind === 'PeriodicalTitle') s.text(String(v_content.value));
    if (v_content.kind === 'ProductionCompany') s.text(String(v_content.value));
    if (v_content.kind === 'PublicationTitle') s.text(String(v_content.value));
    if (v_content.kind === 'Publisher') s.text(String(v_content.value));
    if (v_content.kind === 'RecordingNumber') s.text(String(v_content.value));
    if (v_content.kind === 'RefOrder') s.text(String(v_content.value));
    if (v_content.kind === 'Reporter') s.text(String(v_content.value));
    if (v_content.kind === 'SourceType') s.text(String(v_content.value));
    if (v_content.kind === 'ShortTitle') s.text(String(v_content.value));
    if (v_content.kind === 'StandardNumber') s.text(String(v_content.value));
    if (v_content.kind === 'StateProvince') s.text(String(v_content.value));
    if (v_content.kind === 'Station') s.text(String(v_content.value));
    if (v_content.kind === 'Tag') s.text(String(v_content.value));
    if (v_content.kind === 'Theater') s.text(String(v_content.value));
    if (v_content.kind === 'ThesisType') s.text(String(v_content.value));
    if (v_content.kind === 'Title') s.text(String(v_content.value));
    if (v_content.kind === 'Type') s.text(String(v_content.value));
    if (v_content.kind === 'URL') s.text(String(v_content.value));
    if (v_content.kind === 'Version') s.text(String(v_content.value));
    if (v_content.kind === 'Volume') s.text(String(v_content.value));
    if (v_content.kind === 'Year') s.text(String(v_content.value));
    if (v_content.kind === 'YearAccessed') s.text(String(v_content.value));
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  s.endElement();
}
