/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: validators for the doc-props-custom namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  validateCT_Array,
  validateCT_Empty,
  validateCT_Null,
  validateCT_Vector,
  validateCT_Vstream,
  validateST_Cy,
  validateST_Error,
} from '../doc-props-vt/validator.js';
import { validateST_Guid } from '../shared-types/validator.js';

export type ValidationCode = 'missing-required' | 'invalid-value' | 'facet-violation';
export interface ValidationIssue { readonly path: string; readonly code: ValidationCode; readonly message: string; }
export type ValidationResult = readonly ValidationIssue[];
function issue(path: string, code: ValidationCode, message: string): ValidationIssue { return { path, code, message }; }
function invalidEnum(path: string, value: unknown, values: ReadonlySet<string>): ValidationIssue { return issue(path, 'invalid-value', `value ${String(value)} is not in the enumeration {${[...values].join(', ')}}`); }
const BUILTIN_GROUPS = {
  string: new Set(['xsd:ID', 'xsd:IDREF', 'xsd:NCName', 'xsd:NMTOKEN', 'xsd:anyURI', 'xsd:base64Binary', 'xsd:date', 'xsd:dateTime', 'xsd:duration', 'xsd:hexBinary', 'xsd:language', 'xsd:string', 'xsd:time', 'xsd:token']),
  boolean: new Set(['xsd:boolean']),
  number: new Set(['xsd:byte', 'xsd:decimal', 'xsd:double', 'xsd:float', 'xsd:int', 'xsd:integer', 'xsd:long', 'xsd:nonNegativeInteger', 'xsd:positiveInteger', 'xsd:unsignedByte', 'xsd:unsignedInt', 'xsd:unsignedLong', 'xsd:unsignedShort']),
};
function validBuiltin(value: unknown, name: string): boolean { if (BUILTIN_GROUPS.string.has(name)) return typeof value === 'string'; if (BUILTIN_GROUPS.boolean.has(name)) return typeof value === 'boolean'; if (BUILTIN_GROUPS.number.has(name)) return typeof value === 'number' && Number.isFinite(value); return false; }

export function validateCT_Properties(value: unknown, path = 'CT_Properties'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (!Array.isArray(v['property'])) {
  } else {
    for (let i = 0; i < (v['property'] as readonly unknown[]).length; i += 1) {
      issues.push(...validateCT_Property((v['property'] as readonly unknown[])[i], path + '.property[' + i + ']'));
    }
  }
  return issues;
}

export function validateCT_Property(value: unknown, path = 'CT_Property'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['content'] === undefined) {
    issues.push(issue(path + '.content', 'missing-required', 'required slot is absent'));
  } else {
    if (v['content'] && typeof v['content'] === 'object') {
      const item = v['content'] as { kind?: unknown; value?: unknown };
      if (item.kind !== '$raw') {
        if (item.kind === 'vector') issues.push(...validateCT_Vector(item.value, path + '.content' + '.value'));
        if (item.kind === 'array') issues.push(...validateCT_Array(item.value, path + '.content' + '.value'));
        if (item.kind === 'empty') issues.push(...validateCT_Empty(item.value, path + '.content' + '.value'));
        if (item.kind === 'null') issues.push(...validateCT_Null(item.value, path + '.content' + '.value'));
        if (item.kind === 'cy') issues.push(...validateST_Cy(item.value, path + '.content' + '.value'));
        if (item.kind === 'error') issues.push(...validateST_Error(item.value, path + '.content' + '.value'));
        if (item.kind === 'vstream') issues.push(...validateCT_Vstream(item.value, path + '.content' + '.value'));
        if (item.kind === 'clsid') issues.push(...validateST_Guid(item.value, path + '.content' + '.value'));
        if ('vector' !== item.kind && 'array' !== item.kind && 'blob' !== item.kind && 'oblob' !== item.kind && 'empty' !== item.kind && 'null' !== item.kind && 'i1' !== item.kind && 'i2' !== item.kind && 'i4' !== item.kind && 'i8' !== item.kind && 'int' !== item.kind && 'ui1' !== item.kind && 'ui2' !== item.kind && 'ui4' !== item.kind && 'ui8' !== item.kind && 'uint' !== item.kind && 'r4' !== item.kind && 'r8' !== item.kind && 'decimal' !== item.kind && 'lpstr' !== item.kind && 'lpwstr' !== item.kind && 'bstr' !== item.kind && 'date' !== item.kind && 'filetime' !== item.kind && 'bool' !== item.kind && 'cy' !== item.kind && 'error' !== item.kind && 'stream' !== item.kind && 'ostream' !== item.kind && 'storage' !== item.kind && 'ostorage' !== item.kind && 'vstream' !== item.kind && 'clsid' !== item.kind) issues.push(issue(path + '.content', 'invalid-value', 'unknown choice alternative'));
      }
    }
  }
  if (v['fmtid'] === undefined) {
    issues.push(issue(path + '.fmtid', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_Guid(v['fmtid'], path + '.fmtid'));
  }
  if (v['pid'] === undefined) {
    issues.push(issue(path + '.pid', 'missing-required', 'required attribute is absent'));
  } else {
    if (!validBuiltin(v['pid'], 'xsd:int')) issues.push(issue(path + '.pid', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['name'] === undefined) {
  } else {
    if (!validBuiltin(v['name'], 'xsd:string')) issues.push(issue(path + '.name', 'invalid-value', 'invalid xsd:string'));
  }
  if (v['linkTarget'] === undefined) {
  } else {
    if (!validBuiltin(v['linkTarget'], 'xsd:string')) issues.push(issue(path + '.linkTarget', 'invalid-value', 'invalid xsd:string'));
  }
  return issues;
}
