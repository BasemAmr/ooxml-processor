/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the vml-office namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type RawNode,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  parseDecimal,
  parseDouble,
  parseInteger,
  readScalar,
  requireStart,
} from '../../runtime/index.js';
import { parseST_RelationshipId } from '../relationships/reader.js';
import type { ST_RelationshipId } from '../relationships/types.js';
import {
  parseST_ColorType,
  parseST_Guid,
  parseST_TrueFalse,
  parseST_TrueFalseBlank,
} from '../shared-types/reader.js';
import type {
  ST_ColorType,
  ST_Guid,
  ST_TrueFalse,
  ST_TrueFalseBlank,
} from '../shared-types/types.js';
import {
  parseST_Ext,
  parseST_FillType as vml_parseST_FillType,
  parseST_ImageAspect,
  parseST_StrokeArrowLength,
  parseST_StrokeArrowType,
  parseST_StrokeArrowWidth,
  parseST_StrokeEndCap,
  parseST_StrokeJoinStyle,
  parseST_StrokeLineStyle,
  readCT_Fill as vml_readCT_Fill,
  readCT_Shadow,
  readCT_Stroke,
  readCT_Textbox,
} from '../vml/reader.js';
import type {
  CT_Fill as vml_CT_Fill,
  CT_Shadow,
  CT_Stroke,
  CT_Textbox,
  ST_Ext,
  ST_FillType as vml_ST_FillType,
  ST_ImageAspect,
  ST_StrokeArrowLength,
  ST_StrokeArrowType,
  ST_StrokeArrowWidth,
  ST_StrokeEndCap,
  ST_StrokeJoinStyle,
  ST_StrokeLineStyle,
} from '../vml/types.js';
import {
  type CT_Callout,
  type CT_ClipPath,
  type CT_ColorMenu,
  type CT_ColorMru,
  type CT_Complex,
  type CT_Diagram,
  type CT_Entry,
  type CT_EquationXml,
  type CT_Extrusion,
  type CT_Fill,
  type CT_IdMap,
  type CT_Ink,
  type CT_Lock,
  type CT_OLEObject,
  type CT_Proxy,
  type CT_R,
  type CT_RegroupTable,
  type CT_Relation,
  type CT_RelationTable,
  type CT_Rules,
  type CT_ShapeDefaults,
  type CT_ShapeLayout,
  type CT_SignatureLine,
  type CT_Skew,
  type CT_StrokeChild,
  type ST_AlternateMathContentType,
  type ST_Angle,
  ST_Angle_VALUES,
  type ST_BWMode,
  ST_BWMode_VALUES,
  type ST_CalloutDrop,
  type ST_CalloutPlacement,
  ST_CalloutPlacement_VALUES,
  type ST_ColorMode,
  ST_ColorMode_VALUES,
  type ST_ConnectType,
  ST_ConnectType_VALUES,
  type ST_ConnectorType,
  ST_ConnectorType_VALUES,
  type ST_ContentType,
  type ST_DiagramLayout,
  ST_DiagramLayout_VALUES,
  type ST_ExtrusionPlane,
  ST_ExtrusionPlane_VALUES,
  type ST_ExtrusionRender,
  ST_ExtrusionRender_VALUES,
  type ST_ExtrusionType,
  ST_ExtrusionType_VALUES,
  type ST_FillType,
  ST_FillType_VALUES,
  type ST_How,
  ST_How_VALUES,
  type ST_HrAlign,
  ST_HrAlign_VALUES,
  type ST_InsetMode,
  ST_InsetMode_VALUES,
  type ST_OLEDrawAspect,
  ST_OLEDrawAspect_VALUES,
  type ST_OLELinkType,
  type ST_OLEType,
  ST_OLEType_VALUES,
  type ST_OLEUpdateMode,
  ST_OLEUpdateMode_VALUES,
  type ST_RType,
  ST_RType_VALUES,
  type ST_ScreenSize,
  ST_ScreenSize_VALUES,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_AlternateMathContentType(value: string): ST_AlternateMathContentType | undefined {
  return value as ST_AlternateMathContentType;
}

export function parseST_Angle(value: string): ST_Angle | undefined {
  const s = collapse(value);
  return ST_Angle_VALUES.has(s) ? (s as ST_Angle) : undefined;
}

export function parseST_BWMode(value: string): ST_BWMode | undefined {
  const s = collapse(value);
  return ST_BWMode_VALUES.has(s) ? (s as ST_BWMode) : undefined;
}

export function parseST_CalloutDrop(value: string): ST_CalloutDrop | undefined {
  return value as ST_CalloutDrop;
}

export function parseST_CalloutPlacement(value: string): ST_CalloutPlacement | undefined {
  const s = collapse(value);
  return ST_CalloutPlacement_VALUES.has(s) ? (s as ST_CalloutPlacement) : undefined;
}

export function parseST_ColorMode(value: string): ST_ColorMode | undefined {
  const s = collapse(value);
  return ST_ColorMode_VALUES.has(s) ? (s as ST_ColorMode) : undefined;
}

export function parseST_ConnectType(value: string): ST_ConnectType | undefined {
  const s = collapse(value);
  return ST_ConnectType_VALUES.has(s) ? (s as ST_ConnectType) : undefined;
}

export function parseST_ConnectorType(value: string): ST_ConnectorType | undefined {
  const s = collapse(value);
  return ST_ConnectorType_VALUES.has(s) ? (s as ST_ConnectorType) : undefined;
}

export function parseST_ContentType(value: string): ST_ContentType | undefined {
  return value as ST_ContentType;
}

export function parseST_DiagramLayout(value: string): ST_DiagramLayout | undefined {
  const s = collapse(value);
  return ST_DiagramLayout_VALUES.has(s) ? (s as ST_DiagramLayout) : undefined;
}

export function parseST_ExtrusionPlane(value: string): ST_ExtrusionPlane | undefined {
  const s = collapse(value);
  return ST_ExtrusionPlane_VALUES.has(s) ? (s as ST_ExtrusionPlane) : undefined;
}

export function parseST_ExtrusionRender(value: string): ST_ExtrusionRender | undefined {
  const s = collapse(value);
  return ST_ExtrusionRender_VALUES.has(s) ? (s as ST_ExtrusionRender) : undefined;
}

export function parseST_ExtrusionType(value: string): ST_ExtrusionType | undefined {
  const s = collapse(value);
  return ST_ExtrusionType_VALUES.has(s) ? (s as ST_ExtrusionType) : undefined;
}

export function parseST_FillType(value: string): ST_FillType | undefined {
  const s = collapse(value);
  return ST_FillType_VALUES.has(s) ? (s as ST_FillType) : undefined;
}

export function parseST_How(value: string): ST_How | undefined {
  const s = collapse(value);
  return ST_How_VALUES.has(s) ? (s as ST_How) : undefined;
}

export function parseST_HrAlign(value: string): ST_HrAlign | undefined {
  const s = collapse(value);
  return ST_HrAlign_VALUES.has(s) ? (s as ST_HrAlign) : undefined;
}

export function parseST_InsetMode(value: string): ST_InsetMode | undefined {
  const s = collapse(value);
  return ST_InsetMode_VALUES.has(s) ? (s as ST_InsetMode) : undefined;
}

export function parseST_OLEDrawAspect(value: string): ST_OLEDrawAspect | undefined {
  const s = collapse(value);
  return ST_OLEDrawAspect_VALUES.has(s) ? (s as ST_OLEDrawAspect) : undefined;
}

export function parseST_OLELinkType(value: string): ST_OLELinkType | undefined {
  return value as ST_OLELinkType;
}

export function parseST_OLEType(value: string): ST_OLEType | undefined {
  const s = collapse(value);
  return ST_OLEType_VALUES.has(s) ? (s as ST_OLEType) : undefined;
}

export function parseST_OLEUpdateMode(value: string): ST_OLEUpdateMode | undefined {
  const s = collapse(value);
  return ST_OLEUpdateMode_VALUES.has(s) ? (s as ST_OLEUpdateMode) : undefined;
}

export function parseST_RType(value: string): ST_RType | undefined {
  const s = collapse(value);
  return ST_RType_VALUES.has(s) ? (s as ST_RType) : undefined;
}

export function parseST_ScreenSize(value: string): ST_ScreenSize | undefined {
  const s = collapse(value);
  return ST_ScreenSize_VALUES.has(s) ? (s as ST_ScreenSize) : undefined;
}

// Complex-type readers.

/** Read a `CT_Callout`. See `transitional/vml-officeDrawing.xsd:261`. */
export function readCT_Callout(cur: XmlCursor, ctx: ReadContext): CT_Callout {
  const start = requireStart(cur, 'CT_Callout');
  let ext: ST_Ext | undefined;
  let on: ST_TrueFalse | undefined;
  let type: string | undefined;
  let gap: string | undefined;
  let angle: ST_Angle | undefined;
  let dropauto: ST_TrueFalse | undefined;
  let drop: ST_CalloutDrop | undefined;
  let distance: string | undefined;
  let lengthspecified: ST_TrueFalse | undefined;
  let length: string | undefined;
  let accentbar: ST_TrueFalse | undefined;
  let textborder: ST_TrueFalse | undefined;
  let minusx: ST_TrueFalse | undefined;
  let minusy: ST_TrueFalse | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'on': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            on = v;
            continue;
          }
          ctx.invalidValue('CT_Callout', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'type':
          type = a.value as string;
          continue;

        case 'gap':
          gap = a.value as string;
          continue;

        case 'angle': {
          const v = parseST_Angle(a.value);
          if (v !== undefined) {
            angle = v;
            continue;
          }
          ctx.invalidValue('CT_Callout', a, 'ST_Angle', cur);
          break;
        }
        case 'dropauto': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            dropauto = v;
            continue;
          }
          ctx.invalidValue('CT_Callout', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'drop': {
          const v = parseST_CalloutDrop(a.value);
          if (v !== undefined) {
            drop = v;
            continue;
          }
          ctx.invalidValue('CT_Callout', a, 'ST_CalloutDrop', cur);
          break;
        }
        case 'distance':
          distance = a.value as string;
          continue;

        case 'lengthspecified': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            lengthspecified = v;
            continue;
          }
          ctx.invalidValue('CT_Callout', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'length':
          length = a.value as string;
          continue;

        case 'accentbar': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            accentbar = v;
            continue;
          }
          ctx.invalidValue('CT_Callout', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'textborder': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            textborder = v;
            continue;
          }
          ctx.invalidValue('CT_Callout', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'minusx': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            minusx = v;
            continue;
          }
          ctx.invalidValue('CT_Callout', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'minusy': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            minusy = v;
            continue;
          }
          ctx.invalidValue('CT_Callout', a, 'ST_TrueFalse', cur);
          break;
        }
      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_Callout', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Callout', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Callout', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Callout', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Callout', cur);
    cur.next();
  }
  return {
    ext,
    on,
    type,
    gap,
    angle,
    dropauto,
    drop,
    distance,
    lengthspecified,
    length,
    accentbar,
    textborder,
    minusx,
    minusy,
    $unknownAttrs,
  };
}

/** Read a `CT_ClipPath`. See `transitional/vml-officeDrawing.xsd:337`. */
export function readCT_ClipPath(cur: XmlCursor, ctx: ReadContext): CT_ClipPath {
  const start = requireStart(cur, 'CT_ClipPath');
  let v_: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_office_A = ctx.uris['vml-office'];
  for (const a of start.attrs) {
    if (a.uri === NS_vml_office_A) {
      switch (a.localName) {
        case 'v':
          v_ = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_ClipPath', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (v_ === undefined) ctx.missingRequired('CT_ClipPath', 'v', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ClipPath', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ClipPath', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ClipPath', cur);
    cur.next();
  }
  return {
    v: v_,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorMenu`. See `transitional/vml-officeDrawing.xsd:198`. */
export function readCT_ColorMenu(cur: XmlCursor, ctx: ReadContext): CT_ColorMenu {
  const start = requireStart(cur, 'CT_ColorMenu');
  let ext: ST_Ext | undefined;
  let strokecolor: ST_ColorType | undefined;
  let fillcolor: ST_ColorType | undefined;
  let shadowcolor: ST_ColorType | undefined;
  let extrusioncolor: ST_ColorType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'strokecolor': {
          const v = parseST_ColorType(a.value);
          if (v !== undefined) {
            strokecolor = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMenu', a, 'ST_ColorType', cur);
          break;
        }
        case 'fillcolor': {
          const v = parseST_ColorType(a.value);
          if (v !== undefined) {
            fillcolor = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMenu', a, 'ST_ColorType', cur);
          break;
        }
        case 'shadowcolor': {
          const v = parseST_ColorType(a.value);
          if (v !== undefined) {
            shadowcolor = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMenu', a, 'ST_ColorType', cur);
          break;
        }
        case 'extrusioncolor': {
          const v = parseST_ColorType(a.value);
          if (v !== undefined) {
            extrusioncolor = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMenu', a, 'ST_ColorType', cur);
          break;
        }
      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMenu', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ColorMenu', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorMenu', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ColorMenu', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorMenu', cur);
    cur.next();
  }
  return {
    ext,
    strokecolor,
    fillcolor,
    shadowcolor,
    extrusioncolor,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorMru`. See `transitional/vml-officeDrawing.xsd:194`. */
export function readCT_ColorMru(cur: XmlCursor, ctx: ReadContext): CT_ColorMru {
  const start = requireStart(cur, 'CT_ColorMru');
  let ext: ST_Ext | undefined;
  let colors: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'colors':
          colors = a.value as string;
          continue;

      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMru', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ColorMru', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorMru', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ColorMru', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorMru', cur);
    cur.next();
  }
  return {
    ext,
    colors,
    $unknownAttrs,
  };
}

/** Read a `CT_Complex`. See `transitional/vml-officeDrawing.xsd:305`. */
export function readCT_Complex(cur: XmlCursor, ctx: ReadContext): CT_Complex {
  const start = requireStart(cur, 'CT_Complex');
  let ext: ST_Ext | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_Complex', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Complex', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Complex', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Complex', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Complex', cur);
    cur.next();
  }
  return {
    ext,
    $unknownAttrs,
  };
}

/** Read a `CT_Diagram`. See `transitional/vml-officeDrawing.xsd:158`. */
export function readCT_Diagram(cur: XmlCursor, ctx: ReadContext): CT_Diagram {
  const start = requireStart(cur, 'CT_Diagram');
  let ext: ST_Ext | undefined;
  let dgmstyle: number | undefined;
  let autoformat: ST_TrueFalse | undefined;
  let reverse: ST_TrueFalse | undefined;
  let autolayout: ST_TrueFalse | undefined;
  let dgmscalex: number | undefined;
  let dgmscaley: number | undefined;
  let dgmfontsize: number | undefined;
  let constrainbounds: string | undefined;
  let dgmbasetextscale: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'dgmstyle': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            dgmstyle = v;
            continue;
          }
          ctx.invalidValue('CT_Diagram', a, 'xsd:integer', cur);
          break;
        }
        case 'autoformat': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            autoformat = v;
            continue;
          }
          ctx.invalidValue('CT_Diagram', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'reverse': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            reverse = v;
            continue;
          }
          ctx.invalidValue('CT_Diagram', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'autolayout': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            autolayout = v;
            continue;
          }
          ctx.invalidValue('CT_Diagram', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'dgmscalex': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            dgmscalex = v;
            continue;
          }
          ctx.invalidValue('CT_Diagram', a, 'xsd:integer', cur);
          break;
        }
        case 'dgmscaley': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            dgmscaley = v;
            continue;
          }
          ctx.invalidValue('CT_Diagram', a, 'xsd:integer', cur);
          break;
        }
        case 'dgmfontsize': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            dgmfontsize = v;
            continue;
          }
          ctx.invalidValue('CT_Diagram', a, 'xsd:integer', cur);
          break;
        }
        case 'constrainbounds':
          constrainbounds = a.value as string;
          continue;

        case 'dgmbasetextscale': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            dgmbasetextscale = v;
            continue;
          }
          ctx.invalidValue('CT_Diagram', a, 'xsd:integer', cur);
          break;
        }
      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_Diagram', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Diagram', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let relationtable: CT_RelationTable | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_vml_office = ctx.uris['vml-office'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Diagram', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_vml_office) {
        switch (ev.localName) {
          case 'relationtable': {
            relationtable = readCT_RelationTable(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Diagram', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Diagram', cur);
    cur.next();
  }

  return {
    relationtable,
    ext,
    dgmstyle,
    autoformat,
    reverse,
    autolayout,
    dgmscalex,
    dgmscaley,
    dgmfontsize,
    constrainbounds,
    dgmbasetextscale,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Entry`. See `transitional/vml-officeDrawing.xsd:133`. */
export function readCT_Entry(cur: XmlCursor, ctx: ReadContext): CT_Entry {
  const start = requireStart(cur, 'CT_Entry');
  let new_: number | undefined;
  let old: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'new': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            new_ = v;
            continue;
          }
          ctx.invalidValue('CT_Entry', a, 'xsd:int', cur);
          break;
        }
        case 'old': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            old = v;
            continue;
          }
          ctx.invalidValue('CT_Entry', a, 'xsd:int', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Entry', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Entry', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Entry', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Entry', cur);
    cur.next();
  }
  return {
    new: new_,
    old,
    $unknownAttrs,
  };
}

/** Read a `CT_EquationXml`. See `transitional/vml-officeDrawing.xsd:173`. */
export function readCT_EquationXml(cur: XmlCursor, ctx: ReadContext): CT_EquationXml {
  const start = requireStart(cur, 'CT_EquationXml');
  let contentType: ST_AlternateMathContentType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'contentType': {
          const v = parseST_AlternateMathContentType(a.value);
          if (v !== undefined) {
            contentType = v;
            continue;
          }
          ctx.invalidValue('CT_EquationXml', a, 'ST_AlternateMathContentType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_EquationXml', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const any: RawNode[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EquationXml', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      any.push(cur.skipToRaw());
      $slot = 0;
      $index = any.length - 1;
      continue;
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EquationXml', cur);
    cur.next();
  }

  return {
    any,
    contentType,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Extrusion`. See `transitional/vml-officeDrawing.xsd:226`. */
export function readCT_Extrusion(cur: XmlCursor, ctx: ReadContext): CT_Extrusion {
  const start = requireStart(cur, 'CT_Extrusion');
  let ext: ST_Ext | undefined;
  let on: ST_TrueFalse | undefined;
  let type: ST_ExtrusionType | undefined;
  let render: ST_ExtrusionRender | undefined;
  let viewpointorigin: string | undefined;
  let viewpoint: string | undefined;
  let plane: ST_ExtrusionPlane | undefined;
  let skewangle: number | undefined;
  let skewamt: string | undefined;
  let foredepth: string | undefined;
  let backdepth: string | undefined;
  let orientation: string | undefined;
  let orientationangle: number | undefined;
  let lockrotationcenter: ST_TrueFalse | undefined;
  let autorotationcenter: ST_TrueFalse | undefined;
  let rotationcenter: string | undefined;
  let rotationangle: string | undefined;
  let colormode: ST_ColorMode | undefined;
  let color: ST_ColorType | undefined;
  let shininess: number | undefined;
  let specularity: string | undefined;
  let diffusity: string | undefined;
  let metal: ST_TrueFalse | undefined;
  let edge: string | undefined;
  let facet: string | undefined;
  let lightface: ST_TrueFalse | undefined;
  let brightness: string | undefined;
  let lightposition: string | undefined;
  let lightlevel: string | undefined;
  let lightharsh: ST_TrueFalse | undefined;
  let lightposition2: string | undefined;
  let lightlevel2: string | undefined;
  let lightharsh2: ST_TrueFalse | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'on': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            on = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'type': {
          const v = parseST_ExtrusionType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_ExtrusionType', cur);
          break;
        }
        case 'render': {
          const v = parseST_ExtrusionRender(a.value);
          if (v !== undefined) {
            render = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_ExtrusionRender', cur);
          break;
        }
        case 'viewpointorigin':
          viewpointorigin = a.value as string;
          continue;

        case 'viewpoint':
          viewpoint = a.value as string;
          continue;

        case 'plane': {
          const v = parseST_ExtrusionPlane(a.value);
          if (v !== undefined) {
            plane = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_ExtrusionPlane', cur);
          break;
        }
        case 'skewangle': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            skewangle = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'xsd:float', cur);
          break;
        }
        case 'skewamt':
          skewamt = a.value as string;
          continue;

        case 'foredepth':
          foredepth = a.value as string;
          continue;

        case 'backdepth':
          backdepth = a.value as string;
          continue;

        case 'orientation':
          orientation = a.value as string;
          continue;

        case 'orientationangle': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            orientationangle = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'xsd:float', cur);
          break;
        }
        case 'lockrotationcenter': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            lockrotationcenter = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'autorotationcenter': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            autorotationcenter = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'rotationcenter':
          rotationcenter = a.value as string;
          continue;

        case 'rotationangle':
          rotationangle = a.value as string;
          continue;

        case 'colormode': {
          const v = parseST_ColorMode(a.value);
          if (v !== undefined) {
            colormode = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_ColorMode', cur);
          break;
        }
        case 'color': {
          const v = parseST_ColorType(a.value);
          if (v !== undefined) {
            color = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_ColorType', cur);
          break;
        }
        case 'shininess': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            shininess = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'xsd:float', cur);
          break;
        }
        case 'specularity':
          specularity = a.value as string;
          continue;

        case 'diffusity':
          diffusity = a.value as string;
          continue;

        case 'metal': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            metal = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'edge':
          edge = a.value as string;
          continue;

        case 'facet':
          facet = a.value as string;
          continue;

        case 'lightface': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            lightface = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'brightness':
          brightness = a.value as string;
          continue;

        case 'lightposition':
          lightposition = a.value as string;
          continue;

        case 'lightlevel':
          lightlevel = a.value as string;
          continue;

        case 'lightharsh': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            lightharsh = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'lightposition2':
          lightposition2 = a.value as string;
          continue;

        case 'lightlevel2':
          lightlevel2 = a.value as string;
          continue;

        case 'lightharsh2': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            lightharsh2 = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_TrueFalse', cur);
          break;
        }
      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_Extrusion', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Extrusion', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Extrusion', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Extrusion', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Extrusion', cur);
    cur.next();
  }
  return {
    ext,
    on,
    type,
    render,
    viewpointorigin,
    viewpoint,
    plane,
    skewangle,
    skewamt,
    foredepth,
    backdepth,
    orientation,
    orientationangle,
    lockrotationcenter,
    autorotationcenter,
    rotationcenter,
    rotationangle,
    colormode,
    color,
    shininess,
    specularity,
    diffusity,
    metal,
    edge,
    facet,
    lightface,
    brightness,
    lightposition,
    lightlevel,
    lightharsh,
    lightposition2,
    lightlevel2,
    lightharsh2,
    $unknownAttrs,
  };
}

/** Read a `CT_Fill`. See `transitional/vml-officeDrawing.xsd:340`. */
export function readCT_Fill(cur: XmlCursor, ctx: ReadContext): CT_Fill {
  const start = requireStart(cur, 'CT_Fill');
  let ext: ST_Ext | undefined;
  let type: ST_FillType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_FillType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Fill', a, 'ST_FillType', cur);
          break;
        }
      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_Fill', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Fill', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Fill', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Fill', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Fill', cur);
    cur.next();
  }
  return {
    ext,
    type,
    $unknownAttrs,
  };
}

/** Read a `CT_IdMap`. See `transitional/vml-officeDrawing.xsd:123`. */
export function readCT_IdMap(cur: XmlCursor, ctx: ReadContext): CT_IdMap {
  const start = requireStart(cur, 'CT_IdMap');
  let ext: ST_Ext | undefined;
  let data: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'data':
          data = a.value as string;
          continue;

      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_IdMap', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_IdMap', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_IdMap', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_IdMap', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_IdMap', cur);
    cur.next();
  }
  return {
    ext,
    data,
    $unknownAttrs,
  };
}

/** Read a `CT_Ink`. See `transitional/vml-officeDrawing.xsd:94`. */
export function readCT_Ink(cur: XmlCursor, ctx: ReadContext): CT_Ink {
  const start = requireStart(cur, 'CT_Ink');
  let i: string | undefined;
  let annotation: ST_TrueFalse | undefined;
  let contentType: ST_ContentType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'i':
          i = a.value as string;
          continue;

        case 'annotation': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            annotation = v;
            continue;
          }
          ctx.invalidValue('CT_Ink', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'contentType': {
          const v = parseST_ContentType(a.value);
          if (v !== undefined) {
            contentType = v;
            continue;
          }
          ctx.invalidValue('CT_Ink', a, 'ST_ContentType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Ink', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Ink', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Ink', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Ink', cur);
    cur.next();
  }
  return {
    i,
    annotation,
    contentType,
    $unknownAttrs,
  };
}

/** Read a `CT_Lock`. See `transitional/vml-officeDrawing.xsd:277`. */
export function readCT_Lock(cur: XmlCursor, ctx: ReadContext): CT_Lock {
  const start = requireStart(cur, 'CT_Lock');
  let ext: ST_Ext | undefined;
  let position: ST_TrueFalse | undefined;
  let selection: ST_TrueFalse | undefined;
  let grouping: ST_TrueFalse | undefined;
  let ungrouping: ST_TrueFalse | undefined;
  let rotation: ST_TrueFalse | undefined;
  let cropping: ST_TrueFalse | undefined;
  let verticies: ST_TrueFalse | undefined;
  let adjusthandles: ST_TrueFalse | undefined;
  let text: ST_TrueFalse | undefined;
  let aspectratio: ST_TrueFalse | undefined;
  let shapetype: ST_TrueFalse | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'position': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            position = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'selection': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            selection = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'grouping': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            grouping = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'ungrouping': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            ungrouping = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'rotation': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            rotation = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'cropping': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            cropping = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'verticies': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            verticies = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'adjusthandles': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            adjusthandles = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'text': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            text = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'aspectratio': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            aspectratio = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'shapetype': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            shapetype = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_TrueFalse', cur);
          break;
        }
      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Lock', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Lock', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Lock', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Lock', cur);
    cur.next();
  }
  return {
    ext,
    position,
    selection,
    grouping,
    ungrouping,
    rotation,
    cropping,
    verticies,
    adjusthandles,
    text,
    aspectratio,
    shapetype,
    $unknownAttrs,
  };
}

/** Read a `CT_OLEObject`. See `transitional/vml-officeDrawing.xsd:291`. */
export function readCT_OLEObject(cur: XmlCursor, ctx: ReadContext): CT_OLEObject {
  const start = requireStart(cur, 'CT_OLEObject');
  let Type: ST_OLEType | undefined;
  let ProgID: string | undefined;
  let ShapeID: string | undefined;
  let DrawAspect: ST_OLEDrawAspect | undefined;
  let ObjectID: string | undefined;
  let id: ST_RelationshipId | undefined;
  let UpdateMode: ST_OLEUpdateMode | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'Type': {
          const v = parseST_OLEType(a.value);
          if (v !== undefined) {
            Type = v;
            continue;
          }
          ctx.invalidValue('CT_OLEObject', a, 'ST_OLEType', cur);
          break;
        }
        case 'ProgID':
          ProgID = a.value as string;
          continue;

        case 'ShapeID':
          ShapeID = a.value as string;
          continue;

        case 'DrawAspect': {
          const v = parseST_OLEDrawAspect(a.value);
          if (v !== undefined) {
            DrawAspect = v;
            continue;
          }
          ctx.invalidValue('CT_OLEObject', a, 'ST_OLEDrawAspect', cur);
          break;
        }
        case 'ObjectID':
          ObjectID = a.value as string;
          continue;

        case 'UpdateMode': {
          const v = parseST_OLEUpdateMode(a.value);
          if (v !== undefined) {
            UpdateMode = v;
            continue;
          }
          ctx.invalidValue('CT_OLEObject', a, 'ST_OLEUpdateMode', cur);
          break;
        }
      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_OLEObject', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OLEObject', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let LinkType: ST_OLELinkType | undefined;
  let LockedField: ST_TrueFalseBlank | undefined;
  let FieldCodes: string | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_vml_office = ctx.uris['vml-office'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OLEObject', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_vml_office) {
        switch (ev.localName) {
          case 'LinkType': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_OLELinkType($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_OLEObject', 'LinkType', 'ST_OLELinkType', $s, cur);
            LinkType = ($pv ?? $s) as ST_OLELinkType;
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'LockedField': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_OLEObject', 'LockedField', 'ST_TrueFalseBlank', $s, cur);
            LockedField = ($pv ?? $s) as ST_TrueFalseBlank;
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'FieldCodes': {
            FieldCodes = readScalar(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OLEObject', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OLEObject', cur);
    cur.next();
  }

  return {
    LinkType,
    LockedField,
    FieldCodes,
    Type,
    ProgID,
    ShapeID,
    DrawAspect,
    ObjectID,
    id,
    UpdateMode,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Proxy`. See `transitional/vml-officeDrawing.xsd:152`. */
export function readCT_Proxy(cur: XmlCursor, ctx: ReadContext): CT_Proxy {
  const start = requireStart(cur, 'CT_Proxy');
  let start_: ST_TrueFalseBlank | undefined;
  let end: ST_TrueFalseBlank | undefined;
  let idref: string | undefined;
  let connectloc: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'start': {
          const v = parseST_TrueFalseBlank(a.value);
          if (v !== undefined) {
            start_ = v;
            continue;
          }
          ctx.invalidValue('CT_Proxy', a, 'ST_TrueFalseBlank', cur);
          break;
        }
        case 'end': {
          const v = parseST_TrueFalseBlank(a.value);
          if (v !== undefined) {
            end = v;
            continue;
          }
          ctx.invalidValue('CT_Proxy', a, 'ST_TrueFalseBlank', cur);
          break;
        }
        case 'idref':
          idref = a.value as string;
          continue;

        case 'connectloc': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            connectloc = v;
            continue;
          }
          ctx.invalidValue('CT_Proxy', a, 'xsd:int', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Proxy', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Proxy', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Proxy', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Proxy', cur);
    cur.next();
  }
  return {
    start: start_,
    end,
    idref,
    connectloc,
    $unknownAttrs,
  };
}

/** Read a `CT_R`. See `transitional/vml-officeDrawing.xsd:143`. */
export function readCT_R(cur: XmlCursor, ctx: ReadContext): CT_R {
  const start = requireStart(cur, 'CT_R');
  let id: string | undefined;
  let type: ST_RType | undefined;
  let how: ST_How | undefined;
  let idref: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id':
          id = a.value as string;
          continue;

        case 'type': {
          const v = parseST_RType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_R', a, 'ST_RType', cur);
          break;
        }
        case 'how': {
          const v = parseST_How(a.value);
          if (v !== undefined) {
            how = v;
            continue;
          }
          ctx.invalidValue('CT_R', a, 'ST_How', cur);
          break;
        }
        case 'idref':
          idref = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_R', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_R', 'id', cur);

  const proxy: CT_Proxy[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_vml_office = ctx.uris['vml-office'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_R', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_vml_office) {
        switch (ev.localName) {
          case 'proxy': {
            proxy.push(readCT_Proxy(cur, ctx));
            $slot = 0;
            $index = proxy.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_R', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_R', cur);
    cur.next();
  }

  return {
    proxy,
    id,
    type,
    how,
    idref,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_RegroupTable`. See `transitional/vml-officeDrawing.xsd:127`. */
export function readCT_RegroupTable(cur: XmlCursor, ctx: ReadContext): CT_RegroupTable {
  const start = requireStart(cur, 'CT_RegroupTable');
  let ext: ST_Ext | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_RegroupTable', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_RegroupTable', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const entry: CT_Entry[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_vml_office = ctx.uris['vml-office'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RegroupTable', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_vml_office) {
        switch (ev.localName) {
          case 'entry': {
            entry.push(readCT_Entry(cur, ctx));
            $slot = 0;
            $index = entry.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RegroupTable', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RegroupTable', cur);
    cur.next();
  }

  return {
    entry,
    ext,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Relation`. See `transitional/vml-officeDrawing.xsd:188`. */
export function readCT_Relation(cur: XmlCursor, ctx: ReadContext): CT_Relation {
  const start = requireStart(cur, 'CT_Relation');
  let ext: ST_Ext | undefined;
  let idsrc: string | undefined;
  let iddest: string | undefined;
  let idcntr: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'idsrc':
          idsrc = a.value as string;
          continue;

        case 'iddest':
          iddest = a.value as string;
          continue;

        case 'idcntr':
          idcntr = a.value as string;
          continue;

      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_Relation', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Relation', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Relation', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Relation', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Relation', cur);
    cur.next();
  }
  return {
    ext,
    idsrc,
    iddest,
    idcntr,
    $unknownAttrs,
  };
}

/** Read a `CT_RelationTable`. See `transitional/vml-officeDrawing.xsd:182`. */
export function readCT_RelationTable(cur: XmlCursor, ctx: ReadContext): CT_RelationTable {
  const start = requireStart(cur, 'CT_RelationTable');
  let ext: ST_Ext | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_RelationTable', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_RelationTable', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const rel: CT_Relation[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_vml_office = ctx.uris['vml-office'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RelationTable', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_vml_office) {
        switch (ev.localName) {
          case 'rel': {
            rel.push(readCT_Relation(cur, ctx));
            $slot = 0;
            $index = rel.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RelationTable', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RelationTable', cur);
    cur.next();
  }

  return {
    rel,
    ext,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Rules`. See `transitional/vml-officeDrawing.xsd:137`. */
export function readCT_Rules(cur: XmlCursor, ctx: ReadContext): CT_Rules {
  const start = requireStart(cur, 'CT_Rules');
  let ext: ST_Ext | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_Rules', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Rules', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const r: CT_R[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_vml_office = ctx.uris['vml-office'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Rules', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_vml_office) {
        switch (ev.localName) {
          case 'r': {
            r.push(readCT_R(cur, ctx));
            $slot = 0;
            $index = r.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Rules', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Rules', cur);
    cur.next();
  }

  return {
    r,
    ext,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ShapeDefaults`. See `transitional/vml-officeDrawing.xsd:72`. */
export function readCT_ShapeDefaults(cur: XmlCursor, ctx: ReadContext): CT_ShapeDefaults {
  const start = requireStart(cur, 'CT_ShapeDefaults');
  let ext: ST_Ext | undefined;
  let spidmax: number | undefined;
  let style: string | undefined;
  let fillAttr: ST_TrueFalse | undefined;
  let fillcolor: ST_ColorType | undefined;
  let strokeAttr: ST_TrueFalse | undefined;
  let strokecolor: ST_ColorType | undefined;
  let allowincell: ST_TrueFalse | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  const NS_vml_office_A = ctx.uris['vml-office'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'spidmax': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            spidmax = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeDefaults', a, 'xsd:integer', cur);
          break;
        }
        case 'style':
          style = a.value as string;
          continue;

        case 'fill': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            fillAttr = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeDefaults', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'fillcolor': {
          const v = parseST_ColorType(a.value);
          if (v !== undefined) {
            fillcolor = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeDefaults', a, 'ST_ColorType', cur);
          break;
        }
        case 'stroke': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            strokeAttr = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeDefaults', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'strokecolor': {
          const v = parseST_ColorType(a.value);
          if (v !== undefined) {
            strokecolor = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeDefaults', a, 'ST_ColorType', cur);
          break;
        }
      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeDefaults', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    if (a.uri === NS_vml_office_A) {
      switch (a.localName) {
        case 'allowincell': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            allowincell = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeDefaults', a, 'ST_TrueFalse', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ShapeDefaults', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let fill: vml_CT_Fill | undefined;
  let stroke: CT_Stroke | undefined;
  let textbox: CT_Textbox | undefined;
  let shadow: CT_Shadow | undefined;
  let skew: CT_Skew | undefined;
  let extrusion: CT_Extrusion | undefined;
  let callout: CT_Callout | undefined;
  let lock: CT_Lock | undefined;
  let colormru: CT_ColorMru | undefined;
  let colormenu: CT_ColorMenu | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_vml = ctx.uris['vml'];
  const NS_vml_office = ctx.uris['vml-office'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ShapeDefaults', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_vml) {
        switch (ev.localName) {
          case 'fill': {
            fill = vml_readCT_Fill(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'stroke': {
            stroke = readCT_Stroke(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'textbox': {
            textbox = readCT_Textbox(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'shadow': {
            shadow = readCT_Shadow(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === NS_vml_office) {
        switch (ev.localName) {
          case 'skew': {
            skew = readCT_Skew(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'extrusion': {
            extrusion = readCT_Extrusion(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'callout': {
            callout = readCT_Callout(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'lock': {
            lock = readCT_Lock(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'colormru': {
            colormru = readCT_ColorMru(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'colormenu': {
            colormenu = readCT_ColorMenu(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ShapeDefaults', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ShapeDefaults', cur);
    cur.next();
  }

  return {
    fill,
    stroke,
    textbox,
    shadow,
    skew,
    extrusion,
    callout,
    lock,
    colormru,
    colormenu,
    ext,
    spidmax,
    style,
    fillAttr,
    fillcolor,
    strokeAttr,
    strokecolor,
    allowincell,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ShapeLayout`. See `transitional/vml-officeDrawing.xsd:115`. */
export function readCT_ShapeLayout(cur: XmlCursor, ctx: ReadContext): CT_ShapeLayout {
  const start = requireStart(cur, 'CT_ShapeLayout');
  let ext: ST_Ext | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeLayout', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ShapeLayout', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idmap: CT_IdMap | undefined;
  let regrouptable: CT_RegroupTable | undefined;
  let rules: CT_Rules | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_vml_office = ctx.uris['vml-office'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ShapeLayout', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_vml_office) {
        switch (ev.localName) {
          case 'idmap': {
            idmap = readCT_IdMap(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'regrouptable': {
            regrouptable = readCT_RegroupTable(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'rules': {
            rules = readCT_Rules(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ShapeLayout', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ShapeLayout', cur);
    cur.next();
  }

  return {
    idmap,
    regrouptable,
    rules,
    ext,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SignatureLine`. See `transitional/vml-officeDrawing.xsd:100`. */
export function readCT_SignatureLine(cur: XmlCursor, ctx: ReadContext): CT_SignatureLine {
  const start = requireStart(cur, 'CT_SignatureLine');
  let ext: ST_Ext | undefined;
  let issignatureline: ST_TrueFalse | undefined;
  let id: ST_Guid | undefined;
  let provid: ST_Guid | undefined;
  let signinginstructionsset: ST_TrueFalse | undefined;
  let allowcomments: ST_TrueFalse | undefined;
  let showsigndate: ST_TrueFalse | undefined;
  let suggestedsigner: string | undefined;
  let suggestedsigner2: string | undefined;
  let suggestedsigneremail: string | undefined;
  let signinginstructions: string | undefined;
  let addlxml: string | undefined;
  let sigprovurl: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  const NS_vml_office_A = ctx.uris['vml-office'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'issignatureline': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            issignatureline = v;
            continue;
          }
          ctx.invalidValue('CT_SignatureLine', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'id': {
          const v = parseST_Guid(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_SignatureLine', a, 'ST_Guid', cur);
          break;
        }
        case 'provid': {
          const v = parseST_Guid(a.value);
          if (v !== undefined) {
            provid = v;
            continue;
          }
          ctx.invalidValue('CT_SignatureLine', a, 'ST_Guid', cur);
          break;
        }
        case 'signinginstructionsset': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            signinginstructionsset = v;
            continue;
          }
          ctx.invalidValue('CT_SignatureLine', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'allowcomments': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            allowcomments = v;
            continue;
          }
          ctx.invalidValue('CT_SignatureLine', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'showsigndate': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            showsigndate = v;
            continue;
          }
          ctx.invalidValue('CT_SignatureLine', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'signinginstructions':
          signinginstructions = a.value as string;
          continue;

        case 'addlxml':
          addlxml = a.value as string;
          continue;

        case 'sigprovurl':
          sigprovurl = a.value as string;
          continue;

      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_SignatureLine', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    if (a.uri === NS_vml_office_A) {
      switch (a.localName) {
        case 'suggestedsigner':
          suggestedsigner = a.value as string;
          continue;

        case 'suggestedsigner2':
          suggestedsigner2 = a.value as string;
          continue;

        case 'suggestedsigneremail':
          suggestedsigneremail = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_SignatureLine', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SignatureLine', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SignatureLine', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SignatureLine', cur);
    cur.next();
  }
  return {
    ext,
    issignatureline,
    id,
    provid,
    signinginstructionsset,
    allowcomments,
    showsigndate,
    suggestedsigner,
    suggestedsigner2,
    suggestedsigneremail,
    signinginstructions,
    addlxml,
    sigprovurl,
    $unknownAttrs,
  };
}

/** Read a `CT_Skew`. See `transitional/vml-officeDrawing.xsd:218`. */
export function readCT_Skew(cur: XmlCursor, ctx: ReadContext): CT_Skew {
  const start = requireStart(cur, 'CT_Skew');
  let ext: ST_Ext | undefined;
  let id: string | undefined;
  let on: ST_TrueFalse | undefined;
  let offset: string | undefined;
  let origin: string | undefined;
  let matrix: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id':
          id = a.value as string;
          continue;

        case 'on': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            on = v;
            continue;
          }
          ctx.invalidValue('CT_Skew', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'offset':
          offset = a.value as string;
          continue;

        case 'origin':
          origin = a.value as string;
          continue;

        case 'matrix':
          matrix = a.value as string;
          continue;

      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_Skew', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Skew', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Skew', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Skew', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Skew', cur);
    cur.next();
  }
  return {
    ext,
    id,
    on,
    offset,
    origin,
    matrix,
    $unknownAttrs,
  };
}

/** Read a `CT_StrokeChild`. See `transitional/vml-officeDrawing.xsd:308`. */
export function readCT_StrokeChild(cur: XmlCursor, ctx: ReadContext): CT_StrokeChild {
  const start = requireStart(cur, 'CT_StrokeChild');
  let ext: ST_Ext | undefined;
  let on: ST_TrueFalse | undefined;
  let weight: string | undefined;
  let color: ST_ColorType | undefined;
  let color2: ST_ColorType | undefined;
  let opacity: string | undefined;
  let linestyle: ST_StrokeLineStyle | undefined;
  let miterlimit: number | undefined;
  let joinstyle: ST_StrokeJoinStyle | undefined;
  let endcap: ST_StrokeEndCap | undefined;
  let dashstyle: string | undefined;
  let insetpen: ST_TrueFalse | undefined;
  let filltype: vml_ST_FillType | undefined;
  let src: string | undefined;
  let imageaspect: ST_ImageAspect | undefined;
  let imagesize: string | undefined;
  let imagealignshape: ST_TrueFalse | undefined;
  let startarrow: ST_StrokeArrowType | undefined;
  let startarrowwidth: ST_StrokeArrowWidth | undefined;
  let startarrowlength: ST_StrokeArrowLength | undefined;
  let endarrow: ST_StrokeArrowType | undefined;
  let endarrowwidth: ST_StrokeArrowWidth | undefined;
  let endarrowlength: ST_StrokeArrowLength | undefined;
  let href: string | undefined;
  let althref: string | undefined;
  let title: string | undefined;
  let forcedash: ST_TrueFalse | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_vml_A = ctx.uris['vml'];
  const NS_vml_office_A = ctx.uris['vml-office'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'on': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            on = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'weight':
          weight = a.value as string;
          continue;

        case 'color': {
          const v = parseST_ColorType(a.value);
          if (v !== undefined) {
            color = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_ColorType', cur);
          break;
        }
        case 'color2': {
          const v = parseST_ColorType(a.value);
          if (v !== undefined) {
            color2 = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_ColorType', cur);
          break;
        }
        case 'opacity':
          opacity = a.value as string;
          continue;

        case 'linestyle': {
          const v = parseST_StrokeLineStyle(a.value);
          if (v !== undefined) {
            linestyle = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_StrokeLineStyle', cur);
          break;
        }
        case 'miterlimit': {
          const v = parseDecimal(a.value);
          if (v !== undefined) {
            miterlimit = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'xsd:decimal', cur);
          break;
        }
        case 'joinstyle': {
          const v = parseST_StrokeJoinStyle(a.value);
          if (v !== undefined) {
            joinstyle = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_StrokeJoinStyle', cur);
          break;
        }
        case 'endcap': {
          const v = parseST_StrokeEndCap(a.value);
          if (v !== undefined) {
            endcap = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_StrokeEndCap', cur);
          break;
        }
        case 'dashstyle':
          dashstyle = a.value as string;
          continue;

        case 'insetpen': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            insetpen = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'filltype': {
          const v = vml_parseST_FillType(a.value);
          if (v !== undefined) {
            filltype = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_FillType', cur);
          break;
        }
        case 'src':
          src = a.value as string;
          continue;

        case 'imageaspect': {
          const v = parseST_ImageAspect(a.value);
          if (v !== undefined) {
            imageaspect = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_ImageAspect', cur);
          break;
        }
        case 'imagesize':
          imagesize = a.value as string;
          continue;

        case 'imagealignshape': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            imagealignshape = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_TrueFalse', cur);
          break;
        }
        case 'startarrow': {
          const v = parseST_StrokeArrowType(a.value);
          if (v !== undefined) {
            startarrow = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_StrokeArrowType', cur);
          break;
        }
        case 'startarrowwidth': {
          const v = parseST_StrokeArrowWidth(a.value);
          if (v !== undefined) {
            startarrowwidth = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_StrokeArrowWidth', cur);
          break;
        }
        case 'startarrowlength': {
          const v = parseST_StrokeArrowLength(a.value);
          if (v !== undefined) {
            startarrowlength = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_StrokeArrowLength', cur);
          break;
        }
        case 'endarrow': {
          const v = parseST_StrokeArrowType(a.value);
          if (v !== undefined) {
            endarrow = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_StrokeArrowType', cur);
          break;
        }
        case 'endarrowwidth': {
          const v = parseST_StrokeArrowWidth(a.value);
          if (v !== undefined) {
            endarrowwidth = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_StrokeArrowWidth', cur);
          break;
        }
        case 'endarrowlength': {
          const v = parseST_StrokeArrowLength(a.value);
          if (v !== undefined) {
            endarrowlength = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_StrokeArrowLength', cur);
          break;
        }
      }
    }
    if (a.uri === NS_vml_A) {
      switch (a.localName) {
        case 'ext': {
          const v = parseST_Ext(a.value);
          if (v !== undefined) {
            ext = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_Ext', cur);
          break;
        }
      }
    }
    if (a.uri === NS_vml_office_A) {
      switch (a.localName) {
        case 'href':
          href = a.value as string;
          continue;

        case 'althref':
          althref = a.value as string;
          continue;

        case 'title':
          title = a.value as string;
          continue;

        case 'forcedash': {
          const v = parseST_TrueFalse(a.value);
          if (v !== undefined) {
            forcedash = v;
            continue;
          }
          ctx.invalidValue('CT_StrokeChild', a, 'ST_TrueFalse', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_StrokeChild', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StrokeChild', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_StrokeChild', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StrokeChild', cur);
    cur.next();
  }
  return {
    ext,
    on,
    weight,
    color,
    color2,
    opacity,
    linestyle,
    miterlimit,
    joinstyle,
    endcap,
    dashstyle,
    insetpen,
    filltype,
    src,
    imageaspect,
    imagesize,
    imagealignshape,
    startarrow,
    startarrowwidth,
    startarrowlength,
    endarrow,
    endarrowwidth,
    endarrowlength,
    href,
    althref,
    title,
    forcedash,
    $unknownAttrs,
  };
}
