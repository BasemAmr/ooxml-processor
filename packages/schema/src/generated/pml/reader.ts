/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the pml namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type RawNode,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  parseDouble,
  parseInteger,
  parseXsdBoolean,
  readScalar,
  requireStart,
} from '../../runtime/index.js';
import {
  parseST_Angle,
  parseST_BlackWhiteMode,
  parseST_Coordinate32,
  parseST_DrawingElementId,
  parseST_FixedPercentage,
  parseST_Percentage,
  parseST_PositiveCoordinate32,
  parseST_PositiveFixedPercentage,
  parseST_PositivePercentage,
  parseST_ShapeID,
  readCT_AnimationElementChoice,
  readCT_AnimationGraphicalObjectBuildProperties,
  readCT_AudioCD,
  readCT_AudioFile,
  readCT_BlipFillProperties,
  readCT_Color,
  readCT_ColorMRU,
  readCT_ColorMapping,
  readCT_ColorMappingOverride,
  readCT_EffectContainer,
  readCT_EffectList,
  readCT_EmbeddedWAVAudioFile,
  readCT_GradientFillProperties,
  readCT_GraphicalObject,
  readCT_GroupFillProperties,
  readCT_GroupShapeProperties,
  readCT_NoFillProperties,
  readCT_NonVisualConnectorProperties,
  readCT_NonVisualDrawingProps,
  readCT_NonVisualDrawingShapeProps,
  readCT_NonVisualGraphicFrameProperties,
  readCT_NonVisualGroupDrawingShapeProps,
  readCT_NonVisualPictureProperties,
  readCT_PatternFillProperties,
  readCT_Point2D,
  readCT_PositiveSize2D,
  readCT_QuickTimeFile,
  readCT_Scale2D,
  readCT_ShapeProperties,
  readCT_ShapeStyle,
  readCT_SolidColorFillProperties,
  readCT_StyleMatrixReference,
  readCT_TextBody,
  readCT_TextFont,
  readCT_TextListStyle,
  readCT_Transform2D,
  readCT_VideoFile,
} from '../dml-main/reader.js';
import type {
  CT_BlipFillProperties,
  CT_Color,
  CT_ColorMRU,
  CT_ColorMapping,
  CT_ColorMappingOverride,
  CT_EmbeddedWAVAudioFile,
  CT_GraphicalObject,
  CT_GroupShapeProperties,
  CT_NonVisualConnectorProperties,
  CT_NonVisualDrawingProps,
  CT_NonVisualDrawingShapeProps,
  CT_NonVisualGraphicFrameProperties,
  CT_NonVisualGroupDrawingShapeProps,
  CT_NonVisualPictureProperties,
  CT_Point2D,
  CT_PositiveSize2D,
  CT_Scale2D,
  CT_ShapeProperties,
  CT_ShapeStyle,
  CT_TextBody,
  CT_TextFont,
  CT_TextListStyle,
  CT_Transform2D,
  ST_Angle,
  ST_BlackWhiteMode,
  ST_Coordinate32,
  ST_DrawingElementId,
  ST_FixedPercentage,
  ST_Percentage,
  ST_PositiveCoordinate32,
  ST_PositiveFixedPercentage,
  ST_PositivePercentage,
  ST_ShapeID,
} from '../dml-main/types.js';
import { parseST_RelationshipId } from '../relationships/reader.js';
import type { ST_RelationshipId } from '../relationships/types.js';
import {
  parseST_AlgClass,
  parseST_AlgType,
  parseST_ConformanceClass,
  parseST_CryptProv,
} from '../shared-types/reader.js';
import type {
  ST_AlgClass,
  ST_AlgType,
  ST_ConformanceClass,
  ST_CryptProv,
} from '../shared-types/types.js';
import {
  type CT_ApplicationNonVisualDrawingProps,
  type CT_ApplicationNonVisualDrawingProps_Media,
  type CT_Background,
  type CT_BackgroundProperties,
  type CT_BackgroundProperties_EffectProperties,
  type CT_BackgroundProperties_FillProperties,
  type CT_Background_Background,
  type CT_BuildList,
  type CT_BuildList_Content,
  type CT_Comment,
  type CT_CommentAuthor,
  type CT_CommentAuthorList,
  type CT_CommentList,
  type CT_CommonSlideData,
  type CT_CommonSlideViewProperties,
  type CT_CommonViewProperties,
  type CT_Connector,
  type CT_ConnectorNonVisual,
  type CT_Control,
  type CT_ControlList,
  type CT_CornerDirectionTransition,
  type CT_CustomShow,
  type CT_CustomShowId,
  type CT_CustomShowList,
  type CT_CustomerData,
  type CT_CustomerDataList,
  type CT_EightDirectionTransition,
  type CT_EmbeddedFontDataId,
  type CT_EmbeddedFontList,
  type CT_EmbeddedFontListEntry,
  type CT_Empty,
  type CT_Extension,
  type CT_ExtensionList,
  type CT_ExtensionListModify,
  type CT_GraphicalObjectFrame,
  type CT_GraphicalObjectFrameNonVisual,
  type CT_GroupShape,
  type CT_GroupShapeNonVisual,
  type CT_GroupShape_Content,
  type CT_Guide,
  type CT_GuideList,
  type CT_HandoutMaster,
  type CT_HandoutMasterIdList,
  type CT_HandoutMasterIdListEntry,
  type CT_HeaderFooter,
  type CT_HtmlPublishProperties,
  type CT_HtmlPublishProperties_SlideListChoice,
  type CT_InOutTransition,
  type CT_IndexRange,
  type CT_Kinsoku,
  type CT_ModifyVerifier,
  type CT_NormalViewPortion,
  type CT_NormalViewProperties,
  type CT_NotesMaster,
  type CT_NotesMasterIdList,
  type CT_NotesMasterIdListEntry,
  type CT_NotesSlide,
  type CT_NotesTextViewProperties,
  type CT_NotesViewProperties,
  type CT_OleObject,
  type CT_OleObjectEmbed,
  type CT_OleObjectLink,
  type CT_OleObject_Content,
  type CT_OptionalBlackTransition,
  type CT_OrientationTransition,
  type CT_OutlineViewProperties,
  type CT_OutlineViewSlideEntry,
  type CT_OutlineViewSlideList,
  type CT_PhotoAlbum,
  type CT_Picture,
  type CT_PictureNonVisual,
  type CT_Placeholder,
  type CT_Presentation,
  type CT_PresentationProperties,
  type CT_PrintProperties,
  type CT_Rel,
  type CT_Shape,
  type CT_ShapeNonVisual,
  type CT_ShowInfoBrowse,
  type CT_ShowInfoKiosk,
  type CT_ShowProperties,
  type CT_ShowProperties_ShowType,
  type CT_ShowProperties_SlideListChoice,
  type CT_SideDirectionTransition,
  type CT_Slide,
  type CT_SlideIdList,
  type CT_SlideIdListEntry,
  type CT_SlideLayout,
  type CT_SlideLayoutIdList,
  type CT_SlideLayoutIdListEntry,
  type CT_SlideMaster,
  type CT_SlideMasterIdList,
  type CT_SlideMasterIdListEntry,
  type CT_SlideMasterTextStyles,
  type CT_SlideRelationshipList,
  type CT_SlideRelationshipListEntry,
  type CT_SlideSize,
  type CT_SlideSorterViewProperties,
  type CT_SlideSyncProperties,
  type CT_SlideTiming,
  type CT_SlideTransition,
  type CT_SlideTransition_Content,
  type CT_SlideViewProperties,
  type CT_SmartTags,
  type CT_SplitTransition,
  type CT_StringTag,
  type CT_TLAnimVariant,
  type CT_TLAnimVariantBooleanVal,
  type CT_TLAnimVariantFloatVal,
  type CT_TLAnimVariantIntegerVal,
  type CT_TLAnimVariantStringVal,
  type CT_TLAnimVariant_Content,
  type CT_TLAnimateBehavior,
  type CT_TLAnimateColorBehavior,
  type CT_TLAnimateEffectBehavior,
  type CT_TLAnimateMotionBehavior,
  type CT_TLAnimateRotationBehavior,
  type CT_TLAnimateScaleBehavior,
  type CT_TLBehaviorAttributeNameList,
  type CT_TLBuildDiagram,
  type CT_TLBuildParagraph,
  type CT_TLByAnimateColorTransform,
  type CT_TLByAnimateColorTransform_Content,
  type CT_TLByHslColorTransform,
  type CT_TLByRgbColorTransform,
  type CT_TLCommandBehavior,
  type CT_TLCommonBehaviorData,
  type CT_TLCommonMediaNodeData,
  type CT_TLCommonTimeNodeData,
  type CT_TLGraphicalObjectBuild,
  type CT_TLGraphicalObjectBuild_Content,
  type CT_TLIterateData,
  type CT_TLIterateData_Content,
  type CT_TLIterateIntervalPercentage,
  type CT_TLIterateIntervalTime,
  type CT_TLMediaNodeAudio,
  type CT_TLMediaNodeVideo,
  type CT_TLOleBuildChart,
  type CT_TLOleChartTargetElement,
  type CT_TLPoint,
  type CT_TLSetBehavior,
  type CT_TLShapeTargetElement,
  type CT_TLShapeTargetElement_Content,
  type CT_TLSubShapeId,
  type CT_TLTemplate,
  type CT_TLTemplateList,
  type CT_TLTextTargetElement,
  type CT_TLTextTargetElement_Content,
  type CT_TLTimeAnimateValue,
  type CT_TLTimeAnimateValueList,
  type CT_TLTimeCondition,
  type CT_TLTimeConditionList,
  type CT_TLTimeCondition_Content,
  type CT_TLTimeNodeExclusive,
  type CT_TLTimeNodeParallel,
  type CT_TLTimeNodeSequence,
  type CT_TLTimeTargetElement,
  type CT_TLTimeTargetElement_Content,
  type CT_TLTriggerRuntimeNode,
  type CT_TLTriggerTimeNodeID,
  type CT_TagList,
  type CT_TagsData,
  type CT_TimeNodeList,
  type CT_TimeNodeList_Content,
  type CT_TransitionSoundAction,
  type CT_TransitionSoundAction_Content,
  type CT_TransitionStartSoundAction,
  type CT_ViewProperties,
  type CT_WebProperties,
  type CT_WheelTransition,
  type ST_BookmarkIdSeed,
  type ST_Direction,
  ST_Direction_VALUES,
  type ST_Index,
  type ST_IterateType,
  ST_IterateType_VALUES,
  type ST_Name,
  type ST_OleObjectFollowColorScheme,
  ST_OleObjectFollowColorScheme_VALUES,
  type ST_PhotoAlbumFrameShape,
  ST_PhotoAlbumFrameShape_VALUES,
  type ST_PhotoAlbumLayout,
  ST_PhotoAlbumLayout_VALUES,
  type ST_PlaceholderSize,
  ST_PlaceholderSize_VALUES,
  type ST_PlaceholderType,
  ST_PlaceholderType_VALUES,
  type ST_PrintColorMode,
  ST_PrintColorMode_VALUES,
  type ST_PrintWhat,
  ST_PrintWhat_VALUES,
  type ST_SlideId,
  type ST_SlideLayoutId,
  type ST_SlideLayoutType,
  ST_SlideLayoutType_VALUES,
  type ST_SlideMasterId,
  type ST_SlideSizeCoordinate,
  type ST_SlideSizeType,
  ST_SlideSizeType_VALUES,
  type ST_SplitterBarState,
  ST_SplitterBarState_VALUES,
  type ST_TLAnimateBehaviorCalcMode,
  ST_TLAnimateBehaviorCalcMode_VALUES,
  type ST_TLAnimateBehaviorValueType,
  ST_TLAnimateBehaviorValueType_VALUES,
  type ST_TLAnimateColorDirection,
  ST_TLAnimateColorDirection_VALUES,
  type ST_TLAnimateColorSpace,
  ST_TLAnimateColorSpace_VALUES,
  type ST_TLAnimateEffectTransition,
  ST_TLAnimateEffectTransition_VALUES,
  type ST_TLAnimateMotionBehaviorOrigin,
  ST_TLAnimateMotionBehaviorOrigin_VALUES,
  type ST_TLAnimateMotionPathEditMode,
  ST_TLAnimateMotionPathEditMode_VALUES,
  type ST_TLBehaviorAccumulateType,
  ST_TLBehaviorAccumulateType_VALUES,
  type ST_TLBehaviorAdditiveType,
  ST_TLBehaviorAdditiveType_VALUES,
  type ST_TLBehaviorOverrideType,
  ST_TLBehaviorOverrideType_VALUES,
  type ST_TLBehaviorTransformType,
  ST_TLBehaviorTransformType_VALUES,
  type ST_TLChartSubelementType,
  ST_TLChartSubelementType_VALUES,
  type ST_TLCommandType,
  ST_TLCommandType_VALUES,
  type ST_TLDiagramBuildType,
  ST_TLDiagramBuildType_VALUES,
  type ST_TLNextActionType,
  ST_TLNextActionType_VALUES,
  type ST_TLOleChartBuildType,
  ST_TLOleChartBuildType_VALUES,
  type ST_TLParaBuildType,
  ST_TLParaBuildType_VALUES,
  type ST_TLPreviousActionType,
  ST_TLPreviousActionType_VALUES,
  type ST_TLTime,
  type ST_TLTimeAnimateValueTime,
  type ST_TLTimeIndefinite,
  ST_TLTimeIndefinite_VALUES,
  type ST_TLTimeNodeFillType,
  ST_TLTimeNodeFillType_VALUES,
  type ST_TLTimeNodeID,
  type ST_TLTimeNodeMasterRelation,
  ST_TLTimeNodeMasterRelation_VALUES,
  type ST_TLTimeNodePresetClassType,
  ST_TLTimeNodePresetClassType_VALUES,
  type ST_TLTimeNodeRestartType,
  ST_TLTimeNodeRestartType_VALUES,
  type ST_TLTimeNodeSyncType,
  ST_TLTimeNodeSyncType_VALUES,
  type ST_TLTimeNodeType,
  ST_TLTimeNodeType_VALUES,
  type ST_TLTriggerEvent,
  ST_TLTriggerEvent_VALUES,
  type ST_TLTriggerRuntimeNode,
  ST_TLTriggerRuntimeNode_VALUES,
  type ST_TransitionCornerDirectionType,
  ST_TransitionCornerDirectionType_VALUES,
  type ST_TransitionEightDirectionType,
  type ST_TransitionInOutDirectionType,
  ST_TransitionInOutDirectionType_VALUES,
  type ST_TransitionSideDirectionType,
  ST_TransitionSideDirectionType_VALUES,
  type ST_TransitionSpeed,
  ST_TransitionSpeed_VALUES,
  type ST_ViewType,
  ST_ViewType_VALUES,
  type ST_WebColorType,
  ST_WebColorType_VALUES,
  type ST_WebEncoding,
  type ST_WebScreenSize,
  ST_WebScreenSize_VALUES,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_BookmarkIdSeed(value: string): ST_BookmarkIdSeed | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_BookmarkIdSeed);
}

export function parseST_Direction(value: string): ST_Direction | undefined {
  const s = collapse(value);
  return ST_Direction_VALUES.has(s) ? (s as ST_Direction) : undefined;
}

export function parseST_Index(value: string): ST_Index | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_Index);
}

export function parseST_IterateType(value: string): ST_IterateType | undefined {
  const s = collapse(value);
  return ST_IterateType_VALUES.has(s) ? (s as ST_IterateType) : undefined;
}

export function parseST_Name(value: string): ST_Name | undefined {
  return value as ST_Name;
}

export function parseST_OleObjectFollowColorScheme(value: string): ST_OleObjectFollowColorScheme | undefined {
  const s = collapse(value);
  return ST_OleObjectFollowColorScheme_VALUES.has(s) ? (s as ST_OleObjectFollowColorScheme) : undefined;
}

export function parseST_PhotoAlbumFrameShape(value: string): ST_PhotoAlbumFrameShape | undefined {
  const s = collapse(value);
  return ST_PhotoAlbumFrameShape_VALUES.has(s) ? (s as ST_PhotoAlbumFrameShape) : undefined;
}

export function parseST_PhotoAlbumLayout(value: string): ST_PhotoAlbumLayout | undefined {
  const s = collapse(value);
  return ST_PhotoAlbumLayout_VALUES.has(s) ? (s as ST_PhotoAlbumLayout) : undefined;
}

export function parseST_PlaceholderSize(value: string): ST_PlaceholderSize | undefined {
  const s = collapse(value);
  return ST_PlaceholderSize_VALUES.has(s) ? (s as ST_PlaceholderSize) : undefined;
}

export function parseST_PlaceholderType(value: string): ST_PlaceholderType | undefined {
  const s = collapse(value);
  return ST_PlaceholderType_VALUES.has(s) ? (s as ST_PlaceholderType) : undefined;
}

export function parseST_PrintColorMode(value: string): ST_PrintColorMode | undefined {
  const s = collapse(value);
  return ST_PrintColorMode_VALUES.has(s) ? (s as ST_PrintColorMode) : undefined;
}

export function parseST_PrintWhat(value: string): ST_PrintWhat | undefined {
  const s = collapse(value);
  return ST_PrintWhat_VALUES.has(s) ? (s as ST_PrintWhat) : undefined;
}

export function parseST_SlideId(value: string): ST_SlideId | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_SlideId);
}

export function parseST_SlideLayoutId(value: string): ST_SlideLayoutId | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_SlideLayoutId);
}

export function parseST_SlideLayoutType(value: string): ST_SlideLayoutType | undefined {
  const s = collapse(value);
  return ST_SlideLayoutType_VALUES.has(s) ? (s as ST_SlideLayoutType) : undefined;
}

export function parseST_SlideMasterId(value: string): ST_SlideMasterId | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_SlideMasterId);
}

export function parseST_SlideSizeCoordinate(value: string): ST_SlideSizeCoordinate | undefined {
  return value as ST_SlideSizeCoordinate;
}

export function parseST_SlideSizeType(value: string): ST_SlideSizeType | undefined {
  const s = collapse(value);
  return ST_SlideSizeType_VALUES.has(s) ? (s as ST_SlideSizeType) : undefined;
}

export function parseST_SplitterBarState(value: string): ST_SplitterBarState | undefined {
  const s = collapse(value);
  return ST_SplitterBarState_VALUES.has(s) ? (s as ST_SplitterBarState) : undefined;
}

export function parseST_TLAnimateBehaviorCalcMode(value: string): ST_TLAnimateBehaviorCalcMode | undefined {
  const s = collapse(value);
  return ST_TLAnimateBehaviorCalcMode_VALUES.has(s) ? (s as ST_TLAnimateBehaviorCalcMode) : undefined;
}

export function parseST_TLAnimateBehaviorValueType(value: string): ST_TLAnimateBehaviorValueType | undefined {
  const s = collapse(value);
  return ST_TLAnimateBehaviorValueType_VALUES.has(s) ? (s as ST_TLAnimateBehaviorValueType) : undefined;
}

export function parseST_TLAnimateColorDirection(value: string): ST_TLAnimateColorDirection | undefined {
  const s = collapse(value);
  return ST_TLAnimateColorDirection_VALUES.has(s) ? (s as ST_TLAnimateColorDirection) : undefined;
}

export function parseST_TLAnimateColorSpace(value: string): ST_TLAnimateColorSpace | undefined {
  const s = collapse(value);
  return ST_TLAnimateColorSpace_VALUES.has(s) ? (s as ST_TLAnimateColorSpace) : undefined;
}

export function parseST_TLAnimateEffectTransition(value: string): ST_TLAnimateEffectTransition | undefined {
  const s = collapse(value);
  return ST_TLAnimateEffectTransition_VALUES.has(s) ? (s as ST_TLAnimateEffectTransition) : undefined;
}

export function parseST_TLAnimateMotionBehaviorOrigin(value: string): ST_TLAnimateMotionBehaviorOrigin | undefined {
  const s = collapse(value);
  return ST_TLAnimateMotionBehaviorOrigin_VALUES.has(s) ? (s as ST_TLAnimateMotionBehaviorOrigin) : undefined;
}

export function parseST_TLAnimateMotionPathEditMode(value: string): ST_TLAnimateMotionPathEditMode | undefined {
  const s = collapse(value);
  return ST_TLAnimateMotionPathEditMode_VALUES.has(s) ? (s as ST_TLAnimateMotionPathEditMode) : undefined;
}

export function parseST_TLBehaviorAccumulateType(value: string): ST_TLBehaviorAccumulateType | undefined {
  const s = collapse(value);
  return ST_TLBehaviorAccumulateType_VALUES.has(s) ? (s as ST_TLBehaviorAccumulateType) : undefined;
}

export function parseST_TLBehaviorAdditiveType(value: string): ST_TLBehaviorAdditiveType | undefined {
  const s = collapse(value);
  return ST_TLBehaviorAdditiveType_VALUES.has(s) ? (s as ST_TLBehaviorAdditiveType) : undefined;
}

export function parseST_TLBehaviorOverrideType(value: string): ST_TLBehaviorOverrideType | undefined {
  const s = collapse(value);
  return ST_TLBehaviorOverrideType_VALUES.has(s) ? (s as ST_TLBehaviorOverrideType) : undefined;
}

export function parseST_TLBehaviorTransformType(value: string): ST_TLBehaviorTransformType | undefined {
  const s = collapse(value);
  return ST_TLBehaviorTransformType_VALUES.has(s) ? (s as ST_TLBehaviorTransformType) : undefined;
}

export function parseST_TLChartSubelementType(value: string): ST_TLChartSubelementType | undefined {
  const s = collapse(value);
  return ST_TLChartSubelementType_VALUES.has(s) ? (s as ST_TLChartSubelementType) : undefined;
}

export function parseST_TLCommandType(value: string): ST_TLCommandType | undefined {
  const s = collapse(value);
  return ST_TLCommandType_VALUES.has(s) ? (s as ST_TLCommandType) : undefined;
}

export function parseST_TLDiagramBuildType(value: string): ST_TLDiagramBuildType | undefined {
  const s = collapse(value);
  return ST_TLDiagramBuildType_VALUES.has(s) ? (s as ST_TLDiagramBuildType) : undefined;
}

export function parseST_TLNextActionType(value: string): ST_TLNextActionType | undefined {
  const s = collapse(value);
  return ST_TLNextActionType_VALUES.has(s) ? (s as ST_TLNextActionType) : undefined;
}

export function parseST_TLOleChartBuildType(value: string): ST_TLOleChartBuildType | undefined {
  const s = collapse(value);
  return ST_TLOleChartBuildType_VALUES.has(s) ? (s as ST_TLOleChartBuildType) : undefined;
}

export function parseST_TLParaBuildType(value: string): ST_TLParaBuildType | undefined {
  const s = collapse(value);
  return ST_TLParaBuildType_VALUES.has(s) ? (s as ST_TLParaBuildType) : undefined;
}

export function parseST_TLPreviousActionType(value: string): ST_TLPreviousActionType | undefined {
  const s = collapse(value);
  return ST_TLPreviousActionType_VALUES.has(s) ? (s as ST_TLPreviousActionType) : undefined;
}

export function parseST_TLTime(value: string): ST_TLTime | undefined {
  return (parseInteger(value) ?? parseST_TLTimeIndefinite(value)) as ST_TLTime | undefined;
}

export function parseST_TLTimeAnimateValueTime(value: string): ST_TLTimeAnimateValueTime | undefined {
  return (parseST_PositiveFixedPercentage(value) ?? parseST_TLTimeIndefinite(value)) as ST_TLTimeAnimateValueTime | undefined;
}

export function parseST_TLTimeIndefinite(value: string): ST_TLTimeIndefinite | undefined {
  const s = collapse(value);
  return ST_TLTimeIndefinite_VALUES.has(s) ? (s as ST_TLTimeIndefinite) : undefined;
}

export function parseST_TLTimeNodeFillType(value: string): ST_TLTimeNodeFillType | undefined {
  const s = collapse(value);
  return ST_TLTimeNodeFillType_VALUES.has(s) ? (s as ST_TLTimeNodeFillType) : undefined;
}

export function parseST_TLTimeNodeID(value: string): ST_TLTimeNodeID | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_TLTimeNodeID);
}

export function parseST_TLTimeNodeMasterRelation(value: string): ST_TLTimeNodeMasterRelation | undefined {
  const s = collapse(value);
  return ST_TLTimeNodeMasterRelation_VALUES.has(s) ? (s as ST_TLTimeNodeMasterRelation) : undefined;
}

export function parseST_TLTimeNodePresetClassType(value: string): ST_TLTimeNodePresetClassType | undefined {
  const s = collapse(value);
  return ST_TLTimeNodePresetClassType_VALUES.has(s) ? (s as ST_TLTimeNodePresetClassType) : undefined;
}

export function parseST_TLTimeNodeRestartType(value: string): ST_TLTimeNodeRestartType | undefined {
  const s = collapse(value);
  return ST_TLTimeNodeRestartType_VALUES.has(s) ? (s as ST_TLTimeNodeRestartType) : undefined;
}

export function parseST_TLTimeNodeSyncType(value: string): ST_TLTimeNodeSyncType | undefined {
  const s = collapse(value);
  return ST_TLTimeNodeSyncType_VALUES.has(s) ? (s as ST_TLTimeNodeSyncType) : undefined;
}

export function parseST_TLTimeNodeType(value: string): ST_TLTimeNodeType | undefined {
  const s = collapse(value);
  return ST_TLTimeNodeType_VALUES.has(s) ? (s as ST_TLTimeNodeType) : undefined;
}

export function parseST_TLTriggerEvent(value: string): ST_TLTriggerEvent | undefined {
  const s = collapse(value);
  return ST_TLTriggerEvent_VALUES.has(s) ? (s as ST_TLTriggerEvent) : undefined;
}

export function parseST_TLTriggerRuntimeNode(value: string): ST_TLTriggerRuntimeNode | undefined {
  const s = collapse(value);
  return ST_TLTriggerRuntimeNode_VALUES.has(s) ? (s as ST_TLTriggerRuntimeNode) : undefined;
}

export function parseST_TransitionCornerDirectionType(value: string): ST_TransitionCornerDirectionType | undefined {
  const s = collapse(value);
  return ST_TransitionCornerDirectionType_VALUES.has(s) ? (s as ST_TransitionCornerDirectionType) : undefined;
}

export function parseST_TransitionEightDirectionType(value: string): ST_TransitionEightDirectionType | undefined {
  return (parseST_TransitionSideDirectionType(value) ?? parseST_TransitionCornerDirectionType(value)) as ST_TransitionEightDirectionType | undefined;
}

export function parseST_TransitionInOutDirectionType(value: string): ST_TransitionInOutDirectionType | undefined {
  const s = collapse(value);
  return ST_TransitionInOutDirectionType_VALUES.has(s) ? (s as ST_TransitionInOutDirectionType) : undefined;
}

export function parseST_TransitionSideDirectionType(value: string): ST_TransitionSideDirectionType | undefined {
  const s = collapse(value);
  return ST_TransitionSideDirectionType_VALUES.has(s) ? (s as ST_TransitionSideDirectionType) : undefined;
}

export function parseST_TransitionSpeed(value: string): ST_TransitionSpeed | undefined {
  const s = collapse(value);
  return ST_TransitionSpeed_VALUES.has(s) ? (s as ST_TransitionSpeed) : undefined;
}

export function parseST_ViewType(value: string): ST_ViewType | undefined {
  const s = collapse(value);
  return ST_ViewType_VALUES.has(s) ? (s as ST_ViewType) : undefined;
}

export function parseST_WebColorType(value: string): ST_WebColorType | undefined {
  const s = collapse(value);
  return ST_WebColorType_VALUES.has(s) ? (s as ST_WebColorType) : undefined;
}

export function parseST_WebEncoding(value: string): ST_WebEncoding | undefined {
  return value as ST_WebEncoding;
}

export function parseST_WebScreenSize(value: string): ST_WebScreenSize | undefined {
  const s = collapse(value);
  return ST_WebScreenSize_VALUES.has(s) ? (s as ST_WebScreenSize) : undefined;
}

// Complex-type readers.

/** Read a `CT_ApplicationNonVisualDrawingProps`. See `transitional/pml.xsd:1250`. */
export function readCT_ApplicationNonVisualDrawingProps(cur: XmlCursor, ctx: ReadContext): CT_ApplicationNonVisualDrawingProps {
  const start = requireStart(cur, 'CT_ApplicationNonVisualDrawingProps');
  let isPhoto: boolean | undefined;
  let userDrawn: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'isPhoto': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            isPhoto = v;
            continue;
          }
          ctx.invalidValue('CT_ApplicationNonVisualDrawingProps', a, 'xsd:boolean', cur);
          break;
        }
        case 'userDrawn': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            userDrawn = v;
            continue;
          }
          ctx.invalidValue('CT_ApplicationNonVisualDrawingProps', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ApplicationNonVisualDrawingProps', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ph: CT_Placeholder | undefined;
  let media: CT_ApplicationNonVisualDrawingProps_Media | undefined;
  let custDataLst: CT_CustomerDataList | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ApplicationNonVisualDrawingProps', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'audioCd': {
            media = { kind: 'audioCd', value: readCT_AudioCD(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'wavAudioFile': {
            media = { kind: 'wavAudioFile', value: readCT_EmbeddedWAVAudioFile(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'audioFile': {
            media = { kind: 'audioFile', value: readCT_AudioFile(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'videoFile': {
            media = { kind: 'videoFile', value: readCT_VideoFile(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'quickTimeFile': {
            media = { kind: 'quickTimeFile', value: readCT_QuickTimeFile(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'ph': {
            ph = readCT_Placeholder(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'custDataLst': {
            custDataLst = readCT_CustomerDataList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ApplicationNonVisualDrawingProps', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ApplicationNonVisualDrawingProps', cur);
    cur.next();
  }

  return {
    ph,
    media,
    custDataLst,
    extLst,
    isPhoto,
    userDrawn,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Background`. See `transitional/pml.xsd:1387`. */
export function readCT_Background(cur: XmlCursor, ctx: ReadContext): CT_Background {
  const start = requireStart(cur, 'CT_Background');
  let bwMode: ST_BlackWhiteMode | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'bwMode': {
          const v = parseST_BlackWhiteMode(a.value);
          if (v !== undefined) {
            bwMode = v;
            continue;
          }
          ctx.invalidValue('CT_Background', a, 'ST_BlackWhiteMode', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Background', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let background: CT_Background_Background | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Background', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'bgPr': {
            background = { kind: 'bgPr', value: readCT_BackgroundProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'bgRef': {
            background = { kind: 'bgRef', value: readCT_StyleMatrixReference(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Background', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Background', cur);
    cur.next();
  }

  return {
    background,
    bwMode,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BackgroundProperties`. See `transitional/pml.xsd:1373`. */
export function readCT_BackgroundProperties(cur: XmlCursor, ctx: ReadContext): CT_BackgroundProperties {
  const start = requireStart(cur, 'CT_BackgroundProperties');
  let shadeToTitle: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'shadeToTitle': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            shadeToTitle = v;
            continue;
          }
          ctx.invalidValue('CT_BackgroundProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BackgroundProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let fillProperties: CT_BackgroundProperties_FillProperties | undefined;
  let effectProperties: CT_BackgroundProperties_EffectProperties | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BackgroundProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'noFill': {
            fillProperties = { kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'solidFill': {
            fillProperties = { kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'gradFill': {
            fillProperties = { kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            fillProperties = { kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pattFill': {
            fillProperties = { kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'grpFill': {
            fillProperties = { kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'effectLst': {
            effectProperties = { kind: 'effectLst', value: readCT_EffectList(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'effectDag': {
            effectProperties = { kind: 'effectDag', value: readCT_EffectContainer(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BackgroundProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BackgroundProperties', cur);
    cur.next();
  }

  return {
    fillProperties,
    effectProperties,
    extLst,
    shadeToTitle,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BuildList`. See `transitional/pml.xsd:698`. */
export function readCT_BuildList(cur: XmlCursor, ctx: ReadContext): CT_BuildList {
  const start = requireStart(cur, 'CT_BuildList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BuildList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_BuildList_Content[] = [];

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BuildList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'bldP': {
            content.push({ kind: 'bldP', value: readCT_TLBuildParagraph(cur, ctx) });
            continue;
          }
          case 'bldDgm': {
            content.push({ kind: 'bldDgm', value: readCT_TLBuildDiagram(cur, ctx) });
            continue;
          }
          case 'bldOleChart': {
            content.push({ kind: 'bldOleChart', value: readCT_TLOleBuildChart(cur, ctx) });
            continue;
          }
          case 'bldGraphic': {
            content.push({ kind: 'bldGraphic', value: readCT_TLGraphicalObjectBuild(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BuildList', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BuildList', cur);
    cur.next();
  }

  return {
    content,
    $unknownAttrs,
  };
}

/** Read a `CT_Comment`. See `transitional/pml.xsd:799`. */
export function readCT_Comment(cur: XmlCursor, ctx: ReadContext): CT_Comment {
  const start = requireStart(cur, 'CT_Comment');
  let authorId: number | undefined;
  let dt: string | undefined;
  let idx: ST_Index | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'authorId': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            authorId = v;
            continue;
          }
          ctx.invalidValue('CT_Comment', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'dt':
          dt = a.value as string;
          continue;

        case 'idx': {
          const v = parseST_Index(a.value);
          if (v !== undefined) {
            idx = v;
            continue;
          }
          ctx.invalidValue('CT_Comment', a, 'ST_Index', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Comment', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (authorId === undefined) ctx.missingRequired('CT_Comment', 'authorId', cur);
  if (idx === undefined) ctx.missingRequired('CT_Comment', 'idx', cur);

  let pos: CT_Point2D | undefined;
  let text: string | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Comment', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'pos': {
            pos = readCT_Point2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'text': {
            text = readScalar(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Comment', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Comment', cur);
    cur.next();
  }

  return {
    pos,
    text,
    extLst,
    authorId,
    dt,
    idx,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CommentAuthor`. See `transitional/pml.xsd:783`. */
export function readCT_CommentAuthor(cur: XmlCursor, ctx: ReadContext): CT_CommentAuthor {
  const start = requireStart(cur, 'CT_CommentAuthor');
  let id: number | undefined;
  let name: ST_Name | undefined;
  let initials: ST_Name | undefined;
  let lastIdx: number | undefined;
  let clrIdx: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_CommentAuthor', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'name': {
          const v = parseST_Name(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_CommentAuthor', a, 'ST_Name', cur);
          break;
        }
        case 'initials': {
          const v = parseST_Name(a.value);
          if (v !== undefined) {
            initials = v;
            continue;
          }
          ctx.invalidValue('CT_CommentAuthor', a, 'ST_Name', cur);
          break;
        }
        case 'lastIdx': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            lastIdx = v;
            continue;
          }
          ctx.invalidValue('CT_CommentAuthor', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'clrIdx': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            clrIdx = v;
            continue;
          }
          ctx.invalidValue('CT_CommentAuthor', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CommentAuthor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_CommentAuthor', 'id', cur);
  if (name === undefined) ctx.missingRequired('CT_CommentAuthor', 'name', cur);
  if (initials === undefined) ctx.missingRequired('CT_CommentAuthor', 'initials', cur);
  if (lastIdx === undefined) ctx.missingRequired('CT_CommentAuthor', 'lastIdx', cur);
  if (clrIdx === undefined) ctx.missingRequired('CT_CommentAuthor', 'clrIdx', cur);

  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CommentAuthor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CommentAuthor', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CommentAuthor', cur);
    cur.next();
  }

  return {
    extLst,
    id,
    name,
    initials,
    lastIdx,
    clrIdx,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CommentAuthorList`. See `transitional/pml.xsd:793`. */
export function readCT_CommentAuthorList(cur: XmlCursor, ctx: ReadContext): CT_CommentAuthorList {
  const start = requireStart(cur, 'CT_CommentAuthorList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_CommentAuthorList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const cmAuthor: CT_CommentAuthor[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CommentAuthorList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cmAuthor': {
            cmAuthor.push(readCT_CommentAuthor(cur, ctx));
            $slot = 0;
            $index = cmAuthor.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CommentAuthorList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CommentAuthorList', cur);
    cur.next();
  }

  return {
    cmAuthor,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CommentList`. See `transitional/pml.xsd:809`. */
export function readCT_CommentList(cur: XmlCursor, ctx: ReadContext): CT_CommentList {
  const start = requireStart(cur, 'CT_CommentList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_CommentList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const cm: CT_Comment[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CommentList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cm': {
            cm.push(readCT_Comment(cur, ctx));
            $slot = 0;
            $index = cm.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CommentList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CommentList', cur);
    cur.next();
  }

  return {
    cm,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CommonSlideData`. See `transitional/pml.xsd:1393`. */
export function readCT_CommonSlideData(cur: XmlCursor, ctx: ReadContext): CT_CommonSlideData {
  const start = requireStart(cur, 'CT_CommonSlideData');
  let name: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_CommonSlideData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let bg: CT_Background | undefined;
  let spTree: CT_GroupShape | undefined;
  let custDataLst: CT_CustomerDataList | undefined;
  let controls: CT_ControlList | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CommonSlideData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'bg': {
            bg = readCT_Background(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spTree': {
            spTree = readCT_GroupShape(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'custDataLst': {
            custDataLst = readCT_CustomerDataList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'controls': {
            controls = readCT_ControlList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CommonSlideData', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CommonSlideData', cur);
    cur.next();
  }

  return {
    bg,
    spTree,
    custDataLst,
    controls,
    extLst,
    name,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CommonSlideViewProperties`. See `transitional/pml.xsd:1638`. */
export function readCT_CommonSlideViewProperties(cur: XmlCursor, ctx: ReadContext): CT_CommonSlideViewProperties {
  const start = requireStart(cur, 'CT_CommonSlideViewProperties');
  let snapToGrid: boolean | undefined;
  let snapToObjects: boolean | undefined;
  let showGuides: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'snapToGrid': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            snapToGrid = v;
            continue;
          }
          ctx.invalidValue('CT_CommonSlideViewProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'snapToObjects': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            snapToObjects = v;
            continue;
          }
          ctx.invalidValue('CT_CommonSlideViewProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'showGuides': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showGuides = v;
            continue;
          }
          ctx.invalidValue('CT_CommonSlideViewProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CommonSlideViewProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cViewPr: CT_CommonViewProperties | undefined;
  let guideLst: CT_GuideList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CommonSlideViewProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cViewPr': {
            cViewPr = readCT_CommonViewProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'guideLst': {
            guideLst = readCT_GuideList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CommonSlideViewProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CommonSlideViewProperties', cur);
    cur.next();
  }

  return {
    cViewPr,
    guideLst,
    snapToGrid,
    snapToObjects,
    showGuides,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CommonViewProperties`. See `transitional/pml.xsd:1593`. */
export function readCT_CommonViewProperties(cur: XmlCursor, ctx: ReadContext): CT_CommonViewProperties {
  const start = requireStart(cur, 'CT_CommonViewProperties');
  let varScale: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'varScale': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            varScale = v;
            continue;
          }
          ctx.invalidValue('CT_CommonViewProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CommonViewProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let scale: CT_Scale2D | undefined;
  let origin: CT_Point2D | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CommonViewProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'scale': {
            scale = readCT_Scale2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'origin': {
            origin = readCT_Point2D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CommonViewProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CommonViewProperties', cur);
    cur.next();
  }

  return {
    scale,
    origin,
    varScale,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Connector`. See `transitional/pml.xsd:1287`. */
export function readCT_Connector(cur: XmlCursor, ctx: ReadContext): CT_Connector {
  const start = requireStart(cur, 'CT_Connector');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Connector', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvCxnSpPr: CT_ConnectorNonVisual | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let style: CT_ShapeStyle | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Connector', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'nvCxnSpPr': {
            nvCxnSpPr = readCT_ConnectorNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Connector', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Connector', cur);
    cur.next();
  }

  return {
    nvCxnSpPr,
    spPr,
    style,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ConnectorNonVisual`. See `transitional/pml.xsd:1278`. */
export function readCT_ConnectorNonVisual(cur: XmlCursor, ctx: ReadContext): CT_ConnectorNonVisual {
  const start = requireStart(cur, 'CT_ConnectorNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ConnectorNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvCxnSpPr: CT_NonVisualConnectorProperties | undefined;
  let nvPr: CT_ApplicationNonVisualDrawingProps | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ConnectorNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvCxnSpPr': {
            cNvCxnSpPr = readCT_NonVisualConnectorProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'nvPr': {
            nvPr = readCT_ApplicationNonVisualDrawingProps(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ConnectorNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ConnectorNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvCxnSpPr,
    nvPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Control`. See `transitional/pml.xsd:855`. */
export function readCT_Control(cur: XmlCursor, ctx: ReadContext): CT_Control {
  const start = requireStart(cur, 'CT_Control');
  let spid: ST_ShapeID | undefined;
  let name: string | undefined;
  let showAsIcon: boolean | undefined;
  let id: ST_RelationshipId | undefined;
  let imgW: ST_PositiveCoordinate32 | undefined;
  let imgH: ST_PositiveCoordinate32 | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'spid': {
          const v = parseST_ShapeID(a.value);
          if (v !== undefined) {
            spid = v;
            continue;
          }
          ctx.invalidValue('CT_Control', a, 'ST_ShapeID', cur);
          break;
        }
        case 'name':
          name = a.value as string;
          continue;

        case 'showAsIcon': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showAsIcon = v;
            continue;
          }
          ctx.invalidValue('CT_Control', a, 'xsd:boolean', cur);
          break;
        }
        case 'imgW': {
          const v = parseST_PositiveCoordinate32(a.value);
          if (v !== undefined) {
            imgW = v;
            continue;
          }
          ctx.invalidValue('CT_Control', a, 'ST_PositiveCoordinate32', cur);
          break;
        }
        case 'imgH': {
          const v = parseST_PositiveCoordinate32(a.value);
          if (v !== undefined) {
            imgH = v;
            continue;
          }
          ctx.invalidValue('CT_Control', a, 'ST_PositiveCoordinate32', cur);
          break;
        }
      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Control', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Control', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_ExtensionList | undefined;
  let pic: CT_Picture | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Control', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pic': {
            pic = readCT_Picture(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Control', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Control', cur);
    cur.next();
  }

  return {
    extLst,
    pic,
    spid,
    name,
    showAsIcon,
    id,
    imgW,
    imgH,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ControlList`. See `transitional/pml.xsd:862`. */
export function readCT_ControlList(cur: XmlCursor, ctx: ReadContext): CT_ControlList {
  const start = requireStart(cur, 'CT_ControlList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ControlList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const control: CT_Control[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ControlList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'control': {
            control.push(readCT_Control(cur, ctx));
            $slot = 0;
            $index = control.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ControlList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ControlList', cur);
    cur.next();
  }

  return {
    control,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CornerDirectionTransition`. See `transitional/pml.xsd:41`. */
export function readCT_CornerDirectionTransition(cur: XmlCursor, ctx: ReadContext): CT_CornerDirectionTransition {
  const start = requireStart(cur, 'CT_CornerDirectionTransition');
  let dir: ST_TransitionCornerDirectionType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'dir': {
          const v = parseST_TransitionCornerDirectionType(a.value);
          if (v !== undefined) {
            dir = v;
            continue;
          }
          ctx.invalidValue('CT_CornerDirectionTransition', a, 'ST_TransitionCornerDirectionType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CornerDirectionTransition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CornerDirectionTransition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_CornerDirectionTransition', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CornerDirectionTransition', cur);
    cur.next();
  }
  return {
    dir,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomShow`. See `transitional/pml.xsd:948`. */
export function readCT_CustomShow(cur: XmlCursor, ctx: ReadContext): CT_CustomShow {
  const start = requireStart(cur, 'CT_CustomShow');
  let name: ST_Name | undefined;
  let id: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name': {
          const v = parseST_Name(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_CustomShow', a, 'ST_Name', cur);
          break;
        }
        case 'id': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_CustomShow', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CustomShow', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_CustomShow', 'name', cur);
  if (id === undefined) ctx.missingRequired('CT_CustomShow', 'id', cur);

  let sldLst: CT_SlideRelationshipList | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomShow', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'sldLst': {
            sldLst = readCT_SlideRelationshipList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CustomShow', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomShow', cur);
    cur.next();
  }

  return {
    sldLst,
    extLst,
    name,
    id,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomShowId`. See `transitional/pml.xsd:739`. */
export function readCT_CustomShowId(cur: XmlCursor, ctx: ReadContext): CT_CustomShowId {
  const start = requireStart(cur, 'CT_CustomShowId');
  let id: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_CustomShowId', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CustomShowId', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_CustomShowId', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomShowId', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_CustomShowId', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomShowId', cur);
    cur.next();
  }
  return {
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomShowList`. See `transitional/pml.xsd:956`. */
export function readCT_CustomShowList(cur: XmlCursor, ctx: ReadContext): CT_CustomShowList {
  const start = requireStart(cur, 'CT_CustomShowList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_CustomShowList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const custShow: CT_CustomShow[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomShowList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'custShow': {
            custShow.push(readCT_CustomShow(cur, ctx));
            $slot = 0;
            $index = custShow.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CustomShowList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomShowList', cur);
    cur.next();
  }

  return {
    custShow,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomerData`. See `transitional/pml.xsd:749`. */
export function readCT_CustomerData(cur: XmlCursor, ctx: ReadContext): CT_CustomerData {
  const start = requireStart(cur, 'CT_CustomerData');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_CustomerData', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CustomerData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_CustomerData', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomerData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_CustomerData', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomerData', cur);
    cur.next();
  }
  return {
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomerDataList`. See `transitional/pml.xsd:755`. */
export function readCT_CustomerDataList(cur: XmlCursor, ctx: ReadContext): CT_CustomerDataList {
  const start = requireStart(cur, 'CT_CustomerDataList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_CustomerDataList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const custData: CT_CustomerData[] = [];
  let tags: CT_TagsData | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomerDataList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'custData': {
            custData.push(readCT_CustomerData(cur, ctx));
            $slot = 0;
            $index = custData.length - 1;
            continue;
          }
          case 'tags': {
            tags = readCT_TagsData(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CustomerDataList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomerDataList', cur);
    cur.next();
  }

  return {
    custData,
    tags,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_EightDirectionTransition`. See `transitional/pml.xsd:47`. */
export function readCT_EightDirectionTransition(cur: XmlCursor, ctx: ReadContext): CT_EightDirectionTransition {
  const start = requireStart(cur, 'CT_EightDirectionTransition');
  let dir: ST_TransitionEightDirectionType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'dir': {
          const v = parseST_TransitionEightDirectionType(a.value);
          if (v !== undefined) {
            dir = v;
            continue;
          }
          ctx.invalidValue('CT_EightDirectionTransition', a, 'ST_TransitionEightDirectionType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_EightDirectionTransition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EightDirectionTransition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_EightDirectionTransition', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EightDirectionTransition', cur);
    cur.next();
  }
  return {
    dir,
    $unknownAttrs,
  };
}

/** Read a `CT_EmbeddedFontDataId`. See `transitional/pml.xsd:927`. */
export function readCT_EmbeddedFontDataId(cur: XmlCursor, ctx: ReadContext): CT_EmbeddedFontDataId {
  const start = requireStart(cur, 'CT_EmbeddedFontDataId');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_EmbeddedFontDataId', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_EmbeddedFontDataId', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_EmbeddedFontDataId', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EmbeddedFontDataId', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_EmbeddedFontDataId', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EmbeddedFontDataId', cur);
    cur.next();
  }
  return {
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_EmbeddedFontList`. See `transitional/pml.xsd:939`. */
export function readCT_EmbeddedFontList(cur: XmlCursor, ctx: ReadContext): CT_EmbeddedFontList {
  const start = requireStart(cur, 'CT_EmbeddedFontList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_EmbeddedFontList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const embeddedFont: CT_EmbeddedFontListEntry[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EmbeddedFontList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'embeddedFont': {
            embeddedFont.push(readCT_EmbeddedFontListEntry(cur, ctx));
            $slot = 0;
            $index = embeddedFont.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_EmbeddedFontList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EmbeddedFontList', cur);
    cur.next();
  }

  return {
    embeddedFont,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_EmbeddedFontListEntry`. See `transitional/pml.xsd:930`. */
export function readCT_EmbeddedFontListEntry(cur: XmlCursor, ctx: ReadContext): CT_EmbeddedFontListEntry {
  const start = requireStart(cur, 'CT_EmbeddedFontListEntry');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_EmbeddedFontListEntry', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let font: CT_TextFont | undefined;
  let regular: CT_EmbeddedFontDataId | undefined;
  let bold: CT_EmbeddedFontDataId | undefined;
  let italic: CT_EmbeddedFontDataId | undefined;
  let boldItalic: CT_EmbeddedFontDataId | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EmbeddedFontListEntry', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'font': {
            font = readCT_TextFont(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'regular': {
            regular = readCT_EmbeddedFontDataId(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'bold': {
            bold = readCT_EmbeddedFontDataId(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'italic': {
            italic = readCT_EmbeddedFontDataId(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'boldItalic': {
            boldItalic = readCT_EmbeddedFontDataId(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_EmbeddedFontListEntry', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EmbeddedFontListEntry', cur);
    cur.next();
  }

  return {
    font,
    regular,
    bold,
    italic,
    boldItalic,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Empty`. See `transitional/pml.xsd:713`. */
export function readCT_Empty(cur: XmlCursor, ctx: ReadContext): CT_Empty {
  const start = requireStart(cur, 'CT_Empty');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Empty', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Empty', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Empty', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Empty', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_Extension`. See `transitional/pml.xsd:761`. */
export function readCT_Extension(cur: XmlCursor, ctx: ReadContext): CT_Extension {
  const start = requireStart(cur, 'CT_Extension');
  let uri: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'uri':
          uri = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Extension', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (uri === undefined) ctx.missingRequired('CT_Extension', 'uri', cur);

  const any: RawNode[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Extension', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      any.push(cur.skipToRaw());
      $slot = 0;
      $index = any.length - 1;
      continue;
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Extension', cur);
    cur.next();
  }

  return {
    any,
    uri,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ExtensionList`. See `transitional/pml.xsd:772`. */
export function readCT_ExtensionList(cur: XmlCursor, ctx: ReadContext): CT_ExtensionList {
  const start = requireStart(cur, 'CT_ExtensionList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ExtensionList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const ext: CT_Extension[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ExtensionList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'ext': {
            ext.push(readCT_Extension(cur, ctx));
            $slot = 0;
            $index = ext.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ExtensionList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ExtensionList', cur);
    cur.next();
  }

  return {
    ext,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ExtensionListModify`. See `transitional/pml.xsd:777`. */
export function readCT_ExtensionListModify(cur: XmlCursor, ctx: ReadContext): CT_ExtensionListModify {
  const start = requireStart(cur, 'CT_ExtensionListModify');
  let mod: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'mod': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            mod = v;
            continue;
          }
          ctx.invalidValue('CT_ExtensionListModify', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ExtensionListModify', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const ext: CT_Extension[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ExtensionListModify', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'ext': {
            ext.push(readCT_Extension(cur, ctx));
            $slot = 0;
            $index = ext.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ExtensionListModify', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ExtensionListModify', cur);
    cur.next();
  }

  return {
    ext,
    mod,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GraphicalObjectFrame`. See `transitional/pml.xsd:1322`. */
export function readCT_GraphicalObjectFrame(cur: XmlCursor, ctx: ReadContext): CT_GraphicalObjectFrame {
  const start = requireStart(cur, 'CT_GraphicalObjectFrame');
  let bwMode: ST_BlackWhiteMode | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'bwMode': {
          const v = parseST_BlackWhiteMode(a.value);
          if (v !== undefined) {
            bwMode = v;
            continue;
          }
          ctx.invalidValue('CT_GraphicalObjectFrame', a, 'ST_BlackWhiteMode', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GraphicalObjectFrame', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvGraphicFramePr: CT_GraphicalObjectFrameNonVisual | undefined;
  let xfrm: CT_Transform2D | undefined;
  let graphic: CT_GraphicalObject | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GraphicalObjectFrame', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'graphic': {
            graphic = readCT_GraphicalObject(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'nvGraphicFramePr': {
            nvGraphicFramePr = readCT_GraphicalObjectFrameNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'xfrm': {
            xfrm = readCT_Transform2D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GraphicalObjectFrame', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GraphicalObjectFrame', cur);
    cur.next();
  }

  return {
    nvGraphicFramePr,
    xfrm,
    graphic,
    extLst,
    bwMode,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GraphicalObjectFrameNonVisual`. See `transitional/pml.xsd:1313`. */
export function readCT_GraphicalObjectFrameNonVisual(cur: XmlCursor, ctx: ReadContext): CT_GraphicalObjectFrameNonVisual {
  const start = requireStart(cur, 'CT_GraphicalObjectFrameNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GraphicalObjectFrameNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvGraphicFramePr: CT_NonVisualGraphicFrameProperties | undefined;
  let nvPr: CT_ApplicationNonVisualDrawingProps | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GraphicalObjectFrameNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvGraphicFramePr': {
            cNvGraphicFramePr = readCT_NonVisualGraphicFrameProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'nvPr': {
            nvPr = readCT_ApplicationNonVisualDrawingProps(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GraphicalObjectFrameNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GraphicalObjectFrameNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvGraphicFramePr,
    nvPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GroupShape`. See `transitional/pml.xsd:1341`. */
export function readCT_GroupShape(cur: XmlCursor, ctx: ReadContext): CT_GroupShape {
  const start = requireStart(cur, 'CT_GroupShape');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GroupShape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvGrpSpPr: CT_GroupShapeNonVisual | undefined;
  let grpSpPr: CT_GroupShapeProperties | undefined;
  const content: CT_GroupShape_Content[] = [];
  let extLst: CT_ExtensionListModify | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GroupShape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'nvGrpSpPr': {
            nvGrpSpPr = readCT_GroupShapeNonVisual(cur, ctx);
            continue;
          }
          case 'grpSpPr': {
            grpSpPr = readCT_GroupShapeProperties(cur, ctx);
            continue;
          }
          case 'sp': {
            content.push({ kind: 'sp', value: readCT_Shape(cur, ctx) });
            continue;
          }
          case 'grpSp': {
            content.push({ kind: 'grpSp', value: readCT_GroupShape(cur, ctx) });
            continue;
          }
          case 'graphicFrame': {
            content.push({ kind: 'graphicFrame', value: readCT_GraphicalObjectFrame(cur, ctx) });
            continue;
          }
          case 'cxnSp': {
            content.push({ kind: 'cxnSp', value: readCT_Connector(cur, ctx) });
            continue;
          }
          case 'pic': {
            content.push({ kind: 'pic', value: readCT_Picture(cur, ctx) });
            continue;
          }
          case 'contentPart': {
            content.push({ kind: 'contentPart', value: readCT_Rel(cur, ctx) });
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GroupShape', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GroupShape', cur);
    cur.next();
  }

  return {
    nvGrpSpPr,
    grpSpPr,
    content,
    extLst,
    $unknownAttrs,
  };
}

/** Read a `CT_GroupShapeNonVisual`. See `transitional/pml.xsd:1332`. */
export function readCT_GroupShapeNonVisual(cur: XmlCursor, ctx: ReadContext): CT_GroupShapeNonVisual {
  const start = requireStart(cur, 'CT_GroupShapeNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GroupShapeNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvGrpSpPr: CT_NonVisualGroupDrawingShapeProps | undefined;
  let nvPr: CT_ApplicationNonVisualDrawingProps | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GroupShapeNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvGrpSpPr': {
            cNvGrpSpPr = readCT_NonVisualGroupDrawingShapeProps(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'nvPr': {
            nvPr = readCT_ApplicationNonVisualDrawingProps(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GroupShapeNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GroupShapeNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvGrpSpPr,
    nvPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Guide`. See `transitional/pml.xsd:1629`. */
export function readCT_Guide(cur: XmlCursor, ctx: ReadContext): CT_Guide {
  const start = requireStart(cur, 'CT_Guide');
  let orient: ST_Direction | undefined;
  let pos: ST_Coordinate32 | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'orient': {
          const v = parseST_Direction(a.value);
          if (v !== undefined) {
            orient = v;
            continue;
          }
          ctx.invalidValue('CT_Guide', a, 'ST_Direction', cur);
          break;
        }
        case 'pos': {
          const v = parseST_Coordinate32(a.value);
          if (v !== undefined) {
            pos = v;
            continue;
          }
          ctx.invalidValue('CT_Guide', a, 'ST_Coordinate32', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Guide', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Guide', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Guide', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Guide', cur);
    cur.next();
  }
  return {
    orient,
    pos,
    $unknownAttrs,
  };
}

/** Read a `CT_GuideList`. See `transitional/pml.xsd:1633`. */
export function readCT_GuideList(cur: XmlCursor, ctx: ReadContext): CT_GuideList {
  const start = requireStart(cur, 'CT_GuideList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GuideList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const guide: CT_Guide[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GuideList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'guide': {
            guide.push(readCT_Guide(cur, ctx));
            $slot = 0;
            $index = guide.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GuideList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GuideList', cur);
    cur.next();
  }

  return {
    guide,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_HandoutMaster`. See `transitional/pml.xsd:1511`. */
export function readCT_HandoutMaster(cur: XmlCursor, ctx: ReadContext): CT_HandoutMaster {
  const start = requireStart(cur, 'CT_HandoutMaster');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_HandoutMaster', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cSld: CT_CommonSlideData | undefined;
  let clrMap: CT_ColorMapping | undefined;
  let hf: CT_HeaderFooter | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HandoutMaster', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cSld': {
            cSld = readCT_CommonSlideData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'clrMap': {
            clrMap = readCT_ColorMapping(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'hf': {
            hf = readCT_HeaderFooter(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_HandoutMaster', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HandoutMaster', cur);
    cur.next();
  }

  return {
    cSld,
    clrMap,
    hf,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_HandoutMasterIdList`. See `transitional/pml.xsd:921`. */
export function readCT_HandoutMasterIdList(cur: XmlCursor, ctx: ReadContext): CT_HandoutMasterIdList {
  const start = requireStart(cur, 'CT_HandoutMasterIdList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_HandoutMasterIdList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let handoutMasterId: CT_HandoutMasterIdListEntry | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HandoutMasterIdList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'handoutMasterId': {
            handoutMasterId = readCT_HandoutMasterIdListEntry(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_HandoutMasterIdList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HandoutMasterIdList', cur);
    cur.next();
  }

  return {
    handoutMasterId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_HandoutMasterIdListEntry`. See `transitional/pml.xsd:915`. */
export function readCT_HandoutMasterIdListEntry(cur: XmlCursor, ctx: ReadContext): CT_HandoutMasterIdListEntry {
  const start = requireStart(cur, 'CT_HandoutMasterIdListEntry');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_HandoutMasterIdListEntry', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_HandoutMasterIdListEntry', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_HandoutMasterIdListEntry', 'id', cur);

  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HandoutMasterIdListEntry', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_HandoutMasterIdListEntry', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HandoutMasterIdListEntry', cur);
    cur.next();
  }

  return {
    extLst,
    id,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_HeaderFooter`. See `transitional/pml.xsd:1204`. */
export function readCT_HeaderFooter(cur: XmlCursor, ctx: ReadContext): CT_HeaderFooter {
  const start = requireStart(cur, 'CT_HeaderFooter');
  let sldNum: boolean | undefined;
  let hdr: boolean | undefined;
  let ftr: boolean | undefined;
  let dt: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'sldNum': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            sldNum = v;
            continue;
          }
          ctx.invalidValue('CT_HeaderFooter', a, 'xsd:boolean', cur);
          break;
        }
        case 'hdr': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            hdr = v;
            continue;
          }
          ctx.invalidValue('CT_HeaderFooter', a, 'xsd:boolean', cur);
          break;
        }
        case 'ftr': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            ftr = v;
            continue;
          }
          ctx.invalidValue('CT_HeaderFooter', a, 'xsd:boolean', cur);
          break;
        }
        case 'dt': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            dt = v;
            continue;
          }
          ctx.invalidValue('CT_HeaderFooter', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_HeaderFooter', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HeaderFooter', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_HeaderFooter', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HeaderFooter', cur);
    cur.next();
  }

  return {
    extLst,
    sldNum,
    hdr,
    ftr,
    dt,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_HtmlPublishProperties`. See `transitional/pml.xsd:1086`. */
export function readCT_HtmlPublishProperties(cur: XmlCursor, ctx: ReadContext): CT_HtmlPublishProperties {
  const start = requireStart(cur, 'CT_HtmlPublishProperties');
  let showSpeakerNotes: boolean | undefined;
  let target: string | undefined;
  let title: string | undefined;
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'showSpeakerNotes': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showSpeakerNotes = v;
            continue;
          }
          ctx.invalidValue('CT_HtmlPublishProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'target':
          target = a.value as string;
          continue;

        case 'title':
          title = a.value as string;
          continue;

      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_HtmlPublishProperties', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_HtmlPublishProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_HtmlPublishProperties', 'id', cur);

  let slideListChoice: CT_HtmlPublishProperties_SlideListChoice | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HtmlPublishProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'sldAll': {
            slideListChoice = { kind: 'sldAll', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sldRg': {
            slideListChoice = { kind: 'sldRg', value: readCT_IndexRange(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'custShow': {
            slideListChoice = { kind: 'custShow', value: readCT_CustomShowId(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_HtmlPublishProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HtmlPublishProperties', cur);
    cur.next();
  }

  return {
    slideListChoice,
    extLst,
    showSpeakerNotes,
    target,
    title,
    id,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_InOutTransition`. See `transitional/pml.xsd:53`. */
export function readCT_InOutTransition(cur: XmlCursor, ctx: ReadContext): CT_InOutTransition {
  const start = requireStart(cur, 'CT_InOutTransition');
  let dir: ST_TransitionInOutDirectionType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'dir': {
          const v = parseST_TransitionInOutDirectionType(a.value);
          if (v !== undefined) {
            dir = v;
            continue;
          }
          ctx.invalidValue('CT_InOutTransition', a, 'ST_TransitionInOutDirectionType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_InOutTransition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_InOutTransition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_InOutTransition', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_InOutTransition', cur);
    cur.next();
  }
  return {
    dir,
    $unknownAttrs,
  };
}

/** Read a `CT_IndexRange`. See `transitional/pml.xsd:726`. */
export function readCT_IndexRange(cur: XmlCursor, ctx: ReadContext): CT_IndexRange {
  const start = requireStart(cur, 'CT_IndexRange');
  let st: ST_Index | undefined;
  let end: ST_Index | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'st': {
          const v = parseST_Index(a.value);
          if (v !== undefined) {
            st = v;
            continue;
          }
          ctx.invalidValue('CT_IndexRange', a, 'ST_Index', cur);
          break;
        }
        case 'end': {
          const v = parseST_Index(a.value);
          if (v !== undefined) {
            end = v;
            continue;
          }
          ctx.invalidValue('CT_IndexRange', a, 'ST_Index', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_IndexRange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (st === undefined) ctx.missingRequired('CT_IndexRange', 'st', cur);
  if (end === undefined) ctx.missingRequired('CT_IndexRange', 'end', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_IndexRange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_IndexRange', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_IndexRange', cur);
    cur.next();
  }
  return {
    st,
    end,
    $unknownAttrs,
  };
}

/** Read a `CT_Kinsoku`. See `transitional/pml.xsd:1024`. */
export function readCT_Kinsoku(cur: XmlCursor, ctx: ReadContext): CT_Kinsoku {
  const start = requireStart(cur, 'CT_Kinsoku');
  let lang: string | undefined;
  let invalStChars: string | undefined;
  let invalEndChars: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'lang':
          lang = a.value as string;
          continue;

        case 'invalStChars':
          invalStChars = a.value as string;
          continue;

        case 'invalEndChars':
          invalEndChars = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Kinsoku', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (invalStChars === undefined) ctx.missingRequired('CT_Kinsoku', 'invalStChars', cur);
  if (invalEndChars === undefined) ctx.missingRequired('CT_Kinsoku', 'invalEndChars', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Kinsoku', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Kinsoku', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Kinsoku', cur);
    cur.next();
  }
  return {
    lang,
    invalStChars,
    invalEndChars,
    $unknownAttrs,
  };
}

/** Read a `CT_ModifyVerifier`. See `transitional/pml.xsd:1035`. */
export function readCT_ModifyVerifier(cur: XmlCursor, ctx: ReadContext): CT_ModifyVerifier {
  const start = requireStart(cur, 'CT_ModifyVerifier');
  let algorithmName: string | undefined;
  let hashValue: string | undefined;
  let saltValue: string | undefined;
  let spinValue: number | undefined;
  let cryptProviderType: ST_CryptProv | undefined;
  let cryptAlgorithmClass: ST_AlgClass | undefined;
  let cryptAlgorithmType: ST_AlgType | undefined;
  let cryptAlgorithmSid: number | undefined;
  let spinCount: number | undefined;
  let saltData: string | undefined;
  let hashData: string | undefined;
  let cryptProvider: string | undefined;
  let algIdExt: number | undefined;
  let algIdExtSource: string | undefined;
  let cryptProviderTypeExt: number | undefined;
  let cryptProviderTypeExtSource: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'algorithmName':
          algorithmName = a.value as string;
          continue;

        case 'hashValue':
          hashValue = a.value as string;
          continue;

        case 'saltValue':
          saltValue = a.value as string;
          continue;

        case 'spinValue': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            spinValue = v;
            continue;
          }
          ctx.invalidValue('CT_ModifyVerifier', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'cryptProviderType': {
          const v = parseST_CryptProv(a.value);
          if (v !== undefined) {
            cryptProviderType = v;
            continue;
          }
          ctx.invalidValue('CT_ModifyVerifier', a, 'ST_CryptProv', cur);
          break;
        }
        case 'cryptAlgorithmClass': {
          const v = parseST_AlgClass(a.value);
          if (v !== undefined) {
            cryptAlgorithmClass = v;
            continue;
          }
          ctx.invalidValue('CT_ModifyVerifier', a, 'ST_AlgClass', cur);
          break;
        }
        case 'cryptAlgorithmType': {
          const v = parseST_AlgType(a.value);
          if (v !== undefined) {
            cryptAlgorithmType = v;
            continue;
          }
          ctx.invalidValue('CT_ModifyVerifier', a, 'ST_AlgType', cur);
          break;
        }
        case 'cryptAlgorithmSid': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            cryptAlgorithmSid = v;
            continue;
          }
          ctx.invalidValue('CT_ModifyVerifier', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'spinCount': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            spinCount = v;
            continue;
          }
          ctx.invalidValue('CT_ModifyVerifier', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'saltData':
          saltData = a.value as string;
          continue;

        case 'hashData':
          hashData = a.value as string;
          continue;

        case 'cryptProvider':
          cryptProvider = a.value as string;
          continue;

        case 'algIdExt': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            algIdExt = v;
            continue;
          }
          ctx.invalidValue('CT_ModifyVerifier', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'algIdExtSource':
          algIdExtSource = a.value as string;
          continue;

        case 'cryptProviderTypeExt': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            cryptProviderTypeExt = v;
            continue;
          }
          ctx.invalidValue('CT_ModifyVerifier', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'cryptProviderTypeExtSource':
          cryptProviderTypeExtSource = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_ModifyVerifier', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ModifyVerifier', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ModifyVerifier', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ModifyVerifier', cur);
    cur.next();
  }
  return {
    algorithmName,
    hashValue,
    saltValue,
    spinValue,
    cryptProviderType,
    cryptAlgorithmClass,
    cryptAlgorithmType,
    cryptAlgorithmSid,
    spinCount,
    saltData,
    hashData,
    cryptProvider,
    algIdExt,
    algIdExtSource,
    cryptProviderTypeExt,
    cryptProviderTypeExtSource,
    $unknownAttrs,
  };
}

/** Read a `CT_NormalViewPortion`. See `transitional/pml.xsd:1577`. */
export function readCT_NormalViewPortion(cur: XmlCursor, ctx: ReadContext): CT_NormalViewPortion {
  const start = requireStart(cur, 'CT_NormalViewPortion');
  let sz: ST_PositiveFixedPercentage | undefined;
  let autoAdjust: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'sz': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            sz = v;
            continue;
          }
          ctx.invalidValue('CT_NormalViewPortion', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
        case 'autoAdjust': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            autoAdjust = v;
            continue;
          }
          ctx.invalidValue('CT_NormalViewPortion', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NormalViewPortion', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (sz === undefined) ctx.missingRequired('CT_NormalViewPortion', 'sz', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NormalViewPortion', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_NormalViewPortion', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NormalViewPortion', cur);
    cur.next();
  }
  return {
    sz,
    autoAdjust,
    $unknownAttrs,
  };
}

/** Read a `CT_NormalViewProperties`. See `transitional/pml.xsd:1581`. */
export function readCT_NormalViewProperties(cur: XmlCursor, ctx: ReadContext): CT_NormalViewProperties {
  const start = requireStart(cur, 'CT_NormalViewProperties');
  let showOutlineIcons: boolean | undefined;
  let snapVertSplitter: boolean | undefined;
  let vertBarState: ST_SplitterBarState | undefined;
  let horzBarState: ST_SplitterBarState | undefined;
  let preferSingleView: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'showOutlineIcons': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showOutlineIcons = v;
            continue;
          }
          ctx.invalidValue('CT_NormalViewProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'snapVertSplitter': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            snapVertSplitter = v;
            continue;
          }
          ctx.invalidValue('CT_NormalViewProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'vertBarState': {
          const v = parseST_SplitterBarState(a.value);
          if (v !== undefined) {
            vertBarState = v;
            continue;
          }
          ctx.invalidValue('CT_NormalViewProperties', a, 'ST_SplitterBarState', cur);
          break;
        }
        case 'horzBarState': {
          const v = parseST_SplitterBarState(a.value);
          if (v !== undefined) {
            horzBarState = v;
            continue;
          }
          ctx.invalidValue('CT_NormalViewProperties', a, 'ST_SplitterBarState', cur);
          break;
        }
        case 'preferSingleView': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            preferSingleView = v;
            continue;
          }
          ctx.invalidValue('CT_NormalViewProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NormalViewProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let restoredLeft: CT_NormalViewPortion | undefined;
  let restoredTop: CT_NormalViewPortion | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NormalViewProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'restoredLeft': {
            restoredLeft = readCT_NormalViewPortion(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'restoredTop': {
            restoredTop = readCT_NormalViewPortion(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NormalViewProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NormalViewProperties', cur);
    cur.next();
  }

  return {
    restoredLeft,
    restoredTop,
    extLst,
    showOutlineIcons,
    snapVertSplitter,
    vertBarState,
    horzBarState,
    preferSingleView,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NotesMaster`. See `transitional/pml.xsd:1520`. */
export function readCT_NotesMaster(cur: XmlCursor, ctx: ReadContext): CT_NotesMaster {
  const start = requireStart(cur, 'CT_NotesMaster');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NotesMaster', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cSld: CT_CommonSlideData | undefined;
  let clrMap: CT_ColorMapping | undefined;
  let hf: CT_HeaderFooter | undefined;
  let notesStyle: CT_TextListStyle | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NotesMaster', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cSld': {
            cSld = readCT_CommonSlideData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'clrMap': {
            clrMap = readCT_ColorMapping(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'hf': {
            hf = readCT_HeaderFooter(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'notesStyle': {
            notesStyle = readCT_TextListStyle(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NotesMaster', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NotesMaster', cur);
    cur.next();
  }

  return {
    cSld,
    clrMap,
    hf,
    notesStyle,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NotesMasterIdList`. See `transitional/pml.xsd:909`. */
export function readCT_NotesMasterIdList(cur: XmlCursor, ctx: ReadContext): CT_NotesMasterIdList {
  const start = requireStart(cur, 'CT_NotesMasterIdList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NotesMasterIdList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let notesMasterId: CT_NotesMasterIdListEntry | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NotesMasterIdList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'notesMasterId': {
            notesMasterId = readCT_NotesMasterIdListEntry(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NotesMasterIdList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NotesMasterIdList', cur);
    cur.next();
  }

  return {
    notesMasterId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NotesMasterIdListEntry`. See `transitional/pml.xsd:903`. */
export function readCT_NotesMasterIdListEntry(cur: XmlCursor, ctx: ReadContext): CT_NotesMasterIdListEntry {
  const start = requireStart(cur, 'CT_NotesMasterIdListEntry');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_NotesMasterIdListEntry', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NotesMasterIdListEntry', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_NotesMasterIdListEntry', 'id', cur);

  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NotesMasterIdListEntry', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NotesMasterIdListEntry', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NotesMasterIdListEntry', cur);
    cur.next();
  }

  return {
    extLst,
    id,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NotesSlide`. See `transitional/pml.xsd:1530`. */
export function readCT_NotesSlide(cur: XmlCursor, ctx: ReadContext): CT_NotesSlide {
  const start = requireStart(cur, 'CT_NotesSlide');
  let showMasterSp: boolean | undefined;
  let showMasterPhAnim: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'showMasterSp': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showMasterSp = v;
            continue;
          }
          ctx.invalidValue('CT_NotesSlide', a, 'xsd:boolean', cur);
          break;
        }
        case 'showMasterPhAnim': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showMasterPhAnim = v;
            continue;
          }
          ctx.invalidValue('CT_NotesSlide', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NotesSlide', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cSld: CT_CommonSlideData | undefined;
  let clrMapOvr: CT_ColorMappingOverride | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NotesSlide', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cSld': {
            cSld = readCT_CommonSlideData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'clrMapOvr': {
            clrMapOvr = readCT_ColorMappingOverride(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NotesSlide', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NotesSlide', cur);
    cur.next();
  }

  return {
    cSld,
    clrMapOvr,
    extLst,
    showMasterSp,
    showMasterPhAnim,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NotesTextViewProperties`. See `transitional/pml.xsd:1600`. */
export function readCT_NotesTextViewProperties(cur: XmlCursor, ctx: ReadContext): CT_NotesTextViewProperties {
  const start = requireStart(cur, 'CT_NotesTextViewProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NotesTextViewProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cViewPr: CT_CommonViewProperties | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NotesTextViewProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cViewPr': {
            cViewPr = readCT_CommonViewProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NotesTextViewProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NotesTextViewProperties', cur);
    cur.next();
  }

  return {
    cViewPr,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NotesViewProperties`. See `transitional/pml.xsd:1653`. */
export function readCT_NotesViewProperties(cur: XmlCursor, ctx: ReadContext): CT_NotesViewProperties {
  const start = requireStart(cur, 'CT_NotesViewProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NotesViewProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cSldViewPr: CT_CommonSlideViewProperties | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NotesViewProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cSldViewPr': {
            cSldViewPr = readCT_CommonSlideViewProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NotesViewProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NotesViewProperties', cur);
    cur.next();
  }

  return {
    cSldViewPr,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OleObject`. See `transitional/pml.xsd:843`. */
export function readCT_OleObject(cur: XmlCursor, ctx: ReadContext): CT_OleObject {
  const start = requireStart(cur, 'CT_OleObject');
  let spid: ST_ShapeID | undefined;
  let name: string | undefined;
  let showAsIcon: boolean | undefined;
  let id: ST_RelationshipId | undefined;
  let imgW: ST_PositiveCoordinate32 | undefined;
  let imgH: ST_PositiveCoordinate32 | undefined;
  let progId: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'spid': {
          const v = parseST_ShapeID(a.value);
          if (v !== undefined) {
            spid = v;
            continue;
          }
          ctx.invalidValue('CT_OleObject', a, 'ST_ShapeID', cur);
          break;
        }
        case 'name':
          name = a.value as string;
          continue;

        case 'showAsIcon': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showAsIcon = v;
            continue;
          }
          ctx.invalidValue('CT_OleObject', a, 'xsd:boolean', cur);
          break;
        }
        case 'imgW': {
          const v = parseST_PositiveCoordinate32(a.value);
          if (v !== undefined) {
            imgW = v;
            continue;
          }
          ctx.invalidValue('CT_OleObject', a, 'ST_PositiveCoordinate32', cur);
          break;
        }
        case 'imgH': {
          const v = parseST_PositiveCoordinate32(a.value);
          if (v !== undefined) {
            imgH = v;
            continue;
          }
          ctx.invalidValue('CT_OleObject', a, 'ST_PositiveCoordinate32', cur);
          break;
        }
        case 'progId':
          progId = a.value as string;
          continue;

      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_OleObject', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OleObject', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_OleObject_Content | undefined;
  let pic: CT_Picture | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OleObject', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'embed': {
            content = { kind: 'embed', value: readCT_OleObjectEmbed(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'link': {
            content = { kind: 'link', value: readCT_OleObjectLink(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pic': {
            pic = readCT_Picture(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OleObject', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OleObject', cur);
    cur.next();
  }

  return {
    content,
    pic,
    spid,
    name,
    showAsIcon,
    id,
    imgW,
    imgH,
    progId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OleObjectEmbed`. See `transitional/pml.xsd:830`. */
export function readCT_OleObjectEmbed(cur: XmlCursor, ctx: ReadContext): CT_OleObjectEmbed {
  const start = requireStart(cur, 'CT_OleObjectEmbed');
  let followColorScheme: ST_OleObjectFollowColorScheme | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'followColorScheme': {
          const v = parseST_OleObjectFollowColorScheme(a.value);
          if (v !== undefined) {
            followColorScheme = v;
            continue;
          }
          ctx.invalidValue('CT_OleObjectEmbed', a, 'ST_OleObjectFollowColorScheme', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OleObjectEmbed', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OleObjectEmbed', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OleObjectEmbed', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OleObjectEmbed', cur);
    cur.next();
  }

  return {
    extLst,
    followColorScheme,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OleObjectLink`. See `transitional/pml.xsd:837`. */
export function readCT_OleObjectLink(cur: XmlCursor, ctx: ReadContext): CT_OleObjectLink {
  const start = requireStart(cur, 'CT_OleObjectLink');
  let updateAutomatic: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'updateAutomatic': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            updateAutomatic = v;
            continue;
          }
          ctx.invalidValue('CT_OleObjectLink', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OleObjectLink', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OleObjectLink', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OleObjectLink', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OleObjectLink', cur);
    cur.next();
  }

  return {
    extLst,
    updateAutomatic,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OptionalBlackTransition`. See `transitional/pml.xsd:56`. */
export function readCT_OptionalBlackTransition(cur: XmlCursor, ctx: ReadContext): CT_OptionalBlackTransition {
  const start = requireStart(cur, 'CT_OptionalBlackTransition');
  let thruBlk: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'thruBlk': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            thruBlk = v;
            continue;
          }
          ctx.invalidValue('CT_OptionalBlackTransition', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OptionalBlackTransition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OptionalBlackTransition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_OptionalBlackTransition', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OptionalBlackTransition', cur);
    cur.next();
  }
  return {
    thruBlk,
    $unknownAttrs,
  };
}

/** Read a `CT_OrientationTransition`. See `transitional/pml.xsd:50`. */
export function readCT_OrientationTransition(cur: XmlCursor, ctx: ReadContext): CT_OrientationTransition {
  const start = requireStart(cur, 'CT_OrientationTransition');
  let dir: ST_Direction | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'dir': {
          const v = parseST_Direction(a.value);
          if (v !== undefined) {
            dir = v;
            continue;
          }
          ctx.invalidValue('CT_OrientationTransition', a, 'ST_Direction', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OrientationTransition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OrientationTransition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_OrientationTransition', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OrientationTransition', cur);
    cur.next();
  }
  return {
    dir,
    $unknownAttrs,
  };
}

/** Read a `CT_OutlineViewProperties`. See `transitional/pml.xsd:1615`. */
export function readCT_OutlineViewProperties(cur: XmlCursor, ctx: ReadContext): CT_OutlineViewProperties {
  const start = requireStart(cur, 'CT_OutlineViewProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_OutlineViewProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cViewPr: CT_CommonViewProperties | undefined;
  let sldLst: CT_OutlineViewSlideList | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OutlineViewProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cViewPr': {
            cViewPr = readCT_CommonViewProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sldLst': {
            sldLst = readCT_OutlineViewSlideList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OutlineViewProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OutlineViewProperties', cur);
    cur.next();
  }

  return {
    cViewPr,
    sldLst,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OutlineViewSlideEntry`. See `transitional/pml.xsd:1606`. */
export function readCT_OutlineViewSlideEntry(cur: XmlCursor, ctx: ReadContext): CT_OutlineViewSlideEntry {
  const start = requireStart(cur, 'CT_OutlineViewSlideEntry');
  let id: ST_RelationshipId | undefined;
  let collapse: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'collapse': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            collapse = v;
            continue;
          }
          ctx.invalidValue('CT_OutlineViewSlideEntry', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_OutlineViewSlideEntry', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OutlineViewSlideEntry', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_OutlineViewSlideEntry', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OutlineViewSlideEntry', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_OutlineViewSlideEntry', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OutlineViewSlideEntry', cur);
    cur.next();
  }
  return {
    id,
    collapse,
    $unknownAttrs,
  };
}

/** Read a `CT_OutlineViewSlideList`. See `transitional/pml.xsd:1610`. */
export function readCT_OutlineViewSlideList(cur: XmlCursor, ctx: ReadContext): CT_OutlineViewSlideList {
  const start = requireStart(cur, 'CT_OutlineViewSlideList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_OutlineViewSlideList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const sld: CT_OutlineViewSlideEntry[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OutlineViewSlideList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'sld': {
            sld.push(readCT_OutlineViewSlideEntry(cur, ctx));
            $slot = 0;
            $index = sld.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OutlineViewSlideList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OutlineViewSlideList', cur);
    cur.next();
  }

  return {
    sld,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PhotoAlbum`. See `transitional/pml.xsd:983`. */
export function readCT_PhotoAlbum(cur: XmlCursor, ctx: ReadContext): CT_PhotoAlbum {
  const start = requireStart(cur, 'CT_PhotoAlbum');
  let bw: boolean | undefined;
  let showCaptions: boolean | undefined;
  let layout: ST_PhotoAlbumLayout | undefined;
  let frame: ST_PhotoAlbumFrameShape | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'bw': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            bw = v;
            continue;
          }
          ctx.invalidValue('CT_PhotoAlbum', a, 'xsd:boolean', cur);
          break;
        }
        case 'showCaptions': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showCaptions = v;
            continue;
          }
          ctx.invalidValue('CT_PhotoAlbum', a, 'xsd:boolean', cur);
          break;
        }
        case 'layout': {
          const v = parseST_PhotoAlbumLayout(a.value);
          if (v !== undefined) {
            layout = v;
            continue;
          }
          ctx.invalidValue('CT_PhotoAlbum', a, 'ST_PhotoAlbumLayout', cur);
          break;
        }
        case 'frame': {
          const v = parseST_PhotoAlbumFrameShape(a.value);
          if (v !== undefined) {
            frame = v;
            continue;
          }
          ctx.invalidValue('CT_PhotoAlbum', a, 'ST_PhotoAlbumFrameShape', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PhotoAlbum', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PhotoAlbum', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PhotoAlbum', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PhotoAlbum', cur);
    cur.next();
  }

  return {
    extLst,
    bw,
    showCaptions,
    layout,
    frame,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Picture`. See `transitional/pml.xsd:1304`. */
export function readCT_Picture(cur: XmlCursor, ctx: ReadContext): CT_Picture {
  const start = requireStart(cur, 'CT_Picture');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Picture', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvPicPr: CT_PictureNonVisual | undefined;
  let blipFill: CT_BlipFillProperties | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let style: CT_ShapeStyle | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Picture', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'nvPicPr': {
            nvPicPr = readCT_PictureNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            blipFill = readCT_BlipFillProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Picture', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Picture', cur);
    cur.next();
  }

  return {
    nvPicPr,
    blipFill,
    spPr,
    style,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PictureNonVisual`. See `transitional/pml.xsd:1295`. */
export function readCT_PictureNonVisual(cur: XmlCursor, ctx: ReadContext): CT_PictureNonVisual {
  const start = requireStart(cur, 'CT_PictureNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PictureNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvPicPr: CT_NonVisualPictureProperties | undefined;
  let nvPr: CT_ApplicationNonVisualDrawingProps | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PictureNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvPicPr': {
            cNvPicPr = readCT_NonVisualPictureProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'nvPr': {
            nvPr = readCT_ApplicationNonVisualDrawingProps(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PictureNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PictureNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvPicPr,
    nvPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Placeholder`. See `transitional/pml.xsd:1240`. */
export function readCT_Placeholder(cur: XmlCursor, ctx: ReadContext): CT_Placeholder {
  const start = requireStart(cur, 'CT_Placeholder');
  let type: ST_PlaceholderType | undefined;
  let orient: ST_Direction | undefined;
  let sz: ST_PlaceholderSize | undefined;
  let idx: number | undefined;
  let hasCustomPrompt: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_PlaceholderType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Placeholder', a, 'ST_PlaceholderType', cur);
          break;
        }
        case 'orient': {
          const v = parseST_Direction(a.value);
          if (v !== undefined) {
            orient = v;
            continue;
          }
          ctx.invalidValue('CT_Placeholder', a, 'ST_Direction', cur);
          break;
        }
        case 'sz': {
          const v = parseST_PlaceholderSize(a.value);
          if (v !== undefined) {
            sz = v;
            continue;
          }
          ctx.invalidValue('CT_Placeholder', a, 'ST_PlaceholderSize', cur);
          break;
        }
        case 'idx': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            idx = v;
            continue;
          }
          ctx.invalidValue('CT_Placeholder', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'hasCustomPrompt': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            hasCustomPrompt = v;
            continue;
          }
          ctx.invalidValue('CT_Placeholder', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Placeholder', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Placeholder', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Placeholder', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Placeholder', cur);
    cur.next();
  }

  return {
    extLst,
    type,
    orient,
    sz,
    idx,
    hasCustomPrompt,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Presentation`. See `transitional/pml.xsd:1053`. */
export function readCT_Presentation(cur: XmlCursor, ctx: ReadContext): CT_Presentation {
  const start = requireStart(cur, 'CT_Presentation');
  let serverZoom: ST_Percentage | undefined;
  let firstSlideNum: number | undefined;
  let showSpecialPlsOnTitleSld: boolean | undefined;
  let rtl: boolean | undefined;
  let removePersonalInfoOnSave: boolean | undefined;
  let compatMode: boolean | undefined;
  let strictFirstAndLastChars: boolean | undefined;
  let embedTrueTypeFonts: boolean | undefined;
  let saveSubsetFonts: boolean | undefined;
  let autoCompressPictures: boolean | undefined;
  let bookmarkIdSeed: ST_BookmarkIdSeed | undefined;
  let conformance: ST_ConformanceClass | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'serverZoom': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            serverZoom = v;
            continue;
          }
          ctx.invalidValue('CT_Presentation', a, 'ST_Percentage', cur);
          break;
        }
        case 'firstSlideNum': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            firstSlideNum = v;
            continue;
          }
          ctx.invalidValue('CT_Presentation', a, 'xsd:int', cur);
          break;
        }
        case 'showSpecialPlsOnTitleSld': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showSpecialPlsOnTitleSld = v;
            continue;
          }
          ctx.invalidValue('CT_Presentation', a, 'xsd:boolean', cur);
          break;
        }
        case 'rtl': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            rtl = v;
            continue;
          }
          ctx.invalidValue('CT_Presentation', a, 'xsd:boolean', cur);
          break;
        }
        case 'removePersonalInfoOnSave': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            removePersonalInfoOnSave = v;
            continue;
          }
          ctx.invalidValue('CT_Presentation', a, 'xsd:boolean', cur);
          break;
        }
        case 'compatMode': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            compatMode = v;
            continue;
          }
          ctx.invalidValue('CT_Presentation', a, 'xsd:boolean', cur);
          break;
        }
        case 'strictFirstAndLastChars': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            strictFirstAndLastChars = v;
            continue;
          }
          ctx.invalidValue('CT_Presentation', a, 'xsd:boolean', cur);
          break;
        }
        case 'embedTrueTypeFonts': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            embedTrueTypeFonts = v;
            continue;
          }
          ctx.invalidValue('CT_Presentation', a, 'xsd:boolean', cur);
          break;
        }
        case 'saveSubsetFonts': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            saveSubsetFonts = v;
            continue;
          }
          ctx.invalidValue('CT_Presentation', a, 'xsd:boolean', cur);
          break;
        }
        case 'autoCompressPictures': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            autoCompressPictures = v;
            continue;
          }
          ctx.invalidValue('CT_Presentation', a, 'xsd:boolean', cur);
          break;
        }
        case 'bookmarkIdSeed': {
          const v = parseST_BookmarkIdSeed(a.value);
          if (v !== undefined) {
            bookmarkIdSeed = v;
            continue;
          }
          ctx.invalidValue('CT_Presentation', a, 'ST_BookmarkIdSeed', cur);
          break;
        }
        case 'conformance': {
          const v = parseST_ConformanceClass(a.value);
          if (v !== undefined) {
            conformance = v;
            continue;
          }
          ctx.invalidValue('CT_Presentation', a, 'ST_ConformanceClass', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Presentation', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let sldMasterIdLst: CT_SlideMasterIdList | undefined;
  let notesMasterIdLst: CT_NotesMasterIdList | undefined;
  let handoutMasterIdLst: CT_HandoutMasterIdList | undefined;
  let sldIdLst: CT_SlideIdList | undefined;
  let sldSz: CT_SlideSize | undefined;
  let notesSz: CT_PositiveSize2D | undefined;
  let smartTags: CT_SmartTags | undefined;
  let embeddedFontLst: CT_EmbeddedFontList | undefined;
  let custShowLst: CT_CustomShowList | undefined;
  let photoAlbum: CT_PhotoAlbum | undefined;
  let custDataLst: CT_CustomerDataList | undefined;
  let kinsoku: CT_Kinsoku | undefined;
  let defaultTextStyle: CT_TextListStyle | undefined;
  let modifyVerifier: CT_ModifyVerifier | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Presentation', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'sldMasterIdLst': {
            sldMasterIdLst = readCT_SlideMasterIdList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'notesMasterIdLst': {
            notesMasterIdLst = readCT_NotesMasterIdList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'handoutMasterIdLst': {
            handoutMasterIdLst = readCT_HandoutMasterIdList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'sldIdLst': {
            sldIdLst = readCT_SlideIdList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'sldSz': {
            sldSz = readCT_SlideSize(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'notesSz': {
            notesSz = readCT_PositiveSize2D(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'smartTags': {
            smartTags = readCT_SmartTags(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'embeddedFontLst': {
            embeddedFontLst = readCT_EmbeddedFontList(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'custShowLst': {
            custShowLst = readCT_CustomShowList(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'photoAlbum': {
            photoAlbum = readCT_PhotoAlbum(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'custDataLst': {
            custDataLst = readCT_CustomerDataList(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'kinsoku': {
            kinsoku = readCT_Kinsoku(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'defaultTextStyle': {
            defaultTextStyle = readCT_TextListStyle(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'modifyVerifier': {
            modifyVerifier = readCT_ModifyVerifier(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Presentation', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Presentation', cur);
    cur.next();
  }

  return {
    sldMasterIdLst,
    notesMasterIdLst,
    handoutMasterIdLst,
    sldIdLst,
    sldSz,
    notesSz,
    smartTags,
    embeddedFontLst,
    custShowLst,
    photoAlbum,
    custDataLst,
    kinsoku,
    defaultTextStyle,
    modifyVerifier,
    extLst,
    serverZoom,
    firstSlideNum,
    showSpecialPlsOnTitleSld,
    rtl,
    removePersonalInfoOnSave,
    compatMode,
    strictFirstAndLastChars,
    embedTrueTypeFonts,
    saveSubsetFonts,
    autoCompressPictures,
    bookmarkIdSeed,
    conformance,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PresentationProperties`. See `transitional/pml.xsd:1193`. */
export function readCT_PresentationProperties(cur: XmlCursor, ctx: ReadContext): CT_PresentationProperties {
  const start = requireStart(cur, 'CT_PresentationProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PresentationProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let htmlPubPr: CT_HtmlPublishProperties | undefined;
  let webPr: CT_WebProperties | undefined;
  let prnPr: CT_PrintProperties | undefined;
  let showPr: CT_ShowProperties | undefined;
  let clrMru: CT_ColorMRU | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PresentationProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'htmlPubPr': {
            htmlPubPr = readCT_HtmlPublishProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'webPr': {
            webPr = readCT_WebProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'prnPr': {
            prnPr = readCT_PrintProperties(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'showPr': {
            showPr = readCT_ShowProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'clrMru': {
            clrMru = readCT_ColorMRU(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PresentationProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PresentationProperties', cur);
    cur.next();
  }

  return {
    htmlPubPr,
    webPr,
    prnPr,
    showPr,
    clrMru,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PrintProperties`. See `transitional/pml.xsd:1158`. */
export function readCT_PrintProperties(cur: XmlCursor, ctx: ReadContext): CT_PrintProperties {
  const start = requireStart(cur, 'CT_PrintProperties');
  let prnWhat: ST_PrintWhat | undefined;
  let clrMode: ST_PrintColorMode | undefined;
  let hiddenSlides: boolean | undefined;
  let scaleToFitPaper: boolean | undefined;
  let frameSlides: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'prnWhat': {
          const v = parseST_PrintWhat(a.value);
          if (v !== undefined) {
            prnWhat = v;
            continue;
          }
          ctx.invalidValue('CT_PrintProperties', a, 'ST_PrintWhat', cur);
          break;
        }
        case 'clrMode': {
          const v = parseST_PrintColorMode(a.value);
          if (v !== undefined) {
            clrMode = v;
            continue;
          }
          ctx.invalidValue('CT_PrintProperties', a, 'ST_PrintColorMode', cur);
          break;
        }
        case 'hiddenSlides': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            hiddenSlides = v;
            continue;
          }
          ctx.invalidValue('CT_PrintProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'scaleToFitPaper': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            scaleToFitPaper = v;
            continue;
          }
          ctx.invalidValue('CT_PrintProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'frameSlides': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            frameSlides = v;
            continue;
          }
          ctx.invalidValue('CT_PrintProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PrintProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PrintProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PrintProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PrintProperties', cur);
    cur.next();
  }

  return {
    extLst,
    prnWhat,
    clrMode,
    hiddenSlides,
    scaleToFitPaper,
    frameSlides,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Rel`. See `transitional/pml.xsd:1356`. */
export function readCT_Rel(cur: XmlCursor, ctx: ReadContext): CT_Rel {
  const start = requireStart(cur, 'CT_Rel');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Rel', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Rel', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_Rel', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Rel', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Rel', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Rel', cur);
    cur.next();
  }
  return {
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_Shape`. See `transitional/pml.xsd:1268`. */
export function readCT_Shape(cur: XmlCursor, ctx: ReadContext): CT_Shape {
  const start = requireStart(cur, 'CT_Shape');
  let useBgFill: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'useBgFill': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            useBgFill = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Shape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvSpPr: CT_ShapeNonVisual | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let style: CT_ShapeStyle | undefined;
  let txBody: CT_TextBody | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Shape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'nvSpPr': {
            nvSpPr = readCT_ShapeNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'txBody': {
            txBody = readCT_TextBody(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Shape', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Shape', cur);
    cur.next();
  }

  return {
    nvSpPr,
    spPr,
    style,
    txBody,
    extLst,
    useBgFill,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ShapeNonVisual`. See `transitional/pml.xsd:1260`. */
export function readCT_ShapeNonVisual(cur: XmlCursor, ctx: ReadContext): CT_ShapeNonVisual {
  const start = requireStart(cur, 'CT_ShapeNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ShapeNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvSpPr: CT_NonVisualDrawingShapeProps | undefined;
  let nvPr: CT_ApplicationNonVisualDrawingProps | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ShapeNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvSpPr': {
            cNvSpPr = readCT_NonVisualDrawingShapeProps(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'nvPr': {
            nvPr = readCT_ApplicationNonVisualDrawingProps(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ShapeNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ShapeNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvSpPr,
    nvPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ShowInfoBrowse`. See `transitional/pml.xsd:1168`. */
export function readCT_ShowInfoBrowse(cur: XmlCursor, ctx: ReadContext): CT_ShowInfoBrowse {
  const start = requireStart(cur, 'CT_ShowInfoBrowse');
  let showScrollbar: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'showScrollbar': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showScrollbar = v;
            continue;
          }
          ctx.invalidValue('CT_ShowInfoBrowse', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ShowInfoBrowse', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ShowInfoBrowse', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ShowInfoBrowse', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ShowInfoBrowse', cur);
    cur.next();
  }
  return {
    showScrollbar,
    $unknownAttrs,
  };
}

/** Read a `CT_ShowInfoKiosk`. See `transitional/pml.xsd:1171`. */
export function readCT_ShowInfoKiosk(cur: XmlCursor, ctx: ReadContext): CT_ShowInfoKiosk {
  const start = requireStart(cur, 'CT_ShowInfoKiosk');
  let restart: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'restart': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            restart = v;
            continue;
          }
          ctx.invalidValue('CT_ShowInfoKiosk', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ShowInfoKiosk', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ShowInfoKiosk', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ShowInfoKiosk', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ShowInfoKiosk', cur);
    cur.next();
  }
  return {
    restart,
    $unknownAttrs,
  };
}

/** Read a `CT_ShowProperties`. See `transitional/pml.xsd:1181`. */
export function readCT_ShowProperties(cur: XmlCursor, ctx: ReadContext): CT_ShowProperties {
  const start = requireStart(cur, 'CT_ShowProperties');
  let loop: boolean | undefined;
  let showNarration: boolean | undefined;
  let showAnimation: boolean | undefined;
  let useTimings: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'loop': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            loop = v;
            continue;
          }
          ctx.invalidValue('CT_ShowProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'showNarration': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showNarration = v;
            continue;
          }
          ctx.invalidValue('CT_ShowProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'showAnimation': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showAnimation = v;
            continue;
          }
          ctx.invalidValue('CT_ShowProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'useTimings': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            useTimings = v;
            continue;
          }
          ctx.invalidValue('CT_ShowProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ShowProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let showType: CT_ShowProperties_ShowType | undefined;
  let slideListChoice: CT_ShowProperties_SlideListChoice | undefined;
  let penClr: CT_Color | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ShowProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'present': {
            showType = { kind: 'present', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'browse': {
            showType = { kind: 'browse', value: readCT_ShowInfoBrowse(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'kiosk': {
            showType = { kind: 'kiosk', value: readCT_ShowInfoKiosk(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sldAll': {
            slideListChoice = { kind: 'sldAll', value: readCT_Empty(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sldRg': {
            slideListChoice = { kind: 'sldRg', value: readCT_IndexRange(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'custShow': {
            slideListChoice = { kind: 'custShow', value: readCT_CustomShowId(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'penClr': {
            penClr = readCT_Color(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ShowProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ShowProperties', cur);
    cur.next();
  }

  return {
    showType,
    slideListChoice,
    penClr,
    extLst,
    loop,
    showNarration,
    showAnimation,
    useTimings,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SideDirectionTransition`. See `transitional/pml.xsd:38`. */
export function readCT_SideDirectionTransition(cur: XmlCursor, ctx: ReadContext): CT_SideDirectionTransition {
  const start = requireStart(cur, 'CT_SideDirectionTransition');
  let dir: ST_TransitionSideDirectionType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'dir': {
          const v = parseST_TransitionSideDirectionType(a.value);
          if (v !== undefined) {
            dir = v;
            continue;
          }
          ctx.invalidValue('CT_SideDirectionTransition', a, 'ST_TransitionSideDirectionType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SideDirectionTransition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SideDirectionTransition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SideDirectionTransition', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SideDirectionTransition', cur);
    cur.next();
  }
  return {
    dir,
    $unknownAttrs,
  };
}

/** Read a `CT_Slide`. See `transitional/pml.xsd:1403`. */
export function readCT_Slide(cur: XmlCursor, ctx: ReadContext): CT_Slide {
  const start = requireStart(cur, 'CT_Slide');
  let showMasterSp: boolean | undefined;
  let showMasterPhAnim: boolean | undefined;
  let show: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'showMasterSp': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showMasterSp = v;
            continue;
          }
          ctx.invalidValue('CT_Slide', a, 'xsd:boolean', cur);
          break;
        }
        case 'showMasterPhAnim': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showMasterPhAnim = v;
            continue;
          }
          ctx.invalidValue('CT_Slide', a, 'xsd:boolean', cur);
          break;
        }
        case 'show': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            show = v;
            continue;
          }
          ctx.invalidValue('CT_Slide', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Slide', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cSld: CT_CommonSlideData | undefined;
  let clrMapOvr: CT_ColorMappingOverride | undefined;
  let transition: CT_SlideTransition | undefined;
  let timing: CT_SlideTiming | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Slide', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cSld': {
            cSld = readCT_CommonSlideData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'clrMapOvr': {
            clrMapOvr = readCT_ColorMappingOverride(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'transition': {
            transition = readCT_SlideTransition(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'timing': {
            timing = readCT_SlideTiming(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Slide', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Slide', cur);
    cur.next();
  }

  return {
    cSld,
    clrMapOvr,
    transition,
    timing,
    extLst,
    showMasterSp,
    showMasterPhAnim,
    show,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideIdList`. See `transitional/pml.xsd:880`. */
export function readCT_SlideIdList(cur: XmlCursor, ctx: ReadContext): CT_SlideIdList {
  const start = requireStart(cur, 'CT_SlideIdList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SlideIdList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const sldId: CT_SlideIdListEntry[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideIdList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'sldId': {
            sldId.push(readCT_SlideIdListEntry(cur, ctx));
            $slot = 0;
            $index = sldId.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideIdList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideIdList', cur);
    cur.next();
  }

  return {
    sldId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideIdListEntry`. See `transitional/pml.xsd:873`. */
export function readCT_SlideIdListEntry(cur: XmlCursor, ctx: ReadContext): CT_SlideIdListEntry {
  const start = requireStart(cur, 'CT_SlideIdListEntry');
  let id: ST_SlideId | undefined;
  let id_relationships: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id': {
          const v = parseST_SlideId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_SlideIdListEntry', a, 'ST_SlideId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id_relationships = v;
            continue;
          }
          ctx.invalidValue('CT_SlideIdListEntry', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SlideIdListEntry', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_SlideIdListEntry', 'id', cur);
  if (id_relationships === undefined) ctx.missingRequired('CT_SlideIdListEntry', 'id', cur);

  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideIdListEntry', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideIdListEntry', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideIdListEntry', cur);
    cur.next();
  }

  return {
    extLst,
    id,
    id_relationships,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideLayout`. See `transitional/pml.xsd:1455`. */
export function readCT_SlideLayout(cur: XmlCursor, ctx: ReadContext): CT_SlideLayout {
  const start = requireStart(cur, 'CT_SlideLayout');
  let showMasterSp: boolean | undefined;
  let showMasterPhAnim: boolean | undefined;
  let matchingName: string | undefined;
  let type: ST_SlideLayoutType | undefined;
  let preserve: boolean | undefined;
  let userDrawn: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'showMasterSp': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showMasterSp = v;
            continue;
          }
          ctx.invalidValue('CT_SlideLayout', a, 'xsd:boolean', cur);
          break;
        }
        case 'showMasterPhAnim': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showMasterPhAnim = v;
            continue;
          }
          ctx.invalidValue('CT_SlideLayout', a, 'xsd:boolean', cur);
          break;
        }
        case 'matchingName':
          matchingName = a.value as string;
          continue;

        case 'type': {
          const v = parseST_SlideLayoutType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_SlideLayout', a, 'ST_SlideLayoutType', cur);
          break;
        }
        case 'preserve': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            preserve = v;
            continue;
          }
          ctx.invalidValue('CT_SlideLayout', a, 'xsd:boolean', cur);
          break;
        }
        case 'userDrawn': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            userDrawn = v;
            continue;
          }
          ctx.invalidValue('CT_SlideLayout', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SlideLayout', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cSld: CT_CommonSlideData | undefined;
  let clrMapOvr: CT_ColorMappingOverride | undefined;
  let transition: CT_SlideTransition | undefined;
  let timing: CT_SlideTiming | undefined;
  let hf: CT_HeaderFooter | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideLayout', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cSld': {
            cSld = readCT_CommonSlideData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'clrMapOvr': {
            clrMapOvr = readCT_ColorMappingOverride(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'transition': {
            transition = readCT_SlideTransition(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'timing': {
            timing = readCT_SlideTiming(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'hf': {
            hf = readCT_HeaderFooter(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideLayout', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideLayout', cur);
    cur.next();
  }

  return {
    cSld,
    clrMapOvr,
    transition,
    timing,
    hf,
    extLst,
    showMasterSp,
    showMasterPhAnim,
    matchingName,
    type,
    preserve,
    userDrawn,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideLayoutIdList`. See `transitional/pml.xsd:1491`. */
export function readCT_SlideLayoutIdList(cur: XmlCursor, ctx: ReadContext): CT_SlideLayoutIdList {
  const start = requireStart(cur, 'CT_SlideLayoutIdList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SlideLayoutIdList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const sldLayoutId: CT_SlideLayoutIdListEntry[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideLayoutIdList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'sldLayoutId': {
            sldLayoutId.push(readCT_SlideLayoutIdListEntry(cur, ctx));
            $slot = 0;
            $index = sldLayoutId.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideLayoutIdList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideLayoutIdList', cur);
    cur.next();
  }

  return {
    sldLayoutId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideLayoutIdListEntry`. See `transitional/pml.xsd:1484`. */
export function readCT_SlideLayoutIdListEntry(cur: XmlCursor, ctx: ReadContext): CT_SlideLayoutIdListEntry {
  const start = requireStart(cur, 'CT_SlideLayoutIdListEntry');
  let id: ST_SlideLayoutId | undefined;
  let id_relationships: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id': {
          const v = parseST_SlideLayoutId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_SlideLayoutIdListEntry', a, 'ST_SlideLayoutId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id_relationships = v;
            continue;
          }
          ctx.invalidValue('CT_SlideLayoutIdListEntry', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SlideLayoutIdListEntry', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id_relationships === undefined) ctx.missingRequired('CT_SlideLayoutIdListEntry', 'id', cur);

  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideLayoutIdListEntry', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideLayoutIdListEntry', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideLayoutIdListEntry', cur);
    cur.next();
  }

  return {
    extLst,
    id,
    id_relationships,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideMaster`. See `transitional/pml.xsd:1497`. */
export function readCT_SlideMaster(cur: XmlCursor, ctx: ReadContext): CT_SlideMaster {
  const start = requireStart(cur, 'CT_SlideMaster');
  let preserve: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'preserve': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            preserve = v;
            continue;
          }
          ctx.invalidValue('CT_SlideMaster', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SlideMaster', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cSld: CT_CommonSlideData | undefined;
  let clrMap: CT_ColorMapping | undefined;
  let sldLayoutIdLst: CT_SlideLayoutIdList | undefined;
  let transition: CT_SlideTransition | undefined;
  let timing: CT_SlideTiming | undefined;
  let hf: CT_HeaderFooter | undefined;
  let txStyles: CT_SlideMasterTextStyles | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideMaster', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cSld': {
            cSld = readCT_CommonSlideData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'clrMap': {
            clrMap = readCT_ColorMapping(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sldLayoutIdLst': {
            sldLayoutIdLst = readCT_SlideLayoutIdList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'transition': {
            transition = readCT_SlideTransition(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'timing': {
            timing = readCT_SlideTiming(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'hf': {
            hf = readCT_HeaderFooter(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'txStyles': {
            txStyles = readCT_SlideMasterTextStyles(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideMaster', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideMaster', cur);
    cur.next();
  }

  return {
    cSld,
    clrMap,
    sldLayoutIdLst,
    transition,
    timing,
    hf,
    txStyles,
    extLst,
    preserve,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideMasterIdList`. See `transitional/pml.xsd:897`. */
export function readCT_SlideMasterIdList(cur: XmlCursor, ctx: ReadContext): CT_SlideMasterIdList {
  const start = requireStart(cur, 'CT_SlideMasterIdList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SlideMasterIdList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const sldMasterId: CT_SlideMasterIdListEntry[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideMasterIdList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'sldMasterId': {
            sldMasterId.push(readCT_SlideMasterIdListEntry(cur, ctx));
            $slot = 0;
            $index = sldMasterId.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideMasterIdList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideMasterIdList', cur);
    cur.next();
  }

  return {
    sldMasterId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideMasterIdListEntry`. See `transitional/pml.xsd:890`. */
export function readCT_SlideMasterIdListEntry(cur: XmlCursor, ctx: ReadContext): CT_SlideMasterIdListEntry {
  const start = requireStart(cur, 'CT_SlideMasterIdListEntry');
  let id: ST_SlideMasterId | undefined;
  let id_relationships: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id': {
          const v = parseST_SlideMasterId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_SlideMasterIdListEntry', a, 'ST_SlideMasterId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id_relationships = v;
            continue;
          }
          ctx.invalidValue('CT_SlideMasterIdListEntry', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SlideMasterIdListEntry', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id_relationships === undefined) ctx.missingRequired('CT_SlideMasterIdListEntry', 'id', cur);

  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideMasterIdListEntry', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideMasterIdListEntry', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideMasterIdListEntry', cur);
    cur.next();
  }

  return {
    extLst,
    id,
    id_relationships,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideMasterTextStyles`. See `transitional/pml.xsd:1471`. */
export function readCT_SlideMasterTextStyles(cur: XmlCursor, ctx: ReadContext): CT_SlideMasterTextStyles {
  const start = requireStart(cur, 'CT_SlideMasterTextStyles');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SlideMasterTextStyles', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let titleStyle: CT_TextListStyle | undefined;
  let bodyStyle: CT_TextListStyle | undefined;
  let otherStyle: CT_TextListStyle | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideMasterTextStyles', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'titleStyle': {
            titleStyle = readCT_TextListStyle(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'bodyStyle': {
            bodyStyle = readCT_TextListStyle(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'otherStyle': {
            otherStyle = readCT_TextListStyle(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideMasterTextStyles', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideMasterTextStyles', cur);
    cur.next();
  }

  return {
    titleStyle,
    bodyStyle,
    otherStyle,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideRelationshipList`. See `transitional/pml.xsd:733`. */
export function readCT_SlideRelationshipList(cur: XmlCursor, ctx: ReadContext): CT_SlideRelationshipList {
  const start = requireStart(cur, 'CT_SlideRelationshipList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SlideRelationshipList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const sld: CT_SlideRelationshipListEntry[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideRelationshipList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'sld': {
            sld.push(readCT_SlideRelationshipListEntry(cur, ctx));
            $slot = 0;
            $index = sld.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideRelationshipList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideRelationshipList', cur);
    cur.next();
  }

  return {
    sld,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideRelationshipListEntry`. See `transitional/pml.xsd:730`. */
export function readCT_SlideRelationshipListEntry(cur: XmlCursor, ctx: ReadContext): CT_SlideRelationshipListEntry {
  const start = requireStart(cur, 'CT_SlideRelationshipListEntry');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_SlideRelationshipListEntry', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SlideRelationshipListEntry', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_SlideRelationshipListEntry', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideRelationshipListEntry', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SlideRelationshipListEntry', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideRelationshipListEntry', cur);
    cur.next();
  }
  return {
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideSize`. See `transitional/pml.xsd:1019`. */
export function readCT_SlideSize(cur: XmlCursor, ctx: ReadContext): CT_SlideSize {
  const start = requireStart(cur, 'CT_SlideSize');
  let cx: ST_SlideSizeCoordinate | undefined;
  let cy: ST_SlideSizeCoordinate | undefined;
  let type: ST_SlideSizeType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'cx': {
          const v = parseST_SlideSizeCoordinate(a.value);
          if (v !== undefined) {
            cx = v;
            continue;
          }
          ctx.invalidValue('CT_SlideSize', a, 'ST_SlideSizeCoordinate', cur);
          break;
        }
        case 'cy': {
          const v = parseST_SlideSizeCoordinate(a.value);
          if (v !== undefined) {
            cy = v;
            continue;
          }
          ctx.invalidValue('CT_SlideSize', a, 'ST_SlideSizeCoordinate', cur);
          break;
        }
        case 'type': {
          const v = parseST_SlideSizeType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_SlideSize', a, 'ST_SlideSizeType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SlideSize', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (cx === undefined) ctx.missingRequired('CT_SlideSize', 'cx', cur);
  if (cy === undefined) ctx.missingRequired('CT_SlideSize', 'cy', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideSize', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SlideSize', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideSize', cur);
    cur.next();
  }
  return {
    cx,
    cy,
    type,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideSorterViewProperties`. See `transitional/pml.xsd:1622`. */
export function readCT_SlideSorterViewProperties(cur: XmlCursor, ctx: ReadContext): CT_SlideSorterViewProperties {
  const start = requireStart(cur, 'CT_SlideSorterViewProperties');
  let showFormatting: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'showFormatting': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showFormatting = v;
            continue;
          }
          ctx.invalidValue('CT_SlideSorterViewProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SlideSorterViewProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cViewPr: CT_CommonViewProperties | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideSorterViewProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cViewPr': {
            cViewPr = readCT_CommonViewProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideSorterViewProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideSorterViewProperties', cur);
    cur.next();
  }

  return {
    cViewPr,
    extLst,
    showFormatting,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideSyncProperties`. See `transitional/pml.xsd:1539`. */
export function readCT_SlideSyncProperties(cur: XmlCursor, ctx: ReadContext): CT_SlideSyncProperties {
  const start = requireStart(cur, 'CT_SlideSyncProperties');
  let serverSldId: string | undefined;
  let serverSldModifiedTime: string | undefined;
  let clientInsertedTime: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'serverSldId':
          serverSldId = a.value as string;
          continue;

        case 'serverSldModifiedTime':
          serverSldModifiedTime = a.value as string;
          continue;

        case 'clientInsertedTime':
          clientInsertedTime = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_SlideSyncProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (serverSldId === undefined) ctx.missingRequired('CT_SlideSyncProperties', 'serverSldId', cur);
  if (serverSldModifiedTime === undefined) ctx.missingRequired('CT_SlideSyncProperties', 'serverSldModifiedTime', cur);
  if (clientInsertedTime === undefined) ctx.missingRequired('CT_SlideSyncProperties', 'clientInsertedTime', cur);

  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideSyncProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideSyncProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideSyncProperties', cur);
    cur.next();
  }

  return {
    extLst,
    serverSldId,
    serverSldModifiedTime,
    clientInsertedTime,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideTiming`. See `transitional/pml.xsd:706`. */
export function readCT_SlideTiming(cur: XmlCursor, ctx: ReadContext): CT_SlideTiming {
  const start = requireStart(cur, 'CT_SlideTiming');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SlideTiming', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let tnLst: CT_TimeNodeList | undefined;
  let bldLst: CT_BuildList | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideTiming', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'tnLst': {
            tnLst = readCT_TimeNodeList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'bldLst': {
            bldLst = readCT_BuildList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideTiming', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideTiming', cur);
    cur.next();
  }

  return {
    tnLst,
    bldLst,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideTransition`. See `transitional/pml.xsd:85`. */
export function readCT_SlideTransition(cur: XmlCursor, ctx: ReadContext): CT_SlideTransition {
  const start = requireStart(cur, 'CT_SlideTransition');
  let spd: ST_TransitionSpeed | undefined;
  let advClick: boolean | undefined;
  let advTm: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'spd': {
          const v = parseST_TransitionSpeed(a.value);
          if (v !== undefined) {
            spd = v;
            continue;
          }
          ctx.invalidValue('CT_SlideTransition', a, 'ST_TransitionSpeed', cur);
          break;
        }
        case 'advClick': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            advClick = v;
            continue;
          }
          ctx.invalidValue('CT_SlideTransition', a, 'xsd:boolean', cur);
          break;
        }
        case 'advTm': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            advTm = v;
            continue;
          }
          ctx.invalidValue('CT_SlideTransition', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SlideTransition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_SlideTransition_Content | undefined;
  let sndAc: CT_TransitionSoundAction | undefined;
  let extLst: CT_ExtensionListModify | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideTransition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'blinds': {
            content = { kind: 'blinds', value: readCT_OrientationTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'checker': {
            content = { kind: 'checker', value: readCT_OrientationTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'circle': {
            content = { kind: 'circle', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'dissolve': {
            content = { kind: 'dissolve', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'comb': {
            content = { kind: 'comb', value: readCT_OrientationTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cover': {
            content = { kind: 'cover', value: readCT_EightDirectionTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cut': {
            content = { kind: 'cut', value: readCT_OptionalBlackTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'diamond': {
            content = { kind: 'diamond', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'fade': {
            content = { kind: 'fade', value: readCT_OptionalBlackTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'newsflash': {
            content = { kind: 'newsflash', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'plus': {
            content = { kind: 'plus', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pull': {
            content = { kind: 'pull', value: readCT_EightDirectionTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'push': {
            content = { kind: 'push', value: readCT_SideDirectionTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'random': {
            content = { kind: 'random', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'randomBar': {
            content = { kind: 'randomBar', value: readCT_OrientationTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'split': {
            content = { kind: 'split', value: readCT_SplitTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'strips': {
            content = { kind: 'strips', value: readCT_CornerDirectionTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'wedge': {
            content = { kind: 'wedge', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'wheel': {
            content = { kind: 'wheel', value: readCT_WheelTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'wipe': {
            content = { kind: 'wipe', value: readCT_SideDirectionTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'zoom': {
            content = { kind: 'zoom', value: readCT_InOutTransition(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sndAc': {
            sndAc = readCT_TransitionSoundAction(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionListModify(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideTransition', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideTransition', cur);
    cur.next();
  }

  return {
    content,
    sndAc,
    extLst,
    spd,
    advClick,
    advTm,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SlideViewProperties`. See `transitional/pml.xsd:1647`. */
export function readCT_SlideViewProperties(cur: XmlCursor, ctx: ReadContext): CT_SlideViewProperties {
  const start = requireStart(cur, 'CT_SlideViewProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SlideViewProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cSldViewPr: CT_CommonSlideViewProperties | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SlideViewProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cSldViewPr': {
            cSldViewPr = readCT_CommonSlideViewProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SlideViewProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SlideViewProperties', cur);
    cur.next();
  }

  return {
    cSldViewPr,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SmartTags`. See `transitional/pml.xsd:945`. */
export function readCT_SmartTags(cur: XmlCursor, ctx: ReadContext): CT_SmartTags {
  const start = requireStart(cur, 'CT_SmartTags');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_SmartTags', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SmartTags', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_SmartTags', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SmartTags', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SmartTags', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SmartTags', cur);
    cur.next();
  }
  return {
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_SplitTransition`. See `transitional/pml.xsd:59`. */
export function readCT_SplitTransition(cur: XmlCursor, ctx: ReadContext): CT_SplitTransition {
  const start = requireStart(cur, 'CT_SplitTransition');
  let orient: ST_Direction | undefined;
  let dir: ST_TransitionInOutDirectionType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'orient': {
          const v = parseST_Direction(a.value);
          if (v !== undefined) {
            orient = v;
            continue;
          }
          ctx.invalidValue('CT_SplitTransition', a, 'ST_Direction', cur);
          break;
        }
        case 'dir': {
          const v = parseST_TransitionInOutDirectionType(a.value);
          if (v !== undefined) {
            dir = v;
            continue;
          }
          ctx.invalidValue('CT_SplitTransition', a, 'ST_TransitionInOutDirectionType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SplitTransition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SplitTransition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SplitTransition', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SplitTransition', cur);
    cur.next();
  }
  return {
    orient,
    dir,
    $unknownAttrs,
  };
}

/** Read a `CT_StringTag`. See `transitional/pml.xsd:1548`. */
export function readCT_StringTag(cur: XmlCursor, ctx: ReadContext): CT_StringTag {
  const start = requireStart(cur, 'CT_StringTag');
  let name: string | undefined;
  let val: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

        case 'val':
          val = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_StringTag', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_StringTag', 'name', cur);
  if (val === undefined) ctx.missingRequired('CT_StringTag', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StringTag', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_StringTag', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StringTag', cur);
    cur.next();
  }
  return {
    name,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TLAnimVariant`. See `transitional/pml.xsd:424`. */
export function readCT_TLAnimVariant(cur: XmlCursor, ctx: ReadContext): CT_TLAnimVariant {
  const start = requireStart(cur, 'CT_TLAnimVariant');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TLAnimVariant', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_TLAnimVariant_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLAnimVariant', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'boolVal': {
            content = { kind: 'boolVal', value: readCT_TLAnimVariantBooleanVal(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'intVal': {
            content = { kind: 'intVal', value: readCT_TLAnimVariantIntegerVal(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'fltVal': {
            content = { kind: 'fltVal', value: readCT_TLAnimVariantFloatVal(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'strVal': {
            content = { kind: 'strVal', value: readCT_TLAnimVariantStringVal(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'clrVal': {
            content = { kind: 'clrVal', value: readCT_Color(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLAnimVariant', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLAnimVariant', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLAnimVariantBooleanVal`. See `transitional/pml.xsd:412`. */
export function readCT_TLAnimVariantBooleanVal(cur: XmlCursor, ctx: ReadContext): CT_TLAnimVariantBooleanVal {
  const start = requireStart(cur, 'CT_TLAnimVariantBooleanVal');
  let val: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimVariantBooleanVal', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLAnimVariantBooleanVal', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TLAnimVariantBooleanVal', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLAnimVariantBooleanVal', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLAnimVariantBooleanVal', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLAnimVariantBooleanVal', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TLAnimVariantFloatVal`. See `transitional/pml.xsd:418`. */
export function readCT_TLAnimVariantFloatVal(cur: XmlCursor, ctx: ReadContext): CT_TLAnimVariantFloatVal {
  const start = requireStart(cur, 'CT_TLAnimVariantFloatVal');
  let val: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimVariantFloatVal', a, 'xsd:float', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLAnimVariantFloatVal', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TLAnimVariantFloatVal', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLAnimVariantFloatVal', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLAnimVariantFloatVal', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLAnimVariantFloatVal', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TLAnimVariantIntegerVal`. See `transitional/pml.xsd:415`. */
export function readCT_TLAnimVariantIntegerVal(cur: XmlCursor, ctx: ReadContext): CT_TLAnimVariantIntegerVal {
  const start = requireStart(cur, 'CT_TLAnimVariantIntegerVal');
  let val: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimVariantIntegerVal', a, 'xsd:int', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLAnimVariantIntegerVal', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TLAnimVariantIntegerVal', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLAnimVariantIntegerVal', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLAnimVariantIntegerVal', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLAnimVariantIntegerVal', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TLAnimVariantStringVal`. See `transitional/pml.xsd:421`. */
export function readCT_TLAnimVariantStringVal(cur: XmlCursor, ctx: ReadContext): CT_TLAnimVariantStringVal {
  const start = requireStart(cur, 'CT_TLAnimVariantStringVal');
  let val: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val':
          val = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_TLAnimVariantStringVal', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TLAnimVariantStringVal', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLAnimVariantStringVal', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLAnimVariantStringVal', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLAnimVariantStringVal', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TLAnimateBehavior`. See `transitional/pml.xsd:462`. */
export function readCT_TLAnimateBehavior(cur: XmlCursor, ctx: ReadContext): CT_TLAnimateBehavior {
  const start = requireStart(cur, 'CT_TLAnimateBehavior');
  let by: string | undefined;
  let from: string | undefined;
  let to: string | undefined;
  let calcmode: ST_TLAnimateBehaviorCalcMode | undefined;
  let valueType: ST_TLAnimateBehaviorValueType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'by':
          by = a.value as string;
          continue;

        case 'from':
          from = a.value as string;
          continue;

        case 'to':
          to = a.value as string;
          continue;

        case 'calcmode': {
          const v = parseST_TLAnimateBehaviorCalcMode(a.value);
          if (v !== undefined) {
            calcmode = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimateBehavior', a, 'ST_TLAnimateBehaviorCalcMode', cur);
          break;
        }
        case 'valueType': {
          const v = parseST_TLAnimateBehaviorValueType(a.value);
          if (v !== undefined) {
            valueType = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimateBehavior', a, 'ST_TLAnimateBehaviorValueType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLAnimateBehavior', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cBhvr: CT_TLCommonBehaviorData | undefined;
  let tavLst: CT_TLTimeAnimateValueList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLAnimateBehavior', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cBhvr': {
            cBhvr = readCT_TLCommonBehaviorData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tavLst': {
            tavLst = readCT_TLTimeAnimateValueList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLAnimateBehavior', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLAnimateBehavior', cur);
    cur.next();
  }

  return {
    cBhvr,
    tavLst,
    by,
    from,
    to,
    calcmode,
    valueType,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLAnimateColorBehavior`. See `transitional/pml.xsd:501`. */
export function readCT_TLAnimateColorBehavior(cur: XmlCursor, ctx: ReadContext): CT_TLAnimateColorBehavior {
  const start = requireStart(cur, 'CT_TLAnimateColorBehavior');
  let clrSpc: ST_TLAnimateColorSpace | undefined;
  let dir: ST_TLAnimateColorDirection | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'clrSpc': {
          const v = parseST_TLAnimateColorSpace(a.value);
          if (v !== undefined) {
            clrSpc = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimateColorBehavior', a, 'ST_TLAnimateColorSpace', cur);
          break;
        }
        case 'dir': {
          const v = parseST_TLAnimateColorDirection(a.value);
          if (v !== undefined) {
            dir = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimateColorBehavior', a, 'ST_TLAnimateColorDirection', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLAnimateColorBehavior', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cBhvr: CT_TLCommonBehaviorData | undefined;
  let by: CT_TLByAnimateColorTransform | undefined;
  let from: CT_Color | undefined;
  let to: CT_Color | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLAnimateColorBehavior', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cBhvr': {
            cBhvr = readCT_TLCommonBehaviorData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'by': {
            by = readCT_TLByAnimateColorTransform(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'from': {
            from = readCT_Color(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'to': {
            to = readCT_Color(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLAnimateColorBehavior', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLAnimateColorBehavior', cur);
    cur.next();
  }

  return {
    cBhvr,
    by,
    from,
    to,
    clrSpc,
    dir,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLAnimateEffectBehavior`. See `transitional/pml.xsd:518`. */
export function readCT_TLAnimateEffectBehavior(cur: XmlCursor, ctx: ReadContext): CT_TLAnimateEffectBehavior {
  const start = requireStart(cur, 'CT_TLAnimateEffectBehavior');
  let transition: ST_TLAnimateEffectTransition | undefined;
  let filter: string | undefined;
  let prLst: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'transition': {
          const v = parseST_TLAnimateEffectTransition(a.value);
          if (v !== undefined) {
            transition = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimateEffectBehavior', a, 'ST_TLAnimateEffectTransition', cur);
          break;
        }
        case 'filter':
          filter = a.value as string;
          continue;

        case 'prLst':
          prLst = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_TLAnimateEffectBehavior', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cBhvr: CT_TLCommonBehaviorData | undefined;
  let progress: CT_TLAnimVariant | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLAnimateEffectBehavior', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cBhvr': {
            cBhvr = readCT_TLCommonBehaviorData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'progress': {
            progress = readCT_TLAnimVariant(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLAnimateEffectBehavior', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLAnimateEffectBehavior', cur);
    cur.next();
  }

  return {
    cBhvr,
    progress,
    transition,
    filter,
    prLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLAnimateMotionBehavior`. See `transitional/pml.xsd:543`. */
export function readCT_TLAnimateMotionBehavior(cur: XmlCursor, ctx: ReadContext): CT_TLAnimateMotionBehavior {
  const start = requireStart(cur, 'CT_TLAnimateMotionBehavior');
  let origin: ST_TLAnimateMotionBehaviorOrigin | undefined;
  let path: string | undefined;
  let pathEditMode: ST_TLAnimateMotionPathEditMode | undefined;
  let rAng: ST_Angle | undefined;
  let ptsTypes: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'origin': {
          const v = parseST_TLAnimateMotionBehaviorOrigin(a.value);
          if (v !== undefined) {
            origin = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimateMotionBehavior', a, 'ST_TLAnimateMotionBehaviorOrigin', cur);
          break;
        }
        case 'path':
          path = a.value as string;
          continue;

        case 'pathEditMode': {
          const v = parseST_TLAnimateMotionPathEditMode(a.value);
          if (v !== undefined) {
            pathEditMode = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimateMotionBehavior', a, 'ST_TLAnimateMotionPathEditMode', cur);
          break;
        }
        case 'rAng': {
          const v = parseST_Angle(a.value);
          if (v !== undefined) {
            rAng = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimateMotionBehavior', a, 'ST_Angle', cur);
          break;
        }
        case 'ptsTypes':
          ptsTypes = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_TLAnimateMotionBehavior', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cBhvr: CT_TLCommonBehaviorData | undefined;
  let by: CT_TLPoint | undefined;
  let from: CT_TLPoint | undefined;
  let to: CT_TLPoint | undefined;
  let rCtr: CT_TLPoint | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLAnimateMotionBehavior', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cBhvr': {
            cBhvr = readCT_TLCommonBehaviorData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'by': {
            by = readCT_TLPoint(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'from': {
            from = readCT_TLPoint(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'to': {
            to = readCT_TLPoint(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'rCtr': {
            rCtr = readCT_TLPoint(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLAnimateMotionBehavior', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLAnimateMotionBehavior', cur);
    cur.next();
  }

  return {
    cBhvr,
    by,
    from,
    to,
    rCtr,
    origin,
    path,
    pathEditMode,
    rAng,
    ptsTypes,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLAnimateRotationBehavior`. See `transitional/pml.xsd:557`. */
export function readCT_TLAnimateRotationBehavior(cur: XmlCursor, ctx: ReadContext): CT_TLAnimateRotationBehavior {
  const start = requireStart(cur, 'CT_TLAnimateRotationBehavior');
  let by: ST_Angle | undefined;
  let from: ST_Angle | undefined;
  let to: ST_Angle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'by': {
          const v = parseST_Angle(a.value);
          if (v !== undefined) {
            by = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimateRotationBehavior', a, 'ST_Angle', cur);
          break;
        }
        case 'from': {
          const v = parseST_Angle(a.value);
          if (v !== undefined) {
            from = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimateRotationBehavior', a, 'ST_Angle', cur);
          break;
        }
        case 'to': {
          const v = parseST_Angle(a.value);
          if (v !== undefined) {
            to = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimateRotationBehavior', a, 'ST_Angle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLAnimateRotationBehavior', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cBhvr: CT_TLCommonBehaviorData | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLAnimateRotationBehavior', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cBhvr': {
            cBhvr = readCT_TLCommonBehaviorData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLAnimateRotationBehavior', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLAnimateRotationBehavior', cur);
    cur.next();
  }

  return {
    cBhvr,
    by,
    from,
    to,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLAnimateScaleBehavior`. See `transitional/pml.xsd:565`. */
export function readCT_TLAnimateScaleBehavior(cur: XmlCursor, ctx: ReadContext): CT_TLAnimateScaleBehavior {
  const start = requireStart(cur, 'CT_TLAnimateScaleBehavior');
  let zoomContents: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'zoomContents': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            zoomContents = v;
            continue;
          }
          ctx.invalidValue('CT_TLAnimateScaleBehavior', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLAnimateScaleBehavior', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cBhvr: CT_TLCommonBehaviorData | undefined;
  let by: CT_TLPoint | undefined;
  let from: CT_TLPoint | undefined;
  let to: CT_TLPoint | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLAnimateScaleBehavior', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cBhvr': {
            cBhvr = readCT_TLCommonBehaviorData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'by': {
            by = readCT_TLPoint(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'from': {
            from = readCT_TLPoint(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'to': {
            to = readCT_TLPoint(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLAnimateScaleBehavior', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLAnimateScaleBehavior', cur);
    cur.next();
  }

  return {
    cBhvr,
    by,
    from,
    to,
    zoomContents,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLBehaviorAttributeNameList`. See `transitional/pml.xsd:364`. */
export function readCT_TLBehaviorAttributeNameList(cur: XmlCursor, ctx: ReadContext): CT_TLBehaviorAttributeNameList {
  const start = requireStart(cur, 'CT_TLBehaviorAttributeNameList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TLBehaviorAttributeNameList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const attrName: string[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLBehaviorAttributeNameList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'attrName': {
            attrName.push(readScalar(cur, ctx));
            $slot = 0;
            $index = attrName.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLBehaviorAttributeNameList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLBehaviorAttributeNameList', cur);
    cur.next();
  }

  return {
    attrName,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLBuildDiagram`. See `transitional/pml.xsd:673`. */
export function readCT_TLBuildDiagram(cur: XmlCursor, ctx: ReadContext): CT_TLBuildDiagram {
  const start = requireStart(cur, 'CT_TLBuildDiagram');
  let spid: ST_DrawingElementId | undefined;
  let grpId: number | undefined;
  let uiExpand: boolean | undefined;
  let bld: ST_TLDiagramBuildType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'spid': {
          const v = parseST_DrawingElementId(a.value);
          if (v !== undefined) {
            spid = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildDiagram', a, 'ST_DrawingElementId', cur);
          break;
        }
        case 'grpId': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            grpId = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildDiagram', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'uiExpand': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            uiExpand = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildDiagram', a, 'xsd:boolean', cur);
          break;
        }
        case 'bld': {
          const v = parseST_TLDiagramBuildType(a.value);
          if (v !== undefined) {
            bld = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildDiagram', a, 'ST_TLDiagramBuildType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLBuildDiagram', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (spid === undefined) ctx.missingRequired('CT_TLBuildDiagram', 'spid', cur);
  if (grpId === undefined) ctx.missingRequired('CT_TLBuildDiagram', 'grpId', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLBuildDiagram', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLBuildDiagram', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLBuildDiagram', cur);
    cur.next();
  }
  return {
    spid,
    grpId,
    uiExpand,
    bld,
    $unknownAttrs,
  };
}

/** Read a `CT_TLBuildParagraph`. See `transitional/pml.xsd:640`. */
export function readCT_TLBuildParagraph(cur: XmlCursor, ctx: ReadContext): CT_TLBuildParagraph {
  const start = requireStart(cur, 'CT_TLBuildParagraph');
  let spid: ST_DrawingElementId | undefined;
  let grpId: number | undefined;
  let uiExpand: boolean | undefined;
  let build: ST_TLParaBuildType | undefined;
  let bldLvl: number | undefined;
  let animBg: boolean | undefined;
  let autoUpdateAnimBg: boolean | undefined;
  let rev: boolean | undefined;
  let advAuto: ST_TLTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'spid': {
          const v = parseST_DrawingElementId(a.value);
          if (v !== undefined) {
            spid = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildParagraph', a, 'ST_DrawingElementId', cur);
          break;
        }
        case 'grpId': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            grpId = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildParagraph', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'uiExpand': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            uiExpand = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildParagraph', a, 'xsd:boolean', cur);
          break;
        }
        case 'build': {
          const v = parseST_TLParaBuildType(a.value);
          if (v !== undefined) {
            build = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildParagraph', a, 'ST_TLParaBuildType', cur);
          break;
        }
        case 'bldLvl': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            bldLvl = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildParagraph', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'animBg': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            animBg = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildParagraph', a, 'xsd:boolean', cur);
          break;
        }
        case 'autoUpdateAnimBg': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            autoUpdateAnimBg = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildParagraph', a, 'xsd:boolean', cur);
          break;
        }
        case 'rev': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            rev = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildParagraph', a, 'xsd:boolean', cur);
          break;
        }
        case 'advAuto': {
          const v = parseST_TLTime(a.value);
          if (v !== undefined) {
            advAuto = v;
            continue;
          }
          ctx.invalidValue('CT_TLBuildParagraph', a, 'ST_TLTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLBuildParagraph', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (spid === undefined) ctx.missingRequired('CT_TLBuildParagraph', 'spid', cur);
  if (grpId === undefined) ctx.missingRequired('CT_TLBuildParagraph', 'grpId', cur);

  let tmplLst: CT_TLTemplateList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLBuildParagraph', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'tmplLst': {
            tmplLst = readCT_TLTemplateList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLBuildParagraph', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLBuildParagraph', cur);
    cur.next();
  }

  return {
    tmplLst,
    spid,
    grpId,
    uiExpand,
    build,
    bldLvl,
    animBg,
    autoUpdateAnimBg,
    rev,
    advAuto,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLByAnimateColorTransform`. See `transitional/pml.xsd:483`. */
export function readCT_TLByAnimateColorTransform(cur: XmlCursor, ctx: ReadContext): CT_TLByAnimateColorTransform {
  const start = requireStart(cur, 'CT_TLByAnimateColorTransform');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TLByAnimateColorTransform', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_TLByAnimateColorTransform_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLByAnimateColorTransform', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'rgb': {
            content = { kind: 'rgb', value: readCT_TLByRgbColorTransform(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hsl': {
            content = { kind: 'hsl', value: readCT_TLByHslColorTransform(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLByAnimateColorTransform', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLByAnimateColorTransform', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLByHslColorTransform`. See `transitional/pml.xsd:478`. */
export function readCT_TLByHslColorTransform(cur: XmlCursor, ctx: ReadContext): CT_TLByHslColorTransform {
  const start = requireStart(cur, 'CT_TLByHslColorTransform');
  let h: ST_Angle | undefined;
  let s: ST_FixedPercentage | undefined;
  let l: ST_FixedPercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'h': {
          const v = parseST_Angle(a.value);
          if (v !== undefined) {
            h = v;
            continue;
          }
          ctx.invalidValue('CT_TLByHslColorTransform', a, 'ST_Angle', cur);
          break;
        }
        case 's': {
          const v = parseST_FixedPercentage(a.value);
          if (v !== undefined) {
            s = v;
            continue;
          }
          ctx.invalidValue('CT_TLByHslColorTransform', a, 'ST_FixedPercentage', cur);
          break;
        }
        case 'l': {
          const v = parseST_FixedPercentage(a.value);
          if (v !== undefined) {
            l = v;
            continue;
          }
          ctx.invalidValue('CT_TLByHslColorTransform', a, 'ST_FixedPercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLByHslColorTransform', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (h === undefined) ctx.missingRequired('CT_TLByHslColorTransform', 'h', cur);
  if (s === undefined) ctx.missingRequired('CT_TLByHslColorTransform', 's', cur);
  if (l === undefined) ctx.missingRequired('CT_TLByHslColorTransform', 'l', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLByHslColorTransform', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLByHslColorTransform', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLByHslColorTransform', cur);
    cur.next();
  }
  return {
    h,
    s,
    l,
    $unknownAttrs,
  };
}

/** Read a `CT_TLByRgbColorTransform`. See `transitional/pml.xsd:473`. */
export function readCT_TLByRgbColorTransform(cur: XmlCursor, ctx: ReadContext): CT_TLByRgbColorTransform {
  const start = requireStart(cur, 'CT_TLByRgbColorTransform');
  let r: ST_FixedPercentage | undefined;
  let g: ST_FixedPercentage | undefined;
  let b: ST_FixedPercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'r': {
          const v = parseST_FixedPercentage(a.value);
          if (v !== undefined) {
            r = v;
            continue;
          }
          ctx.invalidValue('CT_TLByRgbColorTransform', a, 'ST_FixedPercentage', cur);
          break;
        }
        case 'g': {
          const v = parseST_FixedPercentage(a.value);
          if (v !== undefined) {
            g = v;
            continue;
          }
          ctx.invalidValue('CT_TLByRgbColorTransform', a, 'ST_FixedPercentage', cur);
          break;
        }
        case 'b': {
          const v = parseST_FixedPercentage(a.value);
          if (v !== undefined) {
            b = v;
            continue;
          }
          ctx.invalidValue('CT_TLByRgbColorTransform', a, 'ST_FixedPercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLByRgbColorTransform', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (r === undefined) ctx.missingRequired('CT_TLByRgbColorTransform', 'r', cur);
  if (g === undefined) ctx.missingRequired('CT_TLByRgbColorTransform', 'g', cur);
  if (b === undefined) ctx.missingRequired('CT_TLByRgbColorTransform', 'b', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLByRgbColorTransform', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLByRgbColorTransform', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLByRgbColorTransform', cur);
    cur.next();
  }
  return {
    r,
    g,
    b,
    $unknownAttrs,
  };
}

/** Read a `CT_TLCommandBehavior`. See `transitional/pml.xsd:581`. */
export function readCT_TLCommandBehavior(cur: XmlCursor, ctx: ReadContext): CT_TLCommandBehavior {
  const start = requireStart(cur, 'CT_TLCommandBehavior');
  let type: ST_TLCommandType | undefined;
  let cmd: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_TLCommandType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommandBehavior', a, 'ST_TLCommandType', cur);
          break;
        }
        case 'cmd':
          cmd = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_TLCommandBehavior', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cBhvr: CT_TLCommonBehaviorData | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLCommandBehavior', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cBhvr': {
            cBhvr = readCT_TLCommonBehaviorData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLCommandBehavior', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLCommandBehavior', cur);
    cur.next();
  }

  return {
    cBhvr,
    type,
    cmd,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLCommonBehaviorData`. See `transitional/pml.xsd:396`. */
export function readCT_TLCommonBehaviorData(cur: XmlCursor, ctx: ReadContext): CT_TLCommonBehaviorData {
  const start = requireStart(cur, 'CT_TLCommonBehaviorData');
  let additive: ST_TLBehaviorAdditiveType | undefined;
  let accumulate: ST_TLBehaviorAccumulateType | undefined;
  let xfrmType: ST_TLBehaviorTransformType | undefined;
  let from: string | undefined;
  let to: string | undefined;
  let by: string | undefined;
  let rctx: string | undefined;
  let override: ST_TLBehaviorOverrideType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'additive': {
          const v = parseST_TLBehaviorAdditiveType(a.value);
          if (v !== undefined) {
            additive = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonBehaviorData', a, 'ST_TLBehaviorAdditiveType', cur);
          break;
        }
        case 'accumulate': {
          const v = parseST_TLBehaviorAccumulateType(a.value);
          if (v !== undefined) {
            accumulate = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonBehaviorData', a, 'ST_TLBehaviorAccumulateType', cur);
          break;
        }
        case 'xfrmType': {
          const v = parseST_TLBehaviorTransformType(a.value);
          if (v !== undefined) {
            xfrmType = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonBehaviorData', a, 'ST_TLBehaviorTransformType', cur);
          break;
        }
        case 'from':
          from = a.value as string;
          continue;

        case 'to':
          to = a.value as string;
          continue;

        case 'by':
          by = a.value as string;
          continue;

        case 'rctx':
          rctx = a.value as string;
          continue;

        case 'override': {
          const v = parseST_TLBehaviorOverrideType(a.value);
          if (v !== undefined) {
            override = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonBehaviorData', a, 'ST_TLBehaviorOverrideType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLCommonBehaviorData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cTn: CT_TLCommonTimeNodeData | undefined;
  let tgtEl: CT_TLTimeTargetElement | undefined;
  let attrNameLst: CT_TLBehaviorAttributeNameList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLCommonBehaviorData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cTn': {
            cTn = readCT_TLCommonTimeNodeData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tgtEl': {
            tgtEl = readCT_TLTimeTargetElement(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'attrNameLst': {
            attrNameLst = readCT_TLBehaviorAttributeNameList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLCommonBehaviorData', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLCommonBehaviorData', cur);
    cur.next();
  }

  return {
    cTn,
    tgtEl,
    attrNameLst,
    additive,
    accumulate,
    xfrmType,
    from,
    to,
    by,
    rctx,
    override,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLCommonMediaNodeData`. See `transitional/pml.xsd:594`. */
export function readCT_TLCommonMediaNodeData(cur: XmlCursor, ctx: ReadContext): CT_TLCommonMediaNodeData {
  const start = requireStart(cur, 'CT_TLCommonMediaNodeData');
  let vol: ST_PositiveFixedPercentage | undefined;
  let mute: boolean | undefined;
  let numSld: number | undefined;
  let showWhenStopped: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'vol': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            vol = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonMediaNodeData', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
        case 'mute': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            mute = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonMediaNodeData', a, 'xsd:boolean', cur);
          break;
        }
        case 'numSld': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            numSld = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonMediaNodeData', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'showWhenStopped': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showWhenStopped = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonMediaNodeData', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLCommonMediaNodeData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cTn: CT_TLCommonTimeNodeData | undefined;
  let tgtEl: CT_TLTimeTargetElement | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLCommonMediaNodeData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cTn': {
            cTn = readCT_TLCommonTimeNodeData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tgtEl': {
            tgtEl = readCT_TLTimeTargetElement(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLCommonMediaNodeData', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLCommonMediaNodeData', cur);
    cur.next();
  }

  return {
    cTn,
    tgtEl,
    vol,
    mute,
    numSld,
    showWhenStopped,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLCommonTimeNodeData`. See `transitional/pml.xsd:299`. */
export function readCT_TLCommonTimeNodeData(cur: XmlCursor, ctx: ReadContext): CT_TLCommonTimeNodeData {
  const start = requireStart(cur, 'CT_TLCommonTimeNodeData');
  let id: ST_TLTimeNodeID | undefined;
  let presetID: number | undefined;
  let presetClass: ST_TLTimeNodePresetClassType | undefined;
  let presetSubtype: number | undefined;
  let dur: ST_TLTime | undefined;
  let repeatCount: ST_TLTime | undefined;
  let repeatDur: ST_TLTime | undefined;
  let spd: ST_Percentage | undefined;
  let accel: ST_PositiveFixedPercentage | undefined;
  let decel: ST_PositiveFixedPercentage | undefined;
  let autoRev: boolean | undefined;
  let restart: ST_TLTimeNodeRestartType | undefined;
  let fill: ST_TLTimeNodeFillType | undefined;
  let syncBehavior: ST_TLTimeNodeSyncType | undefined;
  let tmFilter: string | undefined;
  let evtFilter: string | undefined;
  let display: boolean | undefined;
  let masterRel: ST_TLTimeNodeMasterRelation | undefined;
  let bldLvl: number | undefined;
  let grpId: number | undefined;
  let afterEffect: boolean | undefined;
  let nodeType: ST_TLTimeNodeType | undefined;
  let nodePh: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id': {
          const v = parseST_TLTimeNodeID(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_TLTimeNodeID', cur);
          break;
        }
        case 'presetID': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            presetID = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'xsd:int', cur);
          break;
        }
        case 'presetClass': {
          const v = parseST_TLTimeNodePresetClassType(a.value);
          if (v !== undefined) {
            presetClass = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_TLTimeNodePresetClassType', cur);
          break;
        }
        case 'presetSubtype': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            presetSubtype = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'xsd:int', cur);
          break;
        }
        case 'dur': {
          const v = parseST_TLTime(a.value);
          if (v !== undefined) {
            dur = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_TLTime', cur);
          break;
        }
        case 'repeatCount': {
          const v = parseST_TLTime(a.value);
          if (v !== undefined) {
            repeatCount = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_TLTime', cur);
          break;
        }
        case 'repeatDur': {
          const v = parseST_TLTime(a.value);
          if (v !== undefined) {
            repeatDur = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_TLTime', cur);
          break;
        }
        case 'spd': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            spd = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_Percentage', cur);
          break;
        }
        case 'accel': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            accel = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
        case 'decel': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            decel = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
        case 'autoRev': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            autoRev = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'xsd:boolean', cur);
          break;
        }
        case 'restart': {
          const v = parseST_TLTimeNodeRestartType(a.value);
          if (v !== undefined) {
            restart = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_TLTimeNodeRestartType', cur);
          break;
        }
        case 'fill': {
          const v = parseST_TLTimeNodeFillType(a.value);
          if (v !== undefined) {
            fill = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_TLTimeNodeFillType', cur);
          break;
        }
        case 'syncBehavior': {
          const v = parseST_TLTimeNodeSyncType(a.value);
          if (v !== undefined) {
            syncBehavior = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_TLTimeNodeSyncType', cur);
          break;
        }
        case 'tmFilter':
          tmFilter = a.value as string;
          continue;

        case 'evtFilter':
          evtFilter = a.value as string;
          continue;

        case 'display': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            display = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'xsd:boolean', cur);
          break;
        }
        case 'masterRel': {
          const v = parseST_TLTimeNodeMasterRelation(a.value);
          if (v !== undefined) {
            masterRel = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_TLTimeNodeMasterRelation', cur);
          break;
        }
        case 'bldLvl': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            bldLvl = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'xsd:int', cur);
          break;
        }
        case 'grpId': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            grpId = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'afterEffect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            afterEffect = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'xsd:boolean', cur);
          break;
        }
        case 'nodeType': {
          const v = parseST_TLTimeNodeType(a.value);
          if (v !== undefined) {
            nodeType = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'ST_TLTimeNodeType', cur);
          break;
        }
        case 'nodePh': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            nodePh = v;
            continue;
          }
          ctx.invalidValue('CT_TLCommonTimeNodeData', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLCommonTimeNodeData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let stCondLst: CT_TLTimeConditionList | undefined;
  let endCondLst: CT_TLTimeConditionList | undefined;
  let endSync: CT_TLTimeCondition | undefined;
  let iterate: CT_TLIterateData | undefined;
  let childTnLst: CT_TimeNodeList | undefined;
  let subTnLst: CT_TimeNodeList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLCommonTimeNodeData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'stCondLst': {
            stCondLst = readCT_TLTimeConditionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'endCondLst': {
            endCondLst = readCT_TLTimeConditionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'endSync': {
            endSync = readCT_TLTimeCondition(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'iterate': {
            iterate = readCT_TLIterateData(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'childTnLst': {
            childTnLst = readCT_TimeNodeList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'subTnLst': {
            subTnLst = readCT_TimeNodeList(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLCommonTimeNodeData', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLCommonTimeNodeData', cur);
    cur.next();
  }

  return {
    stCondLst,
    endCondLst,
    endSync,
    iterate,
    childTnLst,
    subTnLst,
    id,
    presetID,
    presetClass,
    presetSubtype,
    dur,
    repeatCount,
    repeatDur,
    spd,
    accel,
    decel,
    autoRev,
    restart,
    fill,
    syncBehavior,
    tmFilter,
    evtFilter,
    display,
    masterRel,
    bldLvl,
    grpId,
    afterEffect,
    nodeType,
    nodePh,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLGraphicalObjectBuild`. See `transitional/pml.xsd:691`. */
export function readCT_TLGraphicalObjectBuild(cur: XmlCursor, ctx: ReadContext): CT_TLGraphicalObjectBuild {
  const start = requireStart(cur, 'CT_TLGraphicalObjectBuild');
  let spid: ST_DrawingElementId | undefined;
  let grpId: number | undefined;
  let uiExpand: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'spid': {
          const v = parseST_DrawingElementId(a.value);
          if (v !== undefined) {
            spid = v;
            continue;
          }
          ctx.invalidValue('CT_TLGraphicalObjectBuild', a, 'ST_DrawingElementId', cur);
          break;
        }
        case 'grpId': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            grpId = v;
            continue;
          }
          ctx.invalidValue('CT_TLGraphicalObjectBuild', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'uiExpand': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            uiExpand = v;
            continue;
          }
          ctx.invalidValue('CT_TLGraphicalObjectBuild', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLGraphicalObjectBuild', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (spid === undefined) ctx.missingRequired('CT_TLGraphicalObjectBuild', 'spid', cur);
  if (grpId === undefined) ctx.missingRequired('CT_TLGraphicalObjectBuild', 'grpId', cur);

  let content: CT_TLGraphicalObjectBuild_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLGraphicalObjectBuild', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'bldAsOne': {
            content = { kind: 'bldAsOne', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'bldSub': {
            content = { kind: 'bldSub', value: readCT_AnimationGraphicalObjectBuildProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLGraphicalObjectBuild', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLGraphicalObjectBuild', cur);
    cur.next();
  }

  return {
    content,
    spid,
    grpId,
    uiExpand,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLIterateData`. See `transitional/pml.xsd:141`. */
export function readCT_TLIterateData(cur: XmlCursor, ctx: ReadContext): CT_TLIterateData {
  const start = requireStart(cur, 'CT_TLIterateData');
  let type: ST_IterateType | undefined;
  let backwards: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_IterateType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_TLIterateData', a, 'ST_IterateType', cur);
          break;
        }
        case 'backwards': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            backwards = v;
            continue;
          }
          ctx.invalidValue('CT_TLIterateData', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLIterateData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_TLIterateData_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLIterateData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'tmAbs': {
            content = { kind: 'tmAbs', value: readCT_TLIterateIntervalTime(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tmPct': {
            content = { kind: 'tmPct', value: readCT_TLIterateIntervalPercentage(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLIterateData', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLIterateData', cur);
    cur.next();
  }

  return {
    content,
    type,
    backwards,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLIterateIntervalPercentage`. See `transitional/pml.xsd:131`. */
export function readCT_TLIterateIntervalPercentage(cur: XmlCursor, ctx: ReadContext): CT_TLIterateIntervalPercentage {
  const start = requireStart(cur, 'CT_TLIterateIntervalPercentage');
  let val: ST_PositivePercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_PositivePercentage(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TLIterateIntervalPercentage', a, 'ST_PositivePercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLIterateIntervalPercentage', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TLIterateIntervalPercentage', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLIterateIntervalPercentage', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLIterateIntervalPercentage', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLIterateIntervalPercentage', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TLIterateIntervalTime`. See `transitional/pml.xsd:128`. */
export function readCT_TLIterateIntervalTime(cur: XmlCursor, ctx: ReadContext): CT_TLIterateIntervalTime {
  const start = requireStart(cur, 'CT_TLIterateIntervalTime');
  let val: ST_TLTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TLTime(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TLIterateIntervalTime', a, 'ST_TLTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLIterateIntervalTime', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TLIterateIntervalTime', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLIterateIntervalTime', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLIterateIntervalTime', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLIterateIntervalTime', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TLMediaNodeAudio`. See `transitional/pml.xsd:604`. */
export function readCT_TLMediaNodeAudio(cur: XmlCursor, ctx: ReadContext): CT_TLMediaNodeAudio {
  const start = requireStart(cur, 'CT_TLMediaNodeAudio');
  let isNarration: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'isNarration': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            isNarration = v;
            continue;
          }
          ctx.invalidValue('CT_TLMediaNodeAudio', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLMediaNodeAudio', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cMediaNode: CT_TLCommonMediaNodeData | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLMediaNodeAudio', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cMediaNode': {
            cMediaNode = readCT_TLCommonMediaNodeData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLMediaNodeAudio', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLMediaNodeAudio', cur);
    cur.next();
  }

  return {
    cMediaNode,
    isNarration,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLMediaNodeVideo`. See `transitional/pml.xsd:610`. */
export function readCT_TLMediaNodeVideo(cur: XmlCursor, ctx: ReadContext): CT_TLMediaNodeVideo {
  const start = requireStart(cur, 'CT_TLMediaNodeVideo');
  let fullScrn: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'fullScrn': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fullScrn = v;
            continue;
          }
          ctx.invalidValue('CT_TLMediaNodeVideo', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLMediaNodeVideo', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cMediaNode: CT_TLCommonMediaNodeData | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLMediaNodeVideo', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cMediaNode': {
            cMediaNode = readCT_TLCommonMediaNodeData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLMediaNodeVideo', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLMediaNodeVideo', cur);
    cur.next();
  }

  return {
    cMediaNode,
    fullScrn,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLOleBuildChart`. See `transitional/pml.xsd:686`. */
export function readCT_TLOleBuildChart(cur: XmlCursor, ctx: ReadContext): CT_TLOleBuildChart {
  const start = requireStart(cur, 'CT_TLOleBuildChart');
  let spid: ST_DrawingElementId | undefined;
  let grpId: number | undefined;
  let uiExpand: boolean | undefined;
  let bld: ST_TLOleChartBuildType | undefined;
  let animBg: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'spid': {
          const v = parseST_DrawingElementId(a.value);
          if (v !== undefined) {
            spid = v;
            continue;
          }
          ctx.invalidValue('CT_TLOleBuildChart', a, 'ST_DrawingElementId', cur);
          break;
        }
        case 'grpId': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            grpId = v;
            continue;
          }
          ctx.invalidValue('CT_TLOleBuildChart', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'uiExpand': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            uiExpand = v;
            continue;
          }
          ctx.invalidValue('CT_TLOleBuildChart', a, 'xsd:boolean', cur);
          break;
        }
        case 'bld': {
          const v = parseST_TLOleChartBuildType(a.value);
          if (v !== undefined) {
            bld = v;
            continue;
          }
          ctx.invalidValue('CT_TLOleBuildChart', a, 'ST_TLOleChartBuildType', cur);
          break;
        }
        case 'animBg': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            animBg = v;
            continue;
          }
          ctx.invalidValue('CT_TLOleBuildChart', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLOleBuildChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (spid === undefined) ctx.missingRequired('CT_TLOleBuildChart', 'spid', cur);
  if (grpId === undefined) ctx.missingRequired('CT_TLOleBuildChart', 'grpId', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLOleBuildChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLOleBuildChart', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLOleBuildChart', cur);
    cur.next();
  }
  return {
    spid,
    grpId,
    uiExpand,
    bld,
    animBg,
    $unknownAttrs,
  };
}

/** Read a `CT_TLOleChartTargetElement`. See `transitional/pml.xsd:167`. */
export function readCT_TLOleChartTargetElement(cur: XmlCursor, ctx: ReadContext): CT_TLOleChartTargetElement {
  const start = requireStart(cur, 'CT_TLOleChartTargetElement');
  let type: ST_TLChartSubelementType | undefined;
  let lvl: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_TLChartSubelementType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_TLOleChartTargetElement', a, 'ST_TLChartSubelementType', cur);
          break;
        }
        case 'lvl': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            lvl = v;
            continue;
          }
          ctx.invalidValue('CT_TLOleChartTargetElement', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLOleChartTargetElement', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (type === undefined) ctx.missingRequired('CT_TLOleChartTargetElement', 'type', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLOleChartTargetElement', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLOleChartTargetElement', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLOleChartTargetElement', cur);
    cur.next();
  }
  return {
    type,
    lvl,
    $unknownAttrs,
  };
}

/** Read a `CT_TLPoint`. See `transitional/pml.xsd:539`. */
export function readCT_TLPoint(cur: XmlCursor, ctx: ReadContext): CT_TLPoint {
  const start = requireStart(cur, 'CT_TLPoint');
  let x: ST_Percentage | undefined;
  let y: ST_Percentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'x': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            x = v;
            continue;
          }
          ctx.invalidValue('CT_TLPoint', a, 'ST_Percentage', cur);
          break;
        }
        case 'y': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            y = v;
            continue;
          }
          ctx.invalidValue('CT_TLPoint', a, 'ST_Percentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLPoint', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (x === undefined) ctx.missingRequired('CT_TLPoint', 'x', cur);
  if (y === undefined) ctx.missingRequired('CT_TLPoint', 'y', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLPoint', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLPoint', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLPoint', cur);
    cur.next();
  }
  return {
    x,
    y,
    $unknownAttrs,
  };
}

/** Read a `CT_TLSetBehavior`. See `transitional/pml.xsd:588`. */
export function readCT_TLSetBehavior(cur: XmlCursor, ctx: ReadContext): CT_TLSetBehavior {
  const start = requireStart(cur, 'CT_TLSetBehavior');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TLSetBehavior', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cBhvr: CT_TLCommonBehaviorData | undefined;
  let to: CT_TLAnimVariant | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLSetBehavior', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cBhvr': {
            cBhvr = readCT_TLCommonBehaviorData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'to': {
            to = readCT_TLAnimVariant(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLSetBehavior', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLSetBehavior', cur);
    cur.next();
  }

  return {
    cBhvr,
    to,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLShapeTargetElement`. See `transitional/pml.xsd:171`. */
export function readCT_TLShapeTargetElement(cur: XmlCursor, ctx: ReadContext): CT_TLShapeTargetElement {
  const start = requireStart(cur, 'CT_TLShapeTargetElement');
  let spid: ST_DrawingElementId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'spid': {
          const v = parseST_DrawingElementId(a.value);
          if (v !== undefined) {
            spid = v;
            continue;
          }
          ctx.invalidValue('CT_TLShapeTargetElement', a, 'ST_DrawingElementId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLShapeTargetElement', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (spid === undefined) ctx.missingRequired('CT_TLShapeTargetElement', 'spid', cur);

  let content: CT_TLShapeTargetElement_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLShapeTargetElement', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'bg': {
            content = { kind: 'bg', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'subSp': {
            content = { kind: 'subSp', value: readCT_TLSubShapeId(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'oleChartEl': {
            content = { kind: 'oleChartEl', value: readCT_TLOleChartTargetElement(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'txEl': {
            content = { kind: 'txEl', value: readCT_TLTextTargetElement(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'graphicEl': {
            content = { kind: 'graphicEl', value: readCT_AnimationElementChoice(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLShapeTargetElement', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLShapeTargetElement', cur);
    cur.next();
  }

  return {
    content,
    spid,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLSubShapeId`. See `transitional/pml.xsd:149`. */
export function readCT_TLSubShapeId(cur: XmlCursor, ctx: ReadContext): CT_TLSubShapeId {
  const start = requireStart(cur, 'CT_TLSubShapeId');
  let spid: ST_ShapeID | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'spid': {
          const v = parseST_ShapeID(a.value);
          if (v !== undefined) {
            spid = v;
            continue;
          }
          ctx.invalidValue('CT_TLSubShapeId', a, 'ST_ShapeID', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLSubShapeId', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (spid === undefined) ctx.missingRequired('CT_TLSubShapeId', 'spid', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLSubShapeId', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLSubShapeId', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLSubShapeId', cur);
    cur.next();
  }
  return {
    spid,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTemplate`. See `transitional/pml.xsd:621`. */
export function readCT_TLTemplate(cur: XmlCursor, ctx: ReadContext): CT_TLTemplate {
  const start = requireStart(cur, 'CT_TLTemplate');
  let lvl: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'lvl': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            lvl = v;
            continue;
          }
          ctx.invalidValue('CT_TLTemplate', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLTemplate', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let tnLst: CT_TimeNodeList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTemplate', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'tnLst': {
            tnLst = readCT_TimeNodeList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLTemplate', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTemplate', cur);
    cur.next();
  }

  return {
    tnLst,
    lvl,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTemplateList`. See `transitional/pml.xsd:627`. */
export function readCT_TLTemplateList(cur: XmlCursor, ctx: ReadContext): CT_TLTemplateList {
  const start = requireStart(cur, 'CT_TLTemplateList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TLTemplateList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const tmpl: CT_TLTemplate[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTemplateList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'tmpl': {
            tmpl.push(readCT_TLTemplate(cur, ctx));
            $slot = 0;
            $index = tmpl.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLTemplateList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTemplateList', cur);
    cur.next();
  }

  return {
    tmpl,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTextTargetElement`. See `transitional/pml.xsd:152`. */
export function readCT_TLTextTargetElement(cur: XmlCursor, ctx: ReadContext): CT_TLTextTargetElement {
  const start = requireStart(cur, 'CT_TLTextTargetElement');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TLTextTargetElement', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_TLTextTargetElement_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTextTargetElement', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'charRg': {
            content = { kind: 'charRg', value: readCT_IndexRange(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pRg': {
            content = { kind: 'pRg', value: readCT_IndexRange(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLTextTargetElement', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTextTargetElement', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTimeAnimateValue`. See `transitional/pml.xsd:436`. */
export function readCT_TLTimeAnimateValue(cur: XmlCursor, ctx: ReadContext): CT_TLTimeAnimateValue {
  const start = requireStart(cur, 'CT_TLTimeAnimateValue');
  let tm: ST_TLTimeAnimateValueTime | undefined;
  let fmla: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'tm': {
          const v = parseST_TLTimeAnimateValueTime(a.value);
          if (v !== undefined) {
            tm = v;
            continue;
          }
          ctx.invalidValue('CT_TLTimeAnimateValue', a, 'ST_TLTimeAnimateValueTime', cur);
          break;
        }
        case 'fmla':
          fmla = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_TLTimeAnimateValue', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let val: CT_TLAnimVariant | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTimeAnimateValue', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'val': {
            val = readCT_TLAnimVariant(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLTimeAnimateValue', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTimeAnimateValue', cur);
    cur.next();
  }

  return {
    val,
    tm,
    fmla,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTimeAnimateValueList`. See `transitional/pml.xsd:443`. */
export function readCT_TLTimeAnimateValueList(cur: XmlCursor, ctx: ReadContext): CT_TLTimeAnimateValueList {
  const start = requireStart(cur, 'CT_TLTimeAnimateValueList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TLTimeAnimateValueList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const tav: CT_TLTimeAnimateValue[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTimeAnimateValueList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'tav': {
            tav.push(readCT_TLTimeAnimateValue(cur, ctx));
            $slot = 0;
            $index = tav.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLTimeAnimateValueList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTimeAnimateValueList', cur);
    cur.next();
  }

  return {
    tav,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTimeCondition`. See `transitional/pml.xsd:217`. */
export function readCT_TLTimeCondition(cur: XmlCursor, ctx: ReadContext): CT_TLTimeCondition {
  const start = requireStart(cur, 'CT_TLTimeCondition');
  let evt: ST_TLTriggerEvent | undefined;
  let delay: ST_TLTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'evt': {
          const v = parseST_TLTriggerEvent(a.value);
          if (v !== undefined) {
            evt = v;
            continue;
          }
          ctx.invalidValue('CT_TLTimeCondition', a, 'ST_TLTriggerEvent', cur);
          break;
        }
        case 'delay': {
          const v = parseST_TLTime(a.value);
          if (v !== undefined) {
            delay = v;
            continue;
          }
          ctx.invalidValue('CT_TLTimeCondition', a, 'ST_TLTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLTimeCondition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_TLTimeCondition_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTimeCondition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'tgtEl': {
            content = { kind: 'tgtEl', value: readCT_TLTimeTargetElement(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tn': {
            content = { kind: 'tn', value: readCT_TLTriggerTimeNodeID(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'rtn': {
            content = { kind: 'rtn', value: readCT_TLTriggerRuntimeNode(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLTimeCondition', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTimeCondition', cur);
    cur.next();
  }

  return {
    content,
    evt,
    delay,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTimeConditionList`. See `transitional/pml.xsd:226`. */
export function readCT_TLTimeConditionList(cur: XmlCursor, ctx: ReadContext): CT_TLTimeConditionList {
  const start = requireStart(cur, 'CT_TLTimeConditionList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TLTimeConditionList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const cond: CT_TLTimeCondition[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTimeConditionList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cond': {
            cond.push(readCT_TLTimeCondition(cur, ctx));
            $slot = 0;
            $index = cond.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLTimeConditionList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTimeConditionList', cur);
    cur.next();
  }

  return {
    cond,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTimeNodeExclusive`. See `transitional/pml.xsd:359`. */
export function readCT_TLTimeNodeExclusive(cur: XmlCursor, ctx: ReadContext): CT_TLTimeNodeExclusive {
  const start = requireStart(cur, 'CT_TLTimeNodeExclusive');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TLTimeNodeExclusive', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cTn: CT_TLCommonTimeNodeData | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTimeNodeExclusive', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cTn': {
            cTn = readCT_TLCommonTimeNodeData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLTimeNodeExclusive', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTimeNodeExclusive', cur);
    cur.next();
  }

  return {
    cTn,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTimeNodeParallel`. See `transitional/pml.xsd:332`. */
export function readCT_TLTimeNodeParallel(cur: XmlCursor, ctx: ReadContext): CT_TLTimeNodeParallel {
  const start = requireStart(cur, 'CT_TLTimeNodeParallel');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TLTimeNodeParallel', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cTn: CT_TLCommonTimeNodeData | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTimeNodeParallel', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cTn': {
            cTn = readCT_TLCommonTimeNodeData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLTimeNodeParallel', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTimeNodeParallel', cur);
    cur.next();
  }

  return {
    cTn,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTimeNodeSequence`. See `transitional/pml.xsd:349`. */
export function readCT_TLTimeNodeSequence(cur: XmlCursor, ctx: ReadContext): CT_TLTimeNodeSequence {
  const start = requireStart(cur, 'CT_TLTimeNodeSequence');
  let concurrent: boolean | undefined;
  let prevAc: ST_TLPreviousActionType | undefined;
  let nextAc: ST_TLNextActionType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'concurrent': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            concurrent = v;
            continue;
          }
          ctx.invalidValue('CT_TLTimeNodeSequence', a, 'xsd:boolean', cur);
          break;
        }
        case 'prevAc': {
          const v = parseST_TLPreviousActionType(a.value);
          if (v !== undefined) {
            prevAc = v;
            continue;
          }
          ctx.invalidValue('CT_TLTimeNodeSequence', a, 'ST_TLPreviousActionType', cur);
          break;
        }
        case 'nextAc': {
          const v = parseST_TLNextActionType(a.value);
          if (v !== undefined) {
            nextAc = v;
            continue;
          }
          ctx.invalidValue('CT_TLTimeNodeSequence', a, 'ST_TLNextActionType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLTimeNodeSequence', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cTn: CT_TLCommonTimeNodeData | undefined;
  let prevCondLst: CT_TLTimeConditionList | undefined;
  let nextCondLst: CT_TLTimeConditionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTimeNodeSequence', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'cTn': {
            cTn = readCT_TLCommonTimeNodeData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prevCondLst': {
            prevCondLst = readCT_TLTimeConditionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'nextCondLst': {
            nextCondLst = readCT_TLTimeConditionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLTimeNodeSequence', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTimeNodeSequence', cur);
    cur.next();
  }

  return {
    cTn,
    prevCondLst,
    nextCondLst,
    concurrent,
    prevAc,
    nextAc,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTimeTargetElement`. See `transitional/pml.xsd:181`. */
export function readCT_TLTimeTargetElement(cur: XmlCursor, ctx: ReadContext): CT_TLTimeTargetElement {
  const start = requireStart(cur, 'CT_TLTimeTargetElement');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TLTimeTargetElement', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_TLTimeTargetElement_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTimeTargetElement', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'sldTgt': {
            content = { kind: 'sldTgt', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sndTgt': {
            content = { kind: 'sndTgt', value: readCT_EmbeddedWAVAudioFile(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spTgt': {
            content = { kind: 'spTgt', value: readCT_TLShapeTargetElement(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'inkTgt': {
            content = { kind: 'inkTgt', value: readCT_TLSubShapeId(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TLTimeTargetElement', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTimeTargetElement', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTriggerRuntimeNode`. See `transitional/pml.xsd:199`. */
export function readCT_TLTriggerRuntimeNode(cur: XmlCursor, ctx: ReadContext): CT_TLTriggerRuntimeNode {
  const start = requireStart(cur, 'CT_TLTriggerRuntimeNode');
  let val: ST_TLTriggerRuntimeNode | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TLTriggerRuntimeNode(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TLTriggerRuntimeNode', a, 'ST_TLTriggerRuntimeNode', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLTriggerRuntimeNode', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TLTriggerRuntimeNode', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTriggerRuntimeNode', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLTriggerRuntimeNode', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTriggerRuntimeNode', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TLTriggerTimeNodeID`. See `transitional/pml.xsd:189`. */
export function readCT_TLTriggerTimeNodeID(cur: XmlCursor, ctx: ReadContext): CT_TLTriggerTimeNodeID {
  const start = requireStart(cur, 'CT_TLTriggerTimeNodeID');
  let val: ST_TLTimeNodeID | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TLTimeNodeID(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TLTriggerTimeNodeID', a, 'ST_TLTimeNodeID', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TLTriggerTimeNodeID', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TLTriggerTimeNodeID', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TLTriggerTimeNodeID', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TLTriggerTimeNodeID', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TLTriggerTimeNodeID', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TagList`. See `transitional/pml.xsd:1552`. */
export function readCT_TagList(cur: XmlCursor, ctx: ReadContext): CT_TagList {
  const start = requireStart(cur, 'CT_TagList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TagList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const tag: CT_StringTag[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TagList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'tag': {
            tag.push(readCT_StringTag(cur, ctx));
            $slot = 0;
            $index = tag.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TagList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TagList', cur);
    cur.next();
  }

  return {
    tag,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TagsData`. See `transitional/pml.xsd:752`. */
export function readCT_TagsData(cur: XmlCursor, ctx: ReadContext): CT_TagsData {
  const start = requireStart(cur, 'CT_TagsData');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TagsData', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TagsData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_TagsData', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TagsData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TagsData', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TagsData', cur);
    cur.next();
  }
  return {
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_TimeNodeList`. See `transitional/pml.xsd:231`. */
export function readCT_TimeNodeList(cur: XmlCursor, ctx: ReadContext): CT_TimeNodeList {
  const start = requireStart(cur, 'CT_TimeNodeList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TimeNodeList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_TimeNodeList_Content[] = [];

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TimeNodeList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'par': {
            content.push({ kind: 'par', value: readCT_TLTimeNodeParallel(cur, ctx) });
            continue;
          }
          case 'seq': {
            content.push({ kind: 'seq', value: readCT_TLTimeNodeSequence(cur, ctx) });
            continue;
          }
          case 'excl': {
            content.push({ kind: 'excl', value: readCT_TLTimeNodeExclusive(cur, ctx) });
            continue;
          }
          case 'anim': {
            content.push({ kind: 'anim', value: readCT_TLAnimateBehavior(cur, ctx) });
            continue;
          }
          case 'animClr': {
            content.push({ kind: 'animClr', value: readCT_TLAnimateColorBehavior(cur, ctx) });
            continue;
          }
          case 'animEffect': {
            content.push({ kind: 'animEffect', value: readCT_TLAnimateEffectBehavior(cur, ctx) });
            continue;
          }
          case 'animMotion': {
            content.push({ kind: 'animMotion', value: readCT_TLAnimateMotionBehavior(cur, ctx) });
            continue;
          }
          case 'animRot': {
            content.push({ kind: 'animRot', value: readCT_TLAnimateRotationBehavior(cur, ctx) });
            continue;
          }
          case 'animScale': {
            content.push({ kind: 'animScale', value: readCT_TLAnimateScaleBehavior(cur, ctx) });
            continue;
          }
          case 'cmd': {
            content.push({ kind: 'cmd', value: readCT_TLCommandBehavior(cur, ctx) });
            continue;
          }
          case 'set': {
            content.push({ kind: 'set', value: readCT_TLSetBehavior(cur, ctx) });
            continue;
          }
          case 'audio': {
            content.push({ kind: 'audio', value: readCT_TLMediaNodeAudio(cur, ctx) });
            continue;
          }
          case 'video': {
            content.push({ kind: 'video', value: readCT_TLMediaNodeVideo(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TimeNodeList', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TimeNodeList', cur);
    cur.next();
  }

  return {
    content,
    $unknownAttrs,
  };
}

/** Read a `CT_TransitionSoundAction`. See `transitional/pml.xsd:72`. */
export function readCT_TransitionSoundAction(cur: XmlCursor, ctx: ReadContext): CT_TransitionSoundAction {
  const start = requireStart(cur, 'CT_TransitionSoundAction');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TransitionSoundAction', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_TransitionSoundAction_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TransitionSoundAction', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'stSnd': {
            content = { kind: 'stSnd', value: readCT_TransitionStartSoundAction(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'endSnd': {
            content = { kind: 'endSnd', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TransitionSoundAction', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TransitionSoundAction', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TransitionStartSoundAction`. See `transitional/pml.xsd:66`. */
export function readCT_TransitionStartSoundAction(cur: XmlCursor, ctx: ReadContext): CT_TransitionStartSoundAction {
  const start = requireStart(cur, 'CT_TransitionStartSoundAction');
  let loop: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'loop': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            loop = v;
            continue;
          }
          ctx.invalidValue('CT_TransitionStartSoundAction', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TransitionStartSoundAction', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let snd: CT_EmbeddedWAVAudioFile | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TransitionStartSoundAction', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'snd': {
            snd = readCT_EmbeddedWAVAudioFile(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TransitionStartSoundAction', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TransitionStartSoundAction', cur);
    cur.next();
  }

  return {
    snd,
    loop,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ViewProperties`. See `transitional/pml.xsd:1659`. */
export function readCT_ViewProperties(cur: XmlCursor, ctx: ReadContext): CT_ViewProperties {
  const start = requireStart(cur, 'CT_ViewProperties');
  let lastView: ST_ViewType | undefined;
  let showComments: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'lastView': {
          const v = parseST_ViewType(a.value);
          if (v !== undefined) {
            lastView = v;
            continue;
          }
          ctx.invalidValue('CT_ViewProperties', a, 'ST_ViewType', cur);
          break;
        }
        case 'showComments': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showComments = v;
            continue;
          }
          ctx.invalidValue('CT_ViewProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ViewProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let normalViewPr: CT_NormalViewProperties | undefined;
  let slideViewPr: CT_SlideViewProperties | undefined;
  let outlineViewPr: CT_OutlineViewProperties | undefined;
  let notesTextViewPr: CT_NotesTextViewProperties | undefined;
  let sorterViewPr: CT_SlideSorterViewProperties | undefined;
  let notesViewPr: CT_NotesViewProperties | undefined;
  let gridSpacing: CT_PositiveSize2D | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ViewProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'normalViewPr': {
            normalViewPr = readCT_NormalViewProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'slideViewPr': {
            slideViewPr = readCT_SlideViewProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'outlineViewPr': {
            outlineViewPr = readCT_OutlineViewProperties(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'notesTextViewPr': {
            notesTextViewPr = readCT_NotesTextViewProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'sorterViewPr': {
            sorterViewPr = readCT_SlideSorterViewProperties(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'notesViewPr': {
            notesViewPr = readCT_NotesViewProperties(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'gridSpacing': {
            gridSpacing = readCT_PositiveSize2D(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ViewProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ViewProperties', cur);
    cur.next();
  }

  return {
    normalViewPr,
    slideViewPr,
    outlineViewPr,
    notesTextViewPr,
    sorterViewPr,
    notesViewPr,
    gridSpacing,
    extLst,
    lastView,
    showComments,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_WebProperties`. See `transitional/pml.xsd:1124`. */
export function readCT_WebProperties(cur: XmlCursor, ctx: ReadContext): CT_WebProperties {
  const start = requireStart(cur, 'CT_WebProperties');
  let showAnimation: boolean | undefined;
  let resizeGraphics: boolean | undefined;
  let allowPng: boolean | undefined;
  let relyOnVml: boolean | undefined;
  let organizeInFolders: boolean | undefined;
  let useLongFilenames: boolean | undefined;
  let imgSz: ST_WebScreenSize | undefined;
  let encoding: ST_WebEncoding | undefined;
  let clr: ST_WebColorType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'showAnimation': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            showAnimation = v;
            continue;
          }
          ctx.invalidValue('CT_WebProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'resizeGraphics': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            resizeGraphics = v;
            continue;
          }
          ctx.invalidValue('CT_WebProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'allowPng': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            allowPng = v;
            continue;
          }
          ctx.invalidValue('CT_WebProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'relyOnVml': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            relyOnVml = v;
            continue;
          }
          ctx.invalidValue('CT_WebProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'organizeInFolders': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            organizeInFolders = v;
            continue;
          }
          ctx.invalidValue('CT_WebProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'useLongFilenames': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            useLongFilenames = v;
            continue;
          }
          ctx.invalidValue('CT_WebProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'imgSz': {
          const v = parseST_WebScreenSize(a.value);
          if (v !== undefined) {
            imgSz = v;
            continue;
          }
          ctx.invalidValue('CT_WebProperties', a, 'ST_WebScreenSize', cur);
          break;
        }
        case 'encoding': {
          const v = parseST_WebEncoding(a.value);
          if (v !== undefined) {
            encoding = v;
            continue;
          }
          ctx.invalidValue('CT_WebProperties', a, 'ST_WebEncoding', cur);
          break;
        }
        case 'clr': {
          const v = parseST_WebColorType(a.value);
          if (v !== undefined) {
            clr = v;
            continue;
          }
          ctx.invalidValue('CT_WebProperties', a, 'ST_WebColorType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_WebProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_pml = ctx.uris['pml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WebProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_pml) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WebProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WebProperties', cur);
    cur.next();
  }

  return {
    extLst,
    showAnimation,
    resizeGraphics,
    allowPng,
    relyOnVml,
    organizeInFolders,
    useLongFilenames,
    imgSz,
    encoding,
    clr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_WheelTransition`. See `transitional/pml.xsd:63`. */
export function readCT_WheelTransition(cur: XmlCursor, ctx: ReadContext): CT_WheelTransition {
  const start = requireStart(cur, 'CT_WheelTransition');
  let spokes: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'spokes': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            spokes = v;
            continue;
          }
          ctx.invalidValue('CT_WheelTransition', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_WheelTransition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WheelTransition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_WheelTransition', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WheelTransition', cur);
    cur.next();
  }
  return {
    spokes,
    $unknownAttrs,
  };
}
