/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: writers for the pml namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  PositionedRawQueue,
  type WriteContext,
  type XmlSink,
  uriFor,
} from '../../runtime/index.js';
import {
  writeCT_AnimationElementChoice,
  writeCT_AnimationGraphicalObjectBuildProperties,
  writeCT_AudioCD,
  writeCT_AudioFile,
  writeCT_BlipFillProperties,
  writeCT_Color,
  writeCT_ColorMRU,
  writeCT_ColorMapping,
  writeCT_ColorMappingOverride,
  writeCT_EffectContainer,
  writeCT_EffectList,
  writeCT_EmbeddedWAVAudioFile,
  writeCT_GradientFillProperties,
  writeCT_GraphicalObject,
  writeCT_GroupFillProperties,
  writeCT_GroupShapeProperties,
  writeCT_NoFillProperties,
  writeCT_NonVisualConnectorProperties,
  writeCT_NonVisualDrawingProps,
  writeCT_NonVisualDrawingShapeProps,
  writeCT_NonVisualGraphicFrameProperties,
  writeCT_NonVisualGroupDrawingShapeProps,
  writeCT_NonVisualPictureProperties,
  writeCT_PatternFillProperties,
  writeCT_Point2D,
  writeCT_PositiveSize2D,
  writeCT_QuickTimeFile,
  writeCT_Scale2D,
  writeCT_ShapeProperties,
  writeCT_ShapeStyle,
  writeCT_SolidColorFillProperties,
  writeCT_StyleMatrixReference,
  writeCT_TextBody,
  writeCT_TextFont,
  writeCT_TextListStyle,
  writeCT_Transform2D,
  writeCT_VideoFile,
} from '../dml-main/writer.js';
import type {
  CT_ApplicationNonVisualDrawingProps,
  CT_Background,
  CT_BackgroundProperties,
  CT_BuildList,
  CT_Comment,
  CT_CommentAuthor,
  CT_CommentAuthorList,
  CT_CommentList,
  CT_CommonSlideData,
  CT_CommonSlideViewProperties,
  CT_CommonViewProperties,
  CT_Connector,
  CT_ConnectorNonVisual,
  CT_Control,
  CT_ControlList,
  CT_CornerDirectionTransition,
  CT_CustomShow,
  CT_CustomShowId,
  CT_CustomShowList,
  CT_CustomerData,
  CT_CustomerDataList,
  CT_EightDirectionTransition,
  CT_EmbeddedFontDataId,
  CT_EmbeddedFontList,
  CT_EmbeddedFontListEntry,
  CT_Empty,
  CT_Extension,
  CT_ExtensionList,
  CT_ExtensionListModify,
  CT_GraphicalObjectFrame,
  CT_GraphicalObjectFrameNonVisual,
  CT_GroupShape,
  CT_GroupShapeNonVisual,
  CT_Guide,
  CT_GuideList,
  CT_HandoutMaster,
  CT_HandoutMasterIdList,
  CT_HandoutMasterIdListEntry,
  CT_HeaderFooter,
  CT_HtmlPublishProperties,
  CT_InOutTransition,
  CT_IndexRange,
  CT_Kinsoku,
  CT_ModifyVerifier,
  CT_NormalViewPortion,
  CT_NormalViewProperties,
  CT_NotesMaster,
  CT_NotesMasterIdList,
  CT_NotesMasterIdListEntry,
  CT_NotesSlide,
  CT_NotesTextViewProperties,
  CT_NotesViewProperties,
  CT_OleObject,
  CT_OleObjectEmbed,
  CT_OleObjectLink,
  CT_OptionalBlackTransition,
  CT_OrientationTransition,
  CT_OutlineViewProperties,
  CT_OutlineViewSlideEntry,
  CT_OutlineViewSlideList,
  CT_PhotoAlbum,
  CT_Picture,
  CT_PictureNonVisual,
  CT_Placeholder,
  CT_Presentation,
  CT_PresentationProperties,
  CT_PrintProperties,
  CT_Rel,
  CT_Shape,
  CT_ShapeNonVisual,
  CT_ShowInfoBrowse,
  CT_ShowInfoKiosk,
  CT_ShowProperties,
  CT_SideDirectionTransition,
  CT_Slide,
  CT_SlideIdList,
  CT_SlideIdListEntry,
  CT_SlideLayout,
  CT_SlideLayoutIdList,
  CT_SlideLayoutIdListEntry,
  CT_SlideMaster,
  CT_SlideMasterIdList,
  CT_SlideMasterIdListEntry,
  CT_SlideMasterTextStyles,
  CT_SlideRelationshipList,
  CT_SlideRelationshipListEntry,
  CT_SlideSize,
  CT_SlideSorterViewProperties,
  CT_SlideSyncProperties,
  CT_SlideTiming,
  CT_SlideTransition,
  CT_SlideViewProperties,
  CT_SmartTags,
  CT_SplitTransition,
  CT_StringTag,
  CT_TLAnimVariant,
  CT_TLAnimVariantBooleanVal,
  CT_TLAnimVariantFloatVal,
  CT_TLAnimVariantIntegerVal,
  CT_TLAnimVariantStringVal,
  CT_TLAnimateBehavior,
  CT_TLAnimateColorBehavior,
  CT_TLAnimateEffectBehavior,
  CT_TLAnimateMotionBehavior,
  CT_TLAnimateRotationBehavior,
  CT_TLAnimateScaleBehavior,
  CT_TLBehaviorAttributeNameList,
  CT_TLBuildDiagram,
  CT_TLBuildParagraph,
  CT_TLByAnimateColorTransform,
  CT_TLByHslColorTransform,
  CT_TLByRgbColorTransform,
  CT_TLCommandBehavior,
  CT_TLCommonBehaviorData,
  CT_TLCommonMediaNodeData,
  CT_TLCommonTimeNodeData,
  CT_TLGraphicalObjectBuild,
  CT_TLIterateData,
  CT_TLIterateIntervalPercentage,
  CT_TLIterateIntervalTime,
  CT_TLMediaNodeAudio,
  CT_TLMediaNodeVideo,
  CT_TLOleBuildChart,
  CT_TLOleChartTargetElement,
  CT_TLPoint,
  CT_TLSetBehavior,
  CT_TLShapeTargetElement,
  CT_TLSubShapeId,
  CT_TLTemplate,
  CT_TLTemplateList,
  CT_TLTextTargetElement,
  CT_TLTimeAnimateValue,
  CT_TLTimeAnimateValueList,
  CT_TLTimeCondition,
  CT_TLTimeConditionList,
  CT_TLTimeNodeExclusive,
  CT_TLTimeNodeParallel,
  CT_TLTimeNodeSequence,
  CT_TLTimeTargetElement,
  CT_TLTriggerRuntimeNode,
  CT_TLTriggerTimeNodeID,
  CT_TagList,
  CT_TagsData,
  CT_TimeNodeList,
  CT_TransitionSoundAction,
  CT_TransitionStartSoundAction,
  CT_ViewProperties,
  CT_WebProperties,
  CT_WheelTransition,
} from './types.js';

// XML writers. Element names are supplied by the owning particle.

/** Write a `CT_ApplicationNonVisualDrawingProps`; the caller supplies its element local name. */
export function writeCT_ApplicationNonVisualDrawingProps(s: XmlSink, value: CT_ApplicationNonVisualDrawingProps, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['isPhoto'] !== undefined) s.attr(null, 'isPhoto', String(value['isPhoto']));
  if (value['userDrawn'] !== undefined) s.attr(null, 'userDrawn', String(value['userDrawn']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['ph'] !== undefined) writeCT_Placeholder(s, value['ph'], ctx, 'ph');
  $q.flush(s, 0);
  if (value['media'] !== undefined) {
    if (value['media'].kind === 'audioCd') writeCT_AudioCD(s, value['media'].value, ctx, 'audioCd');
    if (value['media'].kind === 'wavAudioFile') writeCT_EmbeddedWAVAudioFile(s, value['media'].value, ctx, 'wavAudioFile');
    if (value['media'].kind === 'audioFile') writeCT_AudioFile(s, value['media'].value, ctx, 'audioFile');
    if (value['media'].kind === 'videoFile') writeCT_VideoFile(s, value['media'].value, ctx, 'videoFile');
    if (value['media'].kind === 'quickTimeFile') writeCT_QuickTimeFile(s, value['media'].value, ctx, 'quickTimeFile');
  }
  $q.flush(s, 1);
  if (value['custDataLst'] !== undefined) writeCT_CustomerDataList(s, value['custDataLst'], ctx, 'custDataLst');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Background`; the caller supplies its element local name. */
export function writeCT_Background(s: XmlSink, value: CT_Background, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['bwMode'] !== undefined) s.attr(null, 'bwMode', String(value['bwMode']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['background'] !== undefined) {
    if (value['background'].kind === 'bgPr') writeCT_BackgroundProperties(s, value['background'].value, ctx, 'bgPr');
    if (value['background'].kind === 'bgRef') writeCT_StyleMatrixReference(s, value['background'].value, ctx, 'bgRef');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_BackgroundProperties`; the caller supplies its element local name. */
export function writeCT_BackgroundProperties(s: XmlSink, value: CT_BackgroundProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['shadeToTitle'] !== undefined) s.attr(null, 'shadeToTitle', String(value['shadeToTitle']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['fillProperties'] !== undefined) {
    if (value['fillProperties'].kind === 'noFill') writeCT_NoFillProperties(s, value['fillProperties'].value, ctx, 'noFill');
    if (value['fillProperties'].kind === 'solidFill') writeCT_SolidColorFillProperties(s, value['fillProperties'].value, ctx, 'solidFill');
    if (value['fillProperties'].kind === 'gradFill') writeCT_GradientFillProperties(s, value['fillProperties'].value, ctx, 'gradFill');
    if (value['fillProperties'].kind === 'blipFill') writeCT_BlipFillProperties(s, value['fillProperties'].value, ctx, 'blipFill');
    if (value['fillProperties'].kind === 'pattFill') writeCT_PatternFillProperties(s, value['fillProperties'].value, ctx, 'pattFill');
    if (value['fillProperties'].kind === 'grpFill') writeCT_GroupFillProperties(s, value['fillProperties'].value, ctx, 'grpFill');
  }
  $q.flush(s, 0);
  if (value['effectProperties'] !== undefined) {
    if (value['effectProperties'].kind === 'effectLst') writeCT_EffectList(s, value['effectProperties'].value, ctx, 'effectLst');
    if (value['effectProperties'].kind === 'effectDag') writeCT_EffectContainer(s, value['effectProperties'].value, ctx, 'effectDag');
  }
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_BuildList`; the caller supplies its element local name. */
export function writeCT_BuildList(s: XmlSink, value: CT_BuildList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_content of value['content']) {
    if (v_content.kind === 'bldP') writeCT_TLBuildParagraph(s, v_content.value, ctx, 'bldP');
    if (v_content.kind === 'bldDgm') writeCT_TLBuildDiagram(s, v_content.value, ctx, 'bldDgm');
    if (v_content.kind === 'bldOleChart') writeCT_TLOleBuildChart(s, v_content.value, ctx, 'bldOleChart');
    if (v_content.kind === 'bldGraphic') writeCT_TLGraphicalObjectBuild(s, v_content.value, ctx, 'bldGraphic');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  s.endElement();
}

/** Write a `CT_Comment`; the caller supplies its element local name. */
export function writeCT_Comment(s: XmlSink, value: CT_Comment, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['authorId'] !== undefined) s.attr(null, 'authorId', String(value['authorId']));
  if (value['dt'] !== undefined) s.attr(null, 'dt', String(value['dt']));
  if (value['idx'] !== undefined) s.attr(null, 'idx', String(value['idx']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['pos'] !== undefined) writeCT_Point2D(s, value['pos'], ctx, 'pos');
  $q.flush(s, 0);
  if (value['text'] !== undefined) s.text(String(value['text']));
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_CommentAuthor`; the caller supplies its element local name. */
export function writeCT_CommentAuthor(s: XmlSink, value: CT_CommentAuthor, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(null, 'id', String(value['id']));
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  if (value['initials'] !== undefined) s.attr(null, 'initials', String(value['initials']));
  if (value['lastIdx'] !== undefined) s.attr(null, 'lastIdx', String(value['lastIdx']));
  if (value['clrIdx'] !== undefined) s.attr(null, 'clrIdx', String(value['clrIdx']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_CommentAuthorList`; the caller supplies its element local name. */
export function writeCT_CommentAuthorList(s: XmlSink, value: CT_CommentAuthorList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['cmAuthor'].length; idx++) {
    const v_cmAuthor = value['cmAuthor'][idx]!;
    writeCT_CommentAuthor(s, v_cmAuthor, ctx, 'cmAuthor');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_CommentList`; the caller supplies its element local name. */
export function writeCT_CommentList(s: XmlSink, value: CT_CommentList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['cm'].length; idx++) {
    const v_cm = value['cm'][idx]!;
    writeCT_Comment(s, v_cm, ctx, 'cm');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_CommonSlideData`; the caller supplies its element local name. */
export function writeCT_CommonSlideData(s: XmlSink, value: CT_CommonSlideData, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['bg'] !== undefined) writeCT_Background(s, value['bg'], ctx, 'bg');
  $q.flush(s, 0);
  if (value['spTree'] !== undefined) writeCT_GroupShape(s, value['spTree'], ctx, 'spTree');
  $q.flush(s, 1);
  if (value['custDataLst'] !== undefined) writeCT_CustomerDataList(s, value['custDataLst'], ctx, 'custDataLst');
  $q.flush(s, 2);
  if (value['controls'] !== undefined) writeCT_ControlList(s, value['controls'], ctx, 'controls');
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_CommonSlideViewProperties`; the caller supplies its element local name. */
export function writeCT_CommonSlideViewProperties(s: XmlSink, value: CT_CommonSlideViewProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['snapToGrid'] !== undefined) s.attr(null, 'snapToGrid', String(value['snapToGrid']));
  if (value['snapToObjects'] !== undefined) s.attr(null, 'snapToObjects', String(value['snapToObjects']));
  if (value['showGuides'] !== undefined) s.attr(null, 'showGuides', String(value['showGuides']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cViewPr'] !== undefined) writeCT_CommonViewProperties(s, value['cViewPr'], ctx, 'cViewPr');
  $q.flush(s, 0);
  if (value['guideLst'] !== undefined) writeCT_GuideList(s, value['guideLst'], ctx, 'guideLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_CommonViewProperties`; the caller supplies its element local name. */
export function writeCT_CommonViewProperties(s: XmlSink, value: CT_CommonViewProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['varScale'] !== undefined) s.attr(null, 'varScale', String(value['varScale']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['scale'] !== undefined) writeCT_Scale2D(s, value['scale'], ctx, 'scale');
  $q.flush(s, 0);
  if (value['origin'] !== undefined) writeCT_Point2D(s, value['origin'], ctx, 'origin');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Connector`; the caller supplies its element local name. */
export function writeCT_Connector(s: XmlSink, value: CT_Connector, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['nvCxnSpPr'] !== undefined) writeCT_ConnectorNonVisual(s, value['nvCxnSpPr'], ctx, 'nvCxnSpPr');
  $q.flush(s, 0);
  if (value['spPr'] !== undefined) writeCT_ShapeProperties(s, value['spPr'], ctx, 'spPr');
  $q.flush(s, 1);
  if (value['style'] !== undefined) writeCT_ShapeStyle(s, value['style'], ctx, 'style');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ConnectorNonVisual`; the caller supplies its element local name. */
export function writeCT_ConnectorNonVisual(s: XmlSink, value: CT_ConnectorNonVisual, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cNvPr'] !== undefined) writeCT_NonVisualDrawingProps(s, value['cNvPr'], ctx, 'cNvPr');
  $q.flush(s, 0);
  if (value['cNvCxnSpPr'] !== undefined) writeCT_NonVisualConnectorProperties(s, value['cNvCxnSpPr'], ctx, 'cNvCxnSpPr');
  $q.flush(s, 1);
  if (value['nvPr'] !== undefined) writeCT_ApplicationNonVisualDrawingProps(s, value['nvPr'], ctx, 'nvPr');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Control`; the caller supplies its element local name. */
export function writeCT_Control(s: XmlSink, value: CT_Control, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['spid'] !== undefined) s.attr(null, 'spid', String(value['spid']));
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  if (value['showAsIcon'] !== undefined) s.attr(null, 'showAsIcon', String(value['showAsIcon']));
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  if (value['imgW'] !== undefined) s.attr(null, 'imgW', String(value['imgW']));
  if (value['imgH'] !== undefined) s.attr(null, 'imgH', String(value['imgH']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  if (value['pic'] !== undefined) writeCT_Picture(s, value['pic'], ctx, 'pic');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ControlList`; the caller supplies its element local name. */
export function writeCT_ControlList(s: XmlSink, value: CT_ControlList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['control'].length; idx++) {
    const v_control = value['control'][idx]!;
    writeCT_Control(s, v_control, ctx, 'control');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_CornerDirectionTransition`; the caller supplies its element local name. */
export function writeCT_CornerDirectionTransition(s: XmlSink, value: CT_CornerDirectionTransition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['dir'] !== undefined) s.attr(null, 'dir', String(value['dir']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_CustomerData`; the caller supplies its element local name. */
export function writeCT_CustomerData(s: XmlSink, value: CT_CustomerData, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_CustomerDataList`; the caller supplies its element local name. */
export function writeCT_CustomerDataList(s: XmlSink, value: CT_CustomerDataList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['custData'].length; idx++) {
    const v_custData = value['custData'][idx]!;
    writeCT_CustomerData(s, v_custData, ctx, 'custData');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  if (value['tags'] !== undefined) writeCT_TagsData(s, value['tags'], ctx, 'tags');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_CustomShow`; the caller supplies its element local name. */
export function writeCT_CustomShow(s: XmlSink, value: CT_CustomShow, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  if (value['id'] !== undefined) s.attr(null, 'id', String(value['id']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['sldLst'] !== undefined) writeCT_SlideRelationshipList(s, value['sldLst'], ctx, 'sldLst');
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_CustomShowId`; the caller supplies its element local name. */
export function writeCT_CustomShowId(s: XmlSink, value: CT_CustomShowId, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(null, 'id', String(value['id']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_CustomShowList`; the caller supplies its element local name. */
export function writeCT_CustomShowList(s: XmlSink, value: CT_CustomShowList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['custShow'].length; idx++) {
    const v_custShow = value['custShow'][idx]!;
    writeCT_CustomShow(s, v_custShow, ctx, 'custShow');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_EightDirectionTransition`; the caller supplies its element local name. */
export function writeCT_EightDirectionTransition(s: XmlSink, value: CT_EightDirectionTransition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['dir'] !== undefined) s.attr(null, 'dir', String(value['dir']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_EmbeddedFontDataId`; the caller supplies its element local name. */
export function writeCT_EmbeddedFontDataId(s: XmlSink, value: CT_EmbeddedFontDataId, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_EmbeddedFontList`; the caller supplies its element local name. */
export function writeCT_EmbeddedFontList(s: XmlSink, value: CT_EmbeddedFontList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['embeddedFont'].length; idx++) {
    const v_embeddedFont = value['embeddedFont'][idx]!;
    writeCT_EmbeddedFontListEntry(s, v_embeddedFont, ctx, 'embeddedFont');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_EmbeddedFontListEntry`; the caller supplies its element local name. */
export function writeCT_EmbeddedFontListEntry(s: XmlSink, value: CT_EmbeddedFontListEntry, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['font'] !== undefined) writeCT_TextFont(s, value['font'], ctx, 'font');
  $q.flush(s, 0);
  if (value['regular'] !== undefined) writeCT_EmbeddedFontDataId(s, value['regular'], ctx, 'regular');
  $q.flush(s, 1);
  if (value['bold'] !== undefined) writeCT_EmbeddedFontDataId(s, value['bold'], ctx, 'bold');
  $q.flush(s, 2);
  if (value['italic'] !== undefined) writeCT_EmbeddedFontDataId(s, value['italic'], ctx, 'italic');
  $q.flush(s, 3);
  if (value['boldItalic'] !== undefined) writeCT_EmbeddedFontDataId(s, value['boldItalic'], ctx, 'boldItalic');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Empty`; the caller supplies its element local name. */
export function writeCT_Empty(s: XmlSink, value: CT_Empty, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Extension`; the caller supplies its element local name. */
export function writeCT_Extension(s: XmlSink, value: CT_Extension, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['uri'] !== undefined) s.attr(null, 'uri', String(value['uri']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (const raw of value['any']) {
    s.raw(raw);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ExtensionList`; the caller supplies its element local name. */
export function writeCT_ExtensionList(s: XmlSink, value: CT_ExtensionList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['ext'].length; idx++) {
    const v_ext = value['ext'][idx]!;
    writeCT_Extension(s, v_ext, ctx, 'ext');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ExtensionListModify`; the caller supplies its element local name. */
export function writeCT_ExtensionListModify(s: XmlSink, value: CT_ExtensionListModify, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['mod'] !== undefined) s.attr(null, 'mod', String(value['mod']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['ext'].length; idx++) {
    const v_ext = value['ext'][idx]!;
    writeCT_Extension(s, v_ext, ctx, 'ext');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GraphicalObjectFrame`; the caller supplies its element local name. */
export function writeCT_GraphicalObjectFrame(s: XmlSink, value: CT_GraphicalObjectFrame, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['bwMode'] !== undefined) s.attr(null, 'bwMode', String(value['bwMode']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['nvGraphicFramePr'] !== undefined) writeCT_GraphicalObjectFrameNonVisual(s, value['nvGraphicFramePr'], ctx, 'nvGraphicFramePr');
  $q.flush(s, 0);
  if (value['xfrm'] !== undefined) writeCT_Transform2D(s, value['xfrm'], ctx, 'xfrm');
  $q.flush(s, 1);
  if (value['graphic'] !== undefined) writeCT_GraphicalObject(s, value['graphic'], ctx, 'graphic');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GraphicalObjectFrameNonVisual`; the caller supplies its element local name. */
export function writeCT_GraphicalObjectFrameNonVisual(s: XmlSink, value: CT_GraphicalObjectFrameNonVisual, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cNvPr'] !== undefined) writeCT_NonVisualDrawingProps(s, value['cNvPr'], ctx, 'cNvPr');
  $q.flush(s, 0);
  if (value['cNvGraphicFramePr'] !== undefined) writeCT_NonVisualGraphicFrameProperties(s, value['cNvGraphicFramePr'], ctx, 'cNvGraphicFramePr');
  $q.flush(s, 1);
  if (value['nvPr'] !== undefined) writeCT_ApplicationNonVisualDrawingProps(s, value['nvPr'], ctx, 'nvPr');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GroupShape`; the caller supplies its element local name. */
export function writeCT_GroupShape(s: XmlSink, value: CT_GroupShape, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  if (value['nvGrpSpPr'] !== undefined) writeCT_GroupShapeNonVisual(s, value['nvGrpSpPr'], ctx, 'nvGrpSpPr');
  if (value['grpSpPr'] !== undefined) writeCT_GroupShapeProperties(s, value['grpSpPr'], ctx, 'grpSpPr');
  for (const v_content of value['content']) {
    if (v_content.kind === 'sp') writeCT_Shape(s, v_content.value, ctx, 'sp');
    if (v_content.kind === 'grpSp') writeCT_GroupShape(s, v_content.value, ctx, 'grpSp');
    if (v_content.kind === 'graphicFrame') writeCT_GraphicalObjectFrame(s, v_content.value, ctx, 'graphicFrame');
    if (v_content.kind === 'cxnSp') writeCT_Connector(s, v_content.value, ctx, 'cxnSp');
    if (v_content.kind === 'pic') writeCT_Picture(s, v_content.value, ctx, 'pic');
    if (v_content.kind === 'contentPart') writeCT_Rel(s, v_content.value, ctx, 'contentPart');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  s.endElement();
}

/** Write a `CT_GroupShapeNonVisual`; the caller supplies its element local name. */
export function writeCT_GroupShapeNonVisual(s: XmlSink, value: CT_GroupShapeNonVisual, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cNvPr'] !== undefined) writeCT_NonVisualDrawingProps(s, value['cNvPr'], ctx, 'cNvPr');
  $q.flush(s, 0);
  if (value['cNvGrpSpPr'] !== undefined) writeCT_NonVisualGroupDrawingShapeProps(s, value['cNvGrpSpPr'], ctx, 'cNvGrpSpPr');
  $q.flush(s, 1);
  if (value['nvPr'] !== undefined) writeCT_ApplicationNonVisualDrawingProps(s, value['nvPr'], ctx, 'nvPr');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Guide`; the caller supplies its element local name. */
export function writeCT_Guide(s: XmlSink, value: CT_Guide, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['orient'] !== undefined) s.attr(null, 'orient', String(value['orient']));
  if (value['pos'] !== undefined) s.attr(null, 'pos', String(value['pos']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_GuideList`; the caller supplies its element local name. */
export function writeCT_GuideList(s: XmlSink, value: CT_GuideList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['guide'].length; idx++) {
    const v_guide = value['guide'][idx]!;
    writeCT_Guide(s, v_guide, ctx, 'guide');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_HandoutMaster`; the caller supplies its element local name. */
export function writeCT_HandoutMaster(s: XmlSink, value: CT_HandoutMaster, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cSld'] !== undefined) writeCT_CommonSlideData(s, value['cSld'], ctx, 'cSld');
  $q.flush(s, 0);
  if (value['clrMap'] !== undefined) writeCT_ColorMapping(s, value['clrMap'], ctx, 'clrMap');
  $q.flush(s, 1);
  if (value['hf'] !== undefined) writeCT_HeaderFooter(s, value['hf'], ctx, 'hf');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_HandoutMasterIdList`; the caller supplies its element local name. */
export function writeCT_HandoutMasterIdList(s: XmlSink, value: CT_HandoutMasterIdList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['handoutMasterId'] !== undefined) writeCT_HandoutMasterIdListEntry(s, value['handoutMasterId'], ctx, 'handoutMasterId');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_HandoutMasterIdListEntry`; the caller supplies its element local name. */
export function writeCT_HandoutMasterIdListEntry(s: XmlSink, value: CT_HandoutMasterIdListEntry, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_HeaderFooter`; the caller supplies its element local name. */
export function writeCT_HeaderFooter(s: XmlSink, value: CT_HeaderFooter, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['sldNum'] !== undefined) s.attr(null, 'sldNum', String(value['sldNum']));
  if (value['hdr'] !== undefined) s.attr(null, 'hdr', String(value['hdr']));
  if (value['ftr'] !== undefined) s.attr(null, 'ftr', String(value['ftr']));
  if (value['dt'] !== undefined) s.attr(null, 'dt', String(value['dt']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_HtmlPublishProperties`; the caller supplies its element local name. */
export function writeCT_HtmlPublishProperties(s: XmlSink, value: CT_HtmlPublishProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['showSpeakerNotes'] !== undefined) s.attr(null, 'showSpeakerNotes', String(value['showSpeakerNotes']));
  if (value['target'] !== undefined) s.attr(null, 'target', String(value['target']));
  if (value['title'] !== undefined) s.attr(null, 'title', String(value['title']));
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['slideListChoice'] !== undefined) {
    if (value['slideListChoice'].kind === 'sldAll') writeCT_Empty(s, value['slideListChoice'].value, ctx, 'sldAll');
    if (value['slideListChoice'].kind === 'sldRg') writeCT_IndexRange(s, value['slideListChoice'].value, ctx, 'sldRg');
    if (value['slideListChoice'].kind === 'custShow') writeCT_CustomShowId(s, value['slideListChoice'].value, ctx, 'custShow');
  }
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_IndexRange`; the caller supplies its element local name. */
export function writeCT_IndexRange(s: XmlSink, value: CT_IndexRange, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['st'] !== undefined) s.attr(null, 'st', String(value['st']));
  if (value['end'] !== undefined) s.attr(null, 'end', String(value['end']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_InOutTransition`; the caller supplies its element local name. */
export function writeCT_InOutTransition(s: XmlSink, value: CT_InOutTransition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['dir'] !== undefined) s.attr(null, 'dir', String(value['dir']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Kinsoku`; the caller supplies its element local name. */
export function writeCT_Kinsoku(s: XmlSink, value: CT_Kinsoku, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['lang'] !== undefined) s.attr(null, 'lang', String(value['lang']));
  if (value['invalStChars'] !== undefined) s.attr(null, 'invalStChars', String(value['invalStChars']));
  if (value['invalEndChars'] !== undefined) s.attr(null, 'invalEndChars', String(value['invalEndChars']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ModifyVerifier`; the caller supplies its element local name. */
export function writeCT_ModifyVerifier(s: XmlSink, value: CT_ModifyVerifier, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['algorithmName'] !== undefined) s.attr(null, 'algorithmName', String(value['algorithmName']));
  if (value['hashValue'] !== undefined) s.attr(null, 'hashValue', String(value['hashValue']));
  if (value['saltValue'] !== undefined) s.attr(null, 'saltValue', String(value['saltValue']));
  if (value['spinValue'] !== undefined) s.attr(null, 'spinValue', String(value['spinValue']));
  if (value['cryptProviderType'] !== undefined) s.attr(null, 'cryptProviderType', String(value['cryptProviderType']));
  if (value['cryptAlgorithmClass'] !== undefined) s.attr(null, 'cryptAlgorithmClass', String(value['cryptAlgorithmClass']));
  if (value['cryptAlgorithmType'] !== undefined) s.attr(null, 'cryptAlgorithmType', String(value['cryptAlgorithmType']));
  if (value['cryptAlgorithmSid'] !== undefined) s.attr(null, 'cryptAlgorithmSid', String(value['cryptAlgorithmSid']));
  if (value['spinCount'] !== undefined) s.attr(null, 'spinCount', String(value['spinCount']));
  if (value['saltData'] !== undefined) s.attr(null, 'saltData', String(value['saltData']));
  if (value['hashData'] !== undefined) s.attr(null, 'hashData', String(value['hashData']));
  if (value['cryptProvider'] !== undefined) s.attr(null, 'cryptProvider', String(value['cryptProvider']));
  if (value['algIdExt'] !== undefined) s.attr(null, 'algIdExt', String(value['algIdExt']));
  if (value['algIdExtSource'] !== undefined) s.attr(null, 'algIdExtSource', String(value['algIdExtSource']));
  if (value['cryptProviderTypeExt'] !== undefined) s.attr(null, 'cryptProviderTypeExt', String(value['cryptProviderTypeExt']));
  if (value['cryptProviderTypeExtSource'] !== undefined) s.attr(null, 'cryptProviderTypeExtSource', String(value['cryptProviderTypeExtSource']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_NormalViewPortion`; the caller supplies its element local name. */
export function writeCT_NormalViewPortion(s: XmlSink, value: CT_NormalViewPortion, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['sz'] !== undefined) s.attr(null, 'sz', String(value['sz']));
  if (value['autoAdjust'] !== undefined) s.attr(null, 'autoAdjust', String(value['autoAdjust']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_NormalViewProperties`; the caller supplies its element local name. */
export function writeCT_NormalViewProperties(s: XmlSink, value: CT_NormalViewProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['showOutlineIcons'] !== undefined) s.attr(null, 'showOutlineIcons', String(value['showOutlineIcons']));
  if (value['snapVertSplitter'] !== undefined) s.attr(null, 'snapVertSplitter', String(value['snapVertSplitter']));
  if (value['vertBarState'] !== undefined) s.attr(null, 'vertBarState', String(value['vertBarState']));
  if (value['horzBarState'] !== undefined) s.attr(null, 'horzBarState', String(value['horzBarState']));
  if (value['preferSingleView'] !== undefined) s.attr(null, 'preferSingleView', String(value['preferSingleView']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['restoredLeft'] !== undefined) writeCT_NormalViewPortion(s, value['restoredLeft'], ctx, 'restoredLeft');
  $q.flush(s, 0);
  if (value['restoredTop'] !== undefined) writeCT_NormalViewPortion(s, value['restoredTop'], ctx, 'restoredTop');
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NotesMaster`; the caller supplies its element local name. */
export function writeCT_NotesMaster(s: XmlSink, value: CT_NotesMaster, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cSld'] !== undefined) writeCT_CommonSlideData(s, value['cSld'], ctx, 'cSld');
  $q.flush(s, 0);
  if (value['clrMap'] !== undefined) writeCT_ColorMapping(s, value['clrMap'], ctx, 'clrMap');
  $q.flush(s, 1);
  if (value['hf'] !== undefined) writeCT_HeaderFooter(s, value['hf'], ctx, 'hf');
  $q.flush(s, 2);
  if (value['notesStyle'] !== undefined) writeCT_TextListStyle(s, value['notesStyle'], ctx, 'notesStyle');
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NotesMasterIdList`; the caller supplies its element local name. */
export function writeCT_NotesMasterIdList(s: XmlSink, value: CT_NotesMasterIdList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['notesMasterId'] !== undefined) writeCT_NotesMasterIdListEntry(s, value['notesMasterId'], ctx, 'notesMasterId');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NotesMasterIdListEntry`; the caller supplies its element local name. */
export function writeCT_NotesMasterIdListEntry(s: XmlSink, value: CT_NotesMasterIdListEntry, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NotesSlide`; the caller supplies its element local name. */
export function writeCT_NotesSlide(s: XmlSink, value: CT_NotesSlide, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['showMasterSp'] !== undefined) s.attr(null, 'showMasterSp', String(value['showMasterSp']));
  if (value['showMasterPhAnim'] !== undefined) s.attr(null, 'showMasterPhAnim', String(value['showMasterPhAnim']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cSld'] !== undefined) writeCT_CommonSlideData(s, value['cSld'], ctx, 'cSld');
  $q.flush(s, 0);
  if (value['clrMapOvr'] !== undefined) writeCT_ColorMappingOverride(s, value['clrMapOvr'], ctx, 'clrMapOvr');
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NotesTextViewProperties`; the caller supplies its element local name. */
export function writeCT_NotesTextViewProperties(s: XmlSink, value: CT_NotesTextViewProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cViewPr'] !== undefined) writeCT_CommonViewProperties(s, value['cViewPr'], ctx, 'cViewPr');
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NotesViewProperties`; the caller supplies its element local name. */
export function writeCT_NotesViewProperties(s: XmlSink, value: CT_NotesViewProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cSldViewPr'] !== undefined) writeCT_CommonSlideViewProperties(s, value['cSldViewPr'], ctx, 'cSldViewPr');
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_OleObject`; the caller supplies its element local name. */
export function writeCT_OleObject(s: XmlSink, value: CT_OleObject, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['spid'] !== undefined) s.attr(null, 'spid', String(value['spid']));
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  if (value['showAsIcon'] !== undefined) s.attr(null, 'showAsIcon', String(value['showAsIcon']));
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  if (value['imgW'] !== undefined) s.attr(null, 'imgW', String(value['imgW']));
  if (value['imgH'] !== undefined) s.attr(null, 'imgH', String(value['imgH']));
  if (value['progId'] !== undefined) s.attr(null, 'progId', String(value['progId']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'embed') writeCT_OleObjectEmbed(s, value['content'].value, ctx, 'embed');
    if (value['content'].kind === 'link') writeCT_OleObjectLink(s, value['content'].value, ctx, 'link');
  }
  $q.flush(s, 0);
  if (value['pic'] !== undefined) writeCT_Picture(s, value['pic'], ctx, 'pic');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_OleObjectEmbed`; the caller supplies its element local name. */
export function writeCT_OleObjectEmbed(s: XmlSink, value: CT_OleObjectEmbed, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['followColorScheme'] !== undefined) s.attr(null, 'followColorScheme', String(value['followColorScheme']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_OleObjectLink`; the caller supplies its element local name. */
export function writeCT_OleObjectLink(s: XmlSink, value: CT_OleObjectLink, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['updateAutomatic'] !== undefined) s.attr(null, 'updateAutomatic', String(value['updateAutomatic']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_OptionalBlackTransition`; the caller supplies its element local name. */
export function writeCT_OptionalBlackTransition(s: XmlSink, value: CT_OptionalBlackTransition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['thruBlk'] !== undefined) s.attr(null, 'thruBlk', String(value['thruBlk']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_OrientationTransition`; the caller supplies its element local name. */
export function writeCT_OrientationTransition(s: XmlSink, value: CT_OrientationTransition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['dir'] !== undefined) s.attr(null, 'dir', String(value['dir']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_OutlineViewProperties`; the caller supplies its element local name. */
export function writeCT_OutlineViewProperties(s: XmlSink, value: CT_OutlineViewProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cViewPr'] !== undefined) writeCT_CommonViewProperties(s, value['cViewPr'], ctx, 'cViewPr');
  $q.flush(s, 0);
  if (value['sldLst'] !== undefined) writeCT_OutlineViewSlideList(s, value['sldLst'], ctx, 'sldLst');
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_OutlineViewSlideEntry`; the caller supplies its element local name. */
export function writeCT_OutlineViewSlideEntry(s: XmlSink, value: CT_OutlineViewSlideEntry, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  if (value['collapse'] !== undefined) s.attr(null, 'collapse', String(value['collapse']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_OutlineViewSlideList`; the caller supplies its element local name. */
export function writeCT_OutlineViewSlideList(s: XmlSink, value: CT_OutlineViewSlideList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['sld'].length; idx++) {
    const v_sld = value['sld'][idx]!;
    writeCT_OutlineViewSlideEntry(s, v_sld, ctx, 'sld');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_PhotoAlbum`; the caller supplies its element local name. */
export function writeCT_PhotoAlbum(s: XmlSink, value: CT_PhotoAlbum, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['bw'] !== undefined) s.attr(null, 'bw', String(value['bw']));
  if (value['showCaptions'] !== undefined) s.attr(null, 'showCaptions', String(value['showCaptions']));
  if (value['layout'] !== undefined) s.attr(null, 'layout', String(value['layout']));
  if (value['frame'] !== undefined) s.attr(null, 'frame', String(value['frame']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Picture`; the caller supplies its element local name. */
export function writeCT_Picture(s: XmlSink, value: CT_Picture, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['nvPicPr'] !== undefined) writeCT_PictureNonVisual(s, value['nvPicPr'], ctx, 'nvPicPr');
  $q.flush(s, 0);
  if (value['blipFill'] !== undefined) writeCT_BlipFillProperties(s, value['blipFill'], ctx, 'blipFill');
  $q.flush(s, 1);
  if (value['spPr'] !== undefined) writeCT_ShapeProperties(s, value['spPr'], ctx, 'spPr');
  $q.flush(s, 2);
  if (value['style'] !== undefined) writeCT_ShapeStyle(s, value['style'], ctx, 'style');
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_PictureNonVisual`; the caller supplies its element local name. */
export function writeCT_PictureNonVisual(s: XmlSink, value: CT_PictureNonVisual, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cNvPr'] !== undefined) writeCT_NonVisualDrawingProps(s, value['cNvPr'], ctx, 'cNvPr');
  $q.flush(s, 0);
  if (value['cNvPicPr'] !== undefined) writeCT_NonVisualPictureProperties(s, value['cNvPicPr'], ctx, 'cNvPicPr');
  $q.flush(s, 1);
  if (value['nvPr'] !== undefined) writeCT_ApplicationNonVisualDrawingProps(s, value['nvPr'], ctx, 'nvPr');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Placeholder`; the caller supplies its element local name. */
export function writeCT_Placeholder(s: XmlSink, value: CT_Placeholder, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['orient'] !== undefined) s.attr(null, 'orient', String(value['orient']));
  if (value['sz'] !== undefined) s.attr(null, 'sz', String(value['sz']));
  if (value['idx'] !== undefined) s.attr(null, 'idx', String(value['idx']));
  if (value['hasCustomPrompt'] !== undefined) s.attr(null, 'hasCustomPrompt', String(value['hasCustomPrompt']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Presentation`; the caller supplies its element local name. */
export function writeCT_Presentation(s: XmlSink, value: CT_Presentation, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['serverZoom'] !== undefined) s.attr(null, 'serverZoom', String(value['serverZoom']));
  if (value['firstSlideNum'] !== undefined) s.attr(null, 'firstSlideNum', String(value['firstSlideNum']));
  if (value['showSpecialPlsOnTitleSld'] !== undefined) s.attr(null, 'showSpecialPlsOnTitleSld', String(value['showSpecialPlsOnTitleSld']));
  if (value['rtl'] !== undefined) s.attr(null, 'rtl', String(value['rtl']));
  if (value['removePersonalInfoOnSave'] !== undefined) s.attr(null, 'removePersonalInfoOnSave', String(value['removePersonalInfoOnSave']));
  if (value['compatMode'] !== undefined) s.attr(null, 'compatMode', String(value['compatMode']));
  if (value['strictFirstAndLastChars'] !== undefined) s.attr(null, 'strictFirstAndLastChars', String(value['strictFirstAndLastChars']));
  if (value['embedTrueTypeFonts'] !== undefined) s.attr(null, 'embedTrueTypeFonts', String(value['embedTrueTypeFonts']));
  if (value['saveSubsetFonts'] !== undefined) s.attr(null, 'saveSubsetFonts', String(value['saveSubsetFonts']));
  if (value['autoCompressPictures'] !== undefined) s.attr(null, 'autoCompressPictures', String(value['autoCompressPictures']));
  if (value['bookmarkIdSeed'] !== undefined) s.attr(null, 'bookmarkIdSeed', String(value['bookmarkIdSeed']));
  if (value['conformance'] !== undefined) s.attr(null, 'conformance', String(value['conformance']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['sldMasterIdLst'] !== undefined) writeCT_SlideMasterIdList(s, value['sldMasterIdLst'], ctx, 'sldMasterIdLst');
  $q.flush(s, 0);
  if (value['notesMasterIdLst'] !== undefined) writeCT_NotesMasterIdList(s, value['notesMasterIdLst'], ctx, 'notesMasterIdLst');
  $q.flush(s, 1);
  if (value['handoutMasterIdLst'] !== undefined) writeCT_HandoutMasterIdList(s, value['handoutMasterIdLst'], ctx, 'handoutMasterIdLst');
  $q.flush(s, 2);
  if (value['sldIdLst'] !== undefined) writeCT_SlideIdList(s, value['sldIdLst'], ctx, 'sldIdLst');
  $q.flush(s, 3);
  if (value['sldSz'] !== undefined) writeCT_SlideSize(s, value['sldSz'], ctx, 'sldSz');
  $q.flush(s, 4);
  if (value['notesSz'] !== undefined) writeCT_PositiveSize2D(s, value['notesSz'], ctx, 'notesSz');
  $q.flush(s, 5);
  if (value['smartTags'] !== undefined) writeCT_SmartTags(s, value['smartTags'], ctx, 'smartTags');
  $q.flush(s, 6);
  if (value['embeddedFontLst'] !== undefined) writeCT_EmbeddedFontList(s, value['embeddedFontLst'], ctx, 'embeddedFontLst');
  $q.flush(s, 7);
  if (value['custShowLst'] !== undefined) writeCT_CustomShowList(s, value['custShowLst'], ctx, 'custShowLst');
  $q.flush(s, 8);
  if (value['photoAlbum'] !== undefined) writeCT_PhotoAlbum(s, value['photoAlbum'], ctx, 'photoAlbum');
  $q.flush(s, 9);
  if (value['custDataLst'] !== undefined) writeCT_CustomerDataList(s, value['custDataLst'], ctx, 'custDataLst');
  $q.flush(s, 10);
  if (value['kinsoku'] !== undefined) writeCT_Kinsoku(s, value['kinsoku'], ctx, 'kinsoku');
  $q.flush(s, 11);
  if (value['defaultTextStyle'] !== undefined) writeCT_TextListStyle(s, value['defaultTextStyle'], ctx, 'defaultTextStyle');
  $q.flush(s, 12);
  if (value['modifyVerifier'] !== undefined) writeCT_ModifyVerifier(s, value['modifyVerifier'], ctx, 'modifyVerifier');
  $q.flush(s, 13);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 14);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_PresentationProperties`; the caller supplies its element local name. */
export function writeCT_PresentationProperties(s: XmlSink, value: CT_PresentationProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['htmlPubPr'] !== undefined) writeCT_HtmlPublishProperties(s, value['htmlPubPr'], ctx, 'htmlPubPr');
  $q.flush(s, 0);
  if (value['webPr'] !== undefined) writeCT_WebProperties(s, value['webPr'], ctx, 'webPr');
  $q.flush(s, 1);
  if (value['prnPr'] !== undefined) writeCT_PrintProperties(s, value['prnPr'], ctx, 'prnPr');
  $q.flush(s, 2);
  if (value['showPr'] !== undefined) writeCT_ShowProperties(s, value['showPr'], ctx, 'showPr');
  $q.flush(s, 3);
  if (value['clrMru'] !== undefined) writeCT_ColorMRU(s, value['clrMru'], ctx, 'clrMru');
  $q.flush(s, 4);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 5);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_PrintProperties`; the caller supplies its element local name. */
export function writeCT_PrintProperties(s: XmlSink, value: CT_PrintProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['prnWhat'] !== undefined) s.attr(null, 'prnWhat', String(value['prnWhat']));
  if (value['clrMode'] !== undefined) s.attr(null, 'clrMode', String(value['clrMode']));
  if (value['hiddenSlides'] !== undefined) s.attr(null, 'hiddenSlides', String(value['hiddenSlides']));
  if (value['scaleToFitPaper'] !== undefined) s.attr(null, 'scaleToFitPaper', String(value['scaleToFitPaper']));
  if (value['frameSlides'] !== undefined) s.attr(null, 'frameSlides', String(value['frameSlides']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Rel`; the caller supplies its element local name. */
export function writeCT_Rel(s: XmlSink, value: CT_Rel, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Shape`; the caller supplies its element local name. */
export function writeCT_Shape(s: XmlSink, value: CT_Shape, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['useBgFill'] !== undefined) s.attr(null, 'useBgFill', String(value['useBgFill']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['nvSpPr'] !== undefined) writeCT_ShapeNonVisual(s, value['nvSpPr'], ctx, 'nvSpPr');
  $q.flush(s, 0);
  if (value['spPr'] !== undefined) writeCT_ShapeProperties(s, value['spPr'], ctx, 'spPr');
  $q.flush(s, 1);
  if (value['style'] !== undefined) writeCT_ShapeStyle(s, value['style'], ctx, 'style');
  $q.flush(s, 2);
  if (value['txBody'] !== undefined) writeCT_TextBody(s, value['txBody'], ctx, 'txBody');
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ShapeNonVisual`; the caller supplies its element local name. */
export function writeCT_ShapeNonVisual(s: XmlSink, value: CT_ShapeNonVisual, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cNvPr'] !== undefined) writeCT_NonVisualDrawingProps(s, value['cNvPr'], ctx, 'cNvPr');
  $q.flush(s, 0);
  if (value['cNvSpPr'] !== undefined) writeCT_NonVisualDrawingShapeProps(s, value['cNvSpPr'], ctx, 'cNvSpPr');
  $q.flush(s, 1);
  if (value['nvPr'] !== undefined) writeCT_ApplicationNonVisualDrawingProps(s, value['nvPr'], ctx, 'nvPr');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ShowInfoBrowse`; the caller supplies its element local name. */
export function writeCT_ShowInfoBrowse(s: XmlSink, value: CT_ShowInfoBrowse, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['showScrollbar'] !== undefined) s.attr(null, 'showScrollbar', String(value['showScrollbar']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ShowInfoKiosk`; the caller supplies its element local name. */
export function writeCT_ShowInfoKiosk(s: XmlSink, value: CT_ShowInfoKiosk, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['restart'] !== undefined) s.attr(null, 'restart', String(value['restart']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ShowProperties`; the caller supplies its element local name. */
export function writeCT_ShowProperties(s: XmlSink, value: CT_ShowProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['loop'] !== undefined) s.attr(null, 'loop', String(value['loop']));
  if (value['showNarration'] !== undefined) s.attr(null, 'showNarration', String(value['showNarration']));
  if (value['showAnimation'] !== undefined) s.attr(null, 'showAnimation', String(value['showAnimation']));
  if (value['useTimings'] !== undefined) s.attr(null, 'useTimings', String(value['useTimings']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['showType'] !== undefined) {
    if (value['showType'].kind === 'present') writeCT_Empty(s, value['showType'].value, ctx, 'present');
    if (value['showType'].kind === 'browse') writeCT_ShowInfoBrowse(s, value['showType'].value, ctx, 'browse');
    if (value['showType'].kind === 'kiosk') writeCT_ShowInfoKiosk(s, value['showType'].value, ctx, 'kiosk');
  }
  $q.flush(s, 0);
  if (value['slideListChoice'] !== undefined) {
    if (value['slideListChoice'].kind === 'sldAll') writeCT_Empty(s, value['slideListChoice'].value, ctx, 'sldAll');
    if (value['slideListChoice'].kind === 'sldRg') writeCT_IndexRange(s, value['slideListChoice'].value, ctx, 'sldRg');
    if (value['slideListChoice'].kind === 'custShow') writeCT_CustomShowId(s, value['slideListChoice'].value, ctx, 'custShow');
  }
  $q.flush(s, 1);
  if (value['penClr'] !== undefined) writeCT_Color(s, value['penClr'], ctx, 'penClr');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SideDirectionTransition`; the caller supplies its element local name. */
export function writeCT_SideDirectionTransition(s: XmlSink, value: CT_SideDirectionTransition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['dir'] !== undefined) s.attr(null, 'dir', String(value['dir']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Slide`; the caller supplies its element local name. */
export function writeCT_Slide(s: XmlSink, value: CT_Slide, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['showMasterSp'] !== undefined) s.attr(null, 'showMasterSp', String(value['showMasterSp']));
  if (value['showMasterPhAnim'] !== undefined) s.attr(null, 'showMasterPhAnim', String(value['showMasterPhAnim']));
  if (value['show'] !== undefined) s.attr(null, 'show', String(value['show']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cSld'] !== undefined) writeCT_CommonSlideData(s, value['cSld'], ctx, 'cSld');
  $q.flush(s, 0);
  if (value['clrMapOvr'] !== undefined) writeCT_ColorMappingOverride(s, value['clrMapOvr'], ctx, 'clrMapOvr');
  $q.flush(s, 1);
  if (value['transition'] !== undefined) writeCT_SlideTransition(s, value['transition'], ctx, 'transition');
  $q.flush(s, 2);
  if (value['timing'] !== undefined) writeCT_SlideTiming(s, value['timing'], ctx, 'timing');
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideIdList`; the caller supplies its element local name. */
export function writeCT_SlideIdList(s: XmlSink, value: CT_SlideIdList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['sldId'].length; idx++) {
    const v_sldId = value['sldId'][idx]!;
    writeCT_SlideIdListEntry(s, v_sldId, ctx, 'sldId');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideIdListEntry`; the caller supplies its element local name. */
export function writeCT_SlideIdListEntry(s: XmlSink, value: CT_SlideIdListEntry, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(null, 'id', String(value['id']));
  if (value['id_relationships'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id_relationships']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideLayout`; the caller supplies its element local name. */
export function writeCT_SlideLayout(s: XmlSink, value: CT_SlideLayout, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['showMasterSp'] !== undefined) s.attr(null, 'showMasterSp', String(value['showMasterSp']));
  if (value['showMasterPhAnim'] !== undefined) s.attr(null, 'showMasterPhAnim', String(value['showMasterPhAnim']));
  if (value['matchingName'] !== undefined) s.attr(null, 'matchingName', String(value['matchingName']));
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['preserve'] !== undefined) s.attr(null, 'preserve', String(value['preserve']));
  if (value['userDrawn'] !== undefined) s.attr(null, 'userDrawn', String(value['userDrawn']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cSld'] !== undefined) writeCT_CommonSlideData(s, value['cSld'], ctx, 'cSld');
  $q.flush(s, 0);
  if (value['clrMapOvr'] !== undefined) writeCT_ColorMappingOverride(s, value['clrMapOvr'], ctx, 'clrMapOvr');
  $q.flush(s, 1);
  if (value['transition'] !== undefined) writeCT_SlideTransition(s, value['transition'], ctx, 'transition');
  $q.flush(s, 2);
  if (value['timing'] !== undefined) writeCT_SlideTiming(s, value['timing'], ctx, 'timing');
  $q.flush(s, 3);
  if (value['hf'] !== undefined) writeCT_HeaderFooter(s, value['hf'], ctx, 'hf');
  $q.flush(s, 4);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 5);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideLayoutIdList`; the caller supplies its element local name. */
export function writeCT_SlideLayoutIdList(s: XmlSink, value: CT_SlideLayoutIdList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['sldLayoutId'].length; idx++) {
    const v_sldLayoutId = value['sldLayoutId'][idx]!;
    writeCT_SlideLayoutIdListEntry(s, v_sldLayoutId, ctx, 'sldLayoutId');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideLayoutIdListEntry`; the caller supplies its element local name. */
export function writeCT_SlideLayoutIdListEntry(s: XmlSink, value: CT_SlideLayoutIdListEntry, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(null, 'id', String(value['id']));
  if (value['id_relationships'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id_relationships']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideMaster`; the caller supplies its element local name. */
export function writeCT_SlideMaster(s: XmlSink, value: CT_SlideMaster, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['preserve'] !== undefined) s.attr(null, 'preserve', String(value['preserve']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cSld'] !== undefined) writeCT_CommonSlideData(s, value['cSld'], ctx, 'cSld');
  $q.flush(s, 0);
  if (value['clrMap'] !== undefined) writeCT_ColorMapping(s, value['clrMap'], ctx, 'clrMap');
  $q.flush(s, 1);
  if (value['sldLayoutIdLst'] !== undefined) writeCT_SlideLayoutIdList(s, value['sldLayoutIdLst'], ctx, 'sldLayoutIdLst');
  $q.flush(s, 2);
  if (value['transition'] !== undefined) writeCT_SlideTransition(s, value['transition'], ctx, 'transition');
  $q.flush(s, 3);
  if (value['timing'] !== undefined) writeCT_SlideTiming(s, value['timing'], ctx, 'timing');
  $q.flush(s, 4);
  if (value['hf'] !== undefined) writeCT_HeaderFooter(s, value['hf'], ctx, 'hf');
  $q.flush(s, 5);
  if (value['txStyles'] !== undefined) writeCT_SlideMasterTextStyles(s, value['txStyles'], ctx, 'txStyles');
  $q.flush(s, 6);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 7);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideMasterIdList`; the caller supplies its element local name. */
export function writeCT_SlideMasterIdList(s: XmlSink, value: CT_SlideMasterIdList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['sldMasterId'].length; idx++) {
    const v_sldMasterId = value['sldMasterId'][idx]!;
    writeCT_SlideMasterIdListEntry(s, v_sldMasterId, ctx, 'sldMasterId');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideMasterIdListEntry`; the caller supplies its element local name. */
export function writeCT_SlideMasterIdListEntry(s: XmlSink, value: CT_SlideMasterIdListEntry, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(null, 'id', String(value['id']));
  if (value['id_relationships'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id_relationships']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideMasterTextStyles`; the caller supplies its element local name. */
export function writeCT_SlideMasterTextStyles(s: XmlSink, value: CT_SlideMasterTextStyles, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['titleStyle'] !== undefined) writeCT_TextListStyle(s, value['titleStyle'], ctx, 'titleStyle');
  $q.flush(s, 0);
  if (value['bodyStyle'] !== undefined) writeCT_TextListStyle(s, value['bodyStyle'], ctx, 'bodyStyle');
  $q.flush(s, 1);
  if (value['otherStyle'] !== undefined) writeCT_TextListStyle(s, value['otherStyle'], ctx, 'otherStyle');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideRelationshipList`; the caller supplies its element local name. */
export function writeCT_SlideRelationshipList(s: XmlSink, value: CT_SlideRelationshipList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['sld'].length; idx++) {
    const v_sld = value['sld'][idx]!;
    writeCT_SlideRelationshipListEntry(s, v_sld, ctx, 'sld');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideRelationshipListEntry`; the caller supplies its element local name. */
export function writeCT_SlideRelationshipListEntry(s: XmlSink, value: CT_SlideRelationshipListEntry, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_SlideSize`; the caller supplies its element local name. */
export function writeCT_SlideSize(s: XmlSink, value: CT_SlideSize, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['cx'] !== undefined) s.attr(null, 'cx', String(value['cx']));
  if (value['cy'] !== undefined) s.attr(null, 'cy', String(value['cy']));
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_SlideSorterViewProperties`; the caller supplies its element local name. */
export function writeCT_SlideSorterViewProperties(s: XmlSink, value: CT_SlideSorterViewProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['showFormatting'] !== undefined) s.attr(null, 'showFormatting', String(value['showFormatting']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cViewPr'] !== undefined) writeCT_CommonViewProperties(s, value['cViewPr'], ctx, 'cViewPr');
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideSyncProperties`; the caller supplies its element local name. */
export function writeCT_SlideSyncProperties(s: XmlSink, value: CT_SlideSyncProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['serverSldId'] !== undefined) s.attr(null, 'serverSldId', String(value['serverSldId']));
  if (value['serverSldModifiedTime'] !== undefined) s.attr(null, 'serverSldModifiedTime', String(value['serverSldModifiedTime']));
  if (value['clientInsertedTime'] !== undefined) s.attr(null, 'clientInsertedTime', String(value['clientInsertedTime']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideTiming`; the caller supplies its element local name. */
export function writeCT_SlideTiming(s: XmlSink, value: CT_SlideTiming, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['tnLst'] !== undefined) writeCT_TimeNodeList(s, value['tnLst'], ctx, 'tnLst');
  $q.flush(s, 0);
  if (value['bldLst'] !== undefined) writeCT_BuildList(s, value['bldLst'], ctx, 'bldLst');
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideTransition`; the caller supplies its element local name. */
export function writeCT_SlideTransition(s: XmlSink, value: CT_SlideTransition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['spd'] !== undefined) s.attr(null, 'spd', String(value['spd']));
  if (value['advClick'] !== undefined) s.attr(null, 'advClick', String(value['advClick']));
  if (value['advTm'] !== undefined) s.attr(null, 'advTm', String(value['advTm']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'blinds') writeCT_OrientationTransition(s, value['content'].value, ctx, 'blinds');
    if (value['content'].kind === 'checker') writeCT_OrientationTransition(s, value['content'].value, ctx, 'checker');
    if (value['content'].kind === 'circle') writeCT_Empty(s, value['content'].value, ctx, 'circle');
    if (value['content'].kind === 'dissolve') writeCT_Empty(s, value['content'].value, ctx, 'dissolve');
    if (value['content'].kind === 'comb') writeCT_OrientationTransition(s, value['content'].value, ctx, 'comb');
    if (value['content'].kind === 'cover') writeCT_EightDirectionTransition(s, value['content'].value, ctx, 'cover');
    if (value['content'].kind === 'cut') writeCT_OptionalBlackTransition(s, value['content'].value, ctx, 'cut');
    if (value['content'].kind === 'diamond') writeCT_Empty(s, value['content'].value, ctx, 'diamond');
    if (value['content'].kind === 'fade') writeCT_OptionalBlackTransition(s, value['content'].value, ctx, 'fade');
    if (value['content'].kind === 'newsflash') writeCT_Empty(s, value['content'].value, ctx, 'newsflash');
    if (value['content'].kind === 'plus') writeCT_Empty(s, value['content'].value, ctx, 'plus');
    if (value['content'].kind === 'pull') writeCT_EightDirectionTransition(s, value['content'].value, ctx, 'pull');
    if (value['content'].kind === 'push') writeCT_SideDirectionTransition(s, value['content'].value, ctx, 'push');
    if (value['content'].kind === 'random') writeCT_Empty(s, value['content'].value, ctx, 'random');
    if (value['content'].kind === 'randomBar') writeCT_OrientationTransition(s, value['content'].value, ctx, 'randomBar');
    if (value['content'].kind === 'split') writeCT_SplitTransition(s, value['content'].value, ctx, 'split');
    if (value['content'].kind === 'strips') writeCT_CornerDirectionTransition(s, value['content'].value, ctx, 'strips');
    if (value['content'].kind === 'wedge') writeCT_Empty(s, value['content'].value, ctx, 'wedge');
    if (value['content'].kind === 'wheel') writeCT_WheelTransition(s, value['content'].value, ctx, 'wheel');
    if (value['content'].kind === 'wipe') writeCT_SideDirectionTransition(s, value['content'].value, ctx, 'wipe');
    if (value['content'].kind === 'zoom') writeCT_InOutTransition(s, value['content'].value, ctx, 'zoom');
  }
  $q.flush(s, 0);
  if (value['sndAc'] !== undefined) writeCT_TransitionSoundAction(s, value['sndAc'], ctx, 'sndAc');
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_ExtensionListModify(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SlideViewProperties`; the caller supplies its element local name. */
export function writeCT_SlideViewProperties(s: XmlSink, value: CT_SlideViewProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cSldViewPr'] !== undefined) writeCT_CommonSlideViewProperties(s, value['cSldViewPr'], ctx, 'cSldViewPr');
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SmartTags`; the caller supplies its element local name. */
export function writeCT_SmartTags(s: XmlSink, value: CT_SmartTags, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_SplitTransition`; the caller supplies its element local name. */
export function writeCT_SplitTransition(s: XmlSink, value: CT_SplitTransition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['orient'] !== undefined) s.attr(null, 'orient', String(value['orient']));
  if (value['dir'] !== undefined) s.attr(null, 'dir', String(value['dir']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_StringTag`; the caller supplies its element local name. */
export function writeCT_StringTag(s: XmlSink, value: CT_StringTag, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TagList`; the caller supplies its element local name. */
export function writeCT_TagList(s: XmlSink, value: CT_TagList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['tag'].length; idx++) {
    const v_tag = value['tag'][idx]!;
    writeCT_StringTag(s, v_tag, ctx, 'tag');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TagsData`; the caller supplies its element local name. */
export function writeCT_TagsData(s: XmlSink, value: CT_TagsData, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TimeNodeList`; the caller supplies its element local name. */
export function writeCT_TimeNodeList(s: XmlSink, value: CT_TimeNodeList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_content of value['content']) {
    if (v_content.kind === 'par') writeCT_TLTimeNodeParallel(s, v_content.value, ctx, 'par');
    if (v_content.kind === 'seq') writeCT_TLTimeNodeSequence(s, v_content.value, ctx, 'seq');
    if (v_content.kind === 'excl') writeCT_TLTimeNodeExclusive(s, v_content.value, ctx, 'excl');
    if (v_content.kind === 'anim') writeCT_TLAnimateBehavior(s, v_content.value, ctx, 'anim');
    if (v_content.kind === 'animClr') writeCT_TLAnimateColorBehavior(s, v_content.value, ctx, 'animClr');
    if (v_content.kind === 'animEffect') writeCT_TLAnimateEffectBehavior(s, v_content.value, ctx, 'animEffect');
    if (v_content.kind === 'animMotion') writeCT_TLAnimateMotionBehavior(s, v_content.value, ctx, 'animMotion');
    if (v_content.kind === 'animRot') writeCT_TLAnimateRotationBehavior(s, v_content.value, ctx, 'animRot');
    if (v_content.kind === 'animScale') writeCT_TLAnimateScaleBehavior(s, v_content.value, ctx, 'animScale');
    if (v_content.kind === 'cmd') writeCT_TLCommandBehavior(s, v_content.value, ctx, 'cmd');
    if (v_content.kind === 'set') writeCT_TLSetBehavior(s, v_content.value, ctx, 'set');
    if (v_content.kind === 'audio') writeCT_TLMediaNodeAudio(s, v_content.value, ctx, 'audio');
    if (v_content.kind === 'video') writeCT_TLMediaNodeVideo(s, v_content.value, ctx, 'video');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  s.endElement();
}

/** Write a `CT_TLAnimateBehavior`; the caller supplies its element local name. */
export function writeCT_TLAnimateBehavior(s: XmlSink, value: CT_TLAnimateBehavior, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['by'] !== undefined) s.attr(null, 'by', String(value['by']));
  if (value['from'] !== undefined) s.attr(null, 'from', String(value['from']));
  if (value['to'] !== undefined) s.attr(null, 'to', String(value['to']));
  if (value['calcmode'] !== undefined) s.attr(null, 'calcmode', String(value['calcmode']));
  if (value['valueType'] !== undefined) s.attr(null, 'valueType', String(value['valueType']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cBhvr'] !== undefined) writeCT_TLCommonBehaviorData(s, value['cBhvr'], ctx, 'cBhvr');
  $q.flush(s, 0);
  if (value['tavLst'] !== undefined) writeCT_TLTimeAnimateValueList(s, value['tavLst'], ctx, 'tavLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLAnimateColorBehavior`; the caller supplies its element local name. */
export function writeCT_TLAnimateColorBehavior(s: XmlSink, value: CT_TLAnimateColorBehavior, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['clrSpc'] !== undefined) s.attr(null, 'clrSpc', String(value['clrSpc']));
  if (value['dir'] !== undefined) s.attr(null, 'dir', String(value['dir']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cBhvr'] !== undefined) writeCT_TLCommonBehaviorData(s, value['cBhvr'], ctx, 'cBhvr');
  $q.flush(s, 0);
  if (value['by'] !== undefined) writeCT_TLByAnimateColorTransform(s, value['by'], ctx, 'by');
  $q.flush(s, 1);
  if (value['from'] !== undefined) writeCT_Color(s, value['from'], ctx, 'from');
  $q.flush(s, 2);
  if (value['to'] !== undefined) writeCT_Color(s, value['to'], ctx, 'to');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLAnimateEffectBehavior`; the caller supplies its element local name. */
export function writeCT_TLAnimateEffectBehavior(s: XmlSink, value: CT_TLAnimateEffectBehavior, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['transition'] !== undefined) s.attr(null, 'transition', String(value['transition']));
  if (value['filter'] !== undefined) s.attr(null, 'filter', String(value['filter']));
  if (value['prLst'] !== undefined) s.attr(null, 'prLst', String(value['prLst']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cBhvr'] !== undefined) writeCT_TLCommonBehaviorData(s, value['cBhvr'], ctx, 'cBhvr');
  $q.flush(s, 0);
  if (value['progress'] !== undefined) writeCT_TLAnimVariant(s, value['progress'], ctx, 'progress');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLAnimateMotionBehavior`; the caller supplies its element local name. */
export function writeCT_TLAnimateMotionBehavior(s: XmlSink, value: CT_TLAnimateMotionBehavior, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['origin'] !== undefined) s.attr(null, 'origin', String(value['origin']));
  if (value['path'] !== undefined) s.attr(null, 'path', String(value['path']));
  if (value['pathEditMode'] !== undefined) s.attr(null, 'pathEditMode', String(value['pathEditMode']));
  if (value['rAng'] !== undefined) s.attr(null, 'rAng', String(value['rAng']));
  if (value['ptsTypes'] !== undefined) s.attr(null, 'ptsTypes', String(value['ptsTypes']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cBhvr'] !== undefined) writeCT_TLCommonBehaviorData(s, value['cBhvr'], ctx, 'cBhvr');
  $q.flush(s, 0);
  if (value['by'] !== undefined) writeCT_TLPoint(s, value['by'], ctx, 'by');
  $q.flush(s, 1);
  if (value['from'] !== undefined) writeCT_TLPoint(s, value['from'], ctx, 'from');
  $q.flush(s, 2);
  if (value['to'] !== undefined) writeCT_TLPoint(s, value['to'], ctx, 'to');
  $q.flush(s, 3);
  if (value['rCtr'] !== undefined) writeCT_TLPoint(s, value['rCtr'], ctx, 'rCtr');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLAnimateRotationBehavior`; the caller supplies its element local name. */
export function writeCT_TLAnimateRotationBehavior(s: XmlSink, value: CT_TLAnimateRotationBehavior, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['by'] !== undefined) s.attr(null, 'by', String(value['by']));
  if (value['from'] !== undefined) s.attr(null, 'from', String(value['from']));
  if (value['to'] !== undefined) s.attr(null, 'to', String(value['to']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cBhvr'] !== undefined) writeCT_TLCommonBehaviorData(s, value['cBhvr'], ctx, 'cBhvr');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLAnimateScaleBehavior`; the caller supplies its element local name. */
export function writeCT_TLAnimateScaleBehavior(s: XmlSink, value: CT_TLAnimateScaleBehavior, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['zoomContents'] !== undefined) s.attr(null, 'zoomContents', String(value['zoomContents']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cBhvr'] !== undefined) writeCT_TLCommonBehaviorData(s, value['cBhvr'], ctx, 'cBhvr');
  $q.flush(s, 0);
  if (value['by'] !== undefined) writeCT_TLPoint(s, value['by'], ctx, 'by');
  $q.flush(s, 1);
  if (value['from'] !== undefined) writeCT_TLPoint(s, value['from'], ctx, 'from');
  $q.flush(s, 2);
  if (value['to'] !== undefined) writeCT_TLPoint(s, value['to'], ctx, 'to');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLAnimVariant`; the caller supplies its element local name. */
export function writeCT_TLAnimVariant(s: XmlSink, value: CT_TLAnimVariant, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'boolVal') writeCT_TLAnimVariantBooleanVal(s, value['content'].value, ctx, 'boolVal');
    if (value['content'].kind === 'intVal') writeCT_TLAnimVariantIntegerVal(s, value['content'].value, ctx, 'intVal');
    if (value['content'].kind === 'fltVal') writeCT_TLAnimVariantFloatVal(s, value['content'].value, ctx, 'fltVal');
    if (value['content'].kind === 'strVal') writeCT_TLAnimVariantStringVal(s, value['content'].value, ctx, 'strVal');
    if (value['content'].kind === 'clrVal') writeCT_Color(s, value['content'].value, ctx, 'clrVal');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLAnimVariantBooleanVal`; the caller supplies its element local name. */
export function writeCT_TLAnimVariantBooleanVal(s: XmlSink, value: CT_TLAnimVariantBooleanVal, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLAnimVariantFloatVal`; the caller supplies its element local name. */
export function writeCT_TLAnimVariantFloatVal(s: XmlSink, value: CT_TLAnimVariantFloatVal, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLAnimVariantIntegerVal`; the caller supplies its element local name. */
export function writeCT_TLAnimVariantIntegerVal(s: XmlSink, value: CT_TLAnimVariantIntegerVal, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLAnimVariantStringVal`; the caller supplies its element local name. */
export function writeCT_TLAnimVariantStringVal(s: XmlSink, value: CT_TLAnimVariantStringVal, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLBehaviorAttributeNameList`; the caller supplies its element local name. */
export function writeCT_TLBehaviorAttributeNameList(s: XmlSink, value: CT_TLBehaviorAttributeNameList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['attrName'].length; idx++) {
    const v_attrName = value['attrName'][idx];
    if (v_attrName !== undefined) s.text(String(v_attrName));
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLBuildDiagram`; the caller supplies its element local name. */
export function writeCT_TLBuildDiagram(s: XmlSink, value: CT_TLBuildDiagram, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['spid'] !== undefined) s.attr(null, 'spid', String(value['spid']));
  if (value['grpId'] !== undefined) s.attr(null, 'grpId', String(value['grpId']));
  if (value['uiExpand'] !== undefined) s.attr(null, 'uiExpand', String(value['uiExpand']));
  if (value['bld'] !== undefined) s.attr(null, 'bld', String(value['bld']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLBuildParagraph`; the caller supplies its element local name. */
export function writeCT_TLBuildParagraph(s: XmlSink, value: CT_TLBuildParagraph, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['spid'] !== undefined) s.attr(null, 'spid', String(value['spid']));
  if (value['grpId'] !== undefined) s.attr(null, 'grpId', String(value['grpId']));
  if (value['uiExpand'] !== undefined) s.attr(null, 'uiExpand', String(value['uiExpand']));
  if (value['build'] !== undefined) s.attr(null, 'build', String(value['build']));
  if (value['bldLvl'] !== undefined) s.attr(null, 'bldLvl', String(value['bldLvl']));
  if (value['animBg'] !== undefined) s.attr(null, 'animBg', String(value['animBg']));
  if (value['autoUpdateAnimBg'] !== undefined) s.attr(null, 'autoUpdateAnimBg', String(value['autoUpdateAnimBg']));
  if (value['rev'] !== undefined) s.attr(null, 'rev', String(value['rev']));
  if (value['advAuto'] !== undefined) s.attr(null, 'advAuto', String(value['advAuto']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['tmplLst'] !== undefined) writeCT_TLTemplateList(s, value['tmplLst'], ctx, 'tmplLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLByAnimateColorTransform`; the caller supplies its element local name. */
export function writeCT_TLByAnimateColorTransform(s: XmlSink, value: CT_TLByAnimateColorTransform, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'rgb') writeCT_TLByRgbColorTransform(s, value['content'].value, ctx, 'rgb');
    if (value['content'].kind === 'hsl') writeCT_TLByHslColorTransform(s, value['content'].value, ctx, 'hsl');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLByHslColorTransform`; the caller supplies its element local name. */
export function writeCT_TLByHslColorTransform(s: XmlSink, value: CT_TLByHslColorTransform, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['h'] !== undefined) s.attr(null, 'h', String(value['h']));
  if (value['s'] !== undefined) s.attr(null, 's', String(value['s']));
  if (value['l'] !== undefined) s.attr(null, 'l', String(value['l']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLByRgbColorTransform`; the caller supplies its element local name. */
export function writeCT_TLByRgbColorTransform(s: XmlSink, value: CT_TLByRgbColorTransform, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['r'] !== undefined) s.attr(null, 'r', String(value['r']));
  if (value['g'] !== undefined) s.attr(null, 'g', String(value['g']));
  if (value['b'] !== undefined) s.attr(null, 'b', String(value['b']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLCommandBehavior`; the caller supplies its element local name. */
export function writeCT_TLCommandBehavior(s: XmlSink, value: CT_TLCommandBehavior, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['cmd'] !== undefined) s.attr(null, 'cmd', String(value['cmd']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cBhvr'] !== undefined) writeCT_TLCommonBehaviorData(s, value['cBhvr'], ctx, 'cBhvr');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLCommonBehaviorData`; the caller supplies its element local name. */
export function writeCT_TLCommonBehaviorData(s: XmlSink, value: CT_TLCommonBehaviorData, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['additive'] !== undefined) s.attr(null, 'additive', String(value['additive']));
  if (value['accumulate'] !== undefined) s.attr(null, 'accumulate', String(value['accumulate']));
  if (value['xfrmType'] !== undefined) s.attr(null, 'xfrmType', String(value['xfrmType']));
  if (value['from'] !== undefined) s.attr(null, 'from', String(value['from']));
  if (value['to'] !== undefined) s.attr(null, 'to', String(value['to']));
  if (value['by'] !== undefined) s.attr(null, 'by', String(value['by']));
  if (value['rctx'] !== undefined) s.attr(null, 'rctx', String(value['rctx']));
  if (value['override'] !== undefined) s.attr(null, 'override', String(value['override']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cTn'] !== undefined) writeCT_TLCommonTimeNodeData(s, value['cTn'], ctx, 'cTn');
  $q.flush(s, 0);
  if (value['tgtEl'] !== undefined) writeCT_TLTimeTargetElement(s, value['tgtEl'], ctx, 'tgtEl');
  $q.flush(s, 1);
  if (value['attrNameLst'] !== undefined) writeCT_TLBehaviorAttributeNameList(s, value['attrNameLst'], ctx, 'attrNameLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLCommonMediaNodeData`; the caller supplies its element local name. */
export function writeCT_TLCommonMediaNodeData(s: XmlSink, value: CT_TLCommonMediaNodeData, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['vol'] !== undefined) s.attr(null, 'vol', String(value['vol']));
  if (value['mute'] !== undefined) s.attr(null, 'mute', String(value['mute']));
  if (value['numSld'] !== undefined) s.attr(null, 'numSld', String(value['numSld']));
  if (value['showWhenStopped'] !== undefined) s.attr(null, 'showWhenStopped', String(value['showWhenStopped']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cTn'] !== undefined) writeCT_TLCommonTimeNodeData(s, value['cTn'], ctx, 'cTn');
  $q.flush(s, 0);
  if (value['tgtEl'] !== undefined) writeCT_TLTimeTargetElement(s, value['tgtEl'], ctx, 'tgtEl');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLCommonTimeNodeData`; the caller supplies its element local name. */
export function writeCT_TLCommonTimeNodeData(s: XmlSink, value: CT_TLCommonTimeNodeData, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['id'] !== undefined) s.attr(null, 'id', String(value['id']));
  if (value['presetID'] !== undefined) s.attr(null, 'presetID', String(value['presetID']));
  if (value['presetClass'] !== undefined) s.attr(null, 'presetClass', String(value['presetClass']));
  if (value['presetSubtype'] !== undefined) s.attr(null, 'presetSubtype', String(value['presetSubtype']));
  if (value['dur'] !== undefined) s.attr(null, 'dur', String(value['dur']));
  if (value['repeatCount'] !== undefined) s.attr(null, 'repeatCount', String(value['repeatCount']));
  if (value['repeatDur'] !== undefined) s.attr(null, 'repeatDur', String(value['repeatDur']));
  if (value['spd'] !== undefined) s.attr(null, 'spd', String(value['spd']));
  if (value['accel'] !== undefined) s.attr(null, 'accel', String(value['accel']));
  if (value['decel'] !== undefined) s.attr(null, 'decel', String(value['decel']));
  if (value['autoRev'] !== undefined) s.attr(null, 'autoRev', String(value['autoRev']));
  if (value['restart'] !== undefined) s.attr(null, 'restart', String(value['restart']));
  if (value['fill'] !== undefined) s.attr(null, 'fill', String(value['fill']));
  if (value['syncBehavior'] !== undefined) s.attr(null, 'syncBehavior', String(value['syncBehavior']));
  if (value['tmFilter'] !== undefined) s.attr(null, 'tmFilter', String(value['tmFilter']));
  if (value['evtFilter'] !== undefined) s.attr(null, 'evtFilter', String(value['evtFilter']));
  if (value['display'] !== undefined) s.attr(null, 'display', String(value['display']));
  if (value['masterRel'] !== undefined) s.attr(null, 'masterRel', String(value['masterRel']));
  if (value['bldLvl'] !== undefined) s.attr(null, 'bldLvl', String(value['bldLvl']));
  if (value['grpId'] !== undefined) s.attr(null, 'grpId', String(value['grpId']));
  if (value['afterEffect'] !== undefined) s.attr(null, 'afterEffect', String(value['afterEffect']));
  if (value['nodeType'] !== undefined) s.attr(null, 'nodeType', String(value['nodeType']));
  if (value['nodePh'] !== undefined) s.attr(null, 'nodePh', String(value['nodePh']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['stCondLst'] !== undefined) writeCT_TLTimeConditionList(s, value['stCondLst'], ctx, 'stCondLst');
  $q.flush(s, 0);
  if (value['endCondLst'] !== undefined) writeCT_TLTimeConditionList(s, value['endCondLst'], ctx, 'endCondLst');
  $q.flush(s, 1);
  if (value['endSync'] !== undefined) writeCT_TLTimeCondition(s, value['endSync'], ctx, 'endSync');
  $q.flush(s, 2);
  if (value['iterate'] !== undefined) writeCT_TLIterateData(s, value['iterate'], ctx, 'iterate');
  $q.flush(s, 3);
  if (value['childTnLst'] !== undefined) writeCT_TimeNodeList(s, value['childTnLst'], ctx, 'childTnLst');
  $q.flush(s, 4);
  if (value['subTnLst'] !== undefined) writeCT_TimeNodeList(s, value['subTnLst'], ctx, 'subTnLst');
  $q.flush(s, 5);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLGraphicalObjectBuild`; the caller supplies its element local name. */
export function writeCT_TLGraphicalObjectBuild(s: XmlSink, value: CT_TLGraphicalObjectBuild, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['spid'] !== undefined) s.attr(null, 'spid', String(value['spid']));
  if (value['grpId'] !== undefined) s.attr(null, 'grpId', String(value['grpId']));
  if (value['uiExpand'] !== undefined) s.attr(null, 'uiExpand', String(value['uiExpand']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'bldAsOne') writeCT_Empty(s, value['content'].value, ctx, 'bldAsOne');
    if (value['content'].kind === 'bldSub') writeCT_AnimationGraphicalObjectBuildProperties(s, value['content'].value, ctx, 'bldSub');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLIterateData`; the caller supplies its element local name. */
export function writeCT_TLIterateData(s: XmlSink, value: CT_TLIterateData, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['backwards'] !== undefined) s.attr(null, 'backwards', String(value['backwards']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'tmAbs') writeCT_TLIterateIntervalTime(s, value['content'].value, ctx, 'tmAbs');
    if (value['content'].kind === 'tmPct') writeCT_TLIterateIntervalPercentage(s, value['content'].value, ctx, 'tmPct');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLIterateIntervalPercentage`; the caller supplies its element local name. */
export function writeCT_TLIterateIntervalPercentage(s: XmlSink, value: CT_TLIterateIntervalPercentage, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLIterateIntervalTime`; the caller supplies its element local name. */
export function writeCT_TLIterateIntervalTime(s: XmlSink, value: CT_TLIterateIntervalTime, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLMediaNodeAudio`; the caller supplies its element local name. */
export function writeCT_TLMediaNodeAudio(s: XmlSink, value: CT_TLMediaNodeAudio, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['isNarration'] !== undefined) s.attr(null, 'isNarration', String(value['isNarration']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cMediaNode'] !== undefined) writeCT_TLCommonMediaNodeData(s, value['cMediaNode'], ctx, 'cMediaNode');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLMediaNodeVideo`; the caller supplies its element local name. */
export function writeCT_TLMediaNodeVideo(s: XmlSink, value: CT_TLMediaNodeVideo, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['fullScrn'] !== undefined) s.attr(null, 'fullScrn', String(value['fullScrn']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cMediaNode'] !== undefined) writeCT_TLCommonMediaNodeData(s, value['cMediaNode'], ctx, 'cMediaNode');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLOleBuildChart`; the caller supplies its element local name. */
export function writeCT_TLOleBuildChart(s: XmlSink, value: CT_TLOleBuildChart, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['spid'] !== undefined) s.attr(null, 'spid', String(value['spid']));
  if (value['grpId'] !== undefined) s.attr(null, 'grpId', String(value['grpId']));
  if (value['uiExpand'] !== undefined) s.attr(null, 'uiExpand', String(value['uiExpand']));
  if (value['bld'] !== undefined) s.attr(null, 'bld', String(value['bld']));
  if (value['animBg'] !== undefined) s.attr(null, 'animBg', String(value['animBg']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLOleChartTargetElement`; the caller supplies its element local name. */
export function writeCT_TLOleChartTargetElement(s: XmlSink, value: CT_TLOleChartTargetElement, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['lvl'] !== undefined) s.attr(null, 'lvl', String(value['lvl']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLPoint`; the caller supplies its element local name. */
export function writeCT_TLPoint(s: XmlSink, value: CT_TLPoint, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['x'] !== undefined) s.attr(null, 'x', String(value['x']));
  if (value['y'] !== undefined) s.attr(null, 'y', String(value['y']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLSetBehavior`; the caller supplies its element local name. */
export function writeCT_TLSetBehavior(s: XmlSink, value: CT_TLSetBehavior, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cBhvr'] !== undefined) writeCT_TLCommonBehaviorData(s, value['cBhvr'], ctx, 'cBhvr');
  $q.flush(s, 0);
  if (value['to'] !== undefined) writeCT_TLAnimVariant(s, value['to'], ctx, 'to');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLShapeTargetElement`; the caller supplies its element local name. */
export function writeCT_TLShapeTargetElement(s: XmlSink, value: CT_TLShapeTargetElement, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['spid'] !== undefined) s.attr(null, 'spid', String(value['spid']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'bg') writeCT_Empty(s, value['content'].value, ctx, 'bg');
    if (value['content'].kind === 'subSp') writeCT_TLSubShapeId(s, value['content'].value, ctx, 'subSp');
    if (value['content'].kind === 'oleChartEl') writeCT_TLOleChartTargetElement(s, value['content'].value, ctx, 'oleChartEl');
    if (value['content'].kind === 'txEl') writeCT_TLTextTargetElement(s, value['content'].value, ctx, 'txEl');
    if (value['content'].kind === 'graphicEl') writeCT_AnimationElementChoice(s, value['content'].value, ctx, 'graphicEl');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLSubShapeId`; the caller supplies its element local name. */
export function writeCT_TLSubShapeId(s: XmlSink, value: CT_TLSubShapeId, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['spid'] !== undefined) s.attr(null, 'spid', String(value['spid']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLTemplate`; the caller supplies its element local name. */
export function writeCT_TLTemplate(s: XmlSink, value: CT_TLTemplate, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['lvl'] !== undefined) s.attr(null, 'lvl', String(value['lvl']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['tnLst'] !== undefined) writeCT_TimeNodeList(s, value['tnLst'], ctx, 'tnLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLTemplateList`; the caller supplies its element local name. */
export function writeCT_TLTemplateList(s: XmlSink, value: CT_TLTemplateList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['tmpl'].length; idx++) {
    const v_tmpl = value['tmpl'][idx]!;
    writeCT_TLTemplate(s, v_tmpl, ctx, 'tmpl');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLTextTargetElement`; the caller supplies its element local name. */
export function writeCT_TLTextTargetElement(s: XmlSink, value: CT_TLTextTargetElement, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'charRg') writeCT_IndexRange(s, value['content'].value, ctx, 'charRg');
    if (value['content'].kind === 'pRg') writeCT_IndexRange(s, value['content'].value, ctx, 'pRg');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLTimeAnimateValue`; the caller supplies its element local name. */
export function writeCT_TLTimeAnimateValue(s: XmlSink, value: CT_TLTimeAnimateValue, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['tm'] !== undefined) s.attr(null, 'tm', String(value['tm']));
  if (value['fmla'] !== undefined) s.attr(null, 'fmla', String(value['fmla']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['val'] !== undefined) writeCT_TLAnimVariant(s, value['val'], ctx, 'val');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLTimeAnimateValueList`; the caller supplies its element local name. */
export function writeCT_TLTimeAnimateValueList(s: XmlSink, value: CT_TLTimeAnimateValueList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['tav'].length; idx++) {
    const v_tav = value['tav'][idx]!;
    writeCT_TLTimeAnimateValue(s, v_tav, ctx, 'tav');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLTimeCondition`; the caller supplies its element local name. */
export function writeCT_TLTimeCondition(s: XmlSink, value: CT_TLTimeCondition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['evt'] !== undefined) s.attr(null, 'evt', String(value['evt']));
  if (value['delay'] !== undefined) s.attr(null, 'delay', String(value['delay']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'tgtEl') writeCT_TLTimeTargetElement(s, value['content'].value, ctx, 'tgtEl');
    if (value['content'].kind === 'tn') writeCT_TLTriggerTimeNodeID(s, value['content'].value, ctx, 'tn');
    if (value['content'].kind === 'rtn') writeCT_TLTriggerRuntimeNode(s, value['content'].value, ctx, 'rtn');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLTimeConditionList`; the caller supplies its element local name. */
export function writeCT_TLTimeConditionList(s: XmlSink, value: CT_TLTimeConditionList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['cond'].length; idx++) {
    const v_cond = value['cond'][idx]!;
    writeCT_TLTimeCondition(s, v_cond, ctx, 'cond');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLTimeNodeExclusive`; the caller supplies its element local name. */
export function writeCT_TLTimeNodeExclusive(s: XmlSink, value: CT_TLTimeNodeExclusive, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cTn'] !== undefined) writeCT_TLCommonTimeNodeData(s, value['cTn'], ctx, 'cTn');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLTimeNodeParallel`; the caller supplies its element local name. */
export function writeCT_TLTimeNodeParallel(s: XmlSink, value: CT_TLTimeNodeParallel, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cTn'] !== undefined) writeCT_TLCommonTimeNodeData(s, value['cTn'], ctx, 'cTn');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLTimeNodeSequence`; the caller supplies its element local name. */
export function writeCT_TLTimeNodeSequence(s: XmlSink, value: CT_TLTimeNodeSequence, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['concurrent'] !== undefined) s.attr(null, 'concurrent', String(value['concurrent']));
  if (value['prevAc'] !== undefined) s.attr(null, 'prevAc', String(value['prevAc']));
  if (value['nextAc'] !== undefined) s.attr(null, 'nextAc', String(value['nextAc']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cTn'] !== undefined) writeCT_TLCommonTimeNodeData(s, value['cTn'], ctx, 'cTn');
  $q.flush(s, 0);
  if (value['prevCondLst'] !== undefined) writeCT_TLTimeConditionList(s, value['prevCondLst'], ctx, 'prevCondLst');
  $q.flush(s, 1);
  if (value['nextCondLst'] !== undefined) writeCT_TLTimeConditionList(s, value['nextCondLst'], ctx, 'nextCondLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLTimeTargetElement`; the caller supplies its element local name. */
export function writeCT_TLTimeTargetElement(s: XmlSink, value: CT_TLTimeTargetElement, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'sldTgt') writeCT_Empty(s, value['content'].value, ctx, 'sldTgt');
    if (value['content'].kind === 'sndTgt') writeCT_EmbeddedWAVAudioFile(s, value['content'].value, ctx, 'sndTgt');
    if (value['content'].kind === 'spTgt') writeCT_TLShapeTargetElement(s, value['content'].value, ctx, 'spTgt');
    if (value['content'].kind === 'inkTgt') writeCT_TLSubShapeId(s, value['content'].value, ctx, 'inkTgt');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TLTriggerRuntimeNode`; the caller supplies its element local name. */
export function writeCT_TLTriggerRuntimeNode(s: XmlSink, value: CT_TLTriggerRuntimeNode, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TLTriggerTimeNodeID`; the caller supplies its element local name. */
export function writeCT_TLTriggerTimeNodeID(s: XmlSink, value: CT_TLTriggerTimeNodeID, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TransitionSoundAction`; the caller supplies its element local name. */
export function writeCT_TransitionSoundAction(s: XmlSink, value: CT_TransitionSoundAction, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'stSnd') writeCT_TransitionStartSoundAction(s, value['content'].value, ctx, 'stSnd');
    if (value['content'].kind === 'endSnd') writeCT_Empty(s, value['content'].value, ctx, 'endSnd');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TransitionStartSoundAction`; the caller supplies its element local name. */
export function writeCT_TransitionStartSoundAction(s: XmlSink, value: CT_TransitionStartSoundAction, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['loop'] !== undefined) s.attr(null, 'loop', String(value['loop']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['snd'] !== undefined) writeCT_EmbeddedWAVAudioFile(s, value['snd'], ctx, 'snd');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ViewProperties`; the caller supplies its element local name. */
export function writeCT_ViewProperties(s: XmlSink, value: CT_ViewProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['lastView'] !== undefined) s.attr(null, 'lastView', String(value['lastView']));
  if (value['showComments'] !== undefined) s.attr(null, 'showComments', String(value['showComments']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['normalViewPr'] !== undefined) writeCT_NormalViewProperties(s, value['normalViewPr'], ctx, 'normalViewPr');
  $q.flush(s, 0);
  if (value['slideViewPr'] !== undefined) writeCT_SlideViewProperties(s, value['slideViewPr'], ctx, 'slideViewPr');
  $q.flush(s, 1);
  if (value['outlineViewPr'] !== undefined) writeCT_OutlineViewProperties(s, value['outlineViewPr'], ctx, 'outlineViewPr');
  $q.flush(s, 2);
  if (value['notesTextViewPr'] !== undefined) writeCT_NotesTextViewProperties(s, value['notesTextViewPr'], ctx, 'notesTextViewPr');
  $q.flush(s, 3);
  if (value['sorterViewPr'] !== undefined) writeCT_SlideSorterViewProperties(s, value['sorterViewPr'], ctx, 'sorterViewPr');
  $q.flush(s, 4);
  if (value['notesViewPr'] !== undefined) writeCT_NotesViewProperties(s, value['notesViewPr'], ctx, 'notesViewPr');
  $q.flush(s, 5);
  if (value['gridSpacing'] !== undefined) writeCT_PositiveSize2D(s, value['gridSpacing'], ctx, 'gridSpacing');
  $q.flush(s, 6);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 7);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_WebProperties`; the caller supplies its element local name. */
export function writeCT_WebProperties(s: XmlSink, value: CT_WebProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['showAnimation'] !== undefined) s.attr(null, 'showAnimation', String(value['showAnimation']));
  if (value['resizeGraphics'] !== undefined) s.attr(null, 'resizeGraphics', String(value['resizeGraphics']));
  if (value['allowPng'] !== undefined) s.attr(null, 'allowPng', String(value['allowPng']));
  if (value['relyOnVml'] !== undefined) s.attr(null, 'relyOnVml', String(value['relyOnVml']));
  if (value['organizeInFolders'] !== undefined) s.attr(null, 'organizeInFolders', String(value['organizeInFolders']));
  if (value['useLongFilenames'] !== undefined) s.attr(null, 'useLongFilenames', String(value['useLongFilenames']));
  if (value['imgSz'] !== undefined) s.attr(null, 'imgSz', String(value['imgSz']));
  if (value['encoding'] !== undefined) s.attr(null, 'encoding', String(value['encoding']));
  if (value['clr'] !== undefined) s.attr(null, 'clr', String(value['clr']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_ExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_WheelTransition`; the caller supplies its element local name. */
export function writeCT_WheelTransition(s: XmlSink, value: CT_WheelTransition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'pml'), localName);
  if (value['spokes'] !== undefined) s.attr(null, 'spokes', String(value['spokes']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}
