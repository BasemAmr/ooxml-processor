/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: writers for the vml-word namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import { type WriteContext, type XmlSink, uriFor } from '../../runtime/index.js';
import type { CT_AnchorLock, CT_Border, CT_Wrap } from './types.js';

// XML writers. Element names are supplied by the owning particle.

/** Write a `CT_AnchorLock`; the caller supplies its element local name. */
export function writeCT_AnchorLock(s: XmlSink, value: CT_AnchorLock, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'vml-word'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Border`; the caller supplies its element local name. */
export function writeCT_Border(s: XmlSink, value: CT_Border, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'vml-word'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['width'] !== undefined) s.attr(null, 'width', String(value['width']));
  if (value['shadow'] !== undefined) s.attr(null, 'shadow', String(value['shadow']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Wrap`; the caller supplies its element local name. */
export function writeCT_Wrap(s: XmlSink, value: CT_Wrap, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'vml-word'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['side'] !== undefined) s.attr(null, 'side', String(value['side']));
  if (value['anchorx'] !== undefined) s.attr(null, 'anchorx', String(value['anchorx']));
  if (value['anchory'] !== undefined) s.attr(null, 'anchory', String(value['anchory']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}
