/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: validators for the vml-word namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  ST_BorderShadow_VALUES,
  ST_BorderType_VALUES,
  ST_HorizontalAnchor_VALUES,
  ST_VerticalAnchor_VALUES,
  ST_WrapSide_VALUES,
  ST_WrapType_VALUES,
} from './types.js';

export type ValidationCode = 'missing-required' | 'invalid-value' | 'facet-violation';
export interface ValidationIssue { readonly path: string; readonly code: ValidationCode; readonly message: string; }
export type ValidationResult = readonly ValidationIssue[];
function issue(path: string, code: ValidationCode, message: string): ValidationIssue { return { path, code, message }; }
function invalidEnum(path: string, value: unknown, values: ReadonlySet<string>): ValidationIssue { return issue(path, 'invalid-value', `value ${String(value)} is not in the enumeration {${[...values].join(', ')}}`); }
const BUILTIN_GROUPS = {
  string: new Set(['xsd:ID', 'xsd:IDREF', 'xsd:NCName', 'xsd:NMTOKEN', 'xsd:anyURI', 'xsd:base64Binary', 'xsd:date', 'xsd:dateTime', 'xsd:duration', 'xsd:hexBinary', 'xsd:language', 'xsd:string', 'xsd:time', 'xsd:token']),
  boolean: new Set(['xsd:boolean']),
  number: new Set(['xsd:byte', 'xsd:decimal', 'xsd:double', 'xsd:float', 'xsd:int', 'xsd:integer', 'xsd:long', 'xsd:nonNegativeInteger', 'xsd:positiveInteger', 'xsd:unsignedByte', 'xsd:unsignedInt', 'xsd:unsignedLong', 'xsd:unsignedShort']),
};
function validBuiltin(value: unknown, name: string): boolean { if (BUILTIN_GROUPS.string.has(name)) return typeof value === 'string'; if (BUILTIN_GROUPS.boolean.has(name)) return typeof value === 'boolean'; if (BUILTIN_GROUPS.number.has(name)) return typeof value === 'number' && Number.isFinite(value); return false; }

export function validateST_BorderShadow(value: unknown, path = 'ST_BorderShadow'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_BorderShadow_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_BorderShadow_VALUES));
  }
  return issues;
}

export function validateST_BorderType(value: unknown, path = 'ST_BorderType'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_BorderType_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_BorderType_VALUES));
  }
  return issues;
}

export function validateST_HorizontalAnchor(value: unknown, path = 'ST_HorizontalAnchor'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_HorizontalAnchor_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_HorizontalAnchor_VALUES));
  }
  return issues;
}

export function validateST_VerticalAnchor(value: unknown, path = 'ST_VerticalAnchor'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_VerticalAnchor_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_VerticalAnchor_VALUES));
  }
  return issues;
}

export function validateST_WrapSide(value: unknown, path = 'ST_WrapSide'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_WrapSide_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_WrapSide_VALUES));
  }
  return issues;
}

export function validateST_WrapType(value: unknown, path = 'ST_WrapType'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_WrapType_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_WrapType_VALUES));
  }
  return issues;
}

export function validateCT_AnchorLock(value: unknown, path = 'CT_AnchorLock'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  return issues;
}

export function validateCT_Border(value: unknown, path = 'CT_Border'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['type'] === undefined) {
  } else {
    issues.push(...validateST_BorderType(v['type'], path + '.type'));
  }
  if (v['width'] === undefined) {
  } else {
    if (!validBuiltin(v['width'], 'xsd:positiveInteger')) issues.push(issue(path + '.width', 'invalid-value', 'invalid xsd:positiveInteger'));
  }
  if (v['shadow'] === undefined) {
  } else {
    issues.push(...validateST_BorderShadow(v['shadow'], path + '.shadow'));
  }
  return issues;
}

export function validateCT_Wrap(value: unknown, path = 'CT_Wrap'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['type'] === undefined) {
  } else {
    issues.push(...validateST_WrapType(v['type'], path + '.type'));
  }
  if (v['side'] === undefined) {
  } else {
    issues.push(...validateST_WrapSide(v['side'], path + '.side'));
  }
  if (v['anchorx'] === undefined) {
  } else {
    issues.push(...validateST_HorizontalAnchor(v['anchorx'], path + '.anchorx'));
  }
  if (v['anchory'] === undefined) {
  } else {
    issues.push(...validateST_VerticalAnchor(v['anchory'], path + '.anchory'));
  }
  return issues;
}
