/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the vml-word namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  parseInteger,
  requireStart,
} from '../../runtime/index.js';
import {
  type CT_AnchorLock,
  type CT_Border,
  type CT_Wrap,
  type ST_BorderShadow,
  ST_BorderShadow_VALUES,
  type ST_BorderType,
  ST_BorderType_VALUES,
  type ST_HorizontalAnchor,
  ST_HorizontalAnchor_VALUES,
  type ST_VerticalAnchor,
  ST_VerticalAnchor_VALUES,
  type ST_WrapSide,
  ST_WrapSide_VALUES,
  type ST_WrapType,
  ST_WrapType_VALUES,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_BorderShadow(value: string): ST_BorderShadow | undefined {
  const s = collapse(value);
  return ST_BorderShadow_VALUES.has(s) ? (s as ST_BorderShadow) : undefined;
}

export function parseST_BorderType(value: string): ST_BorderType | undefined {
  const s = collapse(value);
  return ST_BorderType_VALUES.has(s) ? (s as ST_BorderType) : undefined;
}

export function parseST_HorizontalAnchor(value: string): ST_HorizontalAnchor | undefined {
  const s = collapse(value);
  return ST_HorizontalAnchor_VALUES.has(s) ? (s as ST_HorizontalAnchor) : undefined;
}

export function parseST_VerticalAnchor(value: string): ST_VerticalAnchor | undefined {
  const s = collapse(value);
  return ST_VerticalAnchor_VALUES.has(s) ? (s as ST_VerticalAnchor) : undefined;
}

export function parseST_WrapSide(value: string): ST_WrapSide | undefined {
  const s = collapse(value);
  return ST_WrapSide_VALUES.has(s) ? (s as ST_WrapSide) : undefined;
}

export function parseST_WrapType(value: string): ST_WrapType | undefined {
  const s = collapse(value);
  return ST_WrapType_VALUES.has(s) ? (s as ST_WrapType) : undefined;
}

// Complex-type readers.

/** Read a `CT_AnchorLock`. See `transitional/vml-wordprocessingDrawing.xsd:23`. */
export function readCT_AnchorLock(cur: XmlCursor, ctx: ReadContext): CT_AnchorLock {
  const start = requireStart(cur, 'CT_AnchorLock');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AnchorLock', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AnchorLock', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AnchorLock', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AnchorLock', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_Border`. See `transitional/vml-wordprocessingDrawing.xsd:10`. */
export function readCT_Border(cur: XmlCursor, ctx: ReadContext): CT_Border {
  const start = requireStart(cur, 'CT_Border');
  let type: ST_BorderType | undefined;
  let width: number | undefined;
  let shadow: ST_BorderShadow | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_BorderType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Border', a, 'ST_BorderType', cur);
          break;
        }
        case 'width': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            width = v;
            continue;
          }
          ctx.invalidValue('CT_Border', a, 'xsd:positiveInteger', cur);
          break;
        }
        case 'shadow': {
          const v = parseST_BorderShadow(a.value);
          if (v !== undefined) {
            shadow = v;
            continue;
          }
          ctx.invalidValue('CT_Border', a, 'ST_BorderShadow', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Border', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Border', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Border', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Border', cur);
    cur.next();
  }
  return {
    type,
    width,
    shadow,
    $unknownAttrs,
  };
}

/** Read a `CT_Wrap`. See `transitional/vml-wordprocessingDrawing.xsd:16`. */
export function readCT_Wrap(cur: XmlCursor, ctx: ReadContext): CT_Wrap {
  const start = requireStart(cur, 'CT_Wrap');
  let type: ST_WrapType | undefined;
  let side: ST_WrapSide | undefined;
  let anchorx: ST_HorizontalAnchor | undefined;
  let anchory: ST_VerticalAnchor | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_WrapType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Wrap', a, 'ST_WrapType', cur);
          break;
        }
        case 'side': {
          const v = parseST_WrapSide(a.value);
          if (v !== undefined) {
            side = v;
            continue;
          }
          ctx.invalidValue('CT_Wrap', a, 'ST_WrapSide', cur);
          break;
        }
        case 'anchorx': {
          const v = parseST_HorizontalAnchor(a.value);
          if (v !== undefined) {
            anchorx = v;
            continue;
          }
          ctx.invalidValue('CT_Wrap', a, 'ST_HorizontalAnchor', cur);
          break;
        }
        case 'anchory': {
          const v = parseST_VerticalAnchor(a.value);
          if (v !== undefined) {
            anchory = v;
            continue;
          }
          ctx.invalidValue('CT_Wrap', a, 'ST_VerticalAnchor', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Wrap', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Wrap', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Wrap', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Wrap', cur);
    cur.next();
  }
  return {
    type,
    side,
    anchorx,
    anchory,
    $unknownAttrs,
  };
}
