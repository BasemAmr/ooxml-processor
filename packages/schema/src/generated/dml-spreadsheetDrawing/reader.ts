/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the dml-spreadsheetDrawing namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  parseInteger,
  parseXsdBoolean,
  readScalar,
  requireStart,
} from '../../runtime/index.js';
import {
  parseST_Coordinate,
  readCT_BlipFillProperties,
  readCT_GraphicalObject,
  readCT_GroupShapeProperties,
  readCT_NonVisualConnectorProperties,
  readCT_NonVisualDrawingProps,
  readCT_NonVisualDrawingShapeProps,
  readCT_NonVisualGraphicFrameProperties,
  readCT_NonVisualGroupDrawingShapeProps,
  readCT_NonVisualPictureProperties,
  readCT_Point2D,
  readCT_PositiveSize2D,
  readCT_ShapeProperties,
  readCT_ShapeStyle,
  readCT_TextBody,
  readCT_Transform2D,
} from '../dml-main/reader.js';
import type {
  CT_BlipFillProperties,
  CT_GraphicalObject,
  CT_GroupShapeProperties,
  CT_NonVisualConnectorProperties,
  CT_NonVisualDrawingProps,
  CT_NonVisualDrawingShapeProps,
  CT_NonVisualGraphicFrameProperties,
  CT_NonVisualGroupDrawingShapeProps,
  CT_NonVisualPictureProperties,
  CT_Point2D,
  CT_PositiveSize2D,
  CT_ShapeProperties,
  CT_ShapeStyle,
  CT_TextBody,
  CT_Transform2D,
  ST_Coordinate,
} from '../dml-main/types.js';
import { parseST_RelationshipId } from '../relationships/reader.js';
import type { ST_RelationshipId } from '../relationships/types.js';
import {
  type CT_AbsoluteAnchor,
  type CT_AbsoluteAnchor_Content,
  type CT_AnchorClientData,
  type CT_Connector,
  type CT_ConnectorNonVisual,
  type CT_Drawing,
  type CT_Drawing_Anchor,
  type CT_GraphicalObjectFrame,
  type CT_GraphicalObjectFrameNonVisual,
  type CT_GroupShape,
  type CT_GroupShapeNonVisual,
  type CT_GroupShape_Content,
  type CT_Marker,
  type CT_OneCellAnchor,
  type CT_OneCellAnchor_Content,
  type CT_Picture,
  type CT_PictureNonVisual,
  type CT_Rel,
  type CT_Shape,
  type CT_ShapeNonVisual,
  type CT_TwoCellAnchor,
  type CT_TwoCellAnchor_Content,
  type ST_ColID,
  type ST_EditAs,
  ST_EditAs_VALUES,
  type ST_RowID,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_ColID(value: string): ST_ColID | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_ColID);
}

export function parseST_EditAs(value: string): ST_EditAs | undefined {
  const s = collapse(value);
  return ST_EditAs_VALUES.has(s) ? (s as ST_EditAs) : undefined;
}

export function parseST_RowID(value: string): ST_RowID | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_RowID);
}

// Complex-type readers.

/** Read a `CT_AbsoluteAnchor`. See `transitional/dml-spreadsheetDrawing.xsd:164`. */
export function readCT_AbsoluteAnchor(cur: XmlCursor, ctx: ReadContext): CT_AbsoluteAnchor {
  const start = requireStart(cur, 'CT_AbsoluteAnchor');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AbsoluteAnchor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pos: CT_Point2D | undefined;
  let ext: CT_PositiveSize2D | undefined;
  let content: CT_AbsoluteAnchor_Content | undefined;
  let clientData: CT_AnchorClientData | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AbsoluteAnchor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'pos': {
            pos = readCT_Point2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ext': {
            ext = readCT_PositiveSize2D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sp': {
            content = { kind: 'sp', value: readCT_Shape(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'grpSp': {
            content = { kind: 'grpSp', value: readCT_GroupShape(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'graphicFrame': {
            content = { kind: 'graphicFrame', value: readCT_GraphicalObjectFrame(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'cxnSp': {
            content = { kind: 'cxnSp', value: readCT_Connector(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'pic': {
            content = { kind: 'pic', value: readCT_Picture(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'contentPart': {
            content = { kind: 'contentPart', value: readCT_Rel(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'clientData': {
            clientData = readCT_AnchorClientData(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AbsoluteAnchor', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AbsoluteAnchor', cur);
    cur.next();
  }

  return {
    pos,
    ext,
    content,
    clientData,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AnchorClientData`. See `transitional/dml-spreadsheetDrawing.xsd:14`. */
export function readCT_AnchorClientData(cur: XmlCursor, ctx: ReadContext): CT_AnchorClientData {
  const start = requireStart(cur, 'CT_AnchorClientData');
  let fLocksWithSheet: boolean | undefined;
  let fPrintsWithSheet: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'fLocksWithSheet': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fLocksWithSheet = v;
            continue;
          }
          ctx.invalidValue('CT_AnchorClientData', a, 'xsd:boolean', cur);
          break;
        }
        case 'fPrintsWithSheet': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fPrintsWithSheet = v;
            continue;
          }
          ctx.invalidValue('CT_AnchorClientData', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AnchorClientData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AnchorClientData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AnchorClientData', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AnchorClientData', cur);
    cur.next();
  }
  return {
    fLocksWithSheet,
    fPrintsWithSheet,
    $unknownAttrs,
  };
}

/** Read a `CT_Connector`. See `transitional/dml-spreadsheetDrawing.xsd:44`. */
export function readCT_Connector(cur: XmlCursor, ctx: ReadContext): CT_Connector {
  const start = requireStart(cur, 'CT_Connector');
  let macro: string | undefined;
  let fPublished: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'macro':
          macro = a.value as string;
          continue;

        case 'fPublished': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fPublished = v;
            continue;
          }
          ctx.invalidValue('CT_Connector', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Connector', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvCxnSpPr: CT_ConnectorNonVisual | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let style: CT_ShapeStyle | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Connector', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'nvCxnSpPr': {
            nvCxnSpPr = readCT_ConnectorNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Connector', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Connector', cur);
    cur.next();
  }

  return {
    nvCxnSpPr,
    spPr,
    style,
    macro,
    fPublished,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ConnectorNonVisual`. See `transitional/dml-spreadsheetDrawing.xsd:37`. */
export function readCT_ConnectorNonVisual(cur: XmlCursor, ctx: ReadContext): CT_ConnectorNonVisual {
  const start = requireStart(cur, 'CT_ConnectorNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ConnectorNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvCxnSpPr: CT_NonVisualConnectorProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ConnectorNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvCxnSpPr': {
            cNvCxnSpPr = readCT_NonVisualConnectorProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ConnectorNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ConnectorNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvCxnSpPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Drawing`. See `transitional/dml-spreadsheetDrawing.xsd:179`. */
export function readCT_Drawing(cur: XmlCursor, ctx: ReadContext): CT_Drawing {
  const start = requireStart(cur, 'CT_Drawing');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Drawing', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const anchor: CT_Drawing_Anchor[] = [];

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Drawing', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'twoCellAnchor': {
            anchor.push({ kind: 'twoCellAnchor', value: readCT_TwoCellAnchor(cur, ctx) });
            continue;
          }
          case 'oneCellAnchor': {
            anchor.push({ kind: 'oneCellAnchor', value: readCT_OneCellAnchor(cur, ctx) });
            continue;
          }
          case 'absoluteAnchor': {
            anchor.push({ kind: 'absoluteAnchor', value: readCT_AbsoluteAnchor(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Drawing', ev.localName, ev.uri, cur);
      anchor.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Drawing', cur);
    cur.next();
  }

  return {
    anchor,
    $unknownAttrs,
  };
}

/** Read a `CT_GraphicalObjectFrame`. See `transitional/dml-spreadsheetDrawing.xsd:77`. */
export function readCT_GraphicalObjectFrame(cur: XmlCursor, ctx: ReadContext): CT_GraphicalObjectFrame {
  const start = requireStart(cur, 'CT_GraphicalObjectFrame');
  let macro: string | undefined;
  let fPublished: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'macro':
          macro = a.value as string;
          continue;

        case 'fPublished': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fPublished = v;
            continue;
          }
          ctx.invalidValue('CT_GraphicalObjectFrame', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GraphicalObjectFrame', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvGraphicFramePr: CT_GraphicalObjectFrameNonVisual | undefined;
  let xfrm: CT_Transform2D | undefined;
  let graphic: CT_GraphicalObject | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GraphicalObjectFrame', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'graphic': {
            graphic = readCT_GraphicalObject(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'nvGraphicFramePr': {
            nvGraphicFramePr = readCT_GraphicalObjectFrameNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'xfrm': {
            xfrm = readCT_Transform2D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GraphicalObjectFrame', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GraphicalObjectFrame', cur);
    cur.next();
  }

  return {
    nvGraphicFramePr,
    xfrm,
    graphic,
    macro,
    fPublished,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GraphicalObjectFrameNonVisual`. See `transitional/dml-spreadsheetDrawing.xsd:70`. */
export function readCT_GraphicalObjectFrameNonVisual(cur: XmlCursor, ctx: ReadContext): CT_GraphicalObjectFrameNonVisual {
  const start = requireStart(cur, 'CT_GraphicalObjectFrameNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GraphicalObjectFrameNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvGraphicFramePr: CT_NonVisualGraphicFrameProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GraphicalObjectFrameNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvGraphicFramePr': {
            cNvGraphicFramePr = readCT_NonVisualGraphicFrameProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GraphicalObjectFrameNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GraphicalObjectFrameNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvGraphicFramePr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GroupShape`. See `transitional/dml-spreadsheetDrawing.xsd:94`. */
export function readCT_GroupShape(cur: XmlCursor, ctx: ReadContext): CT_GroupShape {
  const start = requireStart(cur, 'CT_GroupShape');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GroupShape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvGrpSpPr: CT_GroupShapeNonVisual | undefined;
  let grpSpPr: CT_GroupShapeProperties | undefined;
  const content: CT_GroupShape_Content[] = [];

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GroupShape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'nvGrpSpPr': {
            nvGrpSpPr = readCT_GroupShapeNonVisual(cur, ctx);
            continue;
          }
          case 'grpSpPr': {
            grpSpPr = readCT_GroupShapeProperties(cur, ctx);
            continue;
          }
          case 'sp': {
            content.push({ kind: 'sp', value: readCT_Shape(cur, ctx) });
            continue;
          }
          case 'grpSp': {
            content.push({ kind: 'grpSp', value: readCT_GroupShape(cur, ctx) });
            continue;
          }
          case 'graphicFrame': {
            content.push({ kind: 'graphicFrame', value: readCT_GraphicalObjectFrame(cur, ctx) });
            continue;
          }
          case 'cxnSp': {
            content.push({ kind: 'cxnSp', value: readCT_Connector(cur, ctx) });
            continue;
          }
          case 'pic': {
            content.push({ kind: 'pic', value: readCT_Picture(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GroupShape', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GroupShape', cur);
    cur.next();
  }

  return {
    nvGrpSpPr,
    grpSpPr,
    content,
    $unknownAttrs,
  };
}

/** Read a `CT_GroupShapeNonVisual`. See `transitional/dml-spreadsheetDrawing.xsd:87`. */
export function readCT_GroupShapeNonVisual(cur: XmlCursor, ctx: ReadContext): CT_GroupShapeNonVisual {
  const start = requireStart(cur, 'CT_GroupShapeNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GroupShapeNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvGrpSpPr: CT_NonVisualGroupDrawingShapeProps | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GroupShapeNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvGrpSpPr': {
            cNvGrpSpPr = readCT_NonVisualGroupDrawingShapeProps(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GroupShapeNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GroupShapeNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvGrpSpPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Marker`. See `transitional/dml-spreadsheetDrawing.xsd:132`. */
export function readCT_Marker(cur: XmlCursor, ctx: ReadContext): CT_Marker {
  const start = requireStart(cur, 'CT_Marker');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Marker', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let col: ST_ColID | undefined;
  let colOff: ST_Coordinate | undefined;
  let row: ST_RowID | undefined;
  let rowOff: ST_Coordinate | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Marker', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'col': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_ColID($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Marker', 'col', 'ST_ColID', $s, cur);
            col = ($pv ?? $s) as ST_ColID;
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'colOff': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Coordinate($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Marker', 'colOff', 'ST_Coordinate', $s, cur);
            colOff = ($pv ?? $s) as ST_Coordinate;
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'row': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_RowID($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Marker', 'row', 'ST_RowID', $s, cur);
            row = ($pv ?? $s) as ST_RowID;
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'rowOff': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Coordinate($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Marker', 'rowOff', 'ST_Coordinate', $s, cur);
            rowOff = ($pv ?? $s) as ST_Coordinate;
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Marker', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Marker', cur);
    cur.next();
  }

  return {
    col,
    colOff,
    row,
    rowOff,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OneCellAnchor`. See `transitional/dml-spreadsheetDrawing.xsd:156`. */
export function readCT_OneCellAnchor(cur: XmlCursor, ctx: ReadContext): CT_OneCellAnchor {
  const start = requireStart(cur, 'CT_OneCellAnchor');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_OneCellAnchor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let from: CT_Marker | undefined;
  let ext: CT_PositiveSize2D | undefined;
  let content: CT_OneCellAnchor_Content | undefined;
  let clientData: CT_AnchorClientData | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OneCellAnchor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'from': {
            from = readCT_Marker(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ext': {
            ext = readCT_PositiveSize2D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sp': {
            content = { kind: 'sp', value: readCT_Shape(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'grpSp': {
            content = { kind: 'grpSp', value: readCT_GroupShape(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'graphicFrame': {
            content = { kind: 'graphicFrame', value: readCT_GraphicalObjectFrame(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'cxnSp': {
            content = { kind: 'cxnSp', value: readCT_Connector(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'pic': {
            content = { kind: 'pic', value: readCT_Picture(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'contentPart': {
            content = { kind: 'contentPart', value: readCT_Rel(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'clientData': {
            clientData = readCT_AnchorClientData(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OneCellAnchor', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OneCellAnchor', cur);
    cur.next();
  }

  return {
    from,
    ext,
    content,
    clientData,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Picture`. See `transitional/dml-spreadsheetDrawing.xsd:60`. */
export function readCT_Picture(cur: XmlCursor, ctx: ReadContext): CT_Picture {
  const start = requireStart(cur, 'CT_Picture');
  let macro: string | undefined;
  let fPublished: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'macro':
          macro = a.value as string;
          continue;

        case 'fPublished': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fPublished = v;
            continue;
          }
          ctx.invalidValue('CT_Picture', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Picture', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvPicPr: CT_PictureNonVisual | undefined;
  let blipFill: CT_BlipFillProperties | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let style: CT_ShapeStyle | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Picture', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'nvPicPr': {
            nvPicPr = readCT_PictureNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            blipFill = readCT_BlipFillProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Picture', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Picture', cur);
    cur.next();
  }

  return {
    nvPicPr,
    blipFill,
    spPr,
    style,
    macro,
    fPublished,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PictureNonVisual`. See `transitional/dml-spreadsheetDrawing.xsd:53`. */
export function readCT_PictureNonVisual(cur: XmlCursor, ctx: ReadContext): CT_PictureNonVisual {
  const start = requireStart(cur, 'CT_PictureNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PictureNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvPicPr: CT_NonVisualPictureProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PictureNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvPicPr': {
            cNvPicPr = readCT_NonVisualPictureProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PictureNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PictureNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvPicPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Rel`. See `transitional/dml-spreadsheetDrawing.xsd:119`. */
export function readCT_Rel(cur: XmlCursor, ctx: ReadContext): CT_Rel {
  const start = requireStart(cur, 'CT_Rel');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Rel', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Rel', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_Rel', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Rel', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Rel', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Rel', cur);
    cur.next();
  }
  return {
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_Shape`. See `transitional/dml-spreadsheetDrawing.xsd:25`. */
export function readCT_Shape(cur: XmlCursor, ctx: ReadContext): CT_Shape {
  const start = requireStart(cur, 'CT_Shape');
  let macro: string | undefined;
  let textlink: string | undefined;
  let fLocksText: boolean | undefined;
  let fPublished: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'macro':
          macro = a.value as string;
          continue;

        case 'textlink':
          textlink = a.value as string;
          continue;

        case 'fLocksText': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fLocksText = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'xsd:boolean', cur);
          break;
        }
        case 'fPublished': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fPublished = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Shape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvSpPr: CT_ShapeNonVisual | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let style: CT_ShapeStyle | undefined;
  let txBody: CT_TextBody | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Shape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'nvSpPr': {
            nvSpPr = readCT_ShapeNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'txBody': {
            txBody = readCT_TextBody(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Shape', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Shape', cur);
    cur.next();
  }

  return {
    nvSpPr,
    spPr,
    style,
    txBody,
    macro,
    textlink,
    fLocksText,
    fPublished,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ShapeNonVisual`. See `transitional/dml-spreadsheetDrawing.xsd:18`. */
export function readCT_ShapeNonVisual(cur: XmlCursor, ctx: ReadContext): CT_ShapeNonVisual {
  const start = requireStart(cur, 'CT_ShapeNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ShapeNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvSpPr: CT_NonVisualDrawingShapeProps | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ShapeNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvSpPr': {
            cNvSpPr = readCT_NonVisualDrawingShapeProps(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ShapeNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ShapeNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvSpPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TwoCellAnchor`. See `transitional/dml-spreadsheetDrawing.xsd:147`. */
export function readCT_TwoCellAnchor(cur: XmlCursor, ctx: ReadContext): CT_TwoCellAnchor {
  const start = requireStart(cur, 'CT_TwoCellAnchor');
  let editAs: ST_EditAs | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'editAs': {
          const v = parseST_EditAs(a.value);
          if (v !== undefined) {
            editAs = v;
            continue;
          }
          ctx.invalidValue('CT_TwoCellAnchor', a, 'ST_EditAs', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TwoCellAnchor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let from: CT_Marker | undefined;
  let to: CT_Marker | undefined;
  let content: CT_TwoCellAnchor_Content | undefined;
  let clientData: CT_AnchorClientData | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_spreadsheetDrawing = ctx.uris['dml-spreadsheetDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TwoCellAnchor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_spreadsheetDrawing) {
        switch (ev.localName) {
          case 'from': {
            from = readCT_Marker(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'to': {
            to = readCT_Marker(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sp': {
            content = { kind: 'sp', value: readCT_Shape(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'grpSp': {
            content = { kind: 'grpSp', value: readCT_GroupShape(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'graphicFrame': {
            content = { kind: 'graphicFrame', value: readCT_GraphicalObjectFrame(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'cxnSp': {
            content = { kind: 'cxnSp', value: readCT_Connector(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'pic': {
            content = { kind: 'pic', value: readCT_Picture(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'contentPart': {
            content = { kind: 'contentPart', value: readCT_Rel(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'clientData': {
            clientData = readCT_AnchorClientData(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TwoCellAnchor', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TwoCellAnchor', cur);
    cur.next();
  }

  return {
    from,
    to,
    content,
    clientData,
    editAs,
    $unknown,
    $unknownAttrs,
  };
}
