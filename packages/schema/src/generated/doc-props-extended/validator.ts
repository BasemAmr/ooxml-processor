/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: validators for the doc-props-extended namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import { validateCT_Vector } from '../doc-props-vt/validator.js';

export type ValidationCode = 'missing-required' | 'invalid-value' | 'facet-violation';
export interface ValidationIssue { readonly path: string; readonly code: ValidationCode; readonly message: string; }
export type ValidationResult = readonly ValidationIssue[];
function issue(path: string, code: ValidationCode, message: string): ValidationIssue { return { path, code, message }; }
function invalidEnum(path: string, value: unknown, values: ReadonlySet<string>): ValidationIssue { return issue(path, 'invalid-value', `value ${String(value)} is not in the enumeration {${[...values].join(', ')}}`); }
const BUILTIN_GROUPS = {
  string: new Set(['xsd:ID', 'xsd:IDREF', 'xsd:NCName', 'xsd:NMTOKEN', 'xsd:anyURI', 'xsd:base64Binary', 'xsd:date', 'xsd:dateTime', 'xsd:duration', 'xsd:hexBinary', 'xsd:language', 'xsd:string', 'xsd:time', 'xsd:token']),
  boolean: new Set(['xsd:boolean']),
  number: new Set(['xsd:byte', 'xsd:decimal', 'xsd:double', 'xsd:float', 'xsd:int', 'xsd:integer', 'xsd:long', 'xsd:nonNegativeInteger', 'xsd:positiveInteger', 'xsd:unsignedByte', 'xsd:unsignedInt', 'xsd:unsignedLong', 'xsd:unsignedShort']),
};
function validBuiltin(value: unknown, name: string): boolean { if (BUILTIN_GROUPS.string.has(name)) return typeof value === 'string'; if (BUILTIN_GROUPS.boolean.has(name)) return typeof value === 'boolean'; if (BUILTIN_GROUPS.number.has(name)) return typeof value === 'number' && Number.isFinite(value); return false; }

export function validateCT_DigSigBlob(value: unknown, path = 'CT_DigSigBlob'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['blob'] === undefined) {
    issues.push(issue(path + '.blob', 'missing-required', 'required slot is absent'));
  } else {
    if (!validBuiltin(v['blob'], 'xsd:base64Binary')) issues.push(issue(path + '.blob', 'invalid-value', 'invalid xsd:base64Binary'));
  }
  return issues;
}

export function validateCT_Properties(value: unknown, path = 'CT_Properties'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['Template'] === undefined) {
  } else {
    if (!validBuiltin(v['Template'], 'xsd:string')) issues.push(issue(path + '.Template', 'invalid-value', 'invalid xsd:string'));
  }
  if (v['Manager'] === undefined) {
  } else {
    if (!validBuiltin(v['Manager'], 'xsd:string')) issues.push(issue(path + '.Manager', 'invalid-value', 'invalid xsd:string'));
  }
  if (v['Company'] === undefined) {
  } else {
    if (!validBuiltin(v['Company'], 'xsd:string')) issues.push(issue(path + '.Company', 'invalid-value', 'invalid xsd:string'));
  }
  if (v['Pages'] === undefined) {
  } else {
    if (!validBuiltin(v['Pages'], 'xsd:int')) issues.push(issue(path + '.Pages', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['Words'] === undefined) {
  } else {
    if (!validBuiltin(v['Words'], 'xsd:int')) issues.push(issue(path + '.Words', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['Characters'] === undefined) {
  } else {
    if (!validBuiltin(v['Characters'], 'xsd:int')) issues.push(issue(path + '.Characters', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['PresentationFormat'] === undefined) {
  } else {
    if (!validBuiltin(v['PresentationFormat'], 'xsd:string')) issues.push(issue(path + '.PresentationFormat', 'invalid-value', 'invalid xsd:string'));
  }
  if (v['Lines'] === undefined) {
  } else {
    if (!validBuiltin(v['Lines'], 'xsd:int')) issues.push(issue(path + '.Lines', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['Paragraphs'] === undefined) {
  } else {
    if (!validBuiltin(v['Paragraphs'], 'xsd:int')) issues.push(issue(path + '.Paragraphs', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['Slides'] === undefined) {
  } else {
    if (!validBuiltin(v['Slides'], 'xsd:int')) issues.push(issue(path + '.Slides', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['Notes'] === undefined) {
  } else {
    if (!validBuiltin(v['Notes'], 'xsd:int')) issues.push(issue(path + '.Notes', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['TotalTime'] === undefined) {
  } else {
    if (!validBuiltin(v['TotalTime'], 'xsd:int')) issues.push(issue(path + '.TotalTime', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['HiddenSlides'] === undefined) {
  } else {
    if (!validBuiltin(v['HiddenSlides'], 'xsd:int')) issues.push(issue(path + '.HiddenSlides', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['MMClips'] === undefined) {
  } else {
    if (!validBuiltin(v['MMClips'], 'xsd:int')) issues.push(issue(path + '.MMClips', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['ScaleCrop'] === undefined) {
  } else {
    if (!validBuiltin(v['ScaleCrop'], 'xsd:boolean')) issues.push(issue(path + '.ScaleCrop', 'invalid-value', 'invalid xsd:boolean'));
  }
  if (v['HeadingPairs'] === undefined) {
  } else {
    issues.push(...validateCT_VectorVariant(v['HeadingPairs'], path + '.HeadingPairs'));
  }
  if (v['TitlesOfParts'] === undefined) {
  } else {
    issues.push(...validateCT_VectorLpstr(v['TitlesOfParts'], path + '.TitlesOfParts'));
  }
  if (v['LinksUpToDate'] === undefined) {
  } else {
    if (!validBuiltin(v['LinksUpToDate'], 'xsd:boolean')) issues.push(issue(path + '.LinksUpToDate', 'invalid-value', 'invalid xsd:boolean'));
  }
  if (v['CharactersWithSpaces'] === undefined) {
  } else {
    if (!validBuiltin(v['CharactersWithSpaces'], 'xsd:int')) issues.push(issue(path + '.CharactersWithSpaces', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['SharedDoc'] === undefined) {
  } else {
    if (!validBuiltin(v['SharedDoc'], 'xsd:boolean')) issues.push(issue(path + '.SharedDoc', 'invalid-value', 'invalid xsd:boolean'));
  }
  if (v['HyperlinkBase'] === undefined) {
  } else {
    if (!validBuiltin(v['HyperlinkBase'], 'xsd:string')) issues.push(issue(path + '.HyperlinkBase', 'invalid-value', 'invalid xsd:string'));
  }
  if (v['HLinks'] === undefined) {
  } else {
    issues.push(...validateCT_VectorVariant(v['HLinks'], path + '.HLinks'));
  }
  if (v['HyperlinksChanged'] === undefined) {
  } else {
    if (!validBuiltin(v['HyperlinksChanged'], 'xsd:boolean')) issues.push(issue(path + '.HyperlinksChanged', 'invalid-value', 'invalid xsd:boolean'));
  }
  if (v['DigSig'] === undefined) {
  } else {
    issues.push(...validateCT_DigSigBlob(v['DigSig'], path + '.DigSig'));
  }
  if (v['Application'] === undefined) {
  } else {
    if (!validBuiltin(v['Application'], 'xsd:string')) issues.push(issue(path + '.Application', 'invalid-value', 'invalid xsd:string'));
  }
  if (v['AppVersion'] === undefined) {
  } else {
    if (!validBuiltin(v['AppVersion'], 'xsd:string')) issues.push(issue(path + '.AppVersion', 'invalid-value', 'invalid xsd:string'));
  }
  if (v['DocSecurity'] === undefined) {
  } else {
    if (!validBuiltin(v['DocSecurity'], 'xsd:int')) issues.push(issue(path + '.DocSecurity', 'invalid-value', 'invalid xsd:int'));
  }
  return issues;
}

export function validateCT_VectorLpstr(value: unknown, path = 'CT_VectorLpstr'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['vector'] === undefined) {
    issues.push(issue(path + '.vector', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_Vector(v['vector'], path + '.vector'));
  }
  return issues;
}

export function validateCT_VectorVariant(value: unknown, path = 'CT_VectorVariant'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['vector'] === undefined) {
    issues.push(issue(path + '.vector', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_Vector(v['vector'], path + '.vector'));
  }
  return issues;
}
