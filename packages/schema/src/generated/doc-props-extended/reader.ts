/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the doc-props-extended namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  isIgnorableWhitespace,
  parseInteger,
  parseXsdBoolean,
  readScalar,
  requireStart,
} from '../../runtime/index.js';
import { readCT_Vector } from '../doc-props-vt/reader.js';
import type { CT_Vector } from '../doc-props-vt/types.js';
import type { CT_DigSigBlob, CT_Properties, CT_VectorLpstr, CT_VectorVariant } from './types.js';

// Complex-type readers.

/** Read a `CT_DigSigBlob`. See `transitional/shared-documentPropertiesExtended.xsd:51`. */
export function readCT_DigSigBlob(cur: XmlCursor, ctx: ReadContext): CT_DigSigBlob {
  const start = requireStart(cur, 'CT_DigSigBlob');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DigSigBlob', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let blob: string | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_doc_props_vt = ctx.uris['doc-props-vt'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DigSigBlob', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_doc_props_vt) {
        switch (ev.localName) {
          case 'blob': {
            blob = readScalar(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DigSigBlob', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DigSigBlob', cur);
    cur.next();
  }

  return {
    blob,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Properties`. See `transitional/shared-documentPropertiesExtended.xsd:10`. */
export function readCT_Properties(cur: XmlCursor, ctx: ReadContext): CT_Properties {
  const start = requireStart(cur, 'CT_Properties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Properties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let Template: string | undefined;
  let Manager: string | undefined;
  let Company: string | undefined;
  let Pages: number | undefined;
  let Words: number | undefined;
  let Characters: number | undefined;
  let PresentationFormat: string | undefined;
  let Lines: number | undefined;
  let Paragraphs: number | undefined;
  let Slides: number | undefined;
  let Notes: number | undefined;
  let TotalTime: number | undefined;
  let HiddenSlides: number | undefined;
  let MMClips: number | undefined;
  let ScaleCrop: boolean | undefined;
  let HeadingPairs: CT_VectorVariant | undefined;
  let TitlesOfParts: CT_VectorLpstr | undefined;
  let LinksUpToDate: boolean | undefined;
  let CharactersWithSpaces: number | undefined;
  let SharedDoc: boolean | undefined;
  let HyperlinkBase: string | undefined;
  let HLinks: CT_VectorVariant | undefined;
  let HyperlinksChanged: boolean | undefined;
  let DigSig: CT_DigSigBlob | undefined;
  let Application: string | undefined;
  let AppVersion: string | undefined;
  let DocSecurity: number | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_doc_props_extended = ctx.uris['doc-props-extended'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Properties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_doc_props_extended) {
        switch (ev.localName) {
          case 'Template': {
            Template = readScalar(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'Manager': {
            Manager = readScalar(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'Company': {
            Company = readScalar(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'Pages': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'Pages', 'xsd:int', $s, cur);
            Pages = ($pv ?? $s) as number;
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'Words': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'Words', 'xsd:int', $s, cur);
            Words = ($pv ?? $s) as number;
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'Characters': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'Characters', 'xsd:int', $s, cur);
            Characters = ($pv ?? $s) as number;
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'PresentationFormat': {
            PresentationFormat = readScalar(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'Lines': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'Lines', 'xsd:int', $s, cur);
            Lines = ($pv ?? $s) as number;
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'Paragraphs': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'Paragraphs', 'xsd:int', $s, cur);
            Paragraphs = ($pv ?? $s) as number;
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'Slides': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'Slides', 'xsd:int', $s, cur);
            Slides = ($pv ?? $s) as number;
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'Notes': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'Notes', 'xsd:int', $s, cur);
            Notes = ($pv ?? $s) as number;
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'TotalTime': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'TotalTime', 'xsd:int', $s, cur);
            TotalTime = ($pv ?? $s) as number;
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'HiddenSlides': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'HiddenSlides', 'xsd:int', $s, cur);
            HiddenSlides = ($pv ?? $s) as number;
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'MMClips': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'MMClips', 'xsd:int', $s, cur);
            MMClips = ($pv ?? $s) as number;
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'ScaleCrop': {
            const $s = readScalar(cur, ctx);
            const $pv = parseXsdBoolean($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'ScaleCrop', 'xsd:boolean', $s, cur);
            ScaleCrop = ($pv ?? $s) as boolean;
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'HeadingPairs': {
            HeadingPairs = readCT_VectorVariant(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'TitlesOfParts': {
            TitlesOfParts = readCT_VectorLpstr(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'LinksUpToDate': {
            const $s = readScalar(cur, ctx);
            const $pv = parseXsdBoolean($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'LinksUpToDate', 'xsd:boolean', $s, cur);
            LinksUpToDate = ($pv ?? $s) as boolean;
            $slot = 17;
            $index = undefined;
            continue;
          }
          case 'CharactersWithSpaces': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'CharactersWithSpaces', 'xsd:int', $s, cur);
            CharactersWithSpaces = ($pv ?? $s) as number;
            $slot = 18;
            $index = undefined;
            continue;
          }
          case 'SharedDoc': {
            const $s = readScalar(cur, ctx);
            const $pv = parseXsdBoolean($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'SharedDoc', 'xsd:boolean', $s, cur);
            SharedDoc = ($pv ?? $s) as boolean;
            $slot = 19;
            $index = undefined;
            continue;
          }
          case 'HyperlinkBase': {
            HyperlinkBase = readScalar(cur, ctx);
            $slot = 20;
            $index = undefined;
            continue;
          }
          case 'HLinks': {
            HLinks = readCT_VectorVariant(cur, ctx);
            $slot = 21;
            $index = undefined;
            continue;
          }
          case 'HyperlinksChanged': {
            const $s = readScalar(cur, ctx);
            const $pv = parseXsdBoolean($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'HyperlinksChanged', 'xsd:boolean', $s, cur);
            HyperlinksChanged = ($pv ?? $s) as boolean;
            $slot = 22;
            $index = undefined;
            continue;
          }
          case 'DigSig': {
            DigSig = readCT_DigSigBlob(cur, ctx);
            $slot = 23;
            $index = undefined;
            continue;
          }
          case 'Application': {
            Application = readScalar(cur, ctx);
            $slot = 24;
            $index = undefined;
            continue;
          }
          case 'AppVersion': {
            AppVersion = readScalar(cur, ctx);
            $slot = 25;
            $index = undefined;
            continue;
          }
          case 'DocSecurity': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Properties', 'DocSecurity', 'xsd:int', $s, cur);
            DocSecurity = ($pv ?? $s) as number;
            $slot = 26;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Properties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Properties', cur);
    cur.next();
  }

  return {
    Template,
    Manager,
    Company,
    Pages,
    Words,
    Characters,
    PresentationFormat,
    Lines,
    Paragraphs,
    Slides,
    Notes,
    TotalTime,
    HiddenSlides,
    MMClips,
    ScaleCrop,
    HeadingPairs,
    TitlesOfParts,
    LinksUpToDate,
    CharactersWithSpaces,
    SharedDoc,
    HyperlinkBase,
    HLinks,
    HyperlinksChanged,
    DigSig,
    Application,
    AppVersion,
    DocSecurity,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_VectorLpstr`. See `transitional/shared-documentPropertiesExtended.xsd:46`. */
export function readCT_VectorLpstr(cur: XmlCursor, ctx: ReadContext): CT_VectorLpstr {
  const start = requireStart(cur, 'CT_VectorLpstr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_VectorLpstr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let vector: CT_Vector | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_doc_props_vt = ctx.uris['doc-props-vt'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_VectorLpstr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_doc_props_vt) {
        switch (ev.localName) {
          case 'vector': {
            vector = readCT_Vector(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_VectorLpstr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_VectorLpstr', cur);
    cur.next();
  }

  return {
    vector,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_VectorVariant`. See `transitional/shared-documentPropertiesExtended.xsd:41`. */
export function readCT_VectorVariant(cur: XmlCursor, ctx: ReadContext): CT_VectorVariant {
  const start = requireStart(cur, 'CT_VectorVariant');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_VectorVariant', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let vector: CT_Vector | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_doc_props_vt = ctx.uris['doc-props-vt'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_VectorVariant', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_doc_props_vt) {
        switch (ev.localName) {
          case 'vector': {
            vector = readCT_Vector(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_VectorVariant', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_VectorVariant', cur);
    cur.next();
  }

  return {
    vector,
    $unknown,
    $unknownAttrs,
  };
}
