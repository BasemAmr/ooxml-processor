/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the vml-excel namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  parseInteger,
  readScalar,
  requireStart,
} from '../../runtime/index.js';
import { parseST_TrueFalseBlank } from '../shared-types/reader.js';
import type { ST_TrueFalseBlank } from '../shared-types/types.js';
import {
  type CT_ClientData,
  type CT_ClientData_Content,
  type ST_CF,
  type ST_ObjectType,
  ST_ObjectType_VALUES,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_CF(value: string): ST_CF | undefined {
  return value as ST_CF;
}

export function parseST_ObjectType(value: string): ST_ObjectType | undefined {
  const s = collapse(value);
  return ST_ObjectType_VALUES.has(s) ? (s as ST_ObjectType) : undefined;
}

// Complex-type readers.

/** Read a `CT_ClientData`. See `transitional/vml-spreadsheetDrawing.xsd:10`. */
export function readCT_ClientData(cur: XmlCursor, ctx: ReadContext): CT_ClientData {
  const start = requireStart(cur, 'CT_ClientData');
  let ObjectType: ST_ObjectType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'ObjectType': {
          const v = parseST_ObjectType(a.value);
          if (v !== undefined) {
            ObjectType = v;
            continue;
          }
          ctx.invalidValue('CT_ClientData', a, 'ST_ObjectType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ClientData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (ObjectType === undefined) ctx.missingRequired('CT_ClientData', 'ObjectType', cur);

  const content: CT_ClientData_Content[] = [];

  const NS_vml_excel = ctx.uris['vml-excel'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ClientData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_vml_excel) {
        switch (ev.localName) {
          case 'MoveWithCells': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'MoveWithCells', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'MoveWithCells', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'SizeWithCells': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'SizeWithCells', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'SizeWithCells', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'Anchor': {
            content.push({ kind: 'Anchor', value: readScalar(cur, ctx) });
            continue;
          }
          case 'Locked': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Locked', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'Locked', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'DefaultSize': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'DefaultSize', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'DefaultSize', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'PrintObject': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'PrintObject', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'PrintObject', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'Disabled': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Disabled', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'Disabled', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'AutoFill': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'AutoFill', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'AutoFill', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'AutoLine': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'AutoLine', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'AutoLine', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'AutoPict': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'AutoPict', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'AutoPict', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'FmlaMacro': {
            content.push({ kind: 'FmlaMacro', value: readScalar(cur, ctx) });
            continue;
          }
          case 'TextHAlign': {
            content.push({ kind: 'TextHAlign', value: readScalar(cur, ctx) });
            continue;
          }
          case 'TextVAlign': {
            content.push({ kind: 'TextVAlign', value: readScalar(cur, ctx) });
            continue;
          }
          case 'LockText': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'LockText', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'LockText', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'JustLastX': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'JustLastX', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'JustLastX', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'SecretEdit': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'SecretEdit', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'SecretEdit', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'Default': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Default', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'Default', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'Help': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Help', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'Help', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'Cancel': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Cancel', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'Cancel', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'Dismiss': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Dismiss', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'Dismiss', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'Accel': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Accel', 'xsd:integer', $s, cur);
            content.push({ kind: 'Accel', value: ($pv ?? $s) as number });
            continue;
          }
          case 'Accel2': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Accel2', 'xsd:integer', $s, cur);
            content.push({ kind: 'Accel2', value: ($pv ?? $s) as number });
            continue;
          }
          case 'Row': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Row', 'xsd:integer', $s, cur);
            content.push({ kind: 'Row', value: ($pv ?? $s) as number });
            continue;
          }
          case 'Column': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Column', 'xsd:integer', $s, cur);
            content.push({ kind: 'Column', value: ($pv ?? $s) as number });
            continue;
          }
          case 'Visible': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Visible', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'Visible', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'RowHidden': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'RowHidden', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'RowHidden', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'ColHidden': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'ColHidden', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'ColHidden', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'VTEdit': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'VTEdit', 'xsd:integer', $s, cur);
            content.push({ kind: 'VTEdit', value: ($pv ?? $s) as number });
            continue;
          }
          case 'MultiLine': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'MultiLine', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'MultiLine', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'VScroll': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'VScroll', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'VScroll', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'ValidIds': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'ValidIds', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'ValidIds', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'FmlaRange': {
            content.push({ kind: 'FmlaRange', value: readScalar(cur, ctx) });
            continue;
          }
          case 'WidthMin': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'WidthMin', 'xsd:integer', $s, cur);
            content.push({ kind: 'WidthMin', value: ($pv ?? $s) as number });
            continue;
          }
          case 'Sel': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Sel', 'xsd:integer', $s, cur);
            content.push({ kind: 'Sel', value: ($pv ?? $s) as number });
            continue;
          }
          case 'NoThreeD2': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'NoThreeD2', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'NoThreeD2', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'SelType': {
            content.push({ kind: 'SelType', value: readScalar(cur, ctx) });
            continue;
          }
          case 'MultiSel': {
            content.push({ kind: 'MultiSel', value: readScalar(cur, ctx) });
            continue;
          }
          case 'LCT': {
            content.push({ kind: 'LCT', value: readScalar(cur, ctx) });
            continue;
          }
          case 'ListItem': {
            content.push({ kind: 'ListItem', value: readScalar(cur, ctx) });
            continue;
          }
          case 'DropStyle': {
            content.push({ kind: 'DropStyle', value: readScalar(cur, ctx) });
            continue;
          }
          case 'Colored': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Colored', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'Colored', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'DropLines': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'DropLines', 'xsd:integer', $s, cur);
            content.push({ kind: 'DropLines', value: ($pv ?? $s) as number });
            continue;
          }
          case 'Checked': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Checked', 'xsd:integer', $s, cur);
            content.push({ kind: 'Checked', value: ($pv ?? $s) as number });
            continue;
          }
          case 'FmlaLink': {
            content.push({ kind: 'FmlaLink', value: readScalar(cur, ctx) });
            continue;
          }
          case 'FmlaPict': {
            content.push({ kind: 'FmlaPict', value: readScalar(cur, ctx) });
            continue;
          }
          case 'NoThreeD': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'NoThreeD', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'NoThreeD', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'FirstButton': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'FirstButton', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'FirstButton', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'FmlaGroup': {
            content.push({ kind: 'FmlaGroup', value: readScalar(cur, ctx) });
            continue;
          }
          case 'Val': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Val', 'xsd:integer', $s, cur);
            content.push({ kind: 'Val', value: ($pv ?? $s) as number });
            continue;
          }
          case 'Min': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Min', 'xsd:integer', $s, cur);
            content.push({ kind: 'Min', value: ($pv ?? $s) as number });
            continue;
          }
          case 'Max': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Max', 'xsd:integer', $s, cur);
            content.push({ kind: 'Max', value: ($pv ?? $s) as number });
            continue;
          }
          case 'Inc': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Inc', 'xsd:integer', $s, cur);
            content.push({ kind: 'Inc', value: ($pv ?? $s) as number });
            continue;
          }
          case 'Page': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Page', 'xsd:integer', $s, cur);
            content.push({ kind: 'Page', value: ($pv ?? $s) as number });
            continue;
          }
          case 'Horiz': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Horiz', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'Horiz', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'Dx': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Dx', 'xsd:integer', $s, cur);
            content.push({ kind: 'Dx', value: ($pv ?? $s) as number });
            continue;
          }
          case 'MapOCX': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'MapOCX', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'MapOCX', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'CF': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_CF($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'CF', 'ST_CF', $s, cur);
            content.push({ kind: 'CF', value: ($pv ?? $s) as ST_CF });
            continue;
          }
          case 'Camera': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'Camera', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'Camera', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'RecalcAlways': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'RecalcAlways', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'RecalcAlways', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'AutoScale': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'AutoScale', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'AutoScale', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'DDE': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'DDE', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'DDE', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'UIObj': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_TrueFalseBlank($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'UIObj', 'ST_TrueFalseBlank', $s, cur);
            content.push({ kind: 'UIObj', value: ($pv ?? $s) as ST_TrueFalseBlank });
            continue;
          }
          case 'ScriptText': {
            content.push({ kind: 'ScriptText', value: readScalar(cur, ctx) });
            continue;
          }
          case 'ScriptExtended': {
            content.push({ kind: 'ScriptExtended', value: readScalar(cur, ctx) });
            continue;
          }
          case 'ScriptLanguage': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'ScriptLanguage', 'xsd:nonNegativeInteger', $s, cur);
            content.push({ kind: 'ScriptLanguage', value: ($pv ?? $s) as number });
            continue;
          }
          case 'ScriptLocation': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_ClientData', 'ScriptLocation', 'xsd:nonNegativeInteger', $s, cur);
            content.push({ kind: 'ScriptLocation', value: ($pv ?? $s) as number });
            continue;
          }
          case 'FmlaTxbx': {
            content.push({ kind: 'FmlaTxbx', value: readScalar(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ClientData', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ClientData', cur);
    cur.next();
  }

  return {
    content,
    ObjectType,
    $unknownAttrs,
  };
}
