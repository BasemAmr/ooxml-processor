/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the math namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  parseInteger,
  requireStart,
} from '../../runtime/index.js';
import {
  parseST_OnOff,
  parseST_String,
  parseST_TwipsMeasure,
  parseST_XAlign,
  parseST_YAlign,
} from '../shared-types/reader.js';
import type {
  ST_OnOff,
  ST_String,
  ST_TwipsMeasure,
  ST_XAlign,
  ST_YAlign,
} from '../shared-types/types.js';
import {
  readCT_Bookmark,
  readCT_Br,
  readCT_CustomXmlRun,
  readCT_Drawing,
  readCT_Empty,
  readCT_FldChar,
  readCT_FtnEdnRef,
  readCT_Hyperlink,
  readCT_Markup,
  readCT_MarkupRange,
  readCT_MathCtrlDel,
  readCT_MathCtrlIns,
  readCT_MoveBookmark,
  readCT_Object,
  readCT_PTab,
  readCT_Perm,
  readCT_PermStart,
  readCT_Picture,
  readCT_ProofErr,
  readCT_RPr,
  readCT_Rel,
  readCT_Ruby,
  readCT_RunTrackChange,
  readCT_SdtRun,
  readCT_SimpleField,
  readCT_SmartTagRun,
  readCT_Sym,
  readCT_Text as wml_readCT_Text,
  readCT_TrackChange,
} from '../wml/reader.js';
import type { CT_RPr } from '../wml/types.js';
import {
  type CT_Acc,
  type CT_AccPr,
  type CT_Bar,
  type CT_BarPr,
  type CT_BorderBox,
  type CT_BorderBoxPr,
  type CT_Box,
  type CT_BoxPr,
  type CT_BreakBin,
  type CT_BreakBinSub,
  type CT_Char,
  type CT_CtrlPr,
  type CT_CtrlPr_RPrMath,
  type CT_D,
  type CT_DPr,
  type CT_EqArr,
  type CT_EqArrPr,
  type CT_F,
  type CT_FPr,
  type CT_FType,
  type CT_Func,
  type CT_FuncPr,
  type CT_GroupChr,
  type CT_GroupChrPr,
  type CT_Integer2,
  type CT_Integer255,
  type CT_LimLoc,
  type CT_LimLow,
  type CT_LimLowPr,
  type CT_LimUpp,
  type CT_LimUppPr,
  type CT_M,
  type CT_MC,
  type CT_MCPr,
  type CT_MCS,
  type CT_MPr,
  type CT_MR,
  type CT_ManualBreak,
  type CT_MathPr,
  type CT_MathPr_Content,
  type CT_Nary,
  type CT_NaryPr,
  type CT_OMath,
  type CT_OMathArg,
  type CT_OMathArgPr,
  type CT_OMathArg_OMathElements,
  type CT_OMathJc,
  type CT_OMathPara,
  type CT_OMathParaPr,
  type CT_OMath_OMathElements,
  type CT_OnOff,
  type CT_Phant,
  type CT_PhantPr,
  type CT_R,
  type CT_RPR,
  type CT_RPR_Content,
  type CT_R_Content,
  type CT_Rad,
  type CT_RadPr,
  type CT_SPre,
  type CT_SPrePr,
  type CT_SSub,
  type CT_SSubPr,
  type CT_SSubSup,
  type CT_SSubSupPr,
  type CT_SSup,
  type CT_SSupPr,
  type CT_Script,
  type CT_Shp,
  type CT_SpacingRule,
  type CT_String,
  type CT_Style,
  type CT_Text,
  type CT_TopBot,
  type CT_TwipsMeasure,
  type CT_UnSignedInteger,
  type CT_XAlign,
  type CT_YAlign,
  type ST_BreakBin,
  type ST_BreakBinSub,
  ST_BreakBinSub_VALUES,
  ST_BreakBin_VALUES,
  type ST_Char,
  type ST_FType,
  ST_FType_VALUES,
  type ST_Integer2,
  type ST_Integer255,
  type ST_Jc,
  ST_Jc_VALUES,
  type ST_LimLoc,
  ST_LimLoc_VALUES,
  type ST_Script,
  ST_Script_VALUES,
  type ST_Shp,
  ST_Shp_VALUES,
  type ST_SpacingRule,
  type ST_Style,
  ST_Style_VALUES,
  type ST_TopBot,
  ST_TopBot_VALUES,
  type ST_UnSignedInteger,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_BreakBin(value: string): ST_BreakBin | undefined {
  const s = collapse(value);
  return ST_BreakBin_VALUES.has(s) ? (s as ST_BreakBin) : undefined;
}

export function parseST_BreakBinSub(value: string): ST_BreakBinSub | undefined {
  const s = collapse(value);
  return ST_BreakBinSub_VALUES.has(s) ? (s as ST_BreakBinSub) : undefined;
}

export function parseST_Char(value: string): ST_Char | undefined {
  return value as ST_Char;
}

export function parseST_FType(value: string): ST_FType | undefined {
  const s = collapse(value);
  return ST_FType_VALUES.has(s) ? (s as ST_FType) : undefined;
}

export function parseST_Integer2(value: string): ST_Integer2 | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_Integer2);
}

export function parseST_Integer255(value: string): ST_Integer255 | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_Integer255);
}

export function parseST_Jc(value: string): ST_Jc | undefined {
  const s = collapse(value);
  return ST_Jc_VALUES.has(s) ? (s as ST_Jc) : undefined;
}

export function parseST_LimLoc(value: string): ST_LimLoc | undefined {
  const s = collapse(value);
  return ST_LimLoc_VALUES.has(s) ? (s as ST_LimLoc) : undefined;
}

export function parseST_Script(value: string): ST_Script | undefined {
  const s = collapse(value);
  return ST_Script_VALUES.has(s) ? (s as ST_Script) : undefined;
}

export function parseST_Shp(value: string): ST_Shp | undefined {
  const s = collapse(value);
  return ST_Shp_VALUES.has(s) ? (s as ST_Shp) : undefined;
}

export function parseST_SpacingRule(value: string): ST_SpacingRule | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_SpacingRule);
}

export function parseST_Style(value: string): ST_Style | undefined {
  const s = collapse(value);
  return ST_Style_VALUES.has(s) ? (s as ST_Style) : undefined;
}

export function parseST_TopBot(value: string): ST_TopBot | undefined {
  const s = collapse(value);
  return ST_TopBot_VALUES.has(s) ? (s as ST_TopBot) : undefined;
}

export function parseST_UnSignedInteger(value: string): ST_UnSignedInteger | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_UnSignedInteger);
}

// Complex-type readers.

/** Read a `CT_Acc`. See `transitional/shared-math.xsd:179`. */
export function readCT_Acc(cur: XmlCursor, ctx: ReadContext): CT_Acc {
  const start = requireStart(cur, 'CT_Acc');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Acc', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let accPr: CT_AccPr | undefined;
  let e: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Acc', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'accPr': {
            accPr = readCT_AccPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Acc', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Acc', cur);
    cur.next();
  }

  return {
    accPr,
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AccPr`. See `transitional/shared-math.xsd:173`. */
export function readCT_AccPr(cur: XmlCursor, ctx: ReadContext): CT_AccPr {
  const start = requireStart(cur, 'CT_AccPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AccPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let chr: CT_Char | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AccPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'chr': {
            chr = readCT_Char(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AccPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AccPr', cur);
    cur.next();
  }

  return {
    chr,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Bar`. See `transitional/shared-math.xsd:191`. */
export function readCT_Bar(cur: XmlCursor, ctx: ReadContext): CT_Bar {
  const start = requireStart(cur, 'CT_Bar');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Bar', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let barPr: CT_BarPr | undefined;
  let e: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Bar', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'barPr': {
            barPr = readCT_BarPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Bar', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Bar', cur);
    cur.next();
  }

  return {
    barPr,
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BarPr`. See `transitional/shared-math.xsd:185`. */
export function readCT_BarPr(cur: XmlCursor, ctx: ReadContext): CT_BarPr {
  const start = requireStart(cur, 'CT_BarPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BarPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pos: CT_TopBot | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BarPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'pos': {
            pos = readCT_TopBot(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BarPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BarPr', cur);
    cur.next();
  }

  return {
    pos,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BorderBox`. See `transitional/shared-math.xsd:226`. */
export function readCT_BorderBox(cur: XmlCursor, ctx: ReadContext): CT_BorderBox {
  const start = requireStart(cur, 'CT_BorderBox');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BorderBox', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let borderBoxPr: CT_BorderBoxPr | undefined;
  let e: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BorderBox', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'borderBoxPr': {
            borderBoxPr = readCT_BorderBoxPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BorderBox', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BorderBox', cur);
    cur.next();
  }

  return {
    borderBoxPr,
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BorderBoxPr`. See `transitional/shared-math.xsd:213`. */
export function readCT_BorderBoxPr(cur: XmlCursor, ctx: ReadContext): CT_BorderBoxPr {
  const start = requireStart(cur, 'CT_BorderBoxPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BorderBoxPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let hideTop: CT_OnOff | undefined;
  let hideBot: CT_OnOff | undefined;
  let hideLeft: CT_OnOff | undefined;
  let hideRight: CT_OnOff | undefined;
  let strikeH: CT_OnOff | undefined;
  let strikeV: CT_OnOff | undefined;
  let strikeBLTR: CT_OnOff | undefined;
  let strikeTLBR: CT_OnOff | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BorderBoxPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'hideTop': {
            hideTop = readCT_OnOff(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hideBot': {
            hideBot = readCT_OnOff(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'hideLeft': {
            hideLeft = readCT_OnOff(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'hideRight': {
            hideRight = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'strikeH': {
            strikeH = readCT_OnOff(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'strikeV': {
            strikeV = readCT_OnOff(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'strikeBLTR': {
            strikeBLTR = readCT_OnOff(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'strikeTLBR': {
            strikeTLBR = readCT_OnOff(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BorderBoxPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BorderBoxPr', cur);
    cur.next();
  }

  return {
    hideTop,
    hideBot,
    hideLeft,
    hideRight,
    strikeH,
    strikeV,
    strikeBLTR,
    strikeTLBR,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Box`. See `transitional/shared-math.xsd:207`. */
export function readCT_Box(cur: XmlCursor, ctx: ReadContext): CT_Box {
  const start = requireStart(cur, 'CT_Box');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Box', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let boxPr: CT_BoxPr | undefined;
  let e: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Box', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'boxPr': {
            boxPr = readCT_BoxPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Box', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Box', cur);
    cur.next();
  }

  return {
    boxPr,
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BoxPr`. See `transitional/shared-math.xsd:197`. */
export function readCT_BoxPr(cur: XmlCursor, ctx: ReadContext): CT_BoxPr {
  const start = requireStart(cur, 'CT_BoxPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BoxPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let opEmu: CT_OnOff | undefined;
  let noBreak: CT_OnOff | undefined;
  let diff: CT_OnOff | undefined;
  let brk: CT_ManualBreak | undefined;
  let aln: CT_OnOff | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BoxPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'opEmu': {
            opEmu = readCT_OnOff(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'noBreak': {
            noBreak = readCT_OnOff(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'diff': {
            diff = readCT_OnOff(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'brk': {
            brk = readCT_ManualBreak(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'aln': {
            aln = readCT_OnOff(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BoxPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BoxPr', cur);
    cur.next();
  }

  return {
    opEmu,
    noBreak,
    diff,
    brk,
    aln,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BreakBin`. See `transitional/shared-math.xsd:533`. */
export function readCT_BreakBin(cur: XmlCursor, ctx: ReadContext): CT_BreakBin {
  const start = requireStart(cur, 'CT_BreakBin');
  let val: ST_BreakBin | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_BreakBin(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_BreakBin', a, 'ST_BreakBin', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BreakBin', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BreakBin', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_BreakBin', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BreakBin', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_BreakBinSub`. See `transitional/shared-math.xsd:543`. */
export function readCT_BreakBinSub(cur: XmlCursor, ctx: ReadContext): CT_BreakBinSub {
  const start = requireStart(cur, 'CT_BreakBinSub');
  let val: ST_BreakBinSub | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_BreakBinSub(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_BreakBinSub', a, 'ST_BreakBinSub', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BreakBinSub', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BreakBinSub', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_BreakBinSub', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BreakBinSub', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Char`. See `transitional/shared-math.xsd:52`. */
export function readCT_Char(cur: XmlCursor, ctx: ReadContext): CT_Char {
  const start = requireStart(cur, 'CT_Char');
  let val: ST_Char | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Char(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Char', a, 'ST_Char', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Char', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Char', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Char', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Char', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Char', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_CtrlPr`. See `transitional/shared-math.xsd:168`. */
export function readCT_CtrlPr(cur: XmlCursor, ctx: ReadContext): CT_CtrlPr {
  const start = requireStart(cur, 'CT_CtrlPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_CtrlPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let rPrMath: CT_CtrlPr_RPrMath | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CtrlPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rPr': {
            rPrMath = { kind: 'rPr', value: readCT_RPr(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ins': {
            rPrMath = { kind: 'ins', value: readCT_MathCtrlIns(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'del': {
            rPrMath = { kind: 'del', value: readCT_MathCtrlDel(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CtrlPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CtrlPr', cur);
    cur.next();
  }

  return {
    rPrMath,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_D`. See `transitional/shared-math.xsd:242`. */
export function readCT_D(cur: XmlCursor, ctx: ReadContext): CT_D {
  const start = requireStart(cur, 'CT_D');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let dPr: CT_DPr | undefined;
  const e: CT_OMathArg[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'dPr': {
            dPr = readCT_DPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e.push(readCT_OMathArg(cur, ctx));
            $slot = 1;
            $index = e.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_D', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_D', cur);
    cur.next();
  }

  return {
    dPr,
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DPr`. See `transitional/shared-math.xsd:232`. */
export function readCT_DPr(cur: XmlCursor, ctx: ReadContext): CT_DPr {
  const start = requireStart(cur, 'CT_DPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let begChr: CT_Char | undefined;
  let sepChr: CT_Char | undefined;
  let endChr: CT_Char | undefined;
  let grow: CT_OnOff | undefined;
  let shp: CT_Shp | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'begChr': {
            begChr = readCT_Char(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sepChr': {
            sepChr = readCT_Char(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'endChr': {
            endChr = readCT_Char(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'grow': {
            grow = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'shp': {
            shp = readCT_Shp(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DPr', cur);
    cur.next();
  }

  return {
    begChr,
    sepChr,
    endChr,
    grow,
    shp,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_EqArr`. See `transitional/shared-math.xsd:258`. */
export function readCT_EqArr(cur: XmlCursor, ctx: ReadContext): CT_EqArr {
  const start = requireStart(cur, 'CT_EqArr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_EqArr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let eqArrPr: CT_EqArrPr | undefined;
  const e: CT_OMathArg[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EqArr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'eqArrPr': {
            eqArrPr = readCT_EqArrPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e.push(readCT_OMathArg(cur, ctx));
            $slot = 1;
            $index = e.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_EqArr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EqArr', cur);
    cur.next();
  }

  return {
    eqArrPr,
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_EqArrPr`. See `transitional/shared-math.xsd:248`. */
export function readCT_EqArrPr(cur: XmlCursor, ctx: ReadContext): CT_EqArrPr {
  const start = requireStart(cur, 'CT_EqArrPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_EqArrPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let baseJc: CT_YAlign | undefined;
  let maxDist: CT_OnOff | undefined;
  let objDist: CT_OnOff | undefined;
  let rSpRule: CT_SpacingRule | undefined;
  let rSp: CT_UnSignedInteger | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EqArrPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'baseJc': {
            baseJc = readCT_YAlign(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'maxDist': {
            maxDist = readCT_OnOff(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'objDist': {
            objDist = readCT_OnOff(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'rSpRule': {
            rSpRule = readCT_SpacingRule(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'rSp': {
            rSp = readCT_UnSignedInteger(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_EqArrPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EqArrPr', cur);
    cur.next();
  }

  return {
    baseJc,
    maxDist,
    objDist,
    rSpRule,
    rSp,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_F`. See `transitional/shared-math.xsd:270`. */
export function readCT_F(cur: XmlCursor, ctx: ReadContext): CT_F {
  const start = requireStart(cur, 'CT_F');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_F', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let fPr: CT_FPr | undefined;
  let num: CT_OMathArg | undefined;
  let den: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_F', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'fPr': {
            fPr = readCT_FPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'num': {
            num = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'den': {
            den = readCT_OMathArg(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_F', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_F', cur);
    cur.next();
  }

  return {
    fPr,
    num,
    den,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FPr`. See `transitional/shared-math.xsd:264`. */
export function readCT_FPr(cur: XmlCursor, ctx: ReadContext): CT_FPr {
  const start = requireStart(cur, 'CT_FPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let type: CT_FType | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'type': {
            type = readCT_FType(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FPr', cur);
    cur.next();
  }

  return {
    type,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FType`. See `transitional/shared-math.xsd:84`. */
export function readCT_FType(cur: XmlCursor, ctx: ReadContext): CT_FType {
  const start = requireStart(cur, 'CT_FType');
  let val: ST_FType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_FType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_FType', a, 'ST_FType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_FType', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Func`. See `transitional/shared-math.xsd:282`. */
export function readCT_Func(cur: XmlCursor, ctx: ReadContext): CT_Func {
  const start = requireStart(cur, 'CT_Func');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Func', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let funcPr: CT_FuncPr | undefined;
  let fName: CT_OMathArg | undefined;
  let e: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Func', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'funcPr': {
            funcPr = readCT_FuncPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'fName': {
            fName = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Func', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Func', cur);
    cur.next();
  }

  return {
    funcPr,
    fName,
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FuncPr`. See `transitional/shared-math.xsd:277`. */
export function readCT_FuncPr(cur: XmlCursor, ctx: ReadContext): CT_FuncPr {
  const start = requireStart(cur, 'CT_FuncPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FuncPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FuncPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FuncPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FuncPr', cur);
    cur.next();
  }

  return {
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GroupChr`. See `transitional/shared-math.xsd:297`. */
export function readCT_GroupChr(cur: XmlCursor, ctx: ReadContext): CT_GroupChr {
  const start = requireStart(cur, 'CT_GroupChr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GroupChr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let groupChrPr: CT_GroupChrPr | undefined;
  let e: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GroupChr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'groupChrPr': {
            groupChrPr = readCT_GroupChrPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GroupChr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GroupChr', cur);
    cur.next();
  }

  return {
    groupChrPr,
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GroupChrPr`. See `transitional/shared-math.xsd:289`. */
export function readCT_GroupChrPr(cur: XmlCursor, ctx: ReadContext): CT_GroupChrPr {
  const start = requireStart(cur, 'CT_GroupChrPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GroupChrPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let chr: CT_Char | undefined;
  let pos: CT_TopBot | undefined;
  let vertJc: CT_TopBot | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GroupChrPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'chr': {
            chr = readCT_Char(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pos': {
            pos = readCT_TopBot(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'vertJc': {
            vertJc = readCT_TopBot(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GroupChrPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GroupChrPr', cur);
    cur.next();
  }

  return {
    chr,
    pos,
    vertJc,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Integer2`. See `transitional/shared-math.xsd:29`. */
export function readCT_Integer2(cur: XmlCursor, ctx: ReadContext): CT_Integer2 {
  const start = requireStart(cur, 'CT_Integer2');
  let val: ST_Integer2 | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Integer2(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Integer2', a, 'ST_Integer2', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Integer2', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Integer2', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Integer2', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Integer2', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Integer2', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Integer255`. See `transitional/shared-math.xsd:20`. */
export function readCT_Integer255(cur: XmlCursor, ctx: ReadContext): CT_Integer255 {
  const start = requireStart(cur, 'CT_Integer255');
  let val: ST_Integer255 | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Integer255(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Integer255', a, 'ST_Integer255', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Integer255', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Integer255', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Integer255', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Integer255', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Integer255', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_LimLoc`. See `transitional/shared-math.xsd:93`. */
export function readCT_LimLoc(cur: XmlCursor, ctx: ReadContext): CT_LimLoc {
  const start = requireStart(cur, 'CT_LimLoc');
  let val: ST_LimLoc | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_LimLoc(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_LimLoc', a, 'ST_LimLoc', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LimLoc', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_LimLoc', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LimLoc', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LimLoc', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LimLoc', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_LimLow`. See `transitional/shared-math.xsd:308`. */
export function readCT_LimLow(cur: XmlCursor, ctx: ReadContext): CT_LimLow {
  const start = requireStart(cur, 'CT_LimLow');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_LimLow', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let limLowPr: CT_LimLowPr | undefined;
  let e: CT_OMathArg | undefined;
  let lim: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LimLow', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'limLowPr': {
            limLowPr = readCT_LimLowPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'lim': {
            lim = readCT_OMathArg(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LimLow', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LimLow', cur);
    cur.next();
  }

  return {
    limLowPr,
    e,
    lim,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LimLowPr`. See `transitional/shared-math.xsd:303`. */
export function readCT_LimLowPr(cur: XmlCursor, ctx: ReadContext): CT_LimLowPr {
  const start = requireStart(cur, 'CT_LimLowPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_LimLowPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LimLowPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LimLowPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LimLowPr', cur);
    cur.next();
  }

  return {
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LimUpp`. See `transitional/shared-math.xsd:320`. */
export function readCT_LimUpp(cur: XmlCursor, ctx: ReadContext): CT_LimUpp {
  const start = requireStart(cur, 'CT_LimUpp');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_LimUpp', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let limUppPr: CT_LimUppPr | undefined;
  let e: CT_OMathArg | undefined;
  let lim: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LimUpp', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'limUppPr': {
            limUppPr = readCT_LimUppPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'lim': {
            lim = readCT_OMathArg(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LimUpp', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LimUpp', cur);
    cur.next();
  }

  return {
    limUppPr,
    e,
    lim,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LimUppPr`. See `transitional/shared-math.xsd:315`. */
export function readCT_LimUppPr(cur: XmlCursor, ctx: ReadContext): CT_LimUppPr {
  const start = requireStart(cur, 'CT_LimUppPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_LimUppPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LimUppPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LimUppPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LimUppPr', cur);
    cur.next();
  }

  return {
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_M`. See `transitional/shared-math.xsd:361`. */
export function readCT_M(cur: XmlCursor, ctx: ReadContext): CT_M {
  const start = requireStart(cur, 'CT_M');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_M', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let mPr: CT_MPr | undefined;
  const mr: CT_MR[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_M', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'mPr': {
            mPr = readCT_MPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'mr': {
            mr.push(readCT_MR(cur, ctx));
            $slot = 1;
            $index = mr.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_M', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_M', cur);
    cur.next();
  }

  return {
    mPr,
    mr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_MC`. See `transitional/shared-math.xsd:333`. */
export function readCT_MC(cur: XmlCursor, ctx: ReadContext): CT_MC {
  const start = requireStart(cur, 'CT_MC');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_MC', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let mcPr: CT_MCPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MC', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'mcPr': {
            mcPr = readCT_MCPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_MC', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MC', cur);
    cur.next();
  }

  return {
    mcPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_MCPr`. See `transitional/shared-math.xsd:327`. */
export function readCT_MCPr(cur: XmlCursor, ctx: ReadContext): CT_MCPr {
  const start = requireStart(cur, 'CT_MCPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_MCPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let count: CT_Integer255 | undefined;
  let mcJc: CT_XAlign | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MCPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'count': {
            count = readCT_Integer255(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'mcJc': {
            mcJc = readCT_XAlign(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_MCPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MCPr', cur);
    cur.next();
  }

  return {
    count,
    mcJc,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_MCS`. See `transitional/shared-math.xsd:338`. */
export function readCT_MCS(cur: XmlCursor, ctx: ReadContext): CT_MCS {
  const start = requireStart(cur, 'CT_MCS');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_MCS', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const mc: CT_MC[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MCS', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'mc': {
            mc.push(readCT_MC(cur, ctx));
            $slot = 0;
            $index = mc.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_MCS', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MCS', cur);
    cur.next();
  }

  return {
    mc,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_MPr`. See `transitional/shared-math.xsd:343`. */
export function readCT_MPr(cur: XmlCursor, ctx: ReadContext): CT_MPr {
  const start = requireStart(cur, 'CT_MPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_MPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let baseJc: CT_YAlign | undefined;
  let plcHide: CT_OnOff | undefined;
  let rSpRule: CT_SpacingRule | undefined;
  let cGpRule: CT_SpacingRule | undefined;
  let rSp: CT_UnSignedInteger | undefined;
  let cSp: CT_UnSignedInteger | undefined;
  let cGp: CT_UnSignedInteger | undefined;
  let mcs: CT_MCS | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'baseJc': {
            baseJc = readCT_YAlign(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'plcHide': {
            plcHide = readCT_OnOff(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'rSpRule': {
            rSpRule = readCT_SpacingRule(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'cGpRule': {
            cGpRule = readCT_SpacingRule(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'rSp': {
            rSp = readCT_UnSignedInteger(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'cSp': {
            cSp = readCT_UnSignedInteger(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'cGp': {
            cGp = readCT_UnSignedInteger(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'mcs': {
            mcs = readCT_MCS(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_MPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MPr', cur);
    cur.next();
  }

  return {
    baseJc,
    plcHide,
    rSpRule,
    cGpRule,
    rSp,
    cSp,
    cGp,
    mcs,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_MR`. See `transitional/shared-math.xsd:356`. */
export function readCT_MR(cur: XmlCursor, ctx: ReadContext): CT_MR {
  const start = requireStart(cur, 'CT_MR');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_MR', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const e: CT_OMathArg[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MR', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'e': {
            e.push(readCT_OMathArg(cur, ctx));
            $slot = 0;
            $index = e.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_MR', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MR', cur);
    cur.next();
  }

  return {
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ManualBreak`. See `transitional/shared-math.xsd:129`. */
export function readCT_ManualBreak(cur: XmlCursor, ctx: ReadContext): CT_ManualBreak {
  const start = requireStart(cur, 'CT_ManualBreak');
  let alnAt: ST_Integer255 | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'alnAt': {
          const v = parseST_Integer255(a.value);
          if (v !== undefined) {
            alnAt = v;
            continue;
          }
          ctx.invalidValue('CT_ManualBreak', a, 'ST_Integer255', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ManualBreak', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ManualBreak', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ManualBreak', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ManualBreak', cur);
    cur.next();
  }
  return {
    alnAt,
    $unknownAttrs,
  };
}

/** Read a `CT_MathPr`. See `transitional/shared-math.xsd:546`. */
export function readCT_MathPr(cur: XmlCursor, ctx: ReadContext): CT_MathPr {
  const start = requireStart(cur, 'CT_MathPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_MathPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let mathFont: CT_String | undefined;
  let brkBin: CT_BreakBin | undefined;
  let brkBinSub: CT_BreakBinSub | undefined;
  let smallFrac: CT_OnOff | undefined;
  let dispDef: CT_OnOff | undefined;
  let lMargin: CT_TwipsMeasure | undefined;
  let rMargin: CT_TwipsMeasure | undefined;
  let defJc: CT_OMathJc | undefined;
  let preSp: CT_TwipsMeasure | undefined;
  let postSp: CT_TwipsMeasure | undefined;
  let interSp: CT_TwipsMeasure | undefined;
  let intraSp: CT_TwipsMeasure | undefined;
  let content: CT_MathPr_Content | undefined;
  let intLim: CT_LimLoc | undefined;
  let naryLim: CT_LimLoc | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MathPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'mathFont': {
            mathFont = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'brkBin': {
            brkBin = readCT_BreakBin(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'brkBinSub': {
            brkBinSub = readCT_BreakBinSub(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'smallFrac': {
            smallFrac = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'dispDef': {
            dispDef = readCT_OnOff(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'lMargin': {
            lMargin = readCT_TwipsMeasure(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'rMargin': {
            rMargin = readCT_TwipsMeasure(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'defJc': {
            defJc = readCT_OMathJc(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'preSp': {
            preSp = readCT_TwipsMeasure(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'postSp': {
            postSp = readCT_TwipsMeasure(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'interSp': {
            interSp = readCT_TwipsMeasure(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'intraSp': {
            intraSp = readCT_TwipsMeasure(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'wrapIndent': {
            content = { kind: 'wrapIndent', value: readCT_TwipsMeasure(cur, ctx) };
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'wrapRight': {
            content = { kind: 'wrapRight', value: readCT_OnOff(cur, ctx) };
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'intLim': {
            intLim = readCT_LimLoc(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'naryLim': {
            naryLim = readCT_LimLoc(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_MathPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MathPr', cur);
    cur.next();
  }

  return {
    mathFont,
    brkBin,
    brkBinSub,
    smallFrac,
    dispDef,
    lMargin,
    rMargin,
    defJc,
    preSp,
    postSp,
    interSp,
    intraSp,
    content,
    intLim,
    naryLim,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Nary`. See `transitional/shared-math.xsd:377`. */
export function readCT_Nary(cur: XmlCursor, ctx: ReadContext): CT_Nary {
  const start = requireStart(cur, 'CT_Nary');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Nary', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let naryPr: CT_NaryPr | undefined;
  let sub: CT_OMathArg | undefined;
  let sup: CT_OMathArg | undefined;
  let e: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Nary', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'naryPr': {
            naryPr = readCT_NaryPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sub': {
            sub = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sup': {
            sup = readCT_OMathArg(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Nary', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Nary', cur);
    cur.next();
  }

  return {
    naryPr,
    sub,
    sup,
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NaryPr`. See `transitional/shared-math.xsd:367`. */
export function readCT_NaryPr(cur: XmlCursor, ctx: ReadContext): CT_NaryPr {
  const start = requireStart(cur, 'CT_NaryPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NaryPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let chr: CT_Char | undefined;
  let limLoc: CT_LimLoc | undefined;
  let grow: CT_OnOff | undefined;
  let subHide: CT_OnOff | undefined;
  let supHide: CT_OnOff | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NaryPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'chr': {
            chr = readCT_Char(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'limLoc': {
            limLoc = readCT_LimLoc(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'grow': {
            grow = readCT_OnOff(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'subHide': {
            subHide = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'supHide': {
            supHide = readCT_OnOff(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NaryPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NaryPr', cur);
    cur.next();
  }

  return {
    chr,
    limLoc,
    grow,
    subHide,
    supHide,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OMath`. See `transitional/shared-math.xsd:575`. */
export function readCT_OMath(cur: XmlCursor, ctx: ReadContext): CT_OMath {
  const start = requireStart(cur, 'CT_OMath');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_OMath', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const oMathElements: CT_OMath_OMathElements[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OMath', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'acc': {
            oMathElements.push({ kind: 'acc', value: readCT_Acc(cur, ctx) });
            continue;
          }
          case 'bar': {
            oMathElements.push({ kind: 'bar', value: readCT_Bar(cur, ctx) });
            continue;
          }
          case 'box': {
            oMathElements.push({ kind: 'box', value: readCT_Box(cur, ctx) });
            continue;
          }
          case 'borderBox': {
            oMathElements.push({ kind: 'borderBox', value: readCT_BorderBox(cur, ctx) });
            continue;
          }
          case 'd': {
            oMathElements.push({ kind: 'd', value: readCT_D(cur, ctx) });
            continue;
          }
          case 'eqArr': {
            oMathElements.push({ kind: 'eqArr', value: readCT_EqArr(cur, ctx) });
            continue;
          }
          case 'f': {
            oMathElements.push({ kind: 'f', value: readCT_F(cur, ctx) });
            continue;
          }
          case 'func': {
            oMathElements.push({ kind: 'func', value: readCT_Func(cur, ctx) });
            continue;
          }
          case 'groupChr': {
            oMathElements.push({ kind: 'groupChr', value: readCT_GroupChr(cur, ctx) });
            continue;
          }
          case 'limLow': {
            oMathElements.push({ kind: 'limLow', value: readCT_LimLow(cur, ctx) });
            continue;
          }
          case 'limUpp': {
            oMathElements.push({ kind: 'limUpp', value: readCT_LimUpp(cur, ctx) });
            continue;
          }
          case 'm': {
            oMathElements.push({ kind: 'm', value: readCT_M(cur, ctx) });
            continue;
          }
          case 'nary': {
            oMathElements.push({ kind: 'nary', value: readCT_Nary(cur, ctx) });
            continue;
          }
          case 'phant': {
            oMathElements.push({ kind: 'phant', value: readCT_Phant(cur, ctx) });
            continue;
          }
          case 'rad': {
            oMathElements.push({ kind: 'rad', value: readCT_Rad(cur, ctx) });
            continue;
          }
          case 'sPre': {
            oMathElements.push({ kind: 'sPre', value: readCT_SPre(cur, ctx) });
            continue;
          }
          case 'sSub': {
            oMathElements.push({ kind: 'sSub', value: readCT_SSub(cur, ctx) });
            continue;
          }
          case 'sSubSup': {
            oMathElements.push({ kind: 'sSubSup', value: readCT_SSubSup(cur, ctx) });
            continue;
          }
          case 'sSup': {
            oMathElements.push({ kind: 'sSup', value: readCT_SSup(cur, ctx) });
            continue;
          }
          case 'r': {
            oMathElements.push({ kind: 'r', value: readCT_R(cur, ctx) });
            continue;
          }
          case 'oMathPara': {
            oMathElements.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            oMathElements.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            oMathElements.push({ kind: 'customXml', value: readCT_CustomXmlRun(cur, ctx) });
            continue;
          }
          case 'fldSimple': {
            oMathElements.push({ kind: 'fldSimple', value: readCT_SimpleField(cur, ctx) });
            continue;
          }
          case 'hyperlink': {
            oMathElements.push({ kind: 'hyperlink', value: readCT_Hyperlink(cur, ctx) });
            continue;
          }
          case 'smartTag': {
            oMathElements.push({ kind: 'smartTag', value: readCT_SmartTagRun(cur, ctx) });
            continue;
          }
          case 'sdt': {
            oMathElements.push({ kind: 'sdt', value: readCT_SdtRun(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            oMathElements.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            oMathElements.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            oMathElements.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            oMathElements.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            oMathElements.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            oMathElements.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            oMathElements.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            oMathElements.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            oMathElements.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            oMathElements.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            oMathElements.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            oMathElements.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            oMathElements.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            oMathElements.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            oMathElements.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            oMathElements.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            oMathElements.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            oMathElements.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            oMathElements.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            oMathElements.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            oMathElements.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            oMathElements.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            oMathElements.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OMath', ev.localName, ev.uri, cur);
      oMathElements.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OMath', cur);
    cur.next();
  }

  return {
    oMathElements,
    $unknownAttrs,
  };
}

/** Read a `CT_OMathArg`. See `transitional/shared-math.xsd:500`. */
export function readCT_OMathArg(cur: XmlCursor, ctx: ReadContext): CT_OMathArg {
  const start = requireStart(cur, 'CT_OMathArg');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_OMathArg', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let argPr: CT_OMathArgPr | undefined;
  const oMathElements: CT_OMathArg_OMathElements[] = [];
  let ctrlPr: CT_CtrlPr | undefined;

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OMathArg', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'argPr': {
            argPr = readCT_OMathArgPr(cur, ctx);
            continue;
          }
          case 'acc': {
            oMathElements.push({ kind: 'acc', value: readCT_Acc(cur, ctx) });
            continue;
          }
          case 'bar': {
            oMathElements.push({ kind: 'bar', value: readCT_Bar(cur, ctx) });
            continue;
          }
          case 'box': {
            oMathElements.push({ kind: 'box', value: readCT_Box(cur, ctx) });
            continue;
          }
          case 'borderBox': {
            oMathElements.push({ kind: 'borderBox', value: readCT_BorderBox(cur, ctx) });
            continue;
          }
          case 'd': {
            oMathElements.push({ kind: 'd', value: readCT_D(cur, ctx) });
            continue;
          }
          case 'eqArr': {
            oMathElements.push({ kind: 'eqArr', value: readCT_EqArr(cur, ctx) });
            continue;
          }
          case 'f': {
            oMathElements.push({ kind: 'f', value: readCT_F(cur, ctx) });
            continue;
          }
          case 'func': {
            oMathElements.push({ kind: 'func', value: readCT_Func(cur, ctx) });
            continue;
          }
          case 'groupChr': {
            oMathElements.push({ kind: 'groupChr', value: readCT_GroupChr(cur, ctx) });
            continue;
          }
          case 'limLow': {
            oMathElements.push({ kind: 'limLow', value: readCT_LimLow(cur, ctx) });
            continue;
          }
          case 'limUpp': {
            oMathElements.push({ kind: 'limUpp', value: readCT_LimUpp(cur, ctx) });
            continue;
          }
          case 'm': {
            oMathElements.push({ kind: 'm', value: readCT_M(cur, ctx) });
            continue;
          }
          case 'nary': {
            oMathElements.push({ kind: 'nary', value: readCT_Nary(cur, ctx) });
            continue;
          }
          case 'phant': {
            oMathElements.push({ kind: 'phant', value: readCT_Phant(cur, ctx) });
            continue;
          }
          case 'rad': {
            oMathElements.push({ kind: 'rad', value: readCT_Rad(cur, ctx) });
            continue;
          }
          case 'sPre': {
            oMathElements.push({ kind: 'sPre', value: readCT_SPre(cur, ctx) });
            continue;
          }
          case 'sSub': {
            oMathElements.push({ kind: 'sSub', value: readCT_SSub(cur, ctx) });
            continue;
          }
          case 'sSubSup': {
            oMathElements.push({ kind: 'sSubSup', value: readCT_SSubSup(cur, ctx) });
            continue;
          }
          case 'sSup': {
            oMathElements.push({ kind: 'sSup', value: readCT_SSup(cur, ctx) });
            continue;
          }
          case 'r': {
            oMathElements.push({ kind: 'r', value: readCT_R(cur, ctx) });
            continue;
          }
          case 'oMathPara': {
            oMathElements.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            oMathElements.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            oMathElements.push({ kind: 'customXml', value: readCT_CustomXmlRun(cur, ctx) });
            continue;
          }
          case 'fldSimple': {
            oMathElements.push({ kind: 'fldSimple', value: readCT_SimpleField(cur, ctx) });
            continue;
          }
          case 'hyperlink': {
            oMathElements.push({ kind: 'hyperlink', value: readCT_Hyperlink(cur, ctx) });
            continue;
          }
          case 'smartTag': {
            oMathElements.push({ kind: 'smartTag', value: readCT_SmartTagRun(cur, ctx) });
            continue;
          }
          case 'sdt': {
            oMathElements.push({ kind: 'sdt', value: readCT_SdtRun(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            oMathElements.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            oMathElements.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            oMathElements.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            oMathElements.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            oMathElements.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            oMathElements.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            oMathElements.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            oMathElements.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            oMathElements.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            oMathElements.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            oMathElements.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            oMathElements.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            oMathElements.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            oMathElements.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            oMathElements.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            oMathElements.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            oMathElements.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            oMathElements.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            oMathElements.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            oMathElements.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            oMathElements.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            oMathElements.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            oMathElements.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OMathArg', ev.localName, ev.uri, cur);
      oMathElements.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OMathArg', cur);
    cur.next();
  }

  return {
    argPr,
    oMathElements,
    ctrlPr,
    $unknownAttrs,
  };
}

/** Read a `CT_OMathArgPr`. See `transitional/shared-math.xsd:495`. */
export function readCT_OMathArgPr(cur: XmlCursor, ctx: ReadContext): CT_OMathArgPr {
  const start = requireStart(cur, 'CT_OMathArgPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_OMathArgPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let argSz: CT_Integer2 | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OMathArgPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'argSz': {
            argSz = readCT_Integer2(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OMathArgPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OMathArgPr', cur);
    cur.next();
  }

  return {
    argSz,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OMathJc`. See `transitional/shared-math.xsd:515`. */
export function readCT_OMathJc(cur: XmlCursor, ctx: ReadContext): CT_OMathJc {
  const start = requireStart(cur, 'CT_OMathJc');
  let val: ST_Jc | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Jc(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_OMathJc', a, 'ST_Jc', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OMathJc', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OMathJc', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_OMathJc', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OMathJc', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_OMathPara`. See `transitional/shared-math.xsd:569`. */
export function readCT_OMathPara(cur: XmlCursor, ctx: ReadContext): CT_OMathPara {
  const start = requireStart(cur, 'CT_OMathPara');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_OMathPara', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let oMathParaPr: CT_OMathParaPr | undefined;
  const oMath: CT_OMath[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OMathPara', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathParaPr': {
            oMathParaPr = readCT_OMathParaPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'oMath': {
            oMath.push(readCT_OMath(cur, ctx));
            $slot = 1;
            $index = oMath.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OMathPara', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OMathPara', cur);
    cur.next();
  }

  return {
    oMathParaPr,
    oMath,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OMathParaPr`. See `transitional/shared-math.xsd:518`. */
export function readCT_OMathParaPr(cur: XmlCursor, ctx: ReadContext): CT_OMathParaPr {
  const start = requireStart(cur, 'CT_OMathParaPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_OMathParaPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let jc: CT_OMathJc | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OMathParaPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'jc': {
            jc = readCT_OMathJc(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OMathParaPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OMathParaPr', cur);
    cur.next();
  }

  return {
    jc,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OnOff`. See `transitional/shared-math.xsd:55`. */
export function readCT_OnOff(cur: XmlCursor, ctx: ReadContext): CT_OnOff {
  const start = requireStart(cur, 'CT_OnOff');
  let val: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_OnOff', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OnOff', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OnOff', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_OnOff', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OnOff', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Phant`. See `transitional/shared-math.xsd:395`. */
export function readCT_Phant(cur: XmlCursor, ctx: ReadContext): CT_Phant {
  const start = requireStart(cur, 'CT_Phant');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Phant', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let phantPr: CT_PhantPr | undefined;
  let e: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Phant', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'phantPr': {
            phantPr = readCT_PhantPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Phant', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Phant', cur);
    cur.next();
  }

  return {
    phantPr,
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PhantPr`. See `transitional/shared-math.xsd:385`. */
export function readCT_PhantPr(cur: XmlCursor, ctx: ReadContext): CT_PhantPr {
  const start = requireStart(cur, 'CT_PhantPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PhantPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let show: CT_OnOff | undefined;
  let zeroWid: CT_OnOff | undefined;
  let zeroAsc: CT_OnOff | undefined;
  let zeroDesc: CT_OnOff | undefined;
  let transp: CT_OnOff | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PhantPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'show': {
            show = readCT_OnOff(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'zeroWid': {
            zeroWid = readCT_OnOff(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'zeroAsc': {
            zeroAsc = readCT_OnOff(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'zeroDesc': {
            zeroDesc = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'transp': {
            transp = readCT_OnOff(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PhantPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PhantPr', cur);
    cur.next();
  }

  return {
    show,
    zeroWid,
    zeroAsc,
    zeroDesc,
    transp,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_R`. See `transitional/shared-math.xsd:158`. */
export function readCT_R(cur: XmlCursor, ctx: ReadContext): CT_R {
  const start = requireStart(cur, 'CT_R');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_R', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let rPr: CT_RPR | undefined;
  let rPr2: CT_RPr | undefined;
  const content: CT_R_Content[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_R', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'rPr': {
            rPr = readCT_RPR(cur, ctx);
            continue;
          }
          case 't': {
            content.push({ kind: 't_math', value: readCT_Text(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rPr': {
            rPr2 = readCT_RPr(cur, ctx);
            continue;
          }
          case 'br': {
            content.push({ kind: 'br', value: readCT_Br(cur, ctx) });
            continue;
          }
          case 't': {
            content.push({ kind: 't_wml', value: wml_readCT_Text(cur, ctx) });
            continue;
          }
          case 'contentPart': {
            content.push({ kind: 'contentPart', value: readCT_Rel(cur, ctx) });
            continue;
          }
          case 'delText': {
            content.push({ kind: 'delText', value: wml_readCT_Text(cur, ctx) });
            continue;
          }
          case 'instrText': {
            content.push({ kind: 'instrText', value: wml_readCT_Text(cur, ctx) });
            continue;
          }
          case 'delInstrText': {
            content.push({ kind: 'delInstrText', value: wml_readCT_Text(cur, ctx) });
            continue;
          }
          case 'noBreakHyphen': {
            content.push({ kind: 'noBreakHyphen', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'softHyphen': {
            content.push({ kind: 'softHyphen', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'dayShort': {
            content.push({ kind: 'dayShort', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'monthShort': {
            content.push({ kind: 'monthShort', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'yearShort': {
            content.push({ kind: 'yearShort', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'dayLong': {
            content.push({ kind: 'dayLong', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'monthLong': {
            content.push({ kind: 'monthLong', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'yearLong': {
            content.push({ kind: 'yearLong', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'annotationRef': {
            content.push({ kind: 'annotationRef', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'footnoteRef': {
            content.push({ kind: 'footnoteRef', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'endnoteRef': {
            content.push({ kind: 'endnoteRef', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'separator': {
            content.push({ kind: 'separator', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'continuationSeparator': {
            content.push({ kind: 'continuationSeparator', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'sym': {
            content.push({ kind: 'sym', value: readCT_Sym(cur, ctx) });
            continue;
          }
          case 'pgNum': {
            content.push({ kind: 'pgNum', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'cr': {
            content.push({ kind: 'cr', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'tab': {
            content.push({ kind: 'tab', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'object': {
            content.push({ kind: 'object', value: readCT_Object(cur, ctx) });
            continue;
          }
          case 'pict': {
            content.push({ kind: 'pict', value: readCT_Picture(cur, ctx) });
            continue;
          }
          case 'fldChar': {
            content.push({ kind: 'fldChar', value: readCT_FldChar(cur, ctx) });
            continue;
          }
          case 'ruby': {
            content.push({ kind: 'ruby', value: readCT_Ruby(cur, ctx) });
            continue;
          }
          case 'footnoteReference': {
            content.push({ kind: 'footnoteReference', value: readCT_FtnEdnRef(cur, ctx) });
            continue;
          }
          case 'endnoteReference': {
            content.push({ kind: 'endnoteReference', value: readCT_FtnEdnRef(cur, ctx) });
            continue;
          }
          case 'commentReference': {
            content.push({ kind: 'commentReference', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'drawing': {
            content.push({ kind: 'drawing', value: readCT_Drawing(cur, ctx) });
            continue;
          }
          case 'ptab': {
            content.push({ kind: 'ptab', value: readCT_PTab(cur, ctx) });
            continue;
          }
          case 'lastRenderedPageBreak': {
            content.push({ kind: 'lastRenderedPageBreak', value: readCT_Empty(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_R', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_R', cur);
    cur.next();
  }

  return {
    rPr,
    rPr2,
    content,
    $unknownAttrs,
  };
}

/** Read a `CT_RPR`. See `transitional/shared-math.xsd:138`. */
export function readCT_RPR(cur: XmlCursor, ctx: ReadContext): CT_RPR {
  const start = requireStart(cur, 'CT_RPR');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_RPR', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let lit: CT_OnOff | undefined;
  let content: CT_RPR_Content | undefined;
  let brk: CT_ManualBreak | undefined;
  let aln: CT_OnOff | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RPR', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'lit': {
            lit = readCT_OnOff(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'nor': {
            content = { kind: 'nor', value: readCT_OnOff(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'scr': {
            content = { kind: 'scr', value: readCT_Script(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sty': {
            content = { kind: 'sty', value: readCT_Style(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'brk': {
            brk = readCT_ManualBreak(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'aln': {
            aln = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RPR', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RPR', cur);
    cur.next();
  }

  return {
    lit,
    content,
    brk,
    aln,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Rad`. See `transitional/shared-math.xsd:407`. */
export function readCT_Rad(cur: XmlCursor, ctx: ReadContext): CT_Rad {
  const start = requireStart(cur, 'CT_Rad');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Rad', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let radPr: CT_RadPr | undefined;
  let deg: CT_OMathArg | undefined;
  let e: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Rad', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'radPr': {
            radPr = readCT_RadPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'deg': {
            deg = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Rad', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Rad', cur);
    cur.next();
  }

  return {
    radPr,
    deg,
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_RadPr`. See `transitional/shared-math.xsd:401`. */
export function readCT_RadPr(cur: XmlCursor, ctx: ReadContext): CT_RadPr {
  const start = requireStart(cur, 'CT_RadPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_RadPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let degHide: CT_OnOff | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RadPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'degHide': {
            degHide = readCT_OnOff(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RadPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RadPr', cur);
    cur.next();
  }

  return {
    degHide,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SPre`. See `transitional/shared-math.xsd:419`. */
export function readCT_SPre(cur: XmlCursor, ctx: ReadContext): CT_SPre {
  const start = requireStart(cur, 'CT_SPre');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SPre', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let sPrePr: CT_SPrePr | undefined;
  let sub: CT_OMathArg | undefined;
  let sup: CT_OMathArg | undefined;
  let e: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SPre', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'sPrePr': {
            sPrePr = readCT_SPrePr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sub': {
            sub = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sup': {
            sup = readCT_OMathArg(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SPre', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SPre', cur);
    cur.next();
  }

  return {
    sPrePr,
    sub,
    sup,
    e,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SPrePr`. See `transitional/shared-math.xsd:414`. */
export function readCT_SPrePr(cur: XmlCursor, ctx: ReadContext): CT_SPrePr {
  const start = requireStart(cur, 'CT_SPrePr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SPrePr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SPrePr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SPrePr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SPrePr', cur);
    cur.next();
  }

  return {
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SSub`. See `transitional/shared-math.xsd:432`. */
export function readCT_SSub(cur: XmlCursor, ctx: ReadContext): CT_SSub {
  const start = requireStart(cur, 'CT_SSub');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SSub', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let sSubPr: CT_SSubPr | undefined;
  let e: CT_OMathArg | undefined;
  let sub: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SSub', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'sSubPr': {
            sSubPr = readCT_SSubPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sub': {
            sub = readCT_OMathArg(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SSub', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SSub', cur);
    cur.next();
  }

  return {
    sSubPr,
    e,
    sub,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SSubPr`. See `transitional/shared-math.xsd:427`. */
export function readCT_SSubPr(cur: XmlCursor, ctx: ReadContext): CT_SSubPr {
  const start = requireStart(cur, 'CT_SSubPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SSubPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SSubPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SSubPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SSubPr', cur);
    cur.next();
  }

  return {
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SSubSup`. See `transitional/shared-math.xsd:445`. */
export function readCT_SSubSup(cur: XmlCursor, ctx: ReadContext): CT_SSubSup {
  const start = requireStart(cur, 'CT_SSubSup');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SSubSup', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let sSubSupPr: CT_SSubSupPr | undefined;
  let e: CT_OMathArg | undefined;
  let sub: CT_OMathArg | undefined;
  let sup: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SSubSup', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'sSubSupPr': {
            sSubSupPr = readCT_SSubSupPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sub': {
            sub = readCT_OMathArg(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'sup': {
            sup = readCT_OMathArg(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SSubSup', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SSubSup', cur);
    cur.next();
  }

  return {
    sSubSupPr,
    e,
    sub,
    sup,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SSubSupPr`. See `transitional/shared-math.xsd:439`. */
export function readCT_SSubSupPr(cur: XmlCursor, ctx: ReadContext): CT_SSubSupPr {
  const start = requireStart(cur, 'CT_SSubSupPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SSubSupPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let alnScr: CT_OnOff | undefined;
  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SSubSupPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'alnScr': {
            alnScr = readCT_OnOff(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SSubSupPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SSubSupPr', cur);
    cur.next();
  }

  return {
    alnScr,
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SSup`. See `transitional/shared-math.xsd:458`. */
export function readCT_SSup(cur: XmlCursor, ctx: ReadContext): CT_SSup {
  const start = requireStart(cur, 'CT_SSup');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SSup', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let sSupPr: CT_SSupPr | undefined;
  let e: CT_OMathArg | undefined;
  let sup: CT_OMathArg | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SSup', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'sSupPr': {
            sSupPr = readCT_SSupPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'e': {
            e = readCT_OMathArg(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sup': {
            sup = readCT_OMathArg(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SSup', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SSup', cur);
    cur.next();
  }

  return {
    sSupPr,
    e,
    sup,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SSupPr`. See `transitional/shared-math.xsd:453`. */
export function readCT_SSupPr(cur: XmlCursor, ctx: ReadContext): CT_SSupPr {
  const start = requireStart(cur, 'CT_SSupPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SSupPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ctrlPr: CT_CtrlPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SSupPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'ctrlPr': {
            ctrlPr = readCT_CtrlPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SSupPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SSupPr', cur);
    cur.next();
  }

  return {
    ctrlPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Script`. See `transitional/shared-math.xsd:115`. */
export function readCT_Script(cur: XmlCursor, ctx: ReadContext): CT_Script {
  const start = requireStart(cur, 'CT_Script');
  let val: ST_Script | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Script(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Script', a, 'ST_Script', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Script', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Script', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Script', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Script', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Shp`. See `transitional/shared-math.xsd:73`. */
export function readCT_Shp(cur: XmlCursor, ctx: ReadContext): CT_Shp {
  const start = requireStart(cur, 'CT_Shp');
  let val: ST_Shp | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Shp(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Shp', a, 'ST_Shp', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Shp', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Shp', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Shp', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Shp', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Shp', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_SpacingRule`. See `transitional/shared-math.xsd:38`. */
export function readCT_SpacingRule(cur: XmlCursor, ctx: ReadContext): CT_SpacingRule {
  const start = requireStart(cur, 'CT_SpacingRule');
  let val: ST_SpacingRule | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_SpacingRule(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_SpacingRule', a, 'ST_SpacingRule', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SpacingRule', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_SpacingRule', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SpacingRule', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SpacingRule', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SpacingRule', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_String`. See `transitional/shared-math.xsd:58`. */
export function readCT_String(cur: XmlCursor, ctx: ReadContext): CT_String {
  const start = requireStart(cur, 'CT_String');
  let val: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_String', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_String', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_String', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_String', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_String', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Style`. See `transitional/shared-math.xsd:126`. */
export function readCT_Style(cur: XmlCursor, ctx: ReadContext): CT_Style {
  const start = requireStart(cur, 'CT_Style');
  let val: ST_Style | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Style(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Style', a, 'ST_Style', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Style', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Style', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Style', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Style', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Text`. See `transitional/shared-math.xsd:151`. */
export function readCT_Text(cur: XmlCursor, ctx: ReadContext): CT_Text {
  const start = requireStart(cur, 'CT_Text');
  let space: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_xml_A = ctx.uris['xml'];
  for (const a of start.attrs) {
    if (a.uri === NS_xml_A) {
      switch (a.localName) {
        case 'space':
          space = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Text', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let $value = '';
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Text', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'text') { $value += ev.value; cur.next(); continue; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Text', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    cur.next();
  }
  const $parsed = parseST_String($value);

  return {
    $value: $parsed ?? $value,
    space,
    $unknownAttrs,
  };
}

/** Read a `CT_TopBot`. See `transitional/shared-math.xsd:102`. */
export function readCT_TopBot(cur: XmlCursor, ctx: ReadContext): CT_TopBot {
  const start = requireStart(cur, 'CT_TopBot');
  let val: ST_TopBot | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TopBot(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TopBot', a, 'ST_TopBot', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TopBot', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TopBot', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TopBot', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TopBot', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TopBot', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TwipsMeasure`. See `transitional/shared-math.xsd:523`. */
export function readCT_TwipsMeasure(cur: XmlCursor, ctx: ReadContext): CT_TwipsMeasure {
  const start = requireStart(cur, 'CT_TwipsMeasure');
  let val: ST_TwipsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TwipsMeasure', a, 'ST_TwipsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TwipsMeasure', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TwipsMeasure', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TwipsMeasure', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TwipsMeasure', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TwipsMeasure', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_UnSignedInteger`. See `transitional/shared-math.xsd:44`. */
export function readCT_UnSignedInteger(cur: XmlCursor, ctx: ReadContext): CT_UnSignedInteger {
  const start = requireStart(cur, 'CT_UnSignedInteger');
  let val: ST_UnSignedInteger | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_UnSignedInteger(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_UnSignedInteger', a, 'ST_UnSignedInteger', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_UnSignedInteger', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_UnSignedInteger', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_UnSignedInteger', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_UnSignedInteger', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_UnSignedInteger', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_XAlign`. See `transitional/shared-math.xsd:61`. */
export function readCT_XAlign(cur: XmlCursor, ctx: ReadContext): CT_XAlign {
  const start = requireStart(cur, 'CT_XAlign');
  let val: ST_XAlign | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_XAlign(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_XAlign', a, 'ST_XAlign', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_XAlign', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_XAlign', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_XAlign', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_XAlign', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_XAlign', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_YAlign`. See `transitional/shared-math.xsd:64`. */
export function readCT_YAlign(cur: XmlCursor, ctx: ReadContext): CT_YAlign {
  const start = requireStart(cur, 'CT_YAlign');
  let val: ST_YAlign | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_math_A = ctx.uris['math'];
  for (const a of start.attrs) {
    if (a.uri === NS_math_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_YAlign(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_YAlign', a, 'ST_YAlign', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_YAlign', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_YAlign', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_YAlign', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_YAlign', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_YAlign', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}
