/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the custom-xml namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  isIgnorableWhitespace,
  requireStart,
} from '../../runtime/index.js';
import { parseST_Guid } from '../shared-types/reader.js';
import type { ST_Guid } from '../shared-types/types.js';
import type { CT_DatastoreItem, CT_DatastoreSchemaRef, CT_DatastoreSchemaRefs } from './types.js';

// Complex-type readers.

/** Read a `CT_DatastoreItem`. See `transitional/shared-customXmlDataProperties.xsd:18`. */
export function readCT_DatastoreItem(cur: XmlCursor, ctx: ReadContext): CT_DatastoreItem {
  const start = requireStart(cur, 'CT_DatastoreItem');
  let itemID: ST_Guid | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_custom_xml_A = ctx.uris['custom-xml'];
  for (const a of start.attrs) {
    if (a.uri === NS_custom_xml_A) {
      switch (a.localName) {
        case 'itemID': {
          const v = parseST_Guid(a.value);
          if (v !== undefined) {
            itemID = v;
            continue;
          }
          ctx.invalidValue('CT_DatastoreItem', a, 'ST_Guid', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DatastoreItem', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (itemID === undefined) ctx.missingRequired('CT_DatastoreItem', 'itemID', cur);

  let schemaRefs: CT_DatastoreSchemaRefs | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_custom_xml = ctx.uris['custom-xml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DatastoreItem', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_custom_xml) {
        switch (ev.localName) {
          case 'schemaRefs': {
            schemaRefs = readCT_DatastoreSchemaRefs(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DatastoreItem', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DatastoreItem', cur);
    cur.next();
  }

  return {
    schemaRefs,
    itemID,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DatastoreSchemaRef`. See `transitional/shared-customXmlDataProperties.xsd:9`. */
export function readCT_DatastoreSchemaRef(cur: XmlCursor, ctx: ReadContext): CT_DatastoreSchemaRef {
  const start = requireStart(cur, 'CT_DatastoreSchemaRef');
  let uri: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_custom_xml_A = ctx.uris['custom-xml'];
  for (const a of start.attrs) {
    if (a.uri === NS_custom_xml_A) {
      switch (a.localName) {
        case 'uri':
          uri = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_DatastoreSchemaRef', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (uri === undefined) ctx.missingRequired('CT_DatastoreSchemaRef', 'uri', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DatastoreSchemaRef', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DatastoreSchemaRef', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DatastoreSchemaRef', cur);
    cur.next();
  }
  return {
    uri,
    $unknownAttrs,
  };
}

/** Read a `CT_DatastoreSchemaRefs`. See `transitional/shared-customXmlDataProperties.xsd:12`. */
export function readCT_DatastoreSchemaRefs(cur: XmlCursor, ctx: ReadContext): CT_DatastoreSchemaRefs {
  const start = requireStart(cur, 'CT_DatastoreSchemaRefs');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DatastoreSchemaRefs', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const schemaRef: CT_DatastoreSchemaRef[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_custom_xml = ctx.uris['custom-xml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DatastoreSchemaRefs', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_custom_xml) {
        switch (ev.localName) {
          case 'schemaRef': {
            schemaRef.push(readCT_DatastoreSchemaRef(cur, ctx));
            $slot = 0;
            $index = schemaRef.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DatastoreSchemaRefs', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DatastoreSchemaRefs', cur);
    cur.next();
  }

  return {
    schemaRef,
    $unknown,
    $unknownAttrs,
  };
}
