/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: writers for the dml-diagram namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import { type WriteContext, type XmlSink, uriFor } from '../../runtime/index.js';
import {
  writeCT_BackgroundFormatting,
  writeCT_FlatText,
  writeCT_HslColor,
  writeCT_OfficeArtExtensionList,
  writeCT_PresetColor,
  writeCT_SRgbColor,
  writeCT_ScRgbColor,
  writeCT_Scene3D,
  writeCT_SchemeColor,
  writeCT_Shape3D,
  writeCT_ShapeProperties,
  writeCT_ShapeStyle,
  writeCT_SystemColor,
  writeCT_TextBody,
  writeCT_WholeE2oFormatting,
} from '../dml-main/writer.js';
import type {
  CT_Adj,
  CT_AdjLst,
  CT_Algorithm,
  CT_AnimLvl,
  CT_AnimOne,
  CT_BulletEnabled,
  CT_CTCategories,
  CT_CTCategory,
  CT_CTDescription,
  CT_CTName,
  CT_CTStyleLabel,
  CT_Categories,
  CT_Category,
  CT_ChildMax,
  CT_ChildPref,
  CT_Choose,
  CT_ColorTransform,
  CT_ColorTransformHeader,
  CT_ColorTransformHeaderLst,
  CT_Colors,
  CT_Constraint,
  CT_Constraints,
  CT_Cxn,
  CT_CxnList,
  CT_DataModel,
  CT_Description,
  CT_DiagramDefinition,
  CT_DiagramDefinitionHeader,
  CT_DiagramDefinitionHeaderLst,
  CT_Direction,
  CT_ElemPropSet,
  CT_ForEach,
  CT_HierBranchStyle,
  CT_LayoutNode,
  CT_LayoutVariablePropertySet,
  CT_Name,
  CT_NumericRule,
  CT_OrgChart,
  CT_Otherwise,
  CT_Parameter,
  CT_PresentationOf,
  CT_Pt,
  CT_PtList,
  CT_RelIds,
  CT_ResizeHandles,
  CT_Rules,
  CT_SDCategories,
  CT_SDCategory,
  CT_SDDescription,
  CT_SDName,
  CT_SampleData,
  CT_Shape,
  CT_StyleDefinition,
  CT_StyleDefinitionHeader,
  CT_StyleDefinitionHeaderLst,
  CT_StyleLabel,
  CT_TextProps,
  CT_When,
} from './types.js';

// XML writers. Element names are supplied by the owning particle.

/** Write a `CT_Adj`; the caller supplies its element local name. */
export function writeCT_Adj(s: XmlSink, value: CT_Adj, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['idx'] !== undefined) s.attr(null, 'idx', String(value['idx']));
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AdjLst`; the caller supplies its element local name. */
export function writeCT_AdjLst(s: XmlSink, value: CT_AdjLst, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  for (const v_adj of value['adj']) {
    writeCT_Adj(s, v_adj, ctx, 'adj');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Algorithm`; the caller supplies its element local name. */
export function writeCT_Algorithm(s: XmlSink, value: CT_Algorithm, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['rev'] !== undefined) s.attr(null, 'rev', String(value['rev']));
  for (const v_param of value['param']) {
    writeCT_Parameter(s, v_param, ctx, 'param');
  }
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AnimLvl`; the caller supplies its element local name. */
export function writeCT_AnimLvl(s: XmlSink, value: CT_AnimLvl, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AnimOne`; the caller supplies its element local name. */
export function writeCT_AnimOne(s: XmlSink, value: CT_AnimOne, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_BulletEnabled`; the caller supplies its element local name. */
export function writeCT_BulletEnabled(s: XmlSink, value: CT_BulletEnabled, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Categories`; the caller supplies its element local name. */
export function writeCT_Categories(s: XmlSink, value: CT_Categories, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  for (const v_cat of value['cat']) {
    writeCT_Category(s, v_cat, ctx, 'cat');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Category`; the caller supplies its element local name. */
export function writeCT_Category(s: XmlSink, value: CT_Category, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['pri'] !== undefined) s.attr(null, 'pri', String(value['pri']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ChildMax`; the caller supplies its element local name. */
export function writeCT_ChildMax(s: XmlSink, value: CT_ChildMax, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ChildPref`; the caller supplies its element local name. */
export function writeCT_ChildPref(s: XmlSink, value: CT_ChildPref, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Choose`; the caller supplies its element local name. */
export function writeCT_Choose(s: XmlSink, value: CT_Choose, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  for (const v_if of value['if']) {
    writeCT_When(s, v_if, ctx, 'if');
  }
  if (value['else'] !== undefined) writeCT_Otherwise(s, value['else'], ctx, 'else');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Colors`; the caller supplies its element local name. */
export function writeCT_Colors(s: XmlSink, value: CT_Colors, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['meth'] !== undefined) s.attr(null, 'meth', String(value['meth']));
  if (value['hueDir'] !== undefined) s.attr(null, 'hueDir', String(value['hueDir']));
  for (const v_colorChoice of value['colorChoice']) {
    if (v_colorChoice.kind === 'scrgbClr') writeCT_ScRgbColor(s, v_colorChoice.value, ctx, 'scrgbClr');
    if (v_colorChoice.kind === 'srgbClr') writeCT_SRgbColor(s, v_colorChoice.value, ctx, 'srgbClr');
    if (v_colorChoice.kind === 'hslClr') writeCT_HslColor(s, v_colorChoice.value, ctx, 'hslClr');
    if (v_colorChoice.kind === 'sysClr') writeCT_SystemColor(s, v_colorChoice.value, ctx, 'sysClr');
    if (v_colorChoice.kind === 'schemeClr') writeCT_SchemeColor(s, v_colorChoice.value, ctx, 'schemeClr');
    if (v_colorChoice.kind === 'prstClr') writeCT_PresetColor(s, v_colorChoice.value, ctx, 'prstClr');
    if (v_colorChoice.kind === '$raw') { s.raw(v_colorChoice.value); }
  }
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ColorTransform`; the caller supplies its element local name. */
export function writeCT_ColorTransform(s: XmlSink, value: CT_ColorTransform, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['uniqueId'] !== undefined) s.attr(null, 'uniqueId', String(value['uniqueId']));
  if (value['minVer'] !== undefined) s.attr(null, 'minVer', String(value['minVer']));
  for (const v_title of value['title']) {
    writeCT_CTName(s, v_title, ctx, 'title');
  }
  for (const v_desc of value['desc']) {
    writeCT_CTDescription(s, v_desc, ctx, 'desc');
  }
  if (value['catLst'] !== undefined) writeCT_CTCategories(s, value['catLst'], ctx, 'catLst');
  for (const v_styleLbl of value['styleLbl']) {
    writeCT_CTStyleLabel(s, v_styleLbl, ctx, 'styleLbl');
  }
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ColorTransformHeader`; the caller supplies its element local name. */
export function writeCT_ColorTransformHeader(s: XmlSink, value: CT_ColorTransformHeader, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['uniqueId'] !== undefined) s.attr(null, 'uniqueId', String(value['uniqueId']));
  if (value['minVer'] !== undefined) s.attr(null, 'minVer', String(value['minVer']));
  if (value['resId'] !== undefined) s.attr(null, 'resId', String(value['resId']));
  for (const v_title of value['title']) {
    writeCT_CTName(s, v_title, ctx, 'title');
  }
  for (const v_desc of value['desc']) {
    writeCT_CTDescription(s, v_desc, ctx, 'desc');
  }
  if (value['catLst'] !== undefined) writeCT_CTCategories(s, value['catLst'], ctx, 'catLst');
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ColorTransformHeaderLst`; the caller supplies its element local name. */
export function writeCT_ColorTransformHeaderLst(s: XmlSink, value: CT_ColorTransformHeaderLst, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  for (const v_colorsDefHdr of value['colorsDefHdr']) {
    writeCT_ColorTransformHeader(s, v_colorsDefHdr, ctx, 'colorsDefHdr');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Constraint`; the caller supplies its element local name. */
export function writeCT_Constraint(s: XmlSink, value: CT_Constraint, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['for'] !== undefined) s.attr(null, 'for', String(value['for']));
  if (value['forName'] !== undefined) s.attr(null, 'forName', String(value['forName']));
  if (value['ptType'] !== undefined) s.attr(null, 'ptType', String(value['ptType']));
  if (value['refType'] !== undefined) s.attr(null, 'refType', String(value['refType']));
  if (value['refFor'] !== undefined) s.attr(null, 'refFor', String(value['refFor']));
  if (value['refForName'] !== undefined) s.attr(null, 'refForName', String(value['refForName']));
  if (value['refPtType'] !== undefined) s.attr(null, 'refPtType', String(value['refPtType']));
  if (value['op'] !== undefined) s.attr(null, 'op', String(value['op']));
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  if (value['fact'] !== undefined) s.attr(null, 'fact', String(value['fact']));
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Constraints`; the caller supplies its element local name. */
export function writeCT_Constraints(s: XmlSink, value: CT_Constraints, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  for (const v_constr of value['constr']) {
    writeCT_Constraint(s, v_constr, ctx, 'constr');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_CTCategories`; the caller supplies its element local name. */
export function writeCT_CTCategories(s: XmlSink, value: CT_CTCategories, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  for (const v_cat of value['cat']) {
    writeCT_CTCategory(s, v_cat, ctx, 'cat');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_CTCategory`; the caller supplies its element local name. */
export function writeCT_CTCategory(s: XmlSink, value: CT_CTCategory, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['pri'] !== undefined) s.attr(null, 'pri', String(value['pri']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_CTDescription`; the caller supplies its element local name. */
export function writeCT_CTDescription(s: XmlSink, value: CT_CTDescription, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['lang'] !== undefined) s.attr(null, 'lang', String(value['lang']));
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_CTName`; the caller supplies its element local name. */
export function writeCT_CTName(s: XmlSink, value: CT_CTName, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['lang'] !== undefined) s.attr(null, 'lang', String(value['lang']));
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_CTStyleLabel`; the caller supplies its element local name. */
export function writeCT_CTStyleLabel(s: XmlSink, value: CT_CTStyleLabel, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  if (value['fillClrLst'] !== undefined) writeCT_Colors(s, value['fillClrLst'], ctx, 'fillClrLst');
  if (value['linClrLst'] !== undefined) writeCT_Colors(s, value['linClrLst'], ctx, 'linClrLst');
  if (value['effectClrLst'] !== undefined) writeCT_Colors(s, value['effectClrLst'], ctx, 'effectClrLst');
  if (value['txLinClrLst'] !== undefined) writeCT_Colors(s, value['txLinClrLst'], ctx, 'txLinClrLst');
  if (value['txFillClrLst'] !== undefined) writeCT_Colors(s, value['txFillClrLst'], ctx, 'txFillClrLst');
  if (value['txEffectClrLst'] !== undefined) writeCT_Colors(s, value['txEffectClrLst'], ctx, 'txEffectClrLst');
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Cxn`; the caller supplies its element local name. */
export function writeCT_Cxn(s: XmlSink, value: CT_Cxn, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['modelId'] !== undefined) s.attr(null, 'modelId', String(value['modelId']));
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['srcId'] !== undefined) s.attr(null, 'srcId', String(value['srcId']));
  if (value['destId'] !== undefined) s.attr(null, 'destId', String(value['destId']));
  if (value['srcOrd'] !== undefined) s.attr(null, 'srcOrd', String(value['srcOrd']));
  if (value['destOrd'] !== undefined) s.attr(null, 'destOrd', String(value['destOrd']));
  if (value['parTransId'] !== undefined) s.attr(null, 'parTransId', String(value['parTransId']));
  if (value['sibTransId'] !== undefined) s.attr(null, 'sibTransId', String(value['sibTransId']));
  if (value['presId'] !== undefined) s.attr(null, 'presId', String(value['presId']));
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_CxnList`; the caller supplies its element local name. */
export function writeCT_CxnList(s: XmlSink, value: CT_CxnList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  for (const v_cxn of value['cxn']) {
    writeCT_Cxn(s, v_cxn, ctx, 'cxn');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_DataModel`; the caller supplies its element local name. */
export function writeCT_DataModel(s: XmlSink, value: CT_DataModel, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['ptLst'] !== undefined) writeCT_PtList(s, value['ptLst'], ctx, 'ptLst');
  if (value['cxnLst'] !== undefined) writeCT_CxnList(s, value['cxnLst'], ctx, 'cxnLst');
  if (value['bg'] !== undefined) writeCT_BackgroundFormatting(s, value['bg'], ctx, 'bg');
  if (value['whole'] !== undefined) writeCT_WholeE2oFormatting(s, value['whole'], ctx, 'whole');
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Description`; the caller supplies its element local name. */
export function writeCT_Description(s: XmlSink, value: CT_Description, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['lang'] !== undefined) s.attr(null, 'lang', String(value['lang']));
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_DiagramDefinition`; the caller supplies its element local name. */
export function writeCT_DiagramDefinition(s: XmlSink, value: CT_DiagramDefinition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['uniqueId'] !== undefined) s.attr(null, 'uniqueId', String(value['uniqueId']));
  if (value['minVer'] !== undefined) s.attr(null, 'minVer', String(value['minVer']));
  if (value['defStyle'] !== undefined) s.attr(null, 'defStyle', String(value['defStyle']));
  for (const v_title of value['title']) {
    writeCT_Name(s, v_title, ctx, 'title');
  }
  for (const v_desc of value['desc']) {
    writeCT_Description(s, v_desc, ctx, 'desc');
  }
  if (value['catLst'] !== undefined) writeCT_Categories(s, value['catLst'], ctx, 'catLst');
  if (value['sampData'] !== undefined) writeCT_SampleData(s, value['sampData'], ctx, 'sampData');
  if (value['styleData'] !== undefined) writeCT_SampleData(s, value['styleData'], ctx, 'styleData');
  if (value['clrData'] !== undefined) writeCT_SampleData(s, value['clrData'], ctx, 'clrData');
  if (value['layoutNode'] !== undefined) writeCT_LayoutNode(s, value['layoutNode'], ctx, 'layoutNode');
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_DiagramDefinitionHeader`; the caller supplies its element local name. */
export function writeCT_DiagramDefinitionHeader(s: XmlSink, value: CT_DiagramDefinitionHeader, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['uniqueId'] !== undefined) s.attr(null, 'uniqueId', String(value['uniqueId']));
  if (value['minVer'] !== undefined) s.attr(null, 'minVer', String(value['minVer']));
  if (value['defStyle'] !== undefined) s.attr(null, 'defStyle', String(value['defStyle']));
  if (value['resId'] !== undefined) s.attr(null, 'resId', String(value['resId']));
  for (const v_title of value['title']) {
    writeCT_Name(s, v_title, ctx, 'title');
  }
  for (const v_desc of value['desc']) {
    writeCT_Description(s, v_desc, ctx, 'desc');
  }
  if (value['catLst'] !== undefined) writeCT_Categories(s, value['catLst'], ctx, 'catLst');
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_DiagramDefinitionHeaderLst`; the caller supplies its element local name. */
export function writeCT_DiagramDefinitionHeaderLst(s: XmlSink, value: CT_DiagramDefinitionHeaderLst, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  for (const v_layoutDefHdr of value['layoutDefHdr']) {
    writeCT_DiagramDefinitionHeader(s, v_layoutDefHdr, ctx, 'layoutDefHdr');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Direction`; the caller supplies its element local name. */
export function writeCT_Direction(s: XmlSink, value: CT_Direction, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ElemPropSet`; the caller supplies its element local name. */
export function writeCT_ElemPropSet(s: XmlSink, value: CT_ElemPropSet, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['presAssocID'] !== undefined) s.attr(null, 'presAssocID', String(value['presAssocID']));
  if (value['presName'] !== undefined) s.attr(null, 'presName', String(value['presName']));
  if (value['presStyleLbl'] !== undefined) s.attr(null, 'presStyleLbl', String(value['presStyleLbl']));
  if (value['presStyleIdx'] !== undefined) s.attr(null, 'presStyleIdx', String(value['presStyleIdx']));
  if (value['presStyleCnt'] !== undefined) s.attr(null, 'presStyleCnt', String(value['presStyleCnt']));
  if (value['loTypeId'] !== undefined) s.attr(null, 'loTypeId', String(value['loTypeId']));
  if (value['loCatId'] !== undefined) s.attr(null, 'loCatId', String(value['loCatId']));
  if (value['qsTypeId'] !== undefined) s.attr(null, 'qsTypeId', String(value['qsTypeId']));
  if (value['qsCatId'] !== undefined) s.attr(null, 'qsCatId', String(value['qsCatId']));
  if (value['csTypeId'] !== undefined) s.attr(null, 'csTypeId', String(value['csTypeId']));
  if (value['csCatId'] !== undefined) s.attr(null, 'csCatId', String(value['csCatId']));
  if (value['coherent3DOff'] !== undefined) s.attr(null, 'coherent3DOff', String(value['coherent3DOff']));
  if (value['phldrT'] !== undefined) s.attr(null, 'phldrT', String(value['phldrT']));
  if (value['phldr'] !== undefined) s.attr(null, 'phldr', String(value['phldr']));
  if (value['custAng'] !== undefined) s.attr(null, 'custAng', String(value['custAng']));
  if (value['custFlipVert'] !== undefined) s.attr(null, 'custFlipVert', String(value['custFlipVert']));
  if (value['custFlipHor'] !== undefined) s.attr(null, 'custFlipHor', String(value['custFlipHor']));
  if (value['custSzX'] !== undefined) s.attr(null, 'custSzX', String(value['custSzX']));
  if (value['custSzY'] !== undefined) s.attr(null, 'custSzY', String(value['custSzY']));
  if (value['custScaleX'] !== undefined) s.attr(null, 'custScaleX', String(value['custScaleX']));
  if (value['custScaleY'] !== undefined) s.attr(null, 'custScaleY', String(value['custScaleY']));
  if (value['custT'] !== undefined) s.attr(null, 'custT', String(value['custT']));
  if (value['custLinFactX'] !== undefined) s.attr(null, 'custLinFactX', String(value['custLinFactX']));
  if (value['custLinFactY'] !== undefined) s.attr(null, 'custLinFactY', String(value['custLinFactY']));
  if (value['custLinFactNeighborX'] !== undefined) s.attr(null, 'custLinFactNeighborX', String(value['custLinFactNeighborX']));
  if (value['custLinFactNeighborY'] !== undefined) s.attr(null, 'custLinFactNeighborY', String(value['custLinFactNeighborY']));
  if (value['custRadScaleRad'] !== undefined) s.attr(null, 'custRadScaleRad', String(value['custRadScaleRad']));
  if (value['custRadScaleInc'] !== undefined) s.attr(null, 'custRadScaleInc', String(value['custRadScaleInc']));
  if (value['presLayoutVars'] !== undefined) writeCT_LayoutVariablePropertySet(s, value['presLayoutVars'], ctx, 'presLayoutVars');
  if (value['style'] !== undefined) writeCT_ShapeStyle(s, value['style'], ctx, 'style');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ForEach`; the caller supplies its element local name. */
export function writeCT_ForEach(s: XmlSink, value: CT_ForEach, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  if (value['ref'] !== undefined) s.attr(null, 'ref', String(value['ref']));
  if (value['axis'] !== undefined) s.attr(null, 'axis', String(value['axis']));
  if (value['ptType'] !== undefined) s.attr(null, 'ptType', String(value['ptType']));
  if (value['hideLastTrans'] !== undefined) s.attr(null, 'hideLastTrans', String(value['hideLastTrans']));
  if (value['st'] !== undefined) s.attr(null, 'st', String(value['st']));
  if (value['cnt'] !== undefined) s.attr(null, 'cnt', String(value['cnt']));
  if (value['step'] !== undefined) s.attr(null, 'step', String(value['step']));
  for (const v_content of value['content']) {
    if (v_content.kind === 'alg') writeCT_Algorithm(s, v_content.value, ctx, 'alg');
    if (v_content.kind === 'shape') writeCT_Shape(s, v_content.value, ctx, 'shape');
    if (v_content.kind === 'presOf') writeCT_PresentationOf(s, v_content.value, ctx, 'presOf');
    if (v_content.kind === 'constrLst') writeCT_Constraints(s, v_content.value, ctx, 'constrLst');
    if (v_content.kind === 'ruleLst') writeCT_Rules(s, v_content.value, ctx, 'ruleLst');
    if (v_content.kind === 'forEach') writeCT_ForEach(s, v_content.value, ctx, 'forEach');
    if (v_content.kind === 'layoutNode') writeCT_LayoutNode(s, v_content.value, ctx, 'layoutNode');
    if (v_content.kind === 'choose') writeCT_Choose(s, v_content.value, ctx, 'choose');
    if (v_content.kind === 'extLst') writeCT_OfficeArtExtensionList(s, v_content.value, ctx, 'extLst');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_HierBranchStyle`; the caller supplies its element local name. */
export function writeCT_HierBranchStyle(s: XmlSink, value: CT_HierBranchStyle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_LayoutNode`; the caller supplies its element local name. */
export function writeCT_LayoutNode(s: XmlSink, value: CT_LayoutNode, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  if (value['styleLbl'] !== undefined) s.attr(null, 'styleLbl', String(value['styleLbl']));
  if (value['chOrder'] !== undefined) s.attr(null, 'chOrder', String(value['chOrder']));
  if (value['moveWith'] !== undefined) s.attr(null, 'moveWith', String(value['moveWith']));
  for (const v_content of value['content']) {
    if (v_content.kind === 'alg') writeCT_Algorithm(s, v_content.value, ctx, 'alg');
    if (v_content.kind === 'shape') writeCT_Shape(s, v_content.value, ctx, 'shape');
    if (v_content.kind === 'presOf') writeCT_PresentationOf(s, v_content.value, ctx, 'presOf');
    if (v_content.kind === 'constrLst') writeCT_Constraints(s, v_content.value, ctx, 'constrLst');
    if (v_content.kind === 'ruleLst') writeCT_Rules(s, v_content.value, ctx, 'ruleLst');
    if (v_content.kind === 'varLst') writeCT_LayoutVariablePropertySet(s, v_content.value, ctx, 'varLst');
    if (v_content.kind === 'forEach') writeCT_ForEach(s, v_content.value, ctx, 'forEach');
    if (v_content.kind === 'layoutNode') writeCT_LayoutNode(s, v_content.value, ctx, 'layoutNode');
    if (v_content.kind === 'choose') writeCT_Choose(s, v_content.value, ctx, 'choose');
    if (v_content.kind === 'extLst') writeCT_OfficeArtExtensionList(s, v_content.value, ctx, 'extLst');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_LayoutVariablePropertySet`; the caller supplies its element local name. */
export function writeCT_LayoutVariablePropertySet(s: XmlSink, value: CT_LayoutVariablePropertySet, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['orgChart'] !== undefined) writeCT_OrgChart(s, value['orgChart'], ctx, 'orgChart');
  if (value['chMax'] !== undefined) writeCT_ChildMax(s, value['chMax'], ctx, 'chMax');
  if (value['chPref'] !== undefined) writeCT_ChildPref(s, value['chPref'], ctx, 'chPref');
  if (value['bulletEnabled'] !== undefined) writeCT_BulletEnabled(s, value['bulletEnabled'], ctx, 'bulletEnabled');
  if (value['dir'] !== undefined) writeCT_Direction(s, value['dir'], ctx, 'dir');
  if (value['hierBranch'] !== undefined) writeCT_HierBranchStyle(s, value['hierBranch'], ctx, 'hierBranch');
  if (value['animOne'] !== undefined) writeCT_AnimOne(s, value['animOne'], ctx, 'animOne');
  if (value['animLvl'] !== undefined) writeCT_AnimLvl(s, value['animLvl'], ctx, 'animLvl');
  if (value['resizeHandles'] !== undefined) writeCT_ResizeHandles(s, value['resizeHandles'], ctx, 'resizeHandles');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Name`; the caller supplies its element local name. */
export function writeCT_Name(s: XmlSink, value: CT_Name, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['lang'] !== undefined) s.attr(null, 'lang', String(value['lang']));
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_NumericRule`; the caller supplies its element local name. */
export function writeCT_NumericRule(s: XmlSink, value: CT_NumericRule, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['for'] !== undefined) s.attr(null, 'for', String(value['for']));
  if (value['forName'] !== undefined) s.attr(null, 'forName', String(value['forName']));
  if (value['ptType'] !== undefined) s.attr(null, 'ptType', String(value['ptType']));
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  if (value['fact'] !== undefined) s.attr(null, 'fact', String(value['fact']));
  if (value['max'] !== undefined) s.attr(null, 'max', String(value['max']));
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_OrgChart`; the caller supplies its element local name. */
export function writeCT_OrgChart(s: XmlSink, value: CT_OrgChart, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Otherwise`; the caller supplies its element local name. */
export function writeCT_Otherwise(s: XmlSink, value: CT_Otherwise, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  for (const v_content of value['content']) {
    if (v_content.kind === 'alg') writeCT_Algorithm(s, v_content.value, ctx, 'alg');
    if (v_content.kind === 'shape') writeCT_Shape(s, v_content.value, ctx, 'shape');
    if (v_content.kind === 'presOf') writeCT_PresentationOf(s, v_content.value, ctx, 'presOf');
    if (v_content.kind === 'constrLst') writeCT_Constraints(s, v_content.value, ctx, 'constrLst');
    if (v_content.kind === 'ruleLst') writeCT_Rules(s, v_content.value, ctx, 'ruleLst');
    if (v_content.kind === 'forEach') writeCT_ForEach(s, v_content.value, ctx, 'forEach');
    if (v_content.kind === 'layoutNode') writeCT_LayoutNode(s, v_content.value, ctx, 'layoutNode');
    if (v_content.kind === 'choose') writeCT_Choose(s, v_content.value, ctx, 'choose');
    if (v_content.kind === 'extLst') writeCT_OfficeArtExtensionList(s, v_content.value, ctx, 'extLst');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Parameter`; the caller supplies its element local name. */
export function writeCT_Parameter(s: XmlSink, value: CT_Parameter, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_PresentationOf`; the caller supplies its element local name. */
export function writeCT_PresentationOf(s: XmlSink, value: CT_PresentationOf, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['axis'] !== undefined) s.attr(null, 'axis', String(value['axis']));
  if (value['ptType'] !== undefined) s.attr(null, 'ptType', String(value['ptType']));
  if (value['hideLastTrans'] !== undefined) s.attr(null, 'hideLastTrans', String(value['hideLastTrans']));
  if (value['st'] !== undefined) s.attr(null, 'st', String(value['st']));
  if (value['cnt'] !== undefined) s.attr(null, 'cnt', String(value['cnt']));
  if (value['step'] !== undefined) s.attr(null, 'step', String(value['step']));
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Pt`; the caller supplies its element local name. */
export function writeCT_Pt(s: XmlSink, value: CT_Pt, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['modelId'] !== undefined) s.attr(null, 'modelId', String(value['modelId']));
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['cxnId'] !== undefined) s.attr(null, 'cxnId', String(value['cxnId']));
  if (value['prSet'] !== undefined) writeCT_ElemPropSet(s, value['prSet'], ctx, 'prSet');
  if (value['spPr'] !== undefined) writeCT_ShapeProperties(s, value['spPr'], ctx, 'spPr');
  if (value['t'] !== undefined) writeCT_TextBody(s, value['t'], ctx, 't');
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_PtList`; the caller supplies its element local name. */
export function writeCT_PtList(s: XmlSink, value: CT_PtList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  for (const v_pt of value['pt']) {
    writeCT_Pt(s, v_pt, ctx, 'pt');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_RelIds`; the caller supplies its element local name. */
export function writeCT_RelIds(s: XmlSink, value: CT_RelIds, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['dm'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'dm', String(value['dm']));
  if (value['lo'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'lo', String(value['lo']));
  if (value['qs'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'qs', String(value['qs']));
  if (value['cs'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'cs', String(value['cs']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ResizeHandles`; the caller supplies its element local name. */
export function writeCT_ResizeHandles(s: XmlSink, value: CT_ResizeHandles, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Rules`; the caller supplies its element local name. */
export function writeCT_Rules(s: XmlSink, value: CT_Rules, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  for (const v_rule of value['rule']) {
    writeCT_NumericRule(s, v_rule, ctx, 'rule');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_SampleData`; the caller supplies its element local name. */
export function writeCT_SampleData(s: XmlSink, value: CT_SampleData, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['useDef'] !== undefined) s.attr(null, 'useDef', String(value['useDef']));
  if (value['dataModel'] !== undefined) writeCT_DataModel(s, value['dataModel'], ctx, 'dataModel');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_SDCategories`; the caller supplies its element local name. */
export function writeCT_SDCategories(s: XmlSink, value: CT_SDCategories, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  for (const v_cat of value['cat']) {
    writeCT_SDCategory(s, v_cat, ctx, 'cat');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_SDCategory`; the caller supplies its element local name. */
export function writeCT_SDCategory(s: XmlSink, value: CT_SDCategory, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['pri'] !== undefined) s.attr(null, 'pri', String(value['pri']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_SDDescription`; the caller supplies its element local name. */
export function writeCT_SDDescription(s: XmlSink, value: CT_SDDescription, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['lang'] !== undefined) s.attr(null, 'lang', String(value['lang']));
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_SDName`; the caller supplies its element local name. */
export function writeCT_SDName(s: XmlSink, value: CT_SDName, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['lang'] !== undefined) s.attr(null, 'lang', String(value['lang']));
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Shape`; the caller supplies its element local name. */
export function writeCT_Shape(s: XmlSink, value: CT_Shape, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['rot'] !== undefined) s.attr(null, 'rot', String(value['rot']));
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['blip'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'blip', String(value['blip']));
  if (value['zOrderOff'] !== undefined) s.attr(null, 'zOrderOff', String(value['zOrderOff']));
  if (value['hideGeom'] !== undefined) s.attr(null, 'hideGeom', String(value['hideGeom']));
  if (value['lkTxEntry'] !== undefined) s.attr(null, 'lkTxEntry', String(value['lkTxEntry']));
  if (value['blipPhldr'] !== undefined) s.attr(null, 'blipPhldr', String(value['blipPhldr']));
  if (value['adjLst'] !== undefined) writeCT_AdjLst(s, value['adjLst'], ctx, 'adjLst');
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_StyleDefinition`; the caller supplies its element local name. */
export function writeCT_StyleDefinition(s: XmlSink, value: CT_StyleDefinition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['uniqueId'] !== undefined) s.attr(null, 'uniqueId', String(value['uniqueId']));
  if (value['minVer'] !== undefined) s.attr(null, 'minVer', String(value['minVer']));
  for (const v_title of value['title']) {
    writeCT_SDName(s, v_title, ctx, 'title');
  }
  for (const v_desc of value['desc']) {
    writeCT_SDDescription(s, v_desc, ctx, 'desc');
  }
  if (value['catLst'] !== undefined) writeCT_SDCategories(s, value['catLst'], ctx, 'catLst');
  if (value['scene3d'] !== undefined) writeCT_Scene3D(s, value['scene3d'], ctx, 'scene3d');
  for (const v_styleLbl of value['styleLbl']) {
    writeCT_StyleLabel(s, v_styleLbl, ctx, 'styleLbl');
  }
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_StyleDefinitionHeader`; the caller supplies its element local name. */
export function writeCT_StyleDefinitionHeader(s: XmlSink, value: CT_StyleDefinitionHeader, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['uniqueId'] !== undefined) s.attr(null, 'uniqueId', String(value['uniqueId']));
  if (value['minVer'] !== undefined) s.attr(null, 'minVer', String(value['minVer']));
  if (value['resId'] !== undefined) s.attr(null, 'resId', String(value['resId']));
  for (const v_title of value['title']) {
    writeCT_SDName(s, v_title, ctx, 'title');
  }
  for (const v_desc of value['desc']) {
    writeCT_SDDescription(s, v_desc, ctx, 'desc');
  }
  if (value['catLst'] !== undefined) writeCT_SDCategories(s, value['catLst'], ctx, 'catLst');
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_StyleDefinitionHeaderLst`; the caller supplies its element local name. */
export function writeCT_StyleDefinitionHeaderLst(s: XmlSink, value: CT_StyleDefinitionHeaderLst, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  for (const v_styleDefHdr of value['styleDefHdr']) {
    writeCT_StyleDefinitionHeader(s, v_styleDefHdr, ctx, 'styleDefHdr');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_StyleLabel`; the caller supplies its element local name. */
export function writeCT_StyleLabel(s: XmlSink, value: CT_StyleLabel, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  if (value['scene3d'] !== undefined) writeCT_Scene3D(s, value['scene3d'], ctx, 'scene3d');
  if (value['sp3d'] !== undefined) writeCT_Shape3D(s, value['sp3d'], ctx, 'sp3d');
  if (value['txPr'] !== undefined) writeCT_TextProps(s, value['txPr'], ctx, 'txPr');
  if (value['style'] !== undefined) writeCT_ShapeStyle(s, value['style'], ctx, 'style');
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextProps`; the caller supplies its element local name. */
export function writeCT_TextProps(s: XmlSink, value: CT_TextProps, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['text3D'] !== undefined) {
    if (value['text3D'].kind === 'sp3d') writeCT_Shape3D(s, value['text3D'].value, ctx, 'sp3d');
    if (value['text3D'].kind === 'flatTx') writeCT_FlatText(s, value['text3D'].value, ctx, 'flatTx');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_When`; the caller supplies its element local name. */
export function writeCT_When(s: XmlSink, value: CT_When, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-diagram'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  if (value['axis'] !== undefined) s.attr(null, 'axis', String(value['axis']));
  if (value['ptType'] !== undefined) s.attr(null, 'ptType', String(value['ptType']));
  if (value['hideLastTrans'] !== undefined) s.attr(null, 'hideLastTrans', String(value['hideLastTrans']));
  if (value['st'] !== undefined) s.attr(null, 'st', String(value['st']));
  if (value['cnt'] !== undefined) s.attr(null, 'cnt', String(value['cnt']));
  if (value['step'] !== undefined) s.attr(null, 'step', String(value['step']));
  if (value['func'] !== undefined) s.attr(null, 'func', String(value['func']));
  if (value['arg'] !== undefined) s.attr(null, 'arg', String(value['arg']));
  if (value['op'] !== undefined) s.attr(null, 'op', String(value['op']));
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const v_content of value['content']) {
    if (v_content.kind === 'alg') writeCT_Algorithm(s, v_content.value, ctx, 'alg');
    if (v_content.kind === 'shape') writeCT_Shape(s, v_content.value, ctx, 'shape');
    if (v_content.kind === 'presOf') writeCT_PresentationOf(s, v_content.value, ctx, 'presOf');
    if (v_content.kind === 'constrLst') writeCT_Constraints(s, v_content.value, ctx, 'constrLst');
    if (v_content.kind === 'ruleLst') writeCT_Rules(s, v_content.value, ctx, 'ruleLst');
    if (v_content.kind === 'forEach') writeCT_ForEach(s, v_content.value, ctx, 'forEach');
    if (v_content.kind === 'layoutNode') writeCT_LayoutNode(s, v_content.value, ctx, 'layoutNode');
    if (v_content.kind === 'choose') writeCT_Choose(s, v_content.value, ctx, 'choose');
    if (v_content.kind === 'extLst') writeCT_OfficeArtExtensionList(s, v_content.value, ctx, 'extLst');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}
