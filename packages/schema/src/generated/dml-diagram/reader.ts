/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the dml-diagram namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  parseDouble,
  parseInteger,
  parseList,
  parseXsdBoolean,
  requireStart,
} from '../../runtime/index.js';
import {
  parseST_ShapeType,
  readCT_BackgroundFormatting,
  readCT_FlatText,
  readCT_HslColor,
  readCT_OfficeArtExtensionList,
  readCT_PresetColor,
  readCT_SRgbColor,
  readCT_ScRgbColor,
  readCT_Scene3D,
  readCT_SchemeColor,
  readCT_Shape3D,
  readCT_ShapeProperties,
  readCT_ShapeStyle,
  readCT_SystemColor,
  readCT_TextBody,
  readCT_WholeE2oFormatting,
} from '../dml-main/reader.js';
import type {
  CT_BackgroundFormatting,
  CT_OfficeArtExtensionList,
  CT_Scene3D,
  CT_Shape3D,
  CT_ShapeProperties,
  CT_ShapeStyle,
  CT_TextBody,
  CT_WholeE2oFormatting,
} from '../dml-main/types.js';
import { parseST_RelationshipId } from '../relationships/reader.js';
import type { ST_RelationshipId } from '../relationships/types.js';
import { parseST_Guid, parseST_Percentage } from '../shared-types/reader.js';
import {
  type CT_Adj,
  type CT_AdjLst,
  type CT_Algorithm,
  type CT_AnimLvl,
  type CT_AnimOne,
  type CT_BulletEnabled,
  type CT_CTCategories,
  type CT_CTCategory,
  type CT_CTDescription,
  type CT_CTName,
  type CT_CTStyleLabel,
  type CT_Categories,
  type CT_Category,
  type CT_ChildMax,
  type CT_ChildPref,
  type CT_Choose,
  type CT_ColorTransform,
  type CT_ColorTransformHeader,
  type CT_ColorTransformHeaderLst,
  type CT_Colors,
  type CT_Colors_ColorChoice,
  type CT_Constraint,
  type CT_Constraints,
  type CT_Cxn,
  type CT_CxnList,
  type CT_DataModel,
  type CT_Description,
  type CT_DiagramDefinition,
  type CT_DiagramDefinitionHeader,
  type CT_DiagramDefinitionHeaderLst,
  type CT_Direction,
  type CT_ElemPropSet,
  type CT_ForEach,
  type CT_ForEach_Content,
  type CT_HierBranchStyle,
  type CT_LayoutNode,
  type CT_LayoutNode_Content,
  type CT_LayoutVariablePropertySet,
  type CT_Name,
  type CT_NumericRule,
  type CT_OrgChart,
  type CT_Otherwise,
  type CT_Otherwise_Content,
  type CT_Parameter,
  type CT_PresentationOf,
  type CT_Pt,
  type CT_PtList,
  type CT_RelIds,
  type CT_ResizeHandles,
  type CT_Rules,
  type CT_SDCategories,
  type CT_SDCategory,
  type CT_SDDescription,
  type CT_SDName,
  type CT_SampleData,
  type CT_Shape,
  type CT_StyleDefinition,
  type CT_StyleDefinitionHeader,
  type CT_StyleDefinitionHeaderLst,
  type CT_StyleLabel,
  type CT_TextProps,
  type CT_TextProps_Text3D,
  type CT_When,
  type CT_When_Content,
  type ST_AlgorithmType,
  ST_AlgorithmType_VALUES,
  type ST_AnimLvlStr,
  ST_AnimLvlStr_VALUES,
  type ST_AnimOneStr,
  ST_AnimOneStr_VALUES,
  type ST_ArrowheadStyle,
  ST_ArrowheadStyle_VALUES,
  type ST_AutoTextRotation,
  ST_AutoTextRotation_VALUES,
  type ST_AxisType,
  ST_AxisType_VALUES,
  type ST_AxisTypes,
  type ST_BendPoint,
  ST_BendPoint_VALUES,
  type ST_BoolOperator,
  ST_BoolOperator_VALUES,
  type ST_Booleans,
  type ST_Breakpoint,
  ST_Breakpoint_VALUES,
  type ST_CenterShapeMapping,
  ST_CenterShapeMapping_VALUES,
  type ST_ChildAlignment,
  ST_ChildAlignment_VALUES,
  type ST_ChildDirection,
  ST_ChildDirection_VALUES,
  type ST_ChildOrderType,
  ST_ChildOrderType_VALUES,
  type ST_ClrAppMethod,
  ST_ClrAppMethod_VALUES,
  type ST_ConnectorDimension,
  ST_ConnectorDimension_VALUES,
  type ST_ConnectorPoint,
  ST_ConnectorPoint_VALUES,
  type ST_ConnectorRouting,
  ST_ConnectorRouting_VALUES,
  type ST_ConstraintRelationship,
  ST_ConstraintRelationship_VALUES,
  type ST_ConstraintType,
  ST_ConstraintType_VALUES,
  type ST_ContinueDirection,
  ST_ContinueDirection_VALUES,
  type ST_CxnType,
  ST_CxnType_VALUES,
  type ST_DiagramHorizontalAlignment,
  ST_DiagramHorizontalAlignment_VALUES,
  type ST_DiagramTextAlignment,
  ST_DiagramTextAlignment_VALUES,
  type ST_Direction,
  ST_Direction_VALUES,
  type ST_ElementType,
  ST_ElementType_VALUES,
  type ST_ElementTypes,
  type ST_FallbackDimension,
  ST_FallbackDimension_VALUES,
  type ST_FlowDirection,
  ST_FlowDirection_VALUES,
  type ST_FunctionArgument,
  type ST_FunctionOperator,
  ST_FunctionOperator_VALUES,
  type ST_FunctionType,
  ST_FunctionType_VALUES,
  type ST_FunctionValue,
  type ST_GrowDirection,
  ST_GrowDirection_VALUES,
  type ST_HierBranchStyle,
  ST_HierBranchStyle_VALUES,
  type ST_HierarchyAlignment,
  ST_HierarchyAlignment_VALUES,
  type ST_HueDir,
  ST_HueDir_VALUES,
  type ST_Index1,
  type ST_Ints,
  type ST_LayoutShapeType,
  type ST_LinearDirection,
  ST_LinearDirection_VALUES,
  type ST_ModelId,
  type ST_NodeCount,
  type ST_NodeHorizontalAlignment,
  ST_NodeHorizontalAlignment_VALUES,
  type ST_NodeVerticalAlignment,
  ST_NodeVerticalAlignment_VALUES,
  type ST_Offset,
  ST_Offset_VALUES,
  type ST_OutputShapeType,
  ST_OutputShapeType_VALUES,
  type ST_ParameterId,
  ST_ParameterId_VALUES,
  type ST_ParameterVal,
  type ST_PrSetCustVal,
  type ST_PtType,
  ST_PtType_VALUES,
  type ST_PyramidAccentPosition,
  ST_PyramidAccentPosition_VALUES,
  type ST_PyramidAccentTextMargin,
  ST_PyramidAccentTextMargin_VALUES,
  type ST_ResizeHandlesStr,
  ST_ResizeHandlesStr_VALUES,
  type ST_RotationPath,
  ST_RotationPath_VALUES,
  type ST_SecondaryChildAlignment,
  ST_SecondaryChildAlignment_VALUES,
  type ST_SecondaryLinearDirection,
  ST_SecondaryLinearDirection_VALUES,
  type ST_StartingElement,
  ST_StartingElement_VALUES,
  type ST_TextAnchorHorizontal,
  ST_TextAnchorHorizontal_VALUES,
  type ST_TextAnchorVertical,
  ST_TextAnchorVertical_VALUES,
  type ST_TextBlockDirection,
  ST_TextBlockDirection_VALUES,
  type ST_TextDirection,
  ST_TextDirection_VALUES,
  type ST_UnsignedInts,
  type ST_VariableType,
  ST_VariableType_VALUES,
  type ST_VerticalAlignment,
  ST_VerticalAlignment_VALUES,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_AlgorithmType(value: string): ST_AlgorithmType | undefined {
  const s = collapse(value);
  return ST_AlgorithmType_VALUES.has(s) ? (s as ST_AlgorithmType) : undefined;
}

export function parseST_AnimLvlStr(value: string): ST_AnimLvlStr | undefined {
  const s = collapse(value);
  return ST_AnimLvlStr_VALUES.has(s) ? (s as ST_AnimLvlStr) : undefined;
}

export function parseST_AnimOneStr(value: string): ST_AnimOneStr | undefined {
  const s = collapse(value);
  return ST_AnimOneStr_VALUES.has(s) ? (s as ST_AnimOneStr) : undefined;
}

export function parseST_ArrowheadStyle(value: string): ST_ArrowheadStyle | undefined {
  const s = collapse(value);
  return ST_ArrowheadStyle_VALUES.has(s) ? (s as ST_ArrowheadStyle) : undefined;
}

export function parseST_AutoTextRotation(value: string): ST_AutoTextRotation | undefined {
  const s = collapse(value);
  return ST_AutoTextRotation_VALUES.has(s) ? (s as ST_AutoTextRotation) : undefined;
}

export function parseST_AxisType(value: string): ST_AxisType | undefined {
  const s = collapse(value);
  return ST_AxisType_VALUES.has(s) ? (s as ST_AxisType) : undefined;
}

export function parseST_AxisTypes(value: string): ST_AxisTypes | undefined {
  return parseList(value, parseST_AxisType) as ST_AxisTypes | undefined;
}

export function parseST_BendPoint(value: string): ST_BendPoint | undefined {
  const s = collapse(value);
  return ST_BendPoint_VALUES.has(s) ? (s as ST_BendPoint) : undefined;
}

export function parseST_BoolOperator(value: string): ST_BoolOperator | undefined {
  const s = collapse(value);
  return ST_BoolOperator_VALUES.has(s) ? (s as ST_BoolOperator) : undefined;
}

export function parseST_Booleans(value: string): ST_Booleans | undefined {
  return parseList(value, parseXsdBoolean) as ST_Booleans | undefined;
}

export function parseST_Breakpoint(value: string): ST_Breakpoint | undefined {
  const s = collapse(value);
  return ST_Breakpoint_VALUES.has(s) ? (s as ST_Breakpoint) : undefined;
}

export function parseST_CenterShapeMapping(value: string): ST_CenterShapeMapping | undefined {
  const s = collapse(value);
  return ST_CenterShapeMapping_VALUES.has(s) ? (s as ST_CenterShapeMapping) : undefined;
}

export function parseST_ChildAlignment(value: string): ST_ChildAlignment | undefined {
  const s = collapse(value);
  return ST_ChildAlignment_VALUES.has(s) ? (s as ST_ChildAlignment) : undefined;
}

export function parseST_ChildDirection(value: string): ST_ChildDirection | undefined {
  const s = collapse(value);
  return ST_ChildDirection_VALUES.has(s) ? (s as ST_ChildDirection) : undefined;
}

export function parseST_ChildOrderType(value: string): ST_ChildOrderType | undefined {
  const s = collapse(value);
  return ST_ChildOrderType_VALUES.has(s) ? (s as ST_ChildOrderType) : undefined;
}

export function parseST_ClrAppMethod(value: string): ST_ClrAppMethod | undefined {
  const s = collapse(value);
  return ST_ClrAppMethod_VALUES.has(s) ? (s as ST_ClrAppMethod) : undefined;
}

export function parseST_ConnectorDimension(value: string): ST_ConnectorDimension | undefined {
  const s = collapse(value);
  return ST_ConnectorDimension_VALUES.has(s) ? (s as ST_ConnectorDimension) : undefined;
}

export function parseST_ConnectorPoint(value: string): ST_ConnectorPoint | undefined {
  const s = collapse(value);
  return ST_ConnectorPoint_VALUES.has(s) ? (s as ST_ConnectorPoint) : undefined;
}

export function parseST_ConnectorRouting(value: string): ST_ConnectorRouting | undefined {
  const s = collapse(value);
  return ST_ConnectorRouting_VALUES.has(s) ? (s as ST_ConnectorRouting) : undefined;
}

export function parseST_ConstraintRelationship(value: string): ST_ConstraintRelationship | undefined {
  const s = collapse(value);
  return ST_ConstraintRelationship_VALUES.has(s) ? (s as ST_ConstraintRelationship) : undefined;
}

export function parseST_ConstraintType(value: string): ST_ConstraintType | undefined {
  const s = collapse(value);
  return ST_ConstraintType_VALUES.has(s) ? (s as ST_ConstraintType) : undefined;
}

export function parseST_ContinueDirection(value: string): ST_ContinueDirection | undefined {
  const s = collapse(value);
  return ST_ContinueDirection_VALUES.has(s) ? (s as ST_ContinueDirection) : undefined;
}

export function parseST_CxnType(value: string): ST_CxnType | undefined {
  const s = collapse(value);
  return ST_CxnType_VALUES.has(s) ? (s as ST_CxnType) : undefined;
}

export function parseST_DiagramHorizontalAlignment(value: string): ST_DiagramHorizontalAlignment | undefined {
  const s = collapse(value);
  return ST_DiagramHorizontalAlignment_VALUES.has(s) ? (s as ST_DiagramHorizontalAlignment) : undefined;
}

export function parseST_DiagramTextAlignment(value: string): ST_DiagramTextAlignment | undefined {
  const s = collapse(value);
  return ST_DiagramTextAlignment_VALUES.has(s) ? (s as ST_DiagramTextAlignment) : undefined;
}

export function parseST_Direction(value: string): ST_Direction | undefined {
  const s = collapse(value);
  return ST_Direction_VALUES.has(s) ? (s as ST_Direction) : undefined;
}

export function parseST_ElementType(value: string): ST_ElementType | undefined {
  const s = collapse(value);
  return ST_ElementType_VALUES.has(s) ? (s as ST_ElementType) : undefined;
}

export function parseST_ElementTypes(value: string): ST_ElementTypes | undefined {
  return parseList(value, parseST_ElementType) as ST_ElementTypes | undefined;
}

export function parseST_FallbackDimension(value: string): ST_FallbackDimension | undefined {
  const s = collapse(value);
  return ST_FallbackDimension_VALUES.has(s) ? (s as ST_FallbackDimension) : undefined;
}

export function parseST_FlowDirection(value: string): ST_FlowDirection | undefined {
  const s = collapse(value);
  return ST_FlowDirection_VALUES.has(s) ? (s as ST_FlowDirection) : undefined;
}

export function parseST_FunctionArgument(value: string): ST_FunctionArgument | undefined {
  return (parseST_VariableType(value)) as ST_FunctionArgument | undefined;
}

export function parseST_FunctionOperator(value: string): ST_FunctionOperator | undefined {
  const s = collapse(value);
  return ST_FunctionOperator_VALUES.has(s) ? (s as ST_FunctionOperator) : undefined;
}

export function parseST_FunctionType(value: string): ST_FunctionType | undefined {
  const s = collapse(value);
  return ST_FunctionType_VALUES.has(s) ? (s as ST_FunctionType) : undefined;
}

export function parseST_FunctionValue(value: string): ST_FunctionValue | undefined {
  return (parseInteger(value) ?? parseXsdBoolean(value) ?? parseST_Direction(value) ?? parseST_HierBranchStyle(value) ?? parseST_AnimOneStr(value) ?? parseST_AnimLvlStr(value) ?? parseST_ResizeHandlesStr(value)) as ST_FunctionValue | undefined;
}

export function parseST_GrowDirection(value: string): ST_GrowDirection | undefined {
  const s = collapse(value);
  return ST_GrowDirection_VALUES.has(s) ? (s as ST_GrowDirection) : undefined;
}

export function parseST_HierBranchStyle(value: string): ST_HierBranchStyle | undefined {
  const s = collapse(value);
  return ST_HierBranchStyle_VALUES.has(s) ? (s as ST_HierBranchStyle) : undefined;
}

export function parseST_HierarchyAlignment(value: string): ST_HierarchyAlignment | undefined {
  const s = collapse(value);
  return ST_HierarchyAlignment_VALUES.has(s) ? (s as ST_HierarchyAlignment) : undefined;
}

export function parseST_HueDir(value: string): ST_HueDir | undefined {
  const s = collapse(value);
  return ST_HueDir_VALUES.has(s) ? (s as ST_HueDir) : undefined;
}

export function parseST_Index1(value: string): ST_Index1 | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_Index1);
}

export function parseST_Ints(value: string): ST_Ints | undefined {
  return parseList(value, parseInteger) as ST_Ints | undefined;
}

export function parseST_LayoutShapeType(value: string): ST_LayoutShapeType | undefined {
  return (parseST_ShapeType(value) ?? parseST_OutputShapeType(value)) as ST_LayoutShapeType | undefined;
}

export function parseST_LinearDirection(value: string): ST_LinearDirection | undefined {
  const s = collapse(value);
  return ST_LinearDirection_VALUES.has(s) ? (s as ST_LinearDirection) : undefined;
}

export function parseST_ModelId(value: string): ST_ModelId | undefined {
  return (parseInteger(value) ?? parseST_Guid(value)) as ST_ModelId | undefined;
}

export function parseST_NodeCount(value: string): ST_NodeCount | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_NodeCount);
}

export function parseST_NodeHorizontalAlignment(value: string): ST_NodeHorizontalAlignment | undefined {
  const s = collapse(value);
  return ST_NodeHorizontalAlignment_VALUES.has(s) ? (s as ST_NodeHorizontalAlignment) : undefined;
}

export function parseST_NodeVerticalAlignment(value: string): ST_NodeVerticalAlignment | undefined {
  const s = collapse(value);
  return ST_NodeVerticalAlignment_VALUES.has(s) ? (s as ST_NodeVerticalAlignment) : undefined;
}

export function parseST_Offset(value: string): ST_Offset | undefined {
  const s = collapse(value);
  return ST_Offset_VALUES.has(s) ? (s as ST_Offset) : undefined;
}

export function parseST_OutputShapeType(value: string): ST_OutputShapeType | undefined {
  const s = collapse(value);
  return ST_OutputShapeType_VALUES.has(s) ? (s as ST_OutputShapeType) : undefined;
}

export function parseST_ParameterId(value: string): ST_ParameterId | undefined {
  const s = collapse(value);
  return ST_ParameterId_VALUES.has(s) ? (s as ST_ParameterId) : undefined;
}

export function parseST_ParameterVal(value: string): ST_ParameterVal | undefined {
  return (parseST_DiagramHorizontalAlignment(value) ?? parseST_VerticalAlignment(value) ?? parseST_ChildDirection(value) ?? parseST_ChildAlignment(value) ?? parseST_SecondaryChildAlignment(value) ?? parseST_LinearDirection(value) ?? parseST_SecondaryLinearDirection(value) ?? parseST_StartingElement(value) ?? parseST_BendPoint(value) ?? parseST_ConnectorRouting(value) ?? parseST_ArrowheadStyle(value) ?? parseST_ConnectorDimension(value) ?? parseST_RotationPath(value) ?? parseST_CenterShapeMapping(value) ?? parseST_NodeHorizontalAlignment(value) ?? parseST_NodeVerticalAlignment(value) ?? parseST_FallbackDimension(value) ?? parseST_TextDirection(value) ?? parseST_PyramidAccentPosition(value) ?? parseST_PyramidAccentTextMargin(value) ?? parseST_TextBlockDirection(value) ?? parseST_TextAnchorHorizontal(value) ?? parseST_TextAnchorVertical(value) ?? parseST_DiagramTextAlignment(value) ?? parseST_AutoTextRotation(value) ?? parseST_GrowDirection(value) ?? parseST_FlowDirection(value) ?? parseST_ContinueDirection(value) ?? parseST_Breakpoint(value) ?? parseST_Offset(value) ?? parseST_HierarchyAlignment(value) ?? parseInteger(value) ?? parseDouble(value) ?? parseXsdBoolean(value) ?? (value as ST_ParameterVal) ?? parseST_ConnectorPoint(value)) as ST_ParameterVal | undefined;
}

export function parseST_PrSetCustVal(value: string): ST_PrSetCustVal | undefined {
  return (parseST_Percentage(value) ?? parseInteger(value)) as ST_PrSetCustVal | undefined;
}

export function parseST_PtType(value: string): ST_PtType | undefined {
  const s = collapse(value);
  return ST_PtType_VALUES.has(s) ? (s as ST_PtType) : undefined;
}

export function parseST_PyramidAccentPosition(value: string): ST_PyramidAccentPosition | undefined {
  const s = collapse(value);
  return ST_PyramidAccentPosition_VALUES.has(s) ? (s as ST_PyramidAccentPosition) : undefined;
}

export function parseST_PyramidAccentTextMargin(value: string): ST_PyramidAccentTextMargin | undefined {
  const s = collapse(value);
  return ST_PyramidAccentTextMargin_VALUES.has(s) ? (s as ST_PyramidAccentTextMargin) : undefined;
}

export function parseST_ResizeHandlesStr(value: string): ST_ResizeHandlesStr | undefined {
  const s = collapse(value);
  return ST_ResizeHandlesStr_VALUES.has(s) ? (s as ST_ResizeHandlesStr) : undefined;
}

export function parseST_RotationPath(value: string): ST_RotationPath | undefined {
  const s = collapse(value);
  return ST_RotationPath_VALUES.has(s) ? (s as ST_RotationPath) : undefined;
}

export function parseST_SecondaryChildAlignment(value: string): ST_SecondaryChildAlignment | undefined {
  const s = collapse(value);
  return ST_SecondaryChildAlignment_VALUES.has(s) ? (s as ST_SecondaryChildAlignment) : undefined;
}

export function parseST_SecondaryLinearDirection(value: string): ST_SecondaryLinearDirection | undefined {
  const s = collapse(value);
  return ST_SecondaryLinearDirection_VALUES.has(s) ? (s as ST_SecondaryLinearDirection) : undefined;
}

export function parseST_StartingElement(value: string): ST_StartingElement | undefined {
  const s = collapse(value);
  return ST_StartingElement_VALUES.has(s) ? (s as ST_StartingElement) : undefined;
}

export function parseST_TextAnchorHorizontal(value: string): ST_TextAnchorHorizontal | undefined {
  const s = collapse(value);
  return ST_TextAnchorHorizontal_VALUES.has(s) ? (s as ST_TextAnchorHorizontal) : undefined;
}

export function parseST_TextAnchorVertical(value: string): ST_TextAnchorVertical | undefined {
  const s = collapse(value);
  return ST_TextAnchorVertical_VALUES.has(s) ? (s as ST_TextAnchorVertical) : undefined;
}

export function parseST_TextBlockDirection(value: string): ST_TextBlockDirection | undefined {
  const s = collapse(value);
  return ST_TextBlockDirection_VALUES.has(s) ? (s as ST_TextBlockDirection) : undefined;
}

export function parseST_TextDirection(value: string): ST_TextDirection | undefined {
  const s = collapse(value);
  return ST_TextDirection_VALUES.has(s) ? (s as ST_TextDirection) : undefined;
}

export function parseST_UnsignedInts(value: string): ST_UnsignedInts | undefined {
  return parseList(value, parseInteger) as ST_UnsignedInts | undefined;
}

export function parseST_VariableType(value: string): ST_VariableType | undefined {
  const s = collapse(value);
  return ST_VariableType_VALUES.has(s) ? (s as ST_VariableType) : undefined;
}

export function parseST_VerticalAlignment(value: string): ST_VerticalAlignment | undefined {
  const s = collapse(value);
  return ST_VerticalAlignment_VALUES.has(s) ? (s as ST_VerticalAlignment) : undefined;
}

// Complex-type readers.

/** Read a `CT_Adj`. See `transitional/dml-diagram.xsd:221`. */
export function readCT_Adj(cur: XmlCursor, ctx: ReadContext): CT_Adj {
  const start = requireStart(cur, 'CT_Adj');
  let idx: ST_Index1 | undefined;
  let val: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'idx': {
          const v = parseST_Index1(a.value);
          if (v !== undefined) {
            idx = v;
            continue;
          }
          ctx.invalidValue('CT_Adj', a, 'ST_Index1', cur);
          break;
        }
        case 'val': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Adj', a, 'xsd:double', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Adj', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (idx === undefined) ctx.missingRequired('CT_Adj', 'idx', cur);
  if (val === undefined) ctx.missingRequired('CT_Adj', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Adj', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Adj', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Adj', cur);
    cur.next();
  }
  return {
    idx,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_AdjLst`. See `transitional/dml-diagram.xsd:225`. */
export function readCT_AdjLst(cur: XmlCursor, ctx: ReadContext): CT_AdjLst {
  const start = requireStart(cur, 'CT_AdjLst');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AdjLst', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const adj: CT_Adj[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AdjLst', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'adj': {
            adj.push(readCT_Adj(cur, ctx));
            $slot = 0;
            $index = adj.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AdjLst', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AdjLst', cur);
    cur.next();
  }

  return {
    adj,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Algorithm`. See `transitional/dml-diagram.xsd:247`. */
export function readCT_Algorithm(cur: XmlCursor, ctx: ReadContext): CT_Algorithm {
  const start = requireStart(cur, 'CT_Algorithm');
  let type: ST_AlgorithmType | undefined;
  let rev: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_AlgorithmType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Algorithm', a, 'ST_AlgorithmType', cur);
          break;
        }
        case 'rev': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            rev = v;
            continue;
          }
          ctx.invalidValue('CT_Algorithm', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Algorithm', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (type === undefined) ctx.missingRequired('CT_Algorithm', 'type', cur);

  const param: CT_Parameter[] = [];
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Algorithm', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'param': {
            param.push(readCT_Parameter(cur, ctx));
            $slot = 0;
            $index = param.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Algorithm', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Algorithm', cur);
    cur.next();
  }

  return {
    param,
    extLst,
    type,
    rev,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AnimLvl`. See `transitional/dml-diagram.xsd:496`. */
export function readCT_AnimLvl(cur: XmlCursor, ctx: ReadContext): CT_AnimLvl {
  const start = requireStart(cur, 'CT_AnimLvl');
  let val: ST_AnimLvlStr | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_AnimLvlStr(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_AnimLvl', a, 'ST_AnimLvlStr', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AnimLvl', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AnimLvl', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AnimLvl', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AnimLvl', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_AnimOne`. See `transitional/dml-diagram.xsd:493`. */
export function readCT_AnimOne(cur: XmlCursor, ctx: ReadContext): CT_AnimOne {
  const start = requireStart(cur, 'CT_AnimOne');
  let val: ST_AnimOneStr | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_AnimOneStr(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_AnimOne', a, 'ST_AnimOneStr', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AnimOne', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AnimOne', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AnimOne', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AnimOne', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_BulletEnabled`. See `transitional/dml-diagram.xsd:484`. */
export function readCT_BulletEnabled(cur: XmlCursor, ctx: ReadContext): CT_BulletEnabled {
  const start = requireStart(cur, 'CT_BulletEnabled');
  let val: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_BulletEnabled', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BulletEnabled', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BulletEnabled', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_BulletEnabled', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BulletEnabled', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_CTCategories`. See `transitional/dml-diagram.xsd:27`. */
export function readCT_CTCategories(cur: XmlCursor, ctx: ReadContext): CT_CTCategories {
  const start = requireStart(cur, 'CT_CTCategories');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_CTCategories', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const cat: CT_CTCategory[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CTCategories', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'cat': {
            cat.push(readCT_CTCategory(cur, ctx));
            $slot = 0;
            $index = cat.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CTCategories', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CTCategories', cur);
    cur.next();
  }

  return {
    cat,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CTCategory`. See `transitional/dml-diagram.xsd:23`. */
export function readCT_CTCategory(cur: XmlCursor, ctx: ReadContext): CT_CTCategory {
  const start = requireStart(cur, 'CT_CTCategory');
  let type: string | undefined;
  let pri: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type':
          type = a.value as string;
          continue;

        case 'pri': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            pri = v;
            continue;
          }
          ctx.invalidValue('CT_CTCategory', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CTCategory', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (type === undefined) ctx.missingRequired('CT_CTCategory', 'type', cur);
  if (pri === undefined) ctx.missingRequired('CT_CTCategory', 'pri', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CTCategory', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_CTCategory', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CTCategory', cur);
    cur.next();
  }
  return {
    type,
    pri,
    $unknownAttrs,
  };
}

/** Read a `CT_CTDescription`. See `transitional/dml-diagram.xsd:19`. */
export function readCT_CTDescription(cur: XmlCursor, ctx: ReadContext): CT_CTDescription {
  const start = requireStart(cur, 'CT_CTDescription');
  let lang: string | undefined;
  let val: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'lang':
          lang = a.value as string;
          continue;

        case 'val':
          val = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_CTDescription', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_CTDescription', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CTDescription', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_CTDescription', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CTDescription', cur);
    cur.next();
  }
  return {
    lang,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_CTName`. See `transitional/dml-diagram.xsd:15`. */
export function readCT_CTName(cur: XmlCursor, ctx: ReadContext): CT_CTName {
  const start = requireStart(cur, 'CT_CTName');
  let lang: string | undefined;
  let val: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'lang':
          lang = a.value as string;
          continue;

        case 'val':
          val = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_CTName', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_CTName', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CTName', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_CTName', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CTName', cur);
    cur.next();
  }
  return {
    lang,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_CTStyleLabel`. See `transitional/dml-diagram.xsd:52`. */
export function readCT_CTStyleLabel(cur: XmlCursor, ctx: ReadContext): CT_CTStyleLabel {
  const start = requireStart(cur, 'CT_CTStyleLabel');
  let name: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_CTStyleLabel', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_CTStyleLabel', 'name', cur);

  let fillClrLst: CT_Colors | undefined;
  let linClrLst: CT_Colors | undefined;
  let effectClrLst: CT_Colors | undefined;
  let txLinClrLst: CT_Colors | undefined;
  let txFillClrLst: CT_Colors | undefined;
  let txEffectClrLst: CT_Colors | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CTStyleLabel', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'fillClrLst': {
            fillClrLst = readCT_Colors(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'linClrLst': {
            linClrLst = readCT_Colors(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'effectClrLst': {
            effectClrLst = readCT_Colors(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'txLinClrLst': {
            txLinClrLst = readCT_Colors(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'txFillClrLst': {
            txFillClrLst = readCT_Colors(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'txEffectClrLst': {
            txEffectClrLst = readCT_Colors(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CTStyleLabel', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CTStyleLabel', cur);
    cur.next();
  }

  return {
    fillClrLst,
    linClrLst,
    effectClrLst,
    txLinClrLst,
    txFillClrLst,
    txEffectClrLst,
    extLst,
    name,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Categories`. See `transitional/dml-diagram.xsd:339`. */
export function readCT_Categories(cur: XmlCursor, ctx: ReadContext): CT_Categories {
  const start = requireStart(cur, 'CT_Categories');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Categories', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const cat: CT_Category[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Categories', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'cat': {
            cat.push(readCT_Category(cur, ctx));
            $slot = 0;
            $index = cat.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Categories', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Categories', cur);
    cur.next();
  }

  return {
    cat,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Category`. See `transitional/dml-diagram.xsd:335`. */
export function readCT_Category(cur: XmlCursor, ctx: ReadContext): CT_Category {
  const start = requireStart(cur, 'CT_Category');
  let type: string | undefined;
  let pri: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type':
          type = a.value as string;
          continue;

        case 'pri': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            pri = v;
            continue;
          }
          ctx.invalidValue('CT_Category', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Category', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (type === undefined) ctx.missingRequired('CT_Category', 'type', cur);
  if (pri === undefined) ctx.missingRequired('CT_Category', 'pri', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Category', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Category', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Category', cur);
    cur.next();
  }
  return {
    type,
    pri,
    $unknownAttrs,
  };
}

/** Read a `CT_ChildMax`. See `transitional/dml-diagram.xsd:478`. */
export function readCT_ChildMax(cur: XmlCursor, ctx: ReadContext): CT_ChildMax {
  const start = requireStart(cur, 'CT_ChildMax');
  let val: ST_NodeCount | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_NodeCount(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_ChildMax', a, 'ST_NodeCount', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ChildMax', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ChildMax', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ChildMax', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ChildMax', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_ChildPref`. See `transitional/dml-diagram.xsd:481`. */
export function readCT_ChildPref(cur: XmlCursor, ctx: ReadContext): CT_ChildPref {
  const start = requireStart(cur, 'CT_ChildPref');
  let val: ST_NodeCount | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_NodeCount(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_ChildPref', a, 'ST_NodeCount', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ChildPref', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ChildPref', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ChildPref', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ChildPref', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Choose`. See `transitional/dml-diagram.xsd:322`. */
export function readCT_Choose(cur: XmlCursor, ctx: ReadContext): CT_Choose {
  const start = requireStart(cur, 'CT_Choose');
  let name: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Choose', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const if_: CT_When[] = [];
  let else_: CT_Otherwise | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Choose', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'if': {
            if_.push(readCT_When(cur, ctx));
            $slot = 0;
            $index = if_.length - 1;
            continue;
          }
          case 'else': {
            else_ = readCT_Otherwise(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Choose', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Choose', cur);
    cur.next();
  }

  return {
    if: if_,
    else: else_,
    name,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorTransform`. See `transitional/dml-diagram.xsd:64`. */
export function readCT_ColorTransform(cur: XmlCursor, ctx: ReadContext): CT_ColorTransform {
  const start = requireStart(cur, 'CT_ColorTransform');
  let uniqueId: string | undefined;
  let minVer: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'uniqueId':
          uniqueId = a.value as string;
          continue;

        case 'minVer':
          minVer = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_ColorTransform', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const title: CT_CTName[] = [];
  const desc: CT_CTDescription[] = [];
  let catLst: CT_CTCategories | undefined;
  const styleLbl: CT_CTStyleLabel[] = [];
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorTransform', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'title': {
            title.push(readCT_CTName(cur, ctx));
            $slot = 0;
            $index = title.length - 1;
            continue;
          }
          case 'desc': {
            desc.push(readCT_CTDescription(cur, ctx));
            $slot = 1;
            $index = desc.length - 1;
            continue;
          }
          case 'catLst': {
            catLst = readCT_CTCategories(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'styleLbl': {
            styleLbl.push(readCT_CTStyleLabel(cur, ctx));
            $slot = 3;
            $index = styleLbl.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ColorTransform', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorTransform', cur);
    cur.next();
  }

  return {
    title,
    desc,
    catLst,
    styleLbl,
    extLst,
    uniqueId,
    minVer,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorTransformHeader`. See `transitional/dml-diagram.xsd:76`. */
export function readCT_ColorTransformHeader(cur: XmlCursor, ctx: ReadContext): CT_ColorTransformHeader {
  const start = requireStart(cur, 'CT_ColorTransformHeader');
  let uniqueId: string | undefined;
  let minVer: string | undefined;
  let resId: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'uniqueId':
          uniqueId = a.value as string;
          continue;

        case 'minVer':
          minVer = a.value as string;
          continue;

        case 'resId': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            resId = v;
            continue;
          }
          ctx.invalidValue('CT_ColorTransformHeader', a, 'xsd:int', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ColorTransformHeader', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (uniqueId === undefined) ctx.missingRequired('CT_ColorTransformHeader', 'uniqueId', cur);

  const title: CT_CTName[] = [];
  const desc: CT_CTDescription[] = [];
  let catLst: CT_CTCategories | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorTransformHeader', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'title': {
            title.push(readCT_CTName(cur, ctx));
            $slot = 0;
            $index = title.length - 1;
            continue;
          }
          case 'desc': {
            desc.push(readCT_CTDescription(cur, ctx));
            $slot = 1;
            $index = desc.length - 1;
            continue;
          }
          case 'catLst': {
            catLst = readCT_CTCategories(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ColorTransformHeader', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorTransformHeader', cur);
    cur.next();
  }

  return {
    title,
    desc,
    catLst,
    extLst,
    uniqueId,
    minVer,
    resId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorTransformHeaderLst`. See `transitional/dml-diagram.xsd:88`. */
export function readCT_ColorTransformHeaderLst(cur: XmlCursor, ctx: ReadContext): CT_ColorTransformHeaderLst {
  const start = requireStart(cur, 'CT_ColorTransformHeaderLst');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ColorTransformHeaderLst', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const colorsDefHdr: CT_ColorTransformHeader[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorTransformHeaderLst', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'colorsDefHdr': {
            colorsDefHdr.push(readCT_ColorTransformHeader(cur, ctx));
            $slot = 0;
            $index = colorsDefHdr.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ColorTransformHeaderLst', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorTransformHeaderLst', cur);
    cur.next();
  }

  return {
    colorsDefHdr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Colors`. See `transitional/dml-diagram.xsd:45`. */
export function readCT_Colors(cur: XmlCursor, ctx: ReadContext): CT_Colors {
  const start = requireStart(cur, 'CT_Colors');
  let meth: ST_ClrAppMethod | undefined;
  let hueDir: ST_HueDir | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'meth': {
          const v = parseST_ClrAppMethod(a.value);
          if (v !== undefined) {
            meth = v;
            continue;
          }
          ctx.invalidValue('CT_Colors', a, 'ST_ClrAppMethod', cur);
          break;
        }
        case 'hueDir': {
          const v = parseST_HueDir(a.value);
          if (v !== undefined) {
            hueDir = v;
            continue;
          }
          ctx.invalidValue('CT_Colors', a, 'ST_HueDir', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Colors', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const colorChoice: CT_Colors_ColorChoice[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Colors', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice.push({ kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) });
            continue;
          }
          case 'srgbClr': {
            colorChoice.push({ kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) });
            continue;
          }
          case 'hslClr': {
            colorChoice.push({ kind: 'hslClr', value: readCT_HslColor(cur, ctx) });
            continue;
          }
          case 'sysClr': {
            colorChoice.push({ kind: 'sysClr', value: readCT_SystemColor(cur, ctx) });
            continue;
          }
          case 'schemeClr': {
            colorChoice.push({ kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) });
            continue;
          }
          case 'prstClr': {
            colorChoice.push({ kind: 'prstClr', value: readCT_PresetColor(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Colors', ev.localName, ev.uri, cur);
      colorChoice.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Colors', cur);
    cur.next();
  }

  return {
    colorChoice,
    meth,
    hueDir,
    $unknownAttrs,
  };
}

/** Read a `CT_Constraint`. See `transitional/dml-diagram.xsd:178`. */
export function readCT_Constraint(cur: XmlCursor, ctx: ReadContext): CT_Constraint {
  const start = requireStart(cur, 'CT_Constraint');
  let type: ST_ConstraintType | undefined;
  let for_: ST_ConstraintRelationship | undefined;
  let forName: string | undefined;
  let ptType: ST_ElementType | undefined;
  let refType: ST_ConstraintType | undefined;
  let refFor: ST_ConstraintRelationship | undefined;
  let refForName: string | undefined;
  let refPtType: ST_ElementType | undefined;
  let op: ST_BoolOperator | undefined;
  let val: number | undefined;
  let fact: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_ConstraintType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Constraint', a, 'ST_ConstraintType', cur);
          break;
        }
        case 'for': {
          const v = parseST_ConstraintRelationship(a.value);
          if (v !== undefined) {
            for_ = v;
            continue;
          }
          ctx.invalidValue('CT_Constraint', a, 'ST_ConstraintRelationship', cur);
          break;
        }
        case 'forName':
          forName = a.value as string;
          continue;

        case 'ptType': {
          const v = parseST_ElementType(a.value);
          if (v !== undefined) {
            ptType = v;
            continue;
          }
          ctx.invalidValue('CT_Constraint', a, 'ST_ElementType', cur);
          break;
        }
        case 'refType': {
          const v = parseST_ConstraintType(a.value);
          if (v !== undefined) {
            refType = v;
            continue;
          }
          ctx.invalidValue('CT_Constraint', a, 'ST_ConstraintType', cur);
          break;
        }
        case 'refFor': {
          const v = parseST_ConstraintRelationship(a.value);
          if (v !== undefined) {
            refFor = v;
            continue;
          }
          ctx.invalidValue('CT_Constraint', a, 'ST_ConstraintRelationship', cur);
          break;
        }
        case 'refForName':
          refForName = a.value as string;
          continue;

        case 'refPtType': {
          const v = parseST_ElementType(a.value);
          if (v !== undefined) {
            refPtType = v;
            continue;
          }
          ctx.invalidValue('CT_Constraint', a, 'ST_ElementType', cur);
          break;
        }
        case 'op': {
          const v = parseST_BoolOperator(a.value);
          if (v !== undefined) {
            op = v;
            continue;
          }
          ctx.invalidValue('CT_Constraint', a, 'ST_BoolOperator', cur);
          break;
        }
        case 'val': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Constraint', a, 'xsd:double', cur);
          break;
        }
        case 'fact': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            fact = v;
            continue;
          }
          ctx.invalidValue('CT_Constraint', a, 'xsd:double', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Constraint', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (type === undefined) ctx.missingRequired('CT_Constraint', 'type', cur);

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Constraint', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Constraint', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Constraint', cur);
    cur.next();
  }

  return {
    extLst,
    type,
    for: for_,
    forName,
    ptType,
    refType,
    refFor,
    refForName,
    refPtType,
    op,
    val,
    fact,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Constraints`. See `transitional/dml-diagram.xsd:188`. */
export function readCT_Constraints(cur: XmlCursor, ctx: ReadContext): CT_Constraints {
  const start = requireStart(cur, 'CT_Constraints');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Constraints', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const constr: CT_Constraint[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Constraints', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'constr': {
            constr.push(readCT_Constraint(cur, ctx));
            $slot = 0;
            $index = constr.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Constraints', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Constraints', cur);
    cur.next();
  }

  return {
    constr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Cxn`. See `transitional/dml-diagram.xsd:129`. */
export function readCT_Cxn(cur: XmlCursor, ctx: ReadContext): CT_Cxn {
  const start = requireStart(cur, 'CT_Cxn');
  let modelId: ST_ModelId | undefined;
  let type: ST_CxnType | undefined;
  let srcId: ST_ModelId | undefined;
  let destId: ST_ModelId | undefined;
  let srcOrd: number | undefined;
  let destOrd: number | undefined;
  let parTransId: ST_ModelId | undefined;
  let sibTransId: ST_ModelId | undefined;
  let presId: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'modelId': {
          const v = parseST_ModelId(a.value);
          if (v !== undefined) {
            modelId = v;
            continue;
          }
          ctx.invalidValue('CT_Cxn', a, 'ST_ModelId', cur);
          break;
        }
        case 'type': {
          const v = parseST_CxnType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Cxn', a, 'ST_CxnType', cur);
          break;
        }
        case 'srcId': {
          const v = parseST_ModelId(a.value);
          if (v !== undefined) {
            srcId = v;
            continue;
          }
          ctx.invalidValue('CT_Cxn', a, 'ST_ModelId', cur);
          break;
        }
        case 'destId': {
          const v = parseST_ModelId(a.value);
          if (v !== undefined) {
            destId = v;
            continue;
          }
          ctx.invalidValue('CT_Cxn', a, 'ST_ModelId', cur);
          break;
        }
        case 'srcOrd': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            srcOrd = v;
            continue;
          }
          ctx.invalidValue('CT_Cxn', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'destOrd': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            destOrd = v;
            continue;
          }
          ctx.invalidValue('CT_Cxn', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'parTransId': {
          const v = parseST_ModelId(a.value);
          if (v !== undefined) {
            parTransId = v;
            continue;
          }
          ctx.invalidValue('CT_Cxn', a, 'ST_ModelId', cur);
          break;
        }
        case 'sibTransId': {
          const v = parseST_ModelId(a.value);
          if (v !== undefined) {
            sibTransId = v;
            continue;
          }
          ctx.invalidValue('CT_Cxn', a, 'ST_ModelId', cur);
          break;
        }
        case 'presId':
          presId = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Cxn', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (modelId === undefined) ctx.missingRequired('CT_Cxn', 'modelId', cur);
  if (srcId === undefined) ctx.missingRequired('CT_Cxn', 'srcId', cur);
  if (destId === undefined) ctx.missingRequired('CT_Cxn', 'destId', cur);
  if (srcOrd === undefined) ctx.missingRequired('CT_Cxn', 'srcOrd', cur);
  if (destOrd === undefined) ctx.missingRequired('CT_Cxn', 'destOrd', cur);

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Cxn', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Cxn', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Cxn', cur);
    cur.next();
  }

  return {
    extLst,
    modelId,
    type,
    srcId,
    destId,
    srcOrd,
    destOrd,
    parTransId,
    sibTransId,
    presId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CxnList`. See `transitional/dml-diagram.xsd:143`. */
export function readCT_CxnList(cur: XmlCursor, ctx: ReadContext): CT_CxnList {
  const start = requireStart(cur, 'CT_CxnList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_CxnList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const cxn: CT_Cxn[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CxnList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'cxn': {
            cxn.push(readCT_Cxn(cur, ctx));
            $slot = 0;
            $index = cxn.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CxnList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CxnList', cur);
    cur.next();
  }

  return {
    cxn,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DataModel`. See `transitional/dml-diagram.xsd:148`. */
export function readCT_DataModel(cur: XmlCursor, ctx: ReadContext): CT_DataModel {
  const start = requireStart(cur, 'CT_DataModel');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DataModel', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ptLst: CT_PtList | undefined;
  let cxnLst: CT_CxnList | undefined;
  let bg: CT_BackgroundFormatting | undefined;
  let whole: CT_WholeE2oFormatting | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DataModel', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'ptLst': {
            ptLst = readCT_PtList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cxnLst': {
            cxnLst = readCT_CxnList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'bg': {
            bg = readCT_BackgroundFormatting(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'whole': {
            whole = readCT_WholeE2oFormatting(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DataModel', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DataModel', cur);
    cur.next();
  }

  return {
    ptLst,
    cxnLst,
    bg,
    whole,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Description`. See `transitional/dml-diagram.xsd:348`. */
export function readCT_Description(cur: XmlCursor, ctx: ReadContext): CT_Description {
  const start = requireStart(cur, 'CT_Description');
  let lang: string | undefined;
  let val: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'lang':
          lang = a.value as string;
          continue;

        case 'val':
          val = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Description', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Description', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Description', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Description', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Description', cur);
    cur.next();
  }
  return {
    lang,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_DiagramDefinition`. See `transitional/dml-diagram.xsd:352`. */
export function readCT_DiagramDefinition(cur: XmlCursor, ctx: ReadContext): CT_DiagramDefinition {
  const start = requireStart(cur, 'CT_DiagramDefinition');
  let uniqueId: string | undefined;
  let minVer: string | undefined;
  let defStyle: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'uniqueId':
          uniqueId = a.value as string;
          continue;

        case 'minVer':
          minVer = a.value as string;
          continue;

        case 'defStyle':
          defStyle = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_DiagramDefinition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const title: CT_Name[] = [];
  const desc: CT_Description[] = [];
  let catLst: CT_Categories | undefined;
  let sampData: CT_SampleData | undefined;
  let styleData: CT_SampleData | undefined;
  let clrData: CT_SampleData | undefined;
  let layoutNode: CT_LayoutNode | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DiagramDefinition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'title': {
            title.push(readCT_Name(cur, ctx));
            $slot = 0;
            $index = title.length - 1;
            continue;
          }
          case 'desc': {
            desc.push(readCT_Description(cur, ctx));
            $slot = 1;
            $index = desc.length - 1;
            continue;
          }
          case 'catLst': {
            catLst = readCT_Categories(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'sampData': {
            sampData = readCT_SampleData(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'styleData': {
            styleData = readCT_SampleData(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'clrData': {
            clrData = readCT_SampleData(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'layoutNode': {
            layoutNode = readCT_LayoutNode(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DiagramDefinition', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DiagramDefinition', cur);
    cur.next();
  }

  return {
    title,
    desc,
    catLst,
    sampData,
    styleData,
    clrData,
    layoutNode,
    extLst,
    uniqueId,
    minVer,
    defStyle,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DiagramDefinitionHeader`. See `transitional/dml-diagram.xsd:368`. */
export function readCT_DiagramDefinitionHeader(cur: XmlCursor, ctx: ReadContext): CT_DiagramDefinitionHeader {
  const start = requireStart(cur, 'CT_DiagramDefinitionHeader');
  let uniqueId: string | undefined;
  let minVer: string | undefined;
  let defStyle: string | undefined;
  let resId: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'uniqueId':
          uniqueId = a.value as string;
          continue;

        case 'minVer':
          minVer = a.value as string;
          continue;

        case 'defStyle':
          defStyle = a.value as string;
          continue;

        case 'resId': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            resId = v;
            continue;
          }
          ctx.invalidValue('CT_DiagramDefinitionHeader', a, 'xsd:int', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DiagramDefinitionHeader', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (uniqueId === undefined) ctx.missingRequired('CT_DiagramDefinitionHeader', 'uniqueId', cur);

  const title: CT_Name[] = [];
  const desc: CT_Description[] = [];
  let catLst: CT_Categories | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DiagramDefinitionHeader', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'title': {
            title.push(readCT_Name(cur, ctx));
            $slot = 0;
            $index = title.length - 1;
            continue;
          }
          case 'desc': {
            desc.push(readCT_Description(cur, ctx));
            $slot = 1;
            $index = desc.length - 1;
            continue;
          }
          case 'catLst': {
            catLst = readCT_Categories(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DiagramDefinitionHeader', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DiagramDefinitionHeader', cur);
    cur.next();
  }

  return {
    title,
    desc,
    catLst,
    extLst,
    uniqueId,
    minVer,
    defStyle,
    resId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DiagramDefinitionHeaderLst`. See `transitional/dml-diagram.xsd:381`. */
export function readCT_DiagramDefinitionHeaderLst(cur: XmlCursor, ctx: ReadContext): CT_DiagramDefinitionHeaderLst {
  const start = requireStart(cur, 'CT_DiagramDefinitionHeaderLst');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DiagramDefinitionHeaderLst', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const layoutDefHdr: CT_DiagramDefinitionHeader[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DiagramDefinitionHeaderLst', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'layoutDefHdr': {
            layoutDefHdr.push(readCT_DiagramDefinitionHeader(cur, ctx));
            $slot = 0;
            $index = layoutDefHdr.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DiagramDefinitionHeaderLst', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DiagramDefinitionHeaderLst', cur);
    cur.next();
  }

  return {
    layoutDefHdr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Direction`. See `transitional/dml-diagram.xsd:487`. */
export function readCT_Direction(cur: XmlCursor, ctx: ReadContext): CT_Direction {
  const start = requireStart(cur, 'CT_Direction');
  let val: ST_Direction | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Direction(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Direction', a, 'ST_Direction', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Direction', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Direction', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Direction', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Direction', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_ElemPropSet`. See `transitional/dml-diagram.xsd:406`. */
export function readCT_ElemPropSet(cur: XmlCursor, ctx: ReadContext): CT_ElemPropSet {
  const start = requireStart(cur, 'CT_ElemPropSet');
  let presAssocID: ST_ModelId | undefined;
  let presName: string | undefined;
  let presStyleLbl: string | undefined;
  let presStyleIdx: number | undefined;
  let presStyleCnt: number | undefined;
  let loTypeId: string | undefined;
  let loCatId: string | undefined;
  let qsTypeId: string | undefined;
  let qsCatId: string | undefined;
  let csTypeId: string | undefined;
  let csCatId: string | undefined;
  let coherent3DOff: boolean | undefined;
  let phldrT: string | undefined;
  let phldr: boolean | undefined;
  let custAng: number | undefined;
  let custFlipVert: boolean | undefined;
  let custFlipHor: boolean | undefined;
  let custSzX: number | undefined;
  let custSzY: number | undefined;
  let custScaleX: ST_PrSetCustVal | undefined;
  let custScaleY: ST_PrSetCustVal | undefined;
  let custT: boolean | undefined;
  let custLinFactX: ST_PrSetCustVal | undefined;
  let custLinFactY: ST_PrSetCustVal | undefined;
  let custLinFactNeighborX: ST_PrSetCustVal | undefined;
  let custLinFactNeighborY: ST_PrSetCustVal | undefined;
  let custRadScaleRad: ST_PrSetCustVal | undefined;
  let custRadScaleInc: ST_PrSetCustVal | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'presAssocID': {
          const v = parseST_ModelId(a.value);
          if (v !== undefined) {
            presAssocID = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'ST_ModelId', cur);
          break;
        }
        case 'presName':
          presName = a.value as string;
          continue;

        case 'presStyleLbl':
          presStyleLbl = a.value as string;
          continue;

        case 'presStyleIdx': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            presStyleIdx = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'xsd:int', cur);
          break;
        }
        case 'presStyleCnt': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            presStyleCnt = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'xsd:int', cur);
          break;
        }
        case 'loTypeId':
          loTypeId = a.value as string;
          continue;

        case 'loCatId':
          loCatId = a.value as string;
          continue;

        case 'qsTypeId':
          qsTypeId = a.value as string;
          continue;

        case 'qsCatId':
          qsCatId = a.value as string;
          continue;

        case 'csTypeId':
          csTypeId = a.value as string;
          continue;

        case 'csCatId':
          csCatId = a.value as string;
          continue;

        case 'coherent3DOff': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            coherent3DOff = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'xsd:boolean', cur);
          break;
        }
        case 'phldrT':
          phldrT = a.value as string;
          continue;

        case 'phldr': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            phldr = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'xsd:boolean', cur);
          break;
        }
        case 'custAng': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            custAng = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'xsd:int', cur);
          break;
        }
        case 'custFlipVert': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            custFlipVert = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'xsd:boolean', cur);
          break;
        }
        case 'custFlipHor': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            custFlipHor = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'xsd:boolean', cur);
          break;
        }
        case 'custSzX': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            custSzX = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'xsd:int', cur);
          break;
        }
        case 'custSzY': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            custSzY = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'xsd:int', cur);
          break;
        }
        case 'custScaleX': {
          const v = parseST_PrSetCustVal(a.value);
          if (v !== undefined) {
            custScaleX = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'ST_PrSetCustVal', cur);
          break;
        }
        case 'custScaleY': {
          const v = parseST_PrSetCustVal(a.value);
          if (v !== undefined) {
            custScaleY = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'ST_PrSetCustVal', cur);
          break;
        }
        case 'custT': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            custT = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'xsd:boolean', cur);
          break;
        }
        case 'custLinFactX': {
          const v = parseST_PrSetCustVal(a.value);
          if (v !== undefined) {
            custLinFactX = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'ST_PrSetCustVal', cur);
          break;
        }
        case 'custLinFactY': {
          const v = parseST_PrSetCustVal(a.value);
          if (v !== undefined) {
            custLinFactY = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'ST_PrSetCustVal', cur);
          break;
        }
        case 'custLinFactNeighborX': {
          const v = parseST_PrSetCustVal(a.value);
          if (v !== undefined) {
            custLinFactNeighborX = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'ST_PrSetCustVal', cur);
          break;
        }
        case 'custLinFactNeighborY': {
          const v = parseST_PrSetCustVal(a.value);
          if (v !== undefined) {
            custLinFactNeighborY = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'ST_PrSetCustVal', cur);
          break;
        }
        case 'custRadScaleRad': {
          const v = parseST_PrSetCustVal(a.value);
          if (v !== undefined) {
            custRadScaleRad = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'ST_PrSetCustVal', cur);
          break;
        }
        case 'custRadScaleInc': {
          const v = parseST_PrSetCustVal(a.value);
          if (v !== undefined) {
            custRadScaleInc = v;
            continue;
          }
          ctx.invalidValue('CT_ElemPropSet', a, 'ST_PrSetCustVal', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ElemPropSet', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let presLayoutVars: CT_LayoutVariablePropertySet | undefined;
  let style: CT_ShapeStyle | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ElemPropSet', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'presLayoutVars': {
            presLayoutVars = readCT_LayoutVariablePropertySet(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ElemPropSet', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ElemPropSet', cur);
    cur.next();
  }

  return {
    presLayoutVars,
    style,
    presAssocID,
    presName,
    presStyleLbl,
    presStyleIdx,
    presStyleCnt,
    loTypeId,
    loCatId,
    qsTypeId,
    qsCatId,
    csTypeId,
    csCatId,
    coherent3DOff,
    phldrT,
    phldr,
    custAng,
    custFlipVert,
    custFlipHor,
    custSzX,
    custSzY,
    custScaleX,
    custScaleY,
    custT,
    custLinFactX,
    custLinFactY,
    custLinFactNeighborX,
    custLinFactNeighborY,
    custRadScaleRad,
    custRadScaleInc,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ForEach`. See `transitional/dml-diagram.xsd:273`. */
export function readCT_ForEach(cur: XmlCursor, ctx: ReadContext): CT_ForEach {
  const start = requireStart(cur, 'CT_ForEach');
  let name: string | undefined;
  let ref: string | undefined;
  let axis: ST_AxisTypes | undefined;
  let ptType: ST_ElementTypes | undefined;
  let hideLastTrans: ST_Booleans | undefined;
  let st: ST_Ints | undefined;
  let cnt: ST_UnsignedInts | undefined;
  let step: ST_Ints | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

        case 'ref':
          ref = a.value as string;
          continue;

        case 'axis': {
          const v = parseST_AxisTypes(a.value);
          if (v !== undefined) {
            axis = v;
            continue;
          }
          ctx.invalidValue('CT_ForEach', a, 'ST_AxisTypes', cur);
          break;
        }
        case 'ptType': {
          const v = parseST_ElementTypes(a.value);
          if (v !== undefined) {
            ptType = v;
            continue;
          }
          ctx.invalidValue('CT_ForEach', a, 'ST_ElementTypes', cur);
          break;
        }
        case 'hideLastTrans': {
          const v = parseST_Booleans(a.value);
          if (v !== undefined) {
            hideLastTrans = v;
            continue;
          }
          ctx.invalidValue('CT_ForEach', a, 'ST_Booleans', cur);
          break;
        }
        case 'st': {
          const v = parseST_Ints(a.value);
          if (v !== undefined) {
            st = v;
            continue;
          }
          ctx.invalidValue('CT_ForEach', a, 'ST_Ints', cur);
          break;
        }
        case 'cnt': {
          const v = parseST_UnsignedInts(a.value);
          if (v !== undefined) {
            cnt = v;
            continue;
          }
          ctx.invalidValue('CT_ForEach', a, 'ST_UnsignedInts', cur);
          break;
        }
        case 'step': {
          const v = parseST_Ints(a.value);
          if (v !== undefined) {
            step = v;
            continue;
          }
          ctx.invalidValue('CT_ForEach', a, 'ST_Ints', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ForEach', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_ForEach_Content[] = [];

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ForEach', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'alg': {
            content.push({ kind: 'alg', value: readCT_Algorithm(cur, ctx) });
            continue;
          }
          case 'shape': {
            content.push({ kind: 'shape', value: readCT_Shape(cur, ctx) });
            continue;
          }
          case 'presOf': {
            content.push({ kind: 'presOf', value: readCT_PresentationOf(cur, ctx) });
            continue;
          }
          case 'constrLst': {
            content.push({ kind: 'constrLst', value: readCT_Constraints(cur, ctx) });
            continue;
          }
          case 'ruleLst': {
            content.push({ kind: 'ruleLst', value: readCT_Rules(cur, ctx) });
            continue;
          }
          case 'forEach': {
            content.push({ kind: 'forEach', value: readCT_ForEach(cur, ctx) });
            continue;
          }
          case 'layoutNode': {
            content.push({ kind: 'layoutNode', value: readCT_LayoutNode(cur, ctx) });
            continue;
          }
          case 'choose': {
            content.push({ kind: 'choose', value: readCT_Choose(cur, ctx) });
            continue;
          }
          case 'extLst': {
            content.push({ kind: 'extLst', value: readCT_OfficeArtExtensionList(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ForEach', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ForEach', cur);
    cur.next();
  }

  return {
    content,
    name,
    ref,
    axis,
    ptType,
    hideLastTrans,
    st,
    cnt,
    step,
    $unknownAttrs,
  };
}

/** Read a `CT_HierBranchStyle`. See `transitional/dml-diagram.xsd:490`. */
export function readCT_HierBranchStyle(cur: XmlCursor, ctx: ReadContext): CT_HierBranchStyle {
  const start = requireStart(cur, 'CT_HierBranchStyle');
  let val: ST_HierBranchStyle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_HierBranchStyle(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_HierBranchStyle', a, 'ST_HierBranchStyle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_HierBranchStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HierBranchStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_HierBranchStyle', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HierBranchStyle', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_LayoutNode`. See `transitional/dml-diagram.xsd:255`. */
export function readCT_LayoutNode(cur: XmlCursor, ctx: ReadContext): CT_LayoutNode {
  const start = requireStart(cur, 'CT_LayoutNode');
  let name: string | undefined;
  let styleLbl: string | undefined;
  let chOrder: ST_ChildOrderType | undefined;
  let moveWith: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

        case 'styleLbl':
          styleLbl = a.value as string;
          continue;

        case 'chOrder': {
          const v = parseST_ChildOrderType(a.value);
          if (v !== undefined) {
            chOrder = v;
            continue;
          }
          ctx.invalidValue('CT_LayoutNode', a, 'ST_ChildOrderType', cur);
          break;
        }
        case 'moveWith':
          moveWith = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_LayoutNode', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_LayoutNode_Content[] = [];

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LayoutNode', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'alg': {
            content.push({ kind: 'alg', value: readCT_Algorithm(cur, ctx) });
            continue;
          }
          case 'shape': {
            content.push({ kind: 'shape', value: readCT_Shape(cur, ctx) });
            continue;
          }
          case 'presOf': {
            content.push({ kind: 'presOf', value: readCT_PresentationOf(cur, ctx) });
            continue;
          }
          case 'constrLst': {
            content.push({ kind: 'constrLst', value: readCT_Constraints(cur, ctx) });
            continue;
          }
          case 'ruleLst': {
            content.push({ kind: 'ruleLst', value: readCT_Rules(cur, ctx) });
            continue;
          }
          case 'varLst': {
            content.push({ kind: 'varLst', value: readCT_LayoutVariablePropertySet(cur, ctx) });
            continue;
          }
          case 'forEach': {
            content.push({ kind: 'forEach', value: readCT_ForEach(cur, ctx) });
            continue;
          }
          case 'layoutNode': {
            content.push({ kind: 'layoutNode', value: readCT_LayoutNode(cur, ctx) });
            continue;
          }
          case 'choose': {
            content.push({ kind: 'choose', value: readCT_Choose(cur, ctx) });
            continue;
          }
          case 'extLst': {
            content.push({ kind: 'extLst', value: readCT_OfficeArtExtensionList(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LayoutNode', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LayoutNode', cur);
    cur.next();
  }

  return {
    content,
    name,
    styleLbl,
    chOrder,
    moveWith,
    $unknownAttrs,
  };
}

/** Read a `CT_LayoutVariablePropertySet`. See `transitional/dml-diagram.xsd:508`. */
export function readCT_LayoutVariablePropertySet(cur: XmlCursor, ctx: ReadContext): CT_LayoutVariablePropertySet {
  const start = requireStart(cur, 'CT_LayoutVariablePropertySet');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_LayoutVariablePropertySet', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let orgChart: CT_OrgChart | undefined;
  let chMax: CT_ChildMax | undefined;
  let chPref: CT_ChildPref | undefined;
  let bulletEnabled: CT_BulletEnabled | undefined;
  let dir: CT_Direction | undefined;
  let hierBranch: CT_HierBranchStyle | undefined;
  let animOne: CT_AnimOne | undefined;
  let animLvl: CT_AnimLvl | undefined;
  let resizeHandles: CT_ResizeHandles | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LayoutVariablePropertySet', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'orgChart': {
            orgChart = readCT_OrgChart(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'chMax': {
            chMax = readCT_ChildMax(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'chPref': {
            chPref = readCT_ChildPref(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'bulletEnabled': {
            bulletEnabled = readCT_BulletEnabled(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'dir': {
            dir = readCT_Direction(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'hierBranch': {
            hierBranch = readCT_HierBranchStyle(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'animOne': {
            animOne = readCT_AnimOne(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'animLvl': {
            animLvl = readCT_AnimLvl(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'resizeHandles': {
            resizeHandles = readCT_ResizeHandles(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LayoutVariablePropertySet', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LayoutVariablePropertySet', cur);
    cur.next();
  }

  return {
    orgChart,
    chMax,
    chPref,
    bulletEnabled,
    dir,
    hierBranch,
    animOne,
    animLvl,
    resizeHandles,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Name`. See `transitional/dml-diagram.xsd:344`. */
export function readCT_Name(cur: XmlCursor, ctx: ReadContext): CT_Name {
  const start = requireStart(cur, 'CT_Name');
  let lang: string | undefined;
  let val: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'lang':
          lang = a.value as string;
          continue;

        case 'val':
          val = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Name', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Name', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Name', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Name', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Name', cur);
    cur.next();
  }
  return {
    lang,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_NumericRule`. See `transitional/dml-diagram.xsd:193`. */
export function readCT_NumericRule(cur: XmlCursor, ctx: ReadContext): CT_NumericRule {
  const start = requireStart(cur, 'CT_NumericRule');
  let type: ST_ConstraintType | undefined;
  let for_: ST_ConstraintRelationship | undefined;
  let forName: string | undefined;
  let ptType: ST_ElementType | undefined;
  let val: number | undefined;
  let fact: number | undefined;
  let max: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_ConstraintType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_NumericRule', a, 'ST_ConstraintType', cur);
          break;
        }
        case 'for': {
          const v = parseST_ConstraintRelationship(a.value);
          if (v !== undefined) {
            for_ = v;
            continue;
          }
          ctx.invalidValue('CT_NumericRule', a, 'ST_ConstraintRelationship', cur);
          break;
        }
        case 'forName':
          forName = a.value as string;
          continue;

        case 'ptType': {
          const v = parseST_ElementType(a.value);
          if (v !== undefined) {
            ptType = v;
            continue;
          }
          ctx.invalidValue('CT_NumericRule', a, 'ST_ElementType', cur);
          break;
        }
        case 'val': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_NumericRule', a, 'xsd:double', cur);
          break;
        }
        case 'fact': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            fact = v;
            continue;
          }
          ctx.invalidValue('CT_NumericRule', a, 'xsd:double', cur);
          break;
        }
        case 'max': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            max = v;
            continue;
          }
          ctx.invalidValue('CT_NumericRule', a, 'xsd:double', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NumericRule', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (type === undefined) ctx.missingRequired('CT_NumericRule', 'type', cur);

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NumericRule', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NumericRule', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NumericRule', cur);
    cur.next();
  }

  return {
    extLst,
    type,
    for: for_,
    forName,
    ptType,
    val,
    fact,
    max,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OrgChart`. See `transitional/dml-diagram.xsd:470`. */
export function readCT_OrgChart(cur: XmlCursor, ctx: ReadContext): CT_OrgChart {
  const start = requireStart(cur, 'CT_OrgChart');
  let val: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_OrgChart', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OrgChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OrgChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_OrgChart', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OrgChart', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Otherwise`. See `transitional/dml-diagram.xsd:308`. */
export function readCT_Otherwise(cur: XmlCursor, ctx: ReadContext): CT_Otherwise {
  const start = requireStart(cur, 'CT_Otherwise');
  let name: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Otherwise', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_Otherwise_Content[] = [];

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Otherwise', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'alg': {
            content.push({ kind: 'alg', value: readCT_Algorithm(cur, ctx) });
            continue;
          }
          case 'shape': {
            content.push({ kind: 'shape', value: readCT_Shape(cur, ctx) });
            continue;
          }
          case 'presOf': {
            content.push({ kind: 'presOf', value: readCT_PresentationOf(cur, ctx) });
            continue;
          }
          case 'constrLst': {
            content.push({ kind: 'constrLst', value: readCT_Constraints(cur, ctx) });
            continue;
          }
          case 'ruleLst': {
            content.push({ kind: 'ruleLst', value: readCT_Rules(cur, ctx) });
            continue;
          }
          case 'forEach': {
            content.push({ kind: 'forEach', value: readCT_ForEach(cur, ctx) });
            continue;
          }
          case 'layoutNode': {
            content.push({ kind: 'layoutNode', value: readCT_LayoutNode(cur, ctx) });
            continue;
          }
          case 'choose': {
            content.push({ kind: 'choose', value: readCT_Choose(cur, ctx) });
            continue;
          }
          case 'extLst': {
            content.push({ kind: 'extLst', value: readCT_OfficeArtExtensionList(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Otherwise', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Otherwise', cur);
    cur.next();
  }

  return {
    content,
    name,
    $unknownAttrs,
  };
}

/** Read a `CT_Parameter`. See `transitional/dml-diagram.xsd:243`. */
export function readCT_Parameter(cur: XmlCursor, ctx: ReadContext): CT_Parameter {
  const start = requireStart(cur, 'CT_Parameter');
  let type: ST_ParameterId | undefined;
  let val: ST_ParameterVal | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_ParameterId(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Parameter', a, 'ST_ParameterId', cur);
          break;
        }
        case 'val': {
          const v = parseST_ParameterVal(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Parameter', a, 'ST_ParameterVal', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Parameter', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (type === undefined) ctx.missingRequired('CT_Parameter', 'type', cur);
  if (val === undefined) ctx.missingRequired('CT_Parameter', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Parameter', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Parameter', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Parameter', cur);
    cur.next();
  }
  return {
    type,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_PresentationOf`. See `transitional/dml-diagram.xsd:207`. */
export function readCT_PresentationOf(cur: XmlCursor, ctx: ReadContext): CT_PresentationOf {
  const start = requireStart(cur, 'CT_PresentationOf');
  let axis: ST_AxisTypes | undefined;
  let ptType: ST_ElementTypes | undefined;
  let hideLastTrans: ST_Booleans | undefined;
  let st: ST_Ints | undefined;
  let cnt: ST_UnsignedInts | undefined;
  let step: ST_Ints | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'axis': {
          const v = parseST_AxisTypes(a.value);
          if (v !== undefined) {
            axis = v;
            continue;
          }
          ctx.invalidValue('CT_PresentationOf', a, 'ST_AxisTypes', cur);
          break;
        }
        case 'ptType': {
          const v = parseST_ElementTypes(a.value);
          if (v !== undefined) {
            ptType = v;
            continue;
          }
          ctx.invalidValue('CT_PresentationOf', a, 'ST_ElementTypes', cur);
          break;
        }
        case 'hideLastTrans': {
          const v = parseST_Booleans(a.value);
          if (v !== undefined) {
            hideLastTrans = v;
            continue;
          }
          ctx.invalidValue('CT_PresentationOf', a, 'ST_Booleans', cur);
          break;
        }
        case 'st': {
          const v = parseST_Ints(a.value);
          if (v !== undefined) {
            st = v;
            continue;
          }
          ctx.invalidValue('CT_PresentationOf', a, 'ST_Ints', cur);
          break;
        }
        case 'cnt': {
          const v = parseST_UnsignedInts(a.value);
          if (v !== undefined) {
            cnt = v;
            continue;
          }
          ctx.invalidValue('CT_PresentationOf', a, 'ST_UnsignedInts', cur);
          break;
        }
        case 'step': {
          const v = parseST_Ints(a.value);
          if (v !== undefined) {
            step = v;
            continue;
          }
          ctx.invalidValue('CT_PresentationOf', a, 'ST_Ints', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PresentationOf', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PresentationOf', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PresentationOf', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PresentationOf', cur);
    cur.next();
  }

  return {
    extLst,
    axis,
    ptType,
    hideLastTrans,
    st,
    cnt,
    step,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Pt`. See `transitional/dml-diagram.xsd:105`. */
export function readCT_Pt(cur: XmlCursor, ctx: ReadContext): CT_Pt {
  const start = requireStart(cur, 'CT_Pt');
  let modelId: ST_ModelId | undefined;
  let type: ST_PtType | undefined;
  let cxnId: ST_ModelId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'modelId': {
          const v = parseST_ModelId(a.value);
          if (v !== undefined) {
            modelId = v;
            continue;
          }
          ctx.invalidValue('CT_Pt', a, 'ST_ModelId', cur);
          break;
        }
        case 'type': {
          const v = parseST_PtType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Pt', a, 'ST_PtType', cur);
          break;
        }
        case 'cxnId': {
          const v = parseST_ModelId(a.value);
          if (v !== undefined) {
            cxnId = v;
            continue;
          }
          ctx.invalidValue('CT_Pt', a, 'ST_ModelId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Pt', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (modelId === undefined) ctx.missingRequired('CT_Pt', 'modelId', cur);

  let prSet: CT_ElemPropSet | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let t: CT_TextBody | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Pt', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'prSet': {
            prSet = readCT_ElemPropSet(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 't': {
            t = readCT_TextBody(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Pt', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Pt', cur);
    cur.next();
  }

  return {
    prSet,
    spPr,
    t,
    extLst,
    modelId,
    type,
    cxnId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PtList`. See `transitional/dml-diagram.xsd:116`. */
export function readCT_PtList(cur: XmlCursor, ctx: ReadContext): CT_PtList {
  const start = requireStart(cur, 'CT_PtList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PtList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const pt: CT_Pt[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PtList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'pt': {
            pt.push(readCT_Pt(cur, ctx));
            $slot = 0;
            $index = pt.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PtList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PtList', cur);
    cur.next();
  }

  return {
    pt,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_RelIds`. See `transitional/dml-diagram.xsd:388`. */
export function readCT_RelIds(cur: XmlCursor, ctx: ReadContext): CT_RelIds {
  const start = requireStart(cur, 'CT_RelIds');
  let dm: ST_RelationshipId | undefined;
  let lo: ST_RelationshipId | undefined;
  let qs: ST_RelationshipId | undefined;
  let cs: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'dm': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            dm = v;
            continue;
          }
          ctx.invalidValue('CT_RelIds', a, 'ST_RelationshipId', cur);
          break;
        }
        case 'lo': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            lo = v;
            continue;
          }
          ctx.invalidValue('CT_RelIds', a, 'ST_RelationshipId', cur);
          break;
        }
        case 'qs': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            qs = v;
            continue;
          }
          ctx.invalidValue('CT_RelIds', a, 'ST_RelationshipId', cur);
          break;
        }
        case 'cs': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            cs = v;
            continue;
          }
          ctx.invalidValue('CT_RelIds', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_RelIds', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (dm === undefined) ctx.missingRequired('CT_RelIds', 'dm', cur);
  if (lo === undefined) ctx.missingRequired('CT_RelIds', 'lo', cur);
  if (qs === undefined) ctx.missingRequired('CT_RelIds', 'qs', cur);
  if (cs === undefined) ctx.missingRequired('CT_RelIds', 'cs', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RelIds', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_RelIds', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RelIds', cur);
    cur.next();
  }
  return {
    dm,
    lo,
    qs,
    cs,
    $unknownAttrs,
  };
}

/** Read a `CT_ResizeHandles`. See `transitional/dml-diagram.xsd:505`. */
export function readCT_ResizeHandles(cur: XmlCursor, ctx: ReadContext): CT_ResizeHandles {
  const start = requireStart(cur, 'CT_ResizeHandles');
  let val: ST_ResizeHandlesStr | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_ResizeHandlesStr(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_ResizeHandles', a, 'ST_ResizeHandlesStr', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ResizeHandles', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ResizeHandles', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ResizeHandles', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ResizeHandles', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Rules`. See `transitional/dml-diagram.xsd:202`. */
export function readCT_Rules(cur: XmlCursor, ctx: ReadContext): CT_Rules {
  const start = requireStart(cur, 'CT_Rules');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Rules', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const rule: CT_NumericRule[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Rules', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'rule': {
            rule.push(readCT_NumericRule(cur, ctx));
            $slot = 0;
            $index = rule.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Rules', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Rules', cur);
    cur.next();
  }

  return {
    rule,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SDCategories`. See `transitional/dml-diagram.xsd:533`. */
export function readCT_SDCategories(cur: XmlCursor, ctx: ReadContext): CT_SDCategories {
  const start = requireStart(cur, 'CT_SDCategories');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SDCategories', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const cat: CT_SDCategory[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SDCategories', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'cat': {
            cat.push(readCT_SDCategory(cur, ctx));
            $slot = 0;
            $index = cat.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SDCategories', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SDCategories', cur);
    cur.next();
  }

  return {
    cat,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SDCategory`. See `transitional/dml-diagram.xsd:529`. */
export function readCT_SDCategory(cur: XmlCursor, ctx: ReadContext): CT_SDCategory {
  const start = requireStart(cur, 'CT_SDCategory');
  let type: string | undefined;
  let pri: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type':
          type = a.value as string;
          continue;

        case 'pri': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            pri = v;
            continue;
          }
          ctx.invalidValue('CT_SDCategory', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SDCategory', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (type === undefined) ctx.missingRequired('CT_SDCategory', 'type', cur);
  if (pri === undefined) ctx.missingRequired('CT_SDCategory', 'pri', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SDCategory', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SDCategory', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SDCategory', cur);
    cur.next();
  }
  return {
    type,
    pri,
    $unknownAttrs,
  };
}

/** Read a `CT_SDDescription`. See `transitional/dml-diagram.xsd:525`. */
export function readCT_SDDescription(cur: XmlCursor, ctx: ReadContext): CT_SDDescription {
  const start = requireStart(cur, 'CT_SDDescription');
  let lang: string | undefined;
  let val: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'lang':
          lang = a.value as string;
          continue;

        case 'val':
          val = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_SDDescription', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_SDDescription', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SDDescription', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SDDescription', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SDDescription', cur);
    cur.next();
  }
  return {
    lang,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_SDName`. See `transitional/dml-diagram.xsd:521`. */
export function readCT_SDName(cur: XmlCursor, ctx: ReadContext): CT_SDName {
  const start = requireStart(cur, 'CT_SDName');
  let lang: string | undefined;
  let val: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'lang':
          lang = a.value as string;
          continue;

        case 'val':
          val = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_SDName', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_SDName', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SDName', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SDName', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SDName', cur);
    cur.next();
  }
  return {
    lang,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_SampleData`. See `transitional/dml-diagram.xsd:329`. */
export function readCT_SampleData(cur: XmlCursor, ctx: ReadContext): CT_SampleData {
  const start = requireStart(cur, 'CT_SampleData');
  let useDef: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'useDef': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            useDef = v;
            continue;
          }
          ctx.invalidValue('CT_SampleData', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SampleData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let dataModel: CT_DataModel | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SampleData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'dataModel': {
            dataModel = readCT_DataModel(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SampleData', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SampleData', cur);
    cur.next();
  }

  return {
    dataModel,
    useDef,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Shape`. See `transitional/dml-diagram.xsd:230`. */
export function readCT_Shape(cur: XmlCursor, ctx: ReadContext): CT_Shape {
  const start = requireStart(cur, 'CT_Shape');
  let rot: number | undefined;
  let type: ST_LayoutShapeType | undefined;
  let blip: ST_RelationshipId | undefined;
  let zOrderOff: number | undefined;
  let hideGeom: boolean | undefined;
  let lkTxEntry: boolean | undefined;
  let blipPhldr: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'rot': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            rot = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'xsd:double', cur);
          break;
        }
        case 'type': {
          const v = parseST_LayoutShapeType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'ST_LayoutShapeType', cur);
          break;
        }
        case 'zOrderOff': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            zOrderOff = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'xsd:int', cur);
          break;
        }
        case 'hideGeom': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            hideGeom = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'xsd:boolean', cur);
          break;
        }
        case 'lkTxEntry': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            lkTxEntry = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'xsd:boolean', cur);
          break;
        }
        case 'blipPhldr': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            blipPhldr = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'blip': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            blip = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Shape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let adjLst: CT_AdjLst | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Shape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'adjLst': {
            adjLst = readCT_AdjLst(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Shape', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Shape', cur);
    cur.next();
  }

  return {
    adjLst,
    extLst,
    rot,
    type,
    blip,
    zOrderOff,
    hideGeom,
    lkTxEntry,
    blipPhldr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_StyleDefinition`. See `transitional/dml-diagram.xsd:553`. */
export function readCT_StyleDefinition(cur: XmlCursor, ctx: ReadContext): CT_StyleDefinition {
  const start = requireStart(cur, 'CT_StyleDefinition');
  let uniqueId: string | undefined;
  let minVer: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'uniqueId':
          uniqueId = a.value as string;
          continue;

        case 'minVer':
          minVer = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_StyleDefinition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const title: CT_SDName[] = [];
  const desc: CT_SDDescription[] = [];
  let catLst: CT_SDCategories | undefined;
  let scene3d: CT_Scene3D | undefined;
  const styleLbl: CT_StyleLabel[] = [];
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StyleDefinition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'title': {
            title.push(readCT_SDName(cur, ctx));
            $slot = 0;
            $index = title.length - 1;
            continue;
          }
          case 'desc': {
            desc.push(readCT_SDDescription(cur, ctx));
            $slot = 1;
            $index = desc.length - 1;
            continue;
          }
          case 'catLst': {
            catLst = readCT_SDCategories(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'scene3d': {
            scene3d = readCT_Scene3D(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'styleLbl': {
            styleLbl.push(readCT_StyleLabel(cur, ctx));
            $slot = 4;
            $index = styleLbl.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_StyleDefinition', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StyleDefinition', cur);
    cur.next();
  }

  return {
    title,
    desc,
    catLst,
    scene3d,
    styleLbl,
    extLst,
    uniqueId,
    minVer,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_StyleDefinitionHeader`. See `transitional/dml-diagram.xsd:566`. */
export function readCT_StyleDefinitionHeader(cur: XmlCursor, ctx: ReadContext): CT_StyleDefinitionHeader {
  const start = requireStart(cur, 'CT_StyleDefinitionHeader');
  let uniqueId: string | undefined;
  let minVer: string | undefined;
  let resId: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'uniqueId':
          uniqueId = a.value as string;
          continue;

        case 'minVer':
          minVer = a.value as string;
          continue;

        case 'resId': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            resId = v;
            continue;
          }
          ctx.invalidValue('CT_StyleDefinitionHeader', a, 'xsd:int', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_StyleDefinitionHeader', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (uniqueId === undefined) ctx.missingRequired('CT_StyleDefinitionHeader', 'uniqueId', cur);

  const title: CT_SDName[] = [];
  const desc: CT_SDDescription[] = [];
  let catLst: CT_SDCategories | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StyleDefinitionHeader', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'title': {
            title.push(readCT_SDName(cur, ctx));
            $slot = 0;
            $index = title.length - 1;
            continue;
          }
          case 'desc': {
            desc.push(readCT_SDDescription(cur, ctx));
            $slot = 1;
            $index = desc.length - 1;
            continue;
          }
          case 'catLst': {
            catLst = readCT_SDCategories(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_StyleDefinitionHeader', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StyleDefinitionHeader', cur);
    cur.next();
  }

  return {
    title,
    desc,
    catLst,
    extLst,
    uniqueId,
    minVer,
    resId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_StyleDefinitionHeaderLst`. See `transitional/dml-diagram.xsd:578`. */
export function readCT_StyleDefinitionHeaderLst(cur: XmlCursor, ctx: ReadContext): CT_StyleDefinitionHeaderLst {
  const start = requireStart(cur, 'CT_StyleDefinitionHeaderLst');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_StyleDefinitionHeaderLst', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const styleDefHdr: CT_StyleDefinitionHeader[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StyleDefinitionHeaderLst', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'styleDefHdr': {
            styleDefHdr.push(readCT_StyleDefinitionHeader(cur, ctx));
            $slot = 0;
            $index = styleDefHdr.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_StyleDefinitionHeaderLst', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StyleDefinitionHeaderLst', cur);
    cur.next();
  }

  return {
    styleDefHdr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_StyleLabel`. See `transitional/dml-diagram.xsd:543`. */
export function readCT_StyleLabel(cur: XmlCursor, ctx: ReadContext): CT_StyleLabel {
  const start = requireStart(cur, 'CT_StyleLabel');
  let name: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_StyleLabel', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_StyleLabel', 'name', cur);

  let scene3d: CT_Scene3D | undefined;
  let sp3d: CT_Shape3D | undefined;
  let txPr: CT_TextProps | undefined;
  let style: CT_ShapeStyle | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StyleLabel', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'scene3d': {
            scene3d = readCT_Scene3D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sp3d': {
            sp3d = readCT_Shape3D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            txPr = readCT_TextProps(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_StyleLabel', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StyleLabel', cur);
    cur.next();
  }

  return {
    scene3d,
    sp3d,
    txPr,
    style,
    extLst,
    name,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextProps`. See `transitional/dml-diagram.xsd:538`. */
export function readCT_TextProps(cur: XmlCursor, ctx: ReadContext): CT_TextProps {
  const start = requireStart(cur, 'CT_TextProps');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextProps', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let text3D: CT_TextProps_Text3D | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextProps', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'sp3d': {
            text3D = { kind: 'sp3d', value: readCT_Shape3D(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'flatTx': {
            text3D = { kind: 'flatTx', value: readCT_FlatText(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextProps', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextProps', cur);
    cur.next();
  }

  return {
    text3D,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_When`. See `transitional/dml-diagram.xsd:289`. */
export function readCT_When(cur: XmlCursor, ctx: ReadContext): CT_When {
  const start = requireStart(cur, 'CT_When');
  let name: string | undefined;
  let axis: ST_AxisTypes | undefined;
  let ptType: ST_ElementTypes | undefined;
  let hideLastTrans: ST_Booleans | undefined;
  let st: ST_Ints | undefined;
  let cnt: ST_UnsignedInts | undefined;
  let step: ST_Ints | undefined;
  let func: ST_FunctionType | undefined;
  let arg: ST_FunctionArgument | undefined;
  let op: ST_FunctionOperator | undefined;
  let val: ST_FunctionValue | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

        case 'axis': {
          const v = parseST_AxisTypes(a.value);
          if (v !== undefined) {
            axis = v;
            continue;
          }
          ctx.invalidValue('CT_When', a, 'ST_AxisTypes', cur);
          break;
        }
        case 'ptType': {
          const v = parseST_ElementTypes(a.value);
          if (v !== undefined) {
            ptType = v;
            continue;
          }
          ctx.invalidValue('CT_When', a, 'ST_ElementTypes', cur);
          break;
        }
        case 'hideLastTrans': {
          const v = parseST_Booleans(a.value);
          if (v !== undefined) {
            hideLastTrans = v;
            continue;
          }
          ctx.invalidValue('CT_When', a, 'ST_Booleans', cur);
          break;
        }
        case 'st': {
          const v = parseST_Ints(a.value);
          if (v !== undefined) {
            st = v;
            continue;
          }
          ctx.invalidValue('CT_When', a, 'ST_Ints', cur);
          break;
        }
        case 'cnt': {
          const v = parseST_UnsignedInts(a.value);
          if (v !== undefined) {
            cnt = v;
            continue;
          }
          ctx.invalidValue('CT_When', a, 'ST_UnsignedInts', cur);
          break;
        }
        case 'step': {
          const v = parseST_Ints(a.value);
          if (v !== undefined) {
            step = v;
            continue;
          }
          ctx.invalidValue('CT_When', a, 'ST_Ints', cur);
          break;
        }
        case 'func': {
          const v = parseST_FunctionType(a.value);
          if (v !== undefined) {
            func = v;
            continue;
          }
          ctx.invalidValue('CT_When', a, 'ST_FunctionType', cur);
          break;
        }
        case 'arg': {
          const v = parseST_FunctionArgument(a.value);
          if (v !== undefined) {
            arg = v;
            continue;
          }
          ctx.invalidValue('CT_When', a, 'ST_FunctionArgument', cur);
          break;
        }
        case 'op': {
          const v = parseST_FunctionOperator(a.value);
          if (v !== undefined) {
            op = v;
            continue;
          }
          ctx.invalidValue('CT_When', a, 'ST_FunctionOperator', cur);
          break;
        }
        case 'val': {
          const v = parseST_FunctionValue(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_When', a, 'ST_FunctionValue', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_When', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (func === undefined) ctx.missingRequired('CT_When', 'func', cur);
  if (op === undefined) ctx.missingRequired('CT_When', 'op', cur);
  if (val === undefined) ctx.missingRequired('CT_When', 'val', cur);

  const content: CT_When_Content[] = [];

  const NS_dml_diagram = ctx.uris['dml-diagram'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_When', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_diagram) {
        switch (ev.localName) {
          case 'alg': {
            content.push({ kind: 'alg', value: readCT_Algorithm(cur, ctx) });
            continue;
          }
          case 'shape': {
            content.push({ kind: 'shape', value: readCT_Shape(cur, ctx) });
            continue;
          }
          case 'presOf': {
            content.push({ kind: 'presOf', value: readCT_PresentationOf(cur, ctx) });
            continue;
          }
          case 'constrLst': {
            content.push({ kind: 'constrLst', value: readCT_Constraints(cur, ctx) });
            continue;
          }
          case 'ruleLst': {
            content.push({ kind: 'ruleLst', value: readCT_Rules(cur, ctx) });
            continue;
          }
          case 'forEach': {
            content.push({ kind: 'forEach', value: readCT_ForEach(cur, ctx) });
            continue;
          }
          case 'layoutNode': {
            content.push({ kind: 'layoutNode', value: readCT_LayoutNode(cur, ctx) });
            continue;
          }
          case 'choose': {
            content.push({ kind: 'choose', value: readCT_Choose(cur, ctx) });
            continue;
          }
          case 'extLst': {
            content.push({ kind: 'extLst', value: readCT_OfficeArtExtensionList(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_When', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_When', cur);
    cur.next();
  }

  return {
    content,
    name,
    axis,
    ptType,
    hideLastTrans,
    st,
    cnt,
    step,
    func,
    arg,
    op,
    val,
    $unknownAttrs,
  };
}
