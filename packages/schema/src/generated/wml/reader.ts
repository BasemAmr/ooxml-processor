/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the wml namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type RawNode,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  parseInteger,
  requireStart,
} from '../../runtime/index.js';
import { readCT_Anchor, readCT_Inline } from '../dml-wordprocessingDrawing/reader.js';
import {
  readCT_Acc,
  readCT_Bar,
  readCT_BorderBox,
  readCT_Box,
  readCT_D,
  readCT_EqArr,
  readCT_F,
  readCT_Func,
  readCT_GroupChr,
  readCT_LimLow,
  readCT_LimUpp,
  readCT_M,
  readCT_MathPr,
  readCT_Nary,
  readCT_OMath,
  readCT_OMathPara,
  readCT_Phant,
  readCT_R as math_readCT_R,
  readCT_Rad,
  readCT_SPre,
  readCT_SSub,
  readCT_SSubSup,
  readCT_SSup,
} from '../math/reader.js';
import type { CT_MathPr } from '../math/types.js';
import { parseST_RelationshipId } from '../relationships/reader.js';
import type { ST_RelationshipId } from '../relationships/types.js';
import { readCT_SchemaLibrary } from '../schema-library/reader.js';
import type { CT_SchemaLibrary } from '../schema-library/types.js';
import {
  parseST_AlgClass,
  parseST_AlgType,
  parseST_CalendarType,
  parseST_ConformanceClass,
  parseST_CryptProv,
  parseST_Guid,
  parseST_HexColorRGB,
  parseST_Lang,
  parseST_OnOff,
  parseST_Panose,
  parseST_Percentage,
  parseST_PositiveUniversalMeasure,
  parseST_String,
  parseST_TwipsMeasure,
  parseST_UniversalMeasure,
  parseST_UnsignedDecimalNumber,
  parseST_VerticalAlignRun,
  parseST_XAlign,
  parseST_XmlName,
  parseST_YAlign,
} from '../shared-types/reader.js';
import type {
  ST_AlgClass,
  ST_AlgType,
  ST_CalendarType,
  ST_ConformanceClass,
  ST_CryptProv,
  ST_Guid,
  ST_Lang,
  ST_OnOff,
  ST_Panose,
  ST_String,
  ST_TwipsMeasure,
  ST_UnsignedDecimalNumber,
  ST_VerticalAlignRun,
  ST_XAlign,
  ST_XmlName,
  ST_YAlign,
} from '../shared-types/types.js';
import {
  type CT_AbstractNum,
  type CT_AltChunk,
  type CT_AltChunkPr,
  type CT_Attr,
  type CT_AutoCaption,
  type CT_AutoCaptions,
  type CT_Background,
  type CT_Base64Binary,
  type CT_BdoContentRun,
  type CT_BdoContentRun_PContent,
  type CT_Body,
  type CT_Body_BlockLevelElts,
  type CT_Bookmark,
  type CT_BookmarkRange,
  type CT_Border,
  type CT_BottomPageBorder,
  type CT_Br,
  type CT_CalendarType,
  type CT_Caption,
  type CT_Captions,
  type CT_CellMergeTrackChange,
  type CT_CharacterSpacing,
  type CT_Charset,
  type CT_Cnf,
  type CT_Color,
  type CT_ColorSchemeMapping,
  type CT_Column,
  type CT_Columns,
  type CT_Comment,
  type CT_Comment_BlockLevelElts,
  type CT_Comments,
  type CT_Compat,
  type CT_CompatSetting,
  type CT_Control,
  type CT_CustomXmlBlock,
  type CT_CustomXmlBlock_ContentBlockContent,
  type CT_CustomXmlCell,
  type CT_CustomXmlCell_ContentCellContent,
  type CT_CustomXmlPr,
  type CT_CustomXmlRow,
  type CT_CustomXmlRow_ContentRowContent,
  type CT_CustomXmlRun,
  type CT_CustomXmlRun_PContent,
  type CT_DataBinding,
  type CT_DecimalNumber,
  type CT_DecimalNumberOrPrecent,
  type CT_DirContentRun,
  type CT_DirContentRun_PContent,
  type CT_Div,
  type CT_DivBdr,
  type CT_Divs,
  type CT_DocDefaults,
  type CT_DocGrid,
  type CT_DocPart,
  type CT_DocPartBehavior,
  type CT_DocPartBehaviors,
  type CT_DocPartCategory,
  type CT_DocPartGallery,
  type CT_DocPartName,
  type CT_DocPartPr,
  type CT_DocPartType,
  type CT_DocPartTypes,
  type CT_DocParts,
  type CT_DocProtect,
  type CT_DocRsids,
  type CT_DocType,
  type CT_DocVar,
  type CT_DocVars,
  type CT_Document,
  type CT_DocumentBase,
  type CT_Drawing,
  type CT_Drawing_Content,
  type CT_EastAsianLayout,
  type CT_EdnDocProps,
  type CT_EdnPos,
  type CT_EdnProps,
  type CT_Em,
  type CT_Empty,
  type CT_Endnotes,
  type CT_FFCheckBox,
  type CT_FFCheckBox_Content,
  type CT_FFDDList,
  type CT_FFData,
  type CT_FFData_Content,
  type CT_FFHelpText,
  type CT_FFName,
  type CT_FFStatusText,
  type CT_FFTextInput,
  type CT_FFTextType,
  type CT_FitText,
  type CT_FldChar,
  type CT_FldChar_Content,
  type CT_Font,
  type CT_FontFamily,
  type CT_FontRel,
  type CT_FontSig,
  type CT_Fonts,
  type CT_FontsList,
  type CT_Footnotes,
  type CT_Frame,
  type CT_FrameLayout,
  type CT_FramePr,
  type CT_FrameScrollbar,
  type CT_Frameset,
  type CT_FramesetSplitbar,
  type CT_Frameset_Content,
  type CT_FtnDocProps,
  type CT_FtnEdn,
  type CT_FtnEdnRef,
  type CT_FtnEdnSepRef,
  type CT_FtnEdn_BlockLevelElts,
  type CT_FtnPos,
  type CT_FtnProps,
  type CT_GlossaryDocument,
  type CT_Guid,
  type CT_HMerge,
  type CT_HdrFtr,
  type CT_HdrFtrRef,
  type CT_HdrFtr_BlockLevelElts,
  type CT_Headers,
  type CT_Height,
  type CT_Highlight,
  type CT_HpsMeasure,
  type CT_Hyperlink,
  type CT_Hyperlink_PContent,
  type CT_Ind,
  type CT_Jc,
  type CT_JcTable,
  type CT_Kinsoku,
  type CT_Lang,
  type CT_Language,
  type CT_LatentStyles,
  type CT_LevelSuffix,
  type CT_LevelText,
  type CT_LineNumber,
  type CT_Lock,
  type CT_LongHexNumber,
  type CT_LsdException,
  type CT_Lvl,
  type CT_LvlLegacy,
  type CT_MacroName,
  type CT_MailMerge,
  type CT_MailMergeDataType,
  type CT_MailMergeDest,
  type CT_MailMergeDocType,
  type CT_MailMergeOdsoFMDFieldType,
  type CT_MailMergeSourceType,
  type CT_Markup,
  type CT_MarkupRange,
  type CT_MathCtrlDel,
  type CT_MathCtrlIns,
  type CT_MathCtrlIns_Content,
  type CT_MoveBookmark,
  type CT_MultiLevelType,
  type CT_Num,
  type CT_NumFmt,
  type CT_NumLvl,
  type CT_NumPicBullet,
  type CT_NumPicBullet_Content,
  type CT_NumPr,
  type CT_NumRestart,
  type CT_Numbering,
  type CT_Object,
  type CT_ObjectEmbed,
  type CT_ObjectLink,
  type CT_Object_Content,
  type CT_Odso,
  type CT_OdsoFieldMapData,
  type CT_OnOff,
  type CT_OptimizeForBrowser,
  type CT_P,
  type CT_PBdr,
  type CT_PPr,
  type CT_PPrBase,
  type CT_PPrChange,
  type CT_PPrDefault,
  type CT_PPrGeneral,
  type CT_PTab,
  type CT_P_PContent,
  type CT_PageBorder,
  type CT_PageBorders,
  type CT_PageMar,
  type CT_PageNumber,
  type CT_PageSz,
  type CT_Panose,
  type CT_PaperSource,
  type CT_ParaRPr,
  type CT_ParaRPrChange,
  type CT_ParaRPrOriginal,
  type CT_ParaRPrOriginal_RPrBase,
  type CT_ParaRPr_RPrBase,
  type CT_Perm,
  type CT_PermStart,
  type CT_Picture,
  type CT_Pitch,
  type CT_PixelsMeasure,
  type CT_Placeholder,
  type CT_Proof,
  type CT_ProofErr,
  type CT_R,
  type CT_RPr,
  type CT_RPrChange,
  type CT_RPrDefault,
  type CT_RPrOriginal,
  type CT_RPrOriginal_RPrBase,
  type CT_RPr_RPrBase,
  type CT_R_RunInnerContent,
  type CT_ReadingModeInkLockDown,
  type CT_RecipientData,
  type CT_Recipients,
  type CT_Rel,
  type CT_Row,
  type CT_Row_ContentCellContent,
  type CT_Ruby,
  type CT_RubyAlign,
  type CT_RubyContent,
  type CT_RubyContent_RubyContent,
  type CT_RubyPr,
  type CT_RunTrackChange,
  type CT_RunTrackChange_Content,
  type CT_SaveThroughXslt,
  type CT_SdtBlock,
  type CT_SdtCell,
  type CT_SdtComboBox,
  type CT_SdtContentBlock,
  type CT_SdtContentBlock_ContentBlockContent,
  type CT_SdtContentCell,
  type CT_SdtContentCell_ContentCellContent,
  type CT_SdtContentRow,
  type CT_SdtContentRow_ContentRowContent,
  type CT_SdtContentRun,
  type CT_SdtContentRun_PContent,
  type CT_SdtDate,
  type CT_SdtDateMappingType,
  type CT_SdtDocPart,
  type CT_SdtDropDownList,
  type CT_SdtEndPr,
  type CT_SdtListItem,
  type CT_SdtPr,
  type CT_SdtPr_Content,
  type CT_SdtRow,
  type CT_SdtRun,
  type CT_SdtText,
  type CT_SectPr,
  type CT_SectPrBase,
  type CT_SectPrChange,
  type CT_SectPr_HdrFtrReferences,
  type CT_SectType,
  type CT_Settings,
  type CT_ShapeDefaults,
  type CT_Shd,
  type CT_SignedHpsMeasure,
  type CT_SignedTwipsMeasure,
  type CT_SimpleField,
  type CT_SimpleField_PContent,
  type CT_SmartTagPr,
  type CT_SmartTagRun,
  type CT_SmartTagRun_PContent,
  type CT_SmartTagType,
  type CT_Spacing,
  type CT_String,
  type CT_Style,
  type CT_StylePaneFilter,
  type CT_StyleSort,
  type CT_Styles,
  type CT_Sym,
  type CT_TabStop,
  type CT_Tabs,
  type CT_TargetScreenSz,
  type CT_Tbl,
  type CT_TblBorders,
  type CT_TblCellMar,
  type CT_TblGrid,
  type CT_TblGridBase,
  type CT_TblGridChange,
  type CT_TblGridCol,
  type CT_TblLayoutType,
  type CT_TblLook,
  type CT_TblOverlap,
  type CT_TblPPr,
  type CT_TblPr,
  type CT_TblPrBase,
  type CT_TblPrChange,
  type CT_TblPrEx,
  type CT_TblPrExBase,
  type CT_TblPrExChange,
  type CT_TblStylePr,
  type CT_TblWidth,
  type CT_Tbl_ContentRowContent,
  type CT_Tbl_RangeMarkupElements,
  type CT_Tc,
  type CT_TcBorders,
  type CT_TcMar,
  type CT_TcPr,
  type CT_TcPrBase,
  type CT_TcPrChange,
  type CT_TcPrInner,
  type CT_TcPrInner_CellMarkupElements,
  type CT_TcPr_CellMarkupElements,
  type CT_Tc_BlockLevelElts,
  type CT_Text,
  type CT_TextAlignment,
  type CT_TextDirection,
  type CT_TextEffect,
  type CT_TextScale,
  type CT_TextboxTightWrap,
  type CT_TopPageBorder,
  type CT_TrPr,
  type CT_TrPrBase,
  type CT_TrPrBase_Content,
  type CT_TrPrChange,
  type CT_TrPr_Content,
  type CT_TrackChange,
  type CT_TrackChangeNumbering,
  type CT_TrackChangeRange,
  type CT_TrackChangesView,
  type CT_TwipsMeasure,
  type CT_TxbxContent,
  type CT_TxbxContent_BlockLevelElts,
  type CT_Underline,
  type CT_UnsignedDecimalNumber,
  type CT_VMerge,
  type CT_VerticalAlignRun,
  type CT_VerticalJc,
  type CT_View,
  type CT_WebSettings,
  type CT_WriteProtection,
  type CT_WritingStyle,
  type CT_Zoom,
  type ST_AnnotationVMerge,
  ST_AnnotationVMerge_VALUES,
  type ST_Border,
  ST_Border_VALUES,
  type ST_BrClear,
  ST_BrClear_VALUES,
  type ST_BrType,
  ST_BrType_VALUES,
  type ST_CaptionPos,
  ST_CaptionPos_VALUES,
  type ST_ChapterSep,
  ST_ChapterSep_VALUES,
  type ST_CharacterSpacing,
  ST_CharacterSpacing_VALUES,
  type ST_Cnf,
  type ST_CombineBrackets,
  ST_CombineBrackets_VALUES,
  type ST_DateTime,
  type ST_DecimalNumber,
  type ST_DecimalNumberOrPercent,
  type ST_Direction,
  ST_Direction_VALUES,
  type ST_DisplacedByCustomXml,
  ST_DisplacedByCustomXml_VALUES,
  type ST_DocGrid,
  ST_DocGrid_VALUES,
  type ST_DocPartBehavior,
  ST_DocPartBehavior_VALUES,
  type ST_DocPartGallery,
  ST_DocPartGallery_VALUES,
  type ST_DocPartType,
  ST_DocPartType_VALUES,
  type ST_DocProtect,
  ST_DocProtect_VALUES,
  type ST_DocType,
  type ST_DropCap,
  ST_DropCap_VALUES,
  type ST_EdGrp,
  ST_EdGrp_VALUES,
  type ST_EdnPos,
  ST_EdnPos_VALUES,
  type ST_EighthPointMeasure,
  type ST_Em,
  ST_Em_VALUES,
  type ST_FFHelpTextVal,
  type ST_FFName,
  type ST_FFStatusTextVal,
  type ST_FFTextType,
  ST_FFTextType_VALUES,
  type ST_FldCharType,
  ST_FldCharType_VALUES,
  type ST_FontFamily,
  ST_FontFamily_VALUES,
  type ST_FrameLayout,
  ST_FrameLayout_VALUES,
  type ST_FrameScrollbar,
  ST_FrameScrollbar_VALUES,
  type ST_FtnEdn,
  ST_FtnEdn_VALUES,
  type ST_FtnPos,
  ST_FtnPos_VALUES,
  type ST_HAnchor,
  ST_HAnchor_VALUES,
  type ST_HdrFtr,
  ST_HdrFtr_VALUES,
  type ST_HeightRule,
  ST_HeightRule_VALUES,
  type ST_HexColor,
  type ST_HexColorAuto,
  ST_HexColorAuto_VALUES,
  type ST_HighlightColor,
  ST_HighlightColor_VALUES,
  type ST_Hint,
  ST_Hint_VALUES,
  type ST_HpsMeasure,
  type ST_InfoTextType,
  ST_InfoTextType_VALUES,
  type ST_Jc,
  type ST_JcTable,
  ST_JcTable_VALUES,
  ST_Jc_VALUES,
  type ST_LevelSuffix,
  ST_LevelSuffix_VALUES,
  type ST_LineNumberRestart,
  ST_LineNumberRestart_VALUES,
  type ST_LineSpacingRule,
  ST_LineSpacingRule_VALUES,
  type ST_Lock,
  ST_Lock_VALUES,
  type ST_LongHexNumber,
  type ST_MacroName,
  type ST_MailMergeDataType,
  type ST_MailMergeDest,
  ST_MailMergeDest_VALUES,
  type ST_MailMergeDocType,
  ST_MailMergeDocType_VALUES,
  type ST_MailMergeOdsoFMDFieldType,
  ST_MailMergeOdsoFMDFieldType_VALUES,
  type ST_MailMergeSourceType,
  ST_MailMergeSourceType_VALUES,
  type ST_MeasurementOrPercent,
  type ST_Merge,
  ST_Merge_VALUES,
  type ST_MultiLevelType,
  ST_MultiLevelType_VALUES,
  type ST_NumberFormat,
  ST_NumberFormat_VALUES,
  type ST_ObjectDrawAspect,
  ST_ObjectDrawAspect_VALUES,
  type ST_ObjectUpdateMode,
  ST_ObjectUpdateMode_VALUES,
  type ST_PTabAlignment,
  ST_PTabAlignment_VALUES,
  type ST_PTabLeader,
  ST_PTabLeader_VALUES,
  type ST_PTabRelativeTo,
  ST_PTabRelativeTo_VALUES,
  type ST_PageBorderDisplay,
  ST_PageBorderDisplay_VALUES,
  type ST_PageBorderOffset,
  ST_PageBorderOffset_VALUES,
  type ST_PageBorderZOrder,
  ST_PageBorderZOrder_VALUES,
  type ST_PageOrientation,
  ST_PageOrientation_VALUES,
  type ST_Pitch,
  ST_Pitch_VALUES,
  type ST_PixelsMeasure,
  type ST_PointMeasure,
  type ST_Proof,
  type ST_ProofErr,
  ST_ProofErr_VALUES,
  ST_Proof_VALUES,
  type ST_RestartNumber,
  ST_RestartNumber_VALUES,
  type ST_RubyAlign,
  ST_RubyAlign_VALUES,
  type ST_SdtDateMappingType,
  ST_SdtDateMappingType_VALUES,
  type ST_SectionMark,
  ST_SectionMark_VALUES,
  type ST_Shd,
  ST_Shd_VALUES,
  type ST_ShortHexNumber,
  type ST_SignedHpsMeasure,
  type ST_SignedTwipsMeasure,
  type ST_StyleSort,
  ST_StyleSort_VALUES,
  type ST_StyleType,
  ST_StyleType_VALUES,
  type ST_TabJc,
  ST_TabJc_VALUES,
  type ST_TabTlc,
  ST_TabTlc_VALUES,
  type ST_TargetScreenSz,
  ST_TargetScreenSz_VALUES,
  type ST_TblLayoutType,
  ST_TblLayoutType_VALUES,
  type ST_TblOverlap,
  ST_TblOverlap_VALUES,
  type ST_TblStyleOverrideType,
  ST_TblStyleOverrideType_VALUES,
  type ST_TblWidth,
  ST_TblWidth_VALUES,
  type ST_TextAlignment,
  ST_TextAlignment_VALUES,
  type ST_TextDirection,
  ST_TextDirection_VALUES,
  type ST_TextEffect,
  ST_TextEffect_VALUES,
  type ST_TextScale,
  type ST_TextScaleDecimal,
  type ST_TextScalePercent,
  type ST_TextboxTightWrap,
  ST_TextboxTightWrap_VALUES,
  type ST_Theme,
  type ST_ThemeColor,
  ST_ThemeColor_VALUES,
  ST_Theme_VALUES,
  type ST_UcharHexNumber,
  type ST_Underline,
  ST_Underline_VALUES,
  type ST_UnqualifiedPercentage,
  type ST_VAnchor,
  ST_VAnchor_VALUES,
  type ST_VerticalJc,
  ST_VerticalJc_VALUES,
  type ST_View,
  ST_View_VALUES,
  type ST_WmlColorSchemeIndex,
  ST_WmlColorSchemeIndex_VALUES,
  type ST_Wrap,
  ST_Wrap_VALUES,
  type ST_Zoom,
  ST_Zoom_VALUES,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_AnnotationVMerge(value: string): ST_AnnotationVMerge | undefined {
  const s = collapse(value);
  return ST_AnnotationVMerge_VALUES.has(s) ? (s as ST_AnnotationVMerge) : undefined;
}

export function parseST_Border(value: string): ST_Border | undefined {
  const s = collapse(value);
  return ST_Border_VALUES.has(s) ? (s as ST_Border) : undefined;
}

export function parseST_BrClear(value: string): ST_BrClear | undefined {
  const s = collapse(value);
  return ST_BrClear_VALUES.has(s) ? (s as ST_BrClear) : undefined;
}

export function parseST_BrType(value: string): ST_BrType | undefined {
  const s = collapse(value);
  return ST_BrType_VALUES.has(s) ? (s as ST_BrType) : undefined;
}

export function parseST_CaptionPos(value: string): ST_CaptionPos | undefined {
  const s = collapse(value);
  return ST_CaptionPos_VALUES.has(s) ? (s as ST_CaptionPos) : undefined;
}

export function parseST_ChapterSep(value: string): ST_ChapterSep | undefined {
  const s = collapse(value);
  return ST_ChapterSep_VALUES.has(s) ? (s as ST_ChapterSep) : undefined;
}

export function parseST_CharacterSpacing(value: string): ST_CharacterSpacing | undefined {
  const s = collapse(value);
  return ST_CharacterSpacing_VALUES.has(s) ? (s as ST_CharacterSpacing) : undefined;
}

export function parseST_Cnf(value: string): ST_Cnf | undefined {
  return value as ST_Cnf;
}

export function parseST_CombineBrackets(value: string): ST_CombineBrackets | undefined {
  const s = collapse(value);
  return ST_CombineBrackets_VALUES.has(s) ? (s as ST_CombineBrackets) : undefined;
}

export function parseST_DateTime(value: string): ST_DateTime | undefined {
  return value as ST_DateTime;
}

export function parseST_DecimalNumber(value: string): ST_DecimalNumber | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_DecimalNumber);
}

export function parseST_DecimalNumberOrPercent(value: string): ST_DecimalNumberOrPercent | undefined {
  return (parseST_UnqualifiedPercentage(value) ?? parseST_Percentage(value)) as ST_DecimalNumberOrPercent | undefined;
}

export function parseST_Direction(value: string): ST_Direction | undefined {
  const s = collapse(value);
  return ST_Direction_VALUES.has(s) ? (s as ST_Direction) : undefined;
}

export function parseST_DisplacedByCustomXml(value: string): ST_DisplacedByCustomXml | undefined {
  const s = collapse(value);
  return ST_DisplacedByCustomXml_VALUES.has(s) ? (s as ST_DisplacedByCustomXml) : undefined;
}

export function parseST_DocGrid(value: string): ST_DocGrid | undefined {
  const s = collapse(value);
  return ST_DocGrid_VALUES.has(s) ? (s as ST_DocGrid) : undefined;
}

export function parseST_DocPartBehavior(value: string): ST_DocPartBehavior | undefined {
  const s = collapse(value);
  return ST_DocPartBehavior_VALUES.has(s) ? (s as ST_DocPartBehavior) : undefined;
}

export function parseST_DocPartGallery(value: string): ST_DocPartGallery | undefined {
  const s = collapse(value);
  return ST_DocPartGallery_VALUES.has(s) ? (s as ST_DocPartGallery) : undefined;
}

export function parseST_DocPartType(value: string): ST_DocPartType | undefined {
  const s = collapse(value);
  return ST_DocPartType_VALUES.has(s) ? (s as ST_DocPartType) : undefined;
}

export function parseST_DocProtect(value: string): ST_DocProtect | undefined {
  const s = collapse(value);
  return ST_DocProtect_VALUES.has(s) ? (s as ST_DocProtect) : undefined;
}

export function parseST_DocType(value: string): ST_DocType | undefined {
  return value as ST_DocType;
}

export function parseST_DropCap(value: string): ST_DropCap | undefined {
  const s = collapse(value);
  return ST_DropCap_VALUES.has(s) ? (s as ST_DropCap) : undefined;
}

export function parseST_EdGrp(value: string): ST_EdGrp | undefined {
  const s = collapse(value);
  return ST_EdGrp_VALUES.has(s) ? (s as ST_EdGrp) : undefined;
}

export function parseST_EdnPos(value: string): ST_EdnPos | undefined {
  const s = collapse(value);
  return ST_EdnPos_VALUES.has(s) ? (s as ST_EdnPos) : undefined;
}

export function parseST_EighthPointMeasure(value: string): ST_EighthPointMeasure | undefined {
  return value as ST_EighthPointMeasure;
}

export function parseST_Em(value: string): ST_Em | undefined {
  const s = collapse(value);
  return ST_Em_VALUES.has(s) ? (s as ST_Em) : undefined;
}

export function parseST_FFHelpTextVal(value: string): ST_FFHelpTextVal | undefined {
  return value as ST_FFHelpTextVal;
}

export function parseST_FFName(value: string): ST_FFName | undefined {
  return value as ST_FFName;
}

export function parseST_FFStatusTextVal(value: string): ST_FFStatusTextVal | undefined {
  return value as ST_FFStatusTextVal;
}

export function parseST_FFTextType(value: string): ST_FFTextType | undefined {
  const s = collapse(value);
  return ST_FFTextType_VALUES.has(s) ? (s as ST_FFTextType) : undefined;
}

export function parseST_FldCharType(value: string): ST_FldCharType | undefined {
  const s = collapse(value);
  return ST_FldCharType_VALUES.has(s) ? (s as ST_FldCharType) : undefined;
}

export function parseST_FontFamily(value: string): ST_FontFamily | undefined {
  const s = collapse(value);
  return ST_FontFamily_VALUES.has(s) ? (s as ST_FontFamily) : undefined;
}

export function parseST_FrameLayout(value: string): ST_FrameLayout | undefined {
  const s = collapse(value);
  return ST_FrameLayout_VALUES.has(s) ? (s as ST_FrameLayout) : undefined;
}

export function parseST_FrameScrollbar(value: string): ST_FrameScrollbar | undefined {
  const s = collapse(value);
  return ST_FrameScrollbar_VALUES.has(s) ? (s as ST_FrameScrollbar) : undefined;
}

export function parseST_FtnEdn(value: string): ST_FtnEdn | undefined {
  const s = collapse(value);
  return ST_FtnEdn_VALUES.has(s) ? (s as ST_FtnEdn) : undefined;
}

export function parseST_FtnPos(value: string): ST_FtnPos | undefined {
  const s = collapse(value);
  return ST_FtnPos_VALUES.has(s) ? (s as ST_FtnPos) : undefined;
}

export function parseST_HAnchor(value: string): ST_HAnchor | undefined {
  const s = collapse(value);
  return ST_HAnchor_VALUES.has(s) ? (s as ST_HAnchor) : undefined;
}

export function parseST_HdrFtr(value: string): ST_HdrFtr | undefined {
  const s = collapse(value);
  return ST_HdrFtr_VALUES.has(s) ? (s as ST_HdrFtr) : undefined;
}

export function parseST_HeightRule(value: string): ST_HeightRule | undefined {
  const s = collapse(value);
  return ST_HeightRule_VALUES.has(s) ? (s as ST_HeightRule) : undefined;
}

export function parseST_HexColor(value: string): ST_HexColor | undefined {
  return (parseST_HexColorAuto(value) ?? parseST_HexColorRGB(value)) as ST_HexColor | undefined;
}

export function parseST_HexColorAuto(value: string): ST_HexColorAuto | undefined {
  const s = collapse(value);
  return ST_HexColorAuto_VALUES.has(s) ? (s as ST_HexColorAuto) : undefined;
}

export function parseST_HighlightColor(value: string): ST_HighlightColor | undefined {
  const s = collapse(value);
  return ST_HighlightColor_VALUES.has(s) ? (s as ST_HighlightColor) : undefined;
}

export function parseST_Hint(value: string): ST_Hint | undefined {
  const s = collapse(value);
  return ST_Hint_VALUES.has(s) ? (s as ST_Hint) : undefined;
}

export function parseST_HpsMeasure(value: string): ST_HpsMeasure | undefined {
  return (parseST_UnsignedDecimalNumber(value) ?? parseST_PositiveUniversalMeasure(value)) as ST_HpsMeasure | undefined;
}

export function parseST_InfoTextType(value: string): ST_InfoTextType | undefined {
  const s = collapse(value);
  return ST_InfoTextType_VALUES.has(s) ? (s as ST_InfoTextType) : undefined;
}

export function parseST_Jc(value: string): ST_Jc | undefined {
  const s = collapse(value);
  return ST_Jc_VALUES.has(s) ? (s as ST_Jc) : undefined;
}

export function parseST_JcTable(value: string): ST_JcTable | undefined {
  const s = collapse(value);
  return ST_JcTable_VALUES.has(s) ? (s as ST_JcTable) : undefined;
}

export function parseST_LevelSuffix(value: string): ST_LevelSuffix | undefined {
  const s = collapse(value);
  return ST_LevelSuffix_VALUES.has(s) ? (s as ST_LevelSuffix) : undefined;
}

export function parseST_LineNumberRestart(value: string): ST_LineNumberRestart | undefined {
  const s = collapse(value);
  return ST_LineNumberRestart_VALUES.has(s) ? (s as ST_LineNumberRestart) : undefined;
}

export function parseST_LineSpacingRule(value: string): ST_LineSpacingRule | undefined {
  const s = collapse(value);
  return ST_LineSpacingRule_VALUES.has(s) ? (s as ST_LineSpacingRule) : undefined;
}

export function parseST_Lock(value: string): ST_Lock | undefined {
  const s = collapse(value);
  return ST_Lock_VALUES.has(s) ? (s as ST_Lock) : undefined;
}

export function parseST_LongHexNumber(value: string): ST_LongHexNumber | undefined {
  return value as ST_LongHexNumber;
}

export function parseST_MacroName(value: string): ST_MacroName | undefined {
  return value as ST_MacroName;
}

export function parseST_MailMergeDataType(value: string): ST_MailMergeDataType | undefined {
  return value as ST_MailMergeDataType;
}

export function parseST_MailMergeDest(value: string): ST_MailMergeDest | undefined {
  const s = collapse(value);
  return ST_MailMergeDest_VALUES.has(s) ? (s as ST_MailMergeDest) : undefined;
}

export function parseST_MailMergeDocType(value: string): ST_MailMergeDocType | undefined {
  const s = collapse(value);
  return ST_MailMergeDocType_VALUES.has(s) ? (s as ST_MailMergeDocType) : undefined;
}

export function parseST_MailMergeOdsoFMDFieldType(value: string): ST_MailMergeOdsoFMDFieldType | undefined {
  const s = collapse(value);
  return ST_MailMergeOdsoFMDFieldType_VALUES.has(s) ? (s as ST_MailMergeOdsoFMDFieldType) : undefined;
}

export function parseST_MailMergeSourceType(value: string): ST_MailMergeSourceType | undefined {
  const s = collapse(value);
  return ST_MailMergeSourceType_VALUES.has(s) ? (s as ST_MailMergeSourceType) : undefined;
}

export function parseST_MeasurementOrPercent(value: string): ST_MeasurementOrPercent | undefined {
  return (parseST_DecimalNumberOrPercent(value) ?? parseST_UniversalMeasure(value)) as ST_MeasurementOrPercent | undefined;
}

export function parseST_Merge(value: string): ST_Merge | undefined {
  const s = collapse(value);
  return ST_Merge_VALUES.has(s) ? (s as ST_Merge) : undefined;
}

export function parseST_MultiLevelType(value: string): ST_MultiLevelType | undefined {
  const s = collapse(value);
  return ST_MultiLevelType_VALUES.has(s) ? (s as ST_MultiLevelType) : undefined;
}

export function parseST_NumberFormat(value: string): ST_NumberFormat | undefined {
  const s = collapse(value);
  return ST_NumberFormat_VALUES.has(s) ? (s as ST_NumberFormat) : undefined;
}

export function parseST_ObjectDrawAspect(value: string): ST_ObjectDrawAspect | undefined {
  const s = collapse(value);
  return ST_ObjectDrawAspect_VALUES.has(s) ? (s as ST_ObjectDrawAspect) : undefined;
}

export function parseST_ObjectUpdateMode(value: string): ST_ObjectUpdateMode | undefined {
  const s = collapse(value);
  return ST_ObjectUpdateMode_VALUES.has(s) ? (s as ST_ObjectUpdateMode) : undefined;
}

export function parseST_PTabAlignment(value: string): ST_PTabAlignment | undefined {
  const s = collapse(value);
  return ST_PTabAlignment_VALUES.has(s) ? (s as ST_PTabAlignment) : undefined;
}

export function parseST_PTabLeader(value: string): ST_PTabLeader | undefined {
  const s = collapse(value);
  return ST_PTabLeader_VALUES.has(s) ? (s as ST_PTabLeader) : undefined;
}

export function parseST_PTabRelativeTo(value: string): ST_PTabRelativeTo | undefined {
  const s = collapse(value);
  return ST_PTabRelativeTo_VALUES.has(s) ? (s as ST_PTabRelativeTo) : undefined;
}

export function parseST_PageBorderDisplay(value: string): ST_PageBorderDisplay | undefined {
  const s = collapse(value);
  return ST_PageBorderDisplay_VALUES.has(s) ? (s as ST_PageBorderDisplay) : undefined;
}

export function parseST_PageBorderOffset(value: string): ST_PageBorderOffset | undefined {
  const s = collapse(value);
  return ST_PageBorderOffset_VALUES.has(s) ? (s as ST_PageBorderOffset) : undefined;
}

export function parseST_PageBorderZOrder(value: string): ST_PageBorderZOrder | undefined {
  const s = collapse(value);
  return ST_PageBorderZOrder_VALUES.has(s) ? (s as ST_PageBorderZOrder) : undefined;
}

export function parseST_PageOrientation(value: string): ST_PageOrientation | undefined {
  const s = collapse(value);
  return ST_PageOrientation_VALUES.has(s) ? (s as ST_PageOrientation) : undefined;
}

export function parseST_Pitch(value: string): ST_Pitch | undefined {
  const s = collapse(value);
  return ST_Pitch_VALUES.has(s) ? (s as ST_Pitch) : undefined;
}

export function parseST_PixelsMeasure(value: string): ST_PixelsMeasure | undefined {
  return value as ST_PixelsMeasure;
}

export function parseST_PointMeasure(value: string): ST_PointMeasure | undefined {
  return value as ST_PointMeasure;
}

export function parseST_Proof(value: string): ST_Proof | undefined {
  const s = collapse(value);
  return ST_Proof_VALUES.has(s) ? (s as ST_Proof) : undefined;
}

export function parseST_ProofErr(value: string): ST_ProofErr | undefined {
  const s = collapse(value);
  return ST_ProofErr_VALUES.has(s) ? (s as ST_ProofErr) : undefined;
}

export function parseST_RestartNumber(value: string): ST_RestartNumber | undefined {
  const s = collapse(value);
  return ST_RestartNumber_VALUES.has(s) ? (s as ST_RestartNumber) : undefined;
}

export function parseST_RubyAlign(value: string): ST_RubyAlign | undefined {
  const s = collapse(value);
  return ST_RubyAlign_VALUES.has(s) ? (s as ST_RubyAlign) : undefined;
}

export function parseST_SdtDateMappingType(value: string): ST_SdtDateMappingType | undefined {
  const s = collapse(value);
  return ST_SdtDateMappingType_VALUES.has(s) ? (s as ST_SdtDateMappingType) : undefined;
}

export function parseST_SectionMark(value: string): ST_SectionMark | undefined {
  const s = collapse(value);
  return ST_SectionMark_VALUES.has(s) ? (s as ST_SectionMark) : undefined;
}

export function parseST_Shd(value: string): ST_Shd | undefined {
  const s = collapse(value);
  return ST_Shd_VALUES.has(s) ? (s as ST_Shd) : undefined;
}

export function parseST_ShortHexNumber(value: string): ST_ShortHexNumber | undefined {
  return value as ST_ShortHexNumber;
}

export function parseST_SignedHpsMeasure(value: string): ST_SignedHpsMeasure | undefined {
  return (parseInteger(value) ?? parseST_UniversalMeasure(value)) as ST_SignedHpsMeasure | undefined;
}

export function parseST_SignedTwipsMeasure(value: string): ST_SignedTwipsMeasure | undefined {
  return (parseInteger(value) ?? parseST_UniversalMeasure(value)) as ST_SignedTwipsMeasure | undefined;
}

export function parseST_StyleSort(value: string): ST_StyleSort | undefined {
  const s = collapse(value);
  return ST_StyleSort_VALUES.has(s) ? (s as ST_StyleSort) : undefined;
}

export function parseST_StyleType(value: string): ST_StyleType | undefined {
  const s = collapse(value);
  return ST_StyleType_VALUES.has(s) ? (s as ST_StyleType) : undefined;
}

export function parseST_TabJc(value: string): ST_TabJc | undefined {
  const s = collapse(value);
  return ST_TabJc_VALUES.has(s) ? (s as ST_TabJc) : undefined;
}

export function parseST_TabTlc(value: string): ST_TabTlc | undefined {
  const s = collapse(value);
  return ST_TabTlc_VALUES.has(s) ? (s as ST_TabTlc) : undefined;
}

export function parseST_TargetScreenSz(value: string): ST_TargetScreenSz | undefined {
  const s = collapse(value);
  return ST_TargetScreenSz_VALUES.has(s) ? (s as ST_TargetScreenSz) : undefined;
}

export function parseST_TblLayoutType(value: string): ST_TblLayoutType | undefined {
  const s = collapse(value);
  return ST_TblLayoutType_VALUES.has(s) ? (s as ST_TblLayoutType) : undefined;
}

export function parseST_TblOverlap(value: string): ST_TblOverlap | undefined {
  const s = collapse(value);
  return ST_TblOverlap_VALUES.has(s) ? (s as ST_TblOverlap) : undefined;
}

export function parseST_TblStyleOverrideType(value: string): ST_TblStyleOverrideType | undefined {
  const s = collapse(value);
  return ST_TblStyleOverrideType_VALUES.has(s) ? (s as ST_TblStyleOverrideType) : undefined;
}

export function parseST_TblWidth(value: string): ST_TblWidth | undefined {
  const s = collapse(value);
  return ST_TblWidth_VALUES.has(s) ? (s as ST_TblWidth) : undefined;
}

export function parseST_TextAlignment(value: string): ST_TextAlignment | undefined {
  const s = collapse(value);
  return ST_TextAlignment_VALUES.has(s) ? (s as ST_TextAlignment) : undefined;
}

export function parseST_TextDirection(value: string): ST_TextDirection | undefined {
  const s = collapse(value);
  return ST_TextDirection_VALUES.has(s) ? (s as ST_TextDirection) : undefined;
}

export function parseST_TextEffect(value: string): ST_TextEffect | undefined {
  const s = collapse(value);
  return ST_TextEffect_VALUES.has(s) ? (s as ST_TextEffect) : undefined;
}

export function parseST_TextScale(value: string): ST_TextScale | undefined {
  return (parseST_TextScalePercent(value) ?? parseST_TextScaleDecimal(value)) as ST_TextScale | undefined;
}

export function parseST_TextScaleDecimal(value: string): ST_TextScaleDecimal | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_TextScaleDecimal);
}

export function parseST_TextScalePercent(value: string): ST_TextScalePercent | undefined {
  return value as ST_TextScalePercent;
}

export function parseST_TextboxTightWrap(value: string): ST_TextboxTightWrap | undefined {
  const s = collapse(value);
  return ST_TextboxTightWrap_VALUES.has(s) ? (s as ST_TextboxTightWrap) : undefined;
}

export function parseST_Theme(value: string): ST_Theme | undefined {
  const s = collapse(value);
  return ST_Theme_VALUES.has(s) ? (s as ST_Theme) : undefined;
}

export function parseST_ThemeColor(value: string): ST_ThemeColor | undefined {
  const s = collapse(value);
  return ST_ThemeColor_VALUES.has(s) ? (s as ST_ThemeColor) : undefined;
}

export function parseST_UcharHexNumber(value: string): ST_UcharHexNumber | undefined {
  return value as ST_UcharHexNumber;
}

export function parseST_Underline(value: string): ST_Underline | undefined {
  const s = collapse(value);
  return ST_Underline_VALUES.has(s) ? (s as ST_Underline) : undefined;
}

export function parseST_UnqualifiedPercentage(value: string): ST_UnqualifiedPercentage | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_UnqualifiedPercentage);
}

export function parseST_VAnchor(value: string): ST_VAnchor | undefined {
  const s = collapse(value);
  return ST_VAnchor_VALUES.has(s) ? (s as ST_VAnchor) : undefined;
}

export function parseST_VerticalJc(value: string): ST_VerticalJc | undefined {
  const s = collapse(value);
  return ST_VerticalJc_VALUES.has(s) ? (s as ST_VerticalJc) : undefined;
}

export function parseST_View(value: string): ST_View | undefined {
  const s = collapse(value);
  return ST_View_VALUES.has(s) ? (s as ST_View) : undefined;
}

export function parseST_WmlColorSchemeIndex(value: string): ST_WmlColorSchemeIndex | undefined {
  const s = collapse(value);
  return ST_WmlColorSchemeIndex_VALUES.has(s) ? (s as ST_WmlColorSchemeIndex) : undefined;
}

export function parseST_Wrap(value: string): ST_Wrap | undefined {
  const s = collapse(value);
  return ST_Wrap_VALUES.has(s) ? (s as ST_Wrap) : undefined;
}

export function parseST_Zoom(value: string): ST_Zoom | undefined {
  const s = collapse(value);
  return ST_Zoom_VALUES.has(s) ? (s as ST_Zoom) : undefined;
}

// Complex-type readers.

/** Read a `CT_AbstractNum`. See `transitional/wml.xsd:3162`. */
export function readCT_AbstractNum(cur: XmlCursor, ctx: ReadContext): CT_AbstractNum {
  const start = requireStart(cur, 'CT_AbstractNum');
  let abstractNumId: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'abstractNumId': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            abstractNumId = v;
            continue;
          }
          ctx.invalidValue('CT_AbstractNum', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AbstractNum', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (abstractNumId === undefined) ctx.missingRequired('CT_AbstractNum', 'abstractNumId', cur);

  let nsid: CT_LongHexNumber | undefined;
  let multiLevelType: CT_MultiLevelType | undefined;
  let tmpl: CT_LongHexNumber | undefined;
  let name: CT_String | undefined;
  let styleLink: CT_String | undefined;
  let numStyleLink: CT_String | undefined;
  const lvl: CT_Lvl[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AbstractNum', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'nsid': {
            nsid = readCT_LongHexNumber(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'multiLevelType': {
            multiLevelType = readCT_MultiLevelType(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tmpl': {
            tmpl = readCT_LongHexNumber(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'name': {
            name = readCT_String(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'styleLink': {
            styleLink = readCT_String(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'numStyleLink': {
            numStyleLink = readCT_String(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'lvl': {
            lvl.push(readCT_Lvl(cur, ctx));
            $slot = 6;
            $index = lvl.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AbstractNum', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AbstractNum', cur);
    cur.next();
  }

  return {
    nsid,
    multiLevelType,
    tmpl,
    name,
    styleLink,
    numStyleLink,
    lvl,
    abstractNumId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AltChunk`. See `transitional/wml.xsd:1929`. */
export function readCT_AltChunk(cur: XmlCursor, ctx: ReadContext): CT_AltChunk {
  const start = requireStart(cur, 'CT_AltChunk');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_AltChunk', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AltChunk', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let altChunkPr: CT_AltChunkPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AltChunk', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'altChunkPr': {
            altChunkPr = readCT_AltChunkPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AltChunk', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AltChunk', cur);
    cur.next();
  }

  return {
    altChunkPr,
    id,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AltChunkPr`. See `transitional/wml.xsd:1935`. */
export function readCT_AltChunkPr(cur: XmlCursor, ctx: ReadContext): CT_AltChunkPr {
  const start = requireStart(cur, 'CT_AltChunkPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AltChunkPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let matchSrc: CT_OnOff | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AltChunkPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'matchSrc': {
            matchSrc = readCT_OnOff(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AltChunkPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AltChunkPr', cur);
    cur.next();
  }

  return {
    matchSrc,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Attr`. See `transitional/wml.xsd:2172`. */
export function readCT_Attr(cur: XmlCursor, ctx: ReadContext): CT_Attr {
  const start = requireStart(cur, 'CT_Attr');
  let uri: ST_String | undefined;
  let name: ST_String | undefined;
  let val: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'uri': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            uri = v;
            continue;
          }
          ctx.invalidValue('CT_Attr', a, 'ST_String', cur);
          break;
        }
        case 'name': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_Attr', a, 'ST_String', cur);
          break;
        }
        case 'val': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Attr', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Attr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_Attr', 'name', cur);
  if (val === undefined) ctx.missingRequired('CT_Attr', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Attr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Attr', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Attr', cur);
    cur.next();
  }
  return {
    uri,
    name,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_AutoCaption`. See `transitional/wml.xsd:3602`. */
export function readCT_AutoCaption(cur: XmlCursor, ctx: ReadContext): CT_AutoCaption {
  const start = requireStart(cur, 'CT_AutoCaption');
  let name: ST_String | undefined;
  let caption: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'name': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_AutoCaption', a, 'ST_String', cur);
          break;
        }
        case 'caption': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            caption = v;
            continue;
          }
          ctx.invalidValue('CT_AutoCaption', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AutoCaption', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_AutoCaption', 'name', cur);
  if (caption === undefined) ctx.missingRequired('CT_AutoCaption', 'caption', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AutoCaption', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AutoCaption', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AutoCaption', cur);
    cur.next();
  }
  return {
    name,
    caption,
    $unknownAttrs,
  };
}

/** Read a `CT_AutoCaptions`. See `transitional/wml.xsd:3606`. */
export function readCT_AutoCaptions(cur: XmlCursor, ctx: ReadContext): CT_AutoCaptions {
  const start = requireStart(cur, 'CT_AutoCaptions');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AutoCaptions', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const autoCaption: CT_AutoCaption[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AutoCaptions', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'autoCaption': {
            autoCaption.push(readCT_AutoCaption(cur, ctx));
            $slot = 0;
            $index = autoCaption.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AutoCaptions', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AutoCaptions', cur);
    cur.next();
  }

  return {
    autoCaption,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Background`. See `transitional/wml.xsd:1149`. */
export function readCT_Background(cur: XmlCursor, ctx: ReadContext): CT_Background {
  const start = requireStart(cur, 'CT_Background');
  let color: ST_HexColor | undefined;
  let themeColor: ST_ThemeColor | undefined;
  let themeTint: ST_UcharHexNumber | undefined;
  let themeShade: ST_UcharHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'color': {
          const v = parseST_HexColor(a.value);
          if (v !== undefined) {
            color = v;
            continue;
          }
          ctx.invalidValue('CT_Background', a, 'ST_HexColor', cur);
          break;
        }
        case 'themeColor': {
          const v = parseST_ThemeColor(a.value);
          if (v !== undefined) {
            themeColor = v;
            continue;
          }
          ctx.invalidValue('CT_Background', a, 'ST_ThemeColor', cur);
          break;
        }
        case 'themeTint': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeTint = v;
            continue;
          }
          ctx.invalidValue('CT_Background', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'themeShade': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeShade = v;
            continue;
          }
          ctx.invalidValue('CT_Background', a, 'ST_UcharHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Background', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const any: RawNode[] = [];
  const any2: RawNode[] = [];
  let drawing: CT_Drawing | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Background', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'drawing': {
            drawing = readCT_Drawing(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === ctx.uris['vml']) {
        any.push(cur.skipToRaw());
        $slot = 0;
        $index = any.length - 1;
        continue;
      }
      if (ev.uri === ctx.uris['vml-office']) {
        any2.push(cur.skipToRaw());
        $slot = 1;
        $index = any2.length - 1;
        continue;
      }
      ctx.unexpectedElement('CT_Background', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Background', cur);
    cur.next();
  }

  return {
    any,
    any2,
    drawing,
    color,
    themeColor,
    themeTint,
    themeShade,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Base64Binary`. See `transitional/wml.xsd:2640`. */
export function readCT_Base64Binary(cur: XmlCursor, ctx: ReadContext): CT_Base64Binary {
  const start = requireStart(cur, 'CT_Base64Binary');
  let val: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val':
          val = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Base64Binary', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Base64Binary', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Base64Binary', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Base64Binary', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Base64Binary', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_BdoContentRun`. See `transitional/wml.xsd:2097`. */
export function readCT_BdoContentRun(cur: XmlCursor, ctx: ReadContext): CT_BdoContentRun {
  const start = requireStart(cur, 'CT_BdoContentRun');
  let val: ST_Direction | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Direction(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_BdoContentRun', a, 'ST_Direction', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BdoContentRun', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const pContent: CT_BdoContentRun_PContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BdoContentRun', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            pContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            pContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            pContent.push({ kind: 'customXml', value: readCT_CustomXmlRun(cur, ctx) });
            continue;
          }
          case 'smartTag': {
            pContent.push({ kind: 'smartTag', value: readCT_SmartTagRun(cur, ctx) });
            continue;
          }
          case 'sdt': {
            pContent.push({ kind: 'sdt', value: readCT_SdtRun(cur, ctx) });
            continue;
          }
          case 'dir': {
            pContent.push({ kind: 'dir', value: readCT_DirContentRun(cur, ctx) });
            continue;
          }
          case 'bdo': {
            pContent.push({ kind: 'bdo', value: readCT_BdoContentRun(cur, ctx) });
            continue;
          }
          case 'r': {
            pContent.push({ kind: 'r', value: readCT_R(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            pContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            pContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            pContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            pContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            pContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            pContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            pContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            pContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            pContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            pContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            pContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            pContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            pContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            pContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            pContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            pContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            pContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            pContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            pContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            pContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            pContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            pContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            pContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'fldSimple': {
            pContent.push({ kind: 'fldSimple', value: readCT_SimpleField(cur, ctx) });
            continue;
          }
          case 'hyperlink': {
            pContent.push({ kind: 'hyperlink', value: readCT_Hyperlink(cur, ctx) });
            continue;
          }
          case 'subDoc': {
            pContent.push({ kind: 'subDoc', value: readCT_Rel(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BdoContentRun', ev.localName, ev.uri, cur);
      pContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BdoContentRun', cur);
    cur.next();
  }

  return {
    pContent,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Body`. See `transitional/wml.xsd:3410`. */
export function readCT_Body(cur: XmlCursor, ctx: ReadContext): CT_Body {
  const start = requireStart(cur, 'CT_Body');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Body', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const blockLevelElts: CT_Body_BlockLevelElts[] = [];
  let sectPr: CT_SectPr | undefined;

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Body', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            blockLevelElts.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            blockLevelElts.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            blockLevelElts.push({ kind: 'customXml', value: readCT_CustomXmlBlock(cur, ctx) });
            continue;
          }
          case 'sdt': {
            blockLevelElts.push({ kind: 'sdt', value: readCT_SdtBlock(cur, ctx) });
            continue;
          }
          case 'p': {
            blockLevelElts.push({ kind: 'p', value: readCT_P(cur, ctx) });
            continue;
          }
          case 'tbl': {
            blockLevelElts.push({ kind: 'tbl', value: readCT_Tbl(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            blockLevelElts.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            blockLevelElts.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            blockLevelElts.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            blockLevelElts.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            blockLevelElts.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            blockLevelElts.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            blockLevelElts.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            blockLevelElts.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            blockLevelElts.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            blockLevelElts.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            blockLevelElts.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            blockLevelElts.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            blockLevelElts.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            blockLevelElts.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            blockLevelElts.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            blockLevelElts.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            blockLevelElts.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'altChunk': {
            blockLevelElts.push({ kind: 'altChunk', value: readCT_AltChunk(cur, ctx) });
            continue;
          }
          case 'sectPr': {
            sectPr = readCT_SectPr(cur, ctx);
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Body', ev.localName, ev.uri, cur);
      blockLevelElts.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Body', cur);
    cur.next();
  }

  return {
    blockLevelElts,
    sectPr,
    $unknownAttrs,
  };
}

/** Read a `CT_Bookmark`. See `transitional/wml.xsd:882`. */
export function readCT_Bookmark(cur: XmlCursor, ctx: ReadContext): CT_Bookmark {
  const start = requireStart(cur, 'CT_Bookmark');
  let id: ST_DecimalNumber | undefined;
  let displacedByCustomXml: ST_DisplacedByCustomXml | undefined;
  let colFirst: ST_DecimalNumber | undefined;
  let colLast: ST_DecimalNumber | undefined;
  let name: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Bookmark', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'displacedByCustomXml': {
          const v = parseST_DisplacedByCustomXml(a.value);
          if (v !== undefined) {
            displacedByCustomXml = v;
            continue;
          }
          ctx.invalidValue('CT_Bookmark', a, 'ST_DisplacedByCustomXml', cur);
          break;
        }
        case 'colFirst': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            colFirst = v;
            continue;
          }
          ctx.invalidValue('CT_Bookmark', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'colLast': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            colLast = v;
            continue;
          }
          ctx.invalidValue('CT_Bookmark', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'name': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_Bookmark', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Bookmark', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_Bookmark', 'id', cur);
  if (name === undefined) ctx.missingRequired('CT_Bookmark', 'name', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Bookmark', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Bookmark', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Bookmark', cur);
    cur.next();
  }
  return {
    id,
    displacedByCustomXml,
    colFirst,
    colLast,
    name,
    $unknownAttrs,
  };
}

/** Read a `CT_BookmarkRange`. See `transitional/wml.xsd:874`. */
export function readCT_BookmarkRange(cur: XmlCursor, ctx: ReadContext): CT_BookmarkRange {
  const start = requireStart(cur, 'CT_BookmarkRange');
  let id: ST_DecimalNumber | undefined;
  let displacedByCustomXml: ST_DisplacedByCustomXml | undefined;
  let colFirst: ST_DecimalNumber | undefined;
  let colLast: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_BookmarkRange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'displacedByCustomXml': {
          const v = parseST_DisplacedByCustomXml(a.value);
          if (v !== undefined) {
            displacedByCustomXml = v;
            continue;
          }
          ctx.invalidValue('CT_BookmarkRange', a, 'ST_DisplacedByCustomXml', cur);
          break;
        }
        case 'colFirst': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            colFirst = v;
            continue;
          }
          ctx.invalidValue('CT_BookmarkRange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'colLast': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            colLast = v;
            continue;
          }
          ctx.invalidValue('CT_BookmarkRange', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BookmarkRange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_BookmarkRange', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BookmarkRange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_BookmarkRange', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BookmarkRange', cur);
    cur.next();
  }
  return {
    id,
    displacedByCustomXml,
    colFirst,
    colLast,
    $unknownAttrs,
  };
}

/** Read a `CT_Border`. See `transitional/wml.xsd:414`. */
export function readCT_Border(cur: XmlCursor, ctx: ReadContext): CT_Border {
  const start = requireStart(cur, 'CT_Border');
  let val: ST_Border | undefined;
  let color: ST_HexColor | undefined;
  let themeColor: ST_ThemeColor | undefined;
  let themeTint: ST_UcharHexNumber | undefined;
  let themeShade: ST_UcharHexNumber | undefined;
  let sz: ST_EighthPointMeasure | undefined;
  let space: ST_PointMeasure | undefined;
  let shadow: ST_OnOff | undefined;
  let frame: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Border(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Border', a, 'ST_Border', cur);
          break;
        }
        case 'color': {
          const v = parseST_HexColor(a.value);
          if (v !== undefined) {
            color = v;
            continue;
          }
          ctx.invalidValue('CT_Border', a, 'ST_HexColor', cur);
          break;
        }
        case 'themeColor': {
          const v = parseST_ThemeColor(a.value);
          if (v !== undefined) {
            themeColor = v;
            continue;
          }
          ctx.invalidValue('CT_Border', a, 'ST_ThemeColor', cur);
          break;
        }
        case 'themeTint': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeTint = v;
            continue;
          }
          ctx.invalidValue('CT_Border', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'themeShade': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeShade = v;
            continue;
          }
          ctx.invalidValue('CT_Border', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'sz': {
          const v = parseST_EighthPointMeasure(a.value);
          if (v !== undefined) {
            sz = v;
            continue;
          }
          ctx.invalidValue('CT_Border', a, 'ST_EighthPointMeasure', cur);
          break;
        }
        case 'space': {
          const v = parseST_PointMeasure(a.value);
          if (v !== undefined) {
            space = v;
            continue;
          }
          ctx.invalidValue('CT_Border', a, 'ST_PointMeasure', cur);
          break;
        }
        case 'shadow': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            shadow = v;
            continue;
          }
          ctx.invalidValue('CT_Border', a, 'ST_OnOff', cur);
          break;
        }
        case 'frame': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            frame = v;
            continue;
          }
          ctx.invalidValue('CT_Border', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Border', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Border', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Border', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Border', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Border', cur);
    cur.next();
  }
  return {
    val,
    color,
    themeColor,
    themeTint,
    themeShade,
    sz,
    space,
    shadow,
    frame,
    $unknownAttrs,
  };
}

/** Read a `CT_BottomPageBorder`. See `transitional/wml.xsd:1495`. */
export function readCT_BottomPageBorder(cur: XmlCursor, ctx: ReadContext): CT_BottomPageBorder {
  const start = requireStart(cur, 'CT_BottomPageBorder');
  let val: ST_Border | undefined;
  let color: ST_HexColor | undefined;
  let themeColor: ST_ThemeColor | undefined;
  let themeTint: ST_UcharHexNumber | undefined;
  let themeShade: ST_UcharHexNumber | undefined;
  let sz: ST_EighthPointMeasure | undefined;
  let space: ST_PointMeasure | undefined;
  let shadow: ST_OnOff | undefined;
  let frame: ST_OnOff | undefined;
  let id: ST_RelationshipId | undefined;
  let bottomLeft: ST_RelationshipId | undefined;
  let bottomRight: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_BottomPageBorder', a, 'ST_RelationshipId', cur);
          break;
        }
        case 'bottomLeft': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            bottomLeft = v;
            continue;
          }
          ctx.invalidValue('CT_BottomPageBorder', a, 'ST_RelationshipId', cur);
          break;
        }
        case 'bottomRight': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            bottomRight = v;
            continue;
          }
          ctx.invalidValue('CT_BottomPageBorder', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Border(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_BottomPageBorder', a, 'ST_Border', cur);
          break;
        }
        case 'color': {
          const v = parseST_HexColor(a.value);
          if (v !== undefined) {
            color = v;
            continue;
          }
          ctx.invalidValue('CT_BottomPageBorder', a, 'ST_HexColor', cur);
          break;
        }
        case 'themeColor': {
          const v = parseST_ThemeColor(a.value);
          if (v !== undefined) {
            themeColor = v;
            continue;
          }
          ctx.invalidValue('CT_BottomPageBorder', a, 'ST_ThemeColor', cur);
          break;
        }
        case 'themeTint': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeTint = v;
            continue;
          }
          ctx.invalidValue('CT_BottomPageBorder', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'themeShade': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeShade = v;
            continue;
          }
          ctx.invalidValue('CT_BottomPageBorder', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'sz': {
          const v = parseST_EighthPointMeasure(a.value);
          if (v !== undefined) {
            sz = v;
            continue;
          }
          ctx.invalidValue('CT_BottomPageBorder', a, 'ST_EighthPointMeasure', cur);
          break;
        }
        case 'space': {
          const v = parseST_PointMeasure(a.value);
          if (v !== undefined) {
            space = v;
            continue;
          }
          ctx.invalidValue('CT_BottomPageBorder', a, 'ST_PointMeasure', cur);
          break;
        }
        case 'shadow': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            shadow = v;
            continue;
          }
          ctx.invalidValue('CT_BottomPageBorder', a, 'ST_OnOff', cur);
          break;
        }
        case 'frame': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            frame = v;
            continue;
          }
          ctx.invalidValue('CT_BottomPageBorder', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BottomPageBorder', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_BottomPageBorder', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BottomPageBorder', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_BottomPageBorder', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BottomPageBorder', cur);
    cur.next();
  }
  return {
    val,
    color,
    themeColor,
    themeTint,
    themeShade,
    sz,
    space,
    shadow,
    frame,
    id,
    bottomLeft,
    bottomRight,
    $unknownAttrs,
  };
}

/** Read a `CT_Br`. See `transitional/wml.xsd:1665`. */
export function readCT_Br(cur: XmlCursor, ctx: ReadContext): CT_Br {
  const start = requireStart(cur, 'CT_Br');
  let type: ST_BrType | undefined;
  let clear: ST_BrClear | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'type': {
          const v = parseST_BrType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Br', a, 'ST_BrType', cur);
          break;
        }
        case 'clear': {
          const v = parseST_BrClear(a.value);
          if (v !== undefined) {
            clear = v;
            continue;
          }
          ctx.invalidValue('CT_Br', a, 'ST_BrClear', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Br', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Br', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Br', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Br', cur);
    cur.next();
  }
  return {
    type,
    clear,
    $unknownAttrs,
  };
}

/** Read a `CT_CalendarType`. See `transitional/wml.xsd:2004`. */
export function readCT_CalendarType(cur: XmlCursor, ctx: ReadContext): CT_CalendarType {
  const start = requireStart(cur, 'CT_CalendarType');
  let val: ST_CalendarType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_CalendarType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_CalendarType', a, 'ST_CalendarType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CalendarType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CalendarType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_CalendarType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CalendarType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Caption`. See `transitional/wml.xsd:3593`. */
export function readCT_Caption(cur: XmlCursor, ctx: ReadContext): CT_Caption {
  const start = requireStart(cur, 'CT_Caption');
  let name: ST_String | undefined;
  let pos: ST_CaptionPos | undefined;
  let chapNum: ST_OnOff | undefined;
  let heading: ST_DecimalNumber | undefined;
  let noLabel: ST_OnOff | undefined;
  let numFmt: ST_NumberFormat | undefined;
  let sep: ST_ChapterSep | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'name': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_Caption', a, 'ST_String', cur);
          break;
        }
        case 'pos': {
          const v = parseST_CaptionPos(a.value);
          if (v !== undefined) {
            pos = v;
            continue;
          }
          ctx.invalidValue('CT_Caption', a, 'ST_CaptionPos', cur);
          break;
        }
        case 'chapNum': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            chapNum = v;
            continue;
          }
          ctx.invalidValue('CT_Caption', a, 'ST_OnOff', cur);
          break;
        }
        case 'heading': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            heading = v;
            continue;
          }
          ctx.invalidValue('CT_Caption', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'noLabel': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            noLabel = v;
            continue;
          }
          ctx.invalidValue('CT_Caption', a, 'ST_OnOff', cur);
          break;
        }
        case 'numFmt': {
          const v = parseST_NumberFormat(a.value);
          if (v !== undefined) {
            numFmt = v;
            continue;
          }
          ctx.invalidValue('CT_Caption', a, 'ST_NumberFormat', cur);
          break;
        }
        case 'sep': {
          const v = parseST_ChapterSep(a.value);
          if (v !== undefined) {
            sep = v;
            continue;
          }
          ctx.invalidValue('CT_Caption', a, 'ST_ChapterSep', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Caption', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_Caption', 'name', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Caption', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Caption', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Caption', cur);
    cur.next();
  }
  return {
    name,
    pos,
    chapNum,
    heading,
    noLabel,
    numFmt,
    sep,
    $unknownAttrs,
  };
}

/** Read a `CT_Captions`. See `transitional/wml.xsd:3611`. */
export function readCT_Captions(cur: XmlCursor, ctx: ReadContext): CT_Captions {
  const start = requireStart(cur, 'CT_Captions');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Captions', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const caption: CT_Caption[] = [];
  let autoCaptions: CT_AutoCaptions | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Captions', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'caption': {
            caption.push(readCT_Caption(cur, ctx));
            $slot = 0;
            $index = caption.length - 1;
            continue;
          }
          case 'autoCaptions': {
            autoCaptions = readCT_AutoCaptions(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Captions', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Captions', cur);
    cur.next();
  }

  return {
    caption,
    autoCaptions,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CellMergeTrackChange`. See `transitional/wml.xsd:852`. */
export function readCT_CellMergeTrackChange(cur: XmlCursor, ctx: ReadContext): CT_CellMergeTrackChange {
  const start = requireStart(cur, 'CT_CellMergeTrackChange');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let vMerge: ST_AnnotationVMerge | undefined;
  let vMergeOrig: ST_AnnotationVMerge | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_CellMergeTrackChange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_CellMergeTrackChange', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_CellMergeTrackChange', a, 'ST_DateTime', cur);
          break;
        }
        case 'vMerge': {
          const v = parseST_AnnotationVMerge(a.value);
          if (v !== undefined) {
            vMerge = v;
            continue;
          }
          ctx.invalidValue('CT_CellMergeTrackChange', a, 'ST_AnnotationVMerge', cur);
          break;
        }
        case 'vMergeOrig': {
          const v = parseST_AnnotationVMerge(a.value);
          if (v !== undefined) {
            vMergeOrig = v;
            continue;
          }
          ctx.invalidValue('CT_CellMergeTrackChange', a, 'ST_AnnotationVMerge', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CellMergeTrackChange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_CellMergeTrackChange', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_CellMergeTrackChange', 'author', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CellMergeTrackChange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_CellMergeTrackChange', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CellMergeTrackChange', cur);
    cur.next();
  }
  return {
    id,
    author,
    date,
    vMerge,
    vMergeOrig,
    $unknownAttrs,
  };
}

/** Read a `CT_CharacterSpacing`. See `transitional/wml.xsd:2825`. */
export function readCT_CharacterSpacing(cur: XmlCursor, ctx: ReadContext): CT_CharacterSpacing {
  const start = requireStart(cur, 'CT_CharacterSpacing');
  let val: ST_CharacterSpacing | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_CharacterSpacing(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_CharacterSpacing', a, 'ST_CharacterSpacing', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CharacterSpacing', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_CharacterSpacing', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CharacterSpacing', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_CharacterSpacing', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CharacterSpacing', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Charset`. See `transitional/wml.xsd:44`. */
export function readCT_Charset(cur: XmlCursor, ctx: ReadContext): CT_Charset {
  const start = requireStart(cur, 'CT_Charset');
  let val: ST_UcharHexNumber | undefined;
  let characterSet: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Charset', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'characterSet': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            characterSet = v;
            continue;
          }
          ctx.invalidValue('CT_Charset', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Charset', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Charset', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Charset', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Charset', cur);
    cur.next();
  }
  return {
    val,
    characterSet,
    $unknownAttrs,
  };
}

/** Read a `CT_Cnf`. See `transitional/wml.xsd:2368`. */
export function readCT_Cnf(cur: XmlCursor, ctx: ReadContext): CT_Cnf {
  const start = requireStart(cur, 'CT_Cnf');
  let val: ST_Cnf | undefined;
  let firstRow: ST_OnOff | undefined;
  let lastRow: ST_OnOff | undefined;
  let firstColumn: ST_OnOff | undefined;
  let lastColumn: ST_OnOff | undefined;
  let oddVBand: ST_OnOff | undefined;
  let evenVBand: ST_OnOff | undefined;
  let oddHBand: ST_OnOff | undefined;
  let evenHBand: ST_OnOff | undefined;
  let firstRowFirstColumn: ST_OnOff | undefined;
  let firstRowLastColumn: ST_OnOff | undefined;
  let lastRowFirstColumn: ST_OnOff | undefined;
  let lastRowLastColumn: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Cnf(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_Cnf', cur);
          break;
        }
        case 'firstRow': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            firstRow = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_OnOff', cur);
          break;
        }
        case 'lastRow': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            lastRow = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_OnOff', cur);
          break;
        }
        case 'firstColumn': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            firstColumn = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_OnOff', cur);
          break;
        }
        case 'lastColumn': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            lastColumn = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_OnOff', cur);
          break;
        }
        case 'oddVBand': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            oddVBand = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_OnOff', cur);
          break;
        }
        case 'evenVBand': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            evenVBand = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_OnOff', cur);
          break;
        }
        case 'oddHBand': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            oddHBand = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_OnOff', cur);
          break;
        }
        case 'evenHBand': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            evenHBand = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_OnOff', cur);
          break;
        }
        case 'firstRowFirstColumn': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            firstRowFirstColumn = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_OnOff', cur);
          break;
        }
        case 'firstRowLastColumn': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            firstRowLastColumn = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_OnOff', cur);
          break;
        }
        case 'lastRowFirstColumn': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            lastRowFirstColumn = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_OnOff', cur);
          break;
        }
        case 'lastRowLastColumn': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            lastRowLastColumn = v;
            continue;
          }
          ctx.invalidValue('CT_Cnf', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Cnf', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Cnf', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Cnf', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Cnf', cur);
    cur.next();
  }
  return {
    val,
    firstRow,
    lastRow,
    firstColumn,
    lastColumn,
    oddVBand,
    evenVBand,
    oddHBand,
    evenHBand,
    firstRowFirstColumn,
    firstRowLastColumn,
    lastRowFirstColumn,
    lastRowLastColumn,
    $unknownAttrs,
  };
}

/** Read a `CT_Color`. See `transitional/wml.xsd:162`. */
export function readCT_Color(cur: XmlCursor, ctx: ReadContext): CT_Color {
  const start = requireStart(cur, 'CT_Color');
  let val: ST_HexColor | undefined;
  let themeColor: ST_ThemeColor | undefined;
  let themeTint: ST_UcharHexNumber | undefined;
  let themeShade: ST_UcharHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_HexColor(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Color', a, 'ST_HexColor', cur);
          break;
        }
        case 'themeColor': {
          const v = parseST_ThemeColor(a.value);
          if (v !== undefined) {
            themeColor = v;
            continue;
          }
          ctx.invalidValue('CT_Color', a, 'ST_ThemeColor', cur);
          break;
        }
        case 'themeTint': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeTint = v;
            continue;
          }
          ctx.invalidValue('CT_Color', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'themeShade': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeShade = v;
            continue;
          }
          ctx.invalidValue('CT_Color', a, 'ST_UcharHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Color', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Color', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Color', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Color', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Color', cur);
    cur.next();
  }
  return {
    val,
    themeColor,
    themeTint,
    themeShade,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorSchemeMapping`. See `transitional/wml.xsd:2864`. */
export function readCT_ColorSchemeMapping(cur: XmlCursor, ctx: ReadContext): CT_ColorSchemeMapping {
  const start = requireStart(cur, 'CT_ColorSchemeMapping');
  let bg1: ST_WmlColorSchemeIndex | undefined;
  let t1: ST_WmlColorSchemeIndex | undefined;
  let bg2: ST_WmlColorSchemeIndex | undefined;
  let t2: ST_WmlColorSchemeIndex | undefined;
  let accent1: ST_WmlColorSchemeIndex | undefined;
  let accent2: ST_WmlColorSchemeIndex | undefined;
  let accent3: ST_WmlColorSchemeIndex | undefined;
  let accent4: ST_WmlColorSchemeIndex | undefined;
  let accent5: ST_WmlColorSchemeIndex | undefined;
  let accent6: ST_WmlColorSchemeIndex | undefined;
  let hyperlink: ST_WmlColorSchemeIndex | undefined;
  let followedHyperlink: ST_WmlColorSchemeIndex | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'bg1': {
          const v = parseST_WmlColorSchemeIndex(a.value);
          if (v !== undefined) {
            bg1 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorSchemeMapping', a, 'ST_WmlColorSchemeIndex', cur);
          break;
        }
        case 't1': {
          const v = parseST_WmlColorSchemeIndex(a.value);
          if (v !== undefined) {
            t1 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorSchemeMapping', a, 'ST_WmlColorSchemeIndex', cur);
          break;
        }
        case 'bg2': {
          const v = parseST_WmlColorSchemeIndex(a.value);
          if (v !== undefined) {
            bg2 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorSchemeMapping', a, 'ST_WmlColorSchemeIndex', cur);
          break;
        }
        case 't2': {
          const v = parseST_WmlColorSchemeIndex(a.value);
          if (v !== undefined) {
            t2 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorSchemeMapping', a, 'ST_WmlColorSchemeIndex', cur);
          break;
        }
        case 'accent1': {
          const v = parseST_WmlColorSchemeIndex(a.value);
          if (v !== undefined) {
            accent1 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorSchemeMapping', a, 'ST_WmlColorSchemeIndex', cur);
          break;
        }
        case 'accent2': {
          const v = parseST_WmlColorSchemeIndex(a.value);
          if (v !== undefined) {
            accent2 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorSchemeMapping', a, 'ST_WmlColorSchemeIndex', cur);
          break;
        }
        case 'accent3': {
          const v = parseST_WmlColorSchemeIndex(a.value);
          if (v !== undefined) {
            accent3 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorSchemeMapping', a, 'ST_WmlColorSchemeIndex', cur);
          break;
        }
        case 'accent4': {
          const v = parseST_WmlColorSchemeIndex(a.value);
          if (v !== undefined) {
            accent4 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorSchemeMapping', a, 'ST_WmlColorSchemeIndex', cur);
          break;
        }
        case 'accent5': {
          const v = parseST_WmlColorSchemeIndex(a.value);
          if (v !== undefined) {
            accent5 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorSchemeMapping', a, 'ST_WmlColorSchemeIndex', cur);
          break;
        }
        case 'accent6': {
          const v = parseST_WmlColorSchemeIndex(a.value);
          if (v !== undefined) {
            accent6 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorSchemeMapping', a, 'ST_WmlColorSchemeIndex', cur);
          break;
        }
        case 'hyperlink': {
          const v = parseST_WmlColorSchemeIndex(a.value);
          if (v !== undefined) {
            hyperlink = v;
            continue;
          }
          ctx.invalidValue('CT_ColorSchemeMapping', a, 'ST_WmlColorSchemeIndex', cur);
          break;
        }
        case 'followedHyperlink': {
          const v = parseST_WmlColorSchemeIndex(a.value);
          if (v !== undefined) {
            followedHyperlink = v;
            continue;
          }
          ctx.invalidValue('CT_ColorSchemeMapping', a, 'ST_WmlColorSchemeIndex', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ColorSchemeMapping', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorSchemeMapping', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ColorSchemeMapping', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorSchemeMapping', cur);
    cur.next();
  }
  return {
    bg1,
    t1,
    bg2,
    t2,
    accent1,
    accent2,
    accent3,
    accent4,
    accent5,
    accent6,
    hyperlink,
    followedHyperlink,
    $unknownAttrs,
  };
}

/** Read a `CT_Column`. See `transitional/wml.xsd:1539`. */
export function readCT_Column(cur: XmlCursor, ctx: ReadContext): CT_Column {
  const start = requireStart(cur, 'CT_Column');
  let w: ST_TwipsMeasure | undefined;
  let space: ST_TwipsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'w': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            w = v;
            continue;
          }
          ctx.invalidValue('CT_Column', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'space': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            space = v;
            continue;
          }
          ctx.invalidValue('CT_Column', a, 'ST_TwipsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Column', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Column', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Column', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Column', cur);
    cur.next();
  }
  return {
    w,
    space,
    $unknownAttrs,
  };
}

/** Read a `CT_Columns`. See `transitional/wml.xsd:1543`. */
export function readCT_Columns(cur: XmlCursor, ctx: ReadContext): CT_Columns {
  const start = requireStart(cur, 'CT_Columns');
  let equalWidth: ST_OnOff | undefined;
  let space: ST_TwipsMeasure | undefined;
  let num: ST_DecimalNumber | undefined;
  let sep: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'equalWidth': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            equalWidth = v;
            continue;
          }
          ctx.invalidValue('CT_Columns', a, 'ST_OnOff', cur);
          break;
        }
        case 'space': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            space = v;
            continue;
          }
          ctx.invalidValue('CT_Columns', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'num': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            num = v;
            continue;
          }
          ctx.invalidValue('CT_Columns', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'sep': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            sep = v;
            continue;
          }
          ctx.invalidValue('CT_Columns', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Columns', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const col: CT_Column[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Columns', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'col': {
            col.push(readCT_Column(cur, ctx));
            $slot = 0;
            $index = col.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Columns', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Columns', cur);
    cur.next();
  }

  return {
    col,
    equalWidth,
    space,
    num,
    sep,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Comment`. See `transitional/wml.xsd:897`. */
export function readCT_Comment(cur: XmlCursor, ctx: ReadContext): CT_Comment {
  const start = requireStart(cur, 'CT_Comment');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let initials: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Comment', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_Comment', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_Comment', a, 'ST_DateTime', cur);
          break;
        }
        case 'initials': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            initials = v;
            continue;
          }
          ctx.invalidValue('CT_Comment', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Comment', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_Comment', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_Comment', 'author', cur);

  const blockLevelElts: CT_Comment_BlockLevelElts[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Comment', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            blockLevelElts.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            blockLevelElts.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            blockLevelElts.push({ kind: 'customXml', value: readCT_CustomXmlBlock(cur, ctx) });
            continue;
          }
          case 'sdt': {
            blockLevelElts.push({ kind: 'sdt', value: readCT_SdtBlock(cur, ctx) });
            continue;
          }
          case 'p': {
            blockLevelElts.push({ kind: 'p', value: readCT_P(cur, ctx) });
            continue;
          }
          case 'tbl': {
            blockLevelElts.push({ kind: 'tbl', value: readCT_Tbl(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            blockLevelElts.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            blockLevelElts.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            blockLevelElts.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            blockLevelElts.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            blockLevelElts.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            blockLevelElts.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            blockLevelElts.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            blockLevelElts.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            blockLevelElts.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            blockLevelElts.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            blockLevelElts.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            blockLevelElts.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            blockLevelElts.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            blockLevelElts.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            blockLevelElts.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            blockLevelElts.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            blockLevelElts.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'altChunk': {
            blockLevelElts.push({ kind: 'altChunk', value: readCT_AltChunk(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Comment', ev.localName, ev.uri, cur);
      blockLevelElts.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Comment', cur);
    cur.next();
  }

  return {
    blockLevelElts,
    id,
    author,
    date,
    initials,
    $unknownAttrs,
  };
}

/** Read a `CT_Comments`. See `transitional/wml.xsd:3422`. */
export function readCT_Comments(cur: XmlCursor, ctx: ReadContext): CT_Comments {
  const start = requireStart(cur, 'CT_Comments');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Comments', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const comment: CT_Comment[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Comments', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'comment': {
            comment.push(readCT_Comment(cur, ctx));
            $slot = 0;
            $index = comment.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Comments', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Comments', cur);
    cur.next();
  }

  return {
    comment,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Compat`. See `transitional/wml.xsd:2727`. */
export function readCT_Compat(cur: XmlCursor, ctx: ReadContext): CT_Compat {
  const start = requireStart(cur, 'CT_Compat');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Compat', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let useSingleBorderforContiguousCells: CT_OnOff | undefined;
  let wpJustification: CT_OnOff | undefined;
  let noTabHangInd: CT_OnOff | undefined;
  let noLeading: CT_OnOff | undefined;
  let spaceForUL: CT_OnOff | undefined;
  let noColumnBalance: CT_OnOff | undefined;
  let balanceSingleByteDoubleByteWidth: CT_OnOff | undefined;
  let noExtraLineSpacing: CT_OnOff | undefined;
  let doNotLeaveBackslashAlone: CT_OnOff | undefined;
  let ulTrailSpace: CT_OnOff | undefined;
  let doNotExpandShiftReturn: CT_OnOff | undefined;
  let spacingInWholePoints: CT_OnOff | undefined;
  let lineWrapLikeWord6: CT_OnOff | undefined;
  let printBodyTextBeforeHeader: CT_OnOff | undefined;
  let printColBlack: CT_OnOff | undefined;
  let wpSpaceWidth: CT_OnOff | undefined;
  let showBreaksInFrames: CT_OnOff | undefined;
  let subFontBySize: CT_OnOff | undefined;
  let suppressBottomSpacing: CT_OnOff | undefined;
  let suppressTopSpacing: CT_OnOff | undefined;
  let suppressSpacingAtTopOfPage: CT_OnOff | undefined;
  let suppressTopSpacingWP: CT_OnOff | undefined;
  let suppressSpBfAfterPgBrk: CT_OnOff | undefined;
  let swapBordersFacingPages: CT_OnOff | undefined;
  let convMailMergeEsc: CT_OnOff | undefined;
  let truncateFontHeightsLikeWP6: CT_OnOff | undefined;
  let mwSmallCaps: CT_OnOff | undefined;
  let usePrinterMetrics: CT_OnOff | undefined;
  let doNotSuppressParagraphBorders: CT_OnOff | undefined;
  let wrapTrailSpaces: CT_OnOff | undefined;
  let footnoteLayoutLikeWW8: CT_OnOff | undefined;
  let shapeLayoutLikeWW8: CT_OnOff | undefined;
  let alignTablesRowByRow: CT_OnOff | undefined;
  let forgetLastTabAlignment: CT_OnOff | undefined;
  let adjustLineHeightInTable: CT_OnOff | undefined;
  let autoSpaceLikeWord95: CT_OnOff | undefined;
  let noSpaceRaiseLower: CT_OnOff | undefined;
  let doNotUseHTMLParagraphAutoSpacing: CT_OnOff | undefined;
  let layoutRawTableWidth: CT_OnOff | undefined;
  let layoutTableRowsApart: CT_OnOff | undefined;
  let useWord97LineBreakRules: CT_OnOff | undefined;
  let doNotBreakWrappedTables: CT_OnOff | undefined;
  let doNotSnapToGridInCell: CT_OnOff | undefined;
  let selectFldWithFirstOrLastChar: CT_OnOff | undefined;
  let applyBreakingRules: CT_OnOff | undefined;
  let doNotWrapTextWithPunct: CT_OnOff | undefined;
  let doNotUseEastAsianBreakRules: CT_OnOff | undefined;
  let useWord2002TableStyleRules: CT_OnOff | undefined;
  let growAutofit: CT_OnOff | undefined;
  let useFELayout: CT_OnOff | undefined;
  let useNormalStyleForList: CT_OnOff | undefined;
  let doNotUseIndentAsNumberingTabStop: CT_OnOff | undefined;
  let useAltKinsokuLineBreakRules: CT_OnOff | undefined;
  let allowSpaceOfSameStyleInTable: CT_OnOff | undefined;
  let doNotSuppressIndentation: CT_OnOff | undefined;
  let doNotAutofitConstrainedTables: CT_OnOff | undefined;
  let autofitToFirstFixedWidthCell: CT_OnOff | undefined;
  let underlineTabInNumList: CT_OnOff | undefined;
  let displayHangulFixedWidth: CT_OnOff | undefined;
  let splitPgBreakAndParaMark: CT_OnOff | undefined;
  let doNotVertAlignCellWithSp: CT_OnOff | undefined;
  let doNotBreakConstrainedForcedTable: CT_OnOff | undefined;
  let doNotVertAlignInTxbx: CT_OnOff | undefined;
  let useAnsiKerningPairs: CT_OnOff | undefined;
  let cachedColBalance: CT_OnOff | undefined;
  const compatSetting: CT_CompatSetting[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Compat', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'useSingleBorderforContiguousCells': {
            useSingleBorderforContiguousCells = readCT_OnOff(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'wpJustification': {
            wpJustification = readCT_OnOff(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'noTabHangInd': {
            noTabHangInd = readCT_OnOff(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'noLeading': {
            noLeading = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'spaceForUL': {
            spaceForUL = readCT_OnOff(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'noColumnBalance': {
            noColumnBalance = readCT_OnOff(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'balanceSingleByteDoubleByteWidth': {
            balanceSingleByteDoubleByteWidth = readCT_OnOff(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'noExtraLineSpacing': {
            noExtraLineSpacing = readCT_OnOff(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'doNotLeaveBackslashAlone': {
            doNotLeaveBackslashAlone = readCT_OnOff(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'ulTrailSpace': {
            ulTrailSpace = readCT_OnOff(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'doNotExpandShiftReturn': {
            doNotExpandShiftReturn = readCT_OnOff(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'spacingInWholePoints': {
            spacingInWholePoints = readCT_OnOff(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'lineWrapLikeWord6': {
            lineWrapLikeWord6 = readCT_OnOff(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'printBodyTextBeforeHeader': {
            printBodyTextBeforeHeader = readCT_OnOff(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'printColBlack': {
            printColBlack = readCT_OnOff(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'wpSpaceWidth': {
            wpSpaceWidth = readCT_OnOff(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'showBreaksInFrames': {
            showBreaksInFrames = readCT_OnOff(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'subFontBySize': {
            subFontBySize = readCT_OnOff(cur, ctx);
            $slot = 17;
            $index = undefined;
            continue;
          }
          case 'suppressBottomSpacing': {
            suppressBottomSpacing = readCT_OnOff(cur, ctx);
            $slot = 18;
            $index = undefined;
            continue;
          }
          case 'suppressTopSpacing': {
            suppressTopSpacing = readCT_OnOff(cur, ctx);
            $slot = 19;
            $index = undefined;
            continue;
          }
          case 'suppressSpacingAtTopOfPage': {
            suppressSpacingAtTopOfPage = readCT_OnOff(cur, ctx);
            $slot = 20;
            $index = undefined;
            continue;
          }
          case 'suppressTopSpacingWP': {
            suppressTopSpacingWP = readCT_OnOff(cur, ctx);
            $slot = 21;
            $index = undefined;
            continue;
          }
          case 'suppressSpBfAfterPgBrk': {
            suppressSpBfAfterPgBrk = readCT_OnOff(cur, ctx);
            $slot = 22;
            $index = undefined;
            continue;
          }
          case 'swapBordersFacingPages': {
            swapBordersFacingPages = readCT_OnOff(cur, ctx);
            $slot = 23;
            $index = undefined;
            continue;
          }
          case 'convMailMergeEsc': {
            convMailMergeEsc = readCT_OnOff(cur, ctx);
            $slot = 24;
            $index = undefined;
            continue;
          }
          case 'truncateFontHeightsLikeWP6': {
            truncateFontHeightsLikeWP6 = readCT_OnOff(cur, ctx);
            $slot = 25;
            $index = undefined;
            continue;
          }
          case 'mwSmallCaps': {
            mwSmallCaps = readCT_OnOff(cur, ctx);
            $slot = 26;
            $index = undefined;
            continue;
          }
          case 'usePrinterMetrics': {
            usePrinterMetrics = readCT_OnOff(cur, ctx);
            $slot = 27;
            $index = undefined;
            continue;
          }
          case 'doNotSuppressParagraphBorders': {
            doNotSuppressParagraphBorders = readCT_OnOff(cur, ctx);
            $slot = 28;
            $index = undefined;
            continue;
          }
          case 'wrapTrailSpaces': {
            wrapTrailSpaces = readCT_OnOff(cur, ctx);
            $slot = 29;
            $index = undefined;
            continue;
          }
          case 'footnoteLayoutLikeWW8': {
            footnoteLayoutLikeWW8 = readCT_OnOff(cur, ctx);
            $slot = 30;
            $index = undefined;
            continue;
          }
          case 'shapeLayoutLikeWW8': {
            shapeLayoutLikeWW8 = readCT_OnOff(cur, ctx);
            $slot = 31;
            $index = undefined;
            continue;
          }
          case 'alignTablesRowByRow': {
            alignTablesRowByRow = readCT_OnOff(cur, ctx);
            $slot = 32;
            $index = undefined;
            continue;
          }
          case 'forgetLastTabAlignment': {
            forgetLastTabAlignment = readCT_OnOff(cur, ctx);
            $slot = 33;
            $index = undefined;
            continue;
          }
          case 'adjustLineHeightInTable': {
            adjustLineHeightInTable = readCT_OnOff(cur, ctx);
            $slot = 34;
            $index = undefined;
            continue;
          }
          case 'autoSpaceLikeWord95': {
            autoSpaceLikeWord95 = readCT_OnOff(cur, ctx);
            $slot = 35;
            $index = undefined;
            continue;
          }
          case 'noSpaceRaiseLower': {
            noSpaceRaiseLower = readCT_OnOff(cur, ctx);
            $slot = 36;
            $index = undefined;
            continue;
          }
          case 'doNotUseHTMLParagraphAutoSpacing': {
            doNotUseHTMLParagraphAutoSpacing = readCT_OnOff(cur, ctx);
            $slot = 37;
            $index = undefined;
            continue;
          }
          case 'layoutRawTableWidth': {
            layoutRawTableWidth = readCT_OnOff(cur, ctx);
            $slot = 38;
            $index = undefined;
            continue;
          }
          case 'layoutTableRowsApart': {
            layoutTableRowsApart = readCT_OnOff(cur, ctx);
            $slot = 39;
            $index = undefined;
            continue;
          }
          case 'useWord97LineBreakRules': {
            useWord97LineBreakRules = readCT_OnOff(cur, ctx);
            $slot = 40;
            $index = undefined;
            continue;
          }
          case 'doNotBreakWrappedTables': {
            doNotBreakWrappedTables = readCT_OnOff(cur, ctx);
            $slot = 41;
            $index = undefined;
            continue;
          }
          case 'doNotSnapToGridInCell': {
            doNotSnapToGridInCell = readCT_OnOff(cur, ctx);
            $slot = 42;
            $index = undefined;
            continue;
          }
          case 'selectFldWithFirstOrLastChar': {
            selectFldWithFirstOrLastChar = readCT_OnOff(cur, ctx);
            $slot = 43;
            $index = undefined;
            continue;
          }
          case 'applyBreakingRules': {
            applyBreakingRules = readCT_OnOff(cur, ctx);
            $slot = 44;
            $index = undefined;
            continue;
          }
          case 'doNotWrapTextWithPunct': {
            doNotWrapTextWithPunct = readCT_OnOff(cur, ctx);
            $slot = 45;
            $index = undefined;
            continue;
          }
          case 'doNotUseEastAsianBreakRules': {
            doNotUseEastAsianBreakRules = readCT_OnOff(cur, ctx);
            $slot = 46;
            $index = undefined;
            continue;
          }
          case 'useWord2002TableStyleRules': {
            useWord2002TableStyleRules = readCT_OnOff(cur, ctx);
            $slot = 47;
            $index = undefined;
            continue;
          }
          case 'growAutofit': {
            growAutofit = readCT_OnOff(cur, ctx);
            $slot = 48;
            $index = undefined;
            continue;
          }
          case 'useFELayout': {
            useFELayout = readCT_OnOff(cur, ctx);
            $slot = 49;
            $index = undefined;
            continue;
          }
          case 'useNormalStyleForList': {
            useNormalStyleForList = readCT_OnOff(cur, ctx);
            $slot = 50;
            $index = undefined;
            continue;
          }
          case 'doNotUseIndentAsNumberingTabStop': {
            doNotUseIndentAsNumberingTabStop = readCT_OnOff(cur, ctx);
            $slot = 51;
            $index = undefined;
            continue;
          }
          case 'useAltKinsokuLineBreakRules': {
            useAltKinsokuLineBreakRules = readCT_OnOff(cur, ctx);
            $slot = 52;
            $index = undefined;
            continue;
          }
          case 'allowSpaceOfSameStyleInTable': {
            allowSpaceOfSameStyleInTable = readCT_OnOff(cur, ctx);
            $slot = 53;
            $index = undefined;
            continue;
          }
          case 'doNotSuppressIndentation': {
            doNotSuppressIndentation = readCT_OnOff(cur, ctx);
            $slot = 54;
            $index = undefined;
            continue;
          }
          case 'doNotAutofitConstrainedTables': {
            doNotAutofitConstrainedTables = readCT_OnOff(cur, ctx);
            $slot = 55;
            $index = undefined;
            continue;
          }
          case 'autofitToFirstFixedWidthCell': {
            autofitToFirstFixedWidthCell = readCT_OnOff(cur, ctx);
            $slot = 56;
            $index = undefined;
            continue;
          }
          case 'underlineTabInNumList': {
            underlineTabInNumList = readCT_OnOff(cur, ctx);
            $slot = 57;
            $index = undefined;
            continue;
          }
          case 'displayHangulFixedWidth': {
            displayHangulFixedWidth = readCT_OnOff(cur, ctx);
            $slot = 58;
            $index = undefined;
            continue;
          }
          case 'splitPgBreakAndParaMark': {
            splitPgBreakAndParaMark = readCT_OnOff(cur, ctx);
            $slot = 59;
            $index = undefined;
            continue;
          }
          case 'doNotVertAlignCellWithSp': {
            doNotVertAlignCellWithSp = readCT_OnOff(cur, ctx);
            $slot = 60;
            $index = undefined;
            continue;
          }
          case 'doNotBreakConstrainedForcedTable': {
            doNotBreakConstrainedForcedTable = readCT_OnOff(cur, ctx);
            $slot = 61;
            $index = undefined;
            continue;
          }
          case 'doNotVertAlignInTxbx': {
            doNotVertAlignInTxbx = readCT_OnOff(cur, ctx);
            $slot = 62;
            $index = undefined;
            continue;
          }
          case 'useAnsiKerningPairs': {
            useAnsiKerningPairs = readCT_OnOff(cur, ctx);
            $slot = 63;
            $index = undefined;
            continue;
          }
          case 'cachedColBalance': {
            cachedColBalance = readCT_OnOff(cur, ctx);
            $slot = 64;
            $index = undefined;
            continue;
          }
          case 'compatSetting': {
            compatSetting.push(readCT_CompatSetting(cur, ctx));
            $slot = 65;
            $index = compatSetting.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Compat', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Compat', cur);
    cur.next();
  }

  return {
    useSingleBorderforContiguousCells,
    wpJustification,
    noTabHangInd,
    noLeading,
    spaceForUL,
    noColumnBalance,
    balanceSingleByteDoubleByteWidth,
    noExtraLineSpacing,
    doNotLeaveBackslashAlone,
    ulTrailSpace,
    doNotExpandShiftReturn,
    spacingInWholePoints,
    lineWrapLikeWord6,
    printBodyTextBeforeHeader,
    printColBlack,
    wpSpaceWidth,
    showBreaksInFrames,
    subFontBySize,
    suppressBottomSpacing,
    suppressTopSpacing,
    suppressSpacingAtTopOfPage,
    suppressTopSpacingWP,
    suppressSpBfAfterPgBrk,
    swapBordersFacingPages,
    convMailMergeEsc,
    truncateFontHeightsLikeWP6,
    mwSmallCaps,
    usePrinterMetrics,
    doNotSuppressParagraphBorders,
    wrapTrailSpaces,
    footnoteLayoutLikeWW8,
    shapeLayoutLikeWW8,
    alignTablesRowByRow,
    forgetLastTabAlignment,
    adjustLineHeightInTable,
    autoSpaceLikeWord95,
    noSpaceRaiseLower,
    doNotUseHTMLParagraphAutoSpacing,
    layoutRawTableWidth,
    layoutTableRowsApart,
    useWord97LineBreakRules,
    doNotBreakWrappedTables,
    doNotSnapToGridInCell,
    selectFldWithFirstOrLastChar,
    applyBreakingRules,
    doNotWrapTextWithPunct,
    doNotUseEastAsianBreakRules,
    useWord2002TableStyleRules,
    growAutofit,
    useFELayout,
    useNormalStyleForList,
    doNotUseIndentAsNumberingTabStop,
    useAltKinsokuLineBreakRules,
    allowSpaceOfSameStyleInTable,
    doNotSuppressIndentation,
    doNotAutofitConstrainedTables,
    autofitToFirstFixedWidthCell,
    underlineTabInNumList,
    displayHangulFixedWidth,
    splitPgBreakAndParaMark,
    doNotVertAlignCellWithSp,
    doNotBreakConstrainedForcedTable,
    doNotVertAlignInTxbx,
    useAnsiKerningPairs,
    cachedColBalance,
    compatSetting,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CompatSetting`. See `transitional/wml.xsd:2798`. */
export function readCT_CompatSetting(cur: XmlCursor, ctx: ReadContext): CT_CompatSetting {
  const start = requireStart(cur, 'CT_CompatSetting');
  let name: ST_String | undefined;
  let uri: ST_String | undefined;
  let val: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'name': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_CompatSetting', a, 'ST_String', cur);
          break;
        }
        case 'uri': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            uri = v;
            continue;
          }
          ctx.invalidValue('CT_CompatSetting', a, 'ST_String', cur);
          break;
        }
        case 'val': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_CompatSetting', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CompatSetting', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CompatSetting', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_CompatSetting', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CompatSetting', cur);
    cur.next();
  }
  return {
    name,
    uri,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Control`. See `transitional/wml.xsd:1144`. */
export function readCT_Control(cur: XmlCursor, ctx: ReadContext): CT_Control {
  const start = requireStart(cur, 'CT_Control');
  let name: ST_String | undefined;
  let shapeid: ST_String | undefined;
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Control', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'name': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_Control', a, 'ST_String', cur);
          break;
        }
        case 'shapeid': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            shapeid = v;
            continue;
          }
          ctx.invalidValue('CT_Control', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Control', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Control', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Control', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Control', cur);
    cur.next();
  }
  return {
    name,
    shapeid,
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomXmlBlock`. See `transitional/wml.xsd:2193`. */
export function readCT_CustomXmlBlock(cur: XmlCursor, ctx: ReadContext): CT_CustomXmlBlock {
  const start = requireStart(cur, 'CT_CustomXmlBlock');
  let uri: ST_String | undefined;
  let element: ST_XmlName | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'uri': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            uri = v;
            continue;
          }
          ctx.invalidValue('CT_CustomXmlBlock', a, 'ST_String', cur);
          break;
        }
        case 'element': {
          const v = parseST_XmlName(a.value);
          if (v !== undefined) {
            element = v;
            continue;
          }
          ctx.invalidValue('CT_CustomXmlBlock', a, 'ST_XmlName', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CustomXmlBlock', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (element === undefined) ctx.missingRequired('CT_CustomXmlBlock', 'element', cur);

  let customXmlPr: CT_CustomXmlPr | undefined;
  const contentBlockContent: CT_CustomXmlBlock_ContentBlockContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomXmlBlock', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            contentBlockContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            contentBlockContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXmlPr': {
            customXmlPr = readCT_CustomXmlPr(cur, ctx);
            continue;
          }
          case 'customXml': {
            contentBlockContent.push({ kind: 'customXml', value: readCT_CustomXmlBlock(cur, ctx) });
            continue;
          }
          case 'sdt': {
            contentBlockContent.push({ kind: 'sdt', value: readCT_SdtBlock(cur, ctx) });
            continue;
          }
          case 'p': {
            contentBlockContent.push({ kind: 'p', value: readCT_P(cur, ctx) });
            continue;
          }
          case 'tbl': {
            contentBlockContent.push({ kind: 'tbl', value: readCT_Tbl(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            contentBlockContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            contentBlockContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            contentBlockContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            contentBlockContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            contentBlockContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            contentBlockContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            contentBlockContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            contentBlockContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            contentBlockContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            contentBlockContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            contentBlockContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            contentBlockContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            contentBlockContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            contentBlockContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            contentBlockContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            contentBlockContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            contentBlockContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            contentBlockContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            contentBlockContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            contentBlockContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            contentBlockContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            contentBlockContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            contentBlockContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CustomXmlBlock', ev.localName, ev.uri, cur);
      contentBlockContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomXmlBlock', cur);
    cur.next();
  }

  return {
    customXmlPr,
    contentBlockContent,
    uri,
    element,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomXmlCell`. See `transitional/wml.xsd:2215`. */
export function readCT_CustomXmlCell(cur: XmlCursor, ctx: ReadContext): CT_CustomXmlCell {
  const start = requireStart(cur, 'CT_CustomXmlCell');
  let uri: ST_String | undefined;
  let element: ST_XmlName | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'uri': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            uri = v;
            continue;
          }
          ctx.invalidValue('CT_CustomXmlCell', a, 'ST_String', cur);
          break;
        }
        case 'element': {
          const v = parseST_XmlName(a.value);
          if (v !== undefined) {
            element = v;
            continue;
          }
          ctx.invalidValue('CT_CustomXmlCell', a, 'ST_XmlName', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CustomXmlCell', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (element === undefined) ctx.missingRequired('CT_CustomXmlCell', 'element', cur);

  let customXmlPr: CT_CustomXmlPr | undefined;
  const contentCellContent: CT_CustomXmlCell_ContentCellContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomXmlCell', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            contentCellContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            contentCellContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXmlPr': {
            customXmlPr = readCT_CustomXmlPr(cur, ctx);
            continue;
          }
          case 'tc': {
            contentCellContent.push({ kind: 'tc', value: readCT_Tc(cur, ctx) });
            continue;
          }
          case 'customXml': {
            contentCellContent.push({ kind: 'customXml', value: readCT_CustomXmlCell(cur, ctx) });
            continue;
          }
          case 'sdt': {
            contentCellContent.push({ kind: 'sdt', value: readCT_SdtCell(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            contentCellContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            contentCellContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            contentCellContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            contentCellContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            contentCellContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            contentCellContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            contentCellContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            contentCellContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            contentCellContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            contentCellContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            contentCellContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            contentCellContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            contentCellContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            contentCellContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            contentCellContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            contentCellContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            contentCellContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            contentCellContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            contentCellContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            contentCellContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            contentCellContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            contentCellContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            contentCellContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CustomXmlCell', ev.localName, ev.uri, cur);
      contentCellContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomXmlCell', cur);
    cur.next();
  }

  return {
    customXmlPr,
    contentCellContent,
    uri,
    element,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomXmlPr`. See `transitional/wml.xsd:2201`. */
export function readCT_CustomXmlPr(cur: XmlCursor, ctx: ReadContext): CT_CustomXmlPr {
  const start = requireStart(cur, 'CT_CustomXmlPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_CustomXmlPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let placeholder: CT_String | undefined;
  const attr: CT_Attr[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomXmlPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'placeholder': {
            placeholder = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'attr': {
            attr.push(readCT_Attr(cur, ctx));
            $slot = 1;
            $index = attr.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CustomXmlPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomXmlPr', cur);
    cur.next();
  }

  return {
    placeholder,
    attr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomXmlRow`. See `transitional/wml.xsd:2207`. */
export function readCT_CustomXmlRow(cur: XmlCursor, ctx: ReadContext): CT_CustomXmlRow {
  const start = requireStart(cur, 'CT_CustomXmlRow');
  let uri: ST_String | undefined;
  let element: ST_XmlName | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'uri': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            uri = v;
            continue;
          }
          ctx.invalidValue('CT_CustomXmlRow', a, 'ST_String', cur);
          break;
        }
        case 'element': {
          const v = parseST_XmlName(a.value);
          if (v !== undefined) {
            element = v;
            continue;
          }
          ctx.invalidValue('CT_CustomXmlRow', a, 'ST_XmlName', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CustomXmlRow', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (element === undefined) ctx.missingRequired('CT_CustomXmlRow', 'element', cur);

  let customXmlPr: CT_CustomXmlPr | undefined;
  const contentRowContent: CT_CustomXmlRow_ContentRowContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomXmlRow', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            contentRowContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            contentRowContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXmlPr': {
            customXmlPr = readCT_CustomXmlPr(cur, ctx);
            continue;
          }
          case 'tr': {
            contentRowContent.push({ kind: 'tr', value: readCT_Row(cur, ctx) });
            continue;
          }
          case 'customXml': {
            contentRowContent.push({ kind: 'customXml', value: readCT_CustomXmlRow(cur, ctx) });
            continue;
          }
          case 'sdt': {
            contentRowContent.push({ kind: 'sdt', value: readCT_SdtRow(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            contentRowContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            contentRowContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            contentRowContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            contentRowContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            contentRowContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            contentRowContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            contentRowContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            contentRowContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            contentRowContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            contentRowContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            contentRowContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            contentRowContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            contentRowContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            contentRowContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            contentRowContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            contentRowContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            contentRowContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            contentRowContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            contentRowContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            contentRowContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            contentRowContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            contentRowContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            contentRowContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CustomXmlRow', ev.localName, ev.uri, cur);
      contentRowContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomXmlRow', cur);
    cur.next();
  }

  return {
    customXmlPr,
    contentRowContent,
    uri,
    element,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomXmlRun`. See `transitional/wml.xsd:2177`. */
export function readCT_CustomXmlRun(cur: XmlCursor, ctx: ReadContext): CT_CustomXmlRun {
  const start = requireStart(cur, 'CT_CustomXmlRun');
  let uri: ST_String | undefined;
  let element: ST_XmlName | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'uri': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            uri = v;
            continue;
          }
          ctx.invalidValue('CT_CustomXmlRun', a, 'ST_String', cur);
          break;
        }
        case 'element': {
          const v = parseST_XmlName(a.value);
          if (v !== undefined) {
            element = v;
            continue;
          }
          ctx.invalidValue('CT_CustomXmlRun', a, 'ST_XmlName', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CustomXmlRun', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (element === undefined) ctx.missingRequired('CT_CustomXmlRun', 'element', cur);

  let customXmlPr: CT_CustomXmlPr | undefined;
  const pContent: CT_CustomXmlRun_PContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomXmlRun', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            pContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            pContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXmlPr': {
            customXmlPr = readCT_CustomXmlPr(cur, ctx);
            continue;
          }
          case 'customXml': {
            pContent.push({ kind: 'customXml', value: readCT_CustomXmlRun(cur, ctx) });
            continue;
          }
          case 'smartTag': {
            pContent.push({ kind: 'smartTag', value: readCT_SmartTagRun(cur, ctx) });
            continue;
          }
          case 'sdt': {
            pContent.push({ kind: 'sdt', value: readCT_SdtRun(cur, ctx) });
            continue;
          }
          case 'dir': {
            pContent.push({ kind: 'dir', value: readCT_DirContentRun(cur, ctx) });
            continue;
          }
          case 'bdo': {
            pContent.push({ kind: 'bdo', value: readCT_BdoContentRun(cur, ctx) });
            continue;
          }
          case 'r': {
            pContent.push({ kind: 'r', value: readCT_R(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            pContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            pContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            pContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            pContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            pContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            pContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            pContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            pContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            pContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            pContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            pContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            pContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            pContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            pContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            pContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            pContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            pContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            pContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            pContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            pContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            pContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            pContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            pContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'fldSimple': {
            pContent.push({ kind: 'fldSimple', value: readCT_SimpleField(cur, ctx) });
            continue;
          }
          case 'hyperlink': {
            pContent.push({ kind: 'hyperlink', value: readCT_Hyperlink(cur, ctx) });
            continue;
          }
          case 'subDoc': {
            pContent.push({ kind: 'subDoc', value: readCT_Rel(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CustomXmlRun', ev.localName, ev.uri, cur);
      pContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomXmlRun', cur);
    cur.next();
  }

  return {
    customXmlPr,
    pContent,
    uri,
    element,
    $unknownAttrs,
  };
}

/** Read a `CT_DataBinding`. See `transitional/wml.xsd:2043`. */
export function readCT_DataBinding(cur: XmlCursor, ctx: ReadContext): CT_DataBinding {
  const start = requireStart(cur, 'CT_DataBinding');
  let prefixMappings: ST_String | undefined;
  let xpath: ST_String | undefined;
  let storeItemID: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'prefixMappings': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            prefixMappings = v;
            continue;
          }
          ctx.invalidValue('CT_DataBinding', a, 'ST_String', cur);
          break;
        }
        case 'xpath': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            xpath = v;
            continue;
          }
          ctx.invalidValue('CT_DataBinding', a, 'ST_String', cur);
          break;
        }
        case 'storeItemID': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            storeItemID = v;
            continue;
          }
          ctx.invalidValue('CT_DataBinding', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DataBinding', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (xpath === undefined) ctx.missingRequired('CT_DataBinding', 'xpath', cur);
  if (storeItemID === undefined) ctx.missingRequired('CT_DataBinding', 'storeItemID', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DataBinding', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DataBinding', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DataBinding', cur);
    cur.next();
  }
  return {
    prefixMappings,
    xpath,
    storeItemID,
    $unknownAttrs,
  };
}

/** Read a `CT_DecimalNumber`. See `transitional/wml.xsd:57`. */
export function readCT_DecimalNumber(cur: XmlCursor, ctx: ReadContext): CT_DecimalNumber {
  const start = requireStart(cur, 'CT_DecimalNumber');
  let val: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_DecimalNumber', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DecimalNumber', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_DecimalNumber', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DecimalNumber', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DecimalNumber', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DecimalNumber', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_DecimalNumberOrPrecent`. See `transitional/wml.xsd:63`. */
export function readCT_DecimalNumberOrPrecent(cur: XmlCursor, ctx: ReadContext): CT_DecimalNumberOrPrecent {
  const start = requireStart(cur, 'CT_DecimalNumberOrPrecent');
  let val: ST_DecimalNumberOrPercent | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_DecimalNumberOrPercent(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_DecimalNumberOrPrecent', a, 'ST_DecimalNumberOrPercent', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DecimalNumberOrPrecent', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_DecimalNumberOrPrecent', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DecimalNumberOrPrecent', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DecimalNumberOrPrecent', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DecimalNumberOrPrecent', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_DirContentRun`. See `transitional/wml.xsd:2093`. */
export function readCT_DirContentRun(cur: XmlCursor, ctx: ReadContext): CT_DirContentRun {
  const start = requireStart(cur, 'CT_DirContentRun');
  let val: ST_Direction | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Direction(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_DirContentRun', a, 'ST_Direction', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DirContentRun', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const pContent: CT_DirContentRun_PContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DirContentRun', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            pContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            pContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            pContent.push({ kind: 'customXml', value: readCT_CustomXmlRun(cur, ctx) });
            continue;
          }
          case 'smartTag': {
            pContent.push({ kind: 'smartTag', value: readCT_SmartTagRun(cur, ctx) });
            continue;
          }
          case 'sdt': {
            pContent.push({ kind: 'sdt', value: readCT_SdtRun(cur, ctx) });
            continue;
          }
          case 'dir': {
            pContent.push({ kind: 'dir', value: readCT_DirContentRun(cur, ctx) });
            continue;
          }
          case 'bdo': {
            pContent.push({ kind: 'bdo', value: readCT_BdoContentRun(cur, ctx) });
            continue;
          }
          case 'r': {
            pContent.push({ kind: 'r', value: readCT_R(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            pContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            pContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            pContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            pContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            pContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            pContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            pContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            pContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            pContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            pContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            pContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            pContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            pContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            pContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            pContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            pContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            pContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            pContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            pContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            pContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            pContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            pContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            pContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'fldSimple': {
            pContent.push({ kind: 'fldSimple', value: readCT_SimpleField(cur, ctx) });
            continue;
          }
          case 'hyperlink': {
            pContent.push({ kind: 'hyperlink', value: readCT_Hyperlink(cur, ctx) });
            continue;
          }
          case 'subDoc': {
            pContent.push({ kind: 'subDoc', value: readCT_Rel(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DirContentRun', ev.localName, ev.uri, cur);
      pContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DirContentRun', cur);
    cur.next();
  }

  return {
    pContent,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Div`. See `transitional/wml.xsd:3358`. */
export function readCT_Div(cur: XmlCursor, ctx: ReadContext): CT_Div {
  const start = requireStart(cur, 'CT_Div');
  let id: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Div', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Div', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_Div', 'id', cur);

  let blockQuote: CT_OnOff | undefined;
  let bodyDiv: CT_OnOff | undefined;
  let marLeft: CT_SignedTwipsMeasure | undefined;
  let marRight: CT_SignedTwipsMeasure | undefined;
  let marTop: CT_SignedTwipsMeasure | undefined;
  let marBottom: CT_SignedTwipsMeasure | undefined;
  let divBdr: CT_DivBdr | undefined;
  const divsChild: CT_Divs[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Div', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'blockQuote': {
            blockQuote = readCT_OnOff(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'bodyDiv': {
            bodyDiv = readCT_OnOff(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'marLeft': {
            marLeft = readCT_SignedTwipsMeasure(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'marRight': {
            marRight = readCT_SignedTwipsMeasure(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'marTop': {
            marTop = readCT_SignedTwipsMeasure(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'marBottom': {
            marBottom = readCT_SignedTwipsMeasure(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'divBdr': {
            divBdr = readCT_DivBdr(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'divsChild': {
            divsChild.push(readCT_Divs(cur, ctx));
            $slot = 7;
            $index = divsChild.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Div', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Div', cur);
    cur.next();
  }

  return {
    blockQuote,
    bodyDiv,
    marLeft,
    marRight,
    marTop,
    marBottom,
    divBdr,
    divsChild,
    id,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DivBdr`. See `transitional/wml.xsd:3350`. */
export function readCT_DivBdr(cur: XmlCursor, ctx: ReadContext): CT_DivBdr {
  const start = requireStart(cur, 'CT_DivBdr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DivBdr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let top: CT_Border | undefined;
  let left: CT_Border | undefined;
  let bottom: CT_Border | undefined;
  let right: CT_Border | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DivBdr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'top': {
            top = readCT_Border(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'left': {
            left = readCT_Border(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'bottom': {
            bottom = readCT_Border(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'right': {
            right = readCT_Border(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DivBdr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DivBdr', cur);
    cur.next();
  }

  return {
    top,
    left,
    bottom,
    right,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Divs`. See `transitional/wml.xsd:3371`. */
export function readCT_Divs(cur: XmlCursor, ctx: ReadContext): CT_Divs {
  const start = requireStart(cur, 'CT_Divs');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Divs', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const div: CT_Div[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Divs', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'div': {
            div.push(readCT_Div(cur, ctx));
            $slot = 0;
            $index = div.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Divs', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Divs', cur);
    cur.next();
  }

  return {
    div,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DocDefaults`. See `transitional/wml.xsd:2842`. */
export function readCT_DocDefaults(cur: XmlCursor, ctx: ReadContext): CT_DocDefaults {
  const start = requireStart(cur, 'CT_DocDefaults');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DocDefaults', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let rPrDefault: CT_RPrDefault | undefined;
  let pPrDefault: CT_PPrDefault | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocDefaults', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rPrDefault': {
            rPrDefault = readCT_RPrDefault(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pPrDefault': {
            pPrDefault = readCT_PPrDefault(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DocDefaults', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocDefaults', cur);
    cur.next();
  }

  return {
    rPrDefault,
    pPrDefault,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DocGrid`. See `transitional/wml.xsd:1571`. */
export function readCT_DocGrid(cur: XmlCursor, ctx: ReadContext): CT_DocGrid {
  const start = requireStart(cur, 'CT_DocGrid');
  let type: ST_DocGrid | undefined;
  let linePitch: ST_DecimalNumber | undefined;
  let charSpace: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'type': {
          const v = parseST_DocGrid(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_DocGrid', a, 'ST_DocGrid', cur);
          break;
        }
        case 'linePitch': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            linePitch = v;
            continue;
          }
          ctx.invalidValue('CT_DocGrid', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'charSpace': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            charSpace = v;
            continue;
          }
          ctx.invalidValue('CT_DocGrid', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DocGrid', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocGrid', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DocGrid', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocGrid', cur);
    cur.next();
  }
  return {
    type,
    linePitch,
    charSpace,
    $unknownAttrs,
  };
}

/** Read a `CT_DocPart`. See `transitional/wml.xsd:3569`. */
export function readCT_DocPart(cur: XmlCursor, ctx: ReadContext): CT_DocPart {
  const start = requireStart(cur, 'CT_DocPart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DocPart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let docPartPr: CT_DocPartPr | undefined;
  let docPartBody: CT_Body | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocPart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'docPartPr': {
            docPartPr = readCT_DocPartPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'docPartBody': {
            docPartBody = readCT_Body(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DocPart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocPart', cur);
    cur.next();
  }

  return {
    docPartPr,
    docPartBody,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DocPartBehavior`. See `transitional/wml.xsd:3475`. */
export function readCT_DocPartBehavior(cur: XmlCursor, ctx: ReadContext): CT_DocPartBehavior {
  const start = requireStart(cur, 'CT_DocPartBehavior');
  let val: ST_DocPartBehavior | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_DocPartBehavior(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_DocPartBehavior', a, 'ST_DocPartBehavior', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DocPartBehavior', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_DocPartBehavior', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocPartBehavior', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DocPartBehavior', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocPartBehavior', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_DocPartBehaviors`. See `transitional/wml.xsd:3478`. */
export function readCT_DocPartBehaviors(cur: XmlCursor, ctx: ReadContext): CT_DocPartBehaviors {
  const start = requireStart(cur, 'CT_DocPartBehaviors');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DocPartBehaviors', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let behavior: CT_DocPartBehavior | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocPartBehaviors', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'behavior': {
            behavior = readCT_DocPartBehavior(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DocPartBehaviors', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocPartBehaviors', cur);
    cur.next();
  }

  return {
    behavior,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DocPartCategory`. See `transitional/wml.xsd:3548`. */
export function readCT_DocPartCategory(cur: XmlCursor, ctx: ReadContext): CT_DocPartCategory {
  const start = requireStart(cur, 'CT_DocPartCategory');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DocPartCategory', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let name: CT_String | undefined;
  let gallery: CT_DocPartGallery | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocPartCategory', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'name': {
            name = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'gallery': {
            gallery = readCT_DocPartGallery(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DocPartCategory', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocPartCategory', cur);
    cur.next();
  }

  return {
    name,
    gallery,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DocPartGallery`. See `transitional/wml.xsd:3545`. */
export function readCT_DocPartGallery(cur: XmlCursor, ctx: ReadContext): CT_DocPartGallery {
  const start = requireStart(cur, 'CT_DocPartGallery');
  let val: ST_DocPartGallery | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_DocPartGallery(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_DocPartGallery', a, 'ST_DocPartGallery', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DocPartGallery', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_DocPartGallery', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocPartGallery', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DocPartGallery', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocPartGallery', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_DocPartName`. See `transitional/wml.xsd:3554`. */
export function readCT_DocPartName(cur: XmlCursor, ctx: ReadContext): CT_DocPartName {
  const start = requireStart(cur, 'CT_DocPartName');
  let val: ST_String | undefined;
  let decorated: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_DocPartName', a, 'ST_String', cur);
          break;
        }
        case 'decorated': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            decorated = v;
            continue;
          }
          ctx.invalidValue('CT_DocPartName', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DocPartName', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_DocPartName', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocPartName', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DocPartName', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocPartName', cur);
    cur.next();
  }
  return {
    val,
    decorated,
    $unknownAttrs,
  };
}

/** Read a `CT_DocPartPr`. See `transitional/wml.xsd:3558`. */
export function readCT_DocPartPr(cur: XmlCursor, ctx: ReadContext): CT_DocPartPr {
  const start = requireStart(cur, 'CT_DocPartPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DocPartPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let name: CT_DocPartName | undefined;
  let style: CT_String | undefined;
  let category: CT_DocPartCategory | undefined;
  let types: CT_DocPartTypes | undefined;
  let behaviors: CT_DocPartBehaviors | undefined;
  let description: CT_String | undefined;
  let guid: CT_Guid | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocPartPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'name': {
            name = readCT_DocPartName(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_String(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'category': {
            category = readCT_DocPartCategory(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'types': {
            types = readCT_DocPartTypes(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'behaviors': {
            behaviors = readCT_DocPartBehaviors(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'description': {
            description = readCT_String(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'guid': {
            guid = readCT_Guid(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DocPartPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocPartPr', cur);
    cur.next();
  }

  return {
    name,
    style,
    category,
    types,
    behaviors,
    description,
    guid,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DocPartType`. See `transitional/wml.xsd:3494`. */
export function readCT_DocPartType(cur: XmlCursor, ctx: ReadContext): CT_DocPartType {
  const start = requireStart(cur, 'CT_DocPartType');
  let val: ST_DocPartType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_DocPartType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_DocPartType', a, 'ST_DocPartType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DocPartType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_DocPartType', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocPartType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DocPartType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocPartType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_DocPartTypes`. See `transitional/wml.xsd:3497`. */
export function readCT_DocPartTypes(cur: XmlCursor, ctx: ReadContext): CT_DocPartTypes {
  const start = requireStart(cur, 'CT_DocPartTypes');
  let all: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'all': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            all = v;
            continue;
          }
          ctx.invalidValue('CT_DocPartTypes', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DocPartTypes', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let type: CT_DocPartType | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocPartTypes', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'type': {
            type = readCT_DocPartType(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DocPartTypes', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocPartTypes', cur);
    cur.next();
  }

  return {
    type,
    all,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DocParts`. See `transitional/wml.xsd:3575`. */
export function readCT_DocParts(cur: XmlCursor, ctx: ReadContext): CT_DocParts {
  const start = requireStart(cur, 'CT_DocParts');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DocParts', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let docPart: CT_DocPart | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocParts', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'docPart': {
            docPart = readCT_DocPart(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DocParts', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocParts', cur);
    cur.next();
  }

  return {
    docPart,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DocProtect`. See `transitional/wml.xsd:741`. */
export function readCT_DocProtect(cur: XmlCursor, ctx: ReadContext): CT_DocProtect {
  const start = requireStart(cur, 'CT_DocProtect');
  let edit: ST_DocProtect | undefined;
  let formatting: ST_OnOff | undefined;
  let enforcement: ST_OnOff | undefined;
  let algorithmName: ST_String | undefined;
  let hashValue: string | undefined;
  let saltValue: string | undefined;
  let spinCount: ST_DecimalNumber | undefined;
  let cryptProviderType: ST_CryptProv | undefined;
  let cryptAlgorithmClass: ST_AlgClass | undefined;
  let cryptAlgorithmType: ST_AlgType | undefined;
  let cryptAlgorithmSid: ST_DecimalNumber | undefined;
  let cryptSpinCount: ST_DecimalNumber | undefined;
  let cryptProvider: ST_String | undefined;
  let algIdExt: ST_LongHexNumber | undefined;
  let algIdExtSource: ST_String | undefined;
  let cryptProviderTypeExt: ST_LongHexNumber | undefined;
  let cryptProviderTypeExtSource: ST_String | undefined;
  let hash: string | undefined;
  let salt: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'edit': {
          const v = parseST_DocProtect(a.value);
          if (v !== undefined) {
            edit = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_DocProtect', cur);
          break;
        }
        case 'formatting': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            formatting = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_OnOff', cur);
          break;
        }
        case 'enforcement': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            enforcement = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_OnOff', cur);
          break;
        }
        case 'algorithmName': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            algorithmName = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_String', cur);
          break;
        }
        case 'hashValue':
          hashValue = a.value as string;
          continue;

        case 'saltValue':
          saltValue = a.value as string;
          continue;

        case 'spinCount': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            spinCount = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'cryptProviderType': {
          const v = parseST_CryptProv(a.value);
          if (v !== undefined) {
            cryptProviderType = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_CryptProv', cur);
          break;
        }
        case 'cryptAlgorithmClass': {
          const v = parseST_AlgClass(a.value);
          if (v !== undefined) {
            cryptAlgorithmClass = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_AlgClass', cur);
          break;
        }
        case 'cryptAlgorithmType': {
          const v = parseST_AlgType(a.value);
          if (v !== undefined) {
            cryptAlgorithmType = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_AlgType', cur);
          break;
        }
        case 'cryptAlgorithmSid': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            cryptAlgorithmSid = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'cryptSpinCount': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            cryptSpinCount = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'cryptProvider': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            cryptProvider = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_String', cur);
          break;
        }
        case 'algIdExt': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            algIdExt = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'algIdExtSource': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            algIdExtSource = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_String', cur);
          break;
        }
        case 'cryptProviderTypeExt': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            cryptProviderTypeExt = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'cryptProviderTypeExtSource': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            cryptProviderTypeExtSource = v;
            continue;
          }
          ctx.invalidValue('CT_DocProtect', a, 'ST_String', cur);
          break;
        }
        case 'hash':
          hash = a.value as string;
          continue;

        case 'salt':
          salt = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_DocProtect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocProtect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DocProtect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocProtect', cur);
    cur.next();
  }
  return {
    edit,
    formatting,
    enforcement,
    algorithmName,
    hashValue,
    saltValue,
    spinCount,
    cryptProviderType,
    cryptAlgorithmClass,
    cryptAlgorithmType,
    cryptAlgorithmSid,
    cryptSpinCount,
    cryptProvider,
    algIdExt,
    algIdExtSource,
    cryptProviderTypeExt,
    cryptProviderTypeExtSource,
    hash,
    salt,
    $unknownAttrs,
  };
}

/** Read a `CT_DocRsids`. See `transitional/wml.xsd:2812`. */
export function readCT_DocRsids(cur: XmlCursor, ctx: ReadContext): CT_DocRsids {
  const start = requireStart(cur, 'CT_DocRsids');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DocRsids', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let rsidRoot: CT_LongHexNumber | undefined;
  const rsid: CT_LongHexNumber[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocRsids', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rsidRoot': {
            rsidRoot = readCT_LongHexNumber(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'rsid': {
            rsid.push(readCT_LongHexNumber(cur, ctx));
            $slot = 1;
            $index = rsid.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DocRsids', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocRsids', cur);
    cur.next();
  }

  return {
    rsidRoot,
    rsid,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DocType`. See `transitional/wml.xsd:709`. */
export function readCT_DocType(cur: XmlCursor, ctx: ReadContext): CT_DocType {
  const start = requireStart(cur, 'CT_DocType');
  let val: ST_DocType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_DocType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_DocType', a, 'ST_DocType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DocType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_DocType', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DocType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_DocVar`. See `transitional/wml.xsd:2803`. */
export function readCT_DocVar(cur: XmlCursor, ctx: ReadContext): CT_DocVar {
  const start = requireStart(cur, 'CT_DocVar');
  let name: ST_String | undefined;
  let val: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'name': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_DocVar', a, 'ST_String', cur);
          break;
        }
        case 'val': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_DocVar', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DocVar', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_DocVar', 'name', cur);
  if (val === undefined) ctx.missingRequired('CT_DocVar', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocVar', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DocVar', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocVar', cur);
    cur.next();
  }
  return {
    name,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_DocVars`. See `transitional/wml.xsd:2807`. */
export function readCT_DocVars(cur: XmlCursor, ctx: ReadContext): CT_DocVars {
  const start = requireStart(cur, 'CT_DocVars');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DocVars', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const docVar: CT_DocVar[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocVars', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'docVar': {
            docVar.push(readCT_DocVar(cur, ctx));
            $slot = 0;
            $index = docVar.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DocVars', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocVars', cur);
    cur.next();
  }

  return {
    docVar,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Document`. See `transitional/wml.xsd:3622`. */
export function readCT_Document(cur: XmlCursor, ctx: ReadContext): CT_Document {
  const start = requireStart(cur, 'CT_Document');
  let conformance: ST_ConformanceClass | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'conformance': {
          const v = parseST_ConformanceClass(a.value);
          if (v !== undefined) {
            conformance = v;
            continue;
          }
          ctx.invalidValue('CT_Document', a, 'ST_ConformanceClass', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Document', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let background: CT_Background | undefined;
  let body: CT_Body | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Document', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'background': {
            background = readCT_Background(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'body': {
            body = readCT_Body(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Document', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Document', cur);
    cur.next();
  }

  return {
    background,
    body,
    conformance,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DocumentBase`. See `transitional/wml.xsd:3617`. */
export function readCT_DocumentBase(cur: XmlCursor, ctx: ReadContext): CT_DocumentBase {
  const start = requireStart(cur, 'CT_DocumentBase');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DocumentBase', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let background: CT_Background | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DocumentBase', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'background': {
            background = readCT_Background(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DocumentBase', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DocumentBase', cur);
    cur.next();
  }

  return {
    background,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Drawing`. See `transitional/wml.xsd:1225`. */
export function readCT_Drawing(cur: XmlCursor, ctx: ReadContext): CT_Drawing {
  const start = requireStart(cur, 'CT_Drawing');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Drawing', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_Drawing_Content[] = [];

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Drawing', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'anchor': {
            content.push({ kind: 'anchor', value: readCT_Anchor(cur, ctx) });
            continue;
          }
          case 'inline': {
            content.push({ kind: 'inline', value: readCT_Inline(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Drawing', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Drawing', cur);
    cur.next();
  }

  return {
    content,
    $unknownAttrs,
  };
}

/** Read a `CT_EastAsianLayout`. See `transitional/wml.xsd:511`. */
export function readCT_EastAsianLayout(cur: XmlCursor, ctx: ReadContext): CT_EastAsianLayout {
  const start = requireStart(cur, 'CT_EastAsianLayout');
  let id: ST_DecimalNumber | undefined;
  let combine: ST_OnOff | undefined;
  let combineBrackets: ST_CombineBrackets | undefined;
  let vert: ST_OnOff | undefined;
  let vertCompress: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_EastAsianLayout', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'combine': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            combine = v;
            continue;
          }
          ctx.invalidValue('CT_EastAsianLayout', a, 'ST_OnOff', cur);
          break;
        }
        case 'combineBrackets': {
          const v = parseST_CombineBrackets(a.value);
          if (v !== undefined) {
            combineBrackets = v;
            continue;
          }
          ctx.invalidValue('CT_EastAsianLayout', a, 'ST_CombineBrackets', cur);
          break;
        }
        case 'vert': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            vert = v;
            continue;
          }
          ctx.invalidValue('CT_EastAsianLayout', a, 'ST_OnOff', cur);
          break;
        }
        case 'vertCompress': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            vertCompress = v;
            continue;
          }
          ctx.invalidValue('CT_EastAsianLayout', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_EastAsianLayout', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EastAsianLayout', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_EastAsianLayout', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EastAsianLayout', cur);
    cur.next();
  }
  return {
    id,
    combine,
    combineBrackets,
    vert,
    vertCompress,
    $unknownAttrs,
  };
}

/** Read a `CT_EdnDocProps`. See `transitional/wml.xsd:2624`. */
export function readCT_EdnDocProps(cur: XmlCursor, ctx: ReadContext): CT_EdnDocProps {
  const start = requireStart(cur, 'CT_EdnDocProps');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_EdnDocProps', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pos: CT_EdnPos | undefined;
  let numFmt: CT_NumFmt | undefined;
  let numStart: CT_DecimalNumber | undefined;
  let numRestart: CT_NumRestart | undefined;
  const endnote: CT_FtnEdnSepRef[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EdnDocProps', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'pos': {
            pos = readCT_EdnPos(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'numFmt': {
            numFmt = readCT_NumFmt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'numStart': {
            numStart = readCT_DecimalNumber(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'numRestart': {
            numRestart = readCT_NumRestart(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'endnote': {
            endnote.push(readCT_FtnEdnSepRef(cur, ctx));
            $slot = 4;
            $index = endnote.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_EdnDocProps', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EdnDocProps', cur);
    cur.next();
  }

  return {
    pos,
    numFmt,
    numStart,
    numRestart,
    endnote,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_EdnPos`. See `transitional/wml.xsd:2564`. */
export function readCT_EdnPos(cur: XmlCursor, ctx: ReadContext): CT_EdnPos {
  const start = requireStart(cur, 'CT_EdnPos');
  let val: ST_EdnPos | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_EdnPos(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_EdnPos', a, 'ST_EdnPos', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_EdnPos', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_EdnPos', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EdnPos', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_EdnPos', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EdnPos', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_EdnProps`. See `transitional/wml.xsd:2608`. */
export function readCT_EdnProps(cur: XmlCursor, ctx: ReadContext): CT_EdnProps {
  const start = requireStart(cur, 'CT_EdnProps');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_EdnProps', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pos: CT_EdnPos | undefined;
  let numFmt: CT_NumFmt | undefined;
  let numStart: CT_DecimalNumber | undefined;
  let numRestart: CT_NumRestart | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EdnProps', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'pos': {
            pos = readCT_EdnPos(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'numFmt': {
            numFmt = readCT_NumFmt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'numStart': {
            numStart = readCT_DecimalNumber(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'numRestart': {
            numRestart = readCT_NumRestart(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_EdnProps', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EdnProps', cur);
    cur.next();
  }

  return {
    pos,
    numFmt,
    numStart,
    numRestart,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Em`. See `transitional/wml.xsd:494`. */
export function readCT_Em(cur: XmlCursor, ctx: ReadContext): CT_Em {
  const start = requireStart(cur, 'CT_Em');
  let val: ST_Em | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Em(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Em', a, 'ST_Em', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Em', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Em', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Em', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Em', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Em', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Empty`. See `transitional/wml.xsd:22`. */
export function readCT_Empty(cur: XmlCursor, ctx: ReadContext): CT_Empty {
  const start = requireStart(cur, 'CT_Empty');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Empty', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Empty', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Empty', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Empty', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_Endnotes`. See `transitional/wml.xsd:3434`. */
export function readCT_Endnotes(cur: XmlCursor, ctx: ReadContext): CT_Endnotes {
  const start = requireStart(cur, 'CT_Endnotes');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Endnotes', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const endnote: CT_FtnEdn[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Endnotes', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'endnote': {
            endnote.push(readCT_FtnEdn(cur, ctx));
            $slot = 0;
            $index = endnote.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Endnotes', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Endnotes', cur);
    cur.next();
  }

  return {
    endnote,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FFCheckBox`. See `transitional/wml.xsd:1329`. */
export function readCT_FFCheckBox(cur: XmlCursor, ctx: ReadContext): CT_FFCheckBox {
  const start = requireStart(cur, 'CT_FFCheckBox');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FFCheckBox', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_FFCheckBox_Content | undefined;
  let default_: CT_OnOff | undefined;
  let checked: CT_OnOff | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FFCheckBox', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'size': {
            content = { kind: 'size', value: readCT_HpsMeasure(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sizeAuto': {
            content = { kind: 'sizeAuto', value: readCT_OnOff(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'default': {
            default_ = readCT_OnOff(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'checked': {
            checked = readCT_OnOff(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FFCheckBox', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FFCheckBox', cur);
    cur.next();
  }

  return {
    content,
    default: default_,
    checked,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FFDDList`. See `transitional/wml.xsd:1339`. */
export function readCT_FFDDList(cur: XmlCursor, ctx: ReadContext): CT_FFDDList {
  const start = requireStart(cur, 'CT_FFDDList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FFDDList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let result: CT_DecimalNumber | undefined;
  let default_: CT_DecimalNumber | undefined;
  const listEntry: CT_String[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FFDDList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'result': {
            result = readCT_DecimalNumber(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'default': {
            default_ = readCT_DecimalNumber(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'listEntry': {
            listEntry.push(readCT_String(cur, ctx));
            $slot = 2;
            $index = listEntry.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FFDDList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FFDDList', cur);
    cur.next();
  }

  return {
    result,
    default: default_,
    listEntry,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FFData`. See `transitional/wml.xsd:1303`. */
export function readCT_FFData(cur: XmlCursor, ctx: ReadContext): CT_FFData {
  const start = requireStart(cur, 'CT_FFData');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FFData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_FFData_Content[] = [];

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FFData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'name': {
            content.push({ kind: 'name', value: readCT_FFName(cur, ctx) });
            continue;
          }
          case 'label': {
            content.push({ kind: 'label', value: readCT_DecimalNumber(cur, ctx) });
            continue;
          }
          case 'tabIndex': {
            content.push({ kind: 'tabIndex', value: readCT_UnsignedDecimalNumber(cur, ctx) });
            continue;
          }
          case 'enabled': {
            content.push({ kind: 'enabled', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'calcOnExit': {
            content.push({ kind: 'calcOnExit', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'entryMacro': {
            content.push({ kind: 'entryMacro', value: readCT_MacroName(cur, ctx) });
            continue;
          }
          case 'exitMacro': {
            content.push({ kind: 'exitMacro', value: readCT_MacroName(cur, ctx) });
            continue;
          }
          case 'helpText': {
            content.push({ kind: 'helpText', value: readCT_FFHelpText(cur, ctx) });
            continue;
          }
          case 'statusText': {
            content.push({ kind: 'statusText', value: readCT_FFStatusText(cur, ctx) });
            continue;
          }
          case 'checkBox': {
            content.push({ kind: 'checkBox', value: readCT_FFCheckBox(cur, ctx) });
            continue;
          }
          case 'ddList': {
            content.push({ kind: 'ddList', value: readCT_FFDDList(cur, ctx) });
            continue;
          }
          case 'textInput': {
            content.push({ kind: 'textInput', value: readCT_FFTextInput(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FFData', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FFData', cur);
    cur.next();
  }

  return {
    content,
    $unknownAttrs,
  };
}

/** Read a `CT_FFHelpText`. See `transitional/wml.xsd:1321`. */
export function readCT_FFHelpText(cur: XmlCursor, ctx: ReadContext): CT_FFHelpText {
  const start = requireStart(cur, 'CT_FFHelpText');
  let type: ST_InfoTextType | undefined;
  let val: ST_FFHelpTextVal | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'type': {
          const v = parseST_InfoTextType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_FFHelpText', a, 'ST_InfoTextType', cur);
          break;
        }
        case 'val': {
          const v = parseST_FFHelpTextVal(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_FFHelpText', a, 'ST_FFHelpTextVal', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FFHelpText', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FFHelpText', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FFHelpText', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FFHelpText', cur);
    cur.next();
  }
  return {
    type,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_FFName`. See `transitional/wml.xsd:1281`. */
export function readCT_FFName(cur: XmlCursor, ctx: ReadContext): CT_FFName {
  const start = requireStart(cur, 'CT_FFName');
  let val: ST_FFName | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_FFName(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_FFName', a, 'ST_FFName', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FFName', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FFName', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FFName', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FFName', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_FFStatusText`. See `transitional/wml.xsd:1325`. */
export function readCT_FFStatusText(cur: XmlCursor, ctx: ReadContext): CT_FFStatusText {
  const start = requireStart(cur, 'CT_FFStatusText');
  let type: ST_InfoTextType | undefined;
  let val: ST_FFStatusTextVal | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'type': {
          const v = parseST_InfoTextType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_FFStatusText', a, 'ST_InfoTextType', cur);
          break;
        }
        case 'val': {
          const v = parseST_FFStatusTextVal(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_FFStatusText', a, 'ST_FFStatusTextVal', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FFStatusText', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FFStatusText', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FFStatusText', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FFStatusText', cur);
    cur.next();
  }
  return {
    type,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_FFTextInput`. See `transitional/wml.xsd:1346`. */
export function readCT_FFTextInput(cur: XmlCursor, ctx: ReadContext): CT_FFTextInput {
  const start = requireStart(cur, 'CT_FFTextInput');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FFTextInput', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let type: CT_FFTextType | undefined;
  let default_: CT_String | undefined;
  let maxLength: CT_DecimalNumber | undefined;
  let format: CT_String | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FFTextInput', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'type': {
            type = readCT_FFTextType(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'default': {
            default_ = readCT_String(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'maxLength': {
            maxLength = readCT_DecimalNumber(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'format': {
            format = readCT_String(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FFTextInput', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FFTextInput', cur);
    cur.next();
  }

  return {
    type,
    default: default_,
    maxLength,
    format,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FFTextType`. See `transitional/wml.xsd:1278`. */
export function readCT_FFTextType(cur: XmlCursor, ctx: ReadContext): CT_FFTextType {
  const start = requireStart(cur, 'CT_FFTextType');
  let val: ST_FFTextType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_FFTextType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_FFTextType', a, 'ST_FFTextType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FFTextType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_FFTextType', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FFTextType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FFTextType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FFTextType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_FitText`. See `transitional/wml.xsd:481`. */
export function readCT_FitText(cur: XmlCursor, ctx: ReadContext): CT_FitText {
  const start = requireStart(cur, 'CT_FitText');
  let val: ST_TwipsMeasure | undefined;
  let id: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_FitText', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_FitText', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FitText', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_FitText', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FitText', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FitText', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FitText', cur);
    cur.next();
  }
  return {
    val,
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_FldChar`. See `transitional/wml.xsd:1284`. */
export function readCT_FldChar(cur: XmlCursor, ctx: ReadContext): CT_FldChar {
  const start = requireStart(cur, 'CT_FldChar');
  let fldCharType: ST_FldCharType | undefined;
  let fldLock: ST_OnOff | undefined;
  let dirty: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'fldCharType': {
          const v = parseST_FldCharType(a.value);
          if (v !== undefined) {
            fldCharType = v;
            continue;
          }
          ctx.invalidValue('CT_FldChar', a, 'ST_FldCharType', cur);
          break;
        }
        case 'fldLock': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            fldLock = v;
            continue;
          }
          ctx.invalidValue('CT_FldChar', a, 'ST_OnOff', cur);
          break;
        }
        case 'dirty': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            dirty = v;
            continue;
          }
          ctx.invalidValue('CT_FldChar', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FldChar', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (fldCharType === undefined) ctx.missingRequired('CT_FldChar', 'fldCharType', cur);

  let content: CT_FldChar_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FldChar', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'fldData': {
            content = { kind: 'fldData', value: readCT_Text(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ffData': {
            content = { kind: 'ffData', value: readCT_FFData(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'numberingChange': {
            content = { kind: 'numberingChange', value: readCT_TrackChangeNumbering(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FldChar', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FldChar', cur);
    cur.next();
  }

  return {
    content,
    fldCharType,
    fldLock,
    dirty,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Font`. See `transitional/wml.xsd:3329`. */
export function readCT_Font(cur: XmlCursor, ctx: ReadContext): CT_Font {
  const start = requireStart(cur, 'CT_Font');
  let name: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'name': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_Font', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Font', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_Font', 'name', cur);

  let altName: CT_String | undefined;
  let panose1: CT_Panose | undefined;
  let charset: CT_Charset | undefined;
  let family: CT_FontFamily | undefined;
  let notTrueType: CT_OnOff | undefined;
  let pitch: CT_Pitch | undefined;
  let sig: CT_FontSig | undefined;
  let embedRegular: CT_FontRel | undefined;
  let embedBold: CT_FontRel | undefined;
  let embedItalic: CT_FontRel | undefined;
  let embedBoldItalic: CT_FontRel | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Font', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'altName': {
            altName = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'panose1': {
            panose1 = readCT_Panose(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'charset': {
            charset = readCT_Charset(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'family': {
            family = readCT_FontFamily(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'notTrueType': {
            notTrueType = readCT_OnOff(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'pitch': {
            pitch = readCT_Pitch(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'sig': {
            sig = readCT_FontSig(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'embedRegular': {
            embedRegular = readCT_FontRel(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'embedBold': {
            embedBold = readCT_FontRel(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'embedItalic': {
            embedItalic = readCT_FontRel(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'embedBoldItalic': {
            embedBoldItalic = readCT_FontRel(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Font', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Font', cur);
    cur.next();
  }

  return {
    altName,
    panose1,
    charset,
    family,
    notTrueType,
    pitch,
    sig,
    embedRegular,
    embedBold,
    embedItalic,
    embedBoldItalic,
    name,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FontFamily`. See `transitional/wml.xsd:3300`. */
export function readCT_FontFamily(cur: XmlCursor, ctx: ReadContext): CT_FontFamily {
  const start = requireStart(cur, 'CT_FontFamily');
  let val: ST_FontFamily | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_FontFamily(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_FontFamily', a, 'ST_FontFamily', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FontFamily', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_FontFamily', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FontFamily', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FontFamily', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FontFamily', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_FontRel`. See `transitional/wml.xsd:3321`. */
export function readCT_FontRel(cur: XmlCursor, ctx: ReadContext): CT_FontRel {
  const start = requireStart(cur, 'CT_FontRel');
  let id: ST_RelationshipId | undefined;
  let fontKey: ST_Guid | undefined;
  let subsetted: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_FontRel', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'fontKey': {
          const v = parseST_Guid(a.value);
          if (v !== undefined) {
            fontKey = v;
            continue;
          }
          ctx.invalidValue('CT_FontRel', a, 'ST_Guid', cur);
          break;
        }
        case 'subsetted': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            subsetted = v;
            continue;
          }
          ctx.invalidValue('CT_FontRel', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FontRel', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_FontRel', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FontRel', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FontRel', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FontRel', cur);
    cur.next();
  }
  return {
    id,
    fontKey,
    subsetted,
    $unknownAttrs,
  };
}

/** Read a `CT_FontSig`. See `transitional/wml.xsd:3313`. */
export function readCT_FontSig(cur: XmlCursor, ctx: ReadContext): CT_FontSig {
  const start = requireStart(cur, 'CT_FontSig');
  let usb0: ST_LongHexNumber | undefined;
  let usb1: ST_LongHexNumber | undefined;
  let usb2: ST_LongHexNumber | undefined;
  let usb3: ST_LongHexNumber | undefined;
  let csb0: ST_LongHexNumber | undefined;
  let csb1: ST_LongHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'usb0': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            usb0 = v;
            continue;
          }
          ctx.invalidValue('CT_FontSig', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'usb1': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            usb1 = v;
            continue;
          }
          ctx.invalidValue('CT_FontSig', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'usb2': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            usb2 = v;
            continue;
          }
          ctx.invalidValue('CT_FontSig', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'usb3': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            usb3 = v;
            continue;
          }
          ctx.invalidValue('CT_FontSig', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'csb0': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            csb0 = v;
            continue;
          }
          ctx.invalidValue('CT_FontSig', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'csb1': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            csb1 = v;
            continue;
          }
          ctx.invalidValue('CT_FontSig', a, 'ST_LongHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FontSig', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (usb0 === undefined) ctx.missingRequired('CT_FontSig', 'usb0', cur);
  if (usb1 === undefined) ctx.missingRequired('CT_FontSig', 'usb1', cur);
  if (usb2 === undefined) ctx.missingRequired('CT_FontSig', 'usb2', cur);
  if (usb3 === undefined) ctx.missingRequired('CT_FontSig', 'usb3', cur);
  if (csb0 === undefined) ctx.missingRequired('CT_FontSig', 'csb0', cur);
  if (csb1 === undefined) ctx.missingRequired('CT_FontSig', 'csb1', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FontSig', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FontSig', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FontSig', cur);
    cur.next();
  }
  return {
    usb0,
    usb1,
    usb2,
    usb3,
    csb0,
    csb1,
    $unknownAttrs,
  };
}

/** Read a `CT_Fonts`. See `transitional/wml.xsd:1807`. */
export function readCT_Fonts(cur: XmlCursor, ctx: ReadContext): CT_Fonts {
  const start = requireStart(cur, 'CT_Fonts');
  let hint: ST_Hint | undefined;
  let ascii: ST_String | undefined;
  let hAnsi: ST_String | undefined;
  let eastAsia: ST_String | undefined;
  let cs: ST_String | undefined;
  let asciiTheme: ST_Theme | undefined;
  let hAnsiTheme: ST_Theme | undefined;
  let eastAsiaTheme: ST_Theme | undefined;
  let cstheme: ST_Theme | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'hint': {
          const v = parseST_Hint(a.value);
          if (v !== undefined) {
            hint = v;
            continue;
          }
          ctx.invalidValue('CT_Fonts', a, 'ST_Hint', cur);
          break;
        }
        case 'ascii': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            ascii = v;
            continue;
          }
          ctx.invalidValue('CT_Fonts', a, 'ST_String', cur);
          break;
        }
        case 'hAnsi': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            hAnsi = v;
            continue;
          }
          ctx.invalidValue('CT_Fonts', a, 'ST_String', cur);
          break;
        }
        case 'eastAsia': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            eastAsia = v;
            continue;
          }
          ctx.invalidValue('CT_Fonts', a, 'ST_String', cur);
          break;
        }
        case 'cs': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            cs = v;
            continue;
          }
          ctx.invalidValue('CT_Fonts', a, 'ST_String', cur);
          break;
        }
        case 'asciiTheme': {
          const v = parseST_Theme(a.value);
          if (v !== undefined) {
            asciiTheme = v;
            continue;
          }
          ctx.invalidValue('CT_Fonts', a, 'ST_Theme', cur);
          break;
        }
        case 'hAnsiTheme': {
          const v = parseST_Theme(a.value);
          if (v !== undefined) {
            hAnsiTheme = v;
            continue;
          }
          ctx.invalidValue('CT_Fonts', a, 'ST_Theme', cur);
          break;
        }
        case 'eastAsiaTheme': {
          const v = parseST_Theme(a.value);
          if (v !== undefined) {
            eastAsiaTheme = v;
            continue;
          }
          ctx.invalidValue('CT_Fonts', a, 'ST_Theme', cur);
          break;
        }
        case 'cstheme': {
          const v = parseST_Theme(a.value);
          if (v !== undefined) {
            cstheme = v;
            continue;
          }
          ctx.invalidValue('CT_Fonts', a, 'ST_Theme', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Fonts', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Fonts', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Fonts', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Fonts', cur);
    cur.next();
  }
  return {
    hint,
    ascii,
    hAnsi,
    eastAsia,
    cs,
    asciiTheme,
    hAnsiTheme,
    eastAsiaTheme,
    cstheme,
    $unknownAttrs,
  };
}

/** Read a `CT_FontsList`. See `transitional/wml.xsd:3345`. */
export function readCT_FontsList(cur: XmlCursor, ctx: ReadContext): CT_FontsList {
  const start = requireStart(cur, 'CT_FontsList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FontsList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const font: CT_Font[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FontsList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'font': {
            font.push(readCT_Font(cur, ctx));
            $slot = 0;
            $index = font.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FontsList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FontsList', cur);
    cur.next();
  }

  return {
    font,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Footnotes`. See `transitional/wml.xsd:3428`. */
export function readCT_Footnotes(cur: XmlCursor, ctx: ReadContext): CT_Footnotes {
  const start = requireStart(cur, 'CT_Footnotes');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Footnotes', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const footnote: CT_FtnEdn[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Footnotes', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'footnote': {
            footnote.push(readCT_FtnEdn(cur, ctx));
            $slot = 0;
            $index = footnote.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Footnotes', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Footnotes', cur);
    cur.next();
  }

  return {
    footnote,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Frame`. See `transitional/wml.xsd:3063`. */
export function readCT_Frame(cur: XmlCursor, ctx: ReadContext): CT_Frame {
  const start = requireStart(cur, 'CT_Frame');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Frame', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let sz: CT_String | undefined;
  let name: CT_String | undefined;
  let title: CT_String | undefined;
  let longDesc: CT_Rel | undefined;
  let sourceFileName: CT_Rel | undefined;
  let marW: CT_PixelsMeasure | undefined;
  let marH: CT_PixelsMeasure | undefined;
  let scrollbar: CT_FrameScrollbar | undefined;
  let noResizeAllowed: CT_OnOff | undefined;
  let linkedToFile: CT_OnOff | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Frame', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'sz': {
            sz = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'name': {
            name = readCT_String(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'title': {
            title = readCT_String(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'longDesc': {
            longDesc = readCT_Rel(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'sourceFileName': {
            sourceFileName = readCT_Rel(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'marW': {
            marW = readCT_PixelsMeasure(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'marH': {
            marH = readCT_PixelsMeasure(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'scrollbar': {
            scrollbar = readCT_FrameScrollbar(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'noResizeAllowed': {
            noResizeAllowed = readCT_OnOff(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'linkedToFile': {
            linkedToFile = readCT_OnOff(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Frame', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Frame', cur);
    cur.next();
  }

  return {
    sz,
    name,
    title,
    longDesc,
    sourceFileName,
    marW,
    marH,
    scrollbar,
    noResizeAllowed,
    linkedToFile,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FrameLayout`. See `transitional/wml.xsd:3084`. */
export function readCT_FrameLayout(cur: XmlCursor, ctx: ReadContext): CT_FrameLayout {
  const start = requireStart(cur, 'CT_FrameLayout');
  let val: ST_FrameLayout | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_FrameLayout(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_FrameLayout', a, 'ST_FrameLayout', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FrameLayout', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_FrameLayout', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FrameLayout', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FrameLayout', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FrameLayout', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_FramePr`. See `transitional/wml.xsd:556`. */
export function readCT_FramePr(cur: XmlCursor, ctx: ReadContext): CT_FramePr {
  const start = requireStart(cur, 'CT_FramePr');
  let dropCap: ST_DropCap | undefined;
  let lines: ST_DecimalNumber | undefined;
  let w: ST_TwipsMeasure | undefined;
  let h: ST_TwipsMeasure | undefined;
  let vSpace: ST_TwipsMeasure | undefined;
  let hSpace: ST_TwipsMeasure | undefined;
  let wrap: ST_Wrap | undefined;
  let hAnchor: ST_HAnchor | undefined;
  let vAnchor: ST_VAnchor | undefined;
  let x: ST_SignedTwipsMeasure | undefined;
  let xAlign: ST_XAlign | undefined;
  let y: ST_SignedTwipsMeasure | undefined;
  let yAlign: ST_YAlign | undefined;
  let hRule: ST_HeightRule | undefined;
  let anchorLock: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'dropCap': {
          const v = parseST_DropCap(a.value);
          if (v !== undefined) {
            dropCap = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_DropCap', cur);
          break;
        }
        case 'lines': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            lines = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'w': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            w = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'h': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            h = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'vSpace': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            vSpace = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'hSpace': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            hSpace = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'wrap': {
          const v = parseST_Wrap(a.value);
          if (v !== undefined) {
            wrap = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_Wrap', cur);
          break;
        }
        case 'hAnchor': {
          const v = parseST_HAnchor(a.value);
          if (v !== undefined) {
            hAnchor = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_HAnchor', cur);
          break;
        }
        case 'vAnchor': {
          const v = parseST_VAnchor(a.value);
          if (v !== undefined) {
            vAnchor = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_VAnchor', cur);
          break;
        }
        case 'x': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            x = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
        case 'xAlign': {
          const v = parseST_XAlign(a.value);
          if (v !== undefined) {
            xAlign = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_XAlign', cur);
          break;
        }
        case 'y': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            y = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
        case 'yAlign': {
          const v = parseST_YAlign(a.value);
          if (v !== undefined) {
            yAlign = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_YAlign', cur);
          break;
        }
        case 'hRule': {
          const v = parseST_HeightRule(a.value);
          if (v !== undefined) {
            hRule = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_HeightRule', cur);
          break;
        }
        case 'anchorLock': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            anchorLock = v;
            continue;
          }
          ctx.invalidValue('CT_FramePr', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FramePr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FramePr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FramePr', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FramePr', cur);
    cur.next();
  }
  return {
    dropCap,
    lines,
    w,
    h,
    vSpace,
    hSpace,
    wrap,
    hAnchor,
    vAnchor,
    x,
    xAlign,
    y,
    yAlign,
    hRule,
    anchorLock,
    $unknownAttrs,
  };
}

/** Read a `CT_FrameScrollbar`. See `transitional/wml.xsd:3053`. */
export function readCT_FrameScrollbar(cur: XmlCursor, ctx: ReadContext): CT_FrameScrollbar {
  const start = requireStart(cur, 'CT_FrameScrollbar');
  let val: ST_FrameScrollbar | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_FrameScrollbar(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_FrameScrollbar', a, 'ST_FrameScrollbar', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FrameScrollbar', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_FrameScrollbar', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FrameScrollbar', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FrameScrollbar', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FrameScrollbar', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Frameset`. See `transitional/wml.xsd:3095`. */
export function readCT_Frameset(cur: XmlCursor, ctx: ReadContext): CT_Frameset {
  const start = requireStart(cur, 'CT_Frameset');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Frameset', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let sz: CT_String | undefined;
  let framesetSplitbar: CT_FramesetSplitbar | undefined;
  let frameLayout: CT_FrameLayout | undefined;
  let title: CT_String | undefined;
  const content: CT_Frameset_Content[] = [];

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Frameset', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'sz': {
            sz = readCT_String(cur, ctx);
            continue;
          }
          case 'framesetSplitbar': {
            framesetSplitbar = readCT_FramesetSplitbar(cur, ctx);
            continue;
          }
          case 'frameLayout': {
            frameLayout = readCT_FrameLayout(cur, ctx);
            continue;
          }
          case 'title': {
            title = readCT_String(cur, ctx);
            continue;
          }
          case 'frameset': {
            content.push({ kind: 'frameset', value: readCT_Frameset(cur, ctx) });
            continue;
          }
          case 'frame': {
            content.push({ kind: 'frame', value: readCT_Frame(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Frameset', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Frameset', cur);
    cur.next();
  }

  return {
    sz,
    framesetSplitbar,
    frameLayout,
    title,
    content,
    $unknownAttrs,
  };
}

/** Read a `CT_FramesetSplitbar`. See `transitional/wml.xsd:3087`. */
export function readCT_FramesetSplitbar(cur: XmlCursor, ctx: ReadContext): CT_FramesetSplitbar {
  const start = requireStart(cur, 'CT_FramesetSplitbar');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FramesetSplitbar', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let w: CT_TwipsMeasure | undefined;
  let color: CT_Color | undefined;
  let noBorder: CT_OnOff | undefined;
  let flatBorders: CT_OnOff | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FramesetSplitbar', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'w': {
            w = readCT_TwipsMeasure(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'color': {
            color = readCT_Color(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'noBorder': {
            noBorder = readCT_OnOff(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'flatBorders': {
            flatBorders = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FramesetSplitbar', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FramesetSplitbar', cur);
    cur.next();
  }

  return {
    w,
    color,
    noBorder,
    flatBorders,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FtnDocProps`. See `transitional/wml.xsd:2615`. */
export function readCT_FtnDocProps(cur: XmlCursor, ctx: ReadContext): CT_FtnDocProps {
  const start = requireStart(cur, 'CT_FtnDocProps');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FtnDocProps', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pos: CT_FtnPos | undefined;
  let numFmt: CT_NumFmt | undefined;
  let numStart: CT_DecimalNumber | undefined;
  let numRestart: CT_NumRestart | undefined;
  const footnote: CT_FtnEdnSepRef[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FtnDocProps', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'pos': {
            pos = readCT_FtnPos(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'numFmt': {
            numFmt = readCT_NumFmt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'numStart': {
            numStart = readCT_DecimalNumber(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'numRestart': {
            numRestart = readCT_NumRestart(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'footnote': {
            footnote.push(readCT_FtnEdnSepRef(cur, ctx));
            $slot = 4;
            $index = footnote.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FtnDocProps', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FtnDocProps', cur);
    cur.next();
  }

  return {
    pos,
    numFmt,
    numStart,
    numRestart,
    footnote,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FtnEdn`. See `transitional/wml.xsd:2588`. */
export function readCT_FtnEdn(cur: XmlCursor, ctx: ReadContext): CT_FtnEdn {
  const start = requireStart(cur, 'CT_FtnEdn');
  let type: ST_FtnEdn | undefined;
  let id: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'type': {
          const v = parseST_FtnEdn(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_FtnEdn', a, 'ST_FtnEdn', cur);
          break;
        }
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_FtnEdn', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FtnEdn', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_FtnEdn', 'id', cur);

  const blockLevelElts: CT_FtnEdn_BlockLevelElts[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FtnEdn', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            blockLevelElts.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            blockLevelElts.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            blockLevelElts.push({ kind: 'customXml', value: readCT_CustomXmlBlock(cur, ctx) });
            continue;
          }
          case 'sdt': {
            blockLevelElts.push({ kind: 'sdt', value: readCT_SdtBlock(cur, ctx) });
            continue;
          }
          case 'p': {
            blockLevelElts.push({ kind: 'p', value: readCT_P(cur, ctx) });
            continue;
          }
          case 'tbl': {
            blockLevelElts.push({ kind: 'tbl', value: readCT_Tbl(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            blockLevelElts.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            blockLevelElts.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            blockLevelElts.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            blockLevelElts.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            blockLevelElts.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            blockLevelElts.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            blockLevelElts.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            blockLevelElts.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            blockLevelElts.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            blockLevelElts.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            blockLevelElts.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            blockLevelElts.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            blockLevelElts.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            blockLevelElts.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            blockLevelElts.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            blockLevelElts.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            blockLevelElts.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'altChunk': {
            blockLevelElts.push({ kind: 'altChunk', value: readCT_AltChunk(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FtnEdn', ev.localName, ev.uri, cur);
      blockLevelElts.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FtnEdn', cur);
    cur.next();
  }

  return {
    blockLevelElts,
    type,
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_FtnEdnRef`. See `transitional/wml.xsd:2581`. */
export function readCT_FtnEdnRef(cur: XmlCursor, ctx: ReadContext): CT_FtnEdnRef {
  const start = requireStart(cur, 'CT_FtnEdnRef');
  let customMarkFollows: ST_OnOff | undefined;
  let id: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'customMarkFollows': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            customMarkFollows = v;
            continue;
          }
          ctx.invalidValue('CT_FtnEdnRef', a, 'ST_OnOff', cur);
          break;
        }
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_FtnEdnRef', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FtnEdnRef', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_FtnEdnRef', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FtnEdnRef', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FtnEdnRef', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FtnEdnRef', cur);
    cur.next();
  }
  return {
    customMarkFollows,
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_FtnEdnSepRef`. See `transitional/wml.xsd:2585`. */
export function readCT_FtnEdnSepRef(cur: XmlCursor, ctx: ReadContext): CT_FtnEdnSepRef {
  const start = requireStart(cur, 'CT_FtnEdnSepRef');
  let id: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_FtnEdnSepRef', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FtnEdnSepRef', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_FtnEdnSepRef', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FtnEdnSepRef', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FtnEdnSepRef', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FtnEdnSepRef', cur);
    cur.next();
  }
  return {
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_FtnPos`. See `transitional/wml.xsd:2555`. */
export function readCT_FtnPos(cur: XmlCursor, ctx: ReadContext): CT_FtnPos {
  const start = requireStart(cur, 'CT_FtnPos');
  let val: ST_FtnPos | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_FtnPos(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_FtnPos', a, 'ST_FtnPos', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FtnPos', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_FtnPos', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FtnPos', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FtnPos', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FtnPos', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_FtnProps`. See `transitional/wml.xsd:2601`. */
export function readCT_FtnProps(cur: XmlCursor, ctx: ReadContext): CT_FtnProps {
  const start = requireStart(cur, 'CT_FtnProps');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FtnProps', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pos: CT_FtnPos | undefined;
  let numFmt: CT_NumFmt | undefined;
  let numStart: CT_DecimalNumber | undefined;
  let numRestart: CT_NumRestart | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FtnProps', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'pos': {
            pos = readCT_FtnPos(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'numFmt': {
            numFmt = readCT_NumFmt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'numStart': {
            numStart = readCT_DecimalNumber(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'numRestart': {
            numRestart = readCT_NumRestart(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FtnProps', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FtnProps', cur);
    cur.next();
  }

  return {
    pos,
    numFmt,
    numStart,
    numRestart,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GlossaryDocument`. See `transitional/wml.xsd:3632`. */
export function readCT_GlossaryDocument(cur: XmlCursor, ctx: ReadContext): CT_GlossaryDocument {
  const start = requireStart(cur, 'CT_GlossaryDocument');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GlossaryDocument', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let background: CT_Background | undefined;
  let docParts: CT_DocParts | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GlossaryDocument', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'background': {
            background = readCT_Background(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'docParts': {
            docParts = readCT_DocParts(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GlossaryDocument', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GlossaryDocument', cur);
    cur.next();
  }

  return {
    background,
    docParts,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Guid`. See `transitional/wml.xsd:171`. */
export function readCT_Guid(cur: XmlCursor, ctx: ReadContext): CT_Guid {
  const start = requireStart(cur, 'CT_Guid');
  let val: ST_Guid | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Guid(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Guid', a, 'ST_Guid', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Guid', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Guid', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Guid', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Guid', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_HMerge`. See `transitional/wml.xsd:2316`. */
export function readCT_HMerge(cur: XmlCursor, ctx: ReadContext): CT_HMerge {
  const start = requireStart(cur, 'CT_HMerge');
  let val: ST_Merge | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Merge(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_HMerge', a, 'ST_Merge', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_HMerge', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HMerge', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_HMerge', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HMerge', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_HdrFtr`. See `transitional/wml.xsd:1604`. */
export function readCT_HdrFtr(cur: XmlCursor, ctx: ReadContext): CT_HdrFtr {
  const start = requireStart(cur, 'CT_HdrFtr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_HdrFtr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const blockLevelElts: CT_HdrFtr_BlockLevelElts[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HdrFtr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            blockLevelElts.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            blockLevelElts.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            blockLevelElts.push({ kind: 'customXml', value: readCT_CustomXmlBlock(cur, ctx) });
            continue;
          }
          case 'sdt': {
            blockLevelElts.push({ kind: 'sdt', value: readCT_SdtBlock(cur, ctx) });
            continue;
          }
          case 'p': {
            blockLevelElts.push({ kind: 'p', value: readCT_P(cur, ctx) });
            continue;
          }
          case 'tbl': {
            blockLevelElts.push({ kind: 'tbl', value: readCT_Tbl(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            blockLevelElts.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            blockLevelElts.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            blockLevelElts.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            blockLevelElts.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            blockLevelElts.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            blockLevelElts.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            blockLevelElts.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            blockLevelElts.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            blockLevelElts.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            blockLevelElts.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            blockLevelElts.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            blockLevelElts.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            blockLevelElts.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            blockLevelElts.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            blockLevelElts.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            blockLevelElts.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            blockLevelElts.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'altChunk': {
            blockLevelElts.push({ kind: 'altChunk', value: readCT_AltChunk(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_HdrFtr', ev.localName, ev.uri, cur);
      blockLevelElts.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HdrFtr', cur);
    cur.next();
  }

  return {
    blockLevelElts,
    $unknownAttrs,
  };
}

/** Read a `CT_HdrFtrRef`. See `transitional/wml.xsd:1591`. */
export function readCT_HdrFtrRef(cur: XmlCursor, ctx: ReadContext): CT_HdrFtrRef {
  const start = requireStart(cur, 'CT_HdrFtrRef');
  let id: ST_RelationshipId | undefined;
  let type: ST_HdrFtr | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_HdrFtrRef', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'type': {
          const v = parseST_HdrFtr(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_HdrFtrRef', a, 'ST_HdrFtr', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_HdrFtrRef', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_HdrFtrRef', 'id', cur);
  if (type === undefined) ctx.missingRequired('CT_HdrFtrRef', 'type', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HdrFtrRef', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_HdrFtrRef', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HdrFtrRef', cur);
    cur.next();
  }
  return {
    id,
    type,
    $unknownAttrs,
  };
}

/** Read a `CT_Headers`. See `transitional/wml.xsd:2383`. */
export function readCT_Headers(cur: XmlCursor, ctx: ReadContext): CT_Headers {
  const start = requireStart(cur, 'CT_Headers');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Headers', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const header: CT_String[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Headers', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'header': {
            header.push(readCT_String(cur, ctx));
            $slot = 0;
            $index = header.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Headers', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Headers', cur);
    cur.next();
  }

  return {
    header,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Height`. See `transitional/wml.xsd:2255`. */
export function readCT_Height(cur: XmlCursor, ctx: ReadContext): CT_Height {
  const start = requireStart(cur, 'CT_Height');
  let val: ST_TwipsMeasure | undefined;
  let hRule: ST_HeightRule | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Height', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'hRule': {
          const v = parseST_HeightRule(a.value);
          if (v !== undefined) {
            hRule = v;
            continue;
          }
          ctx.invalidValue('CT_Height', a, 'ST_HeightRule', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Height', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Height', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Height', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Height', cur);
    cur.next();
  }
  return {
    val,
    hRule,
    $unknownAttrs,
  };
}

/** Read a `CT_Highlight`. See `transitional/wml.xsd:151`. */
export function readCT_Highlight(cur: XmlCursor, ctx: ReadContext): CT_Highlight {
  const start = requireStart(cur, 'CT_Highlight');
  let val: ST_HighlightColor | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_HighlightColor(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Highlight', a, 'ST_HighlightColor', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Highlight', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Highlight', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Highlight', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Highlight', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Highlight', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_HpsMeasure`. See `transitional/wml.xsd:84`. */
export function readCT_HpsMeasure(cur: XmlCursor, ctx: ReadContext): CT_HpsMeasure {
  const start = requireStart(cur, 'CT_HpsMeasure');
  let val: ST_HpsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_HpsMeasure(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_HpsMeasure', a, 'ST_HpsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_HpsMeasure', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_HpsMeasure', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HpsMeasure', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_HpsMeasure', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HpsMeasure', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Hyperlink`. See `transitional/wml.xsd:1294`. */
export function readCT_Hyperlink(cur: XmlCursor, ctx: ReadContext): CT_Hyperlink {
  const start = requireStart(cur, 'CT_Hyperlink');
  let tgtFrame: ST_String | undefined;
  let tooltip: ST_String | undefined;
  let docLocation: ST_String | undefined;
  let history: ST_OnOff | undefined;
  let anchor: ST_String | undefined;
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Hyperlink', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'tgtFrame': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            tgtFrame = v;
            continue;
          }
          ctx.invalidValue('CT_Hyperlink', a, 'ST_String', cur);
          break;
        }
        case 'tooltip': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            tooltip = v;
            continue;
          }
          ctx.invalidValue('CT_Hyperlink', a, 'ST_String', cur);
          break;
        }
        case 'docLocation': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            docLocation = v;
            continue;
          }
          ctx.invalidValue('CT_Hyperlink', a, 'ST_String', cur);
          break;
        }
        case 'history': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            history = v;
            continue;
          }
          ctx.invalidValue('CT_Hyperlink', a, 'ST_OnOff', cur);
          break;
        }
        case 'anchor': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            anchor = v;
            continue;
          }
          ctx.invalidValue('CT_Hyperlink', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Hyperlink', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const pContent: CT_Hyperlink_PContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Hyperlink', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            pContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            pContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            pContent.push({ kind: 'customXml', value: readCT_CustomXmlRun(cur, ctx) });
            continue;
          }
          case 'smartTag': {
            pContent.push({ kind: 'smartTag', value: readCT_SmartTagRun(cur, ctx) });
            continue;
          }
          case 'sdt': {
            pContent.push({ kind: 'sdt', value: readCT_SdtRun(cur, ctx) });
            continue;
          }
          case 'dir': {
            pContent.push({ kind: 'dir', value: readCT_DirContentRun(cur, ctx) });
            continue;
          }
          case 'bdo': {
            pContent.push({ kind: 'bdo', value: readCT_BdoContentRun(cur, ctx) });
            continue;
          }
          case 'r': {
            pContent.push({ kind: 'r', value: readCT_R(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            pContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            pContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            pContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            pContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            pContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            pContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            pContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            pContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            pContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            pContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            pContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            pContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            pContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            pContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            pContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            pContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            pContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            pContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            pContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            pContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            pContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            pContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            pContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'fldSimple': {
            pContent.push({ kind: 'fldSimple', value: readCT_SimpleField(cur, ctx) });
            continue;
          }
          case 'hyperlink': {
            pContent.push({ kind: 'hyperlink', value: readCT_Hyperlink(cur, ctx) });
            continue;
          }
          case 'subDoc': {
            pContent.push({ kind: 'subDoc', value: readCT_Rel(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Hyperlink', ev.localName, ev.uri, cur);
      pContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Hyperlink', cur);
    cur.next();
  }

  return {
    pContent,
    tgtFrame,
    tooltip,
    docLocation,
    history,
    anchor,
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_Ind`. See `transitional/wml.xsd:618`. */
export function readCT_Ind(cur: XmlCursor, ctx: ReadContext): CT_Ind {
  const start = requireStart(cur, 'CT_Ind');
  let start_: ST_SignedTwipsMeasure | undefined;
  let startChars: ST_DecimalNumber | undefined;
  let end: ST_SignedTwipsMeasure | undefined;
  let endChars: ST_DecimalNumber | undefined;
  let left: ST_SignedTwipsMeasure | undefined;
  let leftChars: ST_DecimalNumber | undefined;
  let right: ST_SignedTwipsMeasure | undefined;
  let rightChars: ST_DecimalNumber | undefined;
  let hanging: ST_TwipsMeasure | undefined;
  let hangingChars: ST_DecimalNumber | undefined;
  let firstLine: ST_TwipsMeasure | undefined;
  let firstLineChars: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'start': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            start_ = v;
            continue;
          }
          ctx.invalidValue('CT_Ind', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
        case 'startChars': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            startChars = v;
            continue;
          }
          ctx.invalidValue('CT_Ind', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'end': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            end = v;
            continue;
          }
          ctx.invalidValue('CT_Ind', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
        case 'endChars': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            endChars = v;
            continue;
          }
          ctx.invalidValue('CT_Ind', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'left': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            left = v;
            continue;
          }
          ctx.invalidValue('CT_Ind', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
        case 'leftChars': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            leftChars = v;
            continue;
          }
          ctx.invalidValue('CT_Ind', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'right': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            right = v;
            continue;
          }
          ctx.invalidValue('CT_Ind', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
        case 'rightChars': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            rightChars = v;
            continue;
          }
          ctx.invalidValue('CT_Ind', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'hanging': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            hanging = v;
            continue;
          }
          ctx.invalidValue('CT_Ind', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'hangingChars': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            hangingChars = v;
            continue;
          }
          ctx.invalidValue('CT_Ind', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'firstLine': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            firstLine = v;
            continue;
          }
          ctx.invalidValue('CT_Ind', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'firstLineChars': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            firstLineChars = v;
            continue;
          }
          ctx.invalidValue('CT_Ind', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Ind', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Ind', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Ind', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Ind', cur);
    cur.next();
  }
  return {
    start: start_,
    startChars,
    end,
    endChars,
    left,
    leftChars,
    right,
    rightChars,
    hanging,
    hangingChars,
    firstLine,
    firstLineChars,
    $unknownAttrs,
  };
}

/** Read a `CT_Jc`. See `transitional/wml.xsd:657`. */
export function readCT_Jc(cur: XmlCursor, ctx: ReadContext): CT_Jc {
  const start = requireStart(cur, 'CT_Jc');
  let val: ST_Jc | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Jc(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Jc', a, 'ST_Jc', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Jc', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Jc', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Jc', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Jc', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Jc', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_JcTable`. See `transitional/wml.xsd:660`. */
export function readCT_JcTable(cur: XmlCursor, ctx: ReadContext): CT_JcTable {
  const start = requireStart(cur, 'CT_JcTable');
  let val: ST_JcTable | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_JcTable(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_JcTable', a, 'ST_JcTable', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_JcTable', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_JcTable', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_JcTable', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_JcTable', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_JcTable', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Kinsoku`. See `transitional/wml.xsd:794`. */
export function readCT_Kinsoku(cur: XmlCursor, ctx: ReadContext): CT_Kinsoku {
  const start = requireStart(cur, 'CT_Kinsoku');
  let lang: ST_Lang | undefined;
  let val: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'lang': {
          const v = parseST_Lang(a.value);
          if (v !== undefined) {
            lang = v;
            continue;
          }
          ctx.invalidValue('CT_Kinsoku', a, 'ST_Lang', cur);
          break;
        }
        case 'val': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Kinsoku', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Kinsoku', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (lang === undefined) ctx.missingRequired('CT_Kinsoku', 'lang', cur);
  if (val === undefined) ctx.missingRequired('CT_Kinsoku', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Kinsoku', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Kinsoku', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Kinsoku', cur);
    cur.next();
  }
  return {
    lang,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Lang`. See `transitional/wml.xsd:168`. */
export function readCT_Lang(cur: XmlCursor, ctx: ReadContext): CT_Lang {
  const start = requireStart(cur, 'CT_Lang');
  let val: ST_Lang | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Lang(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Lang', a, 'ST_Lang', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Lang', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Lang', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Lang', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Lang', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Lang', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Language`. See `transitional/wml.xsd:497`. */
export function readCT_Language(cur: XmlCursor, ctx: ReadContext): CT_Language {
  const start = requireStart(cur, 'CT_Language');
  let val: ST_Lang | undefined;
  let eastAsia: ST_Lang | undefined;
  let bidi: ST_Lang | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Lang(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Language', a, 'ST_Lang', cur);
          break;
        }
        case 'eastAsia': {
          const v = parseST_Lang(a.value);
          if (v !== undefined) {
            eastAsia = v;
            continue;
          }
          ctx.invalidValue('CT_Language', a, 'ST_Lang', cur);
          break;
        }
        case 'bidi': {
          const v = parseST_Lang(a.value);
          if (v !== undefined) {
            bidi = v;
            continue;
          }
          ctx.invalidValue('CT_Language', a, 'ST_Lang', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Language', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Language', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Language', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Language', cur);
    cur.next();
  }
  return {
    val,
    eastAsia,
    bidi,
    $unknownAttrs,
  };
}

/** Read a `CT_LatentStyles`. See `transitional/wml.xsd:3269`. */
export function readCT_LatentStyles(cur: XmlCursor, ctx: ReadContext): CT_LatentStyles {
  const start = requireStart(cur, 'CT_LatentStyles');
  let defLockedState: ST_OnOff | undefined;
  let defUIPriority: ST_DecimalNumber | undefined;
  let defSemiHidden: ST_OnOff | undefined;
  let defUnhideWhenUsed: ST_OnOff | undefined;
  let defQFormat: ST_OnOff | undefined;
  let count: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'defLockedState': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            defLockedState = v;
            continue;
          }
          ctx.invalidValue('CT_LatentStyles', a, 'ST_OnOff', cur);
          break;
        }
        case 'defUIPriority': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            defUIPriority = v;
            continue;
          }
          ctx.invalidValue('CT_LatentStyles', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'defSemiHidden': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            defSemiHidden = v;
            continue;
          }
          ctx.invalidValue('CT_LatentStyles', a, 'ST_OnOff', cur);
          break;
        }
        case 'defUnhideWhenUsed': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            defUnhideWhenUsed = v;
            continue;
          }
          ctx.invalidValue('CT_LatentStyles', a, 'ST_OnOff', cur);
          break;
        }
        case 'defQFormat': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            defQFormat = v;
            continue;
          }
          ctx.invalidValue('CT_LatentStyles', a, 'ST_OnOff', cur);
          break;
        }
        case 'count': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            count = v;
            continue;
          }
          ctx.invalidValue('CT_LatentStyles', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LatentStyles', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const lsdException: CT_LsdException[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LatentStyles', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'lsdException': {
            lsdException.push(readCT_LsdException(cur, ctx));
            $slot = 0;
            $index = lsdException.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LatentStyles', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LatentStyles', cur);
    cur.next();
  }

  return {
    lsdException,
    defLockedState,
    defUIPriority,
    defSemiHidden,
    defUnhideWhenUsed,
    defQFormat,
    count,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LevelSuffix`. See `transitional/wml.xsd:3121`. */
export function readCT_LevelSuffix(cur: XmlCursor, ctx: ReadContext): CT_LevelSuffix {
  const start = requireStart(cur, 'CT_LevelSuffix');
  let val: ST_LevelSuffix | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_LevelSuffix(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_LevelSuffix', a, 'ST_LevelSuffix', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LevelSuffix', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_LevelSuffix', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LevelSuffix', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LevelSuffix', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LevelSuffix', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_LevelText`. See `transitional/wml.xsd:3124`. */
export function readCT_LevelText(cur: XmlCursor, ctx: ReadContext): CT_LevelText {
  const start = requireStart(cur, 'CT_LevelText');
  let val: ST_String | undefined;
  let null_: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_LevelText', a, 'ST_String', cur);
          break;
        }
        case 'null': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            null_ = v;
            continue;
          }
          ctx.invalidValue('CT_LevelText', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LevelText', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LevelText', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LevelText', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LevelText', cur);
    cur.next();
  }
  return {
    val,
    null: null_,
    $unknownAttrs,
  };
}

/** Read a `CT_LineNumber`. See `transitional/wml.xsd:1527`. */
export function readCT_LineNumber(cur: XmlCursor, ctx: ReadContext): CT_LineNumber {
  const start = requireStart(cur, 'CT_LineNumber');
  let countBy: ST_DecimalNumber | undefined;
  let start_: ST_DecimalNumber | undefined;
  let distance: ST_TwipsMeasure | undefined;
  let restart: ST_LineNumberRestart | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'countBy': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            countBy = v;
            continue;
          }
          ctx.invalidValue('CT_LineNumber', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'start': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            start_ = v;
            continue;
          }
          ctx.invalidValue('CT_LineNumber', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'distance': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            distance = v;
            continue;
          }
          ctx.invalidValue('CT_LineNumber', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'restart': {
          const v = parseST_LineNumberRestart(a.value);
          if (v !== undefined) {
            restart = v;
            continue;
          }
          ctx.invalidValue('CT_LineNumber', a, 'ST_LineNumberRestart', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LineNumber', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LineNumber', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LineNumber', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LineNumber', cur);
    cur.next();
  }
  return {
    countBy,
    start: start_,
    distance,
    restart,
    $unknownAttrs,
  };
}

/** Read a `CT_Lock`. See `transitional/wml.xsd:1987`. */
export function readCT_Lock(cur: XmlCursor, ctx: ReadContext): CT_Lock {
  const start = requireStart(cur, 'CT_Lock');
  let val: ST_Lock | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Lock(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Lock', a, 'ST_Lock', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Lock', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Lock', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Lock', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Lock', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_LongHexNumber`. See `transitional/wml.xsd:31`. */
export function readCT_LongHexNumber(cur: XmlCursor, ctx: ReadContext): CT_LongHexNumber {
  const start = requireStart(cur, 'CT_LongHexNumber');
  let val: ST_LongHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_LongHexNumber', a, 'ST_LongHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LongHexNumber', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_LongHexNumber', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LongHexNumber', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LongHexNumber', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LongHexNumber', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_LsdException`. See `transitional/wml.xsd:3261`. */
export function readCT_LsdException(cur: XmlCursor, ctx: ReadContext): CT_LsdException {
  const start = requireStart(cur, 'CT_LsdException');
  let name: ST_String | undefined;
  let locked: ST_OnOff | undefined;
  let uiPriority: ST_DecimalNumber | undefined;
  let semiHidden: ST_OnOff | undefined;
  let unhideWhenUsed: ST_OnOff | undefined;
  let qFormat: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'name': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_LsdException', a, 'ST_String', cur);
          break;
        }
        case 'locked': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            locked = v;
            continue;
          }
          ctx.invalidValue('CT_LsdException', a, 'ST_OnOff', cur);
          break;
        }
        case 'uiPriority': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            uiPriority = v;
            continue;
          }
          ctx.invalidValue('CT_LsdException', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'semiHidden': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            semiHidden = v;
            continue;
          }
          ctx.invalidValue('CT_LsdException', a, 'ST_OnOff', cur);
          break;
        }
        case 'unhideWhenUsed': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            unhideWhenUsed = v;
            continue;
          }
          ctx.invalidValue('CT_LsdException', a, 'ST_OnOff', cur);
          break;
        }
        case 'qFormat': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            qFormat = v;
            continue;
          }
          ctx.invalidValue('CT_LsdException', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LsdException', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_LsdException', 'name', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LsdException', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LsdException', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LsdException', cur);
    cur.next();
  }
  return {
    name,
    locked,
    uiPriority,
    semiHidden,
    unhideWhenUsed,
    qFormat,
    $unknownAttrs,
  };
}

/** Read a `CT_Lvl`. See `transitional/wml.xsd:3133`. */
export function readCT_Lvl(cur: XmlCursor, ctx: ReadContext): CT_Lvl {
  const start = requireStart(cur, 'CT_Lvl');
  let ilvl: ST_DecimalNumber | undefined;
  let tplc: ST_LongHexNumber | undefined;
  let tentative: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'ilvl': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            ilvl = v;
            continue;
          }
          ctx.invalidValue('CT_Lvl', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'tplc': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            tplc = v;
            continue;
          }
          ctx.invalidValue('CT_Lvl', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'tentative': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            tentative = v;
            continue;
          }
          ctx.invalidValue('CT_Lvl', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Lvl', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (ilvl === undefined) ctx.missingRequired('CT_Lvl', 'ilvl', cur);

  let start_: CT_DecimalNumber | undefined;
  let numFmt: CT_NumFmt | undefined;
  let lvlRestart: CT_DecimalNumber | undefined;
  let pStyle: CT_String | undefined;
  let isLgl: CT_OnOff | undefined;
  let suff: CT_LevelSuffix | undefined;
  let lvlText: CT_LevelText | undefined;
  let lvlPicBulletId: CT_DecimalNumber | undefined;
  let legacy: CT_LvlLegacy | undefined;
  let lvlJc: CT_Jc | undefined;
  let pPr: CT_PPrGeneral | undefined;
  let rPr: CT_RPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Lvl', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'start': {
            start_ = readCT_DecimalNumber(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'numFmt': {
            numFmt = readCT_NumFmt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'lvlRestart': {
            lvlRestart = readCT_DecimalNumber(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'pStyle': {
            pStyle = readCT_String(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'isLgl': {
            isLgl = readCT_OnOff(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'suff': {
            suff = readCT_LevelSuffix(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'lvlText': {
            lvlText = readCT_LevelText(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'lvlPicBulletId': {
            lvlPicBulletId = readCT_DecimalNumber(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'legacy': {
            legacy = readCT_LvlLegacy(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'lvlJc': {
            lvlJc = readCT_Jc(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'pPr': {
            pPr = readCT_PPrGeneral(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'rPr': {
            rPr = readCT_RPr(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Lvl', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Lvl', cur);
    cur.next();
  }

  return {
    start: start_,
    numFmt,
    lvlRestart,
    pStyle,
    isLgl,
    suff,
    lvlText,
    lvlPicBulletId,
    legacy,
    lvlJc,
    pPr,
    rPr,
    ilvl,
    tplc,
    tentative,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LvlLegacy`. See `transitional/wml.xsd:3128`. */
export function readCT_LvlLegacy(cur: XmlCursor, ctx: ReadContext): CT_LvlLegacy {
  const start = requireStart(cur, 'CT_LvlLegacy');
  let legacy: ST_OnOff | undefined;
  let legacySpace: ST_TwipsMeasure | undefined;
  let legacyIndent: ST_SignedTwipsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'legacy': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            legacy = v;
            continue;
          }
          ctx.invalidValue('CT_LvlLegacy', a, 'ST_OnOff', cur);
          break;
        }
        case 'legacySpace': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            legacySpace = v;
            continue;
          }
          ctx.invalidValue('CT_LvlLegacy', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'legacyIndent': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            legacyIndent = v;
            continue;
          }
          ctx.invalidValue('CT_LvlLegacy', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LvlLegacy', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LvlLegacy', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LvlLegacy', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LvlLegacy', cur);
    cur.next();
  }
  return {
    legacy,
    legacySpace,
    legacyIndent,
    $unknownAttrs,
  };
}

/** Read a `CT_MacroName`. See `transitional/wml.xsd:101`. */
export function readCT_MacroName(cur: XmlCursor, ctx: ReadContext): CT_MacroName {
  const start = requireStart(cur, 'CT_MacroName');
  let val: ST_MacroName | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_MacroName(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_MacroName', a, 'ST_MacroName', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MacroName', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_MacroName', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MacroName', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_MacroName', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MacroName', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_MailMerge`. See `transitional/wml.xsd:2689`. */
export function readCT_MailMerge(cur: XmlCursor, ctx: ReadContext): CT_MailMerge {
  const start = requireStart(cur, 'CT_MailMerge');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_MailMerge', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let mainDocumentType: CT_MailMergeDocType | undefined;
  let linkToQuery: CT_OnOff | undefined;
  let dataType: CT_MailMergeDataType | undefined;
  let connectString: CT_String | undefined;
  let query: CT_String | undefined;
  let dataSource: CT_Rel | undefined;
  let headerSource: CT_Rel | undefined;
  let doNotSuppressBlankLines: CT_OnOff | undefined;
  let destination: CT_MailMergeDest | undefined;
  let addressFieldName: CT_String | undefined;
  let mailSubject: CT_String | undefined;
  let mailAsAttachment: CT_OnOff | undefined;
  let viewMergedData: CT_OnOff | undefined;
  let activeRecord: CT_DecimalNumber | undefined;
  let checkErrors: CT_DecimalNumber | undefined;
  let odso: CT_Odso | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MailMerge', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'mainDocumentType': {
            mainDocumentType = readCT_MailMergeDocType(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'linkToQuery': {
            linkToQuery = readCT_OnOff(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'dataType': {
            dataType = readCT_MailMergeDataType(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'connectString': {
            connectString = readCT_String(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'query': {
            query = readCT_String(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'dataSource': {
            dataSource = readCT_Rel(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'headerSource': {
            headerSource = readCT_Rel(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'doNotSuppressBlankLines': {
            doNotSuppressBlankLines = readCT_OnOff(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'destination': {
            destination = readCT_MailMergeDest(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'addressFieldName': {
            addressFieldName = readCT_String(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'mailSubject': {
            mailSubject = readCT_String(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'mailAsAttachment': {
            mailAsAttachment = readCT_OnOff(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'viewMergedData': {
            viewMergedData = readCT_OnOff(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'activeRecord': {
            activeRecord = readCT_DecimalNumber(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'checkErrors': {
            checkErrors = readCT_DecimalNumber(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'odso': {
            odso = readCT_Odso(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_MailMerge', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MailMerge', cur);
    cur.next();
  }

  return {
    mainDocumentType,
    linkToQuery,
    dataType,
    connectString,
    query,
    dataSource,
    headerSource,
    doNotSuppressBlankLines,
    destination,
    addressFieldName,
    mailSubject,
    mailAsAttachment,
    viewMergedData,
    activeRecord,
    checkErrors,
    odso,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_MailMergeDataType`. See `transitional/wml.xsd:764`. */
export function readCT_MailMergeDataType(cur: XmlCursor, ctx: ReadContext): CT_MailMergeDataType {
  const start = requireStart(cur, 'CT_MailMergeDataType');
  let val: ST_MailMergeDataType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_MailMergeDataType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_MailMergeDataType', a, 'ST_MailMergeDataType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MailMergeDataType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_MailMergeDataType', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MailMergeDataType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_MailMergeDataType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MailMergeDataType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_MailMergeDest`. See `transitional/wml.xsd:775`. */
export function readCT_MailMergeDest(cur: XmlCursor, ctx: ReadContext): CT_MailMergeDest {
  const start = requireStart(cur, 'CT_MailMergeDest');
  let val: ST_MailMergeDest | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_MailMergeDest(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_MailMergeDest', a, 'ST_MailMergeDest', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MailMergeDest', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_MailMergeDest', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MailMergeDest', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_MailMergeDest', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MailMergeDest', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_MailMergeDocType`. See `transitional/wml.xsd:758`. */
export function readCT_MailMergeDocType(cur: XmlCursor, ctx: ReadContext): CT_MailMergeDocType {
  const start = requireStart(cur, 'CT_MailMergeDocType');
  let val: ST_MailMergeDocType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_MailMergeDocType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_MailMergeDocType', a, 'ST_MailMergeDocType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MailMergeDocType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_MailMergeDocType', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MailMergeDocType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_MailMergeDocType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MailMergeDocType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_MailMergeOdsoFMDFieldType`. See `transitional/wml.xsd:784`. */
export function readCT_MailMergeOdsoFMDFieldType(cur: XmlCursor, ctx: ReadContext): CT_MailMergeOdsoFMDFieldType {
  const start = requireStart(cur, 'CT_MailMergeOdsoFMDFieldType');
  let val: ST_MailMergeOdsoFMDFieldType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_MailMergeOdsoFMDFieldType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_MailMergeOdsoFMDFieldType', a, 'ST_MailMergeOdsoFMDFieldType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MailMergeOdsoFMDFieldType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_MailMergeOdsoFMDFieldType', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MailMergeOdsoFMDFieldType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_MailMergeOdsoFMDFieldType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MailMergeOdsoFMDFieldType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_MailMergeSourceType`. See `transitional/wml.xsd:2673`. */
export function readCT_MailMergeSourceType(cur: XmlCursor, ctx: ReadContext): CT_MailMergeSourceType {
  const start = requireStart(cur, 'CT_MailMergeSourceType');
  let val: ST_MailMergeSourceType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_MailMergeSourceType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_MailMergeSourceType', a, 'ST_MailMergeSourceType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MailMergeSourceType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_MailMergeSourceType', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MailMergeSourceType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_MailMergeSourceType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MailMergeSourceType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Markup`. See `transitional/wml.xsd:841`. */
export function readCT_Markup(cur: XmlCursor, ctx: ReadContext): CT_Markup {
  const start = requireStart(cur, 'CT_Markup');
  let id: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Markup', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Markup', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_Markup', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Markup', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Markup', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Markup', cur);
    cur.next();
  }
  return {
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_MarkupRange`. See `transitional/wml.xsd:867`. */
export function readCT_MarkupRange(cur: XmlCursor, ctx: ReadContext): CT_MarkupRange {
  const start = requireStart(cur, 'CT_MarkupRange');
  let id: ST_DecimalNumber | undefined;
  let displacedByCustomXml: ST_DisplacedByCustomXml | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_MarkupRange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'displacedByCustomXml': {
          const v = parseST_DisplacedByCustomXml(a.value);
          if (v !== undefined) {
            displacedByCustomXml = v;
            continue;
          }
          ctx.invalidValue('CT_MarkupRange', a, 'ST_DisplacedByCustomXml', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MarkupRange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_MarkupRange', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MarkupRange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_MarkupRange', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MarkupRange', cur);
    cur.next();
  }
  return {
    id,
    displacedByCustomXml,
    $unknownAttrs,
  };
}

/** Read a `CT_MathCtrlDel`. See `transitional/wml.xsd:1894`. */
export function readCT_MathCtrlDel(cur: XmlCursor, ctx: ReadContext): CT_MathCtrlDel {
  const start = requireStart(cur, 'CT_MathCtrlDel');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_MathCtrlDel', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_MathCtrlDel', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_MathCtrlDel', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MathCtrlDel', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_MathCtrlDel', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_MathCtrlDel', 'author', cur);

  let rPr: CT_RPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MathCtrlDel', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rPr': {
            rPr = readCT_RPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_MathCtrlDel', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MathCtrlDel', cur);
    cur.next();
  }

  return {
    rPr,
    id,
    author,
    date,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_MathCtrlIns`. See `transitional/wml.xsd:1884`. */
export function readCT_MathCtrlIns(cur: XmlCursor, ctx: ReadContext): CT_MathCtrlIns {
  const start = requireStart(cur, 'CT_MathCtrlIns');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_MathCtrlIns', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_MathCtrlIns', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_MathCtrlIns', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MathCtrlIns', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_MathCtrlIns', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_MathCtrlIns', 'author', cur);

  let content: CT_MathCtrlIns_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MathCtrlIns', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'del': {
            content = { kind: 'del', value: readCT_RPrChange(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'rPr': {
            content = { kind: 'rPr', value: readCT_RPr(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_MathCtrlIns', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MathCtrlIns', cur);
    cur.next();
  }

  return {
    content,
    id,
    author,
    date,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_MoveBookmark`. See `transitional/wml.xsd:889`. */
export function readCT_MoveBookmark(cur: XmlCursor, ctx: ReadContext): CT_MoveBookmark {
  const start = requireStart(cur, 'CT_MoveBookmark');
  let id: ST_DecimalNumber | undefined;
  let displacedByCustomXml: ST_DisplacedByCustomXml | undefined;
  let colFirst: ST_DecimalNumber | undefined;
  let colLast: ST_DecimalNumber | undefined;
  let name: ST_String | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_MoveBookmark', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'displacedByCustomXml': {
          const v = parseST_DisplacedByCustomXml(a.value);
          if (v !== undefined) {
            displacedByCustomXml = v;
            continue;
          }
          ctx.invalidValue('CT_MoveBookmark', a, 'ST_DisplacedByCustomXml', cur);
          break;
        }
        case 'colFirst': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            colFirst = v;
            continue;
          }
          ctx.invalidValue('CT_MoveBookmark', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'colLast': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            colLast = v;
            continue;
          }
          ctx.invalidValue('CT_MoveBookmark', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'name': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_MoveBookmark', a, 'ST_String', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_MoveBookmark', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_MoveBookmark', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MoveBookmark', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_MoveBookmark', 'id', cur);
  if (name === undefined) ctx.missingRequired('CT_MoveBookmark', 'name', cur);
  if (author === undefined) ctx.missingRequired('CT_MoveBookmark', 'author', cur);
  if (date === undefined) ctx.missingRequired('CT_MoveBookmark', 'date', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MoveBookmark', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_MoveBookmark', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MoveBookmark', cur);
    cur.next();
  }
  return {
    id,
    displacedByCustomXml,
    colFirst,
    colLast,
    name,
    author,
    date,
    $unknownAttrs,
  };
}

/** Read a `CT_MultiLevelType`. See `transitional/wml.xsd:3159`. */
export function readCT_MultiLevelType(cur: XmlCursor, ctx: ReadContext): CT_MultiLevelType {
  const start = requireStart(cur, 'CT_MultiLevelType');
  let val: ST_MultiLevelType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_MultiLevelType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_MultiLevelType', a, 'ST_MultiLevelType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MultiLevelType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_MultiLevelType', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MultiLevelType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_MultiLevelType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MultiLevelType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Num`. See `transitional/wml.xsd:3181`. */
export function readCT_Num(cur: XmlCursor, ctx: ReadContext): CT_Num {
  const start = requireStart(cur, 'CT_Num');
  let numId: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'numId': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            numId = v;
            continue;
          }
          ctx.invalidValue('CT_Num', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Num', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (numId === undefined) ctx.missingRequired('CT_Num', 'numId', cur);

  let abstractNumId: CT_DecimalNumber | undefined;
  const lvlOverride: CT_NumLvl[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Num', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'abstractNumId': {
            abstractNumId = readCT_DecimalNumber(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lvlOverride': {
            lvlOverride.push(readCT_NumLvl(cur, ctx));
            $slot = 1;
            $index = lvlOverride.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Num', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Num', cur);
    cur.next();
  }

  return {
    abstractNumId,
    lvlOverride,
    numId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NumFmt`. See `transitional/wml.xsd:2567`. */
export function readCT_NumFmt(cur: XmlCursor, ctx: ReadContext): CT_NumFmt {
  const start = requireStart(cur, 'CT_NumFmt');
  let val: ST_NumberFormat | undefined;
  let format: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_NumberFormat(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_NumFmt', a, 'ST_NumberFormat', cur);
          break;
        }
        case 'format': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            format = v;
            continue;
          }
          ctx.invalidValue('CT_NumFmt', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NumFmt', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_NumFmt', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NumFmt', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_NumFmt', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NumFmt', cur);
    cur.next();
  }
  return {
    val,
    format,
    $unknownAttrs,
  };
}

/** Read a `CT_NumLvl`. See `transitional/wml.xsd:3174`. */
export function readCT_NumLvl(cur: XmlCursor, ctx: ReadContext): CT_NumLvl {
  const start = requireStart(cur, 'CT_NumLvl');
  let ilvl: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'ilvl': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            ilvl = v;
            continue;
          }
          ctx.invalidValue('CT_NumLvl', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NumLvl', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (ilvl === undefined) ctx.missingRequired('CT_NumLvl', 'ilvl', cur);

  let startOverride: CT_DecimalNumber | undefined;
  let lvl: CT_Lvl | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NumLvl', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'startOverride': {
            startOverride = readCT_DecimalNumber(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lvl': {
            lvl = readCT_Lvl(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NumLvl', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NumLvl', cur);
    cur.next();
  }

  return {
    startOverride,
    lvl,
    ilvl,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NumPicBullet`. See `transitional/wml.xsd:3107`. */
export function readCT_NumPicBullet(cur: XmlCursor, ctx: ReadContext): CT_NumPicBullet {
  const start = requireStart(cur, 'CT_NumPicBullet');
  let numPicBulletId: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'numPicBulletId': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            numPicBulletId = v;
            continue;
          }
          ctx.invalidValue('CT_NumPicBullet', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NumPicBullet', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (numPicBulletId === undefined) ctx.missingRequired('CT_NumPicBullet', 'numPicBulletId', cur);

  let content: CT_NumPicBullet_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NumPicBullet', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'pict': {
            content = { kind: 'pict', value: readCT_Picture(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'drawing': {
            content = { kind: 'drawing', value: readCT_Drawing(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NumPicBullet', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NumPicBullet', cur);
    cur.next();
  }

  return {
    content,
    numPicBulletId,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NumPr`. See `transitional/wml.xsd:1052`. */
export function readCT_NumPr(cur: XmlCursor, ctx: ReadContext): CT_NumPr {
  const start = requireStart(cur, 'CT_NumPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NumPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ilvl: CT_DecimalNumber | undefined;
  let numId: CT_DecimalNumber | undefined;
  let numberingChange: CT_TrackChangeNumbering | undefined;
  let ins: CT_TrackChange | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NumPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'ilvl': {
            ilvl = readCT_DecimalNumber(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'numId': {
            numId = readCT_DecimalNumber(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'numberingChange': {
            numberingChange = readCT_TrackChangeNumbering(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'ins': {
            ins = readCT_TrackChange(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NumPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NumPr', cur);
    cur.next();
  }

  return {
    ilvl,
    numId,
    numberingChange,
    ins,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NumRestart`. See `transitional/wml.xsd:2578`. */
export function readCT_NumRestart(cur: XmlCursor, ctx: ReadContext): CT_NumRestart {
  const start = requireStart(cur, 'CT_NumRestart');
  let val: ST_RestartNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_RestartNumber(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_NumRestart', a, 'ST_RestartNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NumRestart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_NumRestart', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NumRestart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_NumRestart', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NumRestart', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Numbering`. See `transitional/wml.xsd:3188`. */
export function readCT_Numbering(cur: XmlCursor, ctx: ReadContext): CT_Numbering {
  const start = requireStart(cur, 'CT_Numbering');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Numbering', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const numPicBullet: CT_NumPicBullet[] = [];
  const abstractNum: CT_AbstractNum[] = [];
  const num: CT_Num[] = [];
  let numIdMacAtCleanup: CT_DecimalNumber | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Numbering', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'numPicBullet': {
            numPicBullet.push(readCT_NumPicBullet(cur, ctx));
            $slot = 0;
            $index = numPicBullet.length - 1;
            continue;
          }
          case 'abstractNum': {
            abstractNum.push(readCT_AbstractNum(cur, ctx));
            $slot = 1;
            $index = abstractNum.length - 1;
            continue;
          }
          case 'num': {
            num.push(readCT_Num(cur, ctx));
            $slot = 2;
            $index = num.length - 1;
            continue;
          }
          case 'numIdMacAtCleanup': {
            numIdMacAtCleanup = readCT_DecimalNumber(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Numbering', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Numbering', cur);
    cur.next();
  }

  return {
    numPicBullet,
    abstractNum,
    num,
    numIdMacAtCleanup,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Object`. See `transitional/wml.xsd:1167`. */
export function readCT_Object(cur: XmlCursor, ctx: ReadContext): CT_Object {
  const start = requireStart(cur, 'CT_Object');
  let dxaOrig: ST_TwipsMeasure | undefined;
  let dyaOrig: ST_TwipsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'dxaOrig': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            dxaOrig = v;
            continue;
          }
          ctx.invalidValue('CT_Object', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'dyaOrig': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            dyaOrig = v;
            continue;
          }
          ctx.invalidValue('CT_Object', a, 'ST_TwipsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Object', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const any: RawNode[] = [];
  const any2: RawNode[] = [];
  let drawing: CT_Drawing | undefined;
  let content: CT_Object_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Object', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'drawing': {
            drawing = readCT_Drawing(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'control': {
            content = { kind: 'control', value: readCT_Control(cur, ctx) };
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'objectLink': {
            content = { kind: 'objectLink', value: readCT_ObjectLink(cur, ctx) };
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'objectEmbed': {
            content = { kind: 'objectEmbed', value: readCT_ObjectEmbed(cur, ctx) };
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'movie': {
            content = { kind: 'movie', value: readCT_Rel(cur, ctx) };
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === ctx.uris['vml']) {
        any.push(cur.skipToRaw());
        $slot = 0;
        $index = any.length - 1;
        continue;
      }
      if (ev.uri === ctx.uris['vml-office']) {
        any2.push(cur.skipToRaw());
        $slot = 1;
        $index = any2.length - 1;
        continue;
      }
      ctx.unexpectedElement('CT_Object', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Object', cur);
    cur.next();
  }

  return {
    any,
    any2,
    drawing,
    content,
    dxaOrig,
    dyaOrig,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ObjectEmbed`. See `transitional/wml.xsd:1198`. */
export function readCT_ObjectEmbed(cur: XmlCursor, ctx: ReadContext): CT_ObjectEmbed {
  const start = requireStart(cur, 'CT_ObjectEmbed');
  let drawAspect: ST_ObjectDrawAspect | undefined;
  let id: ST_RelationshipId | undefined;
  let progId: ST_String | undefined;
  let shapeId: ST_String | undefined;
  let fieldCodes: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_ObjectEmbed', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'drawAspect': {
          const v = parseST_ObjectDrawAspect(a.value);
          if (v !== undefined) {
            drawAspect = v;
            continue;
          }
          ctx.invalidValue('CT_ObjectEmbed', a, 'ST_ObjectDrawAspect', cur);
          break;
        }
        case 'progId': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            progId = v;
            continue;
          }
          ctx.invalidValue('CT_ObjectEmbed', a, 'ST_String', cur);
          break;
        }
        case 'shapeId': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            shapeId = v;
            continue;
          }
          ctx.invalidValue('CT_ObjectEmbed', a, 'ST_String', cur);
          break;
        }
        case 'fieldCodes': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            fieldCodes = v;
            continue;
          }
          ctx.invalidValue('CT_ObjectEmbed', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ObjectEmbed', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_ObjectEmbed', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ObjectEmbed', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ObjectEmbed', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ObjectEmbed', cur);
    cur.next();
  }
  return {
    drawAspect,
    id,
    progId,
    shapeId,
    fieldCodes,
    $unknownAttrs,
  };
}

/** Read a `CT_ObjectLink`. See `transitional/wml.xsd:1211`. */
export function readCT_ObjectLink(cur: XmlCursor, ctx: ReadContext): CT_ObjectLink {
  const start = requireStart(cur, 'CT_ObjectLink');
  let drawAspect: ST_ObjectDrawAspect | undefined;
  let id: ST_RelationshipId | undefined;
  let progId: ST_String | undefined;
  let shapeId: ST_String | undefined;
  let fieldCodes: ST_String | undefined;
  let updateMode: ST_ObjectUpdateMode | undefined;
  let lockedField: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_ObjectLink', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'drawAspect': {
          const v = parseST_ObjectDrawAspect(a.value);
          if (v !== undefined) {
            drawAspect = v;
            continue;
          }
          ctx.invalidValue('CT_ObjectLink', a, 'ST_ObjectDrawAspect', cur);
          break;
        }
        case 'progId': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            progId = v;
            continue;
          }
          ctx.invalidValue('CT_ObjectLink', a, 'ST_String', cur);
          break;
        }
        case 'shapeId': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            shapeId = v;
            continue;
          }
          ctx.invalidValue('CT_ObjectLink', a, 'ST_String', cur);
          break;
        }
        case 'fieldCodes': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            fieldCodes = v;
            continue;
          }
          ctx.invalidValue('CT_ObjectLink', a, 'ST_String', cur);
          break;
        }
        case 'updateMode': {
          const v = parseST_ObjectUpdateMode(a.value);
          if (v !== undefined) {
            updateMode = v;
            continue;
          }
          ctx.invalidValue('CT_ObjectLink', a, 'ST_ObjectUpdateMode', cur);
          break;
        }
        case 'lockedField': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            lockedField = v;
            continue;
          }
          ctx.invalidValue('CT_ObjectLink', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ObjectLink', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_ObjectLink', 'id', cur);
  if (updateMode === undefined) ctx.missingRequired('CT_ObjectLink', 'updateMode', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ObjectLink', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ObjectLink', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ObjectLink', cur);
    cur.next();
  }
  return {
    drawAspect,
    id,
    progId,
    shapeId,
    fieldCodes,
    updateMode,
    lockedField,
    $unknownAttrs,
  };
}

/** Read a `CT_Odso`. See `transitional/wml.xsd:2676`. */
export function readCT_Odso(cur: XmlCursor, ctx: ReadContext): CT_Odso {
  const start = requireStart(cur, 'CT_Odso');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Odso', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let udl: CT_String | undefined;
  let table: CT_String | undefined;
  let src: CT_Rel | undefined;
  let colDelim: CT_DecimalNumber | undefined;
  let type: CT_MailMergeSourceType | undefined;
  let fHdr: CT_OnOff | undefined;
  const fieldMapData: CT_OdsoFieldMapData[] = [];
  const recipientData: CT_Rel[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Odso', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'udl': {
            udl = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'table': {
            table = readCT_String(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'src': {
            src = readCT_Rel(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'colDelim': {
            colDelim = readCT_DecimalNumber(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'type': {
            type = readCT_MailMergeSourceType(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'fHdr': {
            fHdr = readCT_OnOff(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'fieldMapData': {
            fieldMapData.push(readCT_OdsoFieldMapData(cur, ctx));
            $slot = 6;
            $index = fieldMapData.length - 1;
            continue;
          }
          case 'recipientData': {
            recipientData.push(readCT_Rel(cur, ctx));
            $slot = 7;
            $index = recipientData.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Odso', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Odso', cur);
    cur.next();
  }

  return {
    udl,
    table,
    src,
    colDelim,
    type,
    fHdr,
    fieldMapData,
    recipientData,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OdsoFieldMapData`. See `transitional/wml.xsd:2650`. */
export function readCT_OdsoFieldMapData(cur: XmlCursor, ctx: ReadContext): CT_OdsoFieldMapData {
  const start = requireStart(cur, 'CT_OdsoFieldMapData');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_OdsoFieldMapData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let type: CT_MailMergeOdsoFMDFieldType | undefined;
  let name: CT_String | undefined;
  let mappedName: CT_String | undefined;
  let column: CT_DecimalNumber | undefined;
  let lid: CT_Lang | undefined;
  let dynamicAddress: CT_OnOff | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OdsoFieldMapData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'type': {
            type = readCT_MailMergeOdsoFMDFieldType(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'name': {
            name = readCT_String(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'mappedName': {
            mappedName = readCT_String(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'column': {
            column = readCT_DecimalNumber(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'lid': {
            lid = readCT_Lang(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'dynamicAddress': {
            dynamicAddress = readCT_OnOff(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OdsoFieldMapData', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OdsoFieldMapData', cur);
    cur.next();
  }

  return {
    type,
    name,
    mappedName,
    column,
    lid,
    dynamicAddress,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OnOff`. See `transitional/wml.xsd:23`. */
export function readCT_OnOff(cur: XmlCursor, ctx: ReadContext): CT_OnOff {
  const start = requireStart(cur, 'CT_OnOff');
  let val: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_OnOff', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OnOff', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OnOff', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_OnOff', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OnOff', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_OptimizeForBrowser`. See `transitional/wml.xsd:3056`. */
export function readCT_OptimizeForBrowser(cur: XmlCursor, ctx: ReadContext): CT_OptimizeForBrowser {
  const start = requireStart(cur, 'CT_OptimizeForBrowser');
  let val: ST_OnOff | undefined;
  let target: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_OptimizeForBrowser', a, 'ST_OnOff', cur);
          break;
        }
        case 'target': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            target = v;
            continue;
          }
          ctx.invalidValue('CT_OptimizeForBrowser', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OptimizeForBrowser', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OptimizeForBrowser', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_OptimizeForBrowser', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OptimizeForBrowser', cur);
    cur.next();
  }
  return {
    val,
    target,
    $unknownAttrs,
  };
}

/** Read a `CT_P`. See `transitional/wml.xsd:2236`. */
export function readCT_P(cur: XmlCursor, ctx: ReadContext): CT_P {
  const start = requireStart(cur, 'CT_P');
  let rsidRPr: ST_LongHexNumber | undefined;
  let rsidR: ST_LongHexNumber | undefined;
  let rsidDel: ST_LongHexNumber | undefined;
  let rsidP: ST_LongHexNumber | undefined;
  let rsidRDefault: ST_LongHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'rsidRPr': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidRPr = v;
            continue;
          }
          ctx.invalidValue('CT_P', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidR': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidR = v;
            continue;
          }
          ctx.invalidValue('CT_P', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidDel': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidDel = v;
            continue;
          }
          ctx.invalidValue('CT_P', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidP': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidP = v;
            continue;
          }
          ctx.invalidValue('CT_P', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidRDefault': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidRDefault = v;
            continue;
          }
          ctx.invalidValue('CT_P', a, 'ST_LongHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_P', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pPr: CT_PPr | undefined;
  const pContent: CT_P_PContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_P', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            pContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            pContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'pPr': {
            pPr = readCT_PPr(cur, ctx);
            continue;
          }
          case 'customXml': {
            pContent.push({ kind: 'customXml', value: readCT_CustomXmlRun(cur, ctx) });
            continue;
          }
          case 'smartTag': {
            pContent.push({ kind: 'smartTag', value: readCT_SmartTagRun(cur, ctx) });
            continue;
          }
          case 'sdt': {
            pContent.push({ kind: 'sdt', value: readCT_SdtRun(cur, ctx) });
            continue;
          }
          case 'dir': {
            pContent.push({ kind: 'dir', value: readCT_DirContentRun(cur, ctx) });
            continue;
          }
          case 'bdo': {
            pContent.push({ kind: 'bdo', value: readCT_BdoContentRun(cur, ctx) });
            continue;
          }
          case 'r': {
            pContent.push({ kind: 'r', value: readCT_R(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            pContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            pContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            pContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            pContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            pContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            pContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            pContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            pContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            pContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            pContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            pContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            pContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            pContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            pContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            pContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            pContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            pContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            pContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            pContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            pContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            pContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            pContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            pContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'fldSimple': {
            pContent.push({ kind: 'fldSimple', value: readCT_SimpleField(cur, ctx) });
            continue;
          }
          case 'hyperlink': {
            pContent.push({ kind: 'hyperlink', value: readCT_Hyperlink(cur, ctx) });
            continue;
          }
          case 'subDoc': {
            pContent.push({ kind: 'subDoc', value: readCT_Rel(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_P', ev.localName, ev.uri, cur);
      pContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_P', cur);
    cur.next();
  }

  return {
    pPr,
    pContent,
    rsidRPr,
    rsidR,
    rsidDel,
    rsidP,
    rsidRDefault,
    $unknownAttrs,
  };
}

/** Read a `CT_PBdr`. See `transitional/wml.xsd:1060`. */
export function readCT_PBdr(cur: XmlCursor, ctx: ReadContext): CT_PBdr {
  const start = requireStart(cur, 'CT_PBdr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PBdr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let top: CT_Border | undefined;
  let left: CT_Border | undefined;
  let bottom: CT_Border | undefined;
  let right: CT_Border | undefined;
  let between: CT_Border | undefined;
  let bar: CT_Border | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PBdr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'top': {
            top = readCT_Border(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'left': {
            left = readCT_Border(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'bottom': {
            bottom = readCT_Border(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'right': {
            right = readCT_Border(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'between': {
            between = readCT_Border(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'bar': {
            bar = readCT_Border(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PBdr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PBdr', cur);
    cur.next();
  }

  return {
    top,
    left,
    bottom,
    right,
    between,
    bar,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PPr`. See `transitional/wml.xsd:1087`. */
export function readCT_PPr(cur: XmlCursor, ctx: ReadContext): CT_PPr {
  const start = requireStart(cur, 'CT_PPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pStyle: CT_String | undefined;
  let keepNext: CT_OnOff | undefined;
  let keepLines: CT_OnOff | undefined;
  let pageBreakBefore: CT_OnOff | undefined;
  let framePr: CT_FramePr | undefined;
  let widowControl: CT_OnOff | undefined;
  let numPr: CT_NumPr | undefined;
  let suppressLineNumbers: CT_OnOff | undefined;
  let pBdr: CT_PBdr | undefined;
  let shd: CT_Shd | undefined;
  let tabs: CT_Tabs | undefined;
  let suppressAutoHyphens: CT_OnOff | undefined;
  let kinsoku: CT_OnOff | undefined;
  let wordWrap: CT_OnOff | undefined;
  let overflowPunct: CT_OnOff | undefined;
  let topLinePunct: CT_OnOff | undefined;
  let autoSpaceDE: CT_OnOff | undefined;
  let autoSpaceDN: CT_OnOff | undefined;
  let bidi: CT_OnOff | undefined;
  let adjustRightInd: CT_OnOff | undefined;
  let snapToGrid: CT_OnOff | undefined;
  let spacing: CT_Spacing | undefined;
  let ind: CT_Ind | undefined;
  let contextualSpacing: CT_OnOff | undefined;
  let mirrorIndents: CT_OnOff | undefined;
  let suppressOverlap: CT_OnOff | undefined;
  let jc: CT_Jc | undefined;
  let textDirection: CT_TextDirection | undefined;
  let textAlignment: CT_TextAlignment | undefined;
  let textboxTightWrap: CT_TextboxTightWrap | undefined;
  let outlineLvl: CT_DecimalNumber | undefined;
  let divId: CT_DecimalNumber | undefined;
  let cnfStyle: CT_Cnf | undefined;
  let rPr: CT_ParaRPr | undefined;
  let sectPr: CT_SectPr | undefined;
  let pPrChange: CT_PPrChange | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'pStyle': {
            pStyle = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'keepNext': {
            keepNext = readCT_OnOff(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'keepLines': {
            keepLines = readCT_OnOff(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'pageBreakBefore': {
            pageBreakBefore = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'framePr': {
            framePr = readCT_FramePr(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'widowControl': {
            widowControl = readCT_OnOff(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'numPr': {
            numPr = readCT_NumPr(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'suppressLineNumbers': {
            suppressLineNumbers = readCT_OnOff(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'pBdr': {
            pBdr = readCT_PBdr(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'shd': {
            shd = readCT_Shd(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tabs': {
            tabs = readCT_Tabs(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'suppressAutoHyphens': {
            suppressAutoHyphens = readCT_OnOff(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'kinsoku': {
            kinsoku = readCT_OnOff(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'wordWrap': {
            wordWrap = readCT_OnOff(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'overflowPunct': {
            overflowPunct = readCT_OnOff(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'topLinePunct': {
            topLinePunct = readCT_OnOff(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'autoSpaceDE': {
            autoSpaceDE = readCT_OnOff(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'autoSpaceDN': {
            autoSpaceDN = readCT_OnOff(cur, ctx);
            $slot = 17;
            $index = undefined;
            continue;
          }
          case 'bidi': {
            bidi = readCT_OnOff(cur, ctx);
            $slot = 18;
            $index = undefined;
            continue;
          }
          case 'adjustRightInd': {
            adjustRightInd = readCT_OnOff(cur, ctx);
            $slot = 19;
            $index = undefined;
            continue;
          }
          case 'snapToGrid': {
            snapToGrid = readCT_OnOff(cur, ctx);
            $slot = 20;
            $index = undefined;
            continue;
          }
          case 'spacing': {
            spacing = readCT_Spacing(cur, ctx);
            $slot = 21;
            $index = undefined;
            continue;
          }
          case 'ind': {
            ind = readCT_Ind(cur, ctx);
            $slot = 22;
            $index = undefined;
            continue;
          }
          case 'contextualSpacing': {
            contextualSpacing = readCT_OnOff(cur, ctx);
            $slot = 23;
            $index = undefined;
            continue;
          }
          case 'mirrorIndents': {
            mirrorIndents = readCT_OnOff(cur, ctx);
            $slot = 24;
            $index = undefined;
            continue;
          }
          case 'suppressOverlap': {
            suppressOverlap = readCT_OnOff(cur, ctx);
            $slot = 25;
            $index = undefined;
            continue;
          }
          case 'jc': {
            jc = readCT_Jc(cur, ctx);
            $slot = 26;
            $index = undefined;
            continue;
          }
          case 'textDirection': {
            textDirection = readCT_TextDirection(cur, ctx);
            $slot = 27;
            $index = undefined;
            continue;
          }
          case 'textAlignment': {
            textAlignment = readCT_TextAlignment(cur, ctx);
            $slot = 28;
            $index = undefined;
            continue;
          }
          case 'textboxTightWrap': {
            textboxTightWrap = readCT_TextboxTightWrap(cur, ctx);
            $slot = 29;
            $index = undefined;
            continue;
          }
          case 'outlineLvl': {
            outlineLvl = readCT_DecimalNumber(cur, ctx);
            $slot = 30;
            $index = undefined;
            continue;
          }
          case 'divId': {
            divId = readCT_DecimalNumber(cur, ctx);
            $slot = 31;
            $index = undefined;
            continue;
          }
          case 'cnfStyle': {
            cnfStyle = readCT_Cnf(cur, ctx);
            $slot = 32;
            $index = undefined;
            continue;
          }
          case 'rPr': {
            rPr = readCT_ParaRPr(cur, ctx);
            $slot = 33;
            $index = undefined;
            continue;
          }
          case 'sectPr': {
            sectPr = readCT_SectPr(cur, ctx);
            $slot = 34;
            $index = undefined;
            continue;
          }
          case 'pPrChange': {
            pPrChange = readCT_PPrChange(cur, ctx);
            $slot = 35;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PPr', cur);
    cur.next();
  }

  return {
    pStyle,
    keepNext,
    keepLines,
    pageBreakBefore,
    framePr,
    widowControl,
    numPr,
    suppressLineNumbers,
    pBdr,
    shd,
    tabs,
    suppressAutoHyphens,
    kinsoku,
    wordWrap,
    overflowPunct,
    topLinePunct,
    autoSpaceDE,
    autoSpaceDN,
    bidi,
    adjustRightInd,
    snapToGrid,
    spacing,
    ind,
    contextualSpacing,
    mirrorIndents,
    suppressOverlap,
    jc,
    textDirection,
    textAlignment,
    textboxTightWrap,
    outlineLvl,
    divId,
    cnfStyle,
    rPr,
    sectPr,
    pPrChange,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PPrBase`. See `transitional/wml.xsd:1098`. */
export function readCT_PPrBase(cur: XmlCursor, ctx: ReadContext): CT_PPrBase {
  const start = requireStart(cur, 'CT_PPrBase');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PPrBase', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pStyle: CT_String | undefined;
  let keepNext: CT_OnOff | undefined;
  let keepLines: CT_OnOff | undefined;
  let pageBreakBefore: CT_OnOff | undefined;
  let framePr: CT_FramePr | undefined;
  let widowControl: CT_OnOff | undefined;
  let numPr: CT_NumPr | undefined;
  let suppressLineNumbers: CT_OnOff | undefined;
  let pBdr: CT_PBdr | undefined;
  let shd: CT_Shd | undefined;
  let tabs: CT_Tabs | undefined;
  let suppressAutoHyphens: CT_OnOff | undefined;
  let kinsoku: CT_OnOff | undefined;
  let wordWrap: CT_OnOff | undefined;
  let overflowPunct: CT_OnOff | undefined;
  let topLinePunct: CT_OnOff | undefined;
  let autoSpaceDE: CT_OnOff | undefined;
  let autoSpaceDN: CT_OnOff | undefined;
  let bidi: CT_OnOff | undefined;
  let adjustRightInd: CT_OnOff | undefined;
  let snapToGrid: CT_OnOff | undefined;
  let spacing: CT_Spacing | undefined;
  let ind: CT_Ind | undefined;
  let contextualSpacing: CT_OnOff | undefined;
  let mirrorIndents: CT_OnOff | undefined;
  let suppressOverlap: CT_OnOff | undefined;
  let jc: CT_Jc | undefined;
  let textDirection: CT_TextDirection | undefined;
  let textAlignment: CT_TextAlignment | undefined;
  let textboxTightWrap: CT_TextboxTightWrap | undefined;
  let outlineLvl: CT_DecimalNumber | undefined;
  let divId: CT_DecimalNumber | undefined;
  let cnfStyle: CT_Cnf | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PPrBase', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'pStyle': {
            pStyle = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'keepNext': {
            keepNext = readCT_OnOff(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'keepLines': {
            keepLines = readCT_OnOff(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'pageBreakBefore': {
            pageBreakBefore = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'framePr': {
            framePr = readCT_FramePr(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'widowControl': {
            widowControl = readCT_OnOff(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'numPr': {
            numPr = readCT_NumPr(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'suppressLineNumbers': {
            suppressLineNumbers = readCT_OnOff(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'pBdr': {
            pBdr = readCT_PBdr(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'shd': {
            shd = readCT_Shd(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tabs': {
            tabs = readCT_Tabs(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'suppressAutoHyphens': {
            suppressAutoHyphens = readCT_OnOff(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'kinsoku': {
            kinsoku = readCT_OnOff(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'wordWrap': {
            wordWrap = readCT_OnOff(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'overflowPunct': {
            overflowPunct = readCT_OnOff(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'topLinePunct': {
            topLinePunct = readCT_OnOff(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'autoSpaceDE': {
            autoSpaceDE = readCT_OnOff(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'autoSpaceDN': {
            autoSpaceDN = readCT_OnOff(cur, ctx);
            $slot = 17;
            $index = undefined;
            continue;
          }
          case 'bidi': {
            bidi = readCT_OnOff(cur, ctx);
            $slot = 18;
            $index = undefined;
            continue;
          }
          case 'adjustRightInd': {
            adjustRightInd = readCT_OnOff(cur, ctx);
            $slot = 19;
            $index = undefined;
            continue;
          }
          case 'snapToGrid': {
            snapToGrid = readCT_OnOff(cur, ctx);
            $slot = 20;
            $index = undefined;
            continue;
          }
          case 'spacing': {
            spacing = readCT_Spacing(cur, ctx);
            $slot = 21;
            $index = undefined;
            continue;
          }
          case 'ind': {
            ind = readCT_Ind(cur, ctx);
            $slot = 22;
            $index = undefined;
            continue;
          }
          case 'contextualSpacing': {
            contextualSpacing = readCT_OnOff(cur, ctx);
            $slot = 23;
            $index = undefined;
            continue;
          }
          case 'mirrorIndents': {
            mirrorIndents = readCT_OnOff(cur, ctx);
            $slot = 24;
            $index = undefined;
            continue;
          }
          case 'suppressOverlap': {
            suppressOverlap = readCT_OnOff(cur, ctx);
            $slot = 25;
            $index = undefined;
            continue;
          }
          case 'jc': {
            jc = readCT_Jc(cur, ctx);
            $slot = 26;
            $index = undefined;
            continue;
          }
          case 'textDirection': {
            textDirection = readCT_TextDirection(cur, ctx);
            $slot = 27;
            $index = undefined;
            continue;
          }
          case 'textAlignment': {
            textAlignment = readCT_TextAlignment(cur, ctx);
            $slot = 28;
            $index = undefined;
            continue;
          }
          case 'textboxTightWrap': {
            textboxTightWrap = readCT_TextboxTightWrap(cur, ctx);
            $slot = 29;
            $index = undefined;
            continue;
          }
          case 'outlineLvl': {
            outlineLvl = readCT_DecimalNumber(cur, ctx);
            $slot = 30;
            $index = undefined;
            continue;
          }
          case 'divId': {
            divId = readCT_DecimalNumber(cur, ctx);
            $slot = 31;
            $index = undefined;
            continue;
          }
          case 'cnfStyle': {
            cnfStyle = readCT_Cnf(cur, ctx);
            $slot = 32;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PPrBase', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PPrBase', cur);
    cur.next();
  }

  return {
    pStyle,
    keepNext,
    keepLines,
    pageBreakBefore,
    framePr,
    widowControl,
    numPr,
    suppressLineNumbers,
    pBdr,
    shd,
    tabs,
    suppressAutoHyphens,
    kinsoku,
    wordWrap,
    overflowPunct,
    topLinePunct,
    autoSpaceDE,
    autoSpaceDN,
    bidi,
    adjustRightInd,
    snapToGrid,
    spacing,
    ind,
    contextualSpacing,
    mirrorIndents,
    suppressOverlap,
    jc,
    textDirection,
    textAlignment,
    textboxTightWrap,
    outlineLvl,
    divId,
    cnfStyle,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PPrChange`. See `transitional/wml.xsd:968`. */
export function readCT_PPrChange(cur: XmlCursor, ctx: ReadContext): CT_PPrChange {
  const start = requireStart(cur, 'CT_PPrChange');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_PPrChange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_PPrChange', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_PPrChange', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PPrChange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_PPrChange', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_PPrChange', 'author', cur);

  let pPr: CT_PPrBase | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PPrChange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'pPr': {
            pPr = readCT_PPrBase(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PPrChange', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PPrChange', cur);
    cur.next();
  }

  return {
    pPr,
    id,
    author,
    date,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PPrDefault`. See `transitional/wml.xsd:2837`. */
export function readCT_PPrDefault(cur: XmlCursor, ctx: ReadContext): CT_PPrDefault {
  const start = requireStart(cur, 'CT_PPrDefault');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PPrDefault', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pPr: CT_PPrGeneral | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PPrDefault', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'pPr': {
            pPr = readCT_PPrGeneral(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PPrDefault', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PPrDefault', cur);
    cur.next();
  }

  return {
    pPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PPrGeneral`. See `transitional/wml.xsd:1135`. */
export function readCT_PPrGeneral(cur: XmlCursor, ctx: ReadContext): CT_PPrGeneral {
  const start = requireStart(cur, 'CT_PPrGeneral');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PPrGeneral', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pStyle: CT_String | undefined;
  let keepNext: CT_OnOff | undefined;
  let keepLines: CT_OnOff | undefined;
  let pageBreakBefore: CT_OnOff | undefined;
  let framePr: CT_FramePr | undefined;
  let widowControl: CT_OnOff | undefined;
  let numPr: CT_NumPr | undefined;
  let suppressLineNumbers: CT_OnOff | undefined;
  let pBdr: CT_PBdr | undefined;
  let shd: CT_Shd | undefined;
  let tabs: CT_Tabs | undefined;
  let suppressAutoHyphens: CT_OnOff | undefined;
  let kinsoku: CT_OnOff | undefined;
  let wordWrap: CT_OnOff | undefined;
  let overflowPunct: CT_OnOff | undefined;
  let topLinePunct: CT_OnOff | undefined;
  let autoSpaceDE: CT_OnOff | undefined;
  let autoSpaceDN: CT_OnOff | undefined;
  let bidi: CT_OnOff | undefined;
  let adjustRightInd: CT_OnOff | undefined;
  let snapToGrid: CT_OnOff | undefined;
  let spacing: CT_Spacing | undefined;
  let ind: CT_Ind | undefined;
  let contextualSpacing: CT_OnOff | undefined;
  let mirrorIndents: CT_OnOff | undefined;
  let suppressOverlap: CT_OnOff | undefined;
  let jc: CT_Jc | undefined;
  let textDirection: CT_TextDirection | undefined;
  let textAlignment: CT_TextAlignment | undefined;
  let textboxTightWrap: CT_TextboxTightWrap | undefined;
  let outlineLvl: CT_DecimalNumber | undefined;
  let divId: CT_DecimalNumber | undefined;
  let cnfStyle: CT_Cnf | undefined;
  let pPrChange: CT_PPrChange | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PPrGeneral', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'pStyle': {
            pStyle = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'keepNext': {
            keepNext = readCT_OnOff(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'keepLines': {
            keepLines = readCT_OnOff(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'pageBreakBefore': {
            pageBreakBefore = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'framePr': {
            framePr = readCT_FramePr(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'widowControl': {
            widowControl = readCT_OnOff(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'numPr': {
            numPr = readCT_NumPr(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'suppressLineNumbers': {
            suppressLineNumbers = readCT_OnOff(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'pBdr': {
            pBdr = readCT_PBdr(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'shd': {
            shd = readCT_Shd(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tabs': {
            tabs = readCT_Tabs(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'suppressAutoHyphens': {
            suppressAutoHyphens = readCT_OnOff(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'kinsoku': {
            kinsoku = readCT_OnOff(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'wordWrap': {
            wordWrap = readCT_OnOff(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'overflowPunct': {
            overflowPunct = readCT_OnOff(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'topLinePunct': {
            topLinePunct = readCT_OnOff(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'autoSpaceDE': {
            autoSpaceDE = readCT_OnOff(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'autoSpaceDN': {
            autoSpaceDN = readCT_OnOff(cur, ctx);
            $slot = 17;
            $index = undefined;
            continue;
          }
          case 'bidi': {
            bidi = readCT_OnOff(cur, ctx);
            $slot = 18;
            $index = undefined;
            continue;
          }
          case 'adjustRightInd': {
            adjustRightInd = readCT_OnOff(cur, ctx);
            $slot = 19;
            $index = undefined;
            continue;
          }
          case 'snapToGrid': {
            snapToGrid = readCT_OnOff(cur, ctx);
            $slot = 20;
            $index = undefined;
            continue;
          }
          case 'spacing': {
            spacing = readCT_Spacing(cur, ctx);
            $slot = 21;
            $index = undefined;
            continue;
          }
          case 'ind': {
            ind = readCT_Ind(cur, ctx);
            $slot = 22;
            $index = undefined;
            continue;
          }
          case 'contextualSpacing': {
            contextualSpacing = readCT_OnOff(cur, ctx);
            $slot = 23;
            $index = undefined;
            continue;
          }
          case 'mirrorIndents': {
            mirrorIndents = readCT_OnOff(cur, ctx);
            $slot = 24;
            $index = undefined;
            continue;
          }
          case 'suppressOverlap': {
            suppressOverlap = readCT_OnOff(cur, ctx);
            $slot = 25;
            $index = undefined;
            continue;
          }
          case 'jc': {
            jc = readCT_Jc(cur, ctx);
            $slot = 26;
            $index = undefined;
            continue;
          }
          case 'textDirection': {
            textDirection = readCT_TextDirection(cur, ctx);
            $slot = 27;
            $index = undefined;
            continue;
          }
          case 'textAlignment': {
            textAlignment = readCT_TextAlignment(cur, ctx);
            $slot = 28;
            $index = undefined;
            continue;
          }
          case 'textboxTightWrap': {
            textboxTightWrap = readCT_TextboxTightWrap(cur, ctx);
            $slot = 29;
            $index = undefined;
            continue;
          }
          case 'outlineLvl': {
            outlineLvl = readCT_DecimalNumber(cur, ctx);
            $slot = 30;
            $index = undefined;
            continue;
          }
          case 'divId': {
            divId = readCT_DecimalNumber(cur, ctx);
            $slot = 31;
            $index = undefined;
            continue;
          }
          case 'cnfStyle': {
            cnfStyle = readCT_Cnf(cur, ctx);
            $slot = 32;
            $index = undefined;
            continue;
          }
          case 'pPrChange': {
            pPrChange = readCT_PPrChange(cur, ctx);
            $slot = 33;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PPrGeneral', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PPrGeneral', cur);
    cur.next();
  }

  return {
    pStyle,
    keepNext,
    keepLines,
    pageBreakBefore,
    framePr,
    widowControl,
    numPr,
    suppressLineNumbers,
    pBdr,
    shd,
    tabs,
    suppressAutoHyphens,
    kinsoku,
    wordWrap,
    overflowPunct,
    topLinePunct,
    autoSpaceDE,
    autoSpaceDN,
    bidi,
    adjustRightInd,
    snapToGrid,
    spacing,
    ind,
    contextualSpacing,
    mirrorIndents,
    suppressOverlap,
    jc,
    textDirection,
    textAlignment,
    textboxTightWrap,
    outlineLvl,
    divId,
    cnfStyle,
    pPrChange,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PTab`. See `transitional/wml.xsd:1691`. */
export function readCT_PTab(cur: XmlCursor, ctx: ReadContext): CT_PTab {
  const start = requireStart(cur, 'CT_PTab');
  let alignment: ST_PTabAlignment | undefined;
  let relativeTo: ST_PTabRelativeTo | undefined;
  let leader: ST_PTabLeader | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'alignment': {
          const v = parseST_PTabAlignment(a.value);
          if (v !== undefined) {
            alignment = v;
            continue;
          }
          ctx.invalidValue('CT_PTab', a, 'ST_PTabAlignment', cur);
          break;
        }
        case 'relativeTo': {
          const v = parseST_PTabRelativeTo(a.value);
          if (v !== undefined) {
            relativeTo = v;
            continue;
          }
          ctx.invalidValue('CT_PTab', a, 'ST_PTabRelativeTo', cur);
          break;
        }
        case 'leader': {
          const v = parseST_PTabLeader(a.value);
          if (v !== undefined) {
            leader = v;
            continue;
          }
          ctx.invalidValue('CT_PTab', a, 'ST_PTabLeader', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PTab', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (alignment === undefined) ctx.missingRequired('CT_PTab', 'alignment', cur);
  if (relativeTo === undefined) ctx.missingRequired('CT_PTab', 'relativeTo', cur);
  if (leader === undefined) ctx.missingRequired('CT_PTab', 'leader', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PTab', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PTab', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PTab', cur);
    cur.next();
  }
  return {
    alignment,
    relativeTo,
    leader,
    $unknownAttrs,
  };
}

/** Read a `CT_PageBorder`. See `transitional/wml.xsd:1488`. */
export function readCT_PageBorder(cur: XmlCursor, ctx: ReadContext): CT_PageBorder {
  const start = requireStart(cur, 'CT_PageBorder');
  let val: ST_Border | undefined;
  let color: ST_HexColor | undefined;
  let themeColor: ST_ThemeColor | undefined;
  let themeTint: ST_UcharHexNumber | undefined;
  let themeShade: ST_UcharHexNumber | undefined;
  let sz: ST_EighthPointMeasure | undefined;
  let space: ST_PointMeasure | undefined;
  let shadow: ST_OnOff | undefined;
  let frame: ST_OnOff | undefined;
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorder', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Border(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorder', a, 'ST_Border', cur);
          break;
        }
        case 'color': {
          const v = parseST_HexColor(a.value);
          if (v !== undefined) {
            color = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorder', a, 'ST_HexColor', cur);
          break;
        }
        case 'themeColor': {
          const v = parseST_ThemeColor(a.value);
          if (v !== undefined) {
            themeColor = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorder', a, 'ST_ThemeColor', cur);
          break;
        }
        case 'themeTint': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeTint = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorder', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'themeShade': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeShade = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorder', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'sz': {
          const v = parseST_EighthPointMeasure(a.value);
          if (v !== undefined) {
            sz = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorder', a, 'ST_EighthPointMeasure', cur);
          break;
        }
        case 'space': {
          const v = parseST_PointMeasure(a.value);
          if (v !== undefined) {
            space = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorder', a, 'ST_PointMeasure', cur);
          break;
        }
        case 'shadow': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            shadow = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorder', a, 'ST_OnOff', cur);
          break;
        }
        case 'frame': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            frame = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorder', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PageBorder', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_PageBorder', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PageBorder', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PageBorder', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PageBorder', cur);
    cur.next();
  }
  return {
    val,
    color,
    themeColor,
    themeTint,
    themeShade,
    sz,
    space,
    shadow,
    frame,
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_PageBorders`. See `transitional/wml.xsd:1477`. */
export function readCT_PageBorders(cur: XmlCursor, ctx: ReadContext): CT_PageBorders {
  const start = requireStart(cur, 'CT_PageBorders');
  let zOrder: ST_PageBorderZOrder | undefined;
  let display: ST_PageBorderDisplay | undefined;
  let offsetFrom: ST_PageBorderOffset | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'zOrder': {
          const v = parseST_PageBorderZOrder(a.value);
          if (v !== undefined) {
            zOrder = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorders', a, 'ST_PageBorderZOrder', cur);
          break;
        }
        case 'display': {
          const v = parseST_PageBorderDisplay(a.value);
          if (v !== undefined) {
            display = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorders', a, 'ST_PageBorderDisplay', cur);
          break;
        }
        case 'offsetFrom': {
          const v = parseST_PageBorderOffset(a.value);
          if (v !== undefined) {
            offsetFrom = v;
            continue;
          }
          ctx.invalidValue('CT_PageBorders', a, 'ST_PageBorderOffset', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PageBorders', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let top: CT_TopPageBorder | undefined;
  let left: CT_PageBorder | undefined;
  let bottom: CT_BottomPageBorder | undefined;
  let right: CT_PageBorder | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PageBorders', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'top': {
            top = readCT_TopPageBorder(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'left': {
            left = readCT_PageBorder(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'bottom': {
            bottom = readCT_BottomPageBorder(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'right': {
            right = readCT_PageBorder(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PageBorders', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PageBorders', cur);
    cur.next();
  }

  return {
    top,
    left,
    bottom,
    right,
    zOrder,
    display,
    offsetFrom,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PageMar`. See `transitional/wml.xsd:1449`. */
export function readCT_PageMar(cur: XmlCursor, ctx: ReadContext): CT_PageMar {
  const start = requireStart(cur, 'CT_PageMar');
  let top: ST_SignedTwipsMeasure | undefined;
  let right: ST_TwipsMeasure | undefined;
  let bottom: ST_SignedTwipsMeasure | undefined;
  let left: ST_TwipsMeasure | undefined;
  let header: ST_TwipsMeasure | undefined;
  let footer: ST_TwipsMeasure | undefined;
  let gutter: ST_TwipsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'top': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            top = v;
            continue;
          }
          ctx.invalidValue('CT_PageMar', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
        case 'right': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            right = v;
            continue;
          }
          ctx.invalidValue('CT_PageMar', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'bottom': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            bottom = v;
            continue;
          }
          ctx.invalidValue('CT_PageMar', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
        case 'left': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            left = v;
            continue;
          }
          ctx.invalidValue('CT_PageMar', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'header': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            header = v;
            continue;
          }
          ctx.invalidValue('CT_PageMar', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'footer': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            footer = v;
            continue;
          }
          ctx.invalidValue('CT_PageMar', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'gutter': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            gutter = v;
            continue;
          }
          ctx.invalidValue('CT_PageMar', a, 'ST_TwipsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PageMar', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (top === undefined) ctx.missingRequired('CT_PageMar', 'top', cur);
  if (right === undefined) ctx.missingRequired('CT_PageMar', 'right', cur);
  if (bottom === undefined) ctx.missingRequired('CT_PageMar', 'bottom', cur);
  if (left === undefined) ctx.missingRequired('CT_PageMar', 'left', cur);
  if (header === undefined) ctx.missingRequired('CT_PageMar', 'header', cur);
  if (footer === undefined) ctx.missingRequired('CT_PageMar', 'footer', cur);
  if (gutter === undefined) ctx.missingRequired('CT_PageMar', 'gutter', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PageMar', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PageMar', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PageMar', cur);
    cur.next();
  }
  return {
    top,
    right,
    bottom,
    left,
    header,
    footer,
    gutter,
    $unknownAttrs,
  };
}

/** Read a `CT_PageNumber`. See `transitional/wml.xsd:1533`. */
export function readCT_PageNumber(cur: XmlCursor, ctx: ReadContext): CT_PageNumber {
  const start = requireStart(cur, 'CT_PageNumber');
  let fmt: ST_NumberFormat | undefined;
  let start_: ST_DecimalNumber | undefined;
  let chapStyle: ST_DecimalNumber | undefined;
  let chapSep: ST_ChapterSep | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'fmt': {
          const v = parseST_NumberFormat(a.value);
          if (v !== undefined) {
            fmt = v;
            continue;
          }
          ctx.invalidValue('CT_PageNumber', a, 'ST_NumberFormat', cur);
          break;
        }
        case 'start': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            start_ = v;
            continue;
          }
          ctx.invalidValue('CT_PageNumber', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'chapStyle': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            chapStyle = v;
            continue;
          }
          ctx.invalidValue('CT_PageNumber', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'chapSep': {
          const v = parseST_ChapterSep(a.value);
          if (v !== undefined) {
            chapSep = v;
            continue;
          }
          ctx.invalidValue('CT_PageNumber', a, 'ST_ChapterSep', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PageNumber', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PageNumber', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PageNumber', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PageNumber', cur);
    cur.next();
  }
  return {
    fmt,
    start: start_,
    chapStyle,
    chapSep,
    $unknownAttrs,
  };
}

/** Read a `CT_PageSz`. See `transitional/wml.xsd:1443`. */
export function readCT_PageSz(cur: XmlCursor, ctx: ReadContext): CT_PageSz {
  const start = requireStart(cur, 'CT_PageSz');
  let w: ST_TwipsMeasure | undefined;
  let h: ST_TwipsMeasure | undefined;
  let orient: ST_PageOrientation | undefined;
  let code: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'w': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            w = v;
            continue;
          }
          ctx.invalidValue('CT_PageSz', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'h': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            h = v;
            continue;
          }
          ctx.invalidValue('CT_PageSz', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'orient': {
          const v = parseST_PageOrientation(a.value);
          if (v !== undefined) {
            orient = v;
            continue;
          }
          ctx.invalidValue('CT_PageSz', a, 'ST_PageOrientation', cur);
          break;
        }
        case 'code': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            code = v;
            continue;
          }
          ctx.invalidValue('CT_PageSz', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PageSz', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PageSz', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PageSz', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PageSz', cur);
    cur.next();
  }
  return {
    w,
    h,
    orient,
    code,
    $unknownAttrs,
  };
}

/** Read a `CT_Panose`. See `transitional/wml.xsd:3287`. */
export function readCT_Panose(cur: XmlCursor, ctx: ReadContext): CT_Panose {
  const start = requireStart(cur, 'CT_Panose');
  let val: ST_Panose | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Panose(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Panose', a, 'ST_Panose', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Panose', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Panose', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Panose', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Panose', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Panose', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_PaperSource`. See `transitional/wml.xsd:1366`. */
export function readCT_PaperSource(cur: XmlCursor, ctx: ReadContext): CT_PaperSource {
  const start = requireStart(cur, 'CT_PaperSource');
  let first: ST_DecimalNumber | undefined;
  let other: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'first': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            first = v;
            continue;
          }
          ctx.invalidValue('CT_PaperSource', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'other': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            other = v;
            continue;
          }
          ctx.invalidValue('CT_PaperSource', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PaperSource', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PaperSource', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PaperSource', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PaperSource', cur);
    cur.next();
  }
  return {
    first,
    other,
    $unknownAttrs,
  };
}

/** Read a `CT_ParaRPr`. See `transitional/wml.xsd:1914`. */
export function readCT_ParaRPr(cur: XmlCursor, ctx: ReadContext): CT_ParaRPr {
  const start = requireStart(cur, 'CT_ParaRPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ParaRPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ins: CT_TrackChange | undefined;
  let del: CT_TrackChange | undefined;
  let moveFrom: CT_TrackChange | undefined;
  let moveTo: CT_TrackChange | undefined;
  const rPrBase: CT_ParaRPr_RPrBase[] = [];
  let rPrChange: CT_ParaRPrChange | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ParaRPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'ins': {
            ins = readCT_TrackChange(cur, ctx);
            continue;
          }
          case 'del': {
            del = readCT_TrackChange(cur, ctx);
            continue;
          }
          case 'moveFrom': {
            moveFrom = readCT_TrackChange(cur, ctx);
            continue;
          }
          case 'moveTo': {
            moveTo = readCT_TrackChange(cur, ctx);
            continue;
          }
          case 'rStyle': {
            rPrBase.push({ kind: 'rStyle', value: readCT_String(cur, ctx) });
            continue;
          }
          case 'rFonts': {
            rPrBase.push({ kind: 'rFonts', value: readCT_Fonts(cur, ctx) });
            continue;
          }
          case 'b': {
            rPrBase.push({ kind: 'b', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'bCs': {
            rPrBase.push({ kind: 'bCs', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'i': {
            rPrBase.push({ kind: 'i', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'iCs': {
            rPrBase.push({ kind: 'iCs', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'caps': {
            rPrBase.push({ kind: 'caps', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'smallCaps': {
            rPrBase.push({ kind: 'smallCaps', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'strike': {
            rPrBase.push({ kind: 'strike', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'dstrike': {
            rPrBase.push({ kind: 'dstrike', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'outline': {
            rPrBase.push({ kind: 'outline', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'shadow': {
            rPrBase.push({ kind: 'shadow', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'emboss': {
            rPrBase.push({ kind: 'emboss', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'imprint': {
            rPrBase.push({ kind: 'imprint', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'noProof': {
            rPrBase.push({ kind: 'noProof', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'snapToGrid': {
            rPrBase.push({ kind: 'snapToGrid', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'vanish': {
            rPrBase.push({ kind: 'vanish', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'webHidden': {
            rPrBase.push({ kind: 'webHidden', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'color': {
            rPrBase.push({ kind: 'color', value: readCT_Color(cur, ctx) });
            continue;
          }
          case 'spacing': {
            rPrBase.push({ kind: 'spacing', value: readCT_SignedTwipsMeasure(cur, ctx) });
            continue;
          }
          case 'w': {
            rPrBase.push({ kind: 'w', value: readCT_TextScale(cur, ctx) });
            continue;
          }
          case 'kern': {
            rPrBase.push({ kind: 'kern', value: readCT_HpsMeasure(cur, ctx) });
            continue;
          }
          case 'position': {
            rPrBase.push({ kind: 'position', value: readCT_SignedHpsMeasure(cur, ctx) });
            continue;
          }
          case 'sz': {
            rPrBase.push({ kind: 'sz', value: readCT_HpsMeasure(cur, ctx) });
            continue;
          }
          case 'szCs': {
            rPrBase.push({ kind: 'szCs', value: readCT_HpsMeasure(cur, ctx) });
            continue;
          }
          case 'highlight': {
            rPrBase.push({ kind: 'highlight', value: readCT_Highlight(cur, ctx) });
            continue;
          }
          case 'u': {
            rPrBase.push({ kind: 'u', value: readCT_Underline(cur, ctx) });
            continue;
          }
          case 'effect': {
            rPrBase.push({ kind: 'effect', value: readCT_TextEffect(cur, ctx) });
            continue;
          }
          case 'bdr': {
            rPrBase.push({ kind: 'bdr', value: readCT_Border(cur, ctx) });
            continue;
          }
          case 'shd': {
            rPrBase.push({ kind: 'shd', value: readCT_Shd(cur, ctx) });
            continue;
          }
          case 'fitText': {
            rPrBase.push({ kind: 'fitText', value: readCT_FitText(cur, ctx) });
            continue;
          }
          case 'vertAlign': {
            rPrBase.push({ kind: 'vertAlign', value: readCT_VerticalAlignRun(cur, ctx) });
            continue;
          }
          case 'rtl': {
            rPrBase.push({ kind: 'rtl', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'cs': {
            rPrBase.push({ kind: 'cs', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'em': {
            rPrBase.push({ kind: 'em', value: readCT_Em(cur, ctx) });
            continue;
          }
          case 'lang': {
            rPrBase.push({ kind: 'lang', value: readCT_Language(cur, ctx) });
            continue;
          }
          case 'eastAsianLayout': {
            rPrBase.push({ kind: 'eastAsianLayout', value: readCT_EastAsianLayout(cur, ctx) });
            continue;
          }
          case 'specVanish': {
            rPrBase.push({ kind: 'specVanish', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'oMath': {
            rPrBase.push({ kind: 'oMath', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'rPrChange': {
            rPrChange = readCT_ParaRPrChange(cur, ctx);
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ParaRPr', ev.localName, ev.uri, cur);
      rPrBase.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ParaRPr', cur);
    cur.next();
  }

  return {
    ins,
    del,
    moveFrom,
    moveTo,
    rPrBase,
    rPrChange,
    $unknownAttrs,
  };
}

/** Read a `CT_ParaRPrChange`. See `transitional/wml.xsd:986`. */
export function readCT_ParaRPrChange(cur: XmlCursor, ctx: ReadContext): CT_ParaRPrChange {
  const start = requireStart(cur, 'CT_ParaRPrChange');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_ParaRPrChange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_ParaRPrChange', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_ParaRPrChange', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ParaRPrChange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_ParaRPrChange', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_ParaRPrChange', 'author', cur);

  let rPr: CT_ParaRPrOriginal | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ParaRPrChange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rPr': {
            rPr = readCT_ParaRPrOriginal(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ParaRPrChange', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ParaRPrChange', cur);
    cur.next();
  }

  return {
    rPr,
    id,
    author,
    date,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ParaRPrOriginal`. See `transitional/wml.xsd:1908`. */
export function readCT_ParaRPrOriginal(cur: XmlCursor, ctx: ReadContext): CT_ParaRPrOriginal {
  const start = requireStart(cur, 'CT_ParaRPrOriginal');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ParaRPrOriginal', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ins: CT_TrackChange | undefined;
  let del: CT_TrackChange | undefined;
  let moveFrom: CT_TrackChange | undefined;
  let moveTo: CT_TrackChange | undefined;
  const rPrBase: CT_ParaRPrOriginal_RPrBase[] = [];

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ParaRPrOriginal', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'ins': {
            ins = readCT_TrackChange(cur, ctx);
            continue;
          }
          case 'del': {
            del = readCT_TrackChange(cur, ctx);
            continue;
          }
          case 'moveFrom': {
            moveFrom = readCT_TrackChange(cur, ctx);
            continue;
          }
          case 'moveTo': {
            moveTo = readCT_TrackChange(cur, ctx);
            continue;
          }
          case 'rStyle': {
            rPrBase.push({ kind: 'rStyle', value: readCT_String(cur, ctx) });
            continue;
          }
          case 'rFonts': {
            rPrBase.push({ kind: 'rFonts', value: readCT_Fonts(cur, ctx) });
            continue;
          }
          case 'b': {
            rPrBase.push({ kind: 'b', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'bCs': {
            rPrBase.push({ kind: 'bCs', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'i': {
            rPrBase.push({ kind: 'i', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'iCs': {
            rPrBase.push({ kind: 'iCs', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'caps': {
            rPrBase.push({ kind: 'caps', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'smallCaps': {
            rPrBase.push({ kind: 'smallCaps', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'strike': {
            rPrBase.push({ kind: 'strike', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'dstrike': {
            rPrBase.push({ kind: 'dstrike', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'outline': {
            rPrBase.push({ kind: 'outline', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'shadow': {
            rPrBase.push({ kind: 'shadow', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'emboss': {
            rPrBase.push({ kind: 'emboss', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'imprint': {
            rPrBase.push({ kind: 'imprint', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'noProof': {
            rPrBase.push({ kind: 'noProof', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'snapToGrid': {
            rPrBase.push({ kind: 'snapToGrid', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'vanish': {
            rPrBase.push({ kind: 'vanish', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'webHidden': {
            rPrBase.push({ kind: 'webHidden', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'color': {
            rPrBase.push({ kind: 'color', value: readCT_Color(cur, ctx) });
            continue;
          }
          case 'spacing': {
            rPrBase.push({ kind: 'spacing', value: readCT_SignedTwipsMeasure(cur, ctx) });
            continue;
          }
          case 'w': {
            rPrBase.push({ kind: 'w', value: readCT_TextScale(cur, ctx) });
            continue;
          }
          case 'kern': {
            rPrBase.push({ kind: 'kern', value: readCT_HpsMeasure(cur, ctx) });
            continue;
          }
          case 'position': {
            rPrBase.push({ kind: 'position', value: readCT_SignedHpsMeasure(cur, ctx) });
            continue;
          }
          case 'sz': {
            rPrBase.push({ kind: 'sz', value: readCT_HpsMeasure(cur, ctx) });
            continue;
          }
          case 'szCs': {
            rPrBase.push({ kind: 'szCs', value: readCT_HpsMeasure(cur, ctx) });
            continue;
          }
          case 'highlight': {
            rPrBase.push({ kind: 'highlight', value: readCT_Highlight(cur, ctx) });
            continue;
          }
          case 'u': {
            rPrBase.push({ kind: 'u', value: readCT_Underline(cur, ctx) });
            continue;
          }
          case 'effect': {
            rPrBase.push({ kind: 'effect', value: readCT_TextEffect(cur, ctx) });
            continue;
          }
          case 'bdr': {
            rPrBase.push({ kind: 'bdr', value: readCT_Border(cur, ctx) });
            continue;
          }
          case 'shd': {
            rPrBase.push({ kind: 'shd', value: readCT_Shd(cur, ctx) });
            continue;
          }
          case 'fitText': {
            rPrBase.push({ kind: 'fitText', value: readCT_FitText(cur, ctx) });
            continue;
          }
          case 'vertAlign': {
            rPrBase.push({ kind: 'vertAlign', value: readCT_VerticalAlignRun(cur, ctx) });
            continue;
          }
          case 'rtl': {
            rPrBase.push({ kind: 'rtl', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'cs': {
            rPrBase.push({ kind: 'cs', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'em': {
            rPrBase.push({ kind: 'em', value: readCT_Em(cur, ctx) });
            continue;
          }
          case 'lang': {
            rPrBase.push({ kind: 'lang', value: readCT_Language(cur, ctx) });
            continue;
          }
          case 'eastAsianLayout': {
            rPrBase.push({ kind: 'eastAsianLayout', value: readCT_EastAsianLayout(cur, ctx) });
            continue;
          }
          case 'specVanish': {
            rPrBase.push({ kind: 'specVanish', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'oMath': {
            rPrBase.push({ kind: 'oMath', value: readCT_OnOff(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ParaRPrOriginal', ev.localName, ev.uri, cur);
      rPrBase.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ParaRPrOriginal', cur);
    cur.next();
  }

  return {
    ins,
    del,
    moveFrom,
    moveTo,
    rPrBase,
    $unknownAttrs,
  };
}

/** Read a `CT_Perm`. See `transitional/wml.xsd:1722`. */
export function readCT_Perm(cur: XmlCursor, ctx: ReadContext): CT_Perm {
  const start = requireStart(cur, 'CT_Perm');
  let id: ST_String | undefined;
  let displacedByCustomXml: ST_DisplacedByCustomXml | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Perm', a, 'ST_String', cur);
          break;
        }
        case 'displacedByCustomXml': {
          const v = parseST_DisplacedByCustomXml(a.value);
          if (v !== undefined) {
            displacedByCustomXml = v;
            continue;
          }
          ctx.invalidValue('CT_Perm', a, 'ST_DisplacedByCustomXml', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Perm', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_Perm', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Perm', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Perm', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Perm', cur);
    cur.next();
  }
  return {
    id,
    displacedByCustomXml,
    $unknownAttrs,
  };
}

/** Read a `CT_PermStart`. See `transitional/wml.xsd:1726`. */
export function readCT_PermStart(cur: XmlCursor, ctx: ReadContext): CT_PermStart {
  const start = requireStart(cur, 'CT_PermStart');
  let id: ST_String | undefined;
  let displacedByCustomXml: ST_DisplacedByCustomXml | undefined;
  let edGrp: ST_EdGrp | undefined;
  let ed: ST_String | undefined;
  let colFirst: ST_DecimalNumber | undefined;
  let colLast: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_PermStart', a, 'ST_String', cur);
          break;
        }
        case 'displacedByCustomXml': {
          const v = parseST_DisplacedByCustomXml(a.value);
          if (v !== undefined) {
            displacedByCustomXml = v;
            continue;
          }
          ctx.invalidValue('CT_PermStart', a, 'ST_DisplacedByCustomXml', cur);
          break;
        }
        case 'edGrp': {
          const v = parseST_EdGrp(a.value);
          if (v !== undefined) {
            edGrp = v;
            continue;
          }
          ctx.invalidValue('CT_PermStart', a, 'ST_EdGrp', cur);
          break;
        }
        case 'ed': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            ed = v;
            continue;
          }
          ctx.invalidValue('CT_PermStart', a, 'ST_String', cur);
          break;
        }
        case 'colFirst': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            colFirst = v;
            continue;
          }
          ctx.invalidValue('CT_PermStart', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'colLast': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            colLast = v;
            continue;
          }
          ctx.invalidValue('CT_PermStart', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PermStart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_PermStart', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PermStart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PermStart', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PermStart', cur);
    cur.next();
  }
  return {
    id,
    displacedByCustomXml,
    edGrp,
    ed,
    colFirst,
    colLast,
    $unknownAttrs,
  };
}

/** Read a `CT_Picture`. See `transitional/wml.xsd:1186`. */
export function readCT_Picture(cur: XmlCursor, ctx: ReadContext): CT_Picture {
  const start = requireStart(cur, 'CT_Picture');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Picture', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const any: RawNode[] = [];
  const any2: RawNode[] = [];
  let movie: CT_Rel | undefined;
  let control: CT_Control | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Picture', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'movie': {
            movie = readCT_Rel(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'control': {
            control = readCT_Control(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === ctx.uris['vml']) {
        any.push(cur.skipToRaw());
        $slot = 0;
        $index = any.length - 1;
        continue;
      }
      if (ev.uri === ctx.uris['vml-office']) {
        any2.push(cur.skipToRaw());
        $slot = 1;
        $index = any2.length - 1;
        continue;
      }
      ctx.unexpectedElement('CT_Picture', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Picture', cur);
    cur.next();
  }

  return {
    any,
    any2,
    movie,
    control,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Pitch`. See `transitional/wml.xsd:3310`. */
export function readCT_Pitch(cur: XmlCursor, ctx: ReadContext): CT_Pitch {
  const start = requireStart(cur, 'CT_Pitch');
  let val: ST_Pitch | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Pitch(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Pitch', a, 'ST_Pitch', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Pitch', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Pitch', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Pitch', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Pitch', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Pitch', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_PixelsMeasure`. See `transitional/wml.xsd:78`. */
export function readCT_PixelsMeasure(cur: XmlCursor, ctx: ReadContext): CT_PixelsMeasure {
  const start = requireStart(cur, 'CT_PixelsMeasure');
  let val: ST_PixelsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_PixelsMeasure(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_PixelsMeasure', a, 'ST_PixelsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PixelsMeasure', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_PixelsMeasure', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PixelsMeasure', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PixelsMeasure', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PixelsMeasure', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Placeholder`. See `transitional/wml.xsd:2035`. */
export function readCT_Placeholder(cur: XmlCursor, ctx: ReadContext): CT_Placeholder {
  const start = requireStart(cur, 'CT_Placeholder');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Placeholder', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let docPart: CT_String | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Placeholder', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'docPart': {
            docPart = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Placeholder', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Placeholder', cur);
    cur.next();
  }

  return {
    docPart,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Proof`. See `transitional/wml.xsd:702`. */
export function readCT_Proof(cur: XmlCursor, ctx: ReadContext): CT_Proof {
  const start = requireStart(cur, 'CT_Proof');
  let spelling: ST_Proof | undefined;
  let grammar: ST_Proof | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'spelling': {
          const v = parseST_Proof(a.value);
          if (v !== undefined) {
            spelling = v;
            continue;
          }
          ctx.invalidValue('CT_Proof', a, 'ST_Proof', cur);
          break;
        }
        case 'grammar': {
          const v = parseST_Proof(a.value);
          if (v !== undefined) {
            grammar = v;
            continue;
          }
          ctx.invalidValue('CT_Proof', a, 'ST_Proof', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Proof', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Proof', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Proof', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Proof', cur);
    cur.next();
  }
  return {
    spelling,
    grammar,
    $unknownAttrs,
  };
}

/** Read a `CT_ProofErr`. See `transitional/wml.xsd:1708`. */
export function readCT_ProofErr(cur: XmlCursor, ctx: ReadContext): CT_ProofErr {
  const start = requireStart(cur, 'CT_ProofErr');
  let type: ST_ProofErr | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'type': {
          const v = parseST_ProofErr(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_ProofErr', a, 'ST_ProofErr', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ProofErr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (type === undefined) ctx.missingRequired('CT_ProofErr', 'type', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ProofErr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ProofErr', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ProofErr', cur);
    cur.next();
  }
  return {
    type,
    $unknownAttrs,
  };
}

/** Read a `CT_R`. See `transitional/wml.xsd:1780`. */
export function readCT_R(cur: XmlCursor, ctx: ReadContext): CT_R {
  const start = requireStart(cur, 'CT_R');
  let rsidRPr: ST_LongHexNumber | undefined;
  let rsidDel: ST_LongHexNumber | undefined;
  let rsidR: ST_LongHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'rsidRPr': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidRPr = v;
            continue;
          }
          ctx.invalidValue('CT_R', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidDel': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidDel = v;
            continue;
          }
          ctx.invalidValue('CT_R', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidR': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidR = v;
            continue;
          }
          ctx.invalidValue('CT_R', a, 'ST_LongHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_R', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let rPr: CT_RPr | undefined;
  const runInnerContent: CT_R_RunInnerContent[] = [];

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_R', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rPr': {
            rPr = readCT_RPr(cur, ctx);
            continue;
          }
          case 'br': {
            runInnerContent.push({ kind: 'br', value: readCT_Br(cur, ctx) });
            continue;
          }
          case 't': {
            runInnerContent.push({ kind: 't', value: readCT_Text(cur, ctx) });
            continue;
          }
          case 'contentPart': {
            runInnerContent.push({ kind: 'contentPart', value: readCT_Rel(cur, ctx) });
            continue;
          }
          case 'delText': {
            runInnerContent.push({ kind: 'delText', value: readCT_Text(cur, ctx) });
            continue;
          }
          case 'instrText': {
            runInnerContent.push({ kind: 'instrText', value: readCT_Text(cur, ctx) });
            continue;
          }
          case 'delInstrText': {
            runInnerContent.push({ kind: 'delInstrText', value: readCT_Text(cur, ctx) });
            continue;
          }
          case 'noBreakHyphen': {
            runInnerContent.push({ kind: 'noBreakHyphen', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'softHyphen': {
            runInnerContent.push({ kind: 'softHyphen', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'dayShort': {
            runInnerContent.push({ kind: 'dayShort', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'monthShort': {
            runInnerContent.push({ kind: 'monthShort', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'yearShort': {
            runInnerContent.push({ kind: 'yearShort', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'dayLong': {
            runInnerContent.push({ kind: 'dayLong', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'monthLong': {
            runInnerContent.push({ kind: 'monthLong', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'yearLong': {
            runInnerContent.push({ kind: 'yearLong', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'annotationRef': {
            runInnerContent.push({ kind: 'annotationRef', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'footnoteRef': {
            runInnerContent.push({ kind: 'footnoteRef', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'endnoteRef': {
            runInnerContent.push({ kind: 'endnoteRef', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'separator': {
            runInnerContent.push({ kind: 'separator', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'continuationSeparator': {
            runInnerContent.push({ kind: 'continuationSeparator', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'sym': {
            runInnerContent.push({ kind: 'sym', value: readCT_Sym(cur, ctx) });
            continue;
          }
          case 'pgNum': {
            runInnerContent.push({ kind: 'pgNum', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'cr': {
            runInnerContent.push({ kind: 'cr', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'tab': {
            runInnerContent.push({ kind: 'tab', value: readCT_Empty(cur, ctx) });
            continue;
          }
          case 'object': {
            runInnerContent.push({ kind: 'object', value: readCT_Object(cur, ctx) });
            continue;
          }
          case 'pict': {
            runInnerContent.push({ kind: 'pict', value: readCT_Picture(cur, ctx) });
            continue;
          }
          case 'fldChar': {
            runInnerContent.push({ kind: 'fldChar', value: readCT_FldChar(cur, ctx) });
            continue;
          }
          case 'ruby': {
            runInnerContent.push({ kind: 'ruby', value: readCT_Ruby(cur, ctx) });
            continue;
          }
          case 'footnoteReference': {
            runInnerContent.push({ kind: 'footnoteReference', value: readCT_FtnEdnRef(cur, ctx) });
            continue;
          }
          case 'endnoteReference': {
            runInnerContent.push({ kind: 'endnoteReference', value: readCT_FtnEdnRef(cur, ctx) });
            continue;
          }
          case 'commentReference': {
            runInnerContent.push({ kind: 'commentReference', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'drawing': {
            runInnerContent.push({ kind: 'drawing', value: readCT_Drawing(cur, ctx) });
            continue;
          }
          case 'ptab': {
            runInnerContent.push({ kind: 'ptab', value: readCT_PTab(cur, ctx) });
            continue;
          }
          case 'lastRenderedPageBreak': {
            runInnerContent.push({ kind: 'lastRenderedPageBreak', value: readCT_Empty(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_R', ev.localName, ev.uri, cur);
      runInnerContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_R', cur);
    cur.next();
  }

  return {
    rPr,
    runInnerContent,
    rsidRPr,
    rsidDel,
    rsidR,
    $unknownAttrs,
  };
}

/** Read a `CT_RPr`. See `transitional/wml.xsd:1867`. */
export function readCT_RPr(cur: XmlCursor, ctx: ReadContext): CT_RPr {
  const start = requireStart(cur, 'CT_RPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_RPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const rPrBase: CT_RPr_RPrBase[] = [];
  let rPrChange: CT_RPrChange | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rStyle': {
            rPrBase.push({ kind: 'rStyle', value: readCT_String(cur, ctx) });
            continue;
          }
          case 'rFonts': {
            rPrBase.push({ kind: 'rFonts', value: readCT_Fonts(cur, ctx) });
            continue;
          }
          case 'b': {
            rPrBase.push({ kind: 'b', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'bCs': {
            rPrBase.push({ kind: 'bCs', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'i': {
            rPrBase.push({ kind: 'i', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'iCs': {
            rPrBase.push({ kind: 'iCs', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'caps': {
            rPrBase.push({ kind: 'caps', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'smallCaps': {
            rPrBase.push({ kind: 'smallCaps', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'strike': {
            rPrBase.push({ kind: 'strike', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'dstrike': {
            rPrBase.push({ kind: 'dstrike', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'outline': {
            rPrBase.push({ kind: 'outline', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'shadow': {
            rPrBase.push({ kind: 'shadow', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'emboss': {
            rPrBase.push({ kind: 'emboss', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'imprint': {
            rPrBase.push({ kind: 'imprint', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'noProof': {
            rPrBase.push({ kind: 'noProof', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'snapToGrid': {
            rPrBase.push({ kind: 'snapToGrid', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'vanish': {
            rPrBase.push({ kind: 'vanish', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'webHidden': {
            rPrBase.push({ kind: 'webHidden', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'color': {
            rPrBase.push({ kind: 'color', value: readCT_Color(cur, ctx) });
            continue;
          }
          case 'spacing': {
            rPrBase.push({ kind: 'spacing', value: readCT_SignedTwipsMeasure(cur, ctx) });
            continue;
          }
          case 'w': {
            rPrBase.push({ kind: 'w', value: readCT_TextScale(cur, ctx) });
            continue;
          }
          case 'kern': {
            rPrBase.push({ kind: 'kern', value: readCT_HpsMeasure(cur, ctx) });
            continue;
          }
          case 'position': {
            rPrBase.push({ kind: 'position', value: readCT_SignedHpsMeasure(cur, ctx) });
            continue;
          }
          case 'sz': {
            rPrBase.push({ kind: 'sz', value: readCT_HpsMeasure(cur, ctx) });
            continue;
          }
          case 'szCs': {
            rPrBase.push({ kind: 'szCs', value: readCT_HpsMeasure(cur, ctx) });
            continue;
          }
          case 'highlight': {
            rPrBase.push({ kind: 'highlight', value: readCT_Highlight(cur, ctx) });
            continue;
          }
          case 'u': {
            rPrBase.push({ kind: 'u', value: readCT_Underline(cur, ctx) });
            continue;
          }
          case 'effect': {
            rPrBase.push({ kind: 'effect', value: readCT_TextEffect(cur, ctx) });
            continue;
          }
          case 'bdr': {
            rPrBase.push({ kind: 'bdr', value: readCT_Border(cur, ctx) });
            continue;
          }
          case 'shd': {
            rPrBase.push({ kind: 'shd', value: readCT_Shd(cur, ctx) });
            continue;
          }
          case 'fitText': {
            rPrBase.push({ kind: 'fitText', value: readCT_FitText(cur, ctx) });
            continue;
          }
          case 'vertAlign': {
            rPrBase.push({ kind: 'vertAlign', value: readCT_VerticalAlignRun(cur, ctx) });
            continue;
          }
          case 'rtl': {
            rPrBase.push({ kind: 'rtl', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'cs': {
            rPrBase.push({ kind: 'cs', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'em': {
            rPrBase.push({ kind: 'em', value: readCT_Em(cur, ctx) });
            continue;
          }
          case 'lang': {
            rPrBase.push({ kind: 'lang', value: readCT_Language(cur, ctx) });
            continue;
          }
          case 'eastAsianLayout': {
            rPrBase.push({ kind: 'eastAsianLayout', value: readCT_EastAsianLayout(cur, ctx) });
            continue;
          }
          case 'specVanish': {
            rPrBase.push({ kind: 'specVanish', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'oMath': {
            rPrBase.push({ kind: 'oMath', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'rPrChange': {
            rPrChange = readCT_RPrChange(cur, ctx);
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RPr', ev.localName, ev.uri, cur);
      rPrBase.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RPr', cur);
    cur.next();
  }

  return {
    rPrBase,
    rPrChange,
    $unknownAttrs,
  };
}

/** Read a `CT_RPrChange`. See `transitional/wml.xsd:977`. */
export function readCT_RPrChange(cur: XmlCursor, ctx: ReadContext): CT_RPrChange {
  const start = requireStart(cur, 'CT_RPrChange');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_RPrChange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_RPrChange', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_RPrChange', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_RPrChange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_RPrChange', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_RPrChange', 'author', cur);

  let rPr: CT_RPrOriginal | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RPrChange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rPr': {
            rPr = readCT_RPrOriginal(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RPrChange', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RPrChange', cur);
    cur.next();
  }

  return {
    rPr,
    id,
    author,
    date,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_RPrDefault`. See `transitional/wml.xsd:2832`. */
export function readCT_RPrDefault(cur: XmlCursor, ctx: ReadContext): CT_RPrDefault {
  const start = requireStart(cur, 'CT_RPrDefault');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_RPrDefault', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let rPr: CT_RPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RPrDefault', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rPr': {
            rPr = readCT_RPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RPrDefault', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RPrDefault', cur);
    cur.next();
  }

  return {
    rPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_RPrOriginal`. See `transitional/wml.xsd:1903`. */
export function readCT_RPrOriginal(cur: XmlCursor, ctx: ReadContext): CT_RPrOriginal {
  const start = requireStart(cur, 'CT_RPrOriginal');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_RPrOriginal', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const rPrBase: CT_RPrOriginal_RPrBase[] = [];

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RPrOriginal', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rStyle': {
            rPrBase.push({ kind: 'rStyle', value: readCT_String(cur, ctx) });
            continue;
          }
          case 'rFonts': {
            rPrBase.push({ kind: 'rFonts', value: readCT_Fonts(cur, ctx) });
            continue;
          }
          case 'b': {
            rPrBase.push({ kind: 'b', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'bCs': {
            rPrBase.push({ kind: 'bCs', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'i': {
            rPrBase.push({ kind: 'i', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'iCs': {
            rPrBase.push({ kind: 'iCs', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'caps': {
            rPrBase.push({ kind: 'caps', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'smallCaps': {
            rPrBase.push({ kind: 'smallCaps', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'strike': {
            rPrBase.push({ kind: 'strike', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'dstrike': {
            rPrBase.push({ kind: 'dstrike', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'outline': {
            rPrBase.push({ kind: 'outline', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'shadow': {
            rPrBase.push({ kind: 'shadow', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'emboss': {
            rPrBase.push({ kind: 'emboss', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'imprint': {
            rPrBase.push({ kind: 'imprint', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'noProof': {
            rPrBase.push({ kind: 'noProof', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'snapToGrid': {
            rPrBase.push({ kind: 'snapToGrid', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'vanish': {
            rPrBase.push({ kind: 'vanish', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'webHidden': {
            rPrBase.push({ kind: 'webHidden', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'color': {
            rPrBase.push({ kind: 'color', value: readCT_Color(cur, ctx) });
            continue;
          }
          case 'spacing': {
            rPrBase.push({ kind: 'spacing', value: readCT_SignedTwipsMeasure(cur, ctx) });
            continue;
          }
          case 'w': {
            rPrBase.push({ kind: 'w', value: readCT_TextScale(cur, ctx) });
            continue;
          }
          case 'kern': {
            rPrBase.push({ kind: 'kern', value: readCT_HpsMeasure(cur, ctx) });
            continue;
          }
          case 'position': {
            rPrBase.push({ kind: 'position', value: readCT_SignedHpsMeasure(cur, ctx) });
            continue;
          }
          case 'sz': {
            rPrBase.push({ kind: 'sz', value: readCT_HpsMeasure(cur, ctx) });
            continue;
          }
          case 'szCs': {
            rPrBase.push({ kind: 'szCs', value: readCT_HpsMeasure(cur, ctx) });
            continue;
          }
          case 'highlight': {
            rPrBase.push({ kind: 'highlight', value: readCT_Highlight(cur, ctx) });
            continue;
          }
          case 'u': {
            rPrBase.push({ kind: 'u', value: readCT_Underline(cur, ctx) });
            continue;
          }
          case 'effect': {
            rPrBase.push({ kind: 'effect', value: readCT_TextEffect(cur, ctx) });
            continue;
          }
          case 'bdr': {
            rPrBase.push({ kind: 'bdr', value: readCT_Border(cur, ctx) });
            continue;
          }
          case 'shd': {
            rPrBase.push({ kind: 'shd', value: readCT_Shd(cur, ctx) });
            continue;
          }
          case 'fitText': {
            rPrBase.push({ kind: 'fitText', value: readCT_FitText(cur, ctx) });
            continue;
          }
          case 'vertAlign': {
            rPrBase.push({ kind: 'vertAlign', value: readCT_VerticalAlignRun(cur, ctx) });
            continue;
          }
          case 'rtl': {
            rPrBase.push({ kind: 'rtl', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'cs': {
            rPrBase.push({ kind: 'cs', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'em': {
            rPrBase.push({ kind: 'em', value: readCT_Em(cur, ctx) });
            continue;
          }
          case 'lang': {
            rPrBase.push({ kind: 'lang', value: readCT_Language(cur, ctx) });
            continue;
          }
          case 'eastAsianLayout': {
            rPrBase.push({ kind: 'eastAsianLayout', value: readCT_EastAsianLayout(cur, ctx) });
            continue;
          }
          case 'specVanish': {
            rPrBase.push({ kind: 'specVanish', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'oMath': {
            rPrBase.push({ kind: 'oMath', value: readCT_OnOff(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RPrOriginal', ev.localName, ev.uri, cur);
      rPrBase.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RPrOriginal', cur);
    cur.next();
  }

  return {
    rPrBase,
    $unknownAttrs,
  };
}

/** Read a `CT_ReadingModeInkLockDown`. See `transitional/wml.xsd:2878`. */
export function readCT_ReadingModeInkLockDown(cur: XmlCursor, ctx: ReadContext): CT_ReadingModeInkLockDown {
  const start = requireStart(cur, 'CT_ReadingModeInkLockDown');
  let actualPg: ST_OnOff | undefined;
  let w: ST_PixelsMeasure | undefined;
  let h: ST_PixelsMeasure | undefined;
  let fontSz: ST_DecimalNumberOrPercent | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'actualPg': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            actualPg = v;
            continue;
          }
          ctx.invalidValue('CT_ReadingModeInkLockDown', a, 'ST_OnOff', cur);
          break;
        }
        case 'w': {
          const v = parseST_PixelsMeasure(a.value);
          if (v !== undefined) {
            w = v;
            continue;
          }
          ctx.invalidValue('CT_ReadingModeInkLockDown', a, 'ST_PixelsMeasure', cur);
          break;
        }
        case 'h': {
          const v = parseST_PixelsMeasure(a.value);
          if (v !== undefined) {
            h = v;
            continue;
          }
          ctx.invalidValue('CT_ReadingModeInkLockDown', a, 'ST_PixelsMeasure', cur);
          break;
        }
        case 'fontSz': {
          const v = parseST_DecimalNumberOrPercent(a.value);
          if (v !== undefined) {
            fontSz = v;
            continue;
          }
          ctx.invalidValue('CT_ReadingModeInkLockDown', a, 'ST_DecimalNumberOrPercent', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ReadingModeInkLockDown', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (actualPg === undefined) ctx.missingRequired('CT_ReadingModeInkLockDown', 'actualPg', cur);
  if (w === undefined) ctx.missingRequired('CT_ReadingModeInkLockDown', 'w', cur);
  if (h === undefined) ctx.missingRequired('CT_ReadingModeInkLockDown', 'h', cur);
  if (fontSz === undefined) ctx.missingRequired('CT_ReadingModeInkLockDown', 'fontSz', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ReadingModeInkLockDown', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ReadingModeInkLockDown', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ReadingModeInkLockDown', cur);
    cur.next();
  }
  return {
    actualPg,
    w,
    h,
    fontSz,
    $unknownAttrs,
  };
}

/** Read a `CT_RecipientData`. See `transitional/wml.xsd:2633`. */
export function readCT_RecipientData(cur: XmlCursor, ctx: ReadContext): CT_RecipientData {
  const start = requireStart(cur, 'CT_RecipientData');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_RecipientData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let active: CT_OnOff | undefined;
  let column: CT_DecimalNumber | undefined;
  let uniqueTag: CT_Base64Binary | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RecipientData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'active': {
            active = readCT_OnOff(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'column': {
            column = readCT_DecimalNumber(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'uniqueTag': {
            uniqueTag = readCT_Base64Binary(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RecipientData', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RecipientData', cur);
    cur.next();
  }

  return {
    active,
    column,
    uniqueTag,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Recipients`. See `transitional/wml.xsd:2643`. */
export function readCT_Recipients(cur: XmlCursor, ctx: ReadContext): CT_Recipients {
  const start = requireStart(cur, 'CT_Recipients');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Recipients', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const recipientData: CT_RecipientData[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Recipients', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'recipientData': {
            recipientData.push(readCT_RecipientData(cur, ctx));
            $slot = 0;
            $index = recipientData.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Recipients', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Recipients', cur);
    cur.next();
  }

  return {
    recipientData,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Rel`. See `transitional/wml.xsd:1164`. */
export function readCT_Rel(cur: XmlCursor, ctx: ReadContext): CT_Rel {
  const start = requireStart(cur, 'CT_Rel');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Rel', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Rel', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_Rel', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Rel', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Rel', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Rel', cur);
    cur.next();
  }
  return {
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_Row`. See `transitional/wml.xsd:2415`. */
export function readCT_Row(cur: XmlCursor, ctx: ReadContext): CT_Row {
  const start = requireStart(cur, 'CT_Row');
  let rsidRPr: ST_LongHexNumber | undefined;
  let rsidR: ST_LongHexNumber | undefined;
  let rsidDel: ST_LongHexNumber | undefined;
  let rsidTr: ST_LongHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'rsidRPr': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidRPr = v;
            continue;
          }
          ctx.invalidValue('CT_Row', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidR': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidR = v;
            continue;
          }
          ctx.invalidValue('CT_Row', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidDel': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidDel = v;
            continue;
          }
          ctx.invalidValue('CT_Row', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidTr': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidTr = v;
            continue;
          }
          ctx.invalidValue('CT_Row', a, 'ST_LongHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Row', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let tblPrEx: CT_TblPrEx | undefined;
  let trPr: CT_TrPr | undefined;
  const contentCellContent: CT_Row_ContentCellContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Row', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            contentCellContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            contentCellContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tblPrEx': {
            tblPrEx = readCT_TblPrEx(cur, ctx);
            continue;
          }
          case 'trPr': {
            trPr = readCT_TrPr(cur, ctx);
            continue;
          }
          case 'tc': {
            contentCellContent.push({ kind: 'tc', value: readCT_Tc(cur, ctx) });
            continue;
          }
          case 'customXml': {
            contentCellContent.push({ kind: 'customXml', value: readCT_CustomXmlCell(cur, ctx) });
            continue;
          }
          case 'sdt': {
            contentCellContent.push({ kind: 'sdt', value: readCT_SdtCell(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            contentCellContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            contentCellContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            contentCellContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            contentCellContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            contentCellContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            contentCellContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            contentCellContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            contentCellContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            contentCellContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            contentCellContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            contentCellContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            contentCellContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            contentCellContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            contentCellContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            contentCellContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            contentCellContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            contentCellContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            contentCellContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            contentCellContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            contentCellContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            contentCellContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            contentCellContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            contentCellContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Row', ev.localName, ev.uri, cur);
      contentCellContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Row', cur);
    cur.next();
  }

  return {
    tblPrEx,
    trPr,
    contentCellContent,
    rsidRPr,
    rsidR,
    rsidDel,
    rsidTr,
    $unknownAttrs,
  };
}

/** Read a `CT_Ruby`. See `transitional/wml.xsd:1972`. */
export function readCT_Ruby(cur: XmlCursor, ctx: ReadContext): CT_Ruby {
  const start = requireStart(cur, 'CT_Ruby');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Ruby', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let rubyPr: CT_RubyPr | undefined;
  let rt: CT_RubyContent | undefined;
  let rubyBase: CT_RubyContent | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Ruby', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rubyPr': {
            rubyPr = readCT_RubyPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'rt': {
            rt = readCT_RubyContent(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'rubyBase': {
            rubyBase = readCT_RubyContent(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Ruby', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Ruby', cur);
    cur.next();
  }

  return {
    rubyPr,
    rt,
    rubyBase,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_RubyAlign`. See `transitional/wml.xsd:1950`. */
export function readCT_RubyAlign(cur: XmlCursor, ctx: ReadContext): CT_RubyAlign {
  const start = requireStart(cur, 'CT_RubyAlign');
  let val: ST_RubyAlign | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_RubyAlign(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_RubyAlign', a, 'ST_RubyAlign', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_RubyAlign', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_RubyAlign', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RubyAlign', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_RubyAlign', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RubyAlign', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_RubyContent`. See `transitional/wml.xsd:1969`. */
export function readCT_RubyContent(cur: XmlCursor, ctx: ReadContext): CT_RubyContent {
  const start = requireStart(cur, 'CT_RubyContent');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_RubyContent', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const rubyContent: CT_RubyContent_RubyContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RubyContent', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            rubyContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            rubyContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'r': {
            rubyContent.push({ kind: 'r', value: readCT_R(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            rubyContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            rubyContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            rubyContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            rubyContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            rubyContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            rubyContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            rubyContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            rubyContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            rubyContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            rubyContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            rubyContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            rubyContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            rubyContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            rubyContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            rubyContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            rubyContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            rubyContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            rubyContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            rubyContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            rubyContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            rubyContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            rubyContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            rubyContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RubyContent', ev.localName, ev.uri, cur);
      rubyContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RubyContent', cur);
    cur.next();
  }

  return {
    rubyContent,
    $unknownAttrs,
  };
}

/** Read a `CT_RubyPr`. See `transitional/wml.xsd:1953`. */
export function readCT_RubyPr(cur: XmlCursor, ctx: ReadContext): CT_RubyPr {
  const start = requireStart(cur, 'CT_RubyPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_RubyPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let rubyAlign: CT_RubyAlign | undefined;
  let hps: CT_HpsMeasure | undefined;
  let hpsRaise: CT_HpsMeasure | undefined;
  let hpsBaseText: CT_HpsMeasure | undefined;
  let lid: CT_Lang | undefined;
  let dirty: CT_OnOff | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RubyPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rubyAlign': {
            rubyAlign = readCT_RubyAlign(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hps': {
            hps = readCT_HpsMeasure(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'hpsRaise': {
            hpsRaise = readCT_HpsMeasure(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'hpsBaseText': {
            hpsBaseText = readCT_HpsMeasure(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'lid': {
            lid = readCT_Lang(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'dirty': {
            dirty = readCT_OnOff(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RubyPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RubyPr', cur);
    cur.next();
  }

  return {
    rubyAlign,
    hps,
    hpsRaise,
    hpsBaseText,
    lid,
    dirty,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_RunTrackChange`. See `transitional/wml.xsd:995`. */
export function readCT_RunTrackChange(cur: XmlCursor, ctx: ReadContext): CT_RunTrackChange {
  const start = requireStart(cur, 'CT_RunTrackChange');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_RunTrackChange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_RunTrackChange', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_RunTrackChange', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_RunTrackChange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_RunTrackChange', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_RunTrackChange', 'author', cur);

  const content: CT_RunTrackChange_Content[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RunTrackChange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            content.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            content.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
          case 'acc': {
            content.push({ kind: 'acc', value: readCT_Acc(cur, ctx) });
            continue;
          }
          case 'bar': {
            content.push({ kind: 'bar', value: readCT_Bar(cur, ctx) });
            continue;
          }
          case 'box': {
            content.push({ kind: 'box', value: readCT_Box(cur, ctx) });
            continue;
          }
          case 'borderBox': {
            content.push({ kind: 'borderBox', value: readCT_BorderBox(cur, ctx) });
            continue;
          }
          case 'd': {
            content.push({ kind: 'd', value: readCT_D(cur, ctx) });
            continue;
          }
          case 'eqArr': {
            content.push({ kind: 'eqArr', value: readCT_EqArr(cur, ctx) });
            continue;
          }
          case 'f': {
            content.push({ kind: 'f', value: readCT_F(cur, ctx) });
            continue;
          }
          case 'func': {
            content.push({ kind: 'func', value: readCT_Func(cur, ctx) });
            continue;
          }
          case 'groupChr': {
            content.push({ kind: 'groupChr', value: readCT_GroupChr(cur, ctx) });
            continue;
          }
          case 'limLow': {
            content.push({ kind: 'limLow', value: readCT_LimLow(cur, ctx) });
            continue;
          }
          case 'limUpp': {
            content.push({ kind: 'limUpp', value: readCT_LimUpp(cur, ctx) });
            continue;
          }
          case 'm': {
            content.push({ kind: 'm', value: readCT_M(cur, ctx) });
            continue;
          }
          case 'nary': {
            content.push({ kind: 'nary', value: readCT_Nary(cur, ctx) });
            continue;
          }
          case 'phant': {
            content.push({ kind: 'phant', value: readCT_Phant(cur, ctx) });
            continue;
          }
          case 'rad': {
            content.push({ kind: 'rad', value: readCT_Rad(cur, ctx) });
            continue;
          }
          case 'sPre': {
            content.push({ kind: 'sPre', value: readCT_SPre(cur, ctx) });
            continue;
          }
          case 'sSub': {
            content.push({ kind: 'sSub', value: readCT_SSub(cur, ctx) });
            continue;
          }
          case 'sSubSup': {
            content.push({ kind: 'sSubSup', value: readCT_SSubSup(cur, ctx) });
            continue;
          }
          case 'sSup': {
            content.push({ kind: 'sSup', value: readCT_SSup(cur, ctx) });
            continue;
          }
          case 'r': {
            content.push({ kind: 'r_math', value: math_readCT_R(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            content.push({ kind: 'customXml', value: readCT_CustomXmlRun(cur, ctx) });
            continue;
          }
          case 'smartTag': {
            content.push({ kind: 'smartTag', value: readCT_SmartTagRun(cur, ctx) });
            continue;
          }
          case 'sdt': {
            content.push({ kind: 'sdt', value: readCT_SdtRun(cur, ctx) });
            continue;
          }
          case 'dir': {
            content.push({ kind: 'dir', value: readCT_DirContentRun(cur, ctx) });
            continue;
          }
          case 'bdo': {
            content.push({ kind: 'bdo', value: readCT_BdoContentRun(cur, ctx) });
            continue;
          }
          case 'r': {
            content.push({ kind: 'r_wml', value: readCT_R(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            content.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            content.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            content.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            content.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            content.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            content.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            content.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            content.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            content.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            content.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            content.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            content.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            content.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            content.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            content.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            content.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            content.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            content.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            content.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            content.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            content.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            content.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            content.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RunTrackChange', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RunTrackChange', cur);
    cur.next();
  }

  return {
    content,
    id,
    author,
    date,
    $unknownAttrs,
  };
}

/** Read a `CT_SaveThroughXslt`. See `transitional/wml.xsd:2828`. */
export function readCT_SaveThroughXslt(cur: XmlCursor, ctx: ReadContext): CT_SaveThroughXslt {
  const start = requireStart(cur, 'CT_SaveThroughXslt');
  let id: ST_RelationshipId | undefined;
  let solutionID: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_SaveThroughXslt', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'solutionID': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            solutionID = v;
            continue;
          }
          ctx.invalidValue('CT_SaveThroughXslt', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SaveThroughXslt', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SaveThroughXslt', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SaveThroughXslt', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SaveThroughXslt', cur);
    cur.next();
  }
  return {
    id,
    solutionID,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtBlock`. See `transitional/wml.xsd:2144`. */
export function readCT_SdtBlock(cur: XmlCursor, ctx: ReadContext): CT_SdtBlock {
  const start = requireStart(cur, 'CT_SdtBlock');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SdtBlock', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let sdtPr: CT_SdtPr | undefined;
  let sdtEndPr: CT_SdtEndPr | undefined;
  let sdtContent: CT_SdtContentBlock | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtBlock', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'sdtPr': {
            sdtPr = readCT_SdtPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sdtEndPr': {
            sdtEndPr = readCT_SdtEndPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sdtContent': {
            sdtContent = readCT_SdtContentBlock(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtBlock', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtBlock', cur);
    cur.next();
  }

  return {
    sdtPr,
    sdtEndPr,
    sdtContent,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtCell`. See `transitional/wml.xsd:2158`. */
export function readCT_SdtCell(cur: XmlCursor, ctx: ReadContext): CT_SdtCell {
  const start = requireStart(cur, 'CT_SdtCell');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SdtCell', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let sdtPr: CT_SdtPr | undefined;
  let sdtEndPr: CT_SdtEndPr | undefined;
  let sdtContent: CT_SdtContentCell | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtCell', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'sdtPr': {
            sdtPr = readCT_SdtPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sdtEndPr': {
            sdtEndPr = readCT_SdtEndPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sdtContent': {
            sdtContent = readCT_SdtContentCell(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtCell', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtCell', cur);
    cur.next();
  }

  return {
    sdtPr,
    sdtEndPr,
    sdtContent,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtComboBox`. See `transitional/wml.xsd:2016`. */
export function readCT_SdtComboBox(cur: XmlCursor, ctx: ReadContext): CT_SdtComboBox {
  const start = requireStart(cur, 'CT_SdtComboBox');
  let lastValue: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'lastValue': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            lastValue = v;
            continue;
          }
          ctx.invalidValue('CT_SdtComboBox', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SdtComboBox', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const listItem: CT_SdtListItem[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtComboBox', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'listItem': {
            listItem.push(readCT_SdtListItem(cur, ctx));
            $slot = 0;
            $index = listItem.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtComboBox', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtComboBox', cur);
    cur.next();
  }

  return {
    listItem,
    lastValue,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtContentBlock`. See `transitional/wml.xsd:2119`. */
export function readCT_SdtContentBlock(cur: XmlCursor, ctx: ReadContext): CT_SdtContentBlock {
  const start = requireStart(cur, 'CT_SdtContentBlock');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SdtContentBlock', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const contentBlockContent: CT_SdtContentBlock_ContentBlockContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtContentBlock', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            contentBlockContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            contentBlockContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            contentBlockContent.push({ kind: 'customXml', value: readCT_CustomXmlBlock(cur, ctx) });
            continue;
          }
          case 'sdt': {
            contentBlockContent.push({ kind: 'sdt', value: readCT_SdtBlock(cur, ctx) });
            continue;
          }
          case 'p': {
            contentBlockContent.push({ kind: 'p', value: readCT_P(cur, ctx) });
            continue;
          }
          case 'tbl': {
            contentBlockContent.push({ kind: 'tbl', value: readCT_Tbl(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            contentBlockContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            contentBlockContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            contentBlockContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            contentBlockContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            contentBlockContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            contentBlockContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            contentBlockContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            contentBlockContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            contentBlockContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            contentBlockContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            contentBlockContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            contentBlockContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            contentBlockContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            contentBlockContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            contentBlockContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            contentBlockContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            contentBlockContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            contentBlockContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            contentBlockContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            contentBlockContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            contentBlockContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            contentBlockContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            contentBlockContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtContentBlock', ev.localName, ev.uri, cur);
      contentBlockContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtContentBlock', cur);
    cur.next();
  }

  return {
    contentBlockContent,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtContentCell`. See `transitional/wml.xsd:2141`. */
export function readCT_SdtContentCell(cur: XmlCursor, ctx: ReadContext): CT_SdtContentCell {
  const start = requireStart(cur, 'CT_SdtContentCell');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SdtContentCell', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const contentCellContent: CT_SdtContentCell_ContentCellContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtContentCell', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            contentCellContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            contentCellContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tc': {
            contentCellContent.push({ kind: 'tc', value: readCT_Tc(cur, ctx) });
            continue;
          }
          case 'customXml': {
            contentCellContent.push({ kind: 'customXml', value: readCT_CustomXmlCell(cur, ctx) });
            continue;
          }
          case 'sdt': {
            contentCellContent.push({ kind: 'sdt', value: readCT_SdtCell(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            contentCellContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            contentCellContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            contentCellContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            contentCellContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            contentCellContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            contentCellContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            contentCellContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            contentCellContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            contentCellContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            contentCellContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            contentCellContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            contentCellContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            contentCellContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            contentCellContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            contentCellContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            contentCellContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            contentCellContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            contentCellContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            contentCellContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            contentCellContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            contentCellContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            contentCellContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            contentCellContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtContentCell', ev.localName, ev.uri, cur);
      contentCellContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtContentCell', cur);
    cur.next();
  }

  return {
    contentCellContent,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtContentRow`. See `transitional/wml.xsd:2130`. */
export function readCT_SdtContentRow(cur: XmlCursor, ctx: ReadContext): CT_SdtContentRow {
  const start = requireStart(cur, 'CT_SdtContentRow');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SdtContentRow', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const contentRowContent: CT_SdtContentRow_ContentRowContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtContentRow', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            contentRowContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            contentRowContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tr': {
            contentRowContent.push({ kind: 'tr', value: readCT_Row(cur, ctx) });
            continue;
          }
          case 'customXml': {
            contentRowContent.push({ kind: 'customXml', value: readCT_CustomXmlRow(cur, ctx) });
            continue;
          }
          case 'sdt': {
            contentRowContent.push({ kind: 'sdt', value: readCT_SdtRow(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            contentRowContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            contentRowContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            contentRowContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            contentRowContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            contentRowContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            contentRowContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            contentRowContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            contentRowContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            contentRowContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            contentRowContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            contentRowContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            contentRowContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            contentRowContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            contentRowContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            contentRowContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            contentRowContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            contentRowContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            contentRowContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            contentRowContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            contentRowContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            contentRowContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            contentRowContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            contentRowContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtContentRow', ev.localName, ev.uri, cur);
      contentRowContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtContentRow', cur);
    cur.next();
  }

  return {
    contentRowContent,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtContentRun`. See `transitional/wml.xsd:2107`. */
export function readCT_SdtContentRun(cur: XmlCursor, ctx: ReadContext): CT_SdtContentRun {
  const start = requireStart(cur, 'CT_SdtContentRun');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SdtContentRun', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const pContent: CT_SdtContentRun_PContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtContentRun', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            pContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            pContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            pContent.push({ kind: 'customXml', value: readCT_CustomXmlRun(cur, ctx) });
            continue;
          }
          case 'smartTag': {
            pContent.push({ kind: 'smartTag', value: readCT_SmartTagRun(cur, ctx) });
            continue;
          }
          case 'sdt': {
            pContent.push({ kind: 'sdt', value: readCT_SdtRun(cur, ctx) });
            continue;
          }
          case 'dir': {
            pContent.push({ kind: 'dir', value: readCT_DirContentRun(cur, ctx) });
            continue;
          }
          case 'bdo': {
            pContent.push({ kind: 'bdo', value: readCT_BdoContentRun(cur, ctx) });
            continue;
          }
          case 'r': {
            pContent.push({ kind: 'r', value: readCT_R(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            pContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            pContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            pContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            pContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            pContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            pContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            pContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            pContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            pContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            pContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            pContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            pContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            pContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            pContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            pContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            pContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            pContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            pContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            pContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            pContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            pContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            pContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            pContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'fldSimple': {
            pContent.push({ kind: 'fldSimple', value: readCT_SimpleField(cur, ctx) });
            continue;
          }
          case 'hyperlink': {
            pContent.push({ kind: 'hyperlink', value: readCT_Hyperlink(cur, ctx) });
            continue;
          }
          case 'subDoc': {
            pContent.push({ kind: 'subDoc', value: readCT_Rel(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtContentRun', ev.localName, ev.uri, cur);
      pContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtContentRun', cur);
    cur.next();
  }

  return {
    pContent,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtDate`. See `transitional/wml.xsd:2007`. */
export function readCT_SdtDate(cur: XmlCursor, ctx: ReadContext): CT_SdtDate {
  const start = requireStart(cur, 'CT_SdtDate');
  let fullDate: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'fullDate': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            fullDate = v;
            continue;
          }
          ctx.invalidValue('CT_SdtDate', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SdtDate', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let dateFormat: CT_String | undefined;
  let lid: CT_Lang | undefined;
  let storeMappedDataAs: CT_SdtDateMappingType | undefined;
  let calendar: CT_CalendarType | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtDate', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'dateFormat': {
            dateFormat = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lid': {
            lid = readCT_Lang(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'storeMappedDataAs': {
            storeMappedDataAs = readCT_SdtDateMappingType(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'calendar': {
            calendar = readCT_CalendarType(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtDate', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtDate', cur);
    cur.next();
  }

  return {
    dateFormat,
    lid,
    storeMappedDataAs,
    calendar,
    fullDate,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtDateMappingType`. See `transitional/wml.xsd:2001`. */
export function readCT_SdtDateMappingType(cur: XmlCursor, ctx: ReadContext): CT_SdtDateMappingType {
  const start = requireStart(cur, 'CT_SdtDateMappingType');
  let val: ST_SdtDateMappingType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_SdtDateMappingType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_SdtDateMappingType', a, 'ST_SdtDateMappingType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SdtDateMappingType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtDateMappingType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SdtDateMappingType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtDateMappingType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtDocPart`. See `transitional/wml.xsd:2022`. */
export function readCT_SdtDocPart(cur: XmlCursor, ctx: ReadContext): CT_SdtDocPart {
  const start = requireStart(cur, 'CT_SdtDocPart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SdtDocPart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let docPartGallery: CT_String | undefined;
  let docPartCategory: CT_String | undefined;
  let docPartUnique: CT_OnOff | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtDocPart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'docPartGallery': {
            docPartGallery = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'docPartCategory': {
            docPartCategory = readCT_String(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'docPartUnique': {
            docPartUnique = readCT_OnOff(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtDocPart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtDocPart', cur);
    cur.next();
  }

  return {
    docPartGallery,
    docPartCategory,
    docPartUnique,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtDropDownList`. See `transitional/wml.xsd:2029`. */
export function readCT_SdtDropDownList(cur: XmlCursor, ctx: ReadContext): CT_SdtDropDownList {
  const start = requireStart(cur, 'CT_SdtDropDownList');
  let lastValue: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'lastValue': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            lastValue = v;
            continue;
          }
          ctx.invalidValue('CT_SdtDropDownList', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SdtDropDownList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const listItem: CT_SdtListItem[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtDropDownList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'listItem': {
            listItem.push(readCT_SdtListItem(cur, ctx));
            $slot = 0;
            $index = listItem.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtDropDownList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtDropDownList', cur);
    cur.next();
  }

  return {
    listItem,
    lastValue,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtEndPr`. See `transitional/wml.xsd:2077`. */
export function readCT_SdtEndPr(cur: XmlCursor, ctx: ReadContext): CT_SdtEndPr {
  const start = requireStart(cur, 'CT_SdtEndPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SdtEndPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const rPr: CT_RPr[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtEndPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rPr': {
            rPr.push(readCT_RPr(cur, ctx));
            $slot = 0;
            $index = rPr.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtEndPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtEndPr', cur);
    cur.next();
  }

  return {
    rPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtListItem`. See `transitional/wml.xsd:1990`. */
export function readCT_SdtListItem(cur: XmlCursor, ctx: ReadContext): CT_SdtListItem {
  const start = requireStart(cur, 'CT_SdtListItem');
  let displayText: ST_String | undefined;
  let value: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'displayText': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            displayText = v;
            continue;
          }
          ctx.invalidValue('CT_SdtListItem', a, 'ST_String', cur);
          break;
        }
        case 'value': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            value = v;
            continue;
          }
          ctx.invalidValue('CT_SdtListItem', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SdtListItem', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtListItem', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SdtListItem', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtListItem', cur);
    cur.next();
  }
  return {
    displayText,
    value,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtPr`. See `transitional/wml.xsd:2048`. */
export function readCT_SdtPr(cur: XmlCursor, ctx: ReadContext): CT_SdtPr {
  const start = requireStart(cur, 'CT_SdtPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SdtPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let rPr: CT_RPr | undefined;
  let alias: CT_String | undefined;
  let tag: CT_String | undefined;
  let id: CT_DecimalNumber | undefined;
  let lock: CT_Lock | undefined;
  let placeholder: CT_Placeholder | undefined;
  let temporary: CT_OnOff | undefined;
  let showingPlcHdr: CT_OnOff | undefined;
  let dataBinding: CT_DataBinding | undefined;
  let label: CT_DecimalNumber | undefined;
  let tabIndex: CT_UnsignedDecimalNumber | undefined;
  let content: CT_SdtPr_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'rPr': {
            rPr = readCT_RPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'alias': {
            alias = readCT_String(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tag': {
            tag = readCT_String(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'id': {
            id = readCT_DecimalNumber(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'lock': {
            lock = readCT_Lock(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'placeholder': {
            placeholder = readCT_Placeholder(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'temporary': {
            temporary = readCT_OnOff(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'showingPlcHdr': {
            showingPlcHdr = readCT_OnOff(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'dataBinding': {
            dataBinding = readCT_DataBinding(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'label': {
            label = readCT_DecimalNumber(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tabIndex': {
            tabIndex = readCT_UnsignedDecimalNumber(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'equation': {
            content = { kind: 'equation', value: readCT_Empty(cur, ctx) };
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'comboBox': {
            content = { kind: 'comboBox', value: readCT_SdtComboBox(cur, ctx) };
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'date': {
            content = { kind: 'date', value: readCT_SdtDate(cur, ctx) };
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'docPartObj': {
            content = { kind: 'docPartObj', value: readCT_SdtDocPart(cur, ctx) };
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'docPartList': {
            content = { kind: 'docPartList', value: readCT_SdtDocPart(cur, ctx) };
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'dropDownList': {
            content = { kind: 'dropDownList', value: readCT_SdtDropDownList(cur, ctx) };
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'picture': {
            content = { kind: 'picture', value: readCT_Empty(cur, ctx) };
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'richText': {
            content = { kind: 'richText', value: readCT_Empty(cur, ctx) };
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'text': {
            content = { kind: 'text', value: readCT_SdtText(cur, ctx) };
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'citation': {
            content = { kind: 'citation', value: readCT_Empty(cur, ctx) };
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'group': {
            content = { kind: 'group', value: readCT_Empty(cur, ctx) };
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'bibliography': {
            content = { kind: 'bibliography', value: readCT_Empty(cur, ctx) };
            $slot = 11;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtPr', cur);
    cur.next();
  }

  return {
    rPr,
    alias,
    tag,
    id,
    lock,
    placeholder,
    temporary,
    showingPlcHdr,
    dataBinding,
    label,
    tabIndex,
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtRow`. See `transitional/wml.xsd:2165`. */
export function readCT_SdtRow(cur: XmlCursor, ctx: ReadContext): CT_SdtRow {
  const start = requireStart(cur, 'CT_SdtRow');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SdtRow', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let sdtPr: CT_SdtPr | undefined;
  let sdtEndPr: CT_SdtEndPr | undefined;
  let sdtContent: CT_SdtContentRow | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtRow', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'sdtPr': {
            sdtPr = readCT_SdtPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sdtEndPr': {
            sdtEndPr = readCT_SdtEndPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sdtContent': {
            sdtContent = readCT_SdtContentRow(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtRow', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtRow', cur);
    cur.next();
  }

  return {
    sdtPr,
    sdtEndPr,
    sdtContent,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtRun`. See `transitional/wml.xsd:2151`. */
export function readCT_SdtRun(cur: XmlCursor, ctx: ReadContext): CT_SdtRun {
  const start = requireStart(cur, 'CT_SdtRun');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SdtRun', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let sdtPr: CT_SdtPr | undefined;
  let sdtEndPr: CT_SdtEndPr | undefined;
  let sdtContent: CT_SdtContentRun | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtRun', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'sdtPr': {
            sdtPr = readCT_SdtPr(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sdtEndPr': {
            sdtEndPr = readCT_SdtEndPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sdtContent': {
            sdtContent = readCT_SdtContentRun(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SdtRun', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtRun', cur);
    cur.next();
  }

  return {
    sdtPr,
    sdtEndPr,
    sdtContent,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SdtText`. See `transitional/wml.xsd:2040`. */
export function readCT_SdtText(cur: XmlCursor, ctx: ReadContext): CT_SdtText {
  const start = requireStart(cur, 'CT_SdtText');
  let multiLine: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'multiLine': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            multiLine = v;
            continue;
          }
          ctx.invalidValue('CT_SdtText', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SdtText', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SdtText', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SdtText', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SdtText', cur);
    cur.next();
  }
  return {
    multiLine,
    $unknownAttrs,
  };
}

/** Read a `CT_SectPr`. See `transitional/wml.xsd:1642`. */
export function readCT_SectPr(cur: XmlCursor, ctx: ReadContext): CT_SectPr {
  const start = requireStart(cur, 'CT_SectPr');
  let rsidRPr: ST_LongHexNumber | undefined;
  let rsidDel: ST_LongHexNumber | undefined;
  let rsidR: ST_LongHexNumber | undefined;
  let rsidSect: ST_LongHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'rsidRPr': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidRPr = v;
            continue;
          }
          ctx.invalidValue('CT_SectPr', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidDel': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidDel = v;
            continue;
          }
          ctx.invalidValue('CT_SectPr', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidR': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidR = v;
            continue;
          }
          ctx.invalidValue('CT_SectPr', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidSect': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidSect = v;
            continue;
          }
          ctx.invalidValue('CT_SectPr', a, 'ST_LongHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SectPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const hdrFtrReferences: CT_SectPr_HdrFtrReferences[] = [];
  let footnotePr: CT_FtnProps | undefined;
  let endnotePr: CT_EdnProps | undefined;
  let type: CT_SectType | undefined;
  let pgSz: CT_PageSz | undefined;
  let pgMar: CT_PageMar | undefined;
  let paperSrc: CT_PaperSource | undefined;
  let pgBorders: CT_PageBorders | undefined;
  let lnNumType: CT_LineNumber | undefined;
  let pgNumType: CT_PageNumber | undefined;
  let cols: CT_Columns | undefined;
  let formProt: CT_OnOff | undefined;
  let vAlign: CT_VerticalJc | undefined;
  let noEndnote: CT_OnOff | undefined;
  let titlePg: CT_OnOff | undefined;
  let textDirection: CT_TextDirection | undefined;
  let bidi: CT_OnOff | undefined;
  let rtlGutter: CT_OnOff | undefined;
  let docGrid: CT_DocGrid | undefined;
  let printerSettings: CT_Rel | undefined;
  let sectPrChange: CT_SectPrChange | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SectPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'headerReference': {
            hdrFtrReferences.push({ kind: 'headerReference', value: readCT_HdrFtrRef(cur, ctx) });
            continue;
          }
          case 'footerReference': {
            hdrFtrReferences.push({ kind: 'footerReference', value: readCT_HdrFtrRef(cur, ctx) });
            continue;
          }
          case 'footnotePr': {
            footnotePr = readCT_FtnProps(cur, ctx);
            continue;
          }
          case 'endnotePr': {
            endnotePr = readCT_EdnProps(cur, ctx);
            continue;
          }
          case 'type': {
            type = readCT_SectType(cur, ctx);
            continue;
          }
          case 'pgSz': {
            pgSz = readCT_PageSz(cur, ctx);
            continue;
          }
          case 'pgMar': {
            pgMar = readCT_PageMar(cur, ctx);
            continue;
          }
          case 'paperSrc': {
            paperSrc = readCT_PaperSource(cur, ctx);
            continue;
          }
          case 'pgBorders': {
            pgBorders = readCT_PageBorders(cur, ctx);
            continue;
          }
          case 'lnNumType': {
            lnNumType = readCT_LineNumber(cur, ctx);
            continue;
          }
          case 'pgNumType': {
            pgNumType = readCT_PageNumber(cur, ctx);
            continue;
          }
          case 'cols': {
            cols = readCT_Columns(cur, ctx);
            continue;
          }
          case 'formProt': {
            formProt = readCT_OnOff(cur, ctx);
            continue;
          }
          case 'vAlign': {
            vAlign = readCT_VerticalJc(cur, ctx);
            continue;
          }
          case 'noEndnote': {
            noEndnote = readCT_OnOff(cur, ctx);
            continue;
          }
          case 'titlePg': {
            titlePg = readCT_OnOff(cur, ctx);
            continue;
          }
          case 'textDirection': {
            textDirection = readCT_TextDirection(cur, ctx);
            continue;
          }
          case 'bidi': {
            bidi = readCT_OnOff(cur, ctx);
            continue;
          }
          case 'rtlGutter': {
            rtlGutter = readCT_OnOff(cur, ctx);
            continue;
          }
          case 'docGrid': {
            docGrid = readCT_DocGrid(cur, ctx);
            continue;
          }
          case 'printerSettings': {
            printerSettings = readCT_Rel(cur, ctx);
            continue;
          }
          case 'sectPrChange': {
            sectPrChange = readCT_SectPrChange(cur, ctx);
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SectPr', ev.localName, ev.uri, cur);
      hdrFtrReferences.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SectPr', cur);
    cur.next();
  }

  return {
    hdrFtrReferences,
    footnotePr,
    endnotePr,
    type,
    pgSz,
    pgMar,
    paperSrc,
    pgBorders,
    lnNumType,
    pgNumType,
    cols,
    formProt,
    vAlign,
    noEndnote,
    titlePg,
    textDirection,
    bidi,
    rtlGutter,
    docGrid,
    printerSettings,
    sectPrChange,
    rsidRPr,
    rsidDel,
    rsidR,
    rsidSect,
    $unknownAttrs,
  };
}

/** Read a `CT_SectPrBase`. See `transitional/wml.xsd:1636`. */
export function readCT_SectPrBase(cur: XmlCursor, ctx: ReadContext): CT_SectPrBase {
  const start = requireStart(cur, 'CT_SectPrBase');
  let rsidRPr: ST_LongHexNumber | undefined;
  let rsidDel: ST_LongHexNumber | undefined;
  let rsidR: ST_LongHexNumber | undefined;
  let rsidSect: ST_LongHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'rsidRPr': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidRPr = v;
            continue;
          }
          ctx.invalidValue('CT_SectPrBase', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidDel': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidDel = v;
            continue;
          }
          ctx.invalidValue('CT_SectPrBase', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidR': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidR = v;
            continue;
          }
          ctx.invalidValue('CT_SectPrBase', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'rsidSect': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            rsidSect = v;
            continue;
          }
          ctx.invalidValue('CT_SectPrBase', a, 'ST_LongHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SectPrBase', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let footnotePr: CT_FtnProps | undefined;
  let endnotePr: CT_EdnProps | undefined;
  let type: CT_SectType | undefined;
  let pgSz: CT_PageSz | undefined;
  let pgMar: CT_PageMar | undefined;
  let paperSrc: CT_PaperSource | undefined;
  let pgBorders: CT_PageBorders | undefined;
  let lnNumType: CT_LineNumber | undefined;
  let pgNumType: CT_PageNumber | undefined;
  let cols: CT_Columns | undefined;
  let formProt: CT_OnOff | undefined;
  let vAlign: CT_VerticalJc | undefined;
  let noEndnote: CT_OnOff | undefined;
  let titlePg: CT_OnOff | undefined;
  let textDirection: CT_TextDirection | undefined;
  let bidi: CT_OnOff | undefined;
  let rtlGutter: CT_OnOff | undefined;
  let docGrid: CT_DocGrid | undefined;
  let printerSettings: CT_Rel | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SectPrBase', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'footnotePr': {
            footnotePr = readCT_FtnProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'endnotePr': {
            endnotePr = readCT_EdnProps(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'type': {
            type = readCT_SectType(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'pgSz': {
            pgSz = readCT_PageSz(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'pgMar': {
            pgMar = readCT_PageMar(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'paperSrc': {
            paperSrc = readCT_PaperSource(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'pgBorders': {
            pgBorders = readCT_PageBorders(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'lnNumType': {
            lnNumType = readCT_LineNumber(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'pgNumType': {
            pgNumType = readCT_PageNumber(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'cols': {
            cols = readCT_Columns(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'formProt': {
            formProt = readCT_OnOff(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'vAlign': {
            vAlign = readCT_VerticalJc(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'noEndnote': {
            noEndnote = readCT_OnOff(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'titlePg': {
            titlePg = readCT_OnOff(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'textDirection': {
            textDirection = readCT_TextDirection(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'bidi': {
            bidi = readCT_OnOff(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'rtlGutter': {
            rtlGutter = readCT_OnOff(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'docGrid': {
            docGrid = readCT_DocGrid(cur, ctx);
            $slot = 17;
            $index = undefined;
            continue;
          }
          case 'printerSettings': {
            printerSettings = readCT_Rel(cur, ctx);
            $slot = 18;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SectPrBase', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SectPrBase', cur);
    cur.next();
  }

  return {
    footnotePr,
    endnotePr,
    type,
    pgSz,
    pgMar,
    paperSrc,
    pgBorders,
    lnNumType,
    pgNumType,
    cols,
    formProt,
    vAlign,
    noEndnote,
    titlePg,
    textDirection,
    bidi,
    rtlGutter,
    docGrid,
    printerSettings,
    rsidRPr,
    rsidDel,
    rsidR,
    rsidSect,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SectPrChange`. See `transitional/wml.xsd:959`. */
export function readCT_SectPrChange(cur: XmlCursor, ctx: ReadContext): CT_SectPrChange {
  const start = requireStart(cur, 'CT_SectPrChange');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_SectPrChange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_SectPrChange', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_SectPrChange', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SectPrChange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_SectPrChange', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_SectPrChange', 'author', cur);

  let sectPr: CT_SectPrBase | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SectPrChange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'sectPr': {
            sectPr = readCT_SectPrBase(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SectPrChange', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SectPrChange', cur);
    cur.next();
  }

  return {
    sectPr,
    id,
    author,
    date,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SectType`. See `transitional/wml.xsd:1363`. */
export function readCT_SectType(cur: XmlCursor, ctx: ReadContext): CT_SectType {
  const start = requireStart(cur, 'CT_SectType');
  let val: ST_SectionMark | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_SectionMark(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_SectType', a, 'ST_SectionMark', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SectType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SectType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SectType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SectType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Settings`. See `transitional/wml.xsd:2889`. */
export function readCT_Settings(cur: XmlCursor, ctx: ReadContext): CT_Settings {
  const start = requireStart(cur, 'CT_Settings');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Settings', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let writeProtection: CT_WriteProtection | undefined;
  let view: CT_View | undefined;
  let zoom: CT_Zoom | undefined;
  let removePersonalInformation: CT_OnOff | undefined;
  let removeDateAndTime: CT_OnOff | undefined;
  let doNotDisplayPageBoundaries: CT_OnOff | undefined;
  let displayBackgroundShape: CT_OnOff | undefined;
  let printPostScriptOverText: CT_OnOff | undefined;
  let printFractionalCharacterWidth: CT_OnOff | undefined;
  let printFormsData: CT_OnOff | undefined;
  let embedTrueTypeFonts: CT_OnOff | undefined;
  let embedSystemFonts: CT_OnOff | undefined;
  let saveSubsetFonts: CT_OnOff | undefined;
  let saveFormsData: CT_OnOff | undefined;
  let mirrorMargins: CT_OnOff | undefined;
  let alignBordersAndEdges: CT_OnOff | undefined;
  let bordersDoNotSurroundHeader: CT_OnOff | undefined;
  let bordersDoNotSurroundFooter: CT_OnOff | undefined;
  let gutterAtTop: CT_OnOff | undefined;
  let hideSpellingErrors: CT_OnOff | undefined;
  let hideGrammaticalErrors: CT_OnOff | undefined;
  const activeWritingStyle: CT_WritingStyle[] = [];
  let proofState: CT_Proof | undefined;
  let formsDesign: CT_OnOff | undefined;
  let attachedTemplate: CT_Rel | undefined;
  let linkStyles: CT_OnOff | undefined;
  let stylePaneFormatFilter: CT_StylePaneFilter | undefined;
  let stylePaneSortMethod: CT_StyleSort | undefined;
  let documentType: CT_DocType | undefined;
  let mailMerge: CT_MailMerge | undefined;
  let revisionView: CT_TrackChangesView | undefined;
  let trackRevisions: CT_OnOff | undefined;
  let doNotTrackMoves: CT_OnOff | undefined;
  let doNotTrackFormatting: CT_OnOff | undefined;
  let documentProtection: CT_DocProtect | undefined;
  let autoFormatOverride: CT_OnOff | undefined;
  let styleLockTheme: CT_OnOff | undefined;
  let styleLockQFSet: CT_OnOff | undefined;
  let defaultTabStop: CT_TwipsMeasure | undefined;
  let autoHyphenation: CT_OnOff | undefined;
  let consecutiveHyphenLimit: CT_DecimalNumber | undefined;
  let hyphenationZone: CT_TwipsMeasure | undefined;
  let doNotHyphenateCaps: CT_OnOff | undefined;
  let showEnvelope: CT_OnOff | undefined;
  let summaryLength: CT_DecimalNumberOrPrecent | undefined;
  let clickAndTypeStyle: CT_String | undefined;
  let defaultTableStyle: CT_String | undefined;
  let evenAndOddHeaders: CT_OnOff | undefined;
  let bookFoldRevPrinting: CT_OnOff | undefined;
  let bookFoldPrinting: CT_OnOff | undefined;
  let bookFoldPrintingSheets: CT_DecimalNumber | undefined;
  let drawingGridHorizontalSpacing: CT_TwipsMeasure | undefined;
  let drawingGridVerticalSpacing: CT_TwipsMeasure | undefined;
  let displayHorizontalDrawingGridEvery: CT_DecimalNumber | undefined;
  let displayVerticalDrawingGridEvery: CT_DecimalNumber | undefined;
  let doNotUseMarginsForDrawingGridOrigin: CT_OnOff | undefined;
  let drawingGridHorizontalOrigin: CT_TwipsMeasure | undefined;
  let drawingGridVerticalOrigin: CT_TwipsMeasure | undefined;
  let doNotShadeFormData: CT_OnOff | undefined;
  let noPunctuationKerning: CT_OnOff | undefined;
  let characterSpacingControl: CT_CharacterSpacing | undefined;
  let printTwoOnOne: CT_OnOff | undefined;
  let strictFirstAndLastChars: CT_OnOff | undefined;
  let noLineBreaksAfter: CT_Kinsoku | undefined;
  let noLineBreaksBefore: CT_Kinsoku | undefined;
  let savePreviewPicture: CT_OnOff | undefined;
  let doNotValidateAgainstSchema: CT_OnOff | undefined;
  let saveInvalidXml: CT_OnOff | undefined;
  let ignoreMixedContent: CT_OnOff | undefined;
  let alwaysShowPlaceholderText: CT_OnOff | undefined;
  let doNotDemarcateInvalidXml: CT_OnOff | undefined;
  let saveXmlDataOnly: CT_OnOff | undefined;
  let useXSLTWhenSaving: CT_OnOff | undefined;
  let saveThroughXslt: CT_SaveThroughXslt | undefined;
  let showXMLTags: CT_OnOff | undefined;
  let alwaysMergeEmptyNamespace: CT_OnOff | undefined;
  let updateFields: CT_OnOff | undefined;
  let hdrShapeDefaults: CT_ShapeDefaults | undefined;
  let footnotePr: CT_FtnDocProps | undefined;
  let endnotePr: CT_EdnDocProps | undefined;
  let compat: CT_Compat | undefined;
  let docVars: CT_DocVars | undefined;
  let rsids: CT_DocRsids | undefined;
  let mathPr: CT_MathPr | undefined;
  const attachedSchema: CT_String[] = [];
  let themeFontLang: CT_Language | undefined;
  let clrSchemeMapping: CT_ColorSchemeMapping | undefined;
  let doNotIncludeSubdocsInStats: CT_OnOff | undefined;
  let doNotAutoCompressPictures: CT_OnOff | undefined;
  let forceUpgrade: CT_Empty | undefined;
  let captions: CT_Captions | undefined;
  let readModeInkLockDown: CT_ReadingModeInkLockDown | undefined;
  const smartTagType: CT_SmartTagType[] = [];
  let schemaLibrary: CT_SchemaLibrary | undefined;
  let shapeDefaults: CT_ShapeDefaults | undefined;
  let doNotEmbedSmartTags: CT_OnOff | undefined;
  let decimalSymbol: CT_String | undefined;
  let listSeparator: CT_String | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  const NS_schema_library = ctx.uris['schema-library'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Settings', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'mathPr': {
            mathPr = readCT_MathPr(cur, ctx);
            $slot = 83;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === NS_schema_library) {
        switch (ev.localName) {
          case 'schemaLibrary': {
            schemaLibrary = readCT_SchemaLibrary(cur, ctx);
            $slot = 93;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'writeProtection': {
            writeProtection = readCT_WriteProtection(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'view': {
            view = readCT_View(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'zoom': {
            zoom = readCT_Zoom(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'removePersonalInformation': {
            removePersonalInformation = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'removeDateAndTime': {
            removeDateAndTime = readCT_OnOff(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'doNotDisplayPageBoundaries': {
            doNotDisplayPageBoundaries = readCT_OnOff(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'displayBackgroundShape': {
            displayBackgroundShape = readCT_OnOff(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'printPostScriptOverText': {
            printPostScriptOverText = readCT_OnOff(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'printFractionalCharacterWidth': {
            printFractionalCharacterWidth = readCT_OnOff(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'printFormsData': {
            printFormsData = readCT_OnOff(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'embedTrueTypeFonts': {
            embedTrueTypeFonts = readCT_OnOff(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'embedSystemFonts': {
            embedSystemFonts = readCT_OnOff(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'saveSubsetFonts': {
            saveSubsetFonts = readCT_OnOff(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'saveFormsData': {
            saveFormsData = readCT_OnOff(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'mirrorMargins': {
            mirrorMargins = readCT_OnOff(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'alignBordersAndEdges': {
            alignBordersAndEdges = readCT_OnOff(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'bordersDoNotSurroundHeader': {
            bordersDoNotSurroundHeader = readCT_OnOff(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'bordersDoNotSurroundFooter': {
            bordersDoNotSurroundFooter = readCT_OnOff(cur, ctx);
            $slot = 17;
            $index = undefined;
            continue;
          }
          case 'gutterAtTop': {
            gutterAtTop = readCT_OnOff(cur, ctx);
            $slot = 18;
            $index = undefined;
            continue;
          }
          case 'hideSpellingErrors': {
            hideSpellingErrors = readCT_OnOff(cur, ctx);
            $slot = 19;
            $index = undefined;
            continue;
          }
          case 'hideGrammaticalErrors': {
            hideGrammaticalErrors = readCT_OnOff(cur, ctx);
            $slot = 20;
            $index = undefined;
            continue;
          }
          case 'activeWritingStyle': {
            activeWritingStyle.push(readCT_WritingStyle(cur, ctx));
            $slot = 21;
            $index = activeWritingStyle.length - 1;
            continue;
          }
          case 'proofState': {
            proofState = readCT_Proof(cur, ctx);
            $slot = 22;
            $index = undefined;
            continue;
          }
          case 'formsDesign': {
            formsDesign = readCT_OnOff(cur, ctx);
            $slot = 23;
            $index = undefined;
            continue;
          }
          case 'attachedTemplate': {
            attachedTemplate = readCT_Rel(cur, ctx);
            $slot = 24;
            $index = undefined;
            continue;
          }
          case 'linkStyles': {
            linkStyles = readCT_OnOff(cur, ctx);
            $slot = 25;
            $index = undefined;
            continue;
          }
          case 'stylePaneFormatFilter': {
            stylePaneFormatFilter = readCT_StylePaneFilter(cur, ctx);
            $slot = 26;
            $index = undefined;
            continue;
          }
          case 'stylePaneSortMethod': {
            stylePaneSortMethod = readCT_StyleSort(cur, ctx);
            $slot = 27;
            $index = undefined;
            continue;
          }
          case 'documentType': {
            documentType = readCT_DocType(cur, ctx);
            $slot = 28;
            $index = undefined;
            continue;
          }
          case 'mailMerge': {
            mailMerge = readCT_MailMerge(cur, ctx);
            $slot = 29;
            $index = undefined;
            continue;
          }
          case 'revisionView': {
            revisionView = readCT_TrackChangesView(cur, ctx);
            $slot = 30;
            $index = undefined;
            continue;
          }
          case 'trackRevisions': {
            trackRevisions = readCT_OnOff(cur, ctx);
            $slot = 31;
            $index = undefined;
            continue;
          }
          case 'doNotTrackMoves': {
            doNotTrackMoves = readCT_OnOff(cur, ctx);
            $slot = 32;
            $index = undefined;
            continue;
          }
          case 'doNotTrackFormatting': {
            doNotTrackFormatting = readCT_OnOff(cur, ctx);
            $slot = 33;
            $index = undefined;
            continue;
          }
          case 'documentProtection': {
            documentProtection = readCT_DocProtect(cur, ctx);
            $slot = 34;
            $index = undefined;
            continue;
          }
          case 'autoFormatOverride': {
            autoFormatOverride = readCT_OnOff(cur, ctx);
            $slot = 35;
            $index = undefined;
            continue;
          }
          case 'styleLockTheme': {
            styleLockTheme = readCT_OnOff(cur, ctx);
            $slot = 36;
            $index = undefined;
            continue;
          }
          case 'styleLockQFSet': {
            styleLockQFSet = readCT_OnOff(cur, ctx);
            $slot = 37;
            $index = undefined;
            continue;
          }
          case 'defaultTabStop': {
            defaultTabStop = readCT_TwipsMeasure(cur, ctx);
            $slot = 38;
            $index = undefined;
            continue;
          }
          case 'autoHyphenation': {
            autoHyphenation = readCT_OnOff(cur, ctx);
            $slot = 39;
            $index = undefined;
            continue;
          }
          case 'consecutiveHyphenLimit': {
            consecutiveHyphenLimit = readCT_DecimalNumber(cur, ctx);
            $slot = 40;
            $index = undefined;
            continue;
          }
          case 'hyphenationZone': {
            hyphenationZone = readCT_TwipsMeasure(cur, ctx);
            $slot = 41;
            $index = undefined;
            continue;
          }
          case 'doNotHyphenateCaps': {
            doNotHyphenateCaps = readCT_OnOff(cur, ctx);
            $slot = 42;
            $index = undefined;
            continue;
          }
          case 'showEnvelope': {
            showEnvelope = readCT_OnOff(cur, ctx);
            $slot = 43;
            $index = undefined;
            continue;
          }
          case 'summaryLength': {
            summaryLength = readCT_DecimalNumberOrPrecent(cur, ctx);
            $slot = 44;
            $index = undefined;
            continue;
          }
          case 'clickAndTypeStyle': {
            clickAndTypeStyle = readCT_String(cur, ctx);
            $slot = 45;
            $index = undefined;
            continue;
          }
          case 'defaultTableStyle': {
            defaultTableStyle = readCT_String(cur, ctx);
            $slot = 46;
            $index = undefined;
            continue;
          }
          case 'evenAndOddHeaders': {
            evenAndOddHeaders = readCT_OnOff(cur, ctx);
            $slot = 47;
            $index = undefined;
            continue;
          }
          case 'bookFoldRevPrinting': {
            bookFoldRevPrinting = readCT_OnOff(cur, ctx);
            $slot = 48;
            $index = undefined;
            continue;
          }
          case 'bookFoldPrinting': {
            bookFoldPrinting = readCT_OnOff(cur, ctx);
            $slot = 49;
            $index = undefined;
            continue;
          }
          case 'bookFoldPrintingSheets': {
            bookFoldPrintingSheets = readCT_DecimalNumber(cur, ctx);
            $slot = 50;
            $index = undefined;
            continue;
          }
          case 'drawingGridHorizontalSpacing': {
            drawingGridHorizontalSpacing = readCT_TwipsMeasure(cur, ctx);
            $slot = 51;
            $index = undefined;
            continue;
          }
          case 'drawingGridVerticalSpacing': {
            drawingGridVerticalSpacing = readCT_TwipsMeasure(cur, ctx);
            $slot = 52;
            $index = undefined;
            continue;
          }
          case 'displayHorizontalDrawingGridEvery': {
            displayHorizontalDrawingGridEvery = readCT_DecimalNumber(cur, ctx);
            $slot = 53;
            $index = undefined;
            continue;
          }
          case 'displayVerticalDrawingGridEvery': {
            displayVerticalDrawingGridEvery = readCT_DecimalNumber(cur, ctx);
            $slot = 54;
            $index = undefined;
            continue;
          }
          case 'doNotUseMarginsForDrawingGridOrigin': {
            doNotUseMarginsForDrawingGridOrigin = readCT_OnOff(cur, ctx);
            $slot = 55;
            $index = undefined;
            continue;
          }
          case 'drawingGridHorizontalOrigin': {
            drawingGridHorizontalOrigin = readCT_TwipsMeasure(cur, ctx);
            $slot = 56;
            $index = undefined;
            continue;
          }
          case 'drawingGridVerticalOrigin': {
            drawingGridVerticalOrigin = readCT_TwipsMeasure(cur, ctx);
            $slot = 57;
            $index = undefined;
            continue;
          }
          case 'doNotShadeFormData': {
            doNotShadeFormData = readCT_OnOff(cur, ctx);
            $slot = 58;
            $index = undefined;
            continue;
          }
          case 'noPunctuationKerning': {
            noPunctuationKerning = readCT_OnOff(cur, ctx);
            $slot = 59;
            $index = undefined;
            continue;
          }
          case 'characterSpacingControl': {
            characterSpacingControl = readCT_CharacterSpacing(cur, ctx);
            $slot = 60;
            $index = undefined;
            continue;
          }
          case 'printTwoOnOne': {
            printTwoOnOne = readCT_OnOff(cur, ctx);
            $slot = 61;
            $index = undefined;
            continue;
          }
          case 'strictFirstAndLastChars': {
            strictFirstAndLastChars = readCT_OnOff(cur, ctx);
            $slot = 62;
            $index = undefined;
            continue;
          }
          case 'noLineBreaksAfter': {
            noLineBreaksAfter = readCT_Kinsoku(cur, ctx);
            $slot = 63;
            $index = undefined;
            continue;
          }
          case 'noLineBreaksBefore': {
            noLineBreaksBefore = readCT_Kinsoku(cur, ctx);
            $slot = 64;
            $index = undefined;
            continue;
          }
          case 'savePreviewPicture': {
            savePreviewPicture = readCT_OnOff(cur, ctx);
            $slot = 65;
            $index = undefined;
            continue;
          }
          case 'doNotValidateAgainstSchema': {
            doNotValidateAgainstSchema = readCT_OnOff(cur, ctx);
            $slot = 66;
            $index = undefined;
            continue;
          }
          case 'saveInvalidXml': {
            saveInvalidXml = readCT_OnOff(cur, ctx);
            $slot = 67;
            $index = undefined;
            continue;
          }
          case 'ignoreMixedContent': {
            ignoreMixedContent = readCT_OnOff(cur, ctx);
            $slot = 68;
            $index = undefined;
            continue;
          }
          case 'alwaysShowPlaceholderText': {
            alwaysShowPlaceholderText = readCT_OnOff(cur, ctx);
            $slot = 69;
            $index = undefined;
            continue;
          }
          case 'doNotDemarcateInvalidXml': {
            doNotDemarcateInvalidXml = readCT_OnOff(cur, ctx);
            $slot = 70;
            $index = undefined;
            continue;
          }
          case 'saveXmlDataOnly': {
            saveXmlDataOnly = readCT_OnOff(cur, ctx);
            $slot = 71;
            $index = undefined;
            continue;
          }
          case 'useXSLTWhenSaving': {
            useXSLTWhenSaving = readCT_OnOff(cur, ctx);
            $slot = 72;
            $index = undefined;
            continue;
          }
          case 'saveThroughXslt': {
            saveThroughXslt = readCT_SaveThroughXslt(cur, ctx);
            $slot = 73;
            $index = undefined;
            continue;
          }
          case 'showXMLTags': {
            showXMLTags = readCT_OnOff(cur, ctx);
            $slot = 74;
            $index = undefined;
            continue;
          }
          case 'alwaysMergeEmptyNamespace': {
            alwaysMergeEmptyNamespace = readCT_OnOff(cur, ctx);
            $slot = 75;
            $index = undefined;
            continue;
          }
          case 'updateFields': {
            updateFields = readCT_OnOff(cur, ctx);
            $slot = 76;
            $index = undefined;
            continue;
          }
          case 'hdrShapeDefaults': {
            hdrShapeDefaults = readCT_ShapeDefaults(cur, ctx);
            $slot = 77;
            $index = undefined;
            continue;
          }
          case 'footnotePr': {
            footnotePr = readCT_FtnDocProps(cur, ctx);
            $slot = 78;
            $index = undefined;
            continue;
          }
          case 'endnotePr': {
            endnotePr = readCT_EdnDocProps(cur, ctx);
            $slot = 79;
            $index = undefined;
            continue;
          }
          case 'compat': {
            compat = readCT_Compat(cur, ctx);
            $slot = 80;
            $index = undefined;
            continue;
          }
          case 'docVars': {
            docVars = readCT_DocVars(cur, ctx);
            $slot = 81;
            $index = undefined;
            continue;
          }
          case 'rsids': {
            rsids = readCT_DocRsids(cur, ctx);
            $slot = 82;
            $index = undefined;
            continue;
          }
          case 'attachedSchema': {
            attachedSchema.push(readCT_String(cur, ctx));
            $slot = 84;
            $index = attachedSchema.length - 1;
            continue;
          }
          case 'themeFontLang': {
            themeFontLang = readCT_Language(cur, ctx);
            $slot = 85;
            $index = undefined;
            continue;
          }
          case 'clrSchemeMapping': {
            clrSchemeMapping = readCT_ColorSchemeMapping(cur, ctx);
            $slot = 86;
            $index = undefined;
            continue;
          }
          case 'doNotIncludeSubdocsInStats': {
            doNotIncludeSubdocsInStats = readCT_OnOff(cur, ctx);
            $slot = 87;
            $index = undefined;
            continue;
          }
          case 'doNotAutoCompressPictures': {
            doNotAutoCompressPictures = readCT_OnOff(cur, ctx);
            $slot = 88;
            $index = undefined;
            continue;
          }
          case 'forceUpgrade': {
            forceUpgrade = readCT_Empty(cur, ctx);
            $slot = 89;
            $index = undefined;
            continue;
          }
          case 'captions': {
            captions = readCT_Captions(cur, ctx);
            $slot = 90;
            $index = undefined;
            continue;
          }
          case 'readModeInkLockDown': {
            readModeInkLockDown = readCT_ReadingModeInkLockDown(cur, ctx);
            $slot = 91;
            $index = undefined;
            continue;
          }
          case 'smartTagType': {
            smartTagType.push(readCT_SmartTagType(cur, ctx));
            $slot = 92;
            $index = smartTagType.length - 1;
            continue;
          }
          case 'shapeDefaults': {
            shapeDefaults = readCT_ShapeDefaults(cur, ctx);
            $slot = 94;
            $index = undefined;
            continue;
          }
          case 'doNotEmbedSmartTags': {
            doNotEmbedSmartTags = readCT_OnOff(cur, ctx);
            $slot = 95;
            $index = undefined;
            continue;
          }
          case 'decimalSymbol': {
            decimalSymbol = readCT_String(cur, ctx);
            $slot = 96;
            $index = undefined;
            continue;
          }
          case 'listSeparator': {
            listSeparator = readCT_String(cur, ctx);
            $slot = 97;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Settings', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Settings', cur);
    cur.next();
  }

  return {
    writeProtection,
    view,
    zoom,
    removePersonalInformation,
    removeDateAndTime,
    doNotDisplayPageBoundaries,
    displayBackgroundShape,
    printPostScriptOverText,
    printFractionalCharacterWidth,
    printFormsData,
    embedTrueTypeFonts,
    embedSystemFonts,
    saveSubsetFonts,
    saveFormsData,
    mirrorMargins,
    alignBordersAndEdges,
    bordersDoNotSurroundHeader,
    bordersDoNotSurroundFooter,
    gutterAtTop,
    hideSpellingErrors,
    hideGrammaticalErrors,
    activeWritingStyle,
    proofState,
    formsDesign,
    attachedTemplate,
    linkStyles,
    stylePaneFormatFilter,
    stylePaneSortMethod,
    documentType,
    mailMerge,
    revisionView,
    trackRevisions,
    doNotTrackMoves,
    doNotTrackFormatting,
    documentProtection,
    autoFormatOverride,
    styleLockTheme,
    styleLockQFSet,
    defaultTabStop,
    autoHyphenation,
    consecutiveHyphenLimit,
    hyphenationZone,
    doNotHyphenateCaps,
    showEnvelope,
    summaryLength,
    clickAndTypeStyle,
    defaultTableStyle,
    evenAndOddHeaders,
    bookFoldRevPrinting,
    bookFoldPrinting,
    bookFoldPrintingSheets,
    drawingGridHorizontalSpacing,
    drawingGridVerticalSpacing,
    displayHorizontalDrawingGridEvery,
    displayVerticalDrawingGridEvery,
    doNotUseMarginsForDrawingGridOrigin,
    drawingGridHorizontalOrigin,
    drawingGridVerticalOrigin,
    doNotShadeFormData,
    noPunctuationKerning,
    characterSpacingControl,
    printTwoOnOne,
    strictFirstAndLastChars,
    noLineBreaksAfter,
    noLineBreaksBefore,
    savePreviewPicture,
    doNotValidateAgainstSchema,
    saveInvalidXml,
    ignoreMixedContent,
    alwaysShowPlaceholderText,
    doNotDemarcateInvalidXml,
    saveXmlDataOnly,
    useXSLTWhenSaving,
    saveThroughXslt,
    showXMLTags,
    alwaysMergeEmptyNamespace,
    updateFields,
    hdrShapeDefaults,
    footnotePr,
    endnotePr,
    compat,
    docVars,
    rsids,
    mathPr,
    attachedSchema,
    themeFontLang,
    clrSchemeMapping,
    doNotIncludeSubdocsInStats,
    doNotAutoCompressPictures,
    forceUpgrade,
    captions,
    readModeInkLockDown,
    smartTagType,
    schemaLibrary,
    shapeDefaults,
    doNotEmbedSmartTags,
    decimalSymbol,
    listSeparator,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ShapeDefaults`. See `transitional/wml.xsd:3416`. */
export function readCT_ShapeDefaults(cur: XmlCursor, ctx: ReadContext): CT_ShapeDefaults {
  const start = requireStart(cur, 'CT_ShapeDefaults');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ShapeDefaults', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const any: RawNode[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ShapeDefaults', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === ctx.uris['vml-office']) {
        any.push(cur.skipToRaw());
        $slot = 0;
        $index = any.length - 1;
        continue;
      }
      ctx.unexpectedElement('CT_ShapeDefaults', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ShapeDefaults', cur);
    cur.next();
  }

  return {
    any,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Shd`. See `transitional/wml.xsd:467`. */
export function readCT_Shd(cur: XmlCursor, ctx: ReadContext): CT_Shd {
  const start = requireStart(cur, 'CT_Shd');
  let val: ST_Shd | undefined;
  let color: ST_HexColor | undefined;
  let themeColor: ST_ThemeColor | undefined;
  let themeTint: ST_UcharHexNumber | undefined;
  let themeShade: ST_UcharHexNumber | undefined;
  let fill: ST_HexColor | undefined;
  let themeFill: ST_ThemeColor | undefined;
  let themeFillTint: ST_UcharHexNumber | undefined;
  let themeFillShade: ST_UcharHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Shd(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Shd', a, 'ST_Shd', cur);
          break;
        }
        case 'color': {
          const v = parseST_HexColor(a.value);
          if (v !== undefined) {
            color = v;
            continue;
          }
          ctx.invalidValue('CT_Shd', a, 'ST_HexColor', cur);
          break;
        }
        case 'themeColor': {
          const v = parseST_ThemeColor(a.value);
          if (v !== undefined) {
            themeColor = v;
            continue;
          }
          ctx.invalidValue('CT_Shd', a, 'ST_ThemeColor', cur);
          break;
        }
        case 'themeTint': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeTint = v;
            continue;
          }
          ctx.invalidValue('CT_Shd', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'themeShade': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeShade = v;
            continue;
          }
          ctx.invalidValue('CT_Shd', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'fill': {
          const v = parseST_HexColor(a.value);
          if (v !== undefined) {
            fill = v;
            continue;
          }
          ctx.invalidValue('CT_Shd', a, 'ST_HexColor', cur);
          break;
        }
        case 'themeFill': {
          const v = parseST_ThemeColor(a.value);
          if (v !== undefined) {
            themeFill = v;
            continue;
          }
          ctx.invalidValue('CT_Shd', a, 'ST_ThemeColor', cur);
          break;
        }
        case 'themeFillTint': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeFillTint = v;
            continue;
          }
          ctx.invalidValue('CT_Shd', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'themeFillShade': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeFillShade = v;
            continue;
          }
          ctx.invalidValue('CT_Shd', a, 'ST_UcharHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Shd', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Shd', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Shd', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Shd', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Shd', cur);
    cur.next();
  }
  return {
    val,
    color,
    themeColor,
    themeTint,
    themeShade,
    fill,
    themeFill,
    themeFillTint,
    themeFillShade,
    $unknownAttrs,
  };
}

/** Read a `CT_SignedHpsMeasure`. See `transitional/wml.xsd:90`. */
export function readCT_SignedHpsMeasure(cur: XmlCursor, ctx: ReadContext): CT_SignedHpsMeasure {
  const start = requireStart(cur, 'CT_SignedHpsMeasure');
  let val: ST_SignedHpsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_SignedHpsMeasure(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_SignedHpsMeasure', a, 'ST_SignedHpsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SignedHpsMeasure', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_SignedHpsMeasure', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SignedHpsMeasure', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SignedHpsMeasure', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SignedHpsMeasure', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_SignedTwipsMeasure`. See `transitional/wml.xsd:72`. */
export function readCT_SignedTwipsMeasure(cur: XmlCursor, ctx: ReadContext): CT_SignedTwipsMeasure {
  const start = requireStart(cur, 'CT_SignedTwipsMeasure');
  let val: ST_SignedTwipsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_SignedTwipsMeasure', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SignedTwipsMeasure', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_SignedTwipsMeasure', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SignedTwipsMeasure', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SignedTwipsMeasure', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SignedTwipsMeasure', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_SimpleField`. See `transitional/wml.xsd:1231`. */
export function readCT_SimpleField(cur: XmlCursor, ctx: ReadContext): CT_SimpleField {
  const start = requireStart(cur, 'CT_SimpleField');
  let instr: ST_String | undefined;
  let fldLock: ST_OnOff | undefined;
  let dirty: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'instr': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            instr = v;
            continue;
          }
          ctx.invalidValue('CT_SimpleField', a, 'ST_String', cur);
          break;
        }
        case 'fldLock': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            fldLock = v;
            continue;
          }
          ctx.invalidValue('CT_SimpleField', a, 'ST_OnOff', cur);
          break;
        }
        case 'dirty': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            dirty = v;
            continue;
          }
          ctx.invalidValue('CT_SimpleField', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SimpleField', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (instr === undefined) ctx.missingRequired('CT_SimpleField', 'instr', cur);

  let fldData: CT_Text | undefined;
  const pContent: CT_SimpleField_PContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SimpleField', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            pContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            pContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'fldData': {
            fldData = readCT_Text(cur, ctx);
            continue;
          }
          case 'customXml': {
            pContent.push({ kind: 'customXml', value: readCT_CustomXmlRun(cur, ctx) });
            continue;
          }
          case 'smartTag': {
            pContent.push({ kind: 'smartTag', value: readCT_SmartTagRun(cur, ctx) });
            continue;
          }
          case 'sdt': {
            pContent.push({ kind: 'sdt', value: readCT_SdtRun(cur, ctx) });
            continue;
          }
          case 'dir': {
            pContent.push({ kind: 'dir', value: readCT_DirContentRun(cur, ctx) });
            continue;
          }
          case 'bdo': {
            pContent.push({ kind: 'bdo', value: readCT_BdoContentRun(cur, ctx) });
            continue;
          }
          case 'r': {
            pContent.push({ kind: 'r', value: readCT_R(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            pContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            pContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            pContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            pContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            pContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            pContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            pContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            pContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            pContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            pContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            pContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            pContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            pContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            pContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            pContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            pContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            pContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            pContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            pContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            pContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            pContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            pContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            pContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'fldSimple': {
            pContent.push({ kind: 'fldSimple', value: readCT_SimpleField(cur, ctx) });
            continue;
          }
          case 'hyperlink': {
            pContent.push({ kind: 'hyperlink', value: readCT_Hyperlink(cur, ctx) });
            continue;
          }
          case 'subDoc': {
            pContent.push({ kind: 'subDoc', value: readCT_Rel(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SimpleField', ev.localName, ev.uri, cur);
      pContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SimpleField', cur);
    cur.next();
  }

  return {
    fldData,
    pContent,
    instr,
    fldLock,
    dirty,
    $unknownAttrs,
  };
}

/** Read a `CT_SmartTagPr`. See `transitional/wml.xsd:2223`. */
export function readCT_SmartTagPr(cur: XmlCursor, ctx: ReadContext): CT_SmartTagPr {
  const start = requireStart(cur, 'CT_SmartTagPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SmartTagPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const attr: CT_Attr[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SmartTagPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'attr': {
            attr.push(readCT_Attr(cur, ctx));
            $slot = 0;
            $index = attr.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SmartTagPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SmartTagPr', cur);
    cur.next();
  }

  return {
    attr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SmartTagRun`. See `transitional/wml.xsd:2185`. */
export function readCT_SmartTagRun(cur: XmlCursor, ctx: ReadContext): CT_SmartTagRun {
  const start = requireStart(cur, 'CT_SmartTagRun');
  let uri: ST_String | undefined;
  let element: ST_XmlName | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'uri': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            uri = v;
            continue;
          }
          ctx.invalidValue('CT_SmartTagRun', a, 'ST_String', cur);
          break;
        }
        case 'element': {
          const v = parseST_XmlName(a.value);
          if (v !== undefined) {
            element = v;
            continue;
          }
          ctx.invalidValue('CT_SmartTagRun', a, 'ST_XmlName', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SmartTagRun', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (element === undefined) ctx.missingRequired('CT_SmartTagRun', 'element', cur);

  let smartTagPr: CT_SmartTagPr | undefined;
  const pContent: CT_SmartTagRun_PContent[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SmartTagRun', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            pContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            pContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'smartTagPr': {
            smartTagPr = readCT_SmartTagPr(cur, ctx);
            continue;
          }
          case 'customXml': {
            pContent.push({ kind: 'customXml', value: readCT_CustomXmlRun(cur, ctx) });
            continue;
          }
          case 'smartTag': {
            pContent.push({ kind: 'smartTag', value: readCT_SmartTagRun(cur, ctx) });
            continue;
          }
          case 'sdt': {
            pContent.push({ kind: 'sdt', value: readCT_SdtRun(cur, ctx) });
            continue;
          }
          case 'dir': {
            pContent.push({ kind: 'dir', value: readCT_DirContentRun(cur, ctx) });
            continue;
          }
          case 'bdo': {
            pContent.push({ kind: 'bdo', value: readCT_BdoContentRun(cur, ctx) });
            continue;
          }
          case 'r': {
            pContent.push({ kind: 'r', value: readCT_R(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            pContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            pContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            pContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            pContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            pContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            pContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            pContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            pContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            pContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            pContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            pContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            pContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            pContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            pContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            pContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            pContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            pContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            pContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            pContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            pContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            pContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            pContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            pContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'fldSimple': {
            pContent.push({ kind: 'fldSimple', value: readCT_SimpleField(cur, ctx) });
            continue;
          }
          case 'hyperlink': {
            pContent.push({ kind: 'hyperlink', value: readCT_Hyperlink(cur, ctx) });
            continue;
          }
          case 'subDoc': {
            pContent.push({ kind: 'subDoc', value: readCT_Rel(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SmartTagRun', ev.localName, ev.uri, cur);
      pContent.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SmartTagRun', cur);
    cur.next();
  }

  return {
    smartTagPr,
    pContent,
    uri,
    element,
    $unknownAttrs,
  };
}

/** Read a `CT_SmartTagType`. See `transitional/wml.xsd:3442`. */
export function readCT_SmartTagType(cur: XmlCursor, ctx: ReadContext): CT_SmartTagType {
  const start = requireStart(cur, 'CT_SmartTagType');
  let namespaceuri: ST_String | undefined;
  let name: ST_String | undefined;
  let url: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'namespaceuri': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            namespaceuri = v;
            continue;
          }
          ctx.invalidValue('CT_SmartTagType', a, 'ST_String', cur);
          break;
        }
        case 'name': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_SmartTagType', a, 'ST_String', cur);
          break;
        }
        case 'url': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            url = v;
            continue;
          }
          ctx.invalidValue('CT_SmartTagType', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SmartTagType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SmartTagType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SmartTagType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SmartTagType', cur);
    cur.next();
  }
  return {
    namespaceuri,
    name,
    url,
    $unknownAttrs,
  };
}

/** Read a `CT_Spacing`. See `transitional/wml.xsd:608`. */
export function readCT_Spacing(cur: XmlCursor, ctx: ReadContext): CT_Spacing {
  const start = requireStart(cur, 'CT_Spacing');
  let before: ST_TwipsMeasure | undefined;
  let beforeLines: ST_DecimalNumber | undefined;
  let beforeAutospacing: ST_OnOff | undefined;
  let after: ST_TwipsMeasure | undefined;
  let afterLines: ST_DecimalNumber | undefined;
  let afterAutospacing: ST_OnOff | undefined;
  let line: ST_SignedTwipsMeasure | undefined;
  let lineRule: ST_LineSpacingRule | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'before': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            before = v;
            continue;
          }
          ctx.invalidValue('CT_Spacing', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'beforeLines': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            beforeLines = v;
            continue;
          }
          ctx.invalidValue('CT_Spacing', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'beforeAutospacing': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            beforeAutospacing = v;
            continue;
          }
          ctx.invalidValue('CT_Spacing', a, 'ST_OnOff', cur);
          break;
        }
        case 'after': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            after = v;
            continue;
          }
          ctx.invalidValue('CT_Spacing', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'afterLines': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            afterLines = v;
            continue;
          }
          ctx.invalidValue('CT_Spacing', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'afterAutospacing': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            afterAutospacing = v;
            continue;
          }
          ctx.invalidValue('CT_Spacing', a, 'ST_OnOff', cur);
          break;
        }
        case 'line': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            line = v;
            continue;
          }
          ctx.invalidValue('CT_Spacing', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
        case 'lineRule': {
          const v = parseST_LineSpacingRule(a.value);
          if (v !== undefined) {
            lineRule = v;
            continue;
          }
          ctx.invalidValue('CT_Spacing', a, 'ST_LineSpacingRule', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Spacing', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Spacing', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Spacing', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Spacing', cur);
    cur.next();
  }
  return {
    before,
    beforeLines,
    beforeAutospacing,
    after,
    afterLines,
    afterAutospacing,
    line,
    lineRule,
    $unknownAttrs,
  };
}

/** Read a `CT_String`. See `transitional/wml.xsd:110`. */
export function readCT_String(cur: XmlCursor, ctx: ReadContext): CT_String {
  const start = requireStart(cur, 'CT_String');
  let val: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_String', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_String', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_String', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_String', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_String', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_String', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Style`. See `transitional/wml.xsd:3231`. */
export function readCT_Style(cur: XmlCursor, ctx: ReadContext): CT_Style {
  const start = requireStart(cur, 'CT_Style');
  let type: ST_StyleType | undefined;
  let styleId: ST_String | undefined;
  let default_: ST_OnOff | undefined;
  let customStyle: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'type': {
          const v = parseST_StyleType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_Style', a, 'ST_StyleType', cur);
          break;
        }
        case 'styleId': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            styleId = v;
            continue;
          }
          ctx.invalidValue('CT_Style', a, 'ST_String', cur);
          break;
        }
        case 'default': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            default_ = v;
            continue;
          }
          ctx.invalidValue('CT_Style', a, 'ST_OnOff', cur);
          break;
        }
        case 'customStyle': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            customStyle = v;
            continue;
          }
          ctx.invalidValue('CT_Style', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Style', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let name: CT_String | undefined;
  let aliases: CT_String | undefined;
  let basedOn: CT_String | undefined;
  let next: CT_String | undefined;
  let link: CT_String | undefined;
  let autoRedefine: CT_OnOff | undefined;
  let hidden: CT_OnOff | undefined;
  let uiPriority: CT_DecimalNumber | undefined;
  let semiHidden: CT_OnOff | undefined;
  let unhideWhenUsed: CT_OnOff | undefined;
  let qFormat: CT_OnOff | undefined;
  let locked: CT_OnOff | undefined;
  let personal: CT_OnOff | undefined;
  let personalCompose: CT_OnOff | undefined;
  let personalReply: CT_OnOff | undefined;
  let rsid: CT_LongHexNumber | undefined;
  let pPr: CT_PPrGeneral | undefined;
  let rPr: CT_RPr | undefined;
  let tblPr: CT_TblPrBase | undefined;
  let trPr: CT_TrPr | undefined;
  let tcPr: CT_TcPr | undefined;
  const tblStylePr: CT_TblStylePr[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Style', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'name': {
            name = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'aliases': {
            aliases = readCT_String(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'basedOn': {
            basedOn = readCT_String(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'next': {
            next = readCT_String(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'link': {
            link = readCT_String(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'autoRedefine': {
            autoRedefine = readCT_OnOff(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'hidden': {
            hidden = readCT_OnOff(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'uiPriority': {
            uiPriority = readCT_DecimalNumber(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'semiHidden': {
            semiHidden = readCT_OnOff(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'unhideWhenUsed': {
            unhideWhenUsed = readCT_OnOff(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'qFormat': {
            qFormat = readCT_OnOff(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'locked': {
            locked = readCT_OnOff(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'personal': {
            personal = readCT_OnOff(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'personalCompose': {
            personalCompose = readCT_OnOff(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'personalReply': {
            personalReply = readCT_OnOff(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'rsid': {
            rsid = readCT_LongHexNumber(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'pPr': {
            pPr = readCT_PPrGeneral(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'rPr': {
            rPr = readCT_RPr(cur, ctx);
            $slot = 17;
            $index = undefined;
            continue;
          }
          case 'tblPr': {
            tblPr = readCT_TblPrBase(cur, ctx);
            $slot = 18;
            $index = undefined;
            continue;
          }
          case 'trPr': {
            trPr = readCT_TrPr(cur, ctx);
            $slot = 19;
            $index = undefined;
            continue;
          }
          case 'tcPr': {
            tcPr = readCT_TcPr(cur, ctx);
            $slot = 20;
            $index = undefined;
            continue;
          }
          case 'tblStylePr': {
            tblStylePr.push(readCT_TblStylePr(cur, ctx));
            $slot = 21;
            $index = tblStylePr.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Style', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Style', cur);
    cur.next();
  }

  return {
    name,
    aliases,
    basedOn,
    next,
    link,
    autoRedefine,
    hidden,
    uiPriority,
    semiHidden,
    unhideWhenUsed,
    qFormat,
    locked,
    personal,
    personalCompose,
    personalReply,
    rsid,
    pPr,
    rPr,
    tblPr,
    trPr,
    tcPr,
    tblStylePr,
    type,
    styleId,
    default: default_,
    customStyle,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_StylePaneFilter`. See `transitional/wml.xsd:2995`. */
export function readCT_StylePaneFilter(cur: XmlCursor, ctx: ReadContext): CT_StylePaneFilter {
  const start = requireStart(cur, 'CT_StylePaneFilter');
  let allStyles: ST_OnOff | undefined;
  let customStyles: ST_OnOff | undefined;
  let latentStyles: ST_OnOff | undefined;
  let stylesInUse: ST_OnOff | undefined;
  let headingStyles: ST_OnOff | undefined;
  let numberingStyles: ST_OnOff | undefined;
  let tableStyles: ST_OnOff | undefined;
  let directFormattingOnRuns: ST_OnOff | undefined;
  let directFormattingOnParagraphs: ST_OnOff | undefined;
  let directFormattingOnNumbering: ST_OnOff | undefined;
  let directFormattingOnTables: ST_OnOff | undefined;
  let clearFormatting: ST_OnOff | undefined;
  let top3HeadingStyles: ST_OnOff | undefined;
  let visibleStyles: ST_OnOff | undefined;
  let alternateStyleNames: ST_OnOff | undefined;
  let val: ST_ShortHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'allStyles': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            allStyles = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'customStyles': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            customStyles = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'latentStyles': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            latentStyles = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'stylesInUse': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            stylesInUse = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'headingStyles': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            headingStyles = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'numberingStyles': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            numberingStyles = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'tableStyles': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            tableStyles = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'directFormattingOnRuns': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            directFormattingOnRuns = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'directFormattingOnParagraphs': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            directFormattingOnParagraphs = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'directFormattingOnNumbering': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            directFormattingOnNumbering = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'directFormattingOnTables': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            directFormattingOnTables = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'clearFormatting': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            clearFormatting = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'top3HeadingStyles': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            top3HeadingStyles = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'visibleStyles': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            visibleStyles = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'alternateStyleNames': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            alternateStyleNames = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_OnOff', cur);
          break;
        }
        case 'val': {
          const v = parseST_ShortHexNumber(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_StylePaneFilter', a, 'ST_ShortHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_StylePaneFilter', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StylePaneFilter', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_StylePaneFilter', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StylePaneFilter', cur);
    cur.next();
  }
  return {
    allStyles,
    customStyles,
    latentStyles,
    stylesInUse,
    headingStyles,
    numberingStyles,
    tableStyles,
    directFormattingOnRuns,
    directFormattingOnParagraphs,
    directFormattingOnNumbering,
    directFormattingOnTables,
    clearFormatting,
    top3HeadingStyles,
    visibleStyles,
    alternateStyleNames,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_StyleSort`. See `transitional/wml.xsd:2992`. */
export function readCT_StyleSort(cur: XmlCursor, ctx: ReadContext): CT_StyleSort {
  const start = requireStart(cur, 'CT_StyleSort');
  let val: ST_StyleSort | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_StyleSort(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_StyleSort', a, 'ST_StyleSort', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_StyleSort', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_StyleSort', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StyleSort', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_StyleSort', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StyleSort', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Styles`. See `transitional/wml.xsd:3280`. */
export function readCT_Styles(cur: XmlCursor, ctx: ReadContext): CT_Styles {
  const start = requireStart(cur, 'CT_Styles');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Styles', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let docDefaults: CT_DocDefaults | undefined;
  let latentStyles: CT_LatentStyles | undefined;
  const style: CT_Style[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Styles', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'docDefaults': {
            docDefaults = readCT_DocDefaults(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'latentStyles': {
            latentStyles = readCT_LatentStyles(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'style': {
            style.push(readCT_Style(cur, ctx));
            $slot = 2;
            $index = style.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Styles', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Styles', cur);
    cur.next();
  }

  return {
    docDefaults,
    latentStyles,
    style,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Sym`. See `transitional/wml.xsd:1696`. */
export function readCT_Sym(cur: XmlCursor, ctx: ReadContext): CT_Sym {
  const start = requireStart(cur, 'CT_Sym');
  let font: ST_String | undefined;
  let char: ST_ShortHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'font': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            font = v;
            continue;
          }
          ctx.invalidValue('CT_Sym', a, 'ST_String', cur);
          break;
        }
        case 'char': {
          const v = parseST_ShortHexNumber(a.value);
          if (v !== undefined) {
            char = v;
            continue;
          }
          ctx.invalidValue('CT_Sym', a, 'ST_ShortHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Sym', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Sym', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Sym', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Sym', cur);
    cur.next();
  }
  return {
    font,
    char,
    $unknownAttrs,
  };
}

/** Read a `CT_TabStop`. See `transitional/wml.xsd:596`. */
export function readCT_TabStop(cur: XmlCursor, ctx: ReadContext): CT_TabStop {
  const start = requireStart(cur, 'CT_TabStop');
  let val: ST_TabJc | undefined;
  let leader: ST_TabTlc | undefined;
  let pos: ST_SignedTwipsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TabJc(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TabStop', a, 'ST_TabJc', cur);
          break;
        }
        case 'leader': {
          const v = parseST_TabTlc(a.value);
          if (v !== undefined) {
            leader = v;
            continue;
          }
          ctx.invalidValue('CT_TabStop', a, 'ST_TabTlc', cur);
          break;
        }
        case 'pos': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            pos = v;
            continue;
          }
          ctx.invalidValue('CT_TabStop', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TabStop', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TabStop', 'val', cur);
  if (pos === undefined) ctx.missingRequired('CT_TabStop', 'pos', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TabStop', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TabStop', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TabStop', cur);
    cur.next();
  }
  return {
    val,
    leader,
    pos,
    $unknownAttrs,
  };
}

/** Read a `CT_Tabs`. See `transitional/wml.xsd:1070`. */
export function readCT_Tabs(cur: XmlCursor, ctx: ReadContext): CT_Tabs {
  const start = requireStart(cur, 'CT_Tabs');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Tabs', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const tab: CT_TabStop[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Tabs', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tab': {
            tab.push(readCT_TabStop(cur, ctx));
            $slot = 0;
            $index = tab.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Tabs', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Tabs', cur);
    cur.next();
  }

  return {
    tab,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TargetScreenSz`. See `transitional/wml.xsd:2724`. */
export function readCT_TargetScreenSz(cur: XmlCursor, ctx: ReadContext): CT_TargetScreenSz {
  const start = requireStart(cur, 'CT_TargetScreenSz');
  let val: ST_TargetScreenSz | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TargetScreenSz(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TargetScreenSz', a, 'ST_TargetScreenSz', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TargetScreenSz', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TargetScreenSz', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TargetScreenSz', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TargetScreenSz', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TargetScreenSz', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Tbl`. See `transitional/wml.xsd:2530`. */
export function readCT_Tbl(cur: XmlCursor, ctx: ReadContext): CT_Tbl {
  const start = requireStart(cur, 'CT_Tbl');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Tbl', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const rangeMarkupElements: CT_Tbl_RangeMarkupElements[] = [];
  let tblPr: CT_TblPr | undefined;
  let tblGrid: CT_TblGrid | undefined;
  const contentRowContent: CT_Tbl_ContentRowContent[] = [];
  let $slot = -1;
  let $index: number | undefined;

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Tbl', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            contentRowContent.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'oMath': {
            contentRowContent.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'bookmarkStart': {
            rangeMarkupElements.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'bookmarkEnd': {
            rangeMarkupElements.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'moveFromRangeStart': {
            rangeMarkupElements.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'moveFromRangeEnd': {
            rangeMarkupElements.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'moveToRangeStart': {
            rangeMarkupElements.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'moveToRangeEnd': {
            rangeMarkupElements.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'commentRangeStart': {
            rangeMarkupElements.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'commentRangeEnd': {
            rangeMarkupElements.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'customXmlInsRangeStart': {
            rangeMarkupElements.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'customXmlInsRangeEnd': {
            rangeMarkupElements.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'customXmlDelRangeStart': {
            rangeMarkupElements.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'customXmlDelRangeEnd': {
            rangeMarkupElements.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            rangeMarkupElements.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            rangeMarkupElements.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            rangeMarkupElements.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            rangeMarkupElements.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            $slot = 0;
            $index = rangeMarkupElements.length - 1;
            continue;
          }
          case 'tblPr': {
            tblPr = readCT_TblPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tblGrid': {
            tblGrid = readCT_TblGrid(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'tr': {
            contentRowContent.push({ kind: 'tr', value: readCT_Row(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'customXml': {
            contentRowContent.push({ kind: 'customXml', value: readCT_CustomXmlRow(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'sdt': {
            contentRowContent.push({ kind: 'sdt', value: readCT_SdtRow(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'proofErr': {
            contentRowContent.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'permStart': {
            contentRowContent.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'permEnd': {
            contentRowContent.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'bookmarkStart': {
            contentRowContent.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'bookmarkEnd': {
            contentRowContent.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'moveFromRangeStart': {
            contentRowContent.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'moveFromRangeEnd': {
            contentRowContent.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'moveToRangeStart': {
            contentRowContent.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'moveToRangeEnd': {
            contentRowContent.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'commentRangeStart': {
            contentRowContent.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'commentRangeEnd': {
            contentRowContent.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'customXmlInsRangeStart': {
            contentRowContent.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'customXmlInsRangeEnd': {
            contentRowContent.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'customXmlDelRangeStart': {
            contentRowContent.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'customXmlDelRangeEnd': {
            contentRowContent.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            contentRowContent.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            contentRowContent.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            contentRowContent.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            contentRowContent.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'ins': {
            contentRowContent.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'del': {
            contentRowContent.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'moveFrom': {
            contentRowContent.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
          case 'moveTo': {
            contentRowContent.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            $slot = 3;
            $index = contentRowContent.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Tbl', ev.localName, ev.uri, cur);
      const $raw = cur.skipToRaw();
      if ($slot <= 0) rangeMarkupElements.push({ kind: '$raw', value: $raw });
      else contentRowContent.push({ kind: '$raw', value: $raw });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Tbl', cur);
    cur.next();
  }

  return {
    rangeMarkupElements,
    tblPr,
    tblGrid,
    contentRowContent,
    $unknownAttrs,
  };
}

/** Read a `CT_TblBorders`. See `transitional/wml.xsd:2466`. */
export function readCT_TblBorders(cur: XmlCursor, ctx: ReadContext): CT_TblBorders {
  const start = requireStart(cur, 'CT_TblBorders');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TblBorders', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let top: CT_Border | undefined;
  let start_: CT_Border | undefined;
  let left: CT_Border | undefined;
  let bottom: CT_Border | undefined;
  let end: CT_Border | undefined;
  let right: CT_Border | undefined;
  let insideH: CT_Border | undefined;
  let insideV: CT_Border | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblBorders', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'top': {
            top = readCT_Border(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'start': {
            start_ = readCT_Border(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'left': {
            left = readCT_Border(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'bottom': {
            bottom = readCT_Border(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'end': {
            end = readCT_Border(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'right': {
            right = readCT_Border(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'insideH': {
            insideH = readCT_Border(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'insideV': {
            insideV = readCT_Border(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TblBorders', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblBorders', cur);
    cur.next();
  }

  return {
    top,
    start: start_,
    left,
    bottom,
    end,
    right,
    insideH,
    insideV,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TblCellMar`. See `transitional/wml.xsd:2456`. */
export function readCT_TblCellMar(cur: XmlCursor, ctx: ReadContext): CT_TblCellMar {
  const start = requireStart(cur, 'CT_TblCellMar');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TblCellMar', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let top: CT_TblWidth | undefined;
  let start_: CT_TblWidth | undefined;
  let left: CT_TblWidth | undefined;
  let bottom: CT_TblWidth | undefined;
  let end: CT_TblWidth | undefined;
  let right: CT_TblWidth | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblCellMar', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'top': {
            top = readCT_TblWidth(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'start': {
            start_ = readCT_TblWidth(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'left': {
            left = readCT_TblWidth(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'bottom': {
            bottom = readCT_TblWidth(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'end': {
            end = readCT_TblWidth(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'right': {
            right = readCT_TblWidth(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TblCellMar', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblCellMar', cur);
    cur.next();
  }

  return {
    top,
    start: start_,
    left,
    bottom,
    end,
    right,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TblGrid`. See `transitional/wml.xsd:2274`. */
export function readCT_TblGrid(cur: XmlCursor, ctx: ReadContext): CT_TblGrid {
  const start = requireStart(cur, 'CT_TblGrid');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TblGrid', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const gridCol: CT_TblGridCol[] = [];
  let tblGridChange: CT_TblGridChange | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblGrid', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'gridCol': {
            gridCol.push(readCT_TblGridCol(cur, ctx));
            $slot = 0;
            $index = gridCol.length - 1;
            continue;
          }
          case 'tblGridChange': {
            tblGridChange = readCT_TblGridChange(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TblGrid', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblGrid', cur);
    cur.next();
  }

  return {
    gridCol,
    tblGridChange,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TblGridBase`. See `transitional/wml.xsd:2269`. */
export function readCT_TblGridBase(cur: XmlCursor, ctx: ReadContext): CT_TblGridBase {
  const start = requireStart(cur, 'CT_TblGridBase');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TblGridBase', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const gridCol: CT_TblGridCol[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblGridBase', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'gridCol': {
            gridCol.push(readCT_TblGridCol(cur, ctx));
            $slot = 0;
            $index = gridCol.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TblGridBase', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblGridBase', cur);
    cur.next();
  }

  return {
    gridCol,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TblGridChange`. See `transitional/wml.xsd:941`. */
export function readCT_TblGridChange(cur: XmlCursor, ctx: ReadContext): CT_TblGridChange {
  const start = requireStart(cur, 'CT_TblGridChange');
  let id: ST_DecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TblGridChange', a, 'ST_DecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TblGridChange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_TblGridChange', 'id', cur);

  let tblGrid: CT_TblGridBase | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblGridChange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tblGrid': {
            tblGrid = readCT_TblGridBase(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TblGridChange', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblGridChange', cur);
    cur.next();
  }

  return {
    tblGrid,
    id,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TblGridCol`. See `transitional/wml.xsd:2266`. */
export function readCT_TblGridCol(cur: XmlCursor, ctx: ReadContext): CT_TblGridCol {
  const start = requireStart(cur, 'CT_TblGridCol');
  let w: ST_TwipsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'w': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            w = v;
            continue;
          }
          ctx.invalidValue('CT_TblGridCol', a, 'ST_TwipsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TblGridCol', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblGridCol', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TblGridCol', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblGridCol', cur);
    cur.next();
  }
  return {
    w,
    $unknownAttrs,
  };
}

/** Read a `CT_TblLayoutType`. See `transitional/wml.xsd:2432`. */
export function readCT_TblLayoutType(cur: XmlCursor, ctx: ReadContext): CT_TblLayoutType {
  const start = requireStart(cur, 'CT_TblLayoutType');
  let type: ST_TblLayoutType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'type': {
          const v = parseST_TblLayoutType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_TblLayoutType', a, 'ST_TblLayoutType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TblLayoutType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblLayoutType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TblLayoutType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblLayoutType', cur);
    cur.next();
  }
  return {
    type,
    $unknownAttrs,
  };
}

/** Read a `CT_TblLook`. See `transitional/wml.xsd:2538`. */
export function readCT_TblLook(cur: XmlCursor, ctx: ReadContext): CT_TblLook {
  const start = requireStart(cur, 'CT_TblLook');
  let firstRow: ST_OnOff | undefined;
  let lastRow: ST_OnOff | undefined;
  let firstColumn: ST_OnOff | undefined;
  let lastColumn: ST_OnOff | undefined;
  let noHBand: ST_OnOff | undefined;
  let noVBand: ST_OnOff | undefined;
  let val: ST_ShortHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'firstRow': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            firstRow = v;
            continue;
          }
          ctx.invalidValue('CT_TblLook', a, 'ST_OnOff', cur);
          break;
        }
        case 'lastRow': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            lastRow = v;
            continue;
          }
          ctx.invalidValue('CT_TblLook', a, 'ST_OnOff', cur);
          break;
        }
        case 'firstColumn': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            firstColumn = v;
            continue;
          }
          ctx.invalidValue('CT_TblLook', a, 'ST_OnOff', cur);
          break;
        }
        case 'lastColumn': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            lastColumn = v;
            continue;
          }
          ctx.invalidValue('CT_TblLook', a, 'ST_OnOff', cur);
          break;
        }
        case 'noHBand': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            noHBand = v;
            continue;
          }
          ctx.invalidValue('CT_TblLook', a, 'ST_OnOff', cur);
          break;
        }
        case 'noVBand': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            noVBand = v;
            continue;
          }
          ctx.invalidValue('CT_TblLook', a, 'ST_OnOff', cur);
          break;
        }
        case 'val': {
          const v = parseST_ShortHexNumber(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TblLook', a, 'ST_ShortHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TblLook', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblLook', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TblLook', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblLook', cur);
    cur.next();
  }
  return {
    firstRow,
    lastRow,
    firstColumn,
    lastColumn,
    noHBand,
    noVBand,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TblOverlap`. See `transitional/wml.xsd:2441`. */
export function readCT_TblOverlap(cur: XmlCursor, ctx: ReadContext): CT_TblOverlap {
  const start = requireStart(cur, 'CT_TblOverlap');
  let val: ST_TblOverlap | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TblOverlap(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TblOverlap', a, 'ST_TblOverlap', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TblOverlap', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TblOverlap', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblOverlap', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TblOverlap', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblOverlap', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TblPPr`. See `transitional/wml.xsd:2444`. */
export function readCT_TblPPr(cur: XmlCursor, ctx: ReadContext): CT_TblPPr {
  const start = requireStart(cur, 'CT_TblPPr');
  let leftFromText: ST_TwipsMeasure | undefined;
  let rightFromText: ST_TwipsMeasure | undefined;
  let topFromText: ST_TwipsMeasure | undefined;
  let bottomFromText: ST_TwipsMeasure | undefined;
  let vertAnchor: ST_VAnchor | undefined;
  let horzAnchor: ST_HAnchor | undefined;
  let tblpXSpec: ST_XAlign | undefined;
  let tblpX: ST_SignedTwipsMeasure | undefined;
  let tblpYSpec: ST_YAlign | undefined;
  let tblpY: ST_SignedTwipsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'leftFromText': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            leftFromText = v;
            continue;
          }
          ctx.invalidValue('CT_TblPPr', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'rightFromText': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            rightFromText = v;
            continue;
          }
          ctx.invalidValue('CT_TblPPr', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'topFromText': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            topFromText = v;
            continue;
          }
          ctx.invalidValue('CT_TblPPr', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'bottomFromText': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            bottomFromText = v;
            continue;
          }
          ctx.invalidValue('CT_TblPPr', a, 'ST_TwipsMeasure', cur);
          break;
        }
        case 'vertAnchor': {
          const v = parseST_VAnchor(a.value);
          if (v !== undefined) {
            vertAnchor = v;
            continue;
          }
          ctx.invalidValue('CT_TblPPr', a, 'ST_VAnchor', cur);
          break;
        }
        case 'horzAnchor': {
          const v = parseST_HAnchor(a.value);
          if (v !== undefined) {
            horzAnchor = v;
            continue;
          }
          ctx.invalidValue('CT_TblPPr', a, 'ST_HAnchor', cur);
          break;
        }
        case 'tblpXSpec': {
          const v = parseST_XAlign(a.value);
          if (v !== undefined) {
            tblpXSpec = v;
            continue;
          }
          ctx.invalidValue('CT_TblPPr', a, 'ST_XAlign', cur);
          break;
        }
        case 'tblpX': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            tblpX = v;
            continue;
          }
          ctx.invalidValue('CT_TblPPr', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
        case 'tblpYSpec': {
          const v = parseST_YAlign(a.value);
          if (v !== undefined) {
            tblpYSpec = v;
            continue;
          }
          ctx.invalidValue('CT_TblPPr', a, 'ST_YAlign', cur);
          break;
        }
        case 'tblpY': {
          const v = parseST_SignedTwipsMeasure(a.value);
          if (v !== undefined) {
            tblpY = v;
            continue;
          }
          ctx.invalidValue('CT_TblPPr', a, 'ST_SignedTwipsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TblPPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblPPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TblPPr', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblPPr', cur);
    cur.next();
  }
  return {
    leftFromText,
    rightFromText,
    topFromText,
    bottomFromText,
    vertAnchor,
    horzAnchor,
    tblpXSpec,
    tblpX,
    tblpYSpec,
    tblpY,
    $unknownAttrs,
  };
}

/** Read a `CT_TblPr`. See `transitional/wml.xsd:2499`. */
export function readCT_TblPr(cur: XmlCursor, ctx: ReadContext): CT_TblPr {
  const start = requireStart(cur, 'CT_TblPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TblPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let tblStyle: CT_String | undefined;
  let tblpPr: CT_TblPPr | undefined;
  let tblOverlap: CT_TblOverlap | undefined;
  let bidiVisual: CT_OnOff | undefined;
  let tblStyleRowBandSize: CT_DecimalNumber | undefined;
  let tblStyleColBandSize: CT_DecimalNumber | undefined;
  let tblW: CT_TblWidth | undefined;
  let jc: CT_JcTable | undefined;
  let tblCellSpacing: CT_TblWidth | undefined;
  let tblInd: CT_TblWidth | undefined;
  let tblBorders: CT_TblBorders | undefined;
  let shd: CT_Shd | undefined;
  let tblLayout: CT_TblLayoutType | undefined;
  let tblCellMar: CT_TblCellMar | undefined;
  let tblLook: CT_TblLook | undefined;
  let tblCaption: CT_String | undefined;
  let tblDescription: CT_String | undefined;
  let tblPrChange: CT_TblPrChange | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tblStyle': {
            tblStyle = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tblpPr': {
            tblpPr = readCT_TblPPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tblOverlap': {
            tblOverlap = readCT_TblOverlap(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'bidiVisual': {
            bidiVisual = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'tblStyleRowBandSize': {
            tblStyleRowBandSize = readCT_DecimalNumber(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'tblStyleColBandSize': {
            tblStyleColBandSize = readCT_DecimalNumber(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'tblW': {
            tblW = readCT_TblWidth(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'jc': {
            jc = readCT_JcTable(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'tblCellSpacing': {
            tblCellSpacing = readCT_TblWidth(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'tblInd': {
            tblInd = readCT_TblWidth(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tblBorders': {
            tblBorders = readCT_TblBorders(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'shd': {
            shd = readCT_Shd(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'tblLayout': {
            tblLayout = readCT_TblLayoutType(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'tblCellMar': {
            tblCellMar = readCT_TblCellMar(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'tblLook': {
            tblLook = readCT_TblLook(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'tblCaption': {
            tblCaption = readCT_String(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'tblDescription': {
            tblDescription = readCT_String(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'tblPrChange': {
            tblPrChange = readCT_TblPrChange(cur, ctx);
            $slot = 17;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TblPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblPr', cur);
    cur.next();
  }

  return {
    tblStyle,
    tblpPr,
    tblOverlap,
    bidiVisual,
    tblStyleRowBandSize,
    tblStyleColBandSize,
    tblW,
    jc,
    tblCellSpacing,
    tblInd,
    tblBorders,
    shd,
    tblLayout,
    tblCellMar,
    tblLook,
    tblCaption,
    tblDescription,
    tblPrChange,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TblPrBase`. See `transitional/wml.xsd:2478`. */
export function readCT_TblPrBase(cur: XmlCursor, ctx: ReadContext): CT_TblPrBase {
  const start = requireStart(cur, 'CT_TblPrBase');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TblPrBase', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let tblStyle: CT_String | undefined;
  let tblpPr: CT_TblPPr | undefined;
  let tblOverlap: CT_TblOverlap | undefined;
  let bidiVisual: CT_OnOff | undefined;
  let tblStyleRowBandSize: CT_DecimalNumber | undefined;
  let tblStyleColBandSize: CT_DecimalNumber | undefined;
  let tblW: CT_TblWidth | undefined;
  let jc: CT_JcTable | undefined;
  let tblCellSpacing: CT_TblWidth | undefined;
  let tblInd: CT_TblWidth | undefined;
  let tblBorders: CT_TblBorders | undefined;
  let shd: CT_Shd | undefined;
  let tblLayout: CT_TblLayoutType | undefined;
  let tblCellMar: CT_TblCellMar | undefined;
  let tblLook: CT_TblLook | undefined;
  let tblCaption: CT_String | undefined;
  let tblDescription: CT_String | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblPrBase', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tblStyle': {
            tblStyle = readCT_String(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tblpPr': {
            tblpPr = readCT_TblPPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tblOverlap': {
            tblOverlap = readCT_TblOverlap(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'bidiVisual': {
            bidiVisual = readCT_OnOff(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'tblStyleRowBandSize': {
            tblStyleRowBandSize = readCT_DecimalNumber(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'tblStyleColBandSize': {
            tblStyleColBandSize = readCT_DecimalNumber(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'tblW': {
            tblW = readCT_TblWidth(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'jc': {
            jc = readCT_JcTable(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'tblCellSpacing': {
            tblCellSpacing = readCT_TblWidth(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'tblInd': {
            tblInd = readCT_TblWidth(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tblBorders': {
            tblBorders = readCT_TblBorders(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'shd': {
            shd = readCT_Shd(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'tblLayout': {
            tblLayout = readCT_TblLayoutType(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'tblCellMar': {
            tblCellMar = readCT_TblCellMar(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'tblLook': {
            tblLook = readCT_TblLook(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'tblCaption': {
            tblCaption = readCT_String(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'tblDescription': {
            tblDescription = readCT_String(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TblPrBase', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblPrBase', cur);
    cur.next();
  }

  return {
    tblStyle,
    tblpPr,
    tblOverlap,
    bidiVisual,
    tblStyleRowBandSize,
    tblStyleColBandSize,
    tblW,
    jc,
    tblCellSpacing,
    tblInd,
    tblBorders,
    shd,
    tblLayout,
    tblCellMar,
    tblLook,
    tblCaption,
    tblDescription,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TblPrChange`. See `transitional/wml.xsd:950`. */
export function readCT_TblPrChange(cur: XmlCursor, ctx: ReadContext): CT_TblPrChange {
  const start = requireStart(cur, 'CT_TblPrChange');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TblPrChange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_TblPrChange', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_TblPrChange', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TblPrChange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_TblPrChange', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_TblPrChange', 'author', cur);

  let tblPr: CT_TblPrBase | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblPrChange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tblPr': {
            tblPr = readCT_TblPrBase(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TblPrChange', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblPrChange', cur);
    cur.next();
  }

  return {
    tblPr,
    id,
    author,
    date,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TblPrEx`. See `transitional/wml.xsd:2521`. */
export function readCT_TblPrEx(cur: XmlCursor, ctx: ReadContext): CT_TblPrEx {
  const start = requireStart(cur, 'CT_TblPrEx');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TblPrEx', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let tblW: CT_TblWidth | undefined;
  let jc: CT_JcTable | undefined;
  let tblCellSpacing: CT_TblWidth | undefined;
  let tblInd: CT_TblWidth | undefined;
  let tblBorders: CT_TblBorders | undefined;
  let shd: CT_Shd | undefined;
  let tblLayout: CT_TblLayoutType | undefined;
  let tblCellMar: CT_TblCellMar | undefined;
  let tblLook: CT_TblLook | undefined;
  let tblPrExChange: CT_TblPrExChange | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblPrEx', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tblW': {
            tblW = readCT_TblWidth(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'jc': {
            jc = readCT_JcTable(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tblCellSpacing': {
            tblCellSpacing = readCT_TblWidth(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'tblInd': {
            tblInd = readCT_TblWidth(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'tblBorders': {
            tblBorders = readCT_TblBorders(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'shd': {
            shd = readCT_Shd(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'tblLayout': {
            tblLayout = readCT_TblLayoutType(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'tblCellMar': {
            tblCellMar = readCT_TblCellMar(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'tblLook': {
            tblLook = readCT_TblLook(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'tblPrExChange': {
            tblPrExChange = readCT_TblPrExChange(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TblPrEx', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblPrEx', cur);
    cur.next();
  }

  return {
    tblW,
    jc,
    tblCellSpacing,
    tblInd,
    tblBorders,
    shd,
    tblLayout,
    tblCellMar,
    tblLook,
    tblPrExChange,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TblPrExBase`. See `transitional/wml.xsd:2508`. */
export function readCT_TblPrExBase(cur: XmlCursor, ctx: ReadContext): CT_TblPrExBase {
  const start = requireStart(cur, 'CT_TblPrExBase');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TblPrExBase', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let tblW: CT_TblWidth | undefined;
  let jc: CT_JcTable | undefined;
  let tblCellSpacing: CT_TblWidth | undefined;
  let tblInd: CT_TblWidth | undefined;
  let tblBorders: CT_TblBorders | undefined;
  let shd: CT_Shd | undefined;
  let tblLayout: CT_TblLayoutType | undefined;
  let tblCellMar: CT_TblCellMar | undefined;
  let tblLook: CT_TblLook | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblPrExBase', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tblW': {
            tblW = readCT_TblWidth(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'jc': {
            jc = readCT_JcTable(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tblCellSpacing': {
            tblCellSpacing = readCT_TblWidth(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'tblInd': {
            tblInd = readCT_TblWidth(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'tblBorders': {
            tblBorders = readCT_TblBorders(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'shd': {
            shd = readCT_Shd(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'tblLayout': {
            tblLayout = readCT_TblLayoutType(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'tblCellMar': {
            tblCellMar = readCT_TblCellMar(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'tblLook': {
            tblLook = readCT_TblLook(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TblPrExBase', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblPrExBase', cur);
    cur.next();
  }

  return {
    tblW,
    jc,
    tblCellSpacing,
    tblInd,
    tblBorders,
    shd,
    tblLayout,
    tblCellMar,
    tblLook,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TblPrExChange`. See `transitional/wml.xsd:914`. */
export function readCT_TblPrExChange(cur: XmlCursor, ctx: ReadContext): CT_TblPrExChange {
  const start = requireStart(cur, 'CT_TblPrExChange');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TblPrExChange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_TblPrExChange', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_TblPrExChange', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TblPrExChange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_TblPrExChange', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_TblPrExChange', 'author', cur);

  let tblPrEx: CT_TblPrExBase | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblPrExChange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tblPrEx': {
            tblPrEx = readCT_TblPrExBase(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TblPrExChange', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblPrExChange', cur);
    cur.next();
  }

  return {
    tblPrEx,
    id,
    author,
    date,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TblStylePr`. See `transitional/wml.xsd:3213`. */
export function readCT_TblStylePr(cur: XmlCursor, ctx: ReadContext): CT_TblStylePr {
  const start = requireStart(cur, 'CT_TblStylePr');
  let type: ST_TblStyleOverrideType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'type': {
          const v = parseST_TblStyleOverrideType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_TblStylePr', a, 'ST_TblStyleOverrideType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TblStylePr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (type === undefined) ctx.missingRequired('CT_TblStylePr', 'type', cur);

  let pPr: CT_PPrGeneral | undefined;
  let rPr: CT_RPr | undefined;
  let tblPr: CT_TblPrBase | undefined;
  let trPr: CT_TrPr | undefined;
  let tcPr: CT_TcPr | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblStylePr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'pPr': {
            pPr = readCT_PPrGeneral(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'rPr': {
            rPr = readCT_RPr(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tblPr': {
            tblPr = readCT_TblPrBase(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'trPr': {
            trPr = readCT_TrPr(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'tcPr': {
            tcPr = readCT_TcPr(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TblStylePr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblStylePr', cur);
    cur.next();
  }

  return {
    pPr,
    rPr,
    tblPr,
    trPr,
    tcPr,
    type,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TblWidth`. See `transitional/wml.xsd:2262`. */
export function readCT_TblWidth(cur: XmlCursor, ctx: ReadContext): CT_TblWidth {
  const start = requireStart(cur, 'CT_TblWidth');
  let w: ST_MeasurementOrPercent | undefined;
  let type: ST_TblWidth | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'w': {
          const v = parseST_MeasurementOrPercent(a.value);
          if (v !== undefined) {
            w = v;
            continue;
          }
          ctx.invalidValue('CT_TblWidth', a, 'ST_MeasurementOrPercent', cur);
          break;
        }
        case 'type': {
          const v = parseST_TblWidth(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_TblWidth', a, 'ST_TblWidth', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TblWidth', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TblWidth', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TblWidth', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TblWidth', cur);
    cur.next();
  }
  return {
    w,
    type,
    $unknownAttrs,
  };
}

/** Read a `CT_Tc`. See `transitional/wml.xsd:2355`. */
export function readCT_Tc(cur: XmlCursor, ctx: ReadContext): CT_Tc {
  const start = requireStart(cur, 'CT_Tc');
  let id: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Tc', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Tc', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let tcPr: CT_TcPr | undefined;
  const blockLevelElts: CT_Tc_BlockLevelElts[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Tc', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            blockLevelElts.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            blockLevelElts.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tcPr': {
            tcPr = readCT_TcPr(cur, ctx);
            continue;
          }
          case 'customXml': {
            blockLevelElts.push({ kind: 'customXml', value: readCT_CustomXmlBlock(cur, ctx) });
            continue;
          }
          case 'sdt': {
            blockLevelElts.push({ kind: 'sdt', value: readCT_SdtBlock(cur, ctx) });
            continue;
          }
          case 'p': {
            blockLevelElts.push({ kind: 'p', value: readCT_P(cur, ctx) });
            continue;
          }
          case 'tbl': {
            blockLevelElts.push({ kind: 'tbl', value: readCT_Tbl(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            blockLevelElts.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            blockLevelElts.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            blockLevelElts.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            blockLevelElts.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            blockLevelElts.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            blockLevelElts.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            blockLevelElts.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            blockLevelElts.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            blockLevelElts.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            blockLevelElts.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            blockLevelElts.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            blockLevelElts.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            blockLevelElts.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            blockLevelElts.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            blockLevelElts.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            blockLevelElts.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            blockLevelElts.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'altChunk': {
            blockLevelElts.push({ kind: 'altChunk', value: readCT_AltChunk(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Tc', ev.localName, ev.uri, cur);
      blockLevelElts.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Tc', cur);
    cur.next();
  }

  return {
    tcPr,
    blockLevelElts,
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_TcBorders`. See `transitional/wml.xsd:2283`. */
export function readCT_TcBorders(cur: XmlCursor, ctx: ReadContext): CT_TcBorders {
  const start = requireStart(cur, 'CT_TcBorders');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TcBorders', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let top: CT_Border | undefined;
  let start_: CT_Border | undefined;
  let left: CT_Border | undefined;
  let bottom: CT_Border | undefined;
  let end: CT_Border | undefined;
  let right: CT_Border | undefined;
  let insideH: CT_Border | undefined;
  let insideV: CT_Border | undefined;
  let tl2br: CT_Border | undefined;
  let tr2bl: CT_Border | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TcBorders', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'top': {
            top = readCT_Border(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'start': {
            start_ = readCT_Border(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'left': {
            left = readCT_Border(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'bottom': {
            bottom = readCT_Border(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'end': {
            end = readCT_Border(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'right': {
            right = readCT_Border(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'insideH': {
            insideH = readCT_Border(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'insideV': {
            insideV = readCT_Border(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'tl2br': {
            tl2br = readCT_Border(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'tr2bl': {
            tr2bl = readCT_Border(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TcBorders', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TcBorders', cur);
    cur.next();
  }

  return {
    top,
    start: start_,
    left,
    bottom,
    end,
    right,
    insideH,
    insideV,
    tl2br,
    tr2bl,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TcMar`. See `transitional/wml.xsd:2297`. */
export function readCT_TcMar(cur: XmlCursor, ctx: ReadContext): CT_TcMar {
  const start = requireStart(cur, 'CT_TcMar');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TcMar', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let top: CT_TblWidth | undefined;
  let start_: CT_TblWidth | undefined;
  let left: CT_TblWidth | undefined;
  let bottom: CT_TblWidth | undefined;
  let end: CT_TblWidth | undefined;
  let right: CT_TblWidth | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TcMar', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'top': {
            top = readCT_TblWidth(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'start': {
            start_ = readCT_TblWidth(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'left': {
            left = readCT_TblWidth(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'bottom': {
            bottom = readCT_TblWidth(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'end': {
            end = readCT_TblWidth(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'right': {
            right = readCT_TblWidth(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TcMar', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TcMar', cur);
    cur.next();
  }

  return {
    top,
    start: start_,
    left,
    bottom,
    end,
    right,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TcPr`. See `transitional/wml.xsd:2337`. */
export function readCT_TcPr(cur: XmlCursor, ctx: ReadContext): CT_TcPr {
  const start = requireStart(cur, 'CT_TcPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TcPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cnfStyle: CT_Cnf | undefined;
  let tcW: CT_TblWidth | undefined;
  let gridSpan: CT_DecimalNumber | undefined;
  let hMerge: CT_HMerge | undefined;
  let vMerge: CT_VMerge | undefined;
  let tcBorders: CT_TcBorders | undefined;
  let shd: CT_Shd | undefined;
  let noWrap: CT_OnOff | undefined;
  let tcMar: CT_TcMar | undefined;
  let textDirection: CT_TextDirection | undefined;
  let tcFitText: CT_OnOff | undefined;
  let vAlign: CT_VerticalJc | undefined;
  let hideMark: CT_OnOff | undefined;
  let headers: CT_Headers | undefined;
  let cellMarkupElements: CT_TcPr_CellMarkupElements | undefined;
  let tcPrChange: CT_TcPrChange | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TcPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'cnfStyle': {
            cnfStyle = readCT_Cnf(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tcW': {
            tcW = readCT_TblWidth(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'gridSpan': {
            gridSpan = readCT_DecimalNumber(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'hMerge': {
            hMerge = readCT_HMerge(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'vMerge': {
            vMerge = readCT_VMerge(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'tcBorders': {
            tcBorders = readCT_TcBorders(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'shd': {
            shd = readCT_Shd(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'noWrap': {
            noWrap = readCT_OnOff(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'tcMar': {
            tcMar = readCT_TcMar(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'textDirection': {
            textDirection = readCT_TextDirection(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tcFitText': {
            tcFitText = readCT_OnOff(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'vAlign': {
            vAlign = readCT_VerticalJc(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'hideMark': {
            hideMark = readCT_OnOff(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'headers': {
            headers = readCT_Headers(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'cellIns': {
            cellMarkupElements = { kind: 'cellIns', value: readCT_TrackChange(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'cellDel': {
            cellMarkupElements = { kind: 'cellDel', value: readCT_TrackChange(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'cellMerge': {
            cellMarkupElements = { kind: 'cellMerge', value: readCT_CellMergeTrackChange(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'tcPrChange': {
            tcPrChange = readCT_TcPrChange(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TcPr', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TcPr', cur);
    cur.next();
  }

  return {
    cnfStyle,
    tcW,
    gridSpan,
    hMerge,
    vMerge,
    tcBorders,
    shd,
    noWrap,
    tcMar,
    textDirection,
    tcFitText,
    vAlign,
    hideMark,
    headers,
    cellMarkupElements,
    tcPrChange,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TcPrBase`. See `transitional/wml.xsd:2319`. */
export function readCT_TcPrBase(cur: XmlCursor, ctx: ReadContext): CT_TcPrBase {
  const start = requireStart(cur, 'CT_TcPrBase');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TcPrBase', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cnfStyle: CT_Cnf | undefined;
  let tcW: CT_TblWidth | undefined;
  let gridSpan: CT_DecimalNumber | undefined;
  let hMerge: CT_HMerge | undefined;
  let vMerge: CT_VMerge | undefined;
  let tcBorders: CT_TcBorders | undefined;
  let shd: CT_Shd | undefined;
  let noWrap: CT_OnOff | undefined;
  let tcMar: CT_TcMar | undefined;
  let textDirection: CT_TextDirection | undefined;
  let tcFitText: CT_OnOff | undefined;
  let vAlign: CT_VerticalJc | undefined;
  let hideMark: CT_OnOff | undefined;
  let headers: CT_Headers | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TcPrBase', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'cnfStyle': {
            cnfStyle = readCT_Cnf(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tcW': {
            tcW = readCT_TblWidth(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'gridSpan': {
            gridSpan = readCT_DecimalNumber(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'hMerge': {
            hMerge = readCT_HMerge(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'vMerge': {
            vMerge = readCT_VMerge(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'tcBorders': {
            tcBorders = readCT_TcBorders(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'shd': {
            shd = readCT_Shd(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'noWrap': {
            noWrap = readCT_OnOff(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'tcMar': {
            tcMar = readCT_TcMar(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'textDirection': {
            textDirection = readCT_TextDirection(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tcFitText': {
            tcFitText = readCT_OnOff(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'vAlign': {
            vAlign = readCT_VerticalJc(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'hideMark': {
            hideMark = readCT_OnOff(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'headers': {
            headers = readCT_Headers(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TcPrBase', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TcPrBase', cur);
    cur.next();
  }

  return {
    cnfStyle,
    tcW,
    gridSpan,
    hMerge,
    vMerge,
    tcBorders,
    shd,
    noWrap,
    tcMar,
    textDirection,
    tcFitText,
    vAlign,
    hideMark,
    headers,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TcPrChange`. See `transitional/wml.xsd:923`. */
export function readCT_TcPrChange(cur: XmlCursor, ctx: ReadContext): CT_TcPrChange {
  const start = requireStart(cur, 'CT_TcPrChange');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TcPrChange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_TcPrChange', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_TcPrChange', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TcPrChange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_TcPrChange', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_TcPrChange', 'author', cur);

  let tcPr: CT_TcPrInner | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TcPrChange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'tcPr': {
            tcPr = readCT_TcPrInner(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TcPrChange', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TcPrChange', cur);
    cur.next();
  }

  return {
    tcPr,
    id,
    author,
    date,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TcPrInner`. See `transitional/wml.xsd:2346`. */
export function readCT_TcPrInner(cur: XmlCursor, ctx: ReadContext): CT_TcPrInner {
  const start = requireStart(cur, 'CT_TcPrInner');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TcPrInner', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cnfStyle: CT_Cnf | undefined;
  let tcW: CT_TblWidth | undefined;
  let gridSpan: CT_DecimalNumber | undefined;
  let hMerge: CT_HMerge | undefined;
  let vMerge: CT_VMerge | undefined;
  let tcBorders: CT_TcBorders | undefined;
  let shd: CT_Shd | undefined;
  let noWrap: CT_OnOff | undefined;
  let tcMar: CT_TcMar | undefined;
  let textDirection: CT_TextDirection | undefined;
  let tcFitText: CT_OnOff | undefined;
  let vAlign: CT_VerticalJc | undefined;
  let hideMark: CT_OnOff | undefined;
  let headers: CT_Headers | undefined;
  let cellMarkupElements: CT_TcPrInner_CellMarkupElements | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TcPrInner', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'cnfStyle': {
            cnfStyle = readCT_Cnf(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tcW': {
            tcW = readCT_TblWidth(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'gridSpan': {
            gridSpan = readCT_DecimalNumber(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'hMerge': {
            hMerge = readCT_HMerge(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'vMerge': {
            vMerge = readCT_VMerge(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'tcBorders': {
            tcBorders = readCT_TcBorders(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'shd': {
            shd = readCT_Shd(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'noWrap': {
            noWrap = readCT_OnOff(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'tcMar': {
            tcMar = readCT_TcMar(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'textDirection': {
            textDirection = readCT_TextDirection(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tcFitText': {
            tcFitText = readCT_OnOff(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'vAlign': {
            vAlign = readCT_VerticalJc(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'hideMark': {
            hideMark = readCT_OnOff(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'headers': {
            headers = readCT_Headers(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'cellIns': {
            cellMarkupElements = { kind: 'cellIns', value: readCT_TrackChange(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'cellDel': {
            cellMarkupElements = { kind: 'cellDel', value: readCT_TrackChange(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'cellMerge': {
            cellMarkupElements = { kind: 'cellMerge', value: readCT_CellMergeTrackChange(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TcPrInner', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TcPrInner', cur);
    cur.next();
  }

  return {
    cnfStyle,
    tcW,
    gridSpan,
    hMerge,
    vMerge,
    tcBorders,
    shd,
    noWrap,
    tcMar,
    textDirection,
    tcFitText,
    vAlign,
    hideMark,
    headers,
    cellMarkupElements,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Text`. See `transitional/wml.xsd:1736`. */
export function readCT_Text(cur: XmlCursor, ctx: ReadContext): CT_Text {
  const start = requireStart(cur, 'CT_Text');
  let space: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_xml_A = ctx.uris['xml'];
  for (const a of start.attrs) {
    if (a.uri === NS_xml_A) {
      switch (a.localName) {
        case 'space':
          space = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Text', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let $value = '';
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Text', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'text') { $value += ev.value; cur.next(); continue; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Text', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    cur.next();
  }
  const $parsed = parseST_String($value);

  return {
    $value: $parsed ?? $value,
    space,
    $unknownAttrs,
  };
}

/** Read a `CT_TextAlignment`. See `transitional/wml.xsd:826`. */
export function readCT_TextAlignment(cur: XmlCursor, ctx: ReadContext): CT_TextAlignment {
  const start = requireStart(cur, 'CT_TextAlignment');
  let val: ST_TextAlignment | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TextAlignment(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TextAlignment', a, 'ST_TextAlignment', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextAlignment', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TextAlignment', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextAlignment', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextAlignment', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextAlignment', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TextDirection`. See `transitional/wml.xsd:814`. */
export function readCT_TextDirection(cur: XmlCursor, ctx: ReadContext): CT_TextDirection {
  const start = requireStart(cur, 'CT_TextDirection');
  let val: ST_TextDirection | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TextDirection(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TextDirection', a, 'ST_TextDirection', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextDirection', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TextDirection', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextDirection', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextDirection', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextDirection', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TextEffect`. See `transitional/wml.xsd:214`. */
export function readCT_TextEffect(cur: XmlCursor, ctx: ReadContext): CT_TextEffect {
  const start = requireStart(cur, 'CT_TextEffect');
  let val: ST_TextEffect | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TextEffect(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TextEffect', a, 'ST_TextEffect', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TextEffect', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextEffect', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TextScale`. See `transitional/wml.xsd:127`. */
export function readCT_TextScale(cur: XmlCursor, ctx: ReadContext): CT_TextScale {
  const start = requireStart(cur, 'CT_TextScale');
  let val: ST_TextScale | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TextScale(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TextScale', a, 'ST_TextScale', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextScale', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextScale', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextScale', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextScale', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TextboxTightWrap`. See `transitional/wml.xsd:1084`. */
export function readCT_TextboxTightWrap(cur: XmlCursor, ctx: ReadContext): CT_TextboxTightWrap {
  const start = requireStart(cur, 'CT_TextboxTightWrap');
  let val: ST_TextboxTightWrap | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TextboxTightWrap(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TextboxTightWrap', a, 'ST_TextboxTightWrap', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextboxTightWrap', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TextboxTightWrap', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextboxTightWrap', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextboxTightWrap', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextboxTightWrap', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TopPageBorder`. See `transitional/wml.xsd:1503`. */
export function readCT_TopPageBorder(cur: XmlCursor, ctx: ReadContext): CT_TopPageBorder {
  const start = requireStart(cur, 'CT_TopPageBorder');
  let val: ST_Border | undefined;
  let color: ST_HexColor | undefined;
  let themeColor: ST_ThemeColor | undefined;
  let themeTint: ST_UcharHexNumber | undefined;
  let themeShade: ST_UcharHexNumber | undefined;
  let sz: ST_EighthPointMeasure | undefined;
  let space: ST_PointMeasure | undefined;
  let shadow: ST_OnOff | undefined;
  let frame: ST_OnOff | undefined;
  let id: ST_RelationshipId | undefined;
  let topLeft: ST_RelationshipId | undefined;
  let topRight: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TopPageBorder', a, 'ST_RelationshipId', cur);
          break;
        }
        case 'topLeft': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            topLeft = v;
            continue;
          }
          ctx.invalidValue('CT_TopPageBorder', a, 'ST_RelationshipId', cur);
          break;
        }
        case 'topRight': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            topRight = v;
            continue;
          }
          ctx.invalidValue('CT_TopPageBorder', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Border(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TopPageBorder', a, 'ST_Border', cur);
          break;
        }
        case 'color': {
          const v = parseST_HexColor(a.value);
          if (v !== undefined) {
            color = v;
            continue;
          }
          ctx.invalidValue('CT_TopPageBorder', a, 'ST_HexColor', cur);
          break;
        }
        case 'themeColor': {
          const v = parseST_ThemeColor(a.value);
          if (v !== undefined) {
            themeColor = v;
            continue;
          }
          ctx.invalidValue('CT_TopPageBorder', a, 'ST_ThemeColor', cur);
          break;
        }
        case 'themeTint': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeTint = v;
            continue;
          }
          ctx.invalidValue('CT_TopPageBorder', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'themeShade': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeShade = v;
            continue;
          }
          ctx.invalidValue('CT_TopPageBorder', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'sz': {
          const v = parseST_EighthPointMeasure(a.value);
          if (v !== undefined) {
            sz = v;
            continue;
          }
          ctx.invalidValue('CT_TopPageBorder', a, 'ST_EighthPointMeasure', cur);
          break;
        }
        case 'space': {
          const v = parseST_PointMeasure(a.value);
          if (v !== undefined) {
            space = v;
            continue;
          }
          ctx.invalidValue('CT_TopPageBorder', a, 'ST_PointMeasure', cur);
          break;
        }
        case 'shadow': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            shadow = v;
            continue;
          }
          ctx.invalidValue('CT_TopPageBorder', a, 'ST_OnOff', cur);
          break;
        }
        case 'frame': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            frame = v;
            continue;
          }
          ctx.invalidValue('CT_TopPageBorder', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TopPageBorder', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TopPageBorder', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TopPageBorder', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TopPageBorder', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TopPageBorder', cur);
    cur.next();
  }
  return {
    val,
    color,
    themeColor,
    themeTint,
    themeShade,
    sz,
    space,
    shadow,
    frame,
    id,
    topLeft,
    topRight,
    $unknownAttrs,
  };
}

/** Read a `CT_TrPr`. See `transitional/wml.xsd:2404`. */
export function readCT_TrPr(cur: XmlCursor, ctx: ReadContext): CT_TrPr {
  const start = requireStart(cur, 'CT_TrPr');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TrPr', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_TrPr_Content[] = [];
  let ins: CT_TrackChange | undefined;
  let del: CT_TrackChange | undefined;
  let trPrChange: CT_TrPrChange | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TrPr', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'cnfStyle': {
            content.push({ kind: 'cnfStyle', value: readCT_Cnf(cur, ctx) });
            continue;
          }
          case 'divId': {
            content.push({ kind: 'divId', value: readCT_DecimalNumber(cur, ctx) });
            continue;
          }
          case 'gridBefore': {
            content.push({ kind: 'gridBefore', value: readCT_DecimalNumber(cur, ctx) });
            continue;
          }
          case 'gridAfter': {
            content.push({ kind: 'gridAfter', value: readCT_DecimalNumber(cur, ctx) });
            continue;
          }
          case 'wBefore': {
            content.push({ kind: 'wBefore', value: readCT_TblWidth(cur, ctx) });
            continue;
          }
          case 'wAfter': {
            content.push({ kind: 'wAfter', value: readCT_TblWidth(cur, ctx) });
            continue;
          }
          case 'cantSplit': {
            content.push({ kind: 'cantSplit', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'trHeight': {
            content.push({ kind: 'trHeight', value: readCT_Height(cur, ctx) });
            continue;
          }
          case 'tblHeader': {
            content.push({ kind: 'tblHeader', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'tblCellSpacing': {
            content.push({ kind: 'tblCellSpacing', value: readCT_TblWidth(cur, ctx) });
            continue;
          }
          case 'jc': {
            content.push({ kind: 'jc', value: readCT_JcTable(cur, ctx) });
            continue;
          }
          case 'hidden': {
            content.push({ kind: 'hidden', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'ins': {
            ins = readCT_TrackChange(cur, ctx);
            continue;
          }
          case 'del': {
            del = readCT_TrackChange(cur, ctx);
            continue;
          }
          case 'trPrChange': {
            trPrChange = readCT_TrPrChange(cur, ctx);
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TrPr', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TrPr', cur);
    cur.next();
  }

  return {
    content,
    ins,
    del,
    trPrChange,
    $unknownAttrs,
  };
}

/** Read a `CT_TrPrBase`. See `transitional/wml.xsd:2388`. */
export function readCT_TrPrBase(cur: XmlCursor, ctx: ReadContext): CT_TrPrBase {
  const start = requireStart(cur, 'CT_TrPrBase');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TrPrBase', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_TrPrBase_Content[] = [];

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TrPrBase', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'cnfStyle': {
            content.push({ kind: 'cnfStyle', value: readCT_Cnf(cur, ctx) });
            continue;
          }
          case 'divId': {
            content.push({ kind: 'divId', value: readCT_DecimalNumber(cur, ctx) });
            continue;
          }
          case 'gridBefore': {
            content.push({ kind: 'gridBefore', value: readCT_DecimalNumber(cur, ctx) });
            continue;
          }
          case 'gridAfter': {
            content.push({ kind: 'gridAfter', value: readCT_DecimalNumber(cur, ctx) });
            continue;
          }
          case 'wBefore': {
            content.push({ kind: 'wBefore', value: readCT_TblWidth(cur, ctx) });
            continue;
          }
          case 'wAfter': {
            content.push({ kind: 'wAfter', value: readCT_TblWidth(cur, ctx) });
            continue;
          }
          case 'cantSplit': {
            content.push({ kind: 'cantSplit', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'trHeight': {
            content.push({ kind: 'trHeight', value: readCT_Height(cur, ctx) });
            continue;
          }
          case 'tblHeader': {
            content.push({ kind: 'tblHeader', value: readCT_OnOff(cur, ctx) });
            continue;
          }
          case 'tblCellSpacing': {
            content.push({ kind: 'tblCellSpacing', value: readCT_TblWidth(cur, ctx) });
            continue;
          }
          case 'jc': {
            content.push({ kind: 'jc', value: readCT_JcTable(cur, ctx) });
            continue;
          }
          case 'hidden': {
            content.push({ kind: 'hidden', value: readCT_OnOff(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TrPrBase', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TrPrBase', cur);
    cur.next();
  }

  return {
    content,
    $unknownAttrs,
  };
}

/** Read a `CT_TrPrChange`. See `transitional/wml.xsd:932`. */
export function readCT_TrPrChange(cur: XmlCursor, ctx: ReadContext): CT_TrPrChange {
  const start = requireStart(cur, 'CT_TrPrChange');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TrPrChange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_TrPrChange', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_TrPrChange', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TrPrChange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_TrPrChange', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_TrPrChange', 'author', cur);

  let trPr: CT_TrPrBase | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TrPrChange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'trPr': {
            trPr = readCT_TrPrBase(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TrPrChange', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TrPrChange', cur);
    cur.next();
  }

  return {
    trPr,
    id,
    author,
    date,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TrackChange`. See `transitional/wml.xsd:844`. */
export function readCT_TrackChange(cur: XmlCursor, ctx: ReadContext): CT_TrackChange {
  const start = requireStart(cur, 'CT_TrackChange');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChange', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChange', a, 'ST_DateTime', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TrackChange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_TrackChange', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_TrackChange', 'author', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TrackChange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TrackChange', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TrackChange', cur);
    cur.next();
  }
  return {
    id,
    author,
    date,
    $unknownAttrs,
  };
}

/** Read a `CT_TrackChangeNumbering`. See `transitional/wml.xsd:907`. */
export function readCT_TrackChangeNumbering(cur: XmlCursor, ctx: ReadContext): CT_TrackChangeNumbering {
  const start = requireStart(cur, 'CT_TrackChangeNumbering');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let original: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangeNumbering', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangeNumbering', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangeNumbering', a, 'ST_DateTime', cur);
          break;
        }
        case 'original': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            original = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangeNumbering', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TrackChangeNumbering', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_TrackChangeNumbering', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_TrackChangeNumbering', 'author', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TrackChangeNumbering', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TrackChangeNumbering', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TrackChangeNumbering', cur);
    cur.next();
  }
  return {
    id,
    author,
    date,
    original,
    $unknownAttrs,
  };
}

/** Read a `CT_TrackChangeRange`. See `transitional/wml.xsd:860`. */
export function readCT_TrackChangeRange(cur: XmlCursor, ctx: ReadContext): CT_TrackChangeRange {
  const start = requireStart(cur, 'CT_TrackChangeRange');
  let id: ST_DecimalNumber | undefined;
  let author: ST_String | undefined;
  let date: ST_DateTime | undefined;
  let displacedByCustomXml: ST_DisplacedByCustomXml | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangeRange', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'author': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            author = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangeRange', a, 'ST_String', cur);
          break;
        }
        case 'date': {
          const v = parseST_DateTime(a.value);
          if (v !== undefined) {
            date = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangeRange', a, 'ST_DateTime', cur);
          break;
        }
        case 'displacedByCustomXml': {
          const v = parseST_DisplacedByCustomXml(a.value);
          if (v !== undefined) {
            displacedByCustomXml = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangeRange', a, 'ST_DisplacedByCustomXml', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TrackChangeRange', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_TrackChangeRange', 'id', cur);
  if (author === undefined) ctx.missingRequired('CT_TrackChangeRange', 'author', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TrackChangeRange', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TrackChangeRange', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TrackChangeRange', cur);
    cur.next();
  }
  return {
    id,
    author,
    date,
    displacedByCustomXml,
    $unknownAttrs,
  };
}

/** Read a `CT_TrackChangesView`. See `transitional/wml.xsd:787`. */
export function readCT_TrackChangesView(cur: XmlCursor, ctx: ReadContext): CT_TrackChangesView {
  const start = requireStart(cur, 'CT_TrackChangesView');
  let markup: ST_OnOff | undefined;
  let comments: ST_OnOff | undefined;
  let insDel: ST_OnOff | undefined;
  let formatting: ST_OnOff | undefined;
  let inkAnnotations: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'markup': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            markup = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangesView', a, 'ST_OnOff', cur);
          break;
        }
        case 'comments': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            comments = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangesView', a, 'ST_OnOff', cur);
          break;
        }
        case 'insDel': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            insDel = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangesView', a, 'ST_OnOff', cur);
          break;
        }
        case 'formatting': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            formatting = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangesView', a, 'ST_OnOff', cur);
          break;
        }
        case 'inkAnnotations': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            inkAnnotations = v;
            continue;
          }
          ctx.invalidValue('CT_TrackChangesView', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TrackChangesView', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TrackChangesView', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TrackChangesView', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TrackChangesView', cur);
    cur.next();
  }
  return {
    markup,
    comments,
    insDel,
    formatting,
    inkAnnotations,
    $unknownAttrs,
  };
}

/** Read a `CT_TwipsMeasure`. See `transitional/wml.xsd:66`. */
export function readCT_TwipsMeasure(cur: XmlCursor, ctx: ReadContext): CT_TwipsMeasure {
  const start = requireStart(cur, 'CT_TwipsMeasure');
  let val: ST_TwipsMeasure | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TwipsMeasure(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TwipsMeasure', a, 'ST_TwipsMeasure', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TwipsMeasure', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TwipsMeasure', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TwipsMeasure', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TwipsMeasure', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TwipsMeasure', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TxbxContent`. See `transitional/wml.xsd:3376`. */
export function readCT_TxbxContent(cur: XmlCursor, ctx: ReadContext): CT_TxbxContent {
  const start = requireStart(cur, 'CT_TxbxContent');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TxbxContent', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const blockLevelElts: CT_TxbxContent_BlockLevelElts[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TxbxContent', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            blockLevelElts.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            blockLevelElts.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            blockLevelElts.push({ kind: 'customXml', value: readCT_CustomXmlBlock(cur, ctx) });
            continue;
          }
          case 'sdt': {
            blockLevelElts.push({ kind: 'sdt', value: readCT_SdtBlock(cur, ctx) });
            continue;
          }
          case 'p': {
            blockLevelElts.push({ kind: 'p', value: readCT_P(cur, ctx) });
            continue;
          }
          case 'tbl': {
            blockLevelElts.push({ kind: 'tbl', value: readCT_Tbl(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            blockLevelElts.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            blockLevelElts.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            blockLevelElts.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            blockLevelElts.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            blockLevelElts.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            blockLevelElts.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            blockLevelElts.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            blockLevelElts.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            blockLevelElts.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            blockLevelElts.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            blockLevelElts.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            blockLevelElts.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            blockLevelElts.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            blockLevelElts.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            blockLevelElts.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            blockLevelElts.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            blockLevelElts.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'altChunk': {
            blockLevelElts.push({ kind: 'altChunk', value: readCT_AltChunk(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TxbxContent', ev.localName, ev.uri, cur);
      blockLevelElts.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TxbxContent', cur);
    cur.next();
  }

  return {
    blockLevelElts,
    $unknownAttrs,
  };
}

/** Read a `CT_Underline`. See `transitional/wml.xsd:196`. */
export function readCT_Underline(cur: XmlCursor, ctx: ReadContext): CT_Underline {
  const start = requireStart(cur, 'CT_Underline');
  let val: ST_Underline | undefined;
  let color: ST_HexColor | undefined;
  let themeColor: ST_ThemeColor | undefined;
  let themeTint: ST_UcharHexNumber | undefined;
  let themeShade: ST_UcharHexNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Underline(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Underline', a, 'ST_Underline', cur);
          break;
        }
        case 'color': {
          const v = parseST_HexColor(a.value);
          if (v !== undefined) {
            color = v;
            continue;
          }
          ctx.invalidValue('CT_Underline', a, 'ST_HexColor', cur);
          break;
        }
        case 'themeColor': {
          const v = parseST_ThemeColor(a.value);
          if (v !== undefined) {
            themeColor = v;
            continue;
          }
          ctx.invalidValue('CT_Underline', a, 'ST_ThemeColor', cur);
          break;
        }
        case 'themeTint': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeTint = v;
            continue;
          }
          ctx.invalidValue('CT_Underline', a, 'ST_UcharHexNumber', cur);
          break;
        }
        case 'themeShade': {
          const v = parseST_UcharHexNumber(a.value);
          if (v !== undefined) {
            themeShade = v;
            continue;
          }
          ctx.invalidValue('CT_Underline', a, 'ST_UcharHexNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Underline', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Underline', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Underline', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Underline', cur);
    cur.next();
  }
  return {
    val,
    color,
    themeColor,
    themeTint,
    themeShade,
    $unknownAttrs,
  };
}

/** Read a `CT_UnsignedDecimalNumber`. See `transitional/wml.xsd:60`. */
export function readCT_UnsignedDecimalNumber(cur: XmlCursor, ctx: ReadContext): CT_UnsignedDecimalNumber {
  const start = requireStart(cur, 'CT_UnsignedDecimalNumber');
  let val: ST_UnsignedDecimalNumber | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_UnsignedDecimalNumber(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_UnsignedDecimalNumber', a, 'ST_UnsignedDecimalNumber', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_UnsignedDecimalNumber', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_UnsignedDecimalNumber', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_UnsignedDecimalNumber', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_UnsignedDecimalNumber', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_UnsignedDecimalNumber', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_VMerge`. See `transitional/wml.xsd:2313`. */
export function readCT_VMerge(cur: XmlCursor, ctx: ReadContext): CT_VMerge {
  const start = requireStart(cur, 'CT_VMerge');
  let val: ST_Merge | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Merge(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_VMerge', a, 'ST_Merge', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_VMerge', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_VMerge', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_VMerge', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_VMerge', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_VerticalAlignRun`. See `transitional/wml.xsd:478`. */
export function readCT_VerticalAlignRun(cur: XmlCursor, ctx: ReadContext): CT_VerticalAlignRun {
  const start = requireStart(cur, 'CT_VerticalAlignRun');
  let val: ST_VerticalAlignRun | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_VerticalAlignRun(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_VerticalAlignRun', a, 'ST_VerticalAlignRun', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_VerticalAlignRun', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_VerticalAlignRun', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_VerticalAlignRun', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_VerticalAlignRun', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_VerticalAlignRun', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_VerticalJc`. See `transitional/wml.xsd:1560`. */
export function readCT_VerticalJc(cur: XmlCursor, ctx: ReadContext): CT_VerticalJc {
  const start = requireStart(cur, 'CT_VerticalJc');
  let val: ST_VerticalJc | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_VerticalJc(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_VerticalJc', a, 'ST_VerticalJc', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_VerticalJc', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_VerticalJc', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_VerticalJc', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_VerticalJc', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_VerticalJc', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_View`. See `transitional/wml.xsd:673`. */
export function readCT_View(cur: XmlCursor, ctx: ReadContext): CT_View {
  const start = requireStart(cur, 'CT_View');
  let val: ST_View | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_View(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_View', a, 'ST_View', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_View', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_View', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_View', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_View', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_View', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_WebSettings`. See `transitional/wml.xsd:3029`. */
export function readCT_WebSettings(cur: XmlCursor, ctx: ReadContext): CT_WebSettings {
  const start = requireStart(cur, 'CT_WebSettings');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_WebSettings', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let frameset: CT_Frameset | undefined;
  let divs: CT_Divs | undefined;
  let encoding: CT_String | undefined;
  let optimizeForBrowser: CT_OptimizeForBrowser | undefined;
  let relyOnVML: CT_OnOff | undefined;
  let allowPNG: CT_OnOff | undefined;
  let doNotRelyOnCSS: CT_OnOff | undefined;
  let doNotSaveAsSingleFile: CT_OnOff | undefined;
  let doNotOrganizeInFolder: CT_OnOff | undefined;
  let doNotUseLongFileNames: CT_OnOff | undefined;
  let pixelsPerInch: CT_DecimalNumber | undefined;
  let targetScreenSz: CT_TargetScreenSz | undefined;
  let saveSmartTagsAsXml: CT_OnOff | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WebSettings', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'frameset': {
            frameset = readCT_Frameset(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'divs': {
            divs = readCT_Divs(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'encoding': {
            encoding = readCT_String(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'optimizeForBrowser': {
            optimizeForBrowser = readCT_OptimizeForBrowser(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'relyOnVML': {
            relyOnVML = readCT_OnOff(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'allowPNG': {
            allowPNG = readCT_OnOff(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'doNotRelyOnCSS': {
            doNotRelyOnCSS = readCT_OnOff(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'doNotSaveAsSingleFile': {
            doNotSaveAsSingleFile = readCT_OnOff(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'doNotOrganizeInFolder': {
            doNotOrganizeInFolder = readCT_OnOff(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'doNotUseLongFileNames': {
            doNotUseLongFileNames = readCT_OnOff(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'pixelsPerInch': {
            pixelsPerInch = readCT_DecimalNumber(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'targetScreenSz': {
            targetScreenSz = readCT_TargetScreenSz(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'saveSmartTagsAsXml': {
            saveSmartTagsAsXml = readCT_OnOff(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WebSettings', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WebSettings', cur);
    cur.next();
  }

  return {
    frameset,
    divs,
    encoding,
    optimizeForBrowser,
    relyOnVML,
    allowPNG,
    doNotRelyOnCSS,
    doNotSaveAsSingleFile,
    doNotOrganizeInFolder,
    doNotUseLongFileNames,
    pixelsPerInch,
    targetScreenSz,
    saveSmartTagsAsXml,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_WriteProtection`. See `transitional/wml.xsd:2884`. */
export function readCT_WriteProtection(cur: XmlCursor, ctx: ReadContext): CT_WriteProtection {
  const start = requireStart(cur, 'CT_WriteProtection');
  let recommended: ST_OnOff | undefined;
  let algorithmName: ST_String | undefined;
  let hashValue: string | undefined;
  let saltValue: string | undefined;
  let spinCount: ST_DecimalNumber | undefined;
  let cryptProviderType: ST_CryptProv | undefined;
  let cryptAlgorithmClass: ST_AlgClass | undefined;
  let cryptAlgorithmType: ST_AlgType | undefined;
  let cryptAlgorithmSid: ST_DecimalNumber | undefined;
  let cryptSpinCount: ST_DecimalNumber | undefined;
  let cryptProvider: ST_String | undefined;
  let algIdExt: ST_LongHexNumber | undefined;
  let algIdExtSource: ST_String | undefined;
  let cryptProviderTypeExt: ST_LongHexNumber | undefined;
  let cryptProviderTypeExtSource: ST_String | undefined;
  let hash: string | undefined;
  let salt: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'recommended': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            recommended = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_OnOff', cur);
          break;
        }
        case 'algorithmName': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            algorithmName = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_String', cur);
          break;
        }
        case 'hashValue':
          hashValue = a.value as string;
          continue;

        case 'saltValue':
          saltValue = a.value as string;
          continue;

        case 'spinCount': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            spinCount = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'cryptProviderType': {
          const v = parseST_CryptProv(a.value);
          if (v !== undefined) {
            cryptProviderType = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_CryptProv', cur);
          break;
        }
        case 'cryptAlgorithmClass': {
          const v = parseST_AlgClass(a.value);
          if (v !== undefined) {
            cryptAlgorithmClass = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_AlgClass', cur);
          break;
        }
        case 'cryptAlgorithmType': {
          const v = parseST_AlgType(a.value);
          if (v !== undefined) {
            cryptAlgorithmType = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_AlgType', cur);
          break;
        }
        case 'cryptAlgorithmSid': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            cryptAlgorithmSid = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'cryptSpinCount': {
          const v = parseST_DecimalNumber(a.value);
          if (v !== undefined) {
            cryptSpinCount = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_DecimalNumber', cur);
          break;
        }
        case 'cryptProvider': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            cryptProvider = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_String', cur);
          break;
        }
        case 'algIdExt': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            algIdExt = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'algIdExtSource': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            algIdExtSource = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_String', cur);
          break;
        }
        case 'cryptProviderTypeExt': {
          const v = parseST_LongHexNumber(a.value);
          if (v !== undefined) {
            cryptProviderTypeExt = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_LongHexNumber', cur);
          break;
        }
        case 'cryptProviderTypeExtSource': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            cryptProviderTypeExtSource = v;
            continue;
          }
          ctx.invalidValue('CT_WriteProtection', a, 'ST_String', cur);
          break;
        }
        case 'hash':
          hash = a.value as string;
          continue;

        case 'salt':
          salt = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_WriteProtection', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WriteProtection', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_WriteProtection', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WriteProtection', cur);
    cur.next();
  }
  return {
    recommended,
    algorithmName,
    hashValue,
    saltValue,
    spinCount,
    cryptProviderType,
    cryptAlgorithmClass,
    cryptAlgorithmType,
    cryptAlgorithmSid,
    cryptSpinCount,
    cryptProvider,
    algIdExt,
    algIdExtSource,
    cryptProviderTypeExt,
    cryptProviderTypeExtSource,
    hash,
    salt,
    $unknownAttrs,
  };
}

/** Read a `CT_WritingStyle`. See `transitional/wml.xsd:688`. */
export function readCT_WritingStyle(cur: XmlCursor, ctx: ReadContext): CT_WritingStyle {
  const start = requireStart(cur, 'CT_WritingStyle');
  let lang: ST_Lang | undefined;
  let vendorID: ST_String | undefined;
  let dllVersion: ST_String | undefined;
  let nlCheck: ST_OnOff | undefined;
  let checkStyle: ST_OnOff | undefined;
  let appName: ST_String | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'lang': {
          const v = parseST_Lang(a.value);
          if (v !== undefined) {
            lang = v;
            continue;
          }
          ctx.invalidValue('CT_WritingStyle', a, 'ST_Lang', cur);
          break;
        }
        case 'vendorID': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            vendorID = v;
            continue;
          }
          ctx.invalidValue('CT_WritingStyle', a, 'ST_String', cur);
          break;
        }
        case 'dllVersion': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            dllVersion = v;
            continue;
          }
          ctx.invalidValue('CT_WritingStyle', a, 'ST_String', cur);
          break;
        }
        case 'nlCheck': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            nlCheck = v;
            continue;
          }
          ctx.invalidValue('CT_WritingStyle', a, 'ST_OnOff', cur);
          break;
        }
        case 'checkStyle': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            checkStyle = v;
            continue;
          }
          ctx.invalidValue('CT_WritingStyle', a, 'ST_OnOff', cur);
          break;
        }
        case 'appName': {
          const v = parseST_String(a.value);
          if (v !== undefined) {
            appName = v;
            continue;
          }
          ctx.invalidValue('CT_WritingStyle', a, 'ST_String', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_WritingStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (lang === undefined) ctx.missingRequired('CT_WritingStyle', 'lang', cur);
  if (vendorID === undefined) ctx.missingRequired('CT_WritingStyle', 'vendorID', cur);
  if (dllVersion === undefined) ctx.missingRequired('CT_WritingStyle', 'dllVersion', cur);
  if (checkStyle === undefined) ctx.missingRequired('CT_WritingStyle', 'checkStyle', cur);
  if (appName === undefined) ctx.missingRequired('CT_WritingStyle', 'appName', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WritingStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_WritingStyle', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WritingStyle', cur);
    cur.next();
  }
  return {
    lang,
    vendorID,
    dllVersion,
    nlCheck,
    checkStyle,
    appName,
    $unknownAttrs,
  };
}

/** Read a `CT_Zoom`. See `transitional/wml.xsd:684`. */
export function readCT_Zoom(cur: XmlCursor, ctx: ReadContext): CT_Zoom {
  const start = requireStart(cur, 'CT_Zoom');
  let val: ST_Zoom | undefined;
  let percent: ST_DecimalNumberOrPercent | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_wml_A = ctx.uris['wml'];
  for (const a of start.attrs) {
    if (a.uri === NS_wml_A) {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Zoom(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Zoom', a, 'ST_Zoom', cur);
          break;
        }
        case 'percent': {
          const v = parseST_DecimalNumberOrPercent(a.value);
          if (v !== undefined) {
            percent = v;
            continue;
          }
          ctx.invalidValue('CT_Zoom', a, 'ST_DecimalNumberOrPercent', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Zoom', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (percent === undefined) ctx.missingRequired('CT_Zoom', 'percent', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Zoom', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Zoom', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Zoom', cur);
    cur.next();
  }
  return {
    val,
    percent,
    $unknownAttrs,
  };
}
