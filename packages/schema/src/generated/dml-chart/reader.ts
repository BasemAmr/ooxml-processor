/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the dml-chart namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type RawNode,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  parseDouble,
  parseInteger,
  parseXsdBoolean,
  readScalar,
  requireStart,
} from '../../runtime/index.js';
import {
  readCT_ColorMapping,
  readCT_ShapeProperties,
  readCT_TextBody,
} from '../dml-main/reader.js';
import type { CT_ColorMapping, CT_ShapeProperties, CT_TextBody } from '../dml-main/types.js';
import { parseST_RelationshipId } from '../relationships/reader.js';
import type { ST_RelationshipId } from '../relationships/types.js';
import {
  parseST_Lang,
  parseST_PositiveUniversalMeasure,
  parseST_Xstring,
} from '../shared-types/reader.js';
import type { ST_Lang, ST_PositiveUniversalMeasure, ST_Xstring } from '../shared-types/types.js';
import {
  type CT_Area3DChart,
  type CT_AreaChart,
  type CT_AreaSer,
  type CT_AxDataSource,
  type CT_AxDataSource_Content,
  type CT_AxPos,
  type CT_AxisUnit,
  type CT_BandFmt,
  type CT_BandFmts,
  type CT_Bar3DChart,
  type CT_BarChart,
  type CT_BarDir,
  type CT_BarGrouping,
  type CT_BarSer,
  type CT_Boolean,
  type CT_BubbleChart,
  type CT_BubbleScale,
  type CT_BubbleSer,
  type CT_BuiltInUnit,
  type CT_CatAx,
  type CT_CatAx_Content,
  type CT_Chart,
  type CT_ChartLines,
  type CT_ChartSpace,
  type CT_CrossBetween,
  type CT_Crosses,
  type CT_CustSplit,
  type CT_DLbl,
  type CT_DLblPos,
  type CT_DLbl_Content,
  type CT_DLbls,
  type CT_DLbls_Content,
  type CT_DPt,
  type CT_DTable,
  type CT_DateAx,
  type CT_DateAx_Content,
  type CT_DepthPercent,
  type CT_DispBlanksAs,
  type CT_DispUnits,
  type CT_DispUnitsLbl,
  type CT_DispUnits_Content,
  type CT_Double,
  type CT_DoughnutChart,
  type CT_ErrBarType,
  type CT_ErrBars,
  type CT_ErrDir,
  type CT_ErrValType,
  type CT_Extension,
  type CT_ExtensionList,
  type CT_ExternalData,
  type CT_FirstSliceAng,
  type CT_GapAmount,
  type CT_Grouping,
  type CT_HPercent,
  type CT_HeaderFooter,
  type CT_HoleSize,
  type CT_Layout,
  type CT_LayoutMode,
  type CT_LayoutTarget,
  type CT_LblAlgn,
  type CT_LblOffset,
  type CT_Legend,
  type CT_LegendEntry,
  type CT_LegendEntry_Content,
  type CT_LegendPos,
  type CT_Line3DChart,
  type CT_LineChart,
  type CT_LineSer,
  type CT_LogBase,
  type CT_Lvl,
  type CT_ManualLayout,
  type CT_Marker,
  type CT_MarkerSize,
  type CT_MarkerStyle,
  type CT_MultiLvlStrData,
  type CT_MultiLvlStrRef,
  type CT_NumData,
  type CT_NumDataSource,
  type CT_NumDataSource_Content,
  type CT_NumFmt,
  type CT_NumRef,
  type CT_NumVal,
  type CT_OfPieChart,
  type CT_OfPieType,
  type CT_Order,
  type CT_Orientation,
  type CT_Overlap,
  type CT_PageMargins,
  type CT_PageSetup,
  type CT_Period,
  type CT_Perspective,
  type CT_PictureFormat,
  type CT_PictureOptions,
  type CT_PictureStackUnit,
  type CT_Pie3DChart,
  type CT_PieChart,
  type CT_PieSer,
  type CT_PivotFmt,
  type CT_PivotFmts,
  type CT_PivotSource,
  type CT_PlotArea,
  type CT_PlotArea_Content,
  type CT_PlotArea_Content2,
  type CT_PrintSettings,
  type CT_Protection,
  type CT_RadarChart,
  type CT_RadarSer,
  type CT_RadarStyle,
  type CT_RelId,
  type CT_RotX,
  type CT_RotY,
  type CT_Scaling,
  type CT_ScatterChart,
  type CT_ScatterSer,
  type CT_ScatterStyle,
  type CT_SecondPieSize,
  type CT_SerAx,
  type CT_SerAx_Content,
  type CT_SerTx,
  type CT_SerTx_Content,
  type CT_Shape,
  type CT_SizeRepresents,
  type CT_Skip,
  type CT_SplitType,
  type CT_StockChart,
  type CT_StrData,
  type CT_StrRef,
  type CT_StrVal,
  type CT_Style,
  type CT_Surface,
  type CT_Surface3DChart,
  type CT_SurfaceChart,
  type CT_SurfaceSer,
  type CT_TextLanguageID,
  type CT_Thickness,
  type CT_TickLblPos,
  type CT_TickMark,
  type CT_TimeUnit,
  type CT_Title,
  type CT_Trendline,
  type CT_TrendlineLbl,
  type CT_TrendlineType,
  type CT_Tx,
  type CT_Tx_Content,
  type CT_UnsignedInt,
  type CT_UpDownBar,
  type CT_UpDownBars,
  type CT_ValAx,
  type CT_ValAx_Content,
  type CT_View3D,
  type ST_AxPos,
  ST_AxPos_VALUES,
  type ST_AxisUnit,
  type ST_BarDir,
  ST_BarDir_VALUES,
  type ST_BarGrouping,
  ST_BarGrouping_VALUES,
  type ST_BubbleScale,
  type ST_BubbleScalePercent,
  type ST_BubbleScaleUInt,
  type ST_BuiltInUnit,
  ST_BuiltInUnit_VALUES,
  type ST_CrossBetween,
  ST_CrossBetween_VALUES,
  type ST_Crosses,
  ST_Crosses_VALUES,
  type ST_DLblPos,
  ST_DLblPos_VALUES,
  type ST_DepthPercent,
  type ST_DepthPercentUShort,
  type ST_DepthPercentWithSymbol,
  type ST_DispBlanksAs,
  ST_DispBlanksAs_VALUES,
  type ST_ErrBarType,
  ST_ErrBarType_VALUES,
  type ST_ErrDir,
  ST_ErrDir_VALUES,
  type ST_ErrValType,
  ST_ErrValType_VALUES,
  type ST_FirstSliceAng,
  type ST_GapAmount,
  type ST_GapAmountPercent,
  type ST_GapAmountUShort,
  type ST_Grouping,
  ST_Grouping_VALUES,
  type ST_HPercent,
  type ST_HPercentUShort,
  type ST_HPercentWithSymbol,
  type ST_HoleSize,
  type ST_HoleSizePercent,
  type ST_HoleSizeUByte,
  type ST_LayoutMode,
  ST_LayoutMode_VALUES,
  type ST_LayoutTarget,
  ST_LayoutTarget_VALUES,
  type ST_LblAlgn,
  ST_LblAlgn_VALUES,
  type ST_LblOffset,
  type ST_LblOffsetPercent,
  type ST_LblOffsetUShort,
  type ST_LegendPos,
  ST_LegendPos_VALUES,
  type ST_LogBase,
  type ST_MarkerSize,
  type ST_MarkerStyle,
  ST_MarkerStyle_VALUES,
  type ST_OfPieType,
  ST_OfPieType_VALUES,
  type ST_Order,
  type ST_Orientation,
  ST_Orientation_VALUES,
  type ST_Overlap,
  type ST_OverlapByte,
  type ST_OverlapPercent,
  type ST_PageSetupOrientation,
  ST_PageSetupOrientation_VALUES,
  type ST_Period,
  type ST_Perspective,
  type ST_PictureFormat,
  ST_PictureFormat_VALUES,
  type ST_PictureStackUnit,
  type ST_RadarStyle,
  ST_RadarStyle_VALUES,
  type ST_RotX,
  type ST_RotY,
  type ST_ScatterStyle,
  ST_ScatterStyle_VALUES,
  type ST_SecondPieSize,
  type ST_SecondPieSizePercent,
  type ST_SecondPieSizeUShort,
  type ST_Shape,
  ST_Shape_VALUES,
  type ST_SizeRepresents,
  ST_SizeRepresents_VALUES,
  type ST_Skip,
  type ST_SplitType,
  ST_SplitType_VALUES,
  type ST_Style,
  type ST_Thickness,
  type ST_ThicknessPercent,
  type ST_TickLblPos,
  ST_TickLblPos_VALUES,
  type ST_TickMark,
  ST_TickMark_VALUES,
  type ST_TimeUnit,
  ST_TimeUnit_VALUES,
  type ST_TrendlineType,
  ST_TrendlineType_VALUES,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_AxPos(value: string): ST_AxPos | undefined {
  const s = collapse(value);
  return ST_AxPos_VALUES.has(s) ? (s as ST_AxPos) : undefined;
}

export function parseST_AxisUnit(value: string): ST_AxisUnit | undefined {
  const n = parseDouble(value);
  return n === undefined ? undefined : (n as ST_AxisUnit);
}

export function parseST_BarDir(value: string): ST_BarDir | undefined {
  const s = collapse(value);
  return ST_BarDir_VALUES.has(s) ? (s as ST_BarDir) : undefined;
}

export function parseST_BarGrouping(value: string): ST_BarGrouping | undefined {
  const s = collapse(value);
  return ST_BarGrouping_VALUES.has(s) ? (s as ST_BarGrouping) : undefined;
}

export function parseST_BubbleScale(value: string): ST_BubbleScale | undefined {
  return (parseST_BubbleScalePercent(value) ?? parseST_BubbleScaleUInt(value)) as ST_BubbleScale | undefined;
}

export function parseST_BubbleScalePercent(value: string): ST_BubbleScalePercent | undefined {
  return value as ST_BubbleScalePercent;
}

export function parseST_BubbleScaleUInt(value: string): ST_BubbleScaleUInt | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_BubbleScaleUInt);
}

export function parseST_BuiltInUnit(value: string): ST_BuiltInUnit | undefined {
  const s = collapse(value);
  return ST_BuiltInUnit_VALUES.has(s) ? (s as ST_BuiltInUnit) : undefined;
}

export function parseST_CrossBetween(value: string): ST_CrossBetween | undefined {
  const s = collapse(value);
  return ST_CrossBetween_VALUES.has(s) ? (s as ST_CrossBetween) : undefined;
}

export function parseST_Crosses(value: string): ST_Crosses | undefined {
  const s = collapse(value);
  return ST_Crosses_VALUES.has(s) ? (s as ST_Crosses) : undefined;
}

export function parseST_DLblPos(value: string): ST_DLblPos | undefined {
  const s = collapse(value);
  return ST_DLblPos_VALUES.has(s) ? (s as ST_DLblPos) : undefined;
}

export function parseST_DepthPercent(value: string): ST_DepthPercent | undefined {
  return (parseST_DepthPercentWithSymbol(value) ?? parseST_DepthPercentUShort(value)) as ST_DepthPercent | undefined;
}

export function parseST_DepthPercentUShort(value: string): ST_DepthPercentUShort | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_DepthPercentUShort);
}

export function parseST_DepthPercentWithSymbol(value: string): ST_DepthPercentWithSymbol | undefined {
  return value as ST_DepthPercentWithSymbol;
}

export function parseST_DispBlanksAs(value: string): ST_DispBlanksAs | undefined {
  const s = collapse(value);
  return ST_DispBlanksAs_VALUES.has(s) ? (s as ST_DispBlanksAs) : undefined;
}

export function parseST_ErrBarType(value: string): ST_ErrBarType | undefined {
  const s = collapse(value);
  return ST_ErrBarType_VALUES.has(s) ? (s as ST_ErrBarType) : undefined;
}

export function parseST_ErrDir(value: string): ST_ErrDir | undefined {
  const s = collapse(value);
  return ST_ErrDir_VALUES.has(s) ? (s as ST_ErrDir) : undefined;
}

export function parseST_ErrValType(value: string): ST_ErrValType | undefined {
  const s = collapse(value);
  return ST_ErrValType_VALUES.has(s) ? (s as ST_ErrValType) : undefined;
}

export function parseST_FirstSliceAng(value: string): ST_FirstSliceAng | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_FirstSliceAng);
}

export function parseST_GapAmount(value: string): ST_GapAmount | undefined {
  return (parseST_GapAmountPercent(value) ?? parseST_GapAmountUShort(value)) as ST_GapAmount | undefined;
}

export function parseST_GapAmountPercent(value: string): ST_GapAmountPercent | undefined {
  return value as ST_GapAmountPercent;
}

export function parseST_GapAmountUShort(value: string): ST_GapAmountUShort | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_GapAmountUShort);
}

export function parseST_Grouping(value: string): ST_Grouping | undefined {
  const s = collapse(value);
  return ST_Grouping_VALUES.has(s) ? (s as ST_Grouping) : undefined;
}

export function parseST_HPercent(value: string): ST_HPercent | undefined {
  return (parseST_HPercentWithSymbol(value) ?? parseST_HPercentUShort(value)) as ST_HPercent | undefined;
}

export function parseST_HPercentUShort(value: string): ST_HPercentUShort | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_HPercentUShort);
}

export function parseST_HPercentWithSymbol(value: string): ST_HPercentWithSymbol | undefined {
  return value as ST_HPercentWithSymbol;
}

export function parseST_HoleSize(value: string): ST_HoleSize | undefined {
  return (parseST_HoleSizePercent(value) ?? parseST_HoleSizeUByte(value)) as ST_HoleSize | undefined;
}

export function parseST_HoleSizePercent(value: string): ST_HoleSizePercent | undefined {
  return value as ST_HoleSizePercent;
}

export function parseST_HoleSizeUByte(value: string): ST_HoleSizeUByte | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_HoleSizeUByte);
}

export function parseST_LayoutMode(value: string): ST_LayoutMode | undefined {
  const s = collapse(value);
  return ST_LayoutMode_VALUES.has(s) ? (s as ST_LayoutMode) : undefined;
}

export function parseST_LayoutTarget(value: string): ST_LayoutTarget | undefined {
  const s = collapse(value);
  return ST_LayoutTarget_VALUES.has(s) ? (s as ST_LayoutTarget) : undefined;
}

export function parseST_LblAlgn(value: string): ST_LblAlgn | undefined {
  const s = collapse(value);
  return ST_LblAlgn_VALUES.has(s) ? (s as ST_LblAlgn) : undefined;
}

export function parseST_LblOffset(value: string): ST_LblOffset | undefined {
  return (parseST_LblOffsetPercent(value) ?? parseST_LblOffsetUShort(value)) as ST_LblOffset | undefined;
}

export function parseST_LblOffsetPercent(value: string): ST_LblOffsetPercent | undefined {
  return value as ST_LblOffsetPercent;
}

export function parseST_LblOffsetUShort(value: string): ST_LblOffsetUShort | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_LblOffsetUShort);
}

export function parseST_LegendPos(value: string): ST_LegendPos | undefined {
  const s = collapse(value);
  return ST_LegendPos_VALUES.has(s) ? (s as ST_LegendPos) : undefined;
}

export function parseST_LogBase(value: string): ST_LogBase | undefined {
  const n = parseDouble(value);
  return n === undefined ? undefined : (n as ST_LogBase);
}

export function parseST_MarkerSize(value: string): ST_MarkerSize | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_MarkerSize);
}

export function parseST_MarkerStyle(value: string): ST_MarkerStyle | undefined {
  const s = collapse(value);
  return ST_MarkerStyle_VALUES.has(s) ? (s as ST_MarkerStyle) : undefined;
}

export function parseST_OfPieType(value: string): ST_OfPieType | undefined {
  const s = collapse(value);
  return ST_OfPieType_VALUES.has(s) ? (s as ST_OfPieType) : undefined;
}

export function parseST_Order(value: string): ST_Order | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_Order);
}

export function parseST_Orientation(value: string): ST_Orientation | undefined {
  const s = collapse(value);
  return ST_Orientation_VALUES.has(s) ? (s as ST_Orientation) : undefined;
}

export function parseST_Overlap(value: string): ST_Overlap | undefined {
  return (parseST_OverlapPercent(value) ?? parseST_OverlapByte(value)) as ST_Overlap | undefined;
}

export function parseST_OverlapByte(value: string): ST_OverlapByte | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_OverlapByte);
}

export function parseST_OverlapPercent(value: string): ST_OverlapPercent | undefined {
  return value as ST_OverlapPercent;
}

export function parseST_PageSetupOrientation(value: string): ST_PageSetupOrientation | undefined {
  const s = collapse(value);
  return ST_PageSetupOrientation_VALUES.has(s) ? (s as ST_PageSetupOrientation) : undefined;
}

export function parseST_Period(value: string): ST_Period | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_Period);
}

export function parseST_Perspective(value: string): ST_Perspective | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_Perspective);
}

export function parseST_PictureFormat(value: string): ST_PictureFormat | undefined {
  const s = collapse(value);
  return ST_PictureFormat_VALUES.has(s) ? (s as ST_PictureFormat) : undefined;
}

export function parseST_PictureStackUnit(value: string): ST_PictureStackUnit | undefined {
  const n = parseDouble(value);
  return n === undefined ? undefined : (n as ST_PictureStackUnit);
}

export function parseST_RadarStyle(value: string): ST_RadarStyle | undefined {
  const s = collapse(value);
  return ST_RadarStyle_VALUES.has(s) ? (s as ST_RadarStyle) : undefined;
}

export function parseST_RotX(value: string): ST_RotX | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_RotX);
}

export function parseST_RotY(value: string): ST_RotY | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_RotY);
}

export function parseST_ScatterStyle(value: string): ST_ScatterStyle | undefined {
  const s = collapse(value);
  return ST_ScatterStyle_VALUES.has(s) ? (s as ST_ScatterStyle) : undefined;
}

export function parseST_SecondPieSize(value: string): ST_SecondPieSize | undefined {
  return (parseST_SecondPieSizePercent(value) ?? parseST_SecondPieSizeUShort(value)) as ST_SecondPieSize | undefined;
}

export function parseST_SecondPieSizePercent(value: string): ST_SecondPieSizePercent | undefined {
  return value as ST_SecondPieSizePercent;
}

export function parseST_SecondPieSizeUShort(value: string): ST_SecondPieSizeUShort | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_SecondPieSizeUShort);
}

export function parseST_Shape(value: string): ST_Shape | undefined {
  const s = collapse(value);
  return ST_Shape_VALUES.has(s) ? (s as ST_Shape) : undefined;
}

export function parseST_SizeRepresents(value: string): ST_SizeRepresents | undefined {
  const s = collapse(value);
  return ST_SizeRepresents_VALUES.has(s) ? (s as ST_SizeRepresents) : undefined;
}

export function parseST_Skip(value: string): ST_Skip | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_Skip);
}

export function parseST_SplitType(value: string): ST_SplitType | undefined {
  const s = collapse(value);
  return ST_SplitType_VALUES.has(s) ? (s as ST_SplitType) : undefined;
}

export function parseST_Style(value: string): ST_Style | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_Style);
}

export function parseST_Thickness(value: string): ST_Thickness | undefined {
  return (parseST_ThicknessPercent(value) ?? parseInteger(value)) as ST_Thickness | undefined;
}

export function parseST_ThicknessPercent(value: string): ST_ThicknessPercent | undefined {
  return value as ST_ThicknessPercent;
}

export function parseST_TickLblPos(value: string): ST_TickLblPos | undefined {
  const s = collapse(value);
  return ST_TickLblPos_VALUES.has(s) ? (s as ST_TickLblPos) : undefined;
}

export function parseST_TickMark(value: string): ST_TickMark | undefined {
  const s = collapse(value);
  return ST_TickMark_VALUES.has(s) ? (s as ST_TickMark) : undefined;
}

export function parseST_TimeUnit(value: string): ST_TimeUnit | undefined {
  const s = collapse(value);
  return ST_TimeUnit_VALUES.has(s) ? (s as ST_TimeUnit) : undefined;
}

export function parseST_TrendlineType(value: string): ST_TrendlineType | undefined {
  const s = collapse(value);
  return ST_TrendlineType_VALUES.has(s) ? (s as ST_TrendlineType) : undefined;
}

// Complex-type readers.

/** Read a `CT_Area3DChart`. See `transitional/dml-chart.xsd:934`. */
export function readCT_Area3DChart(cur: XmlCursor, ctx: ReadContext): CT_Area3DChart {
  const start = requireStart(cur, 'CT_Area3DChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Area3DChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let grouping: CT_Grouping | undefined;
  let varyColors: CT_Boolean | undefined;
  const ser: CT_AreaSer[] = [];
  let dLbls: CT_DLbls | undefined;
  let dropLines: CT_ChartLines | undefined;
  let gapDepth: CT_GapAmount | undefined;
  const axId: CT_UnsignedInt[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Area3DChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'grouping': {
            grouping = readCT_Grouping(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_AreaSer(cur, ctx));
            $slot = 2;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'dropLines': {
            dropLines = readCT_ChartLines(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'gapDepth': {
            gapDepth = readCT_GapAmount(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'axId': {
            axId.push(readCT_UnsignedInt(cur, ctx));
            $slot = 6;
            $index = axId.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Area3DChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Area3DChart', cur);
    cur.next();
  }

  return {
    grouping,
    varyColors,
    ser,
    dLbls,
    dropLines,
    gapDepth,
    axId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AreaChart`. See `transitional/dml-chart.xsd:927`. */
export function readCT_AreaChart(cur: XmlCursor, ctx: ReadContext): CT_AreaChart {
  const start = requireStart(cur, 'CT_AreaChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AreaChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let grouping: CT_Grouping | undefined;
  let varyColors: CT_Boolean | undefined;
  const ser: CT_AreaSer[] = [];
  let dLbls: CT_DLbls | undefined;
  let dropLines: CT_ChartLines | undefined;
  const axId: CT_UnsignedInt[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AreaChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'grouping': {
            grouping = readCT_Grouping(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_AreaSer(cur, ctx));
            $slot = 2;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'dropLines': {
            dropLines = readCT_ChartLines(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'axId': {
            axId.push(readCT_UnsignedInt(cur, ctx));
            $slot = 5;
            $index = axId.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AreaChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AreaChart', cur);
    cur.next();
  }

  return {
    grouping,
    varyColors,
    ser,
    dLbls,
    dropLines,
    axId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AreaSer`. See `transitional/dml-chart.xsd:712`. */
export function readCT_AreaSer(cur: XmlCursor, ctx: ReadContext): CT_AreaSer {
  const start = requireStart(cur, 'CT_AreaSer');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AreaSer', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let order: CT_UnsignedInt | undefined;
  let tx: CT_SerTx | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let pictureOptions: CT_PictureOptions | undefined;
  const dPt: CT_DPt[] = [];
  let dLbls: CT_DLbls | undefined;
  const trendline: CT_Trendline[] = [];
  const errBars: CT_ErrBars[] = [];
  let cat: CT_AxDataSource | undefined;
  let val: CT_NumDataSource | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AreaSer', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'order': {
            order = readCT_UnsignedInt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tx': {
            tx = readCT_SerTx(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'pictureOptions': {
            pictureOptions = readCT_PictureOptions(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'dPt': {
            dPt.push(readCT_DPt(cur, ctx));
            $slot = 5;
            $index = dPt.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'trendline': {
            trendline.push(readCT_Trendline(cur, ctx));
            $slot = 7;
            $index = trendline.length - 1;
            continue;
          }
          case 'errBars': {
            errBars.push(readCT_ErrBars(cur, ctx));
            $slot = 8;
            $index = errBars.length - 1;
            continue;
          }
          case 'cat': {
            cat = readCT_AxDataSource(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'val': {
            val = readCT_NumDataSource(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AreaSer', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AreaSer', cur);
    cur.next();
  }

  return {
    idx,
    order,
    tx,
    spPr,
    pictureOptions,
    dPt,
    dLbls,
    trendline,
    errBars,
    cat,
    val,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AxDataSource`. See `transitional/dml-chart.xsd:121`. */
export function readCT_AxDataSource(cur: XmlCursor, ctx: ReadContext): CT_AxDataSource {
  const start = requireStart(cur, 'CT_AxDataSource');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AxDataSource', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_AxDataSource_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AxDataSource', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'multiLvlStrRef': {
            content = { kind: 'multiLvlStrRef', value: readCT_MultiLvlStrRef(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'numRef': {
            content = { kind: 'numRef', value: readCT_NumRef(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'numLit': {
            content = { kind: 'numLit', value: readCT_NumData(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'strRef': {
            content = { kind: 'strRef', value: readCT_StrRef(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'strLit': {
            content = { kind: 'strLit', value: readCT_StrData(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AxDataSource', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AxDataSource', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AxPos`. See `transitional/dml-chart.xsd:1045`. */
export function readCT_AxPos(cur: XmlCursor, ctx: ReadContext): CT_AxPos {
  const start = requireStart(cur, 'CT_AxPos');
  let val: ST_AxPos | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_AxPos(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_AxPos', a, 'ST_AxPos', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AxPos', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_AxPos', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AxPos', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AxPos', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AxPos', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_AxisUnit`. See `transitional/dml-chart.xsd:1112`. */
export function readCT_AxisUnit(cur: XmlCursor, ctx: ReadContext): CT_AxisUnit {
  const start = requireStart(cur, 'CT_AxisUnit');
  let val: ST_AxisUnit | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_AxisUnit(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_AxisUnit', a, 'ST_AxisUnit', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AxisUnit', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_AxisUnit', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AxisUnit', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AxisUnit', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AxisUnit', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_BandFmt`. See `transitional/dml-chart.xsd:1005`. */
export function readCT_BandFmt(cur: XmlCursor, ctx: ReadContext): CT_BandFmt {
  const start = requireStart(cur, 'CT_BandFmt');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BandFmt', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BandFmt', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BandFmt', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BandFmt', cur);
    cur.next();
  }

  return {
    idx,
    spPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BandFmts`. See `transitional/dml-chart.xsd:1011`. */
export function readCT_BandFmts(cur: XmlCursor, ctx: ReadContext): CT_BandFmts {
  const start = requireStart(cur, 'CT_BandFmts');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BandFmts', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const bandFmt: CT_BandFmt[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BandFmts', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'bandFmt': {
            bandFmt.push(readCT_BandFmt(cur, ctx));
            $slot = 0;
            $index = bandFmt.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BandFmts', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BandFmts', cur);
    cur.next();
  }

  return {
    bandFmt,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Bar3DChart`. See `transitional/dml-chart.xsd:908`. */
export function readCT_Bar3DChart(cur: XmlCursor, ctx: ReadContext): CT_Bar3DChart {
  const start = requireStart(cur, 'CT_Bar3DChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Bar3DChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let barDir: CT_BarDir | undefined;
  let grouping: CT_BarGrouping | undefined;
  let varyColors: CT_Boolean | undefined;
  const ser: CT_BarSer[] = [];
  let dLbls: CT_DLbls | undefined;
  let gapWidth: CT_GapAmount | undefined;
  let gapDepth: CT_GapAmount | undefined;
  let shape: CT_Shape | undefined;
  const axId: CT_UnsignedInt[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Bar3DChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'barDir': {
            barDir = readCT_BarDir(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'grouping': {
            grouping = readCT_BarGrouping(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_BarSer(cur, ctx));
            $slot = 3;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'gapWidth': {
            gapWidth = readCT_GapAmount(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'gapDepth': {
            gapDepth = readCT_GapAmount(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'shape': {
            shape = readCT_Shape(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'axId': {
            axId.push(readCT_UnsignedInt(cur, ctx));
            $slot = 8;
            $index = axId.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Bar3DChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Bar3DChart', cur);
    cur.next();
  }

  return {
    barDir,
    grouping,
    varyColors,
    ser,
    dLbls,
    gapWidth,
    gapDepth,
    shape,
    axId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BarChart`. See `transitional/dml-chart.xsd:898`. */
export function readCT_BarChart(cur: XmlCursor, ctx: ReadContext): CT_BarChart {
  const start = requireStart(cur, 'CT_BarChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BarChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let barDir: CT_BarDir | undefined;
  let grouping: CT_BarGrouping | undefined;
  let varyColors: CT_Boolean | undefined;
  const ser: CT_BarSer[] = [];
  let dLbls: CT_DLbls | undefined;
  let gapWidth: CT_GapAmount | undefined;
  let overlap: CT_Overlap | undefined;
  const serLines: CT_ChartLines[] = [];
  const axId: CT_UnsignedInt[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BarChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'barDir': {
            barDir = readCT_BarDir(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'grouping': {
            grouping = readCT_BarGrouping(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_BarSer(cur, ctx));
            $slot = 3;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'gapWidth': {
            gapWidth = readCT_GapAmount(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'overlap': {
            overlap = readCT_Overlap(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'serLines': {
            serLines.push(readCT_ChartLines(cur, ctx));
            $slot = 7;
            $index = serLines.length - 1;
            continue;
          }
          case 'axId': {
            axId.push(readCT_UnsignedInt(cur, ctx));
            $slot = 8;
            $index = axId.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BarChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BarChart', cur);
    cur.next();
  }

  return {
    barDir,
    grouping,
    varyColors,
    ser,
    dLbls,
    gapWidth,
    overlap,
    serLines,
    axId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BarDir`. See `transitional/dml-chart.xsd:873`. */
export function readCT_BarDir(cur: XmlCursor, ctx: ReadContext): CT_BarDir {
  const start = requireStart(cur, 'CT_BarDir');
  let val: ST_BarDir | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_BarDir(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_BarDir', a, 'ST_BarDir', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BarDir', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BarDir', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_BarDir', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BarDir', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_BarGrouping`. See `transitional/dml-chart.xsd:864`. */
export function readCT_BarGrouping(cur: XmlCursor, ctx: ReadContext): CT_BarGrouping {
  const start = requireStart(cur, 'CT_BarGrouping');
  let val: ST_BarGrouping | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_BarGrouping(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_BarGrouping', a, 'ST_BarGrouping', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BarGrouping', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BarGrouping', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_BarGrouping', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BarGrouping', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_BarSer`. See `transitional/dml-chart.xsd:697`. */
export function readCT_BarSer(cur: XmlCursor, ctx: ReadContext): CT_BarSer {
  const start = requireStart(cur, 'CT_BarSer');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BarSer', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let order: CT_UnsignedInt | undefined;
  let tx: CT_SerTx | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let invertIfNegative: CT_Boolean | undefined;
  let pictureOptions: CT_PictureOptions | undefined;
  const dPt: CT_DPt[] = [];
  let dLbls: CT_DLbls | undefined;
  const trendline: CT_Trendline[] = [];
  let errBars: CT_ErrBars | undefined;
  let cat: CT_AxDataSource | undefined;
  let val: CT_NumDataSource | undefined;
  let shape: CT_Shape | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BarSer', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'order': {
            order = readCT_UnsignedInt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tx': {
            tx = readCT_SerTx(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'invertIfNegative': {
            invertIfNegative = readCT_Boolean(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'pictureOptions': {
            pictureOptions = readCT_PictureOptions(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'dPt': {
            dPt.push(readCT_DPt(cur, ctx));
            $slot = 6;
            $index = dPt.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'trendline': {
            trendline.push(readCT_Trendline(cur, ctx));
            $slot = 8;
            $index = trendline.length - 1;
            continue;
          }
          case 'errBars': {
            errBars = readCT_ErrBars(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'cat': {
            cat = readCT_AxDataSource(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'val': {
            val = readCT_NumDataSource(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'shape': {
            shape = readCT_Shape(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BarSer', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BarSer', cur);
    cur.next();
  }

  return {
    idx,
    order,
    tx,
    spPr,
    invertIfNegative,
    pictureOptions,
    dPt,
    dLbls,
    trendline,
    errBars,
    cat,
    val,
    shape,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Boolean`. See `transitional/dml-chart.xsd:18`. */
export function readCT_Boolean(cur: XmlCursor, ctx: ReadContext): CT_Boolean {
  const start = requireStart(cur, 'CT_Boolean');
  let val: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Boolean', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Boolean', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Boolean', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Boolean', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Boolean', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_BubbleChart`. See `transitional/dml-chart.xsd:992`. */
export function readCT_BubbleChart(cur: XmlCursor, ctx: ReadContext): CT_BubbleChart {
  const start = requireStart(cur, 'CT_BubbleChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BubbleChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let varyColors: CT_Boolean | undefined;
  const ser: CT_BubbleSer[] = [];
  let dLbls: CT_DLbls | undefined;
  let bubble3D: CT_Boolean | undefined;
  let bubbleScale: CT_BubbleScale | undefined;
  let showNegBubbles: CT_Boolean | undefined;
  let sizeRepresents: CT_SizeRepresents | undefined;
  const axId: CT_UnsignedInt[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BubbleChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_BubbleSer(cur, ctx));
            $slot = 1;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'bubble3D': {
            bubble3D = readCT_Boolean(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'bubbleScale': {
            bubbleScale = readCT_BubbleScale(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'showNegBubbles': {
            showNegBubbles = readCT_Boolean(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'sizeRepresents': {
            sizeRepresents = readCT_SizeRepresents(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'axId': {
            axId.push(readCT_UnsignedInt(cur, ctx));
            $slot = 7;
            $index = axId.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BubbleChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BubbleChart', cur);
    cur.next();
  }

  return {
    varyColors,
    ser,
    dLbls,
    bubble3D,
    bubbleScale,
    showNegBubbles,
    sizeRepresents,
    axId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BubbleScale`. See `transitional/dml-chart.xsd:338`. */
export function readCT_BubbleScale(cur: XmlCursor, ctx: ReadContext): CT_BubbleScale {
  const start = requireStart(cur, 'CT_BubbleScale');
  let val: ST_BubbleScale | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_BubbleScale(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_BubbleScale', a, 'ST_BubbleScale', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BubbleScale', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BubbleScale', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_BubbleScale', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BubbleScale', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_BubbleSer`. See `transitional/dml-chart.xsd:736`. */
export function readCT_BubbleSer(cur: XmlCursor, ctx: ReadContext): CT_BubbleSer {
  const start = requireStart(cur, 'CT_BubbleSer');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BubbleSer', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let order: CT_UnsignedInt | undefined;
  let tx: CT_SerTx | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let invertIfNegative: CT_Boolean | undefined;
  const dPt: CT_DPt[] = [];
  let dLbls: CT_DLbls | undefined;
  const trendline: CT_Trendline[] = [];
  const errBars: CT_ErrBars[] = [];
  let xVal: CT_AxDataSource | undefined;
  let yVal: CT_NumDataSource | undefined;
  let bubbleSize: CT_NumDataSource | undefined;
  let bubble3D: CT_Boolean | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BubbleSer', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'order': {
            order = readCT_UnsignedInt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tx': {
            tx = readCT_SerTx(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'invertIfNegative': {
            invertIfNegative = readCT_Boolean(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'dPt': {
            dPt.push(readCT_DPt(cur, ctx));
            $slot = 5;
            $index = dPt.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'trendline': {
            trendline.push(readCT_Trendline(cur, ctx));
            $slot = 7;
            $index = trendline.length - 1;
            continue;
          }
          case 'errBars': {
            errBars.push(readCT_ErrBars(cur, ctx));
            $slot = 8;
            $index = errBars.length - 1;
            continue;
          }
          case 'xVal': {
            xVal = readCT_AxDataSource(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'yVal': {
            yVal = readCT_NumDataSource(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'bubbleSize': {
            bubbleSize = readCT_NumDataSource(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'bubble3D': {
            bubble3D = readCT_Boolean(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BubbleSer', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BubbleSer', cur);
    cur.next();
  }

  return {
    idx,
    order,
    tx,
    spPr,
    invertIfNegative,
    dPt,
    dLbls,
    trendline,
    errBars,
    xVal,
    yVal,
    bubbleSize,
    bubble3D,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BuiltInUnit`. See `transitional/dml-chart.xsd:1128`. */
export function readCT_BuiltInUnit(cur: XmlCursor, ctx: ReadContext): CT_BuiltInUnit {
  const start = requireStart(cur, 'CT_BuiltInUnit');
  let val: ST_BuiltInUnit | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_BuiltInUnit(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_BuiltInUnit', a, 'ST_BuiltInUnit', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BuiltInUnit', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BuiltInUnit', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_BuiltInUnit', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BuiltInUnit', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_CatAx`. See `transitional/dml-chart.xsd:1242`. */
export function readCT_CatAx(cur: XmlCursor, ctx: ReadContext): CT_CatAx {
  const start = requireStart(cur, 'CT_CatAx');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_CatAx', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let axId: CT_UnsignedInt | undefined;
  let scaling: CT_Scaling | undefined;
  let delete_: CT_Boolean | undefined;
  let axPos: CT_AxPos | undefined;
  let majorGridlines: CT_ChartLines | undefined;
  let minorGridlines: CT_ChartLines | undefined;
  let title: CT_Title | undefined;
  let numFmt: CT_NumFmt | undefined;
  let majorTickMark: CT_TickMark | undefined;
  let minorTickMark: CT_TickMark | undefined;
  let tickLblPos: CT_TickLblPos | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let txPr: CT_TextBody | undefined;
  let crossAx: CT_UnsignedInt | undefined;
  let content: CT_CatAx_Content | undefined;
  let auto: CT_Boolean | undefined;
  let lblAlgn: CT_LblAlgn | undefined;
  let lblOffset: CT_LblOffset | undefined;
  let tickLblSkip: CT_Skip | undefined;
  let tickMarkSkip: CT_Skip | undefined;
  let noMultiLvlLbl: CT_Boolean | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CatAx', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'axId': {
            axId = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'scaling': {
            scaling = readCT_Scaling(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'delete': {
            delete_ = readCT_Boolean(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'axPos': {
            axPos = readCT_AxPos(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'majorGridlines': {
            majorGridlines = readCT_ChartLines(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'minorGridlines': {
            minorGridlines = readCT_ChartLines(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'title': {
            title = readCT_Title(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'numFmt': {
            numFmt = readCT_NumFmt(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'majorTickMark': {
            majorTickMark = readCT_TickMark(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'minorTickMark': {
            minorTickMark = readCT_TickMark(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tickLblPos': {
            tickLblPos = readCT_TickLblPos(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            txPr = readCT_TextBody(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'crossAx': {
            crossAx = readCT_UnsignedInt(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'crosses': {
            content = { kind: 'crosses', value: readCT_Crosses(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'crossesAt': {
            content = { kind: 'crossesAt', value: readCT_Double(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'auto': {
            auto = readCT_Boolean(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'lblAlgn': {
            lblAlgn = readCT_LblAlgn(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'lblOffset': {
            lblOffset = readCT_LblOffset(cur, ctx);
            $slot = 17;
            $index = undefined;
            continue;
          }
          case 'tickLblSkip': {
            tickLblSkip = readCT_Skip(cur, ctx);
            $slot = 18;
            $index = undefined;
            continue;
          }
          case 'tickMarkSkip': {
            tickMarkSkip = readCT_Skip(cur, ctx);
            $slot = 19;
            $index = undefined;
            continue;
          }
          case 'noMultiLvlLbl': {
            noMultiLvlLbl = readCT_Boolean(cur, ctx);
            $slot = 20;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 21;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CatAx', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CatAx', cur);
    cur.next();
  }

  return {
    axId,
    scaling,
    delete: delete_,
    axPos,
    majorGridlines,
    minorGridlines,
    title,
    numFmt,
    majorTickMark,
    minorTickMark,
    tickLblPos,
    spPr,
    txPr,
    crossAx,
    content,
    auto,
    lblAlgn,
    lblOffset,
    tickLblSkip,
    tickMarkSkip,
    noMultiLvlLbl,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Chart`. See `transitional/dml-chart.xsd:1380`. */
export function readCT_Chart(cur: XmlCursor, ctx: ReadContext): CT_Chart {
  const start = requireStart(cur, 'CT_Chart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Chart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let title: CT_Title | undefined;
  let autoTitleDeleted: CT_Boolean | undefined;
  let pivotFmts: CT_PivotFmts | undefined;
  let view3D: CT_View3D | undefined;
  let floor: CT_Surface | undefined;
  let sideWall: CT_Surface | undefined;
  let backWall: CT_Surface | undefined;
  let plotArea: CT_PlotArea | undefined;
  let legend: CT_Legend | undefined;
  let plotVisOnly: CT_Boolean | undefined;
  let dispBlanksAs: CT_DispBlanksAs | undefined;
  let showDLblsOverMax: CT_Boolean | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Chart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'title': {
            title = readCT_Title(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'autoTitleDeleted': {
            autoTitleDeleted = readCT_Boolean(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'pivotFmts': {
            pivotFmts = readCT_PivotFmts(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'view3D': {
            view3D = readCT_View3D(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'floor': {
            floor = readCT_Surface(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'sideWall': {
            sideWall = readCT_Surface(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'backWall': {
            backWall = readCT_Surface(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'plotArea': {
            plotArea = readCT_PlotArea(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'legend': {
            legend = readCT_Legend(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'plotVisOnly': {
            plotVisOnly = readCT_Boolean(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'dispBlanksAs': {
            dispBlanksAs = readCT_DispBlanksAs(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'showDLblsOverMax': {
            showDLblsOverMax = readCT_Boolean(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Chart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Chart', cur);
    cur.next();
  }

  return {
    title,
    autoTitleDeleted,
    pivotFmts,
    view3D,
    floor,
    sideWall,
    backWall,
    plotArea,
    legend,
    plotVisOnly,
    dispBlanksAs,
    showDLblsOverMax,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ChartLines`. See `transitional/dml-chart.xsd:769`. */
export function readCT_ChartLines(cur: XmlCursor, ctx: ReadContext): CT_ChartLines {
  const start = requireStart(cur, 'CT_ChartLines');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ChartLines', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let spPr: CT_ShapeProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ChartLines', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ChartLines', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ChartLines', cur);
    cur.next();
  }

  return {
    spPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ChartSpace`. See `transitional/dml-chart.xsd:1478`. */
export function readCT_ChartSpace(cur: XmlCursor, ctx: ReadContext): CT_ChartSpace {
  const start = requireStart(cur, 'CT_ChartSpace');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ChartSpace', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let date1904: CT_Boolean | undefined;
  let lang: CT_TextLanguageID | undefined;
  let roundedCorners: CT_Boolean | undefined;
  let style: CT_Style | undefined;
  let clrMapOvr: CT_ColorMapping | undefined;
  let pivotSource: CT_PivotSource | undefined;
  let protection: CT_Protection | undefined;
  let chart: CT_Chart | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let txPr: CT_TextBody | undefined;
  let externalData: CT_ExternalData | undefined;
  let printSettings: CT_PrintSettings | undefined;
  let userShapes: CT_RelId | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ChartSpace', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'date1904': {
            date1904 = readCT_Boolean(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lang': {
            lang = readCT_TextLanguageID(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'roundedCorners': {
            roundedCorners = readCT_Boolean(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_Style(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'clrMapOvr': {
            clrMapOvr = readCT_ColorMapping(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'pivotSource': {
            pivotSource = readCT_PivotSource(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'protection': {
            protection = readCT_Protection(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'chart': {
            chart = readCT_Chart(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            txPr = readCT_TextBody(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'externalData': {
            externalData = readCT_ExternalData(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'printSettings': {
            printSettings = readCT_PrintSettings(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'userShapes': {
            userShapes = readCT_RelId(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ChartSpace', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ChartSpace', cur);
    cur.next();
  }

  return {
    date1904,
    lang,
    roundedCorners,
    style,
    clrMapOvr,
    pivotSource,
    protection,
    chart,
    spPr,
    txPr,
    externalData,
    printSettings,
    userShapes,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CrossBetween`. See `transitional/dml-chart.xsd:1064`. */
export function readCT_CrossBetween(cur: XmlCursor, ctx: ReadContext): CT_CrossBetween {
  const start = requireStart(cur, 'CT_CrossBetween');
  let val: ST_CrossBetween | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_CrossBetween(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_CrossBetween', a, 'ST_CrossBetween', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_CrossBetween', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_CrossBetween', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CrossBetween', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_CrossBetween', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CrossBetween', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Crosses`. See `transitional/dml-chart.xsd:1055`. */
export function readCT_Crosses(cur: XmlCursor, ctx: ReadContext): CT_Crosses {
  const start = requireStart(cur, 'CT_Crosses');
  let val: ST_Crosses | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Crosses(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Crosses', a, 'ST_Crosses', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Crosses', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Crosses', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Crosses', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Crosses', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Crosses', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_CustSplit`. See `transitional/dml-chart.xsd:388`. */
export function readCT_CustSplit(cur: XmlCursor, ctx: ReadContext): CT_CustSplit {
  const start = requireStart(cur, 'CT_CustSplit');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_CustSplit', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const secondPiePt: CT_UnsignedInt[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustSplit', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'secondPiePt': {
            secondPiePt.push(readCT_UnsignedInt(cur, ctx));
            $slot = 0;
            $index = secondPiePt.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CustSplit', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustSplit', cur);
    cur.next();
  }

  return {
    secondPiePt,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DLbl`. See `transitional/dml-chart.xsd:462`. */
export function readCT_DLbl(cur: XmlCursor, ctx: ReadContext): CT_DLbl {
  const start = requireStart(cur, 'CT_DLbl');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DLbl', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let content: CT_DLbl_Content | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DLbl', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'delete': {
            content = { kind: 'delete', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'layout': {
            content = { kind: 'layout', value: readCT_Layout(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tx': {
            content = { kind: 'tx', value: readCT_Tx(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'numFmt': {
            content = { kind: 'numFmt', value: readCT_NumFmt(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            content = { kind: 'spPr', value: readCT_ShapeProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            content = { kind: 'txPr', value: readCT_TextBody(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'dLblPos': {
            content = { kind: 'dLblPos', value: readCT_DLblPos(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showLegendKey': {
            content = { kind: 'showLegendKey', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showVal': {
            content = { kind: 'showVal', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showCatName': {
            content = { kind: 'showCatName', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showSerName': {
            content = { kind: 'showSerName', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showPercent': {
            content = { kind: 'showPercent', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showBubbleSize': {
            content = { kind: 'showBubbleSize', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'separator': {
            content = { kind: 'separator', value: readScalar(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DLbl', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DLbl', cur);
    cur.next();
  }

  return {
    idx,
    content,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DLblPos`. See `transitional/dml-chart.xsd:437`. */
export function readCT_DLblPos(cur: XmlCursor, ctx: ReadContext): CT_DLblPos {
  const start = requireStart(cur, 'CT_DLblPos');
  let val: ST_DLblPos | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_DLblPos(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_DLblPos', a, 'ST_DLblPos', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DLblPos', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_DLblPos', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DLblPos', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DLblPos', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DLblPos', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_DLbls`. See `transitional/dml-chart.xsd:479`. */
export function readCT_DLbls(cur: XmlCursor, ctx: ReadContext): CT_DLbls {
  const start = requireStart(cur, 'CT_DLbls');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DLbls', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const dLbl: CT_DLbl[] = [];
  let content: CT_DLbls_Content | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DLbls', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'dLbl': {
            dLbl.push(readCT_DLbl(cur, ctx));
            $slot = 0;
            $index = dLbl.length - 1;
            continue;
          }
          case 'delete': {
            content = { kind: 'delete', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'numFmt': {
            content = { kind: 'numFmt', value: readCT_NumFmt(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            content = { kind: 'spPr', value: readCT_ShapeProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            content = { kind: 'txPr', value: readCT_TextBody(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'dLblPos': {
            content = { kind: 'dLblPos', value: readCT_DLblPos(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showLegendKey': {
            content = { kind: 'showLegendKey', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showVal': {
            content = { kind: 'showVal', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showCatName': {
            content = { kind: 'showCatName', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showSerName': {
            content = { kind: 'showSerName', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showPercent': {
            content = { kind: 'showPercent', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showBubbleSize': {
            content = { kind: 'showBubbleSize', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'separator': {
            content = { kind: 'separator', value: readScalar(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showLeaderLines': {
            content = { kind: 'showLeaderLines', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'leaderLines': {
            content = { kind: 'leaderLines', value: readCT_ChartLines(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DLbls', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DLbls', cur);
    cur.next();
  }

  return {
    dLbl,
    content,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DPt`. See `transitional/dml-chart.xsd:525`. */
export function readCT_DPt(cur: XmlCursor, ctx: ReadContext): CT_DPt {
  const start = requireStart(cur, 'CT_DPt');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DPt', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let invertIfNegative: CT_Boolean | undefined;
  let marker: CT_Marker | undefined;
  let bubble3D: CT_Boolean | undefined;
  let explosion: CT_UnsignedInt | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let pictureOptions: CT_PictureOptions | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DPt', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'invertIfNegative': {
            invertIfNegative = readCT_Boolean(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'marker': {
            marker = readCT_Marker(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'bubble3D': {
            bubble3D = readCT_Boolean(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'explosion': {
            explosion = readCT_UnsignedInt(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'pictureOptions': {
            pictureOptions = readCT_PictureOptions(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DPt', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DPt', cur);
    cur.next();
  }

  return {
    idx,
    invertIfNegative,
    marker,
    bubble3D,
    explosion,
    spPr,
    pictureOptions,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DTable`. See `transitional/dml-chart.xsd:279`. */
export function readCT_DTable(cur: XmlCursor, ctx: ReadContext): CT_DTable {
  const start = requireStart(cur, 'CT_DTable');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DTable', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let showHorzBorder: CT_Boolean | undefined;
  let showVertBorder: CT_Boolean | undefined;
  let showOutline: CT_Boolean | undefined;
  let showKeys: CT_Boolean | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let txPr: CT_TextBody | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DTable', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'showHorzBorder': {
            showHorzBorder = readCT_Boolean(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'showVertBorder': {
            showVertBorder = readCT_Boolean(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'showOutline': {
            showOutline = readCT_Boolean(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'showKeys': {
            showKeys = readCT_Boolean(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            txPr = readCT_TextBody(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DTable', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DTable', cur);
    cur.next();
  }

  return {
    showHorzBorder,
    showVertBorder,
    showOutline,
    showKeys,
    spPr,
    txPr,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DateAx`. See `transitional/dml-chart.xsd:1254`. */
export function readCT_DateAx(cur: XmlCursor, ctx: ReadContext): CT_DateAx {
  const start = requireStart(cur, 'CT_DateAx');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DateAx', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let axId: CT_UnsignedInt | undefined;
  let scaling: CT_Scaling | undefined;
  let delete_: CT_Boolean | undefined;
  let axPos: CT_AxPos | undefined;
  let majorGridlines: CT_ChartLines | undefined;
  let minorGridlines: CT_ChartLines | undefined;
  let title: CT_Title | undefined;
  let numFmt: CT_NumFmt | undefined;
  let majorTickMark: CT_TickMark | undefined;
  let minorTickMark: CT_TickMark | undefined;
  let tickLblPos: CT_TickLblPos | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let txPr: CT_TextBody | undefined;
  let crossAx: CT_UnsignedInt | undefined;
  let content: CT_DateAx_Content | undefined;
  let auto: CT_Boolean | undefined;
  let lblOffset: CT_LblOffset | undefined;
  let baseTimeUnit: CT_TimeUnit | undefined;
  let majorUnit: CT_AxisUnit | undefined;
  let majorTimeUnit: CT_TimeUnit | undefined;
  let minorUnit: CT_AxisUnit | undefined;
  let minorTimeUnit: CT_TimeUnit | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DateAx', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'axId': {
            axId = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'scaling': {
            scaling = readCT_Scaling(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'delete': {
            delete_ = readCT_Boolean(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'axPos': {
            axPos = readCT_AxPos(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'majorGridlines': {
            majorGridlines = readCT_ChartLines(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'minorGridlines': {
            minorGridlines = readCT_ChartLines(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'title': {
            title = readCT_Title(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'numFmt': {
            numFmt = readCT_NumFmt(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'majorTickMark': {
            majorTickMark = readCT_TickMark(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'minorTickMark': {
            minorTickMark = readCT_TickMark(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tickLblPos': {
            tickLblPos = readCT_TickLblPos(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            txPr = readCT_TextBody(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'crossAx': {
            crossAx = readCT_UnsignedInt(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'crosses': {
            content = { kind: 'crosses', value: readCT_Crosses(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'crossesAt': {
            content = { kind: 'crossesAt', value: readCT_Double(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'auto': {
            auto = readCT_Boolean(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'lblOffset': {
            lblOffset = readCT_LblOffset(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'baseTimeUnit': {
            baseTimeUnit = readCT_TimeUnit(cur, ctx);
            $slot = 17;
            $index = undefined;
            continue;
          }
          case 'majorUnit': {
            majorUnit = readCT_AxisUnit(cur, ctx);
            $slot = 18;
            $index = undefined;
            continue;
          }
          case 'majorTimeUnit': {
            majorTimeUnit = readCT_TimeUnit(cur, ctx);
            $slot = 19;
            $index = undefined;
            continue;
          }
          case 'minorUnit': {
            minorUnit = readCT_AxisUnit(cur, ctx);
            $slot = 20;
            $index = undefined;
            continue;
          }
          case 'minorTimeUnit': {
            minorTimeUnit = readCT_TimeUnit(cur, ctx);
            $slot = 21;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 22;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DateAx', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DateAx', cur);
    cur.next();
  }

  return {
    axId,
    scaling,
    delete: delete_,
    axPos,
    majorGridlines,
    minorGridlines,
    title,
    numFmt,
    majorTickMark,
    minorTickMark,
    tickLblPos,
    spPr,
    txPr,
    crossAx,
    content,
    auto,
    lblOffset,
    baseTimeUnit,
    majorUnit,
    majorTimeUnit,
    minorUnit,
    minorTimeUnit,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DepthPercent`. See `transitional/dml-chart.xsd:237`. */
export function readCT_DepthPercent(cur: XmlCursor, ctx: ReadContext): CT_DepthPercent {
  const start = requireStart(cur, 'CT_DepthPercent');
  let val: ST_DepthPercent | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_DepthPercent(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_DepthPercent', a, 'ST_DepthPercent', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DepthPercent', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DepthPercent', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DepthPercent', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DepthPercent', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_DispBlanksAs`. See `transitional/dml-chart.xsd:1377`. */
export function readCT_DispBlanksAs(cur: XmlCursor, ctx: ReadContext): CT_DispBlanksAs {
  const start = requireStart(cur, 'CT_DispBlanksAs');
  let val: ST_DispBlanksAs | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_DispBlanksAs(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_DispBlanksAs', a, 'ST_DispBlanksAs', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DispBlanksAs', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DispBlanksAs', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DispBlanksAs', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DispBlanksAs', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_DispUnits`. See `transitional/dml-chart.xsd:1166`. */
export function readCT_DispUnits(cur: XmlCursor, ctx: ReadContext): CT_DispUnits {
  const start = requireStart(cur, 'CT_DispUnits');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DispUnits', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_DispUnits_Content | undefined;
  let dispUnitsLbl: CT_DispUnitsLbl | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DispUnits', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'custUnit': {
            content = { kind: 'custUnit', value: readCT_Double(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'builtInUnit': {
            content = { kind: 'builtInUnit', value: readCT_BuiltInUnit(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'dispUnitsLbl': {
            dispUnitsLbl = readCT_DispUnitsLbl(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DispUnits', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DispUnits', cur);
    cur.next();
  }

  return {
    content,
    dispUnitsLbl,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DispUnitsLbl`. See `transitional/dml-chart.xsd:1158`. */
export function readCT_DispUnitsLbl(cur: XmlCursor, ctx: ReadContext): CT_DispUnitsLbl {
  const start = requireStart(cur, 'CT_DispUnitsLbl');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DispUnitsLbl', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let layout: CT_Layout | undefined;
  let tx: CT_Tx | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let txPr: CT_TextBody | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DispUnitsLbl', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'layout': {
            layout = readCT_Layout(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tx': {
            tx = readCT_Tx(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            txPr = readCT_TextBody(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DispUnitsLbl', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DispUnitsLbl', cur);
    cur.next();
  }

  return {
    layout,
    tx,
    spPr,
    txPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Double`. See `transitional/dml-chart.xsd:21`. */
export function readCT_Double(cur: XmlCursor, ctx: ReadContext): CT_Double {
  const start = requireStart(cur, 'CT_Double');
  let val: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Double', a, 'xsd:double', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Double', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Double', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Double', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Double', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Double', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_DoughnutChart`. See `transitional/dml-chart.xsd:962`. */
export function readCT_DoughnutChart(cur: XmlCursor, ctx: ReadContext): CT_DoughnutChart {
  const start = requireStart(cur, 'CT_DoughnutChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DoughnutChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let varyColors: CT_Boolean | undefined;
  const ser: CT_PieSer[] = [];
  let dLbls: CT_DLbls | undefined;
  let firstSliceAng: CT_FirstSliceAng | undefined;
  let holeSize: CT_HoleSize | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DoughnutChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_PieSer(cur, ctx));
            $slot = 1;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'firstSliceAng': {
            firstSliceAng = readCT_FirstSliceAng(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'holeSize': {
            holeSize = readCT_HoleSize(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DoughnutChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DoughnutChart', cur);
    cur.next();
  }

  return {
    varyColors,
    ser,
    dLbls,
    firstSliceAng,
    holeSize,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ErrBarType`. See `transitional/dml-chart.xsd:609`. */
export function readCT_ErrBarType(cur: XmlCursor, ctx: ReadContext): CT_ErrBarType {
  const start = requireStart(cur, 'CT_ErrBarType');
  let val: ST_ErrBarType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_ErrBarType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_ErrBarType', a, 'ST_ErrBarType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ErrBarType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ErrBarType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ErrBarType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ErrBarType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_ErrBars`. See `transitional/dml-chart.xsd:624`. */
export function readCT_ErrBars(cur: XmlCursor, ctx: ReadContext): CT_ErrBars {
  const start = requireStart(cur, 'CT_ErrBars');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ErrBars', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let errDir: CT_ErrDir | undefined;
  let errBarType: CT_ErrBarType | undefined;
  let errValType: CT_ErrValType | undefined;
  let noEndCap: CT_Boolean | undefined;
  let plus: CT_NumDataSource | undefined;
  let minus: CT_NumDataSource | undefined;
  let val: CT_Double | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ErrBars', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'errDir': {
            errDir = readCT_ErrDir(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'errBarType': {
            errBarType = readCT_ErrBarType(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'errValType': {
            errValType = readCT_ErrValType(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'noEndCap': {
            noEndCap = readCT_Boolean(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'plus': {
            plus = readCT_NumDataSource(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'minus': {
            minus = readCT_NumDataSource(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'val': {
            val = readCT_Double(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ErrBars', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ErrBars', cur);
    cur.next();
  }

  return {
    errDir,
    errBarType,
    errValType,
    noEndCap,
    plus,
    minus,
    val,
    spPr,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ErrDir`. See `transitional/dml-chart.xsd:599`. */
export function readCT_ErrDir(cur: XmlCursor, ctx: ReadContext): CT_ErrDir {
  const start = requireStart(cur, 'CT_ErrDir');
  let val: ST_ErrDir | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_ErrDir(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_ErrDir', a, 'ST_ErrDir', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ErrDir', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_ErrDir', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ErrDir', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ErrDir', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ErrDir', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_ErrValType`. See `transitional/dml-chart.xsd:621`. */
export function readCT_ErrValType(cur: XmlCursor, ctx: ReadContext): CT_ErrValType {
  const start = requireStart(cur, 'CT_ErrValType');
  let val: ST_ErrValType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_ErrValType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_ErrValType', a, 'ST_ErrValType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ErrValType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ErrValType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ErrValType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ErrValType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Extension`. See `transitional/dml-chart.xsd:30`. */
export function readCT_Extension(cur: XmlCursor, ctx: ReadContext): CT_Extension {
  const start = requireStart(cur, 'CT_Extension');
  let uri: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'uri':
          uri = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Extension', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const any: RawNode[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Extension', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      any.push(cur.skipToRaw());
      $slot = 0;
      $index = any.length - 1;
      continue;
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Extension', cur);
    cur.next();
  }

  return {
    any,
    uri,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ExtensionList`. See `transitional/dml-chart.xsd:36`. */
export function readCT_ExtensionList(cur: XmlCursor, ctx: ReadContext): CT_ExtensionList {
  const start = requireStart(cur, 'CT_ExtensionList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ExtensionList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const ext: CT_Extension[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ExtensionList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'ext': {
            ext.push(readCT_Extension(cur, ctx));
            $slot = 0;
            $index = ext.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ExtensionList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ExtensionList', cur);
    cur.next();
  }

  return {
    ext,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ExternalData`. See `transitional/dml-chart.xsd:1450`. */
export function readCT_ExternalData(cur: XmlCursor, ctx: ReadContext): CT_ExternalData {
  const start = requireStart(cur, 'CT_ExternalData');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_ExternalData', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ExternalData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_ExternalData', 'id', cur);

  let autoUpdate: CT_Boolean | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ExternalData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'autoUpdate': {
            autoUpdate = readCT_Boolean(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ExternalData', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ExternalData', cur);
    cur.next();
  }

  return {
    autoUpdate,
    id,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FirstSliceAng`. See `transitional/dml-chart.xsd:356`. */
export function readCT_FirstSliceAng(cur: XmlCursor, ctx: ReadContext): CT_FirstSliceAng {
  const start = requireStart(cur, 'CT_FirstSliceAng');
  let val: ST_FirstSliceAng | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_FirstSliceAng(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_FirstSliceAng', a, 'ST_FirstSliceAng', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FirstSliceAng', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FirstSliceAng', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FirstSliceAng', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FirstSliceAng', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_GapAmount`. See `transitional/dml-chart.xsd:304`. */
export function readCT_GapAmount(cur: XmlCursor, ctx: ReadContext): CT_GapAmount {
  const start = requireStart(cur, 'CT_GapAmount');
  let val: ST_GapAmount | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_GapAmount(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_GapAmount', a, 'ST_GapAmount', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GapAmount', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GapAmount', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_GapAmount', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GapAmount', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Grouping`. See `transitional/dml-chart.xsd:766`. */
export function readCT_Grouping(cur: XmlCursor, ctx: ReadContext): CT_Grouping {
  const start = requireStart(cur, 'CT_Grouping');
  let val: ST_Grouping | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Grouping(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Grouping', a, 'ST_Grouping', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Grouping', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Grouping', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Grouping', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Grouping', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_HPercent`. See `transitional/dml-chart.xsd:211`. */
export function readCT_HPercent(cur: XmlCursor, ctx: ReadContext): CT_HPercent {
  const start = requireStart(cur, 'CT_HPercent');
  let val: ST_HPercent | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_HPercent(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_HPercent', a, 'ST_HPercent', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_HPercent', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HPercent', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_HPercent', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HPercent', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_HeaderFooter`. See `transitional/dml-chart.xsd:1422`. */
export function readCT_HeaderFooter(cur: XmlCursor, ctx: ReadContext): CT_HeaderFooter {
  const start = requireStart(cur, 'CT_HeaderFooter');
  let alignWithMargins: boolean | undefined;
  let differentOddEven: boolean | undefined;
  let differentFirst: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'alignWithMargins': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            alignWithMargins = v;
            continue;
          }
          ctx.invalidValue('CT_HeaderFooter', a, 'xsd:boolean', cur);
          break;
        }
        case 'differentOddEven': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            differentOddEven = v;
            continue;
          }
          ctx.invalidValue('CT_HeaderFooter', a, 'xsd:boolean', cur);
          break;
        }
        case 'differentFirst': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            differentFirst = v;
            continue;
          }
          ctx.invalidValue('CT_HeaderFooter', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_HeaderFooter', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let oddHeader: ST_Xstring | undefined;
  let oddFooter: ST_Xstring | undefined;
  let evenHeader: ST_Xstring | undefined;
  let evenFooter: ST_Xstring | undefined;
  let firstHeader: ST_Xstring | undefined;
  let firstFooter: ST_Xstring | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HeaderFooter', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'oddHeader': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Xstring($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_HeaderFooter', 'oddHeader', 'ST_Xstring', $s, cur);
            oddHeader = ($pv ?? $s) as ST_Xstring;
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'oddFooter': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Xstring($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_HeaderFooter', 'oddFooter', 'ST_Xstring', $s, cur);
            oddFooter = ($pv ?? $s) as ST_Xstring;
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'evenHeader': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Xstring($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_HeaderFooter', 'evenHeader', 'ST_Xstring', $s, cur);
            evenHeader = ($pv ?? $s) as ST_Xstring;
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'evenFooter': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Xstring($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_HeaderFooter', 'evenFooter', 'ST_Xstring', $s, cur);
            evenFooter = ($pv ?? $s) as ST_Xstring;
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'firstHeader': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Xstring($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_HeaderFooter', 'firstHeader', 'ST_Xstring', $s, cur);
            firstHeader = ($pv ?? $s) as ST_Xstring;
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'firstFooter': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Xstring($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_HeaderFooter', 'firstFooter', 'ST_Xstring', $s, cur);
            firstFooter = ($pv ?? $s) as ST_Xstring;
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_HeaderFooter', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HeaderFooter', cur);
    cur.next();
  }

  return {
    oddHeader,
    oddFooter,
    evenHeader,
    evenFooter,
    firstHeader,
    firstFooter,
    alignWithMargins,
    differentOddEven,
    differentFirst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_HoleSize`. See `transitional/dml-chart.xsd:373`. */
export function readCT_HoleSize(cur: XmlCursor, ctx: ReadContext): CT_HoleSize {
  const start = requireStart(cur, 'CT_HoleSize');
  let val: ST_HoleSize | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_HoleSize(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_HoleSize', a, 'ST_HoleSize', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_HoleSize', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HoleSize', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_HoleSize', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HoleSize', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Layout`. See `transitional/dml-chart.xsd:172`. */
export function readCT_Layout(cur: XmlCursor, ctx: ReadContext): CT_Layout {
  const start = requireStart(cur, 'CT_Layout');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Layout', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let manualLayout: CT_ManualLayout | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Layout', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'manualLayout': {
            manualLayout = readCT_ManualLayout(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Layout', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Layout', cur);
    cur.next();
  }

  return {
    manualLayout,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LayoutMode`. See `transitional/dml-chart.xsd:155`. */
export function readCT_LayoutMode(cur: XmlCursor, ctx: ReadContext): CT_LayoutMode {
  const start = requireStart(cur, 'CT_LayoutMode');
  let val: ST_LayoutMode | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_LayoutMode(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_LayoutMode', a, 'ST_LayoutMode', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LayoutMode', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LayoutMode', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LayoutMode', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LayoutMode', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_LayoutTarget`. See `transitional/dml-chart.xsd:146`. */
export function readCT_LayoutTarget(cur: XmlCursor, ctx: ReadContext): CT_LayoutTarget {
  const start = requireStart(cur, 'CT_LayoutTarget');
  let val: ST_LayoutTarget | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_LayoutTarget(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_LayoutTarget', a, 'ST_LayoutTarget', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LayoutTarget', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LayoutTarget', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LayoutTarget', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LayoutTarget', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_LblAlgn`. See `transitional/dml-chart.xsd:421`. */
export function readCT_LblAlgn(cur: XmlCursor, ctx: ReadContext): CT_LblAlgn {
  const start = requireStart(cur, 'CT_LblAlgn');
  let val: ST_LblAlgn | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_LblAlgn(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_LblAlgn', a, 'ST_LblAlgn', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LblAlgn', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_LblAlgn', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LblAlgn', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LblAlgn', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LblAlgn', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_LblOffset`. See `transitional/dml-chart.xsd:1217`. */
export function readCT_LblOffset(cur: XmlCursor, ctx: ReadContext): CT_LblOffset {
  const start = requireStart(cur, 'CT_LblOffset');
  let val: ST_LblOffset | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_LblOffset(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_LblOffset', a, 'ST_LblOffset', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LblOffset', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LblOffset', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LblOffset', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LblOffset', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Legend`. See `transitional/dml-chart.xsd:1359`. */
export function readCT_Legend(cur: XmlCursor, ctx: ReadContext): CT_Legend {
  const start = requireStart(cur, 'CT_Legend');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Legend', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let legendPos: CT_LegendPos | undefined;
  const legendEntry: CT_LegendEntry[] = [];
  let layout: CT_Layout | undefined;
  let overlay: CT_Boolean | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let txPr: CT_TextBody | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Legend', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'legendPos': {
            legendPos = readCT_LegendPos(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'legendEntry': {
            legendEntry.push(readCT_LegendEntry(cur, ctx));
            $slot = 1;
            $index = legendEntry.length - 1;
            continue;
          }
          case 'layout': {
            layout = readCT_Layout(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'overlay': {
            overlay = readCT_Boolean(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            txPr = readCT_TextBody(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Legend', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Legend', cur);
    cur.next();
  }

  return {
    legendPos,
    legendEntry,
    layout,
    overlay,
    spPr,
    txPr,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LegendEntry`. See `transitional/dml-chart.xsd:1349`. */
export function readCT_LegendEntry(cur: XmlCursor, ctx: ReadContext): CT_LegendEntry {
  const start = requireStart(cur, 'CT_LegendEntry');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_LegendEntry', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let content: CT_LegendEntry_Content | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LegendEntry', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'delete': {
            content = { kind: 'delete', value: readCT_Boolean(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            content = { kind: 'txPr', value: readCT_TextBody(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LegendEntry', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LegendEntry', cur);
    cur.next();
  }

  return {
    idx,
    content,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LegendPos`. See `transitional/dml-chart.xsd:1341`. */
export function readCT_LegendPos(cur: XmlCursor, ctx: ReadContext): CT_LegendPos {
  const start = requireStart(cur, 'CT_LegendPos');
  let val: ST_LegendPos | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_LegendPos(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_LegendPos', a, 'ST_LegendPos', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LegendPos', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LegendPos', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LegendPos', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LegendPos', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Line3DChart`. See `transitional/dml-chart.xsd:794`. */
export function readCT_Line3DChart(cur: XmlCursor, ctx: ReadContext): CT_Line3DChart {
  const start = requireStart(cur, 'CT_Line3DChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Line3DChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let grouping: CT_Grouping | undefined;
  let varyColors: CT_Boolean | undefined;
  const ser: CT_LineSer[] = [];
  let dLbls: CT_DLbls | undefined;
  let dropLines: CT_ChartLines | undefined;
  let gapDepth: CT_GapAmount | undefined;
  const axId: CT_UnsignedInt[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Line3DChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'grouping': {
            grouping = readCT_Grouping(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_LineSer(cur, ctx));
            $slot = 2;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'dropLines': {
            dropLines = readCT_ChartLines(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'gapDepth': {
            gapDepth = readCT_GapAmount(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'axId': {
            axId.push(readCT_UnsignedInt(cur, ctx));
            $slot = 6;
            $index = axId.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Line3DChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Line3DChart', cur);
    cur.next();
  }

  return {
    grouping,
    varyColors,
    ser,
    dLbls,
    dropLines,
    gapDepth,
    axId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LineChart`. See `transitional/dml-chart.xsd:783`. */
export function readCT_LineChart(cur: XmlCursor, ctx: ReadContext): CT_LineChart {
  const start = requireStart(cur, 'CT_LineChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_LineChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let grouping: CT_Grouping | undefined;
  let varyColors: CT_Boolean | undefined;
  const ser: CT_LineSer[] = [];
  let dLbls: CT_DLbls | undefined;
  let dropLines: CT_ChartLines | undefined;
  let hiLowLines: CT_ChartLines | undefined;
  let upDownBars: CT_UpDownBars | undefined;
  let marker: CT_Boolean | undefined;
  let smooth: CT_Boolean | undefined;
  const axId: CT_UnsignedInt[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LineChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'grouping': {
            grouping = readCT_Grouping(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_LineSer(cur, ctx));
            $slot = 2;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'dropLines': {
            dropLines = readCT_ChartLines(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'hiLowLines': {
            hiLowLines = readCT_ChartLines(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'upDownBars': {
            upDownBars = readCT_UpDownBars(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'marker': {
            marker = readCT_Boolean(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'smooth': {
            smooth = readCT_Boolean(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'axId': {
            axId.push(readCT_UnsignedInt(cur, ctx));
            $slot = 9;
            $index = axId.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LineChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LineChart', cur);
    cur.next();
  }

  return {
    grouping,
    varyColors,
    ser,
    dLbls,
    dropLines,
    hiLowLines,
    upDownBars,
    marker,
    smooth,
    axId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LineSer`. See `transitional/dml-chart.xsd:658`. */
export function readCT_LineSer(cur: XmlCursor, ctx: ReadContext): CT_LineSer {
  const start = requireStart(cur, 'CT_LineSer');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_LineSer', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let order: CT_UnsignedInt | undefined;
  let tx: CT_SerTx | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let marker: CT_Marker | undefined;
  const dPt: CT_DPt[] = [];
  let dLbls: CT_DLbls | undefined;
  const trendline: CT_Trendline[] = [];
  let errBars: CT_ErrBars | undefined;
  let cat: CT_AxDataSource | undefined;
  let val: CT_NumDataSource | undefined;
  let smooth: CT_Boolean | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LineSer', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'order': {
            order = readCT_UnsignedInt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tx': {
            tx = readCT_SerTx(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'marker': {
            marker = readCT_Marker(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'dPt': {
            dPt.push(readCT_DPt(cur, ctx));
            $slot = 5;
            $index = dPt.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'trendline': {
            trendline.push(readCT_Trendline(cur, ctx));
            $slot = 7;
            $index = trendline.length - 1;
            continue;
          }
          case 'errBars': {
            errBars = readCT_ErrBars(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'cat': {
            cat = readCT_AxDataSource(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'val': {
            val = readCT_NumDataSource(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'smooth': {
            smooth = readCT_Boolean(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LineSer', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LineSer', cur);
    cur.next();
  }

  return {
    idx,
    order,
    tx,
    spPr,
    marker,
    dPt,
    dLbls,
    trendline,
    errBars,
    cat,
    val,
    smooth,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LogBase`. See `transitional/dml-chart.xsd:1191`. */
export function readCT_LogBase(cur: XmlCursor, ctx: ReadContext): CT_LogBase {
  const start = requireStart(cur, 'CT_LogBase');
  let val: ST_LogBase | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_LogBase(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_LogBase', a, 'ST_LogBase', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LogBase', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_LogBase', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LogBase', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LogBase', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LogBase', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Lvl`. See `transitional/dml-chart.xsd:102`. */
export function readCT_Lvl(cur: XmlCursor, ctx: ReadContext): CT_Lvl {
  const start = requireStart(cur, 'CT_Lvl');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Lvl', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const pt: CT_StrVal[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Lvl', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'pt': {
            pt.push(readCT_StrVal(cur, ctx));
            $slot = 0;
            $index = pt.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Lvl', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Lvl', cur);
    cur.next();
  }

  return {
    pt,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ManualLayout`. See `transitional/dml-chart.xsd:158`. */
export function readCT_ManualLayout(cur: XmlCursor, ctx: ReadContext): CT_ManualLayout {
  const start = requireStart(cur, 'CT_ManualLayout');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ManualLayout', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let layoutTarget: CT_LayoutTarget | undefined;
  let xMode: CT_LayoutMode | undefined;
  let yMode: CT_LayoutMode | undefined;
  let wMode: CT_LayoutMode | undefined;
  let hMode: CT_LayoutMode | undefined;
  let x: CT_Double | undefined;
  let y: CT_Double | undefined;
  let w: CT_Double | undefined;
  let h: CT_Double | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ManualLayout', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'layoutTarget': {
            layoutTarget = readCT_LayoutTarget(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'xMode': {
            xMode = readCT_LayoutMode(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'yMode': {
            yMode = readCT_LayoutMode(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'wMode': {
            wMode = readCT_LayoutMode(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'hMode': {
            hMode = readCT_LayoutMode(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'x': {
            x = readCT_Double(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'y': {
            y = readCT_Double(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'w': {
            w = readCT_Double(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'h': {
            h = readCT_Double(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ManualLayout', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ManualLayout', cur);
    cur.next();
  }

  return {
    layoutTarget,
    xMode,
    yMode,
    wMode,
    hMode,
    x,
    y,
    w,
    h,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Marker`. See `transitional/dml-chart.xsd:517`. */
export function readCT_Marker(cur: XmlCursor, ctx: ReadContext): CT_Marker {
  const start = requireStart(cur, 'CT_Marker');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Marker', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let symbol: CT_MarkerStyle | undefined;
  let size: CT_MarkerSize | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Marker', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'symbol': {
            symbol = readCT_MarkerStyle(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'size': {
            size = readCT_MarkerSize(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Marker', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Marker', cur);
    cur.next();
  }

  return {
    symbol,
    size,
    spPr,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_MarkerSize`. See `transitional/dml-chart.xsd:514`. */
export function readCT_MarkerSize(cur: XmlCursor, ctx: ReadContext): CT_MarkerSize {
  const start = requireStart(cur, 'CT_MarkerSize');
  let val: ST_MarkerSize | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_MarkerSize(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_MarkerSize', a, 'ST_MarkerSize', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MarkerSize', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MarkerSize', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_MarkerSize', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MarkerSize', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_MarkerStyle`. See `transitional/dml-chart.xsd:505`. */
export function readCT_MarkerStyle(cur: XmlCursor, ctx: ReadContext): CT_MarkerStyle {
  const start = requireStart(cur, 'CT_MarkerStyle');
  let val: ST_MarkerStyle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_MarkerStyle(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_MarkerStyle', a, 'ST_MarkerStyle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_MarkerStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_MarkerStyle', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MarkerStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_MarkerStyle', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MarkerStyle', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_MultiLvlStrData`. See `transitional/dml-chart.xsd:107`. */
export function readCT_MultiLvlStrData(cur: XmlCursor, ctx: ReadContext): CT_MultiLvlStrData {
  const start = requireStart(cur, 'CT_MultiLvlStrData');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_MultiLvlStrData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ptCount: CT_UnsignedInt | undefined;
  const lvl: CT_Lvl[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MultiLvlStrData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'ptCount': {
            ptCount = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lvl': {
            lvl.push(readCT_Lvl(cur, ctx));
            $slot = 1;
            $index = lvl.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_MultiLvlStrData', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MultiLvlStrData', cur);
    cur.next();
  }

  return {
    ptCount,
    lvl,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_MultiLvlStrRef`. See `transitional/dml-chart.xsd:114`. */
export function readCT_MultiLvlStrRef(cur: XmlCursor, ctx: ReadContext): CT_MultiLvlStrRef {
  const start = requireStart(cur, 'CT_MultiLvlStrRef');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_MultiLvlStrRef', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let f: string | undefined;
  let multiLvlStrCache: CT_MultiLvlStrData | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_MultiLvlStrRef', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'f': {
            f = readScalar(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'multiLvlStrCache': {
            multiLvlStrCache = readCT_MultiLvlStrData(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_MultiLvlStrRef', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_MultiLvlStrRef', cur);
    cur.next();
  }

  return {
    f,
    multiLvlStrCache,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NumData`. See `transitional/dml-chart.xsd:48`. */
export function readCT_NumData(cur: XmlCursor, ctx: ReadContext): CT_NumData {
  const start = requireStart(cur, 'CT_NumData');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NumData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let formatCode: ST_Xstring | undefined;
  let ptCount: CT_UnsignedInt | undefined;
  const pt: CT_NumVal[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NumData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'formatCode': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Xstring($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_NumData', 'formatCode', 'ST_Xstring', $s, cur);
            formatCode = ($pv ?? $s) as ST_Xstring;
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ptCount': {
            ptCount = readCT_UnsignedInt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'pt': {
            pt.push(readCT_NumVal(cur, ctx));
            $slot = 2;
            $index = pt.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NumData', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NumData', cur);
    cur.next();
  }

  return {
    formatCode,
    ptCount,
    pt,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NumDataSource`. See `transitional/dml-chart.xsd:63`. */
export function readCT_NumDataSource(cur: XmlCursor, ctx: ReadContext): CT_NumDataSource {
  const start = requireStart(cur, 'CT_NumDataSource');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NumDataSource', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_NumDataSource_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NumDataSource', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'numRef': {
            content = { kind: 'numRef', value: readCT_NumRef(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'numLit': {
            content = { kind: 'numLit', value: readCT_NumData(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NumDataSource', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NumDataSource', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NumFmt`. See `transitional/dml-chart.xsd:410`. */
export function readCT_NumFmt(cur: XmlCursor, ctx: ReadContext): CT_NumFmt {
  const start = requireStart(cur, 'CT_NumFmt');
  let formatCode: ST_Xstring | undefined;
  let sourceLinked: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'formatCode': {
          const v = parseST_Xstring(a.value);
          if (v !== undefined) {
            formatCode = v;
            continue;
          }
          ctx.invalidValue('CT_NumFmt', a, 'ST_Xstring', cur);
          break;
        }
        case 'sourceLinked': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            sourceLinked = v;
            continue;
          }
          ctx.invalidValue('CT_NumFmt', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NumFmt', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (formatCode === undefined) ctx.missingRequired('CT_NumFmt', 'formatCode', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NumFmt', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_NumFmt', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NumFmt', cur);
    cur.next();
  }
  return {
    formatCode,
    sourceLinked,
    $unknownAttrs,
  };
}

/** Read a `CT_NumRef`. See `transitional/dml-chart.xsd:56`. */
export function readCT_NumRef(cur: XmlCursor, ctx: ReadContext): CT_NumRef {
  const start = requireStart(cur, 'CT_NumRef');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NumRef', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let f: string | undefined;
  let numCache: CT_NumData | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NumRef', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'f': {
            f = readScalar(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'numCache': {
            numCache = readCT_NumData(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NumRef', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NumRef', cur);
    cur.next();
  }

  return {
    f,
    numCache,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NumVal`. See `transitional/dml-chart.xsd:41`. */
export function readCT_NumVal(cur: XmlCursor, ctx: ReadContext): CT_NumVal {
  const start = requireStart(cur, 'CT_NumVal');
  let idx: number | undefined;
  let formatCode: ST_Xstring | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'idx': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            idx = v;
            continue;
          }
          ctx.invalidValue('CT_NumVal', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'formatCode': {
          const v = parseST_Xstring(a.value);
          if (v !== undefined) {
            formatCode = v;
            continue;
          }
          ctx.invalidValue('CT_NumVal', a, 'ST_Xstring', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NumVal', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (idx === undefined) ctx.missingRequired('CT_NumVal', 'idx', cur);

  let v_: ST_Xstring | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NumVal', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'v': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Xstring($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_NumVal', 'v', 'ST_Xstring', $s, cur);
            v_ = ($pv ?? $s) as ST_Xstring;
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NumVal', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NumVal', cur);
    cur.next();
  }

  return {
    v: v_,
    idx,
    formatCode,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OfPieChart`. See `transitional/dml-chart.xsd:979`. */
export function readCT_OfPieChart(cur: XmlCursor, ctx: ReadContext): CT_OfPieChart {
  const start = requireStart(cur, 'CT_OfPieChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_OfPieChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ofPieType: CT_OfPieType | undefined;
  let varyColors: CT_Boolean | undefined;
  const ser: CT_PieSer[] = [];
  let dLbls: CT_DLbls | undefined;
  let gapWidth: CT_GapAmount | undefined;
  let splitType: CT_SplitType | undefined;
  let splitPos: CT_Double | undefined;
  let custSplit: CT_CustSplit | undefined;
  let secondPieSize: CT_SecondPieSize | undefined;
  const serLines: CT_ChartLines[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OfPieChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'ofPieType': {
            ofPieType = readCT_OfPieType(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_PieSer(cur, ctx));
            $slot = 2;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'gapWidth': {
            gapWidth = readCT_GapAmount(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'splitType': {
            splitType = readCT_SplitType(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'splitPos': {
            splitPos = readCT_Double(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'custSplit': {
            custSplit = readCT_CustSplit(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'secondPieSize': {
            secondPieSize = readCT_SecondPieSize(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'serLines': {
            serLines.push(readCT_ChartLines(cur, ctx));
            $slot = 9;
            $index = serLines.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OfPieChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OfPieChart', cur);
    cur.next();
  }

  return {
    ofPieType,
    varyColors,
    ser,
    dLbls,
    gapWidth,
    splitType,
    splitPos,
    custSplit,
    secondPieSize,
    serLines,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OfPieType`. See `transitional/dml-chart.xsd:976`. */
export function readCT_OfPieType(cur: XmlCursor, ctx: ReadContext): CT_OfPieType {
  const start = requireStart(cur, 'CT_OfPieType');
  let val: ST_OfPieType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_OfPieType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_OfPieType', a, 'ST_OfPieType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OfPieType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OfPieType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_OfPieType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OfPieType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Order`. See `transitional/dml-chart.xsd:556`. */
export function readCT_Order(cur: XmlCursor, ctx: ReadContext): CT_Order {
  const start = requireStart(cur, 'CT_Order');
  let val: ST_Order | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Order(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Order', a, 'ST_Order', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Order', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Order', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Order', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Order', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Orientation`. See `transitional/dml-chart.xsd:1182`. */
export function readCT_Orientation(cur: XmlCursor, ctx: ReadContext): CT_Orientation {
  const start = requireStart(cur, 'CT_Orientation');
  let val: ST_Orientation | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Orientation(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Orientation', a, 'ST_Orientation', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Orientation', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Orientation', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Orientation', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Orientation', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Overlap`. See `transitional/dml-chart.xsd:321`. */
export function readCT_Overlap(cur: XmlCursor, ctx: ReadContext): CT_Overlap {
  const start = requireStart(cur, 'CT_Overlap');
  let val: ST_Overlap | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Overlap(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Overlap', a, 'ST_Overlap', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Overlap', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Overlap', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Overlap', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Overlap', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_PageMargins`. See `transitional/dml-chart.xsd:1435`. */
export function readCT_PageMargins(cur: XmlCursor, ctx: ReadContext): CT_PageMargins {
  const start = requireStart(cur, 'CT_PageMargins');
  let l: number | undefined;
  let r: number | undefined;
  let t: number | undefined;
  let b: number | undefined;
  let header: number | undefined;
  let footer: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'l': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            l = v;
            continue;
          }
          ctx.invalidValue('CT_PageMargins', a, 'xsd:double', cur);
          break;
        }
        case 'r': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            r = v;
            continue;
          }
          ctx.invalidValue('CT_PageMargins', a, 'xsd:double', cur);
          break;
        }
        case 't': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            t = v;
            continue;
          }
          ctx.invalidValue('CT_PageMargins', a, 'xsd:double', cur);
          break;
        }
        case 'b': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            b = v;
            continue;
          }
          ctx.invalidValue('CT_PageMargins', a, 'xsd:double', cur);
          break;
        }
        case 'header': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            header = v;
            continue;
          }
          ctx.invalidValue('CT_PageMargins', a, 'xsd:double', cur);
          break;
        }
        case 'footer': {
          const v = parseDouble(a.value);
          if (v !== undefined) {
            footer = v;
            continue;
          }
          ctx.invalidValue('CT_PageMargins', a, 'xsd:double', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PageMargins', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (l === undefined) ctx.missingRequired('CT_PageMargins', 'l', cur);
  if (r === undefined) ctx.missingRequired('CT_PageMargins', 'r', cur);
  if (t === undefined) ctx.missingRequired('CT_PageMargins', 't', cur);
  if (b === undefined) ctx.missingRequired('CT_PageMargins', 'b', cur);
  if (header === undefined) ctx.missingRequired('CT_PageMargins', 'header', cur);
  if (footer === undefined) ctx.missingRequired('CT_PageMargins', 'footer', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PageMargins', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PageMargins', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PageMargins', cur);
    cur.next();
  }
  return {
    l,
    r,
    t,
    b,
    header,
    footer,
    $unknownAttrs,
  };
}

/** Read a `CT_PageSetup`. See `transitional/dml-chart.xsd:1456`. */
export function readCT_PageSetup(cur: XmlCursor, ctx: ReadContext): CT_PageSetup {
  const start = requireStart(cur, 'CT_PageSetup');
  let paperSize: number | undefined;
  let paperHeight: ST_PositiveUniversalMeasure | undefined;
  let paperWidth: ST_PositiveUniversalMeasure | undefined;
  let firstPageNumber: number | undefined;
  let orientation: ST_PageSetupOrientation | undefined;
  let blackAndWhite: boolean | undefined;
  let draft: boolean | undefined;
  let useFirstPageNumber: boolean | undefined;
  let horizontalDpi: number | undefined;
  let verticalDpi: number | undefined;
  let copies: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'paperSize': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            paperSize = v;
            continue;
          }
          ctx.invalidValue('CT_PageSetup', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'paperHeight': {
          const v = parseST_PositiveUniversalMeasure(a.value);
          if (v !== undefined) {
            paperHeight = v;
            continue;
          }
          ctx.invalidValue('CT_PageSetup', a, 'ST_PositiveUniversalMeasure', cur);
          break;
        }
        case 'paperWidth': {
          const v = parseST_PositiveUniversalMeasure(a.value);
          if (v !== undefined) {
            paperWidth = v;
            continue;
          }
          ctx.invalidValue('CT_PageSetup', a, 'ST_PositiveUniversalMeasure', cur);
          break;
        }
        case 'firstPageNumber': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            firstPageNumber = v;
            continue;
          }
          ctx.invalidValue('CT_PageSetup', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'orientation': {
          const v = parseST_PageSetupOrientation(a.value);
          if (v !== undefined) {
            orientation = v;
            continue;
          }
          ctx.invalidValue('CT_PageSetup', a, 'ST_PageSetupOrientation', cur);
          break;
        }
        case 'blackAndWhite': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            blackAndWhite = v;
            continue;
          }
          ctx.invalidValue('CT_PageSetup', a, 'xsd:boolean', cur);
          break;
        }
        case 'draft': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            draft = v;
            continue;
          }
          ctx.invalidValue('CT_PageSetup', a, 'xsd:boolean', cur);
          break;
        }
        case 'useFirstPageNumber': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            useFirstPageNumber = v;
            continue;
          }
          ctx.invalidValue('CT_PageSetup', a, 'xsd:boolean', cur);
          break;
        }
        case 'horizontalDpi': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            horizontalDpi = v;
            continue;
          }
          ctx.invalidValue('CT_PageSetup', a, 'xsd:int', cur);
          break;
        }
        case 'verticalDpi': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            verticalDpi = v;
            continue;
          }
          ctx.invalidValue('CT_PageSetup', a, 'xsd:int', cur);
          break;
        }
        case 'copies': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            copies = v;
            continue;
          }
          ctx.invalidValue('CT_PageSetup', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PageSetup', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PageSetup', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PageSetup', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PageSetup', cur);
    cur.next();
  }
  return {
    paperSize,
    paperHeight,
    paperWidth,
    firstPageNumber,
    orientation,
    blackAndWhite,
    draft,
    useFirstPageNumber,
    horizontalDpi,
    verticalDpi,
    copies,
    $unknownAttrs,
  };
}

/** Read a `CT_Period`. See `transitional/dml-chart.xsd:564`. */
export function readCT_Period(cur: XmlCursor, ctx: ReadContext): CT_Period {
  const start = requireStart(cur, 'CT_Period');
  let val: ST_Period | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Period(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Period', a, 'ST_Period', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Period', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Period', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Period', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Period', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Perspective`. See `transitional/dml-chart.xsd:246`. */
export function readCT_Perspective(cur: XmlCursor, ctx: ReadContext): CT_Perspective {
  const start = requireStart(cur, 'CT_Perspective');
  let val: ST_Perspective | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Perspective(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Perspective', a, 'ST_Perspective', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Perspective', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Perspective', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Perspective', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Perspective', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_PictureFormat`. See `transitional/dml-chart.xsd:1138`. */
export function readCT_PictureFormat(cur: XmlCursor, ctx: ReadContext): CT_PictureFormat {
  const start = requireStart(cur, 'CT_PictureFormat');
  let val: ST_PictureFormat | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_PictureFormat(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_PictureFormat', a, 'ST_PictureFormat', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PictureFormat', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_PictureFormat', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PictureFormat', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PictureFormat', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PictureFormat', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_PictureOptions`. See `transitional/dml-chart.xsd:1149`. */
export function readCT_PictureOptions(cur: XmlCursor, ctx: ReadContext): CT_PictureOptions {
  const start = requireStart(cur, 'CT_PictureOptions');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PictureOptions', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let applyToFront: CT_Boolean | undefined;
  let applyToSides: CT_Boolean | undefined;
  let applyToEnd: CT_Boolean | undefined;
  let pictureFormat: CT_PictureFormat | undefined;
  let pictureStackUnit: CT_PictureStackUnit | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PictureOptions', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'applyToFront': {
            applyToFront = readCT_Boolean(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'applyToSides': {
            applyToSides = readCT_Boolean(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'applyToEnd': {
            applyToEnd = readCT_Boolean(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'pictureFormat': {
            pictureFormat = readCT_PictureFormat(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'pictureStackUnit': {
            pictureStackUnit = readCT_PictureStackUnit(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PictureOptions', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PictureOptions', cur);
    cur.next();
  }

  return {
    applyToFront,
    applyToSides,
    applyToEnd,
    pictureFormat,
    pictureStackUnit,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PictureStackUnit`. See `transitional/dml-chart.xsd:1146`. */
export function readCT_PictureStackUnit(cur: XmlCursor, ctx: ReadContext): CT_PictureStackUnit {
  const start = requireStart(cur, 'CT_PictureStackUnit');
  let val: ST_PictureStackUnit | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_PictureStackUnit(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_PictureStackUnit', a, 'ST_PictureStackUnit', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PictureStackUnit', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_PictureStackUnit', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PictureStackUnit', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PictureStackUnit', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PictureStackUnit', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Pie3DChart`. See `transitional/dml-chart.xsd:956`. */
export function readCT_Pie3DChart(cur: XmlCursor, ctx: ReadContext): CT_Pie3DChart {
  const start = requireStart(cur, 'CT_Pie3DChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Pie3DChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let varyColors: CT_Boolean | undefined;
  const ser: CT_PieSer[] = [];
  let dLbls: CT_DLbls | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Pie3DChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_PieSer(cur, ctx));
            $slot = 1;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Pie3DChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Pie3DChart', cur);
    cur.next();
  }

  return {
    varyColors,
    ser,
    dLbls,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PieChart`. See `transitional/dml-chart.xsd:949`. */
export function readCT_PieChart(cur: XmlCursor, ctx: ReadContext): CT_PieChart {
  const start = requireStart(cur, 'CT_PieChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PieChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let varyColors: CT_Boolean | undefined;
  const ser: CT_PieSer[] = [];
  let dLbls: CT_DLbls | undefined;
  let firstSliceAng: CT_FirstSliceAng | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PieChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_PieSer(cur, ctx));
            $slot = 1;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'firstSliceAng': {
            firstSliceAng = readCT_FirstSliceAng(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PieChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PieChart', cur);
    cur.next();
  }

  return {
    varyColors,
    ser,
    dLbls,
    firstSliceAng,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PieSer`. See `transitional/dml-chart.xsd:725`. */
export function readCT_PieSer(cur: XmlCursor, ctx: ReadContext): CT_PieSer {
  const start = requireStart(cur, 'CT_PieSer');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PieSer', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let order: CT_UnsignedInt | undefined;
  let tx: CT_SerTx | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let explosion: CT_UnsignedInt | undefined;
  const dPt: CT_DPt[] = [];
  let dLbls: CT_DLbls | undefined;
  let cat: CT_AxDataSource | undefined;
  let val: CT_NumDataSource | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PieSer', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'order': {
            order = readCT_UnsignedInt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tx': {
            tx = readCT_SerTx(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'explosion': {
            explosion = readCT_UnsignedInt(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'dPt': {
            dPt.push(readCT_DPt(cur, ctx));
            $slot = 5;
            $index = dPt.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'cat': {
            cat = readCT_AxDataSource(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'val': {
            val = readCT_NumDataSource(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PieSer', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PieSer', cur);
    cur.next();
  }

  return {
    idx,
    order,
    tx,
    spPr,
    explosion,
    dPt,
    dLbls,
    cat,
    val,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PivotFmt`. See `transitional/dml-chart.xsd:1317`. */
export function readCT_PivotFmt(cur: XmlCursor, ctx: ReadContext): CT_PivotFmt {
  const start = requireStart(cur, 'CT_PivotFmt');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PivotFmt', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let txPr: CT_TextBody | undefined;
  let marker: CT_Marker | undefined;
  let dLbl: CT_DLbl | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PivotFmt', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            txPr = readCT_TextBody(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'marker': {
            marker = readCT_Marker(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'dLbl': {
            dLbl = readCT_DLbl(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PivotFmt', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PivotFmt', cur);
    cur.next();
  }

  return {
    idx,
    spPr,
    txPr,
    marker,
    dLbl,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PivotFmts`. See `transitional/dml-chart.xsd:1327`. */
export function readCT_PivotFmts(cur: XmlCursor, ctx: ReadContext): CT_PivotFmts {
  const start = requireStart(cur, 'CT_PivotFmts');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PivotFmts', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const pivotFmt: CT_PivotFmt[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PivotFmts', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'pivotFmt': {
            pivotFmt.push(readCT_PivotFmt(cur, ctx));
            $slot = 0;
            $index = pivotFmt.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PivotFmts', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PivotFmts', cur);
    cur.next();
  }

  return {
    pivotFmt,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PivotSource`. See `transitional/dml-chart.xsd:1406`. */
export function readCT_PivotSource(cur: XmlCursor, ctx: ReadContext): CT_PivotSource {
  const start = requireStart(cur, 'CT_PivotSource');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PivotSource', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let name: ST_Xstring | undefined;
  let fmtId: CT_UnsignedInt | undefined;
  const extLst: CT_ExtensionList[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PivotSource', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'name': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Xstring($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_PivotSource', 'name', 'ST_Xstring', $s, cur);
            name = ($pv ?? $s) as ST_Xstring;
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'fmtId': {
            fmtId = readCT_UnsignedInt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst.push(readCT_ExtensionList(cur, ctx));
            $slot = 2;
            $index = extLst.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PivotSource', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PivotSource', cur);
    cur.next();
  }

  return {
    name,
    fmtId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PlotArea`. See `transitional/dml-chart.xsd:1285`. */
export function readCT_PlotArea(cur: XmlCursor, ctx: ReadContext): CT_PlotArea {
  const start = requireStart(cur, 'CT_PlotArea');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PlotArea', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let layout: CT_Layout | undefined;
  const content: CT_PlotArea_Content[] = [];
  const content2: CT_PlotArea_Content2[] = [];
  let dTable: CT_DTable | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PlotArea', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'layout': {
            layout = readCT_Layout(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'areaChart': {
            content.push({ kind: 'areaChart', value: readCT_AreaChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'area3DChart': {
            content.push({ kind: 'area3DChart', value: readCT_Area3DChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'lineChart': {
            content.push({ kind: 'lineChart', value: readCT_LineChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'line3DChart': {
            content.push({ kind: 'line3DChart', value: readCT_Line3DChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'stockChart': {
            content.push({ kind: 'stockChart', value: readCT_StockChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'radarChart': {
            content.push({ kind: 'radarChart', value: readCT_RadarChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'scatterChart': {
            content.push({ kind: 'scatterChart', value: readCT_ScatterChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'pieChart': {
            content.push({ kind: 'pieChart', value: readCT_PieChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'pie3DChart': {
            content.push({ kind: 'pie3DChart', value: readCT_Pie3DChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'doughnutChart': {
            content.push({ kind: 'doughnutChart', value: readCT_DoughnutChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'barChart': {
            content.push({ kind: 'barChart', value: readCT_BarChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'bar3DChart': {
            content.push({ kind: 'bar3DChart', value: readCT_Bar3DChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'ofPieChart': {
            content.push({ kind: 'ofPieChart', value: readCT_OfPieChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'surfaceChart': {
            content.push({ kind: 'surfaceChart', value: readCT_SurfaceChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'surface3DChart': {
            content.push({ kind: 'surface3DChart', value: readCT_Surface3DChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'bubbleChart': {
            content.push({ kind: 'bubbleChart', value: readCT_BubbleChart(cur, ctx) });
            $slot = 1;
            $index = content.length - 1;
            continue;
          }
          case 'valAx': {
            content2.push({ kind: 'valAx', value: readCT_ValAx(cur, ctx) });
            $slot = 2;
            $index = content2.length - 1;
            continue;
          }
          case 'catAx': {
            content2.push({ kind: 'catAx', value: readCT_CatAx(cur, ctx) });
            $slot = 2;
            $index = content2.length - 1;
            continue;
          }
          case 'dateAx': {
            content2.push({ kind: 'dateAx', value: readCT_DateAx(cur, ctx) });
            $slot = 2;
            $index = content2.length - 1;
            continue;
          }
          case 'serAx': {
            content2.push({ kind: 'serAx', value: readCT_SerAx(cur, ctx) });
            $slot = 2;
            $index = content2.length - 1;
            continue;
          }
          case 'dTable': {
            dTable = readCT_DTable(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PlotArea', ev.localName, ev.uri, cur);
      const $raw = cur.skipToRaw();
      if ($slot <= 1) content.push({ kind: '$raw', value: $raw });
      else content2.push({ kind: '$raw', value: $raw });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PlotArea', cur);
    cur.next();
  }

  return {
    layout,
    content,
    content2,
    dTable,
    spPr,
    extLst,
    $unknownAttrs,
  };
}

/** Read a `CT_PrintSettings`. See `transitional/dml-chart.xsd:1470`. */
export function readCT_PrintSettings(cur: XmlCursor, ctx: ReadContext): CT_PrintSettings {
  const start = requireStart(cur, 'CT_PrintSettings');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PrintSettings', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let headerFooter: CT_HeaderFooter | undefined;
  let pageMargins: CT_PageMargins | undefined;
  let pageSetup: CT_PageSetup | undefined;
  let legacyDrawingHF: CT_RelId | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PrintSettings', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'headerFooter': {
            headerFooter = readCT_HeaderFooter(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pageMargins': {
            pageMargins = readCT_PageMargins(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'pageSetup': {
            pageSetup = readCT_PageSetup(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'legacyDrawingHF': {
            legacyDrawingHF = readCT_RelId(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PrintSettings', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PrintSettings', cur);
    cur.next();
  }

  return {
    headerFooter,
    pageMargins,
    pageSetup,
    legacyDrawingHF,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Protection`. See `transitional/dml-chart.xsd:1413`. */
export function readCT_Protection(cur: XmlCursor, ctx: ReadContext): CT_Protection {
  const start = requireStart(cur, 'CT_Protection');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Protection', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let chartObject: CT_Boolean | undefined;
  let data: CT_Boolean | undefined;
  let formatting: CT_Boolean | undefined;
  let selection: CT_Boolean | undefined;
  let userInterface: CT_Boolean | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Protection', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'chartObject': {
            chartObject = readCT_Boolean(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'data': {
            data = readCT_Boolean(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'formatting': {
            formatting = readCT_Boolean(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'selection': {
            selection = readCT_Boolean(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'userInterface': {
            userInterface = readCT_Boolean(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Protection', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Protection', cur);
    cur.next();
  }

  return {
    chartObject,
    data,
    formatting,
    selection,
    userInterface,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_RadarChart`. See `transitional/dml-chart.xsd:846`. */
export function readCT_RadarChart(cur: XmlCursor, ctx: ReadContext): CT_RadarChart {
  const start = requireStart(cur, 'CT_RadarChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_RadarChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let radarStyle: CT_RadarStyle | undefined;
  let varyColors: CT_Boolean | undefined;
  const ser: CT_RadarSer[] = [];
  let dLbls: CT_DLbls | undefined;
  const axId: CT_UnsignedInt[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RadarChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'radarStyle': {
            radarStyle = readCT_RadarStyle(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_RadarSer(cur, ctx));
            $slot = 2;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'axId': {
            axId.push(readCT_UnsignedInt(cur, ctx));
            $slot = 4;
            $index = axId.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RadarChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RadarChart', cur);
    cur.next();
  }

  return {
    radarStyle,
    varyColors,
    ser,
    dLbls,
    axId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_RadarSer`. See `transitional/dml-chart.xsd:686`. */
export function readCT_RadarSer(cur: XmlCursor, ctx: ReadContext): CT_RadarSer {
  const start = requireStart(cur, 'CT_RadarSer');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_RadarSer', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let order: CT_UnsignedInt | undefined;
  let tx: CT_SerTx | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let marker: CT_Marker | undefined;
  const dPt: CT_DPt[] = [];
  let dLbls: CT_DLbls | undefined;
  let cat: CT_AxDataSource | undefined;
  let val: CT_NumDataSource | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RadarSer', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'order': {
            order = readCT_UnsignedInt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tx': {
            tx = readCT_SerTx(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'marker': {
            marker = readCT_Marker(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'dPt': {
            dPt.push(readCT_DPt(cur, ctx));
            $slot = 5;
            $index = dPt.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'cat': {
            cat = readCT_AxDataSource(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'val': {
            val = readCT_NumDataSource(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RadarSer', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RadarSer', cur);
    cur.next();
  }

  return {
    idx,
    order,
    tx,
    spPr,
    marker,
    dPt,
    dLbls,
    cat,
    val,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_RadarStyle`. See `transitional/dml-chart.xsd:843`. */
export function readCT_RadarStyle(cur: XmlCursor, ctx: ReadContext): CT_RadarStyle {
  const start = requireStart(cur, 'CT_RadarStyle');
  let val: ST_RadarStyle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_RadarStyle(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_RadarStyle', a, 'ST_RadarStyle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_RadarStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RadarStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_RadarStyle', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RadarStyle', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_RelId`. See `transitional/dml-chart.xsd:27`. */
export function readCT_RelId(cur: XmlCursor, ctx: ReadContext): CT_RelId {
  const start = requireStart(cur, 'CT_RelId');
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_RelId', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_RelId', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_RelId', 'id', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RelId', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_RelId', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RelId', cur);
    cur.next();
  }
  return {
    id,
    $unknownAttrs,
  };
}

/** Read a `CT_RotX`. See `transitional/dml-chart.xsd:194`. */
export function readCT_RotX(cur: XmlCursor, ctx: ReadContext): CT_RotX {
  const start = requireStart(cur, 'CT_RotX');
  let val: ST_RotX | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_RotX(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_RotX', a, 'ST_RotX', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_RotX', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RotX', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_RotX', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RotX', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_RotY`. See `transitional/dml-chart.xsd:220`. */
export function readCT_RotY(cur: XmlCursor, ctx: ReadContext): CT_RotY {
  const start = requireStart(cur, 'CT_RotY');
  let val: ST_RotY | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_RotY(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_RotY', a, 'ST_RotY', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_RotY', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RotY', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_RotY', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RotY', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Scaling`. See `transitional/dml-chart.xsd:1194`. */
export function readCT_Scaling(cur: XmlCursor, ctx: ReadContext): CT_Scaling {
  const start = requireStart(cur, 'CT_Scaling');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Scaling', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let logBase: CT_LogBase | undefined;
  let orientation: CT_Orientation | undefined;
  let max: CT_Double | undefined;
  let min: CT_Double | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Scaling', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'logBase': {
            logBase = readCT_LogBase(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'orientation': {
            orientation = readCT_Orientation(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'max': {
            max = readCT_Double(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'min': {
            min = readCT_Double(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Scaling', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Scaling', cur);
    cur.next();
  }

  return {
    logBase,
    orientation,
    max,
    min,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ScatterChart`. See `transitional/dml-chart.xsd:826`. */
export function readCT_ScatterChart(cur: XmlCursor, ctx: ReadContext): CT_ScatterChart {
  const start = requireStart(cur, 'CT_ScatterChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ScatterChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let scatterStyle: CT_ScatterStyle | undefined;
  let varyColors: CT_Boolean | undefined;
  const ser: CT_ScatterSer[] = [];
  let dLbls: CT_DLbls | undefined;
  const axId: CT_UnsignedInt[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ScatterChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'scatterStyle': {
            scatterStyle = readCT_ScatterStyle(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'varyColors': {
            varyColors = readCT_Boolean(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_ScatterSer(cur, ctx));
            $slot = 2;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'axId': {
            axId.push(readCT_UnsignedInt(cur, ctx));
            $slot = 4;
            $index = axId.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ScatterChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ScatterChart', cur);
    cur.next();
  }

  return {
    scatterStyle,
    varyColors,
    ser,
    dLbls,
    axId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ScatterSer`. See `transitional/dml-chart.xsd:672`. */
export function readCT_ScatterSer(cur: XmlCursor, ctx: ReadContext): CT_ScatterSer {
  const start = requireStart(cur, 'CT_ScatterSer');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ScatterSer', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let order: CT_UnsignedInt | undefined;
  let tx: CT_SerTx | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let marker: CT_Marker | undefined;
  const dPt: CT_DPt[] = [];
  let dLbls: CT_DLbls | undefined;
  const trendline: CT_Trendline[] = [];
  const errBars: CT_ErrBars[] = [];
  let xVal: CT_AxDataSource | undefined;
  let yVal: CT_NumDataSource | undefined;
  let smooth: CT_Boolean | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ScatterSer', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'order': {
            order = readCT_UnsignedInt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tx': {
            tx = readCT_SerTx(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'marker': {
            marker = readCT_Marker(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'dPt': {
            dPt.push(readCT_DPt(cur, ctx));
            $slot = 5;
            $index = dPt.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'trendline': {
            trendline.push(readCT_Trendline(cur, ctx));
            $slot = 7;
            $index = trendline.length - 1;
            continue;
          }
          case 'errBars': {
            errBars.push(readCT_ErrBars(cur, ctx));
            $slot = 8;
            $index = errBars.length - 1;
            continue;
          }
          case 'xVal': {
            xVal = readCT_AxDataSource(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'yVal': {
            yVal = readCT_NumDataSource(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'smooth': {
            smooth = readCT_Boolean(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ScatterSer', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ScatterSer', cur);
    cur.next();
  }

  return {
    idx,
    order,
    tx,
    spPr,
    marker,
    dPt,
    dLbls,
    trendline,
    errBars,
    xVal,
    yVal,
    smooth,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ScatterStyle`. See `transitional/dml-chart.xsd:823`. */
export function readCT_ScatterStyle(cur: XmlCursor, ctx: ReadContext): CT_ScatterStyle {
  const start = requireStart(cur, 'CT_ScatterStyle');
  let val: ST_ScatterStyle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_ScatterStyle(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_ScatterStyle', a, 'ST_ScatterStyle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ScatterStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ScatterStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ScatterStyle', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ScatterStyle', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_SecondPieSize`. See `transitional/dml-chart.xsd:407`. */
export function readCT_SecondPieSize(cur: XmlCursor, ctx: ReadContext): CT_SecondPieSize {
  const start = requireStart(cur, 'CT_SecondPieSize');
  let val: ST_SecondPieSize | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_SecondPieSize(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_SecondPieSize', a, 'ST_SecondPieSize', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SecondPieSize', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SecondPieSize', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SecondPieSize', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SecondPieSize', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_SerAx`. See `transitional/dml-chart.xsd:1267`. */
export function readCT_SerAx(cur: XmlCursor, ctx: ReadContext): CT_SerAx {
  const start = requireStart(cur, 'CT_SerAx');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SerAx', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let axId: CT_UnsignedInt | undefined;
  let scaling: CT_Scaling | undefined;
  let delete_: CT_Boolean | undefined;
  let axPos: CT_AxPos | undefined;
  let majorGridlines: CT_ChartLines | undefined;
  let minorGridlines: CT_ChartLines | undefined;
  let title: CT_Title | undefined;
  let numFmt: CT_NumFmt | undefined;
  let majorTickMark: CT_TickMark | undefined;
  let minorTickMark: CT_TickMark | undefined;
  let tickLblPos: CT_TickLblPos | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let txPr: CT_TextBody | undefined;
  let crossAx: CT_UnsignedInt | undefined;
  let content: CT_SerAx_Content | undefined;
  let tickLblSkip: CT_Skip | undefined;
  let tickMarkSkip: CT_Skip | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SerAx', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'axId': {
            axId = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'scaling': {
            scaling = readCT_Scaling(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'delete': {
            delete_ = readCT_Boolean(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'axPos': {
            axPos = readCT_AxPos(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'majorGridlines': {
            majorGridlines = readCT_ChartLines(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'minorGridlines': {
            minorGridlines = readCT_ChartLines(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'title': {
            title = readCT_Title(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'numFmt': {
            numFmt = readCT_NumFmt(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'majorTickMark': {
            majorTickMark = readCT_TickMark(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'minorTickMark': {
            minorTickMark = readCT_TickMark(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tickLblPos': {
            tickLblPos = readCT_TickLblPos(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            txPr = readCT_TextBody(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'crossAx': {
            crossAx = readCT_UnsignedInt(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'crosses': {
            content = { kind: 'crosses', value: readCT_Crosses(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'crossesAt': {
            content = { kind: 'crossesAt', value: readCT_Double(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'tickLblSkip': {
            tickLblSkip = readCT_Skip(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'tickMarkSkip': {
            tickMarkSkip = readCT_Skip(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 17;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SerAx', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SerAx', cur);
    cur.next();
  }

  return {
    axId,
    scaling,
    delete: delete_,
    axPos,
    majorGridlines,
    minorGridlines,
    title,
    numFmt,
    majorTickMark,
    minorTickMark,
    tickLblPos,
    spPr,
    txPr,
    crossAx,
    content,
    tickLblSkip,
    tickMarkSkip,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SerTx`. See `transitional/dml-chart.xsd:132`. */
export function readCT_SerTx(cur: XmlCursor, ctx: ReadContext): CT_SerTx {
  const start = requireStart(cur, 'CT_SerTx');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SerTx', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_SerTx_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SerTx', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'strRef': {
            content = { kind: 'strRef', value: readCT_StrRef(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'v': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Xstring($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_SerTx', 'v', 'ST_Xstring', $s, cur);
            content = { kind: 'v', value: ($pv ?? $s) as ST_Xstring };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SerTx', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SerTx', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Shape`. See `transitional/dml-chart.xsd:886`. */
export function readCT_Shape(cur: XmlCursor, ctx: ReadContext): CT_Shape {
  const start = requireStart(cur, 'CT_Shape');
  let val: ST_Shape | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Shape(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'ST_Shape', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Shape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Shape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Shape', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Shape', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_SizeRepresents`. See `transitional/dml-chart.xsd:347`. */
export function readCT_SizeRepresents(cur: XmlCursor, ctx: ReadContext): CT_SizeRepresents {
  const start = requireStart(cur, 'CT_SizeRepresents');
  let val: ST_SizeRepresents | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_SizeRepresents(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_SizeRepresents', a, 'ST_SizeRepresents', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SizeRepresents', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SizeRepresents', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SizeRepresents', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SizeRepresents', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Skip`. See `transitional/dml-chart.xsd:1094`. */
export function readCT_Skip(cur: XmlCursor, ctx: ReadContext): CT_Skip {
  const start = requireStart(cur, 'CT_Skip');
  let val: ST_Skip | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Skip(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Skip', a, 'ST_Skip', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Skip', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Skip', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Skip', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Skip', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Skip', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_SplitType`. See `transitional/dml-chart.xsd:385`. */
export function readCT_SplitType(cur: XmlCursor, ctx: ReadContext): CT_SplitType {
  const start = requireStart(cur, 'CT_SplitType');
  let val: ST_SplitType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_SplitType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_SplitType', a, 'ST_SplitType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SplitType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SplitType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SplitType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SplitType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_StockChart`. See `transitional/dml-chart.xsd:802`. */
export function readCT_StockChart(cur: XmlCursor, ctx: ReadContext): CT_StockChart {
  const start = requireStart(cur, 'CT_StockChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_StockChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const ser: CT_LineSer[] = [];
  let dLbls: CT_DLbls | undefined;
  let dropLines: CT_ChartLines | undefined;
  let hiLowLines: CT_ChartLines | undefined;
  let upDownBars: CT_UpDownBars | undefined;
  const axId: CT_UnsignedInt[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StockChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'ser': {
            ser.push(readCT_LineSer(cur, ctx));
            $slot = 0;
            $index = ser.length - 1;
            continue;
          }
          case 'dLbls': {
            dLbls = readCT_DLbls(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'dropLines': {
            dropLines = readCT_ChartLines(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'hiLowLines': {
            hiLowLines = readCT_ChartLines(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'upDownBars': {
            upDownBars = readCT_UpDownBars(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'axId': {
            axId.push(readCT_UnsignedInt(cur, ctx));
            $slot = 5;
            $index = axId.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_StockChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StockChart', cur);
    cur.next();
  }

  return {
    ser,
    dLbls,
    dropLines,
    hiLowLines,
    upDownBars,
    axId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_StrData`. See `transitional/dml-chart.xsd:77`. */
export function readCT_StrData(cur: XmlCursor, ctx: ReadContext): CT_StrData {
  const start = requireStart(cur, 'CT_StrData');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_StrData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ptCount: CT_UnsignedInt | undefined;
  const pt: CT_StrVal[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StrData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'ptCount': {
            ptCount = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pt': {
            pt.push(readCT_StrVal(cur, ctx));
            $slot = 1;
            $index = pt.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_StrData', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StrData', cur);
    cur.next();
  }

  return {
    ptCount,
    pt,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_StrRef`. See `transitional/dml-chart.xsd:84`. */
export function readCT_StrRef(cur: XmlCursor, ctx: ReadContext): CT_StrRef {
  const start = requireStart(cur, 'CT_StrRef');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_StrRef', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let f: string | undefined;
  let strCache: CT_StrData | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StrRef', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'f': {
            f = readScalar(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'strCache': {
            strCache = readCT_StrData(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_StrRef', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StrRef', cur);
    cur.next();
  }

  return {
    f,
    strCache,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_StrVal`. See `transitional/dml-chart.xsd:71`. */
export function readCT_StrVal(cur: XmlCursor, ctx: ReadContext): CT_StrVal {
  const start = requireStart(cur, 'CT_StrVal');
  let idx: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'idx': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            idx = v;
            continue;
          }
          ctx.invalidValue('CT_StrVal', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_StrVal', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (idx === undefined) ctx.missingRequired('CT_StrVal', 'idx', cur);

  let v_: ST_Xstring | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StrVal', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'v': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Xstring($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_StrVal', 'v', 'ST_Xstring', $s, cur);
            v_ = ($pv ?? $s) as ST_Xstring;
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_StrVal', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StrVal', cur);
    cur.next();
  }

  return {
    v: v_,
    idx,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Style`. See `transitional/dml-chart.xsd:1403`. */
export function readCT_Style(cur: XmlCursor, ctx: ReadContext): CT_Style {
  const start = requireStart(cur, 'CT_Style');
  let val: ST_Style | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Style(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Style', a, 'ST_Style', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Style', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Style', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Style', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Style', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Style', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Surface`. See `transitional/dml-chart.xsd:260`. */
export function readCT_Surface(cur: XmlCursor, ctx: ReadContext): CT_Surface {
  const start = requireStart(cur, 'CT_Surface');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Surface', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let thickness: CT_Thickness | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let pictureOptions: CT_PictureOptions | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Surface', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'thickness': {
            thickness = readCT_Thickness(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'pictureOptions': {
            pictureOptions = readCT_PictureOptions(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Surface', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Surface', cur);
    cur.next();
  }

  return {
    thickness,
    spPr,
    pictureOptions,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Surface3DChart`. See `transitional/dml-chart.xsd:1030`. */
export function readCT_Surface3DChart(cur: XmlCursor, ctx: ReadContext): CT_Surface3DChart {
  const start = requireStart(cur, 'CT_Surface3DChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Surface3DChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let wireframe: CT_Boolean | undefined;
  const ser: CT_SurfaceSer[] = [];
  let bandFmts: CT_BandFmts | undefined;
  const axId: CT_UnsignedInt[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Surface3DChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'wireframe': {
            wireframe = readCT_Boolean(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_SurfaceSer(cur, ctx));
            $slot = 1;
            $index = ser.length - 1;
            continue;
          }
          case 'bandFmts': {
            bandFmts = readCT_BandFmts(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'axId': {
            axId.push(readCT_UnsignedInt(cur, ctx));
            $slot = 3;
            $index = axId.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Surface3DChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Surface3DChart', cur);
    cur.next();
  }

  return {
    wireframe,
    ser,
    bandFmts,
    axId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SurfaceChart`. See `transitional/dml-chart.xsd:1023`. */
export function readCT_SurfaceChart(cur: XmlCursor, ctx: ReadContext): CT_SurfaceChart {
  const start = requireStart(cur, 'CT_SurfaceChart');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SurfaceChart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let wireframe: CT_Boolean | undefined;
  const ser: CT_SurfaceSer[] = [];
  let bandFmts: CT_BandFmts | undefined;
  const axId: CT_UnsignedInt[] = [];
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SurfaceChart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'wireframe': {
            wireframe = readCT_Boolean(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ser': {
            ser.push(readCT_SurfaceSer(cur, ctx));
            $slot = 1;
            $index = ser.length - 1;
            continue;
          }
          case 'bandFmts': {
            bandFmts = readCT_BandFmts(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'axId': {
            axId.push(readCT_UnsignedInt(cur, ctx));
            $slot = 3;
            $index = axId.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SurfaceChart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SurfaceChart', cur);
    cur.next();
  }

  return {
    wireframe,
    ser,
    bandFmts,
    axId,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SurfaceSer`. See `transitional/dml-chart.xsd:751`. */
export function readCT_SurfaceSer(cur: XmlCursor, ctx: ReadContext): CT_SurfaceSer {
  const start = requireStart(cur, 'CT_SurfaceSer');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SurfaceSer', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let idx: CT_UnsignedInt | undefined;
  let order: CT_UnsignedInt | undefined;
  let tx: CT_SerTx | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let cat: CT_AxDataSource | undefined;
  let val: CT_NumDataSource | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SurfaceSer', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'idx': {
            idx = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'order': {
            order = readCT_UnsignedInt(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tx': {
            tx = readCT_SerTx(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'cat': {
            cat = readCT_AxDataSource(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'val': {
            val = readCT_NumDataSource(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SurfaceSer', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SurfaceSer', cur);
    cur.next();
  }

  return {
    idx,
    order,
    tx,
    spPr,
    cat,
    val,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextLanguageID`. See `transitional/dml-chart.xsd:99`. */
export function readCT_TextLanguageID(cur: XmlCursor, ctx: ReadContext): CT_TextLanguageID {
  const start = requireStart(cur, 'CT_TextLanguageID');
  let val: ST_Lang | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Lang(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TextLanguageID', a, 'ST_Lang', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextLanguageID', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TextLanguageID', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextLanguageID', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextLanguageID', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextLanguageID', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Thickness`. See `transitional/dml-chart.xsd:276`. */
export function readCT_Thickness(cur: XmlCursor, ctx: ReadContext): CT_Thickness {
  const start = requireStart(cur, 'CT_Thickness');
  let val: ST_Thickness | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Thickness(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Thickness', a, 'ST_Thickness', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Thickness', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Thickness', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Thickness', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Thickness', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Thickness', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TickLblPos`. See `transitional/dml-chart.xsd:1086`. */
export function readCT_TickLblPos(cur: XmlCursor, ctx: ReadContext): CT_TickLblPos {
  const start = requireStart(cur, 'CT_TickLblPos');
  let val: ST_TickLblPos | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TickLblPos(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TickLblPos', a, 'ST_TickLblPos', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TickLblPos', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TickLblPos', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TickLblPos', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TickLblPos', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TickMark`. See `transitional/dml-chart.xsd:1075`. */
export function readCT_TickMark(cur: XmlCursor, ctx: ReadContext): CT_TickMark {
  const start = requireStart(cur, 'CT_TickMark');
  let val: ST_TickMark | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TickMark(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TickMark', a, 'ST_TickMark', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TickMark', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TickMark', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TickMark', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TickMark', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TimeUnit`. See `transitional/dml-chart.xsd:1104`. */
export function readCT_TimeUnit(cur: XmlCursor, ctx: ReadContext): CT_TimeUnit {
  const start = requireStart(cur, 'CT_TimeUnit');
  let val: ST_TimeUnit | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TimeUnit(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TimeUnit', a, 'ST_TimeUnit', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TimeUnit', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TimeUnit', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TimeUnit', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TimeUnit', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Title`. See `transitional/dml-chart.xsd:178`. */
export function readCT_Title(cur: XmlCursor, ctx: ReadContext): CT_Title {
  const start = requireStart(cur, 'CT_Title');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Title', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let tx: CT_Tx | undefined;
  let layout: CT_Layout | undefined;
  let overlay: CT_Boolean | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let txPr: CT_TextBody | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Title', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'tx': {
            tx = readCT_Tx(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'layout': {
            layout = readCT_Layout(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'overlay': {
            overlay = readCT_Boolean(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            txPr = readCT_TextBody(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Title', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Title', cur);
    cur.next();
  }

  return {
    tx,
    layout,
    overlay,
    spPr,
    txPr,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Trendline`. See `transitional/dml-chart.xsd:577`. */
export function readCT_Trendline(cur: XmlCursor, ctx: ReadContext): CT_Trendline {
  const start = requireStart(cur, 'CT_Trendline');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Trendline', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let name: string | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let trendlineType: CT_TrendlineType | undefined;
  let order: CT_Order | undefined;
  let period: CT_Period | undefined;
  let forward: CT_Double | undefined;
  let backward: CT_Double | undefined;
  let intercept: CT_Double | undefined;
  let dispRSqr: CT_Boolean | undefined;
  let dispEq: CT_Boolean | undefined;
  let trendlineLbl: CT_TrendlineLbl | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Trendline', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'name': {
            name = readScalar(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'trendlineType': {
            trendlineType = readCT_TrendlineType(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'order': {
            order = readCT_Order(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'period': {
            period = readCT_Period(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'forward': {
            forward = readCT_Double(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'backward': {
            backward = readCT_Double(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'intercept': {
            intercept = readCT_Double(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'dispRSqr': {
            dispRSqr = readCT_Boolean(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'dispEq': {
            dispEq = readCT_Boolean(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'trendlineLbl': {
            trendlineLbl = readCT_TrendlineLbl(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Trendline', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Trendline', cur);
    cur.next();
  }

  return {
    name,
    spPr,
    trendlineType,
    order,
    period,
    forward,
    backward,
    intercept,
    dispRSqr,
    dispEq,
    trendlineLbl,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TrendlineLbl`. See `transitional/dml-chart.xsd:567`. */
export function readCT_TrendlineLbl(cur: XmlCursor, ctx: ReadContext): CT_TrendlineLbl {
  const start = requireStart(cur, 'CT_TrendlineLbl');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TrendlineLbl', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let layout: CT_Layout | undefined;
  let tx: CT_Tx | undefined;
  let numFmt: CT_NumFmt | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let txPr: CT_TextBody | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TrendlineLbl', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'layout': {
            layout = readCT_Layout(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tx': {
            tx = readCT_Tx(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'numFmt': {
            numFmt = readCT_NumFmt(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            txPr = readCT_TextBody(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TrendlineLbl', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TrendlineLbl', cur);
    cur.next();
  }

  return {
    layout,
    tx,
    numFmt,
    spPr,
    txPr,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TrendlineType`. See `transitional/dml-chart.xsd:547`. */
export function readCT_TrendlineType(cur: XmlCursor, ctx: ReadContext): CT_TrendlineType {
  const start = requireStart(cur, 'CT_TrendlineType');
  let val: ST_TrendlineType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TrendlineType(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TrendlineType', a, 'ST_TrendlineType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TrendlineType', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TrendlineType', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TrendlineType', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TrendlineType', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Tx`. See `transitional/dml-chart.xsd:91`. */
export function readCT_Tx(cur: XmlCursor, ctx: ReadContext): CT_Tx {
  const start = requireStart(cur, 'CT_Tx');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Tx', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_Tx_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Tx', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'strRef': {
            content = { kind: 'strRef', value: readCT_StrRef(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'rich': {
            content = { kind: 'rich', value: readCT_TextBody(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Tx', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Tx', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_UnsignedInt`. See `transitional/dml-chart.xsd:24`. */
export function readCT_UnsignedInt(cur: XmlCursor, ctx: ReadContext): CT_UnsignedInt {
  const start = requireStart(cur, 'CT_UnsignedInt');
  let val: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_UnsignedInt', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_UnsignedInt', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_UnsignedInt', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_UnsignedInt', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_UnsignedInt', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_UnsignedInt', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_UpDownBar`. See `transitional/dml-chart.xsd:637`. */
export function readCT_UpDownBar(cur: XmlCursor, ctx: ReadContext): CT_UpDownBar {
  const start = requireStart(cur, 'CT_UpDownBar');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_UpDownBar', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let spPr: CT_ShapeProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_UpDownBar', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_UpDownBar', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_UpDownBar', cur);
    cur.next();
  }

  return {
    spPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_UpDownBars`. See `transitional/dml-chart.xsd:642`. */
export function readCT_UpDownBars(cur: XmlCursor, ctx: ReadContext): CT_UpDownBars {
  const start = requireStart(cur, 'CT_UpDownBars');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_UpDownBars', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let gapWidth: CT_GapAmount | undefined;
  let upBars: CT_UpDownBar | undefined;
  let downBars: CT_UpDownBar | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_UpDownBars', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'gapWidth': {
            gapWidth = readCT_GapAmount(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'upBars': {
            upBars = readCT_UpDownBar(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'downBars': {
            downBars = readCT_UpDownBar(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_UpDownBars', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_UpDownBars', cur);
    cur.next();
  }

  return {
    gapWidth,
    upBars,
    downBars,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ValAx`. See `transitional/dml-chart.xsd:1275`. */
export function readCT_ValAx(cur: XmlCursor, ctx: ReadContext): CT_ValAx {
  const start = requireStart(cur, 'CT_ValAx');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ValAx', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let axId: CT_UnsignedInt | undefined;
  let scaling: CT_Scaling | undefined;
  let delete_: CT_Boolean | undefined;
  let axPos: CT_AxPos | undefined;
  let majorGridlines: CT_ChartLines | undefined;
  let minorGridlines: CT_ChartLines | undefined;
  let title: CT_Title | undefined;
  let numFmt: CT_NumFmt | undefined;
  let majorTickMark: CT_TickMark | undefined;
  let minorTickMark: CT_TickMark | undefined;
  let tickLblPos: CT_TickLblPos | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let txPr: CT_TextBody | undefined;
  let crossAx: CT_UnsignedInt | undefined;
  let content: CT_ValAx_Content | undefined;
  let crossBetween: CT_CrossBetween | undefined;
  let majorUnit: CT_AxisUnit | undefined;
  let minorUnit: CT_AxisUnit | undefined;
  let dispUnits: CT_DispUnits | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ValAx', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'axId': {
            axId = readCT_UnsignedInt(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'scaling': {
            scaling = readCT_Scaling(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'delete': {
            delete_ = readCT_Boolean(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'axPos': {
            axPos = readCT_AxPos(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'majorGridlines': {
            majorGridlines = readCT_ChartLines(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'minorGridlines': {
            minorGridlines = readCT_ChartLines(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'title': {
            title = readCT_Title(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'numFmt': {
            numFmt = readCT_NumFmt(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'majorTickMark': {
            majorTickMark = readCT_TickMark(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'minorTickMark': {
            minorTickMark = readCT_TickMark(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'tickLblPos': {
            tickLblPos = readCT_TickLblPos(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'txPr': {
            txPr = readCT_TextBody(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'crossAx': {
            crossAx = readCT_UnsignedInt(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'crosses': {
            content = { kind: 'crosses', value: readCT_Crosses(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'crossesAt': {
            content = { kind: 'crossesAt', value: readCT_Double(cur, ctx) };
            $slot = 14;
            $index = undefined;
            continue;
          }
          case 'crossBetween': {
            crossBetween = readCT_CrossBetween(cur, ctx);
            $slot = 15;
            $index = undefined;
            continue;
          }
          case 'majorUnit': {
            majorUnit = readCT_AxisUnit(cur, ctx);
            $slot = 16;
            $index = undefined;
            continue;
          }
          case 'minorUnit': {
            minorUnit = readCT_AxisUnit(cur, ctx);
            $slot = 17;
            $index = undefined;
            continue;
          }
          case 'dispUnits': {
            dispUnits = readCT_DispUnits(cur, ctx);
            $slot = 18;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 19;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ValAx', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ValAx', cur);
    cur.next();
  }

  return {
    axId,
    scaling,
    delete: delete_,
    axPos,
    majorGridlines,
    minorGridlines,
    title,
    numFmt,
    majorTickMark,
    minorTickMark,
    tickLblPos,
    spPr,
    txPr,
    crossAx,
    content,
    crossBetween,
    majorUnit,
    minorUnit,
    dispUnits,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_View3D`. See `transitional/dml-chart.xsd:249`. */
export function readCT_View3D(cur: XmlCursor, ctx: ReadContext): CT_View3D {
  const start = requireStart(cur, 'CT_View3D');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_View3D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let rotX: CT_RotX | undefined;
  let hPercent: CT_HPercent | undefined;
  let rotY: CT_RotY | undefined;
  let depthPercent: CT_DepthPercent | undefined;
  let rAngAx: CT_Boolean | undefined;
  let perspective: CT_Perspective | undefined;
  let extLst: CT_ExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chart = ctx.uris['dml-chart'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_View3D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chart) {
        switch (ev.localName) {
          case 'rotX': {
            rotX = readCT_RotX(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hPercent': {
            hPercent = readCT_HPercent(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'rotY': {
            rotY = readCT_RotY(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'depthPercent': {
            depthPercent = readCT_DepthPercent(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'rAngAx': {
            rAngAx = readCT_Boolean(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'perspective': {
            perspective = readCT_Perspective(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_ExtensionList(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_View3D', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_View3D', cur);
    cur.next();
  }

  return {
    rotX,
    hPercent,
    rotY,
    depthPercent,
    rAngAx,
    perspective,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}
