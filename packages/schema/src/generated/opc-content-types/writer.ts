/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: writers for the opc-content-types namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import { type WriteContext, type XmlSink, uriFor } from '../../runtime/index.js';
import type { CT_Default, CT_Override, CT_Types } from './types.js';

// XML writers. Element names are supplied by the owning particle.

/** Write a `CT_Default`; the caller supplies its element local name. */
export function writeCT_Default(s: XmlSink, value: CT_Default, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'opc-content-types'), localName);
  if (value['Extension'] !== undefined) s.attr(null, 'Extension', String(value['Extension']));
  if (value['ContentType'] !== undefined) s.attr(null, 'ContentType', String(value['ContentType']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Override`; the caller supplies its element local name. */
export function writeCT_Override(s: XmlSink, value: CT_Override, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'opc-content-types'), localName);
  if (value['ContentType'] !== undefined) s.attr(null, 'ContentType', String(value['ContentType']));
  if (value['PartName'] !== undefined) s.attr(null, 'PartName', String(value['PartName']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Types`; the caller supplies its element local name. */
export function writeCT_Types(s: XmlSink, value: CT_Types, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'opc-content-types'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_content of value['content']) {
    if (v_content.kind === 'Default') writeCT_Default(s, v_content.value, ctx, 'Default');
    if (v_content.kind === 'Override') writeCT_Override(s, v_content.value, ctx, 'Override');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  s.endElement();
}
