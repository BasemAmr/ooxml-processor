/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the opc-content-types namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  isIgnorableWhitespace,
  requireStart,
} from '../../runtime/index.js';
import type {
  CT_Default,
  CT_Override,
  CT_Types,
  CT_Types_Content,
  ST_ContentType,
  ST_Extension,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_ContentType(value: string): ST_ContentType | undefined {
  return value as ST_ContentType;
}

export function parseST_Extension(value: string): ST_Extension | undefined {
  return value as ST_Extension;
}

// Complex-type readers.

/** Read a `CT_Default`. See `opc/opc-contentTypes.xsd:18`. */
export function readCT_Default(cur: XmlCursor, ctx: ReadContext): CT_Default {
  const start = requireStart(cur, 'CT_Default');
  let Extension: ST_Extension | undefined;
  let ContentType: ST_ContentType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'Extension': {
          const v = parseST_Extension(a.value);
          if (v !== undefined) {
            Extension = v;
            continue;
          }
          ctx.invalidValue('CT_Default', a, 'ST_Extension', cur);
          break;
        }
        case 'ContentType': {
          const v = parseST_ContentType(a.value);
          if (v !== undefined) {
            ContentType = v;
            continue;
          }
          ctx.invalidValue('CT_Default', a, 'ST_ContentType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Default', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (Extension === undefined) ctx.missingRequired('CT_Default', 'Extension', cur);
  if (ContentType === undefined) ctx.missingRequired('CT_Default', 'ContentType', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Default', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Default', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Default', cur);
    cur.next();
  }
  return {
    Extension,
    ContentType,
    $unknownAttrs,
  };
}

/** Read a `CT_Override`. See `opc/opc-contentTypes.xsd:23`. */
export function readCT_Override(cur: XmlCursor, ctx: ReadContext): CT_Override {
  const start = requireStart(cur, 'CT_Override');
  let ContentType: ST_ContentType | undefined;
  let PartName: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'ContentType': {
          const v = parseST_ContentType(a.value);
          if (v !== undefined) {
            ContentType = v;
            continue;
          }
          ctx.invalidValue('CT_Override', a, 'ST_ContentType', cur);
          break;
        }
        case 'PartName':
          PartName = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Override', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (ContentType === undefined) ctx.missingRequired('CT_Override', 'ContentType', cur);
  if (PartName === undefined) ctx.missingRequired('CT_Override', 'PartName', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Override', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Override', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Override', cur);
    cur.next();
  }
  return {
    ContentType,
    PartName,
    $unknownAttrs,
  };
}

/** Read a `CT_Types`. See `opc/opc-contentTypes.xsd:11`. */
export function readCT_Types(cur: XmlCursor, ctx: ReadContext): CT_Types {
  const start = requireStart(cur, 'CT_Types');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Types', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_Types_Content[] = [];

  const NS_opc_content_types = ctx.uris['opc-content-types'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Types', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_opc_content_types) {
        switch (ev.localName) {
          case 'Default': {
            content.push({ kind: 'Default', value: readCT_Default(cur, ctx) });
            continue;
          }
          case 'Override': {
            content.push({ kind: 'Override', value: readCT_Override(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Types', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Types', cur);
    cur.next();
  }

  return {
    content,
    $unknownAttrs,
  };
}
