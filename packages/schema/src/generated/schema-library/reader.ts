/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the schema-library namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  isIgnorableWhitespace,
  requireStart,
} from '../../runtime/index.js';
import type { CT_Schema, CT_SchemaLibrary } from './types.js';

// Complex-type readers.

/** Read a `CT_Schema`. See `transitional/shared-customXmlSchemaProperties.xsd:6`. */
export function readCT_Schema(cur: XmlCursor, ctx: ReadContext): CT_Schema {
  const start = requireStart(cur, 'CT_Schema');
  let uri: string | undefined;
  let manifestLocation: string | undefined;
  let schemaLocation: string | undefined;
  let schemaLanguage: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_schema_library_A = ctx.uris['schema-library'];
  for (const a of start.attrs) {
    if (a.uri === NS_schema_library_A) {
      switch (a.localName) {
        case 'uri':
          uri = a.value as string;
          continue;

        case 'manifestLocation':
          manifestLocation = a.value as string;
          continue;

        case 'schemaLocation':
          schemaLocation = a.value as string;
          continue;

        case 'schemaLanguage':
          schemaLanguage = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Schema', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Schema', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Schema', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Schema', cur);
    cur.next();
  }
  return {
    uri,
    manifestLocation,
    schemaLocation,
    schemaLanguage,
    $unknownAttrs,
  };
}

/** Read a `CT_SchemaLibrary`. See `transitional/shared-customXmlSchemaProperties.xsd:12`. */
export function readCT_SchemaLibrary(cur: XmlCursor, ctx: ReadContext): CT_SchemaLibrary {
  const start = requireStart(cur, 'CT_SchemaLibrary');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SchemaLibrary', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const schema: CT_Schema[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_schema_library = ctx.uris['schema-library'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SchemaLibrary', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_schema_library) {
        switch (ev.localName) {
          case 'schema': {
            schema.push(readCT_Schema(cur, ctx));
            $slot = 0;
            $index = schema.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SchemaLibrary', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SchemaLibrary', cur);
    cur.next();
  }

  return {
    schema,
    $unknown,
    $unknownAttrs,
  };
}
