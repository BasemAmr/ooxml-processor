/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: writers for the schema-library namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import { type WriteContext, type XmlSink, uriFor } from '../../runtime/index.js';
import type { CT_Schema, CT_SchemaLibrary } from './types.js';

// XML writers. Element names are supplied by the owning particle.

/** Write a `CT_Schema`; the caller supplies its element local name. */
export function writeCT_Schema(s: XmlSink, value: CT_Schema, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'schema-library'), localName);
  if (value['uri'] !== undefined) s.attr(uriFor(ctx, 'schema-library'), 'uri', String(value['uri']));
  if (value['manifestLocation'] !== undefined) s.attr(uriFor(ctx, 'schema-library'), 'manifestLocation', String(value['manifestLocation']));
  if (value['schemaLocation'] !== undefined) s.attr(uriFor(ctx, 'schema-library'), 'schemaLocation', String(value['schemaLocation']));
  if (value['schemaLanguage'] !== undefined) s.attr(uriFor(ctx, 'schema-library'), 'schemaLanguage', String(value['schemaLanguage']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_SchemaLibrary`; the caller supplies its element local name. */
export function writeCT_SchemaLibrary(s: XmlSink, value: CT_SchemaLibrary, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'schema-library'), localName);
  for (const v_schema of value['schema']) {
    writeCT_Schema(s, v_schema, ctx, 'schema');
  }
  for (const u of value.$unknown ?? []) s.raw(u.node);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}
