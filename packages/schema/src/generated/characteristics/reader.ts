/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the characteristics namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  requireStart,
} from '../../runtime/index.js';
import {
  type CT_AdditionalCharacteristics,
  type CT_Characteristic,
  type ST_Relation,
  ST_Relation_VALUES,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_Relation(value: string): ST_Relation | undefined {
  const s = collapse(value);
  return ST_Relation_VALUES.has(s) ? (s as ST_Relation) : undefined;
}

// Complex-type readers.

/**
 * Read a `CT_AdditionalCharacteristics`. See
 * `transitional/shared-additionalCharacteristics.xsd:6`.
 */
export function readCT_AdditionalCharacteristics(cur: XmlCursor, ctx: ReadContext): CT_AdditionalCharacteristics {
  const start = requireStart(cur, 'CT_AdditionalCharacteristics');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AdditionalCharacteristics', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const characteristic: CT_Characteristic[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_characteristics = ctx.uris['characteristics'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AdditionalCharacteristics', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_characteristics) {
        switch (ev.localName) {
          case 'characteristic': {
            characteristic.push(readCT_Characteristic(cur, ctx));
            $slot = 0;
            $index = characteristic.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AdditionalCharacteristics', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AdditionalCharacteristics', cur);
    cur.next();
  }

  return {
    characteristic,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Characteristic`. See `transitional/shared-additionalCharacteristics.xsd:12`. */
export function readCT_Characteristic(cur: XmlCursor, ctx: ReadContext): CT_Characteristic {
  const start = requireStart(cur, 'CT_Characteristic');
  let name: string | undefined;
  let relation: ST_Relation | undefined;
  let val: string | undefined;
  let vocabulary: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

        case 'relation': {
          const v = parseST_Relation(a.value);
          if (v !== undefined) {
            relation = v;
            continue;
          }
          ctx.invalidValue('CT_Characteristic', a, 'ST_Relation', cur);
          break;
        }
        case 'val':
          val = a.value as string;
          continue;

        case 'vocabulary':
          vocabulary = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_Characteristic', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_Characteristic', 'name', cur);
  if (relation === undefined) ctx.missingRequired('CT_Characteristic', 'relation', cur);
  if (val === undefined) ctx.missingRequired('CT_Characteristic', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Characteristic', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Characteristic', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Characteristic', cur);
    cur.next();
  }
  return {
    name,
    relation,
    val,
    vocabulary,
    $unknownAttrs,
  };
}
