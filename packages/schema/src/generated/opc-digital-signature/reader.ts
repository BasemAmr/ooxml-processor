/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the opc-digital-signature namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  isIgnorableWhitespace,
  readScalar,
  requireStart,
} from '../../runtime/index.js';
import type {
  CT_RelationshipReference,
  CT_RelationshipsGroupReference,
  CT_SignatureTime,
  ST_Format,
  ST_Value,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_Format(value: string): ST_Format | undefined {
  return value as ST_Format;
}

export function parseST_Value(value: string): ST_Value | undefined {
  return value as ST_Value;
}

// Complex-type readers.

/** Read a `CT_RelationshipReference`. See `opc/opc-digSig.xsd:18`. */
export function readCT_RelationshipReference(cur: XmlCursor, ctx: ReadContext): CT_RelationshipReference {
  const start = requireStart(cur, 'CT_RelationshipReference');
  let SourceId: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'SourceId':
          SourceId = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_RelationshipReference', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (SourceId === undefined) ctx.missingRequired('CT_RelationshipReference', 'SourceId', cur);

  let $value = '';
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RelationshipReference', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'text') { $value += ev.value; cur.next(); continue; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_RelationshipReference', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    cur.next();
  }

  return {
    $value: $value,
    SourceId,
    $unknownAttrs,
  };
}

/** Read a `CT_RelationshipsGroupReference`. See `opc/opc-digSig.xsd:26`. */
export function readCT_RelationshipsGroupReference(cur: XmlCursor, ctx: ReadContext): CT_RelationshipsGroupReference {
  const start = requireStart(cur, 'CT_RelationshipsGroupReference');
  let SourceType: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'SourceType':
          SourceType = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_RelationshipsGroupReference', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (SourceType === undefined) ctx.missingRequired('CT_RelationshipsGroupReference', 'SourceType', cur);

  let $value = '';
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RelationshipsGroupReference', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'text') { $value += ev.value; cur.next(); continue; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_RelationshipsGroupReference', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    cur.next();
  }

  return {
    $value: $value,
    SourceType,
    $unknownAttrs,
  };
}

/** Read a `CT_SignatureTime`. See `opc/opc-digSig.xsd:11`. */
export function readCT_SignatureTime(cur: XmlCursor, ctx: ReadContext): CT_SignatureTime {
  const start = requireStart(cur, 'CT_SignatureTime');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SignatureTime', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let Format: ST_Format | undefined;
  let Value: ST_Value | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_opc_digital_signature = ctx.uris['opc-digital-signature'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SignatureTime', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_opc_digital_signature) {
        switch (ev.localName) {
          case 'Format': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Format($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_SignatureTime', 'Format', 'ST_Format', $s, cur);
            Format = ($pv ?? $s) as ST_Format;
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'Value': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Value($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_SignatureTime', 'Value', 'ST_Value', $s, cur);
            Value = ($pv ?? $s) as ST_Value;
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SignatureTime', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SignatureTime', cur);
    cur.next();
  }

  return {
    Format,
    Value,
    $unknown,
    $unknownAttrs,
  };
}
