/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: writers for the dml-main namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  PositionedRawQueue,
  type WriteContext,
  type XmlSink,
  uriFor,
} from '../../runtime/index.js';
import type {
  CT_AdjPoint2D,
  CT_AdjustHandleList,
  CT_AlphaBiLevelEffect,
  CT_AlphaCeilingEffect,
  CT_AlphaFloorEffect,
  CT_AlphaInverseEffect,
  CT_AlphaModulateEffect,
  CT_AlphaModulateFixedEffect,
  CT_AlphaOutsetEffect,
  CT_AlphaReplaceEffect,
  CT_Angle,
  CT_AnimationChartBuildProperties,
  CT_AnimationChartElement,
  CT_AnimationDgmBuildProperties,
  CT_AnimationDgmElement,
  CT_AnimationElementChoice,
  CT_AnimationGraphicalObjectBuildProperties,
  CT_AudioCD,
  CT_AudioCDTime,
  CT_AudioFile,
  CT_Backdrop,
  CT_BackgroundFillStyleList,
  CT_BackgroundFormatting,
  CT_BaseStyles,
  CT_BaseStylesOverride,
  CT_Bevel,
  CT_BiLevelEffect,
  CT_BlendEffect,
  CT_Blip,
  CT_BlipFillProperties,
  CT_BlurEffect,
  CT_Boolean,
  CT_Camera,
  CT_Cell3D,
  CT_ClipboardStyleSheet,
  CT_Color,
  CT_ColorChangeEffect,
  CT_ColorMRU,
  CT_ColorMapping,
  CT_ColorMappingOverride,
  CT_ColorReplaceEffect,
  CT_ColorScheme,
  CT_ColorSchemeAndMapping,
  CT_ColorSchemeList,
  CT_ComplementTransform,
  CT_Connection,
  CT_ConnectionSite,
  CT_ConnectionSiteList,
  CT_ConnectorLocking,
  CT_ContentPartLocking,
  CT_CustomColor,
  CT_CustomColorList,
  CT_CustomGeometry2D,
  CT_DashStop,
  CT_DashStopList,
  CT_DefaultShapeDefinition,
  CT_DuotoneEffect,
  CT_EffectContainer,
  CT_EffectList,
  CT_EffectProperties,
  CT_EffectReference,
  CT_EffectStyleItem,
  CT_EffectStyleList,
  CT_EmbeddedWAVAudioFile,
  CT_EmptyElement,
  CT_FillEffect,
  CT_FillOverlayEffect,
  CT_FillProperties,
  CT_FillStyleList,
  CT_FixedPercentage,
  CT_FlatText,
  CT_FontCollection,
  CT_FontReference,
  CT_FontScheme,
  CT_GammaTransform,
  CT_GeomGuide,
  CT_GeomGuideList,
  CT_GeomRect,
  CT_GlowEffect,
  CT_GradientFillProperties,
  CT_GradientStop,
  CT_GradientStopList,
  CT_GraphicalObject,
  CT_GraphicalObjectData,
  CT_GraphicalObjectFrameLocking,
  CT_GrayscaleEffect,
  CT_GrayscaleTransform,
  CT_GroupFillProperties,
  CT_GroupLocking,
  CT_GroupShapeProperties,
  CT_GroupTransform2D,
  CT_GvmlConnector,
  CT_GvmlConnectorNonVisual,
  CT_GvmlGraphicFrameNonVisual,
  CT_GvmlGraphicalObjectFrame,
  CT_GvmlGroupShape,
  CT_GvmlGroupShapeNonVisual,
  CT_GvmlPicture,
  CT_GvmlPictureNonVisual,
  CT_GvmlShape,
  CT_GvmlShapeNonVisual,
  CT_GvmlTextShape,
  CT_GvmlUseShapeRectangle,
  CT_HSLEffect,
  CT_Headers,
  CT_HslColor,
  CT_Hyperlink,
  CT_InnerShadowEffect,
  CT_InverseGammaTransform,
  CT_InverseTransform,
  CT_LightRig,
  CT_LineEndProperties,
  CT_LineJoinBevel,
  CT_LineJoinMiterProperties,
  CT_LineJoinRound,
  CT_LineProperties,
  CT_LineStyleList,
  CT_LinearShadeProperties,
  CT_LuminanceEffect,
  CT_NoFillProperties,
  CT_NonVisualConnectorProperties,
  CT_NonVisualContentPartProperties,
  CT_NonVisualDrawingProps,
  CT_NonVisualDrawingShapeProps,
  CT_NonVisualGraphicFrameProperties,
  CT_NonVisualGroupDrawingShapeProps,
  CT_NonVisualPictureProperties,
  CT_ObjectStyleDefaults,
  CT_OfficeArtExtension,
  CT_OfficeArtExtensionList,
  CT_OfficeStyleSheet,
  CT_OuterShadowEffect,
  CT_Path2D,
  CT_Path2DArcTo,
  CT_Path2DClose,
  CT_Path2DCubicBezierTo,
  CT_Path2DLineTo,
  CT_Path2DList,
  CT_Path2DMoveTo,
  CT_Path2DQuadBezierTo,
  CT_PathShadeProperties,
  CT_PatternFillProperties,
  CT_Percentage,
  CT_PictureLocking,
  CT_Point2D,
  CT_Point3D,
  CT_PolarAdjustHandle,
  CT_PositiveFixedAngle,
  CT_PositiveFixedPercentage,
  CT_PositivePercentage,
  CT_PositiveSize2D,
  CT_PresetColor,
  CT_PresetGeometry2D,
  CT_PresetLineDashProperties,
  CT_PresetShadowEffect,
  CT_PresetTextShape,
  CT_QuickTimeFile,
  CT_Ratio,
  CT_ReflectionEffect,
  CT_RegularTextRun,
  CT_RelativeOffsetEffect,
  CT_RelativeRect,
  CT_SRgbColor,
  CT_ScRgbColor,
  CT_Scale2D,
  CT_Scene3D,
  CT_SchemeColor,
  CT_Shape3D,
  CT_ShapeLocking,
  CT_ShapeProperties,
  CT_ShapeStyle,
  CT_SoftEdgesEffect,
  CT_SolidColorFillProperties,
  CT_SphereCoords,
  CT_StretchInfoProperties,
  CT_StyleMatrix,
  CT_StyleMatrixReference,
  CT_SupplementalFont,
  CT_SystemColor,
  CT_Table,
  CT_TableBackgroundStyle,
  CT_TableCell,
  CT_TableCellBorderStyle,
  CT_TableCellProperties,
  CT_TableCol,
  CT_TableGrid,
  CT_TablePartStyle,
  CT_TableProperties,
  CT_TableRow,
  CT_TableStyle,
  CT_TableStyleCellStyle,
  CT_TableStyleList,
  CT_TableStyleTextStyle,
  CT_TextAutonumberBullet,
  CT_TextBlipBullet,
  CT_TextBody,
  CT_TextBodyProperties,
  CT_TextBulletColorFollowText,
  CT_TextBulletSizeFollowText,
  CT_TextBulletSizePercent,
  CT_TextBulletSizePoint,
  CT_TextBulletTypefaceFollowText,
  CT_TextCharBullet,
  CT_TextCharacterProperties,
  CT_TextField,
  CT_TextFont,
  CT_TextLineBreak,
  CT_TextListStyle,
  CT_TextNoAutofit,
  CT_TextNoBullet,
  CT_TextNormalAutofit,
  CT_TextParagraph,
  CT_TextParagraphProperties,
  CT_TextShapeAutofit,
  CT_TextSpacing,
  CT_TextSpacingPercent,
  CT_TextSpacingPoint,
  CT_TextTabStop,
  CT_TextTabStopList,
  CT_TextUnderlineFillFollowText,
  CT_TextUnderlineFillGroupWrapper,
  CT_TextUnderlineLineFollowText,
  CT_ThemeableLineStyle,
  CT_TileInfoProperties,
  CT_TintEffect,
  CT_Transform2D,
  CT_TransformEffect,
  CT_Vector3D,
  CT_VideoFile,
  CT_WholeE2oFormatting,
  CT_XYAdjustHandle,
} from './types.js';

// XML writers. Element names are supplied by the owning particle.

/** Write a `CT_AdjPoint2D`; the caller supplies its element local name. */
export function writeCT_AdjPoint2D(s: XmlSink, value: CT_AdjPoint2D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['x'] !== undefined) s.attr(null, 'x', String(value['x']));
  if (value['y'] !== undefined) s.attr(null, 'y', String(value['y']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AdjustHandleList`; the caller supplies its element local name. */
export function writeCT_AdjustHandleList(s: XmlSink, value: CT_AdjustHandleList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_content of value['content']) {
    if (v_content.kind === 'ahXY') writeCT_XYAdjustHandle(s, v_content.value, ctx, 'ahXY');
    if (v_content.kind === 'ahPolar') writeCT_PolarAdjustHandle(s, v_content.value, ctx, 'ahPolar');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  s.endElement();
}

/** Write a `CT_AlphaBiLevelEffect`; the caller supplies its element local name. */
export function writeCT_AlphaBiLevelEffect(s: XmlSink, value: CT_AlphaBiLevelEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['thresh'] !== undefined) s.attr(null, 'thresh', String(value['thresh']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AlphaCeilingEffect`; the caller supplies its element local name. */
export function writeCT_AlphaCeilingEffect(s: XmlSink, value: CT_AlphaCeilingEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AlphaFloorEffect`; the caller supplies its element local name. */
export function writeCT_AlphaFloorEffect(s: XmlSink, value: CT_AlphaFloorEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AlphaInverseEffect`; the caller supplies its element local name. */
export function writeCT_AlphaInverseEffect(s: XmlSink, value: CT_AlphaInverseEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_AlphaModulateEffect`; the caller supplies its element local name. */
export function writeCT_AlphaModulateEffect(s: XmlSink, value: CT_AlphaModulateEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cont'] !== undefined) writeCT_EffectContainer(s, value['cont'], ctx, 'cont');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_AlphaModulateFixedEffect`; the caller supplies its element local name. */
export function writeCT_AlphaModulateFixedEffect(s: XmlSink, value: CT_AlphaModulateFixedEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['amt'] !== undefined) s.attr(null, 'amt', String(value['amt']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AlphaOutsetEffect`; the caller supplies its element local name. */
export function writeCT_AlphaOutsetEffect(s: XmlSink, value: CT_AlphaOutsetEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['rad'] !== undefined) s.attr(null, 'rad', String(value['rad']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AlphaReplaceEffect`; the caller supplies its element local name. */
export function writeCT_AlphaReplaceEffect(s: XmlSink, value: CT_AlphaReplaceEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['a'] !== undefined) s.attr(null, 'a', String(value['a']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Angle`; the caller supplies its element local name. */
export function writeCT_Angle(s: XmlSink, value: CT_Angle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AnimationChartBuildProperties`; the caller supplies its element local name. */
export function writeCT_AnimationChartBuildProperties(s: XmlSink, value: CT_AnimationChartBuildProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['bld'] !== undefined) s.attr(null, 'bld', String(value['bld']));
  if (value['animBg'] !== undefined) s.attr(null, 'animBg', String(value['animBg']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AnimationChartElement`; the caller supplies its element local name. */
export function writeCT_AnimationChartElement(s: XmlSink, value: CT_AnimationChartElement, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['seriesIdx'] !== undefined) s.attr(null, 'seriesIdx', String(value['seriesIdx']));
  if (value['categoryIdx'] !== undefined) s.attr(null, 'categoryIdx', String(value['categoryIdx']));
  if (value['bldStep'] !== undefined) s.attr(null, 'bldStep', String(value['bldStep']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AnimationDgmBuildProperties`; the caller supplies its element local name. */
export function writeCT_AnimationDgmBuildProperties(s: XmlSink, value: CT_AnimationDgmBuildProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['bld'] !== undefined) s.attr(null, 'bld', String(value['bld']));
  if (value['rev'] !== undefined) s.attr(null, 'rev', String(value['rev']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AnimationDgmElement`; the caller supplies its element local name. */
export function writeCT_AnimationDgmElement(s: XmlSink, value: CT_AnimationDgmElement, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['id'] !== undefined) s.attr(null, 'id', String(value['id']));
  if (value['bldStep'] !== undefined) s.attr(null, 'bldStep', String(value['bldStep']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AnimationElementChoice`; the caller supplies its element local name. */
export function writeCT_AnimationElementChoice(s: XmlSink, value: CT_AnimationElementChoice, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'dgm') writeCT_AnimationDgmElement(s, value['content'].value, ctx, 'dgm');
    if (value['content'].kind === 'chart') writeCT_AnimationChartElement(s, value['content'].value, ctx, 'chart');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/**
 * Write a `CT_AnimationGraphicalObjectBuildProperties`; the caller supplies its element local
 * name.
 */
export function writeCT_AnimationGraphicalObjectBuildProperties(s: XmlSink, value: CT_AnimationGraphicalObjectBuildProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'bldDgm') writeCT_AnimationDgmBuildProperties(s, value['content'].value, ctx, 'bldDgm');
    if (value['content'].kind === 'bldChart') writeCT_AnimationChartBuildProperties(s, value['content'].value, ctx, 'bldChart');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_AudioCD`; the caller supplies its element local name. */
export function writeCT_AudioCD(s: XmlSink, value: CT_AudioCD, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['st'] !== undefined) writeCT_AudioCDTime(s, value['st'], ctx, 'st');
  $q.flush(s, 0);
  if (value['end'] !== undefined) writeCT_AudioCDTime(s, value['end'], ctx, 'end');
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_AudioCDTime`; the caller supplies its element local name. */
export function writeCT_AudioCDTime(s: XmlSink, value: CT_AudioCDTime, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['track'] !== undefined) s.attr(null, 'track', String(value['track']));
  if (value['time'] !== undefined) s.attr(null, 'time', String(value['time']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_AudioFile`; the caller supplies its element local name. */
export function writeCT_AudioFile(s: XmlSink, value: CT_AudioFile, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['link'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'link', String(value['link']));
  if (value['contentType'] !== undefined) s.attr(null, 'contentType', String(value['contentType']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Backdrop`; the caller supplies its element local name. */
export function writeCT_Backdrop(s: XmlSink, value: CT_Backdrop, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['anchor'] !== undefined) writeCT_Point3D(s, value['anchor'], ctx, 'anchor');
  $q.flush(s, 0);
  if (value['norm'] !== undefined) writeCT_Vector3D(s, value['norm'], ctx, 'norm');
  $q.flush(s, 1);
  if (value['up'] !== undefined) writeCT_Vector3D(s, value['up'], ctx, 'up');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_BackgroundFillStyleList`; the caller supplies its element local name. */
export function writeCT_BackgroundFillStyleList(s: XmlSink, value: CT_BackgroundFillStyleList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_fillProperties of value['fillProperties']) {
    if (v_fillProperties.kind === 'noFill') writeCT_NoFillProperties(s, v_fillProperties.value, ctx, 'noFill');
    if (v_fillProperties.kind === 'solidFill') writeCT_SolidColorFillProperties(s, v_fillProperties.value, ctx, 'solidFill');
    if (v_fillProperties.kind === 'gradFill') writeCT_GradientFillProperties(s, v_fillProperties.value, ctx, 'gradFill');
    if (v_fillProperties.kind === 'blipFill') writeCT_BlipFillProperties(s, v_fillProperties.value, ctx, 'blipFill');
    if (v_fillProperties.kind === 'pattFill') writeCT_PatternFillProperties(s, v_fillProperties.value, ctx, 'pattFill');
    if (v_fillProperties.kind === 'grpFill') writeCT_GroupFillProperties(s, v_fillProperties.value, ctx, 'grpFill');
    if (v_fillProperties.kind === '$raw') { s.raw(v_fillProperties.value); }
  }
  s.endElement();
}

/** Write a `CT_BackgroundFormatting`; the caller supplies its element local name. */
export function writeCT_BackgroundFormatting(s: XmlSink, value: CT_BackgroundFormatting, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['fillProperties'] !== undefined) {
    if (value['fillProperties'].kind === 'noFill') writeCT_NoFillProperties(s, value['fillProperties'].value, ctx, 'noFill');
    if (value['fillProperties'].kind === 'solidFill') writeCT_SolidColorFillProperties(s, value['fillProperties'].value, ctx, 'solidFill');
    if (value['fillProperties'].kind === 'gradFill') writeCT_GradientFillProperties(s, value['fillProperties'].value, ctx, 'gradFill');
    if (value['fillProperties'].kind === 'blipFill') writeCT_BlipFillProperties(s, value['fillProperties'].value, ctx, 'blipFill');
    if (value['fillProperties'].kind === 'pattFill') writeCT_PatternFillProperties(s, value['fillProperties'].value, ctx, 'pattFill');
    if (value['fillProperties'].kind === 'grpFill') writeCT_GroupFillProperties(s, value['fillProperties'].value, ctx, 'grpFill');
  }
  $q.flush(s, 0);
  if (value['effectProperties'] !== undefined) {
    if (value['effectProperties'].kind === 'effectLst') writeCT_EffectList(s, value['effectProperties'].value, ctx, 'effectLst');
    if (value['effectProperties'].kind === 'effectDag') writeCT_EffectContainer(s, value['effectProperties'].value, ctx, 'effectDag');
  }
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_BaseStyles`; the caller supplies its element local name. */
export function writeCT_BaseStyles(s: XmlSink, value: CT_BaseStyles, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['clrScheme'] !== undefined) writeCT_ColorScheme(s, value['clrScheme'], ctx, 'clrScheme');
  $q.flush(s, 0);
  if (value['fontScheme'] !== undefined) writeCT_FontScheme(s, value['fontScheme'], ctx, 'fontScheme');
  $q.flush(s, 1);
  if (value['fmtScheme'] !== undefined) writeCT_StyleMatrix(s, value['fmtScheme'], ctx, 'fmtScheme');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_BaseStylesOverride`; the caller supplies its element local name. */
export function writeCT_BaseStylesOverride(s: XmlSink, value: CT_BaseStylesOverride, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['clrScheme'] !== undefined) writeCT_ColorScheme(s, value['clrScheme'], ctx, 'clrScheme');
  $q.flush(s, 0);
  if (value['fontScheme'] !== undefined) writeCT_FontScheme(s, value['fontScheme'], ctx, 'fontScheme');
  $q.flush(s, 1);
  if (value['fmtScheme'] !== undefined) writeCT_StyleMatrix(s, value['fmtScheme'], ctx, 'fmtScheme');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Bevel`; the caller supplies its element local name. */
export function writeCT_Bevel(s: XmlSink, value: CT_Bevel, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['w'] !== undefined) s.attr(null, 'w', String(value['w']));
  if (value['h'] !== undefined) s.attr(null, 'h', String(value['h']));
  if (value['prst'] !== undefined) s.attr(null, 'prst', String(value['prst']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_BiLevelEffect`; the caller supplies its element local name. */
export function writeCT_BiLevelEffect(s: XmlSink, value: CT_BiLevelEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['thresh'] !== undefined) s.attr(null, 'thresh', String(value['thresh']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_BlendEffect`; the caller supplies its element local name. */
export function writeCT_BlendEffect(s: XmlSink, value: CT_BlendEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['blend'] !== undefined) s.attr(null, 'blend', String(value['blend']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cont'] !== undefined) writeCT_EffectContainer(s, value['cont'], ctx, 'cont');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Blip`; the caller supplies its element local name. */
export function writeCT_Blip(s: XmlSink, value: CT_Blip, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['embed'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'embed', String(value['embed']));
  if (value['link'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'link', String(value['link']));
  if (value['cstate'] !== undefined) s.attr(null, 'cstate', String(value['cstate']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_content of value['content']) {
    if (v_content.kind === 'alphaBiLevel') writeCT_AlphaBiLevelEffect(s, v_content.value, ctx, 'alphaBiLevel');
    if (v_content.kind === 'alphaCeiling') writeCT_AlphaCeilingEffect(s, v_content.value, ctx, 'alphaCeiling');
    if (v_content.kind === 'alphaFloor') writeCT_AlphaFloorEffect(s, v_content.value, ctx, 'alphaFloor');
    if (v_content.kind === 'alphaInv') writeCT_AlphaInverseEffect(s, v_content.value, ctx, 'alphaInv');
    if (v_content.kind === 'alphaMod') writeCT_AlphaModulateEffect(s, v_content.value, ctx, 'alphaMod');
    if (v_content.kind === 'alphaModFix') writeCT_AlphaModulateFixedEffect(s, v_content.value, ctx, 'alphaModFix');
    if (v_content.kind === 'alphaRepl') writeCT_AlphaReplaceEffect(s, v_content.value, ctx, 'alphaRepl');
    if (v_content.kind === 'biLevel') writeCT_BiLevelEffect(s, v_content.value, ctx, 'biLevel');
    if (v_content.kind === 'blur') writeCT_BlurEffect(s, v_content.value, ctx, 'blur');
    if (v_content.kind === 'clrChange') writeCT_ColorChangeEffect(s, v_content.value, ctx, 'clrChange');
    if (v_content.kind === 'clrRepl') writeCT_ColorReplaceEffect(s, v_content.value, ctx, 'clrRepl');
    if (v_content.kind === 'duotone') writeCT_DuotoneEffect(s, v_content.value, ctx, 'duotone');
    if (v_content.kind === 'fillOverlay') writeCT_FillOverlayEffect(s, v_content.value, ctx, 'fillOverlay');
    if (v_content.kind === 'grayscl') writeCT_GrayscaleEffect(s, v_content.value, ctx, 'grayscl');
    if (v_content.kind === 'hsl') writeCT_HSLEffect(s, v_content.value, ctx, 'hsl');
    if (v_content.kind === 'lum') writeCT_LuminanceEffect(s, v_content.value, ctx, 'lum');
    if (v_content.kind === 'tint') writeCT_TintEffect(s, v_content.value, ctx, 'tint');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  s.endElement();
}

/** Write a `CT_BlipFillProperties`; the caller supplies its element local name. */
export function writeCT_BlipFillProperties(s: XmlSink, value: CT_BlipFillProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['dpi'] !== undefined) s.attr(null, 'dpi', String(value['dpi']));
  if (value['rotWithShape'] !== undefined) s.attr(null, 'rotWithShape', String(value['rotWithShape']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['blip'] !== undefined) writeCT_Blip(s, value['blip'], ctx, 'blip');
  $q.flush(s, 0);
  if (value['srcRect'] !== undefined) writeCT_RelativeRect(s, value['srcRect'], ctx, 'srcRect');
  $q.flush(s, 1);
  if (value['fillModeProperties'] !== undefined) {
    if (value['fillModeProperties'].kind === 'tile') writeCT_TileInfoProperties(s, value['fillModeProperties'].value, ctx, 'tile');
    if (value['fillModeProperties'].kind === 'stretch') writeCT_StretchInfoProperties(s, value['fillModeProperties'].value, ctx, 'stretch');
  }
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_BlurEffect`; the caller supplies its element local name. */
export function writeCT_BlurEffect(s: XmlSink, value: CT_BlurEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['rad'] !== undefined) s.attr(null, 'rad', String(value['rad']));
  if (value['grow'] !== undefined) s.attr(null, 'grow', String(value['grow']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Boolean`; the caller supplies its element local name. */
export function writeCT_Boolean(s: XmlSink, value: CT_Boolean, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Camera`; the caller supplies its element local name. */
export function writeCT_Camera(s: XmlSink, value: CT_Camera, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['prst'] !== undefined) s.attr(null, 'prst', String(value['prst']));
  if (value['fov'] !== undefined) s.attr(null, 'fov', String(value['fov']));
  if (value['zoom'] !== undefined) s.attr(null, 'zoom', String(value['zoom']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['rot'] !== undefined) writeCT_SphereCoords(s, value['rot'], ctx, 'rot');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Cell3D`; the caller supplies its element local name. */
export function writeCT_Cell3D(s: XmlSink, value: CT_Cell3D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['prstMaterial'] !== undefined) s.attr(null, 'prstMaterial', String(value['prstMaterial']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['bevel'] !== undefined) writeCT_Bevel(s, value['bevel'], ctx, 'bevel');
  $q.flush(s, 0);
  if (value['lightRig'] !== undefined) writeCT_LightRig(s, value['lightRig'], ctx, 'lightRig');
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ClipboardStyleSheet`; the caller supplies its element local name. */
export function writeCT_ClipboardStyleSheet(s: XmlSink, value: CT_ClipboardStyleSheet, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['themeElements'] !== undefined) writeCT_BaseStyles(s, value['themeElements'], ctx, 'themeElements');
  $q.flush(s, 0);
  if (value['clrMap'] !== undefined) writeCT_ColorMapping(s, value['clrMap'], ctx, 'clrMap');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Color`; the caller supplies its element local name. */
export function writeCT_Color(s: XmlSink, value: CT_Color, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ColorChangeEffect`; the caller supplies its element local name. */
export function writeCT_ColorChangeEffect(s: XmlSink, value: CT_ColorChangeEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['useA'] !== undefined) s.attr(null, 'useA', String(value['useA']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['clrFrom'] !== undefined) writeCT_Color(s, value['clrFrom'], ctx, 'clrFrom');
  $q.flush(s, 0);
  if (value['clrTo'] !== undefined) writeCT_Color(s, value['clrTo'], ctx, 'clrTo');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ColorMapping`; the caller supplies its element local name. */
export function writeCT_ColorMapping(s: XmlSink, value: CT_ColorMapping, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['bg1'] !== undefined) s.attr(null, 'bg1', String(value['bg1']));
  if (value['tx1'] !== undefined) s.attr(null, 'tx1', String(value['tx1']));
  if (value['bg2'] !== undefined) s.attr(null, 'bg2', String(value['bg2']));
  if (value['tx2'] !== undefined) s.attr(null, 'tx2', String(value['tx2']));
  if (value['accent1'] !== undefined) s.attr(null, 'accent1', String(value['accent1']));
  if (value['accent2'] !== undefined) s.attr(null, 'accent2', String(value['accent2']));
  if (value['accent3'] !== undefined) s.attr(null, 'accent3', String(value['accent3']));
  if (value['accent4'] !== undefined) s.attr(null, 'accent4', String(value['accent4']));
  if (value['accent5'] !== undefined) s.attr(null, 'accent5', String(value['accent5']));
  if (value['accent6'] !== undefined) s.attr(null, 'accent6', String(value['accent6']));
  if (value['hlink'] !== undefined) s.attr(null, 'hlink', String(value['hlink']));
  if (value['folHlink'] !== undefined) s.attr(null, 'folHlink', String(value['folHlink']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ColorMappingOverride`; the caller supplies its element local name. */
export function writeCT_ColorMappingOverride(s: XmlSink, value: CT_ColorMappingOverride, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'masterClrMapping') writeCT_EmptyElement(s, value['content'].value, ctx, 'masterClrMapping');
    if (value['content'].kind === 'overrideClrMapping') writeCT_ColorMapping(s, value['content'].value, ctx, 'overrideClrMapping');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ColorMRU`; the caller supplies its element local name. */
export function writeCT_ColorMRU(s: XmlSink, value: CT_ColorMRU, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_colorChoice of value['colorChoice']) {
    if (v_colorChoice.kind === 'scrgbClr') writeCT_ScRgbColor(s, v_colorChoice.value, ctx, 'scrgbClr');
    if (v_colorChoice.kind === 'srgbClr') writeCT_SRgbColor(s, v_colorChoice.value, ctx, 'srgbClr');
    if (v_colorChoice.kind === 'hslClr') writeCT_HslColor(s, v_colorChoice.value, ctx, 'hslClr');
    if (v_colorChoice.kind === 'sysClr') writeCT_SystemColor(s, v_colorChoice.value, ctx, 'sysClr');
    if (v_colorChoice.kind === 'schemeClr') writeCT_SchemeColor(s, v_colorChoice.value, ctx, 'schemeClr');
    if (v_colorChoice.kind === 'prstClr') writeCT_PresetColor(s, v_colorChoice.value, ctx, 'prstClr');
    if (v_colorChoice.kind === '$raw') { s.raw(v_colorChoice.value); }
  }
  s.endElement();
}

/** Write a `CT_ColorReplaceEffect`; the caller supplies its element local name. */
export function writeCT_ColorReplaceEffect(s: XmlSink, value: CT_ColorReplaceEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ColorScheme`; the caller supplies its element local name. */
export function writeCT_ColorScheme(s: XmlSink, value: CT_ColorScheme, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['dk1'] !== undefined) writeCT_Color(s, value['dk1'], ctx, 'dk1');
  $q.flush(s, 0);
  if (value['lt1'] !== undefined) writeCT_Color(s, value['lt1'], ctx, 'lt1');
  $q.flush(s, 1);
  if (value['dk2'] !== undefined) writeCT_Color(s, value['dk2'], ctx, 'dk2');
  $q.flush(s, 2);
  if (value['lt2'] !== undefined) writeCT_Color(s, value['lt2'], ctx, 'lt2');
  $q.flush(s, 3);
  if (value['accent1'] !== undefined) writeCT_Color(s, value['accent1'], ctx, 'accent1');
  $q.flush(s, 4);
  if (value['accent2'] !== undefined) writeCT_Color(s, value['accent2'], ctx, 'accent2');
  $q.flush(s, 5);
  if (value['accent3'] !== undefined) writeCT_Color(s, value['accent3'], ctx, 'accent3');
  $q.flush(s, 6);
  if (value['accent4'] !== undefined) writeCT_Color(s, value['accent4'], ctx, 'accent4');
  $q.flush(s, 7);
  if (value['accent5'] !== undefined) writeCT_Color(s, value['accent5'], ctx, 'accent5');
  $q.flush(s, 8);
  if (value['accent6'] !== undefined) writeCT_Color(s, value['accent6'], ctx, 'accent6');
  $q.flush(s, 9);
  if (value['hlink'] !== undefined) writeCT_Color(s, value['hlink'], ctx, 'hlink');
  $q.flush(s, 10);
  if (value['folHlink'] !== undefined) writeCT_Color(s, value['folHlink'], ctx, 'folHlink');
  $q.flush(s, 11);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 12);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ColorSchemeAndMapping`; the caller supplies its element local name. */
export function writeCT_ColorSchemeAndMapping(s: XmlSink, value: CT_ColorSchemeAndMapping, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['clrScheme'] !== undefined) writeCT_ColorScheme(s, value['clrScheme'], ctx, 'clrScheme');
  $q.flush(s, 0);
  if (value['clrMap'] !== undefined) writeCT_ColorMapping(s, value['clrMap'], ctx, 'clrMap');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ColorSchemeList`; the caller supplies its element local name. */
export function writeCT_ColorSchemeList(s: XmlSink, value: CT_ColorSchemeList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['extraClrScheme'].length; idx++) {
    const v_extraClrScheme = value['extraClrScheme'][idx]!;
    writeCT_ColorSchemeAndMapping(s, v_extraClrScheme, ctx, 'extraClrScheme');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ComplementTransform`; the caller supplies its element local name. */
export function writeCT_ComplementTransform(s: XmlSink, value: CT_ComplementTransform, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Connection`; the caller supplies its element local name. */
export function writeCT_Connection(s: XmlSink, value: CT_Connection, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['id'] !== undefined) s.attr(null, 'id', String(value['id']));
  if (value['idx'] !== undefined) s.attr(null, 'idx', String(value['idx']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ConnectionSite`; the caller supplies its element local name. */
export function writeCT_ConnectionSite(s: XmlSink, value: CT_ConnectionSite, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['ang'] !== undefined) s.attr(null, 'ang', String(value['ang']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['pos'] !== undefined) writeCT_AdjPoint2D(s, value['pos'], ctx, 'pos');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ConnectionSiteList`; the caller supplies its element local name. */
export function writeCT_ConnectionSiteList(s: XmlSink, value: CT_ConnectionSiteList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['cxn'].length; idx++) {
    const v_cxn = value['cxn'][idx]!;
    writeCT_ConnectionSite(s, v_cxn, ctx, 'cxn');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ConnectorLocking`; the caller supplies its element local name. */
export function writeCT_ConnectorLocking(s: XmlSink, value: CT_ConnectorLocking, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['noGrp'] !== undefined) s.attr(null, 'noGrp', String(value['noGrp']));
  if (value['noSelect'] !== undefined) s.attr(null, 'noSelect', String(value['noSelect']));
  if (value['noRot'] !== undefined) s.attr(null, 'noRot', String(value['noRot']));
  if (value['noChangeAspect'] !== undefined) s.attr(null, 'noChangeAspect', String(value['noChangeAspect']));
  if (value['noMove'] !== undefined) s.attr(null, 'noMove', String(value['noMove']));
  if (value['noResize'] !== undefined) s.attr(null, 'noResize', String(value['noResize']));
  if (value['noEditPoints'] !== undefined) s.attr(null, 'noEditPoints', String(value['noEditPoints']));
  if (value['noAdjustHandles'] !== undefined) s.attr(null, 'noAdjustHandles', String(value['noAdjustHandles']));
  if (value['noChangeArrowheads'] !== undefined) s.attr(null, 'noChangeArrowheads', String(value['noChangeArrowheads']));
  if (value['noChangeShapeType'] !== undefined) s.attr(null, 'noChangeShapeType', String(value['noChangeShapeType']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ContentPartLocking`; the caller supplies its element local name. */
export function writeCT_ContentPartLocking(s: XmlSink, value: CT_ContentPartLocking, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['noGrp'] !== undefined) s.attr(null, 'noGrp', String(value['noGrp']));
  if (value['noSelect'] !== undefined) s.attr(null, 'noSelect', String(value['noSelect']));
  if (value['noRot'] !== undefined) s.attr(null, 'noRot', String(value['noRot']));
  if (value['noChangeAspect'] !== undefined) s.attr(null, 'noChangeAspect', String(value['noChangeAspect']));
  if (value['noMove'] !== undefined) s.attr(null, 'noMove', String(value['noMove']));
  if (value['noResize'] !== undefined) s.attr(null, 'noResize', String(value['noResize']));
  if (value['noEditPoints'] !== undefined) s.attr(null, 'noEditPoints', String(value['noEditPoints']));
  if (value['noAdjustHandles'] !== undefined) s.attr(null, 'noAdjustHandles', String(value['noAdjustHandles']));
  if (value['noChangeArrowheads'] !== undefined) s.attr(null, 'noChangeArrowheads', String(value['noChangeArrowheads']));
  if (value['noChangeShapeType'] !== undefined) s.attr(null, 'noChangeShapeType', String(value['noChangeShapeType']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_CustomColor`; the caller supplies its element local name. */
export function writeCT_CustomColor(s: XmlSink, value: CT_CustomColor, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_CustomColorList`; the caller supplies its element local name. */
export function writeCT_CustomColorList(s: XmlSink, value: CT_CustomColorList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['custClr'].length; idx++) {
    const v_custClr = value['custClr'][idx]!;
    writeCT_CustomColor(s, v_custClr, ctx, 'custClr');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_CustomGeometry2D`; the caller supplies its element local name. */
export function writeCT_CustomGeometry2D(s: XmlSink, value: CT_CustomGeometry2D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['avLst'] !== undefined) writeCT_GeomGuideList(s, value['avLst'], ctx, 'avLst');
  $q.flush(s, 0);
  if (value['gdLst'] !== undefined) writeCT_GeomGuideList(s, value['gdLst'], ctx, 'gdLst');
  $q.flush(s, 1);
  if (value['ahLst'] !== undefined) writeCT_AdjustHandleList(s, value['ahLst'], ctx, 'ahLst');
  $q.flush(s, 2);
  if (value['cxnLst'] !== undefined) writeCT_ConnectionSiteList(s, value['cxnLst'], ctx, 'cxnLst');
  $q.flush(s, 3);
  if (value['rect'] !== undefined) writeCT_GeomRect(s, value['rect'], ctx, 'rect');
  $q.flush(s, 4);
  if (value['pathLst'] !== undefined) writeCT_Path2DList(s, value['pathLst'], ctx, 'pathLst');
  $q.flush(s, 5);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_DashStop`; the caller supplies its element local name. */
export function writeCT_DashStop(s: XmlSink, value: CT_DashStop, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['d'] !== undefined) s.attr(null, 'd', String(value['d']));
  if (value['sp'] !== undefined) s.attr(null, 'sp', String(value['sp']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_DashStopList`; the caller supplies its element local name. */
export function writeCT_DashStopList(s: XmlSink, value: CT_DashStopList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['ds'].length; idx++) {
    const v_ds = value['ds'][idx]!;
    writeCT_DashStop(s, v_ds, ctx, 'ds');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_DefaultShapeDefinition`; the caller supplies its element local name. */
export function writeCT_DefaultShapeDefinition(s: XmlSink, value: CT_DefaultShapeDefinition, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['spPr'] !== undefined) writeCT_ShapeProperties(s, value['spPr'], ctx, 'spPr');
  $q.flush(s, 0);
  if (value['bodyPr'] !== undefined) writeCT_TextBodyProperties(s, value['bodyPr'], ctx, 'bodyPr');
  $q.flush(s, 1);
  if (value['lstStyle'] !== undefined) writeCT_TextListStyle(s, value['lstStyle'], ctx, 'lstStyle');
  $q.flush(s, 2);
  if (value['style'] !== undefined) writeCT_ShapeStyle(s, value['style'], ctx, 'style');
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_DuotoneEffect`; the caller supplies its element local name. */
export function writeCT_DuotoneEffect(s: XmlSink, value: CT_DuotoneEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_colorChoice of value['colorChoice']) {
    if (v_colorChoice.kind === 'scrgbClr') writeCT_ScRgbColor(s, v_colorChoice.value, ctx, 'scrgbClr');
    if (v_colorChoice.kind === 'srgbClr') writeCT_SRgbColor(s, v_colorChoice.value, ctx, 'srgbClr');
    if (v_colorChoice.kind === 'hslClr') writeCT_HslColor(s, v_colorChoice.value, ctx, 'hslClr');
    if (v_colorChoice.kind === 'sysClr') writeCT_SystemColor(s, v_colorChoice.value, ctx, 'sysClr');
    if (v_colorChoice.kind === 'schemeClr') writeCT_SchemeColor(s, v_colorChoice.value, ctx, 'schemeClr');
    if (v_colorChoice.kind === 'prstClr') writeCT_PresetColor(s, v_colorChoice.value, ctx, 'prstClr');
    if (v_colorChoice.kind === '$raw') { s.raw(v_colorChoice.value); }
  }
  s.endElement();
}

/** Write a `CT_EffectContainer`; the caller supplies its element local name. */
export function writeCT_EffectContainer(s: XmlSink, value: CT_EffectContainer, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_effect of value['effect']) {
    if (v_effect.kind === 'cont') writeCT_EffectContainer(s, v_effect.value, ctx, 'cont');
    if (v_effect.kind === 'effect') writeCT_EffectReference(s, v_effect.value, ctx, 'effect');
    if (v_effect.kind === 'alphaBiLevel') writeCT_AlphaBiLevelEffect(s, v_effect.value, ctx, 'alphaBiLevel');
    if (v_effect.kind === 'alphaCeiling') writeCT_AlphaCeilingEffect(s, v_effect.value, ctx, 'alphaCeiling');
    if (v_effect.kind === 'alphaFloor') writeCT_AlphaFloorEffect(s, v_effect.value, ctx, 'alphaFloor');
    if (v_effect.kind === 'alphaInv') writeCT_AlphaInverseEffect(s, v_effect.value, ctx, 'alphaInv');
    if (v_effect.kind === 'alphaMod') writeCT_AlphaModulateEffect(s, v_effect.value, ctx, 'alphaMod');
    if (v_effect.kind === 'alphaModFix') writeCT_AlphaModulateFixedEffect(s, v_effect.value, ctx, 'alphaModFix');
    if (v_effect.kind === 'alphaOutset') writeCT_AlphaOutsetEffect(s, v_effect.value, ctx, 'alphaOutset');
    if (v_effect.kind === 'alphaRepl') writeCT_AlphaReplaceEffect(s, v_effect.value, ctx, 'alphaRepl');
    if (v_effect.kind === 'biLevel') writeCT_BiLevelEffect(s, v_effect.value, ctx, 'biLevel');
    if (v_effect.kind === 'blend') writeCT_BlendEffect(s, v_effect.value, ctx, 'blend');
    if (v_effect.kind === 'blur') writeCT_BlurEffect(s, v_effect.value, ctx, 'blur');
    if (v_effect.kind === 'clrChange') writeCT_ColorChangeEffect(s, v_effect.value, ctx, 'clrChange');
    if (v_effect.kind === 'clrRepl') writeCT_ColorReplaceEffect(s, v_effect.value, ctx, 'clrRepl');
    if (v_effect.kind === 'duotone') writeCT_DuotoneEffect(s, v_effect.value, ctx, 'duotone');
    if (v_effect.kind === 'fill') writeCT_FillEffect(s, v_effect.value, ctx, 'fill');
    if (v_effect.kind === 'fillOverlay') writeCT_FillOverlayEffect(s, v_effect.value, ctx, 'fillOverlay');
    if (v_effect.kind === 'glow') writeCT_GlowEffect(s, v_effect.value, ctx, 'glow');
    if (v_effect.kind === 'grayscl') writeCT_GrayscaleEffect(s, v_effect.value, ctx, 'grayscl');
    if (v_effect.kind === 'hsl') writeCT_HSLEffect(s, v_effect.value, ctx, 'hsl');
    if (v_effect.kind === 'innerShdw') writeCT_InnerShadowEffect(s, v_effect.value, ctx, 'innerShdw');
    if (v_effect.kind === 'lum') writeCT_LuminanceEffect(s, v_effect.value, ctx, 'lum');
    if (v_effect.kind === 'outerShdw') writeCT_OuterShadowEffect(s, v_effect.value, ctx, 'outerShdw');
    if (v_effect.kind === 'prstShdw') writeCT_PresetShadowEffect(s, v_effect.value, ctx, 'prstShdw');
    if (v_effect.kind === 'reflection') writeCT_ReflectionEffect(s, v_effect.value, ctx, 'reflection');
    if (v_effect.kind === 'relOff') writeCT_RelativeOffsetEffect(s, v_effect.value, ctx, 'relOff');
    if (v_effect.kind === 'softEdge') writeCT_SoftEdgesEffect(s, v_effect.value, ctx, 'softEdge');
    if (v_effect.kind === 'tint') writeCT_TintEffect(s, v_effect.value, ctx, 'tint');
    if (v_effect.kind === 'xfrm') writeCT_TransformEffect(s, v_effect.value, ctx, 'xfrm');
    if (v_effect.kind === '$raw') { s.raw(v_effect.value); }
  }
  s.endElement();
}

/** Write a `CT_EffectList`; the caller supplies its element local name. */
export function writeCT_EffectList(s: XmlSink, value: CT_EffectList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['blur'] !== undefined) writeCT_BlurEffect(s, value['blur'], ctx, 'blur');
  $q.flush(s, 0);
  if (value['fillOverlay'] !== undefined) writeCT_FillOverlayEffect(s, value['fillOverlay'], ctx, 'fillOverlay');
  $q.flush(s, 1);
  if (value['glow'] !== undefined) writeCT_GlowEffect(s, value['glow'], ctx, 'glow');
  $q.flush(s, 2);
  if (value['innerShdw'] !== undefined) writeCT_InnerShadowEffect(s, value['innerShdw'], ctx, 'innerShdw');
  $q.flush(s, 3);
  if (value['outerShdw'] !== undefined) writeCT_OuterShadowEffect(s, value['outerShdw'], ctx, 'outerShdw');
  $q.flush(s, 4);
  if (value['prstShdw'] !== undefined) writeCT_PresetShadowEffect(s, value['prstShdw'], ctx, 'prstShdw');
  $q.flush(s, 5);
  if (value['reflection'] !== undefined) writeCT_ReflectionEffect(s, value['reflection'], ctx, 'reflection');
  $q.flush(s, 6);
  if (value['softEdge'] !== undefined) writeCT_SoftEdgesEffect(s, value['softEdge'], ctx, 'softEdge');
  $q.flush(s, 7);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_EffectProperties`; the caller supplies its element local name. */
export function writeCT_EffectProperties(s: XmlSink, value: CT_EffectProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['effectProperties'] !== undefined) {
    if (value['effectProperties'].kind === 'effectLst') writeCT_EffectList(s, value['effectProperties'].value, ctx, 'effectLst');
    if (value['effectProperties'].kind === 'effectDag') writeCT_EffectContainer(s, value['effectProperties'].value, ctx, 'effectDag');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_EffectReference`; the caller supplies its element local name. */
export function writeCT_EffectReference(s: XmlSink, value: CT_EffectReference, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['ref'] !== undefined) s.attr(null, 'ref', String(value['ref']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_EffectStyleItem`; the caller supplies its element local name. */
export function writeCT_EffectStyleItem(s: XmlSink, value: CT_EffectStyleItem, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['effectProperties'] !== undefined) {
    if (value['effectProperties'].kind === 'effectLst') writeCT_EffectList(s, value['effectProperties'].value, ctx, 'effectLst');
    if (value['effectProperties'].kind === 'effectDag') writeCT_EffectContainer(s, value['effectProperties'].value, ctx, 'effectDag');
  }
  $q.flush(s, 0);
  if (value['scene3d'] !== undefined) writeCT_Scene3D(s, value['scene3d'], ctx, 'scene3d');
  $q.flush(s, 1);
  if (value['sp3d'] !== undefined) writeCT_Shape3D(s, value['sp3d'], ctx, 'sp3d');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_EffectStyleList`; the caller supplies its element local name. */
export function writeCT_EffectStyleList(s: XmlSink, value: CT_EffectStyleList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['effectStyle'].length; idx++) {
    const v_effectStyle = value['effectStyle'][idx]!;
    writeCT_EffectStyleItem(s, v_effectStyle, ctx, 'effectStyle');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_EmbeddedWAVAudioFile`; the caller supplies its element local name. */
export function writeCT_EmbeddedWAVAudioFile(s: XmlSink, value: CT_EmbeddedWAVAudioFile, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['embed'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'embed', String(value['embed']));
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_EmptyElement`; the caller supplies its element local name. */
export function writeCT_EmptyElement(s: XmlSink, value: CT_EmptyElement, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_FillEffect`; the caller supplies its element local name. */
export function writeCT_FillEffect(s: XmlSink, value: CT_FillEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['fillProperties'] !== undefined) {
    if (value['fillProperties'].kind === 'noFill') writeCT_NoFillProperties(s, value['fillProperties'].value, ctx, 'noFill');
    if (value['fillProperties'].kind === 'solidFill') writeCT_SolidColorFillProperties(s, value['fillProperties'].value, ctx, 'solidFill');
    if (value['fillProperties'].kind === 'gradFill') writeCT_GradientFillProperties(s, value['fillProperties'].value, ctx, 'gradFill');
    if (value['fillProperties'].kind === 'blipFill') writeCT_BlipFillProperties(s, value['fillProperties'].value, ctx, 'blipFill');
    if (value['fillProperties'].kind === 'pattFill') writeCT_PatternFillProperties(s, value['fillProperties'].value, ctx, 'pattFill');
    if (value['fillProperties'].kind === 'grpFill') writeCT_GroupFillProperties(s, value['fillProperties'].value, ctx, 'grpFill');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_FillOverlayEffect`; the caller supplies its element local name. */
export function writeCT_FillOverlayEffect(s: XmlSink, value: CT_FillOverlayEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['blend'] !== undefined) s.attr(null, 'blend', String(value['blend']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['fillProperties'] !== undefined) {
    if (value['fillProperties'].kind === 'noFill') writeCT_NoFillProperties(s, value['fillProperties'].value, ctx, 'noFill');
    if (value['fillProperties'].kind === 'solidFill') writeCT_SolidColorFillProperties(s, value['fillProperties'].value, ctx, 'solidFill');
    if (value['fillProperties'].kind === 'gradFill') writeCT_GradientFillProperties(s, value['fillProperties'].value, ctx, 'gradFill');
    if (value['fillProperties'].kind === 'blipFill') writeCT_BlipFillProperties(s, value['fillProperties'].value, ctx, 'blipFill');
    if (value['fillProperties'].kind === 'pattFill') writeCT_PatternFillProperties(s, value['fillProperties'].value, ctx, 'pattFill');
    if (value['fillProperties'].kind === 'grpFill') writeCT_GroupFillProperties(s, value['fillProperties'].value, ctx, 'grpFill');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_FillProperties`; the caller supplies its element local name. */
export function writeCT_FillProperties(s: XmlSink, value: CT_FillProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['fillProperties'] !== undefined) {
    if (value['fillProperties'].kind === 'noFill') writeCT_NoFillProperties(s, value['fillProperties'].value, ctx, 'noFill');
    if (value['fillProperties'].kind === 'solidFill') writeCT_SolidColorFillProperties(s, value['fillProperties'].value, ctx, 'solidFill');
    if (value['fillProperties'].kind === 'gradFill') writeCT_GradientFillProperties(s, value['fillProperties'].value, ctx, 'gradFill');
    if (value['fillProperties'].kind === 'blipFill') writeCT_BlipFillProperties(s, value['fillProperties'].value, ctx, 'blipFill');
    if (value['fillProperties'].kind === 'pattFill') writeCT_PatternFillProperties(s, value['fillProperties'].value, ctx, 'pattFill');
    if (value['fillProperties'].kind === 'grpFill') writeCT_GroupFillProperties(s, value['fillProperties'].value, ctx, 'grpFill');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_FillStyleList`; the caller supplies its element local name. */
export function writeCT_FillStyleList(s: XmlSink, value: CT_FillStyleList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_fillProperties of value['fillProperties']) {
    if (v_fillProperties.kind === 'noFill') writeCT_NoFillProperties(s, v_fillProperties.value, ctx, 'noFill');
    if (v_fillProperties.kind === 'solidFill') writeCT_SolidColorFillProperties(s, v_fillProperties.value, ctx, 'solidFill');
    if (v_fillProperties.kind === 'gradFill') writeCT_GradientFillProperties(s, v_fillProperties.value, ctx, 'gradFill');
    if (v_fillProperties.kind === 'blipFill') writeCT_BlipFillProperties(s, v_fillProperties.value, ctx, 'blipFill');
    if (v_fillProperties.kind === 'pattFill') writeCT_PatternFillProperties(s, v_fillProperties.value, ctx, 'pattFill');
    if (v_fillProperties.kind === 'grpFill') writeCT_GroupFillProperties(s, v_fillProperties.value, ctx, 'grpFill');
    if (v_fillProperties.kind === '$raw') { s.raw(v_fillProperties.value); }
  }
  s.endElement();
}

/** Write a `CT_FixedPercentage`; the caller supplies its element local name. */
export function writeCT_FixedPercentage(s: XmlSink, value: CT_FixedPercentage, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_FlatText`; the caller supplies its element local name. */
export function writeCT_FlatText(s: XmlSink, value: CT_FlatText, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['z'] !== undefined) s.attr(null, 'z', String(value['z']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_FontCollection`; the caller supplies its element local name. */
export function writeCT_FontCollection(s: XmlSink, value: CT_FontCollection, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['latin'] !== undefined) writeCT_TextFont(s, value['latin'], ctx, 'latin');
  $q.flush(s, 0);
  if (value['ea'] !== undefined) writeCT_TextFont(s, value['ea'], ctx, 'ea');
  $q.flush(s, 1);
  if (value['cs'] !== undefined) writeCT_TextFont(s, value['cs'], ctx, 'cs');
  $q.flush(s, 2);
  for (let idx = 0; idx < value['font'].length; idx++) {
    const v_font = value['font'][idx]!;
    writeCT_SupplementalFont(s, v_font, ctx, 'font');
    $q.flush(s, 3, idx);
  }
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_FontReference`; the caller supplies its element local name. */
export function writeCT_FontReference(s: XmlSink, value: CT_FontReference, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['idx'] !== undefined) s.attr(null, 'idx', String(value['idx']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_FontScheme`; the caller supplies its element local name. */
export function writeCT_FontScheme(s: XmlSink, value: CT_FontScheme, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['majorFont'] !== undefined) writeCT_FontCollection(s, value['majorFont'], ctx, 'majorFont');
  $q.flush(s, 0);
  if (value['minorFont'] !== undefined) writeCT_FontCollection(s, value['minorFont'], ctx, 'minorFont');
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GammaTransform`; the caller supplies its element local name. */
export function writeCT_GammaTransform(s: XmlSink, value: CT_GammaTransform, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_GeomGuide`; the caller supplies its element local name. */
export function writeCT_GeomGuide(s: XmlSink, value: CT_GeomGuide, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  if (value['fmla'] !== undefined) s.attr(null, 'fmla', String(value['fmla']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_GeomGuideList`; the caller supplies its element local name. */
export function writeCT_GeomGuideList(s: XmlSink, value: CT_GeomGuideList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['gd'].length; idx++) {
    const v_gd = value['gd'][idx]!;
    writeCT_GeomGuide(s, v_gd, ctx, 'gd');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GeomRect`; the caller supplies its element local name. */
export function writeCT_GeomRect(s: XmlSink, value: CT_GeomRect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['l'] !== undefined) s.attr(null, 'l', String(value['l']));
  if (value['t'] !== undefined) s.attr(null, 't', String(value['t']));
  if (value['r'] !== undefined) s.attr(null, 'r', String(value['r']));
  if (value['b'] !== undefined) s.attr(null, 'b', String(value['b']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_GlowEffect`; the caller supplies its element local name. */
export function writeCT_GlowEffect(s: XmlSink, value: CT_GlowEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['rad'] !== undefined) s.attr(null, 'rad', String(value['rad']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GradientFillProperties`; the caller supplies its element local name. */
export function writeCT_GradientFillProperties(s: XmlSink, value: CT_GradientFillProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['flip'] !== undefined) s.attr(null, 'flip', String(value['flip']));
  if (value['rotWithShape'] !== undefined) s.attr(null, 'rotWithShape', String(value['rotWithShape']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['gsLst'] !== undefined) writeCT_GradientStopList(s, value['gsLst'], ctx, 'gsLst');
  $q.flush(s, 0);
  if (value['shadeProperties'] !== undefined) {
    if (value['shadeProperties'].kind === 'lin') writeCT_LinearShadeProperties(s, value['shadeProperties'].value, ctx, 'lin');
    if (value['shadeProperties'].kind === 'path') writeCT_PathShadeProperties(s, value['shadeProperties'].value, ctx, 'path');
  }
  $q.flush(s, 1);
  if (value['tileRect'] !== undefined) writeCT_RelativeRect(s, value['tileRect'], ctx, 'tileRect');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GradientStop`; the caller supplies its element local name. */
export function writeCT_GradientStop(s: XmlSink, value: CT_GradientStop, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['pos'] !== undefined) s.attr(null, 'pos', String(value['pos']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GradientStopList`; the caller supplies its element local name. */
export function writeCT_GradientStopList(s: XmlSink, value: CT_GradientStopList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['gs'].length; idx++) {
    const v_gs = value['gs'][idx]!;
    writeCT_GradientStop(s, v_gs, ctx, 'gs');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GraphicalObject`; the caller supplies its element local name. */
export function writeCT_GraphicalObject(s: XmlSink, value: CT_GraphicalObject, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['graphicData'] !== undefined) writeCT_GraphicalObjectData(s, value['graphicData'], ctx, 'graphicData');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GraphicalObjectData`; the caller supplies its element local name. */
export function writeCT_GraphicalObjectData(s: XmlSink, value: CT_GraphicalObjectData, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['uri'] !== undefined) s.attr(null, 'uri', String(value['uri']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (const raw of value['any']) {
    s.raw(raw);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GraphicalObjectFrameLocking`; the caller supplies its element local name. */
export function writeCT_GraphicalObjectFrameLocking(s: XmlSink, value: CT_GraphicalObjectFrameLocking, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['noGrp'] !== undefined) s.attr(null, 'noGrp', String(value['noGrp']));
  if (value['noDrilldown'] !== undefined) s.attr(null, 'noDrilldown', String(value['noDrilldown']));
  if (value['noSelect'] !== undefined) s.attr(null, 'noSelect', String(value['noSelect']));
  if (value['noChangeAspect'] !== undefined) s.attr(null, 'noChangeAspect', String(value['noChangeAspect']));
  if (value['noMove'] !== undefined) s.attr(null, 'noMove', String(value['noMove']));
  if (value['noResize'] !== undefined) s.attr(null, 'noResize', String(value['noResize']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GrayscaleEffect`; the caller supplies its element local name. */
export function writeCT_GrayscaleEffect(s: XmlSink, value: CT_GrayscaleEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_GrayscaleTransform`; the caller supplies its element local name. */
export function writeCT_GrayscaleTransform(s: XmlSink, value: CT_GrayscaleTransform, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_GroupFillProperties`; the caller supplies its element local name. */
export function writeCT_GroupFillProperties(s: XmlSink, value: CT_GroupFillProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_GroupLocking`; the caller supplies its element local name. */
export function writeCT_GroupLocking(s: XmlSink, value: CT_GroupLocking, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['noGrp'] !== undefined) s.attr(null, 'noGrp', String(value['noGrp']));
  if (value['noUngrp'] !== undefined) s.attr(null, 'noUngrp', String(value['noUngrp']));
  if (value['noSelect'] !== undefined) s.attr(null, 'noSelect', String(value['noSelect']));
  if (value['noRot'] !== undefined) s.attr(null, 'noRot', String(value['noRot']));
  if (value['noChangeAspect'] !== undefined) s.attr(null, 'noChangeAspect', String(value['noChangeAspect']));
  if (value['noMove'] !== undefined) s.attr(null, 'noMove', String(value['noMove']));
  if (value['noResize'] !== undefined) s.attr(null, 'noResize', String(value['noResize']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GroupShapeProperties`; the caller supplies its element local name. */
export function writeCT_GroupShapeProperties(s: XmlSink, value: CT_GroupShapeProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['bwMode'] !== undefined) s.attr(null, 'bwMode', String(value['bwMode']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['xfrm'] !== undefined) writeCT_GroupTransform2D(s, value['xfrm'], ctx, 'xfrm');
  $q.flush(s, 0);
  if (value['fillProperties'] !== undefined) {
    if (value['fillProperties'].kind === 'noFill') writeCT_NoFillProperties(s, value['fillProperties'].value, ctx, 'noFill');
    if (value['fillProperties'].kind === 'solidFill') writeCT_SolidColorFillProperties(s, value['fillProperties'].value, ctx, 'solidFill');
    if (value['fillProperties'].kind === 'gradFill') writeCT_GradientFillProperties(s, value['fillProperties'].value, ctx, 'gradFill');
    if (value['fillProperties'].kind === 'blipFill') writeCT_BlipFillProperties(s, value['fillProperties'].value, ctx, 'blipFill');
    if (value['fillProperties'].kind === 'pattFill') writeCT_PatternFillProperties(s, value['fillProperties'].value, ctx, 'pattFill');
    if (value['fillProperties'].kind === 'grpFill') writeCT_GroupFillProperties(s, value['fillProperties'].value, ctx, 'grpFill');
  }
  $q.flush(s, 1);
  if (value['effectProperties'] !== undefined) {
    if (value['effectProperties'].kind === 'effectLst') writeCT_EffectList(s, value['effectProperties'].value, ctx, 'effectLst');
    if (value['effectProperties'].kind === 'effectDag') writeCT_EffectContainer(s, value['effectProperties'].value, ctx, 'effectDag');
  }
  $q.flush(s, 2);
  if (value['scene3d'] !== undefined) writeCT_Scene3D(s, value['scene3d'], ctx, 'scene3d');
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GroupTransform2D`; the caller supplies its element local name. */
export function writeCT_GroupTransform2D(s: XmlSink, value: CT_GroupTransform2D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['rot'] !== undefined) s.attr(null, 'rot', String(value['rot']));
  if (value['flipH'] !== undefined) s.attr(null, 'flipH', String(value['flipH']));
  if (value['flipV'] !== undefined) s.attr(null, 'flipV', String(value['flipV']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['off'] !== undefined) writeCT_Point2D(s, value['off'], ctx, 'off');
  $q.flush(s, 0);
  if (value['ext'] !== undefined) writeCT_PositiveSize2D(s, value['ext'], ctx, 'ext');
  $q.flush(s, 1);
  if (value['chOff'] !== undefined) writeCT_Point2D(s, value['chOff'], ctx, 'chOff');
  $q.flush(s, 2);
  if (value['chExt'] !== undefined) writeCT_PositiveSize2D(s, value['chExt'], ctx, 'chExt');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GvmlConnector`; the caller supplies its element local name. */
export function writeCT_GvmlConnector(s: XmlSink, value: CT_GvmlConnector, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['nvCxnSpPr'] !== undefined) writeCT_GvmlConnectorNonVisual(s, value['nvCxnSpPr'], ctx, 'nvCxnSpPr');
  $q.flush(s, 0);
  if (value['spPr'] !== undefined) writeCT_ShapeProperties(s, value['spPr'], ctx, 'spPr');
  $q.flush(s, 1);
  if (value['style'] !== undefined) writeCT_ShapeStyle(s, value['style'], ctx, 'style');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GvmlConnectorNonVisual`; the caller supplies its element local name. */
export function writeCT_GvmlConnectorNonVisual(s: XmlSink, value: CT_GvmlConnectorNonVisual, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cNvPr'] !== undefined) writeCT_NonVisualDrawingProps(s, value['cNvPr'], ctx, 'cNvPr');
  $q.flush(s, 0);
  if (value['cNvCxnSpPr'] !== undefined) writeCT_NonVisualConnectorProperties(s, value['cNvCxnSpPr'], ctx, 'cNvCxnSpPr');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GvmlGraphicalObjectFrame`; the caller supplies its element local name. */
export function writeCT_GvmlGraphicalObjectFrame(s: XmlSink, value: CT_GvmlGraphicalObjectFrame, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['nvGraphicFramePr'] !== undefined) writeCT_GvmlGraphicFrameNonVisual(s, value['nvGraphicFramePr'], ctx, 'nvGraphicFramePr');
  $q.flush(s, 0);
  if (value['graphic'] !== undefined) writeCT_GraphicalObject(s, value['graphic'], ctx, 'graphic');
  $q.flush(s, 1);
  if (value['xfrm'] !== undefined) writeCT_Transform2D(s, value['xfrm'], ctx, 'xfrm');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GvmlGraphicFrameNonVisual`; the caller supplies its element local name. */
export function writeCT_GvmlGraphicFrameNonVisual(s: XmlSink, value: CT_GvmlGraphicFrameNonVisual, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cNvPr'] !== undefined) writeCT_NonVisualDrawingProps(s, value['cNvPr'], ctx, 'cNvPr');
  $q.flush(s, 0);
  if (value['cNvGraphicFramePr'] !== undefined) writeCT_NonVisualGraphicFrameProperties(s, value['cNvGraphicFramePr'], ctx, 'cNvGraphicFramePr');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GvmlGroupShape`; the caller supplies its element local name. */
export function writeCT_GvmlGroupShape(s: XmlSink, value: CT_GvmlGroupShape, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  if (value['nvGrpSpPr'] !== undefined) writeCT_GvmlGroupShapeNonVisual(s, value['nvGrpSpPr'], ctx, 'nvGrpSpPr');
  if (value['grpSpPr'] !== undefined) writeCT_GroupShapeProperties(s, value['grpSpPr'], ctx, 'grpSpPr');
  for (const v_content of value['content']) {
    if (v_content.kind === 'txSp') writeCT_GvmlTextShape(s, v_content.value, ctx, 'txSp');
    if (v_content.kind === 'sp') writeCT_GvmlShape(s, v_content.value, ctx, 'sp');
    if (v_content.kind === 'cxnSp') writeCT_GvmlConnector(s, v_content.value, ctx, 'cxnSp');
    if (v_content.kind === 'pic') writeCT_GvmlPicture(s, v_content.value, ctx, 'pic');
    if (v_content.kind === 'graphicFrame') writeCT_GvmlGraphicalObjectFrame(s, v_content.value, ctx, 'graphicFrame');
    if (v_content.kind === 'grpSp') writeCT_GvmlGroupShape(s, v_content.value, ctx, 'grpSp');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  s.endElement();
}

/** Write a `CT_GvmlGroupShapeNonVisual`; the caller supplies its element local name. */
export function writeCT_GvmlGroupShapeNonVisual(s: XmlSink, value: CT_GvmlGroupShapeNonVisual, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cNvPr'] !== undefined) writeCT_NonVisualDrawingProps(s, value['cNvPr'], ctx, 'cNvPr');
  $q.flush(s, 0);
  if (value['cNvGrpSpPr'] !== undefined) writeCT_NonVisualGroupDrawingShapeProps(s, value['cNvGrpSpPr'], ctx, 'cNvGrpSpPr');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GvmlPicture`; the caller supplies its element local name. */
export function writeCT_GvmlPicture(s: XmlSink, value: CT_GvmlPicture, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['nvPicPr'] !== undefined) writeCT_GvmlPictureNonVisual(s, value['nvPicPr'], ctx, 'nvPicPr');
  $q.flush(s, 0);
  if (value['blipFill'] !== undefined) writeCT_BlipFillProperties(s, value['blipFill'], ctx, 'blipFill');
  $q.flush(s, 1);
  if (value['spPr'] !== undefined) writeCT_ShapeProperties(s, value['spPr'], ctx, 'spPr');
  $q.flush(s, 2);
  if (value['style'] !== undefined) writeCT_ShapeStyle(s, value['style'], ctx, 'style');
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GvmlPictureNonVisual`; the caller supplies its element local name. */
export function writeCT_GvmlPictureNonVisual(s: XmlSink, value: CT_GvmlPictureNonVisual, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cNvPr'] !== undefined) writeCT_NonVisualDrawingProps(s, value['cNvPr'], ctx, 'cNvPr');
  $q.flush(s, 0);
  if (value['cNvPicPr'] !== undefined) writeCT_NonVisualPictureProperties(s, value['cNvPicPr'], ctx, 'cNvPicPr');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GvmlShape`; the caller supplies its element local name. */
export function writeCT_GvmlShape(s: XmlSink, value: CT_GvmlShape, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['nvSpPr'] !== undefined) writeCT_GvmlShapeNonVisual(s, value['nvSpPr'], ctx, 'nvSpPr');
  $q.flush(s, 0);
  if (value['spPr'] !== undefined) writeCT_ShapeProperties(s, value['spPr'], ctx, 'spPr');
  $q.flush(s, 1);
  if (value['txSp'] !== undefined) writeCT_GvmlTextShape(s, value['txSp'], ctx, 'txSp');
  $q.flush(s, 2);
  if (value['style'] !== undefined) writeCT_ShapeStyle(s, value['style'], ctx, 'style');
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GvmlShapeNonVisual`; the caller supplies its element local name. */
export function writeCT_GvmlShapeNonVisual(s: XmlSink, value: CT_GvmlShapeNonVisual, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cNvPr'] !== undefined) writeCT_NonVisualDrawingProps(s, value['cNvPr'], ctx, 'cNvPr');
  $q.flush(s, 0);
  if (value['cNvSpPr'] !== undefined) writeCT_NonVisualDrawingShapeProps(s, value['cNvSpPr'], ctx, 'cNvSpPr');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GvmlTextShape`; the caller supplies its element local name. */
export function writeCT_GvmlTextShape(s: XmlSink, value: CT_GvmlTextShape, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['txBody'] !== undefined) writeCT_TextBody(s, value['txBody'], ctx, 'txBody');
  $q.flush(s, 0);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'useSpRect') writeCT_GvmlUseShapeRectangle(s, value['content'].value, ctx, 'useSpRect');
    if (value['content'].kind === 'xfrm') writeCT_Transform2D(s, value['content'].value, ctx, 'xfrm');
  }
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_GvmlUseShapeRectangle`; the caller supplies its element local name. */
export function writeCT_GvmlUseShapeRectangle(s: XmlSink, value: CT_GvmlUseShapeRectangle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Headers`; the caller supplies its element local name. */
export function writeCT_Headers(s: XmlSink, value: CT_Headers, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['header'].length; idx++) {
    const v_header = value['header'][idx];
    if (v_header !== undefined) s.text(String(v_header));
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_HslColor`; the caller supplies its element local name. */
export function writeCT_HslColor(s: XmlSink, value: CT_HslColor, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['hue'] !== undefined) s.attr(null, 'hue', String(value['hue']));
  if (value['sat'] !== undefined) s.attr(null, 'sat', String(value['sat']));
  if (value['lum'] !== undefined) s.attr(null, 'lum', String(value['lum']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_colorTransform of value['colorTransform']) {
    if (v_colorTransform.kind === 'tint') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'tint');
    if (v_colorTransform.kind === 'shade') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'shade');
    if (v_colorTransform.kind === 'comp') writeCT_ComplementTransform(s, v_colorTransform.value, ctx, 'comp');
    if (v_colorTransform.kind === 'inv') writeCT_InverseTransform(s, v_colorTransform.value, ctx, 'inv');
    if (v_colorTransform.kind === 'gray') writeCT_GrayscaleTransform(s, v_colorTransform.value, ctx, 'gray');
    if (v_colorTransform.kind === 'alpha') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'alpha');
    if (v_colorTransform.kind === 'alphaOff') writeCT_FixedPercentage(s, v_colorTransform.value, ctx, 'alphaOff');
    if (v_colorTransform.kind === 'alphaMod') writeCT_PositivePercentage(s, v_colorTransform.value, ctx, 'alphaMod');
    if (v_colorTransform.kind === 'hue') writeCT_PositiveFixedAngle(s, v_colorTransform.value, ctx, 'hue');
    if (v_colorTransform.kind === 'hueOff') writeCT_Angle(s, v_colorTransform.value, ctx, 'hueOff');
    if (v_colorTransform.kind === 'hueMod') writeCT_PositivePercentage(s, v_colorTransform.value, ctx, 'hueMod');
    if (v_colorTransform.kind === 'sat') writeCT_Percentage(s, v_colorTransform.value, ctx, 'sat');
    if (v_colorTransform.kind === 'satOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'satOff');
    if (v_colorTransform.kind === 'satMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'satMod');
    if (v_colorTransform.kind === 'lum') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lum');
    if (v_colorTransform.kind === 'lumOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lumOff');
    if (v_colorTransform.kind === 'lumMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lumMod');
    if (v_colorTransform.kind === 'red') writeCT_Percentage(s, v_colorTransform.value, ctx, 'red');
    if (v_colorTransform.kind === 'redOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'redOff');
    if (v_colorTransform.kind === 'redMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'redMod');
    if (v_colorTransform.kind === 'green') writeCT_Percentage(s, v_colorTransform.value, ctx, 'green');
    if (v_colorTransform.kind === 'greenOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'greenOff');
    if (v_colorTransform.kind === 'greenMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'greenMod');
    if (v_colorTransform.kind === 'blue') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blue');
    if (v_colorTransform.kind === 'blueOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blueOff');
    if (v_colorTransform.kind === 'blueMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blueMod');
    if (v_colorTransform.kind === 'gamma') writeCT_GammaTransform(s, v_colorTransform.value, ctx, 'gamma');
    if (v_colorTransform.kind === 'invGamma') writeCT_InverseGammaTransform(s, v_colorTransform.value, ctx, 'invGamma');
    if (v_colorTransform.kind === '$raw') { s.raw(v_colorTransform.value); }
  }
  s.endElement();
}

/** Write a `CT_HSLEffect`; the caller supplies its element local name. */
export function writeCT_HSLEffect(s: XmlSink, value: CT_HSLEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['hue'] !== undefined) s.attr(null, 'hue', String(value['hue']));
  if (value['sat'] !== undefined) s.attr(null, 'sat', String(value['sat']));
  if (value['lum'] !== undefined) s.attr(null, 'lum', String(value['lum']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Hyperlink`; the caller supplies its element local name. */
export function writeCT_Hyperlink(s: XmlSink, value: CT_Hyperlink, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['id'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'id', String(value['id']));
  if (value['invalidUrl'] !== undefined) s.attr(null, 'invalidUrl', String(value['invalidUrl']));
  if (value['action'] !== undefined) s.attr(null, 'action', String(value['action']));
  if (value['tgtFrame'] !== undefined) s.attr(null, 'tgtFrame', String(value['tgtFrame']));
  if (value['tooltip'] !== undefined) s.attr(null, 'tooltip', String(value['tooltip']));
  if (value['history'] !== undefined) s.attr(null, 'history', String(value['history']));
  if (value['highlightClick'] !== undefined) s.attr(null, 'highlightClick', String(value['highlightClick']));
  if (value['endSnd'] !== undefined) s.attr(null, 'endSnd', String(value['endSnd']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['snd'] !== undefined) writeCT_EmbeddedWAVAudioFile(s, value['snd'], ctx, 'snd');
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_InnerShadowEffect`; the caller supplies its element local name. */
export function writeCT_InnerShadowEffect(s: XmlSink, value: CT_InnerShadowEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['blurRad'] !== undefined) s.attr(null, 'blurRad', String(value['blurRad']));
  if (value['dist'] !== undefined) s.attr(null, 'dist', String(value['dist']));
  if (value['dir'] !== undefined) s.attr(null, 'dir', String(value['dir']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_InverseGammaTransform`; the caller supplies its element local name. */
export function writeCT_InverseGammaTransform(s: XmlSink, value: CT_InverseGammaTransform, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_InverseTransform`; the caller supplies its element local name. */
export function writeCT_InverseTransform(s: XmlSink, value: CT_InverseTransform, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_LightRig`; the caller supplies its element local name. */
export function writeCT_LightRig(s: XmlSink, value: CT_LightRig, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['rig'] !== undefined) s.attr(null, 'rig', String(value['rig']));
  if (value['dir'] !== undefined) s.attr(null, 'dir', String(value['dir']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['rot'] !== undefined) writeCT_SphereCoords(s, value['rot'], ctx, 'rot');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_LinearShadeProperties`; the caller supplies its element local name. */
export function writeCT_LinearShadeProperties(s: XmlSink, value: CT_LinearShadeProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['ang'] !== undefined) s.attr(null, 'ang', String(value['ang']));
  if (value['scaled'] !== undefined) s.attr(null, 'scaled', String(value['scaled']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_LineEndProperties`; the caller supplies its element local name. */
export function writeCT_LineEndProperties(s: XmlSink, value: CT_LineEndProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['w'] !== undefined) s.attr(null, 'w', String(value['w']));
  if (value['len'] !== undefined) s.attr(null, 'len', String(value['len']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_LineJoinBevel`; the caller supplies its element local name. */
export function writeCT_LineJoinBevel(s: XmlSink, value: CT_LineJoinBevel, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_LineJoinMiterProperties`; the caller supplies its element local name. */
export function writeCT_LineJoinMiterProperties(s: XmlSink, value: CT_LineJoinMiterProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['lim'] !== undefined) s.attr(null, 'lim', String(value['lim']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_LineJoinRound`; the caller supplies its element local name. */
export function writeCT_LineJoinRound(s: XmlSink, value: CT_LineJoinRound, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_LineProperties`; the caller supplies its element local name. */
export function writeCT_LineProperties(s: XmlSink, value: CT_LineProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['w'] !== undefined) s.attr(null, 'w', String(value['w']));
  if (value['cap'] !== undefined) s.attr(null, 'cap', String(value['cap']));
  if (value['cmpd'] !== undefined) s.attr(null, 'cmpd', String(value['cmpd']));
  if (value['algn'] !== undefined) s.attr(null, 'algn', String(value['algn']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['lineFillProperties'] !== undefined) {
    if (value['lineFillProperties'].kind === 'noFill') writeCT_NoFillProperties(s, value['lineFillProperties'].value, ctx, 'noFill');
    if (value['lineFillProperties'].kind === 'solidFill') writeCT_SolidColorFillProperties(s, value['lineFillProperties'].value, ctx, 'solidFill');
    if (value['lineFillProperties'].kind === 'gradFill') writeCT_GradientFillProperties(s, value['lineFillProperties'].value, ctx, 'gradFill');
    if (value['lineFillProperties'].kind === 'pattFill') writeCT_PatternFillProperties(s, value['lineFillProperties'].value, ctx, 'pattFill');
  }
  $q.flush(s, 0);
  if (value['lineDashProperties'] !== undefined) {
    if (value['lineDashProperties'].kind === 'prstDash') writeCT_PresetLineDashProperties(s, value['lineDashProperties'].value, ctx, 'prstDash');
    if (value['lineDashProperties'].kind === 'custDash') writeCT_DashStopList(s, value['lineDashProperties'].value, ctx, 'custDash');
  }
  $q.flush(s, 1);
  if (value['lineJoinProperties'] !== undefined) {
    if (value['lineJoinProperties'].kind === 'round') writeCT_LineJoinRound(s, value['lineJoinProperties'].value, ctx, 'round');
    if (value['lineJoinProperties'].kind === 'bevel') writeCT_LineJoinBevel(s, value['lineJoinProperties'].value, ctx, 'bevel');
    if (value['lineJoinProperties'].kind === 'miter') writeCT_LineJoinMiterProperties(s, value['lineJoinProperties'].value, ctx, 'miter');
  }
  $q.flush(s, 2);
  if (value['headEnd'] !== undefined) writeCT_LineEndProperties(s, value['headEnd'], ctx, 'headEnd');
  $q.flush(s, 3);
  if (value['tailEnd'] !== undefined) writeCT_LineEndProperties(s, value['tailEnd'], ctx, 'tailEnd');
  $q.flush(s, 4);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 5);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_LineStyleList`; the caller supplies its element local name. */
export function writeCT_LineStyleList(s: XmlSink, value: CT_LineStyleList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['ln'].length; idx++) {
    const v_ln = value['ln'][idx]!;
    writeCT_LineProperties(s, v_ln, ctx, 'ln');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_LuminanceEffect`; the caller supplies its element local name. */
export function writeCT_LuminanceEffect(s: XmlSink, value: CT_LuminanceEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['bright'] !== undefined) s.attr(null, 'bright', String(value['bright']));
  if (value['contrast'] !== undefined) s.attr(null, 'contrast', String(value['contrast']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_NoFillProperties`; the caller supplies its element local name. */
export function writeCT_NoFillProperties(s: XmlSink, value: CT_NoFillProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_NonVisualConnectorProperties`; the caller supplies its element local name. */
export function writeCT_NonVisualConnectorProperties(s: XmlSink, value: CT_NonVisualConnectorProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cxnSpLocks'] !== undefined) writeCT_ConnectorLocking(s, value['cxnSpLocks'], ctx, 'cxnSpLocks');
  $q.flush(s, 0);
  if (value['stCxn'] !== undefined) writeCT_Connection(s, value['stCxn'], ctx, 'stCxn');
  $q.flush(s, 1);
  if (value['endCxn'] !== undefined) writeCT_Connection(s, value['endCxn'], ctx, 'endCxn');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NonVisualContentPartProperties`; the caller supplies its element local name. */
export function writeCT_NonVisualContentPartProperties(s: XmlSink, value: CT_NonVisualContentPartProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['isComment'] !== undefined) s.attr(null, 'isComment', String(value['isComment']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['cpLocks'] !== undefined) writeCT_ContentPartLocking(s, value['cpLocks'], ctx, 'cpLocks');
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NonVisualDrawingProps`; the caller supplies its element local name. */
export function writeCT_NonVisualDrawingProps(s: XmlSink, value: CT_NonVisualDrawingProps, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['id'] !== undefined) s.attr(null, 'id', String(value['id']));
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  if (value['descr'] !== undefined) s.attr(null, 'descr', String(value['descr']));
  if (value['hidden'] !== undefined) s.attr(null, 'hidden', String(value['hidden']));
  if (value['title'] !== undefined) s.attr(null, 'title', String(value['title']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['hlinkClick'] !== undefined) writeCT_Hyperlink(s, value['hlinkClick'], ctx, 'hlinkClick');
  $q.flush(s, 0);
  if (value['hlinkHover'] !== undefined) writeCT_Hyperlink(s, value['hlinkHover'], ctx, 'hlinkHover');
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NonVisualDrawingShapeProps`; the caller supplies its element local name. */
export function writeCT_NonVisualDrawingShapeProps(s: XmlSink, value: CT_NonVisualDrawingShapeProps, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['txBox'] !== undefined) s.attr(null, 'txBox', String(value['txBox']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['spLocks'] !== undefined) writeCT_ShapeLocking(s, value['spLocks'], ctx, 'spLocks');
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NonVisualGraphicFrameProperties`; the caller supplies its element local name. */
export function writeCT_NonVisualGraphicFrameProperties(s: XmlSink, value: CT_NonVisualGraphicFrameProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['graphicFrameLocks'] !== undefined) writeCT_GraphicalObjectFrameLocking(s, value['graphicFrameLocks'], ctx, 'graphicFrameLocks');
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NonVisualGroupDrawingShapeProps`; the caller supplies its element local name. */
export function writeCT_NonVisualGroupDrawingShapeProps(s: XmlSink, value: CT_NonVisualGroupDrawingShapeProps, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['grpSpLocks'] !== undefined) writeCT_GroupLocking(s, value['grpSpLocks'], ctx, 'grpSpLocks');
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_NonVisualPictureProperties`; the caller supplies its element local name. */
export function writeCT_NonVisualPictureProperties(s: XmlSink, value: CT_NonVisualPictureProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['preferRelativeResize'] !== undefined) s.attr(null, 'preferRelativeResize', String(value['preferRelativeResize']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['picLocks'] !== undefined) writeCT_PictureLocking(s, value['picLocks'], ctx, 'picLocks');
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ObjectStyleDefaults`; the caller supplies its element local name. */
export function writeCT_ObjectStyleDefaults(s: XmlSink, value: CT_ObjectStyleDefaults, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['spDef'] !== undefined) writeCT_DefaultShapeDefinition(s, value['spDef'], ctx, 'spDef');
  $q.flush(s, 0);
  if (value['lnDef'] !== undefined) writeCT_DefaultShapeDefinition(s, value['lnDef'], ctx, 'lnDef');
  $q.flush(s, 1);
  if (value['txDef'] !== undefined) writeCT_DefaultShapeDefinition(s, value['txDef'], ctx, 'txDef');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_OfficeArtExtension`; the caller supplies its element local name. */
export function writeCT_OfficeArtExtension(s: XmlSink, value: CT_OfficeArtExtension, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['uri'] !== undefined) s.attr(null, 'uri', String(value['uri']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (const raw of value['any']) {
    s.raw(raw);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_OfficeArtExtensionList`; the caller supplies its element local name. */
export function writeCT_OfficeArtExtensionList(s: XmlSink, value: CT_OfficeArtExtensionList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['ext'].length; idx++) {
    const v_ext = value['ext'][idx]!;
    writeCT_OfficeArtExtension(s, v_ext, ctx, 'ext');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_OfficeStyleSheet`; the caller supplies its element local name. */
export function writeCT_OfficeStyleSheet(s: XmlSink, value: CT_OfficeStyleSheet, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['themeElements'] !== undefined) writeCT_BaseStyles(s, value['themeElements'], ctx, 'themeElements');
  $q.flush(s, 0);
  if (value['objectDefaults'] !== undefined) writeCT_ObjectStyleDefaults(s, value['objectDefaults'], ctx, 'objectDefaults');
  $q.flush(s, 1);
  if (value['extraClrSchemeLst'] !== undefined) writeCT_ColorSchemeList(s, value['extraClrSchemeLst'], ctx, 'extraClrSchemeLst');
  $q.flush(s, 2);
  if (value['custClrLst'] !== undefined) writeCT_CustomColorList(s, value['custClrLst'], ctx, 'custClrLst');
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_OuterShadowEffect`; the caller supplies its element local name. */
export function writeCT_OuterShadowEffect(s: XmlSink, value: CT_OuterShadowEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['blurRad'] !== undefined) s.attr(null, 'blurRad', String(value['blurRad']));
  if (value['dist'] !== undefined) s.attr(null, 'dist', String(value['dist']));
  if (value['dir'] !== undefined) s.attr(null, 'dir', String(value['dir']));
  if (value['sx'] !== undefined) s.attr(null, 'sx', String(value['sx']));
  if (value['sy'] !== undefined) s.attr(null, 'sy', String(value['sy']));
  if (value['kx'] !== undefined) s.attr(null, 'kx', String(value['kx']));
  if (value['ky'] !== undefined) s.attr(null, 'ky', String(value['ky']));
  if (value['algn'] !== undefined) s.attr(null, 'algn', String(value['algn']));
  if (value['rotWithShape'] !== undefined) s.attr(null, 'rotWithShape', String(value['rotWithShape']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Path2D`; the caller supplies its element local name. */
export function writeCT_Path2D(s: XmlSink, value: CT_Path2D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['w'] !== undefined) s.attr(null, 'w', String(value['w']));
  if (value['h'] !== undefined) s.attr(null, 'h', String(value['h']));
  if (value['fill'] !== undefined) s.attr(null, 'fill', String(value['fill']));
  if (value['stroke'] !== undefined) s.attr(null, 'stroke', String(value['stroke']));
  if (value['extrusionOk'] !== undefined) s.attr(null, 'extrusionOk', String(value['extrusionOk']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_content of value['content']) {
    if (v_content.kind === 'close') writeCT_Path2DClose(s, v_content.value, ctx, 'close');
    if (v_content.kind === 'moveTo') writeCT_Path2DMoveTo(s, v_content.value, ctx, 'moveTo');
    if (v_content.kind === 'lnTo') writeCT_Path2DLineTo(s, v_content.value, ctx, 'lnTo');
    if (v_content.kind === 'arcTo') writeCT_Path2DArcTo(s, v_content.value, ctx, 'arcTo');
    if (v_content.kind === 'quadBezTo') writeCT_Path2DQuadBezierTo(s, v_content.value, ctx, 'quadBezTo');
    if (v_content.kind === 'cubicBezTo') writeCT_Path2DCubicBezierTo(s, v_content.value, ctx, 'cubicBezTo');
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  s.endElement();
}

/** Write a `CT_Path2DArcTo`; the caller supplies its element local name. */
export function writeCT_Path2DArcTo(s: XmlSink, value: CT_Path2DArcTo, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['wR'] !== undefined) s.attr(null, 'wR', String(value['wR']));
  if (value['hR'] !== undefined) s.attr(null, 'hR', String(value['hR']));
  if (value['stAng'] !== undefined) s.attr(null, 'stAng', String(value['stAng']));
  if (value['swAng'] !== undefined) s.attr(null, 'swAng', String(value['swAng']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Path2DClose`; the caller supplies its element local name. */
export function writeCT_Path2DClose(s: XmlSink, value: CT_Path2DClose, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Path2DCubicBezierTo`; the caller supplies its element local name. */
export function writeCT_Path2DCubicBezierTo(s: XmlSink, value: CT_Path2DCubicBezierTo, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['pt'].length; idx++) {
    const v_pt = value['pt'][idx]!;
    writeCT_AdjPoint2D(s, v_pt, ctx, 'pt');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Path2DLineTo`; the caller supplies its element local name. */
export function writeCT_Path2DLineTo(s: XmlSink, value: CT_Path2DLineTo, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['pt'] !== undefined) writeCT_AdjPoint2D(s, value['pt'], ctx, 'pt');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Path2DList`; the caller supplies its element local name. */
export function writeCT_Path2DList(s: XmlSink, value: CT_Path2DList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['path'].length; idx++) {
    const v_path = value['path'][idx]!;
    writeCT_Path2D(s, v_path, ctx, 'path');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Path2DMoveTo`; the caller supplies its element local name. */
export function writeCT_Path2DMoveTo(s: XmlSink, value: CT_Path2DMoveTo, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['pt'] !== undefined) writeCT_AdjPoint2D(s, value['pt'], ctx, 'pt');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Path2DQuadBezierTo`; the caller supplies its element local name. */
export function writeCT_Path2DQuadBezierTo(s: XmlSink, value: CT_Path2DQuadBezierTo, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['pt'].length; idx++) {
    const v_pt = value['pt'][idx]!;
    writeCT_AdjPoint2D(s, v_pt, ctx, 'pt');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_PathShadeProperties`; the caller supplies its element local name. */
export function writeCT_PathShadeProperties(s: XmlSink, value: CT_PathShadeProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['path'] !== undefined) s.attr(null, 'path', String(value['path']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['fillToRect'] !== undefined) writeCT_RelativeRect(s, value['fillToRect'], ctx, 'fillToRect');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_PatternFillProperties`; the caller supplies its element local name. */
export function writeCT_PatternFillProperties(s: XmlSink, value: CT_PatternFillProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['prst'] !== undefined) s.attr(null, 'prst', String(value['prst']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['fgClr'] !== undefined) writeCT_Color(s, value['fgClr'], ctx, 'fgClr');
  $q.flush(s, 0);
  if (value['bgClr'] !== undefined) writeCT_Color(s, value['bgClr'], ctx, 'bgClr');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Percentage`; the caller supplies its element local name. */
export function writeCT_Percentage(s: XmlSink, value: CT_Percentage, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_PictureLocking`; the caller supplies its element local name. */
export function writeCT_PictureLocking(s: XmlSink, value: CT_PictureLocking, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['noGrp'] !== undefined) s.attr(null, 'noGrp', String(value['noGrp']));
  if (value['noSelect'] !== undefined) s.attr(null, 'noSelect', String(value['noSelect']));
  if (value['noRot'] !== undefined) s.attr(null, 'noRot', String(value['noRot']));
  if (value['noChangeAspect'] !== undefined) s.attr(null, 'noChangeAspect', String(value['noChangeAspect']));
  if (value['noMove'] !== undefined) s.attr(null, 'noMove', String(value['noMove']));
  if (value['noResize'] !== undefined) s.attr(null, 'noResize', String(value['noResize']));
  if (value['noEditPoints'] !== undefined) s.attr(null, 'noEditPoints', String(value['noEditPoints']));
  if (value['noAdjustHandles'] !== undefined) s.attr(null, 'noAdjustHandles', String(value['noAdjustHandles']));
  if (value['noChangeArrowheads'] !== undefined) s.attr(null, 'noChangeArrowheads', String(value['noChangeArrowheads']));
  if (value['noChangeShapeType'] !== undefined) s.attr(null, 'noChangeShapeType', String(value['noChangeShapeType']));
  if (value['noCrop'] !== undefined) s.attr(null, 'noCrop', String(value['noCrop']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Point2D`; the caller supplies its element local name. */
export function writeCT_Point2D(s: XmlSink, value: CT_Point2D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['x'] !== undefined) s.attr(null, 'x', String(value['x']));
  if (value['y'] !== undefined) s.attr(null, 'y', String(value['y']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Point3D`; the caller supplies its element local name. */
export function writeCT_Point3D(s: XmlSink, value: CT_Point3D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['x'] !== undefined) s.attr(null, 'x', String(value['x']));
  if (value['y'] !== undefined) s.attr(null, 'y', String(value['y']));
  if (value['z'] !== undefined) s.attr(null, 'z', String(value['z']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_PolarAdjustHandle`; the caller supplies its element local name. */
export function writeCT_PolarAdjustHandle(s: XmlSink, value: CT_PolarAdjustHandle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['gdRefR'] !== undefined) s.attr(null, 'gdRefR', String(value['gdRefR']));
  if (value['minR'] !== undefined) s.attr(null, 'minR', String(value['minR']));
  if (value['maxR'] !== undefined) s.attr(null, 'maxR', String(value['maxR']));
  if (value['gdRefAng'] !== undefined) s.attr(null, 'gdRefAng', String(value['gdRefAng']));
  if (value['minAng'] !== undefined) s.attr(null, 'minAng', String(value['minAng']));
  if (value['maxAng'] !== undefined) s.attr(null, 'maxAng', String(value['maxAng']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['pos'] !== undefined) writeCT_AdjPoint2D(s, value['pos'], ctx, 'pos');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_PositiveFixedAngle`; the caller supplies its element local name. */
export function writeCT_PositiveFixedAngle(s: XmlSink, value: CT_PositiveFixedAngle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_PositiveFixedPercentage`; the caller supplies its element local name. */
export function writeCT_PositiveFixedPercentage(s: XmlSink, value: CT_PositiveFixedPercentage, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_PositivePercentage`; the caller supplies its element local name. */
export function writeCT_PositivePercentage(s: XmlSink, value: CT_PositivePercentage, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_PositiveSize2D`; the caller supplies its element local name. */
export function writeCT_PositiveSize2D(s: XmlSink, value: CT_PositiveSize2D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['cx'] !== undefined) s.attr(null, 'cx', String(value['cx']));
  if (value['cy'] !== undefined) s.attr(null, 'cy', String(value['cy']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_PresetColor`; the caller supplies its element local name. */
export function writeCT_PresetColor(s: XmlSink, value: CT_PresetColor, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_colorTransform of value['colorTransform']) {
    if (v_colorTransform.kind === 'tint') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'tint');
    if (v_colorTransform.kind === 'shade') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'shade');
    if (v_colorTransform.kind === 'comp') writeCT_ComplementTransform(s, v_colorTransform.value, ctx, 'comp');
    if (v_colorTransform.kind === 'inv') writeCT_InverseTransform(s, v_colorTransform.value, ctx, 'inv');
    if (v_colorTransform.kind === 'gray') writeCT_GrayscaleTransform(s, v_colorTransform.value, ctx, 'gray');
    if (v_colorTransform.kind === 'alpha') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'alpha');
    if (v_colorTransform.kind === 'alphaOff') writeCT_FixedPercentage(s, v_colorTransform.value, ctx, 'alphaOff');
    if (v_colorTransform.kind === 'alphaMod') writeCT_PositivePercentage(s, v_colorTransform.value, ctx, 'alphaMod');
    if (v_colorTransform.kind === 'hue') writeCT_PositiveFixedAngle(s, v_colorTransform.value, ctx, 'hue');
    if (v_colorTransform.kind === 'hueOff') writeCT_Angle(s, v_colorTransform.value, ctx, 'hueOff');
    if (v_colorTransform.kind === 'hueMod') writeCT_PositivePercentage(s, v_colorTransform.value, ctx, 'hueMod');
    if (v_colorTransform.kind === 'sat') writeCT_Percentage(s, v_colorTransform.value, ctx, 'sat');
    if (v_colorTransform.kind === 'satOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'satOff');
    if (v_colorTransform.kind === 'satMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'satMod');
    if (v_colorTransform.kind === 'lum') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lum');
    if (v_colorTransform.kind === 'lumOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lumOff');
    if (v_colorTransform.kind === 'lumMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lumMod');
    if (v_colorTransform.kind === 'red') writeCT_Percentage(s, v_colorTransform.value, ctx, 'red');
    if (v_colorTransform.kind === 'redOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'redOff');
    if (v_colorTransform.kind === 'redMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'redMod');
    if (v_colorTransform.kind === 'green') writeCT_Percentage(s, v_colorTransform.value, ctx, 'green');
    if (v_colorTransform.kind === 'greenOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'greenOff');
    if (v_colorTransform.kind === 'greenMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'greenMod');
    if (v_colorTransform.kind === 'blue') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blue');
    if (v_colorTransform.kind === 'blueOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blueOff');
    if (v_colorTransform.kind === 'blueMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blueMod');
    if (v_colorTransform.kind === 'gamma') writeCT_GammaTransform(s, v_colorTransform.value, ctx, 'gamma');
    if (v_colorTransform.kind === 'invGamma') writeCT_InverseGammaTransform(s, v_colorTransform.value, ctx, 'invGamma');
    if (v_colorTransform.kind === '$raw') { s.raw(v_colorTransform.value); }
  }
  s.endElement();
}

/** Write a `CT_PresetGeometry2D`; the caller supplies its element local name. */
export function writeCT_PresetGeometry2D(s: XmlSink, value: CT_PresetGeometry2D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['prst'] !== undefined) s.attr(null, 'prst', String(value['prst']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['avLst'] !== undefined) writeCT_GeomGuideList(s, value['avLst'], ctx, 'avLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_PresetLineDashProperties`; the caller supplies its element local name. */
export function writeCT_PresetLineDashProperties(s: XmlSink, value: CT_PresetLineDashProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_PresetShadowEffect`; the caller supplies its element local name. */
export function writeCT_PresetShadowEffect(s: XmlSink, value: CT_PresetShadowEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['prst'] !== undefined) s.attr(null, 'prst', String(value['prst']));
  if (value['dist'] !== undefined) s.attr(null, 'dist', String(value['dist']));
  if (value['dir'] !== undefined) s.attr(null, 'dir', String(value['dir']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_PresetTextShape`; the caller supplies its element local name. */
export function writeCT_PresetTextShape(s: XmlSink, value: CT_PresetTextShape, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['prst'] !== undefined) s.attr(null, 'prst', String(value['prst']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['avLst'] !== undefined) writeCT_GeomGuideList(s, value['avLst'], ctx, 'avLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_QuickTimeFile`; the caller supplies its element local name. */
export function writeCT_QuickTimeFile(s: XmlSink, value: CT_QuickTimeFile, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['link'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'link', String(value['link']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Ratio`; the caller supplies its element local name. */
export function writeCT_Ratio(s: XmlSink, value: CT_Ratio, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['n'] !== undefined) s.attr(null, 'n', String(value['n']));
  if (value['d'] !== undefined) s.attr(null, 'd', String(value['d']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ReflectionEffect`; the caller supplies its element local name. */
export function writeCT_ReflectionEffect(s: XmlSink, value: CT_ReflectionEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['blurRad'] !== undefined) s.attr(null, 'blurRad', String(value['blurRad']));
  if (value['stA'] !== undefined) s.attr(null, 'stA', String(value['stA']));
  if (value['stPos'] !== undefined) s.attr(null, 'stPos', String(value['stPos']));
  if (value['endA'] !== undefined) s.attr(null, 'endA', String(value['endA']));
  if (value['endPos'] !== undefined) s.attr(null, 'endPos', String(value['endPos']));
  if (value['dist'] !== undefined) s.attr(null, 'dist', String(value['dist']));
  if (value['dir'] !== undefined) s.attr(null, 'dir', String(value['dir']));
  if (value['fadeDir'] !== undefined) s.attr(null, 'fadeDir', String(value['fadeDir']));
  if (value['sx'] !== undefined) s.attr(null, 'sx', String(value['sx']));
  if (value['sy'] !== undefined) s.attr(null, 'sy', String(value['sy']));
  if (value['kx'] !== undefined) s.attr(null, 'kx', String(value['kx']));
  if (value['ky'] !== undefined) s.attr(null, 'ky', String(value['ky']));
  if (value['algn'] !== undefined) s.attr(null, 'algn', String(value['algn']));
  if (value['rotWithShape'] !== undefined) s.attr(null, 'rotWithShape', String(value['rotWithShape']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_RegularTextRun`; the caller supplies its element local name. */
export function writeCT_RegularTextRun(s: XmlSink, value: CT_RegularTextRun, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['rPr'] !== undefined) writeCT_TextCharacterProperties(s, value['rPr'], ctx, 'rPr');
  $q.flush(s, 0);
  if (value['t'] !== undefined) s.text(String(value['t']));
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_RelativeOffsetEffect`; the caller supplies its element local name. */
export function writeCT_RelativeOffsetEffect(s: XmlSink, value: CT_RelativeOffsetEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['tx'] !== undefined) s.attr(null, 'tx', String(value['tx']));
  if (value['ty'] !== undefined) s.attr(null, 'ty', String(value['ty']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_RelativeRect`; the caller supplies its element local name. */
export function writeCT_RelativeRect(s: XmlSink, value: CT_RelativeRect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['l'] !== undefined) s.attr(null, 'l', String(value['l']));
  if (value['t'] !== undefined) s.attr(null, 't', String(value['t']));
  if (value['r'] !== undefined) s.attr(null, 'r', String(value['r']));
  if (value['b'] !== undefined) s.attr(null, 'b', String(value['b']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Scale2D`; the caller supplies its element local name. */
export function writeCT_Scale2D(s: XmlSink, value: CT_Scale2D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['sx'] !== undefined) writeCT_Ratio(s, value['sx'], ctx, 'sx');
  $q.flush(s, 0);
  if (value['sy'] !== undefined) writeCT_Ratio(s, value['sy'], ctx, 'sy');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Scene3D`; the caller supplies its element local name. */
export function writeCT_Scene3D(s: XmlSink, value: CT_Scene3D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['camera'] !== undefined) writeCT_Camera(s, value['camera'], ctx, 'camera');
  $q.flush(s, 0);
  if (value['lightRig'] !== undefined) writeCT_LightRig(s, value['lightRig'], ctx, 'lightRig');
  $q.flush(s, 1);
  if (value['backdrop'] !== undefined) writeCT_Backdrop(s, value['backdrop'], ctx, 'backdrop');
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SchemeColor`; the caller supplies its element local name. */
export function writeCT_SchemeColor(s: XmlSink, value: CT_SchemeColor, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_colorTransform of value['colorTransform']) {
    if (v_colorTransform.kind === 'tint') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'tint');
    if (v_colorTransform.kind === 'shade') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'shade');
    if (v_colorTransform.kind === 'comp') writeCT_ComplementTransform(s, v_colorTransform.value, ctx, 'comp');
    if (v_colorTransform.kind === 'inv') writeCT_InverseTransform(s, v_colorTransform.value, ctx, 'inv');
    if (v_colorTransform.kind === 'gray') writeCT_GrayscaleTransform(s, v_colorTransform.value, ctx, 'gray');
    if (v_colorTransform.kind === 'alpha') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'alpha');
    if (v_colorTransform.kind === 'alphaOff') writeCT_FixedPercentage(s, v_colorTransform.value, ctx, 'alphaOff');
    if (v_colorTransform.kind === 'alphaMod') writeCT_PositivePercentage(s, v_colorTransform.value, ctx, 'alphaMod');
    if (v_colorTransform.kind === 'hue') writeCT_PositiveFixedAngle(s, v_colorTransform.value, ctx, 'hue');
    if (v_colorTransform.kind === 'hueOff') writeCT_Angle(s, v_colorTransform.value, ctx, 'hueOff');
    if (v_colorTransform.kind === 'hueMod') writeCT_PositivePercentage(s, v_colorTransform.value, ctx, 'hueMod');
    if (v_colorTransform.kind === 'sat') writeCT_Percentage(s, v_colorTransform.value, ctx, 'sat');
    if (v_colorTransform.kind === 'satOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'satOff');
    if (v_colorTransform.kind === 'satMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'satMod');
    if (v_colorTransform.kind === 'lum') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lum');
    if (v_colorTransform.kind === 'lumOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lumOff');
    if (v_colorTransform.kind === 'lumMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lumMod');
    if (v_colorTransform.kind === 'red') writeCT_Percentage(s, v_colorTransform.value, ctx, 'red');
    if (v_colorTransform.kind === 'redOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'redOff');
    if (v_colorTransform.kind === 'redMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'redMod');
    if (v_colorTransform.kind === 'green') writeCT_Percentage(s, v_colorTransform.value, ctx, 'green');
    if (v_colorTransform.kind === 'greenOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'greenOff');
    if (v_colorTransform.kind === 'greenMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'greenMod');
    if (v_colorTransform.kind === 'blue') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blue');
    if (v_colorTransform.kind === 'blueOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blueOff');
    if (v_colorTransform.kind === 'blueMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blueMod');
    if (v_colorTransform.kind === 'gamma') writeCT_GammaTransform(s, v_colorTransform.value, ctx, 'gamma');
    if (v_colorTransform.kind === 'invGamma') writeCT_InverseGammaTransform(s, v_colorTransform.value, ctx, 'invGamma');
    if (v_colorTransform.kind === '$raw') { s.raw(v_colorTransform.value); }
  }
  s.endElement();
}

/** Write a `CT_ScRgbColor`; the caller supplies its element local name. */
export function writeCT_ScRgbColor(s: XmlSink, value: CT_ScRgbColor, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['r'] !== undefined) s.attr(null, 'r', String(value['r']));
  if (value['g'] !== undefined) s.attr(null, 'g', String(value['g']));
  if (value['b'] !== undefined) s.attr(null, 'b', String(value['b']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_colorTransform of value['colorTransform']) {
    if (v_colorTransform.kind === 'tint') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'tint');
    if (v_colorTransform.kind === 'shade') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'shade');
    if (v_colorTransform.kind === 'comp') writeCT_ComplementTransform(s, v_colorTransform.value, ctx, 'comp');
    if (v_colorTransform.kind === 'inv') writeCT_InverseTransform(s, v_colorTransform.value, ctx, 'inv');
    if (v_colorTransform.kind === 'gray') writeCT_GrayscaleTransform(s, v_colorTransform.value, ctx, 'gray');
    if (v_colorTransform.kind === 'alpha') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'alpha');
    if (v_colorTransform.kind === 'alphaOff') writeCT_FixedPercentage(s, v_colorTransform.value, ctx, 'alphaOff');
    if (v_colorTransform.kind === 'alphaMod') writeCT_PositivePercentage(s, v_colorTransform.value, ctx, 'alphaMod');
    if (v_colorTransform.kind === 'hue') writeCT_PositiveFixedAngle(s, v_colorTransform.value, ctx, 'hue');
    if (v_colorTransform.kind === 'hueOff') writeCT_Angle(s, v_colorTransform.value, ctx, 'hueOff');
    if (v_colorTransform.kind === 'hueMod') writeCT_PositivePercentage(s, v_colorTransform.value, ctx, 'hueMod');
    if (v_colorTransform.kind === 'sat') writeCT_Percentage(s, v_colorTransform.value, ctx, 'sat');
    if (v_colorTransform.kind === 'satOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'satOff');
    if (v_colorTransform.kind === 'satMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'satMod');
    if (v_colorTransform.kind === 'lum') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lum');
    if (v_colorTransform.kind === 'lumOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lumOff');
    if (v_colorTransform.kind === 'lumMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lumMod');
    if (v_colorTransform.kind === 'red') writeCT_Percentage(s, v_colorTransform.value, ctx, 'red');
    if (v_colorTransform.kind === 'redOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'redOff');
    if (v_colorTransform.kind === 'redMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'redMod');
    if (v_colorTransform.kind === 'green') writeCT_Percentage(s, v_colorTransform.value, ctx, 'green');
    if (v_colorTransform.kind === 'greenOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'greenOff');
    if (v_colorTransform.kind === 'greenMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'greenMod');
    if (v_colorTransform.kind === 'blue') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blue');
    if (v_colorTransform.kind === 'blueOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blueOff');
    if (v_colorTransform.kind === 'blueMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blueMod');
    if (v_colorTransform.kind === 'gamma') writeCT_GammaTransform(s, v_colorTransform.value, ctx, 'gamma');
    if (v_colorTransform.kind === 'invGamma') writeCT_InverseGammaTransform(s, v_colorTransform.value, ctx, 'invGamma');
    if (v_colorTransform.kind === '$raw') { s.raw(v_colorTransform.value); }
  }
  s.endElement();
}

/** Write a `CT_Shape3D`; the caller supplies its element local name. */
export function writeCT_Shape3D(s: XmlSink, value: CT_Shape3D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['z'] !== undefined) s.attr(null, 'z', String(value['z']));
  if (value['extrusionH'] !== undefined) s.attr(null, 'extrusionH', String(value['extrusionH']));
  if (value['contourW'] !== undefined) s.attr(null, 'contourW', String(value['contourW']));
  if (value['prstMaterial'] !== undefined) s.attr(null, 'prstMaterial', String(value['prstMaterial']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['bevelT'] !== undefined) writeCT_Bevel(s, value['bevelT'], ctx, 'bevelT');
  $q.flush(s, 0);
  if (value['bevelB'] !== undefined) writeCT_Bevel(s, value['bevelB'], ctx, 'bevelB');
  $q.flush(s, 1);
  if (value['extrusionClr'] !== undefined) writeCT_Color(s, value['extrusionClr'], ctx, 'extrusionClr');
  $q.flush(s, 2);
  if (value['contourClr'] !== undefined) writeCT_Color(s, value['contourClr'], ctx, 'contourClr');
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ShapeLocking`; the caller supplies its element local name. */
export function writeCT_ShapeLocking(s: XmlSink, value: CT_ShapeLocking, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['noGrp'] !== undefined) s.attr(null, 'noGrp', String(value['noGrp']));
  if (value['noSelect'] !== undefined) s.attr(null, 'noSelect', String(value['noSelect']));
  if (value['noRot'] !== undefined) s.attr(null, 'noRot', String(value['noRot']));
  if (value['noChangeAspect'] !== undefined) s.attr(null, 'noChangeAspect', String(value['noChangeAspect']));
  if (value['noMove'] !== undefined) s.attr(null, 'noMove', String(value['noMove']));
  if (value['noResize'] !== undefined) s.attr(null, 'noResize', String(value['noResize']));
  if (value['noEditPoints'] !== undefined) s.attr(null, 'noEditPoints', String(value['noEditPoints']));
  if (value['noAdjustHandles'] !== undefined) s.attr(null, 'noAdjustHandles', String(value['noAdjustHandles']));
  if (value['noChangeArrowheads'] !== undefined) s.attr(null, 'noChangeArrowheads', String(value['noChangeArrowheads']));
  if (value['noChangeShapeType'] !== undefined) s.attr(null, 'noChangeShapeType', String(value['noChangeShapeType']));
  if (value['noTextEdit'] !== undefined) s.attr(null, 'noTextEdit', String(value['noTextEdit']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ShapeProperties`; the caller supplies its element local name. */
export function writeCT_ShapeProperties(s: XmlSink, value: CT_ShapeProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['bwMode'] !== undefined) s.attr(null, 'bwMode', String(value['bwMode']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['xfrm'] !== undefined) writeCT_Transform2D(s, value['xfrm'], ctx, 'xfrm');
  $q.flush(s, 0);
  if (value['geometry'] !== undefined) {
    if (value['geometry'].kind === 'custGeom') writeCT_CustomGeometry2D(s, value['geometry'].value, ctx, 'custGeom');
    if (value['geometry'].kind === 'prstGeom') writeCT_PresetGeometry2D(s, value['geometry'].value, ctx, 'prstGeom');
  }
  $q.flush(s, 1);
  if (value['fillProperties'] !== undefined) {
    if (value['fillProperties'].kind === 'noFill') writeCT_NoFillProperties(s, value['fillProperties'].value, ctx, 'noFill');
    if (value['fillProperties'].kind === 'solidFill') writeCT_SolidColorFillProperties(s, value['fillProperties'].value, ctx, 'solidFill');
    if (value['fillProperties'].kind === 'gradFill') writeCT_GradientFillProperties(s, value['fillProperties'].value, ctx, 'gradFill');
    if (value['fillProperties'].kind === 'blipFill') writeCT_BlipFillProperties(s, value['fillProperties'].value, ctx, 'blipFill');
    if (value['fillProperties'].kind === 'pattFill') writeCT_PatternFillProperties(s, value['fillProperties'].value, ctx, 'pattFill');
    if (value['fillProperties'].kind === 'grpFill') writeCT_GroupFillProperties(s, value['fillProperties'].value, ctx, 'grpFill');
  }
  $q.flush(s, 2);
  if (value['ln'] !== undefined) writeCT_LineProperties(s, value['ln'], ctx, 'ln');
  $q.flush(s, 3);
  if (value['effectProperties'] !== undefined) {
    if (value['effectProperties'].kind === 'effectLst') writeCT_EffectList(s, value['effectProperties'].value, ctx, 'effectLst');
    if (value['effectProperties'].kind === 'effectDag') writeCT_EffectContainer(s, value['effectProperties'].value, ctx, 'effectDag');
  }
  $q.flush(s, 4);
  if (value['scene3d'] !== undefined) writeCT_Scene3D(s, value['scene3d'], ctx, 'scene3d');
  $q.flush(s, 5);
  if (value['sp3d'] !== undefined) writeCT_Shape3D(s, value['sp3d'], ctx, 'sp3d');
  $q.flush(s, 6);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 7);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_ShapeStyle`; the caller supplies its element local name. */
export function writeCT_ShapeStyle(s: XmlSink, value: CT_ShapeStyle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['lnRef'] !== undefined) writeCT_StyleMatrixReference(s, value['lnRef'], ctx, 'lnRef');
  $q.flush(s, 0);
  if (value['fillRef'] !== undefined) writeCT_StyleMatrixReference(s, value['fillRef'], ctx, 'fillRef');
  $q.flush(s, 1);
  if (value['effectRef'] !== undefined) writeCT_StyleMatrixReference(s, value['effectRef'], ctx, 'effectRef');
  $q.flush(s, 2);
  if (value['fontRef'] !== undefined) writeCT_FontReference(s, value['fontRef'], ctx, 'fontRef');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SoftEdgesEffect`; the caller supplies its element local name. */
export function writeCT_SoftEdgesEffect(s: XmlSink, value: CT_SoftEdgesEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['rad'] !== undefined) s.attr(null, 'rad', String(value['rad']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_SolidColorFillProperties`; the caller supplies its element local name. */
export function writeCT_SolidColorFillProperties(s: XmlSink, value: CT_SolidColorFillProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SphereCoords`; the caller supplies its element local name. */
export function writeCT_SphereCoords(s: XmlSink, value: CT_SphereCoords, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['lat'] !== undefined) s.attr(null, 'lat', String(value['lat']));
  if (value['lon'] !== undefined) s.attr(null, 'lon', String(value['lon']));
  if (value['rev'] !== undefined) s.attr(null, 'rev', String(value['rev']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_SRgbColor`; the caller supplies its element local name. */
export function writeCT_SRgbColor(s: XmlSink, value: CT_SRgbColor, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_colorTransform of value['colorTransform']) {
    if (v_colorTransform.kind === 'tint') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'tint');
    if (v_colorTransform.kind === 'shade') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'shade');
    if (v_colorTransform.kind === 'comp') writeCT_ComplementTransform(s, v_colorTransform.value, ctx, 'comp');
    if (v_colorTransform.kind === 'inv') writeCT_InverseTransform(s, v_colorTransform.value, ctx, 'inv');
    if (v_colorTransform.kind === 'gray') writeCT_GrayscaleTransform(s, v_colorTransform.value, ctx, 'gray');
    if (v_colorTransform.kind === 'alpha') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'alpha');
    if (v_colorTransform.kind === 'alphaOff') writeCT_FixedPercentage(s, v_colorTransform.value, ctx, 'alphaOff');
    if (v_colorTransform.kind === 'alphaMod') writeCT_PositivePercentage(s, v_colorTransform.value, ctx, 'alphaMod');
    if (v_colorTransform.kind === 'hue') writeCT_PositiveFixedAngle(s, v_colorTransform.value, ctx, 'hue');
    if (v_colorTransform.kind === 'hueOff') writeCT_Angle(s, v_colorTransform.value, ctx, 'hueOff');
    if (v_colorTransform.kind === 'hueMod') writeCT_PositivePercentage(s, v_colorTransform.value, ctx, 'hueMod');
    if (v_colorTransform.kind === 'sat') writeCT_Percentage(s, v_colorTransform.value, ctx, 'sat');
    if (v_colorTransform.kind === 'satOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'satOff');
    if (v_colorTransform.kind === 'satMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'satMod');
    if (v_colorTransform.kind === 'lum') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lum');
    if (v_colorTransform.kind === 'lumOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lumOff');
    if (v_colorTransform.kind === 'lumMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lumMod');
    if (v_colorTransform.kind === 'red') writeCT_Percentage(s, v_colorTransform.value, ctx, 'red');
    if (v_colorTransform.kind === 'redOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'redOff');
    if (v_colorTransform.kind === 'redMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'redMod');
    if (v_colorTransform.kind === 'green') writeCT_Percentage(s, v_colorTransform.value, ctx, 'green');
    if (v_colorTransform.kind === 'greenOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'greenOff');
    if (v_colorTransform.kind === 'greenMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'greenMod');
    if (v_colorTransform.kind === 'blue') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blue');
    if (v_colorTransform.kind === 'blueOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blueOff');
    if (v_colorTransform.kind === 'blueMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blueMod');
    if (v_colorTransform.kind === 'gamma') writeCT_GammaTransform(s, v_colorTransform.value, ctx, 'gamma');
    if (v_colorTransform.kind === 'invGamma') writeCT_InverseGammaTransform(s, v_colorTransform.value, ctx, 'invGamma');
    if (v_colorTransform.kind === '$raw') { s.raw(v_colorTransform.value); }
  }
  s.endElement();
}

/** Write a `CT_StretchInfoProperties`; the caller supplies its element local name. */
export function writeCT_StretchInfoProperties(s: XmlSink, value: CT_StretchInfoProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['fillRect'] !== undefined) writeCT_RelativeRect(s, value['fillRect'], ctx, 'fillRect');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_StyleMatrix`; the caller supplies its element local name. */
export function writeCT_StyleMatrix(s: XmlSink, value: CT_StyleMatrix, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['name'] !== undefined) s.attr(null, 'name', String(value['name']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['fillStyleLst'] !== undefined) writeCT_FillStyleList(s, value['fillStyleLst'], ctx, 'fillStyleLst');
  $q.flush(s, 0);
  if (value['lnStyleLst'] !== undefined) writeCT_LineStyleList(s, value['lnStyleLst'], ctx, 'lnStyleLst');
  $q.flush(s, 1);
  if (value['effectStyleLst'] !== undefined) writeCT_EffectStyleList(s, value['effectStyleLst'], ctx, 'effectStyleLst');
  $q.flush(s, 2);
  if (value['bgFillStyleLst'] !== undefined) writeCT_BackgroundFillStyleList(s, value['bgFillStyleLst'], ctx, 'bgFillStyleLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_StyleMatrixReference`; the caller supplies its element local name. */
export function writeCT_StyleMatrixReference(s: XmlSink, value: CT_StyleMatrixReference, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['idx'] !== undefined) s.attr(null, 'idx', String(value['idx']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_SupplementalFont`; the caller supplies its element local name. */
export function writeCT_SupplementalFont(s: XmlSink, value: CT_SupplementalFont, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['script'] !== undefined) s.attr(null, 'script', String(value['script']));
  if (value['typeface'] !== undefined) s.attr(null, 'typeface', String(value['typeface']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_SystemColor`; the caller supplies its element local name. */
export function writeCT_SystemColor(s: XmlSink, value: CT_SystemColor, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  if (value['lastClr'] !== undefined) s.attr(null, 'lastClr', String(value['lastClr']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_colorTransform of value['colorTransform']) {
    if (v_colorTransform.kind === 'tint') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'tint');
    if (v_colorTransform.kind === 'shade') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'shade');
    if (v_colorTransform.kind === 'comp') writeCT_ComplementTransform(s, v_colorTransform.value, ctx, 'comp');
    if (v_colorTransform.kind === 'inv') writeCT_InverseTransform(s, v_colorTransform.value, ctx, 'inv');
    if (v_colorTransform.kind === 'gray') writeCT_GrayscaleTransform(s, v_colorTransform.value, ctx, 'gray');
    if (v_colorTransform.kind === 'alpha') writeCT_PositiveFixedPercentage(s, v_colorTransform.value, ctx, 'alpha');
    if (v_colorTransform.kind === 'alphaOff') writeCT_FixedPercentage(s, v_colorTransform.value, ctx, 'alphaOff');
    if (v_colorTransform.kind === 'alphaMod') writeCT_PositivePercentage(s, v_colorTransform.value, ctx, 'alphaMod');
    if (v_colorTransform.kind === 'hue') writeCT_PositiveFixedAngle(s, v_colorTransform.value, ctx, 'hue');
    if (v_colorTransform.kind === 'hueOff') writeCT_Angle(s, v_colorTransform.value, ctx, 'hueOff');
    if (v_colorTransform.kind === 'hueMod') writeCT_PositivePercentage(s, v_colorTransform.value, ctx, 'hueMod');
    if (v_colorTransform.kind === 'sat') writeCT_Percentage(s, v_colorTransform.value, ctx, 'sat');
    if (v_colorTransform.kind === 'satOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'satOff');
    if (v_colorTransform.kind === 'satMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'satMod');
    if (v_colorTransform.kind === 'lum') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lum');
    if (v_colorTransform.kind === 'lumOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lumOff');
    if (v_colorTransform.kind === 'lumMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'lumMod');
    if (v_colorTransform.kind === 'red') writeCT_Percentage(s, v_colorTransform.value, ctx, 'red');
    if (v_colorTransform.kind === 'redOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'redOff');
    if (v_colorTransform.kind === 'redMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'redMod');
    if (v_colorTransform.kind === 'green') writeCT_Percentage(s, v_colorTransform.value, ctx, 'green');
    if (v_colorTransform.kind === 'greenOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'greenOff');
    if (v_colorTransform.kind === 'greenMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'greenMod');
    if (v_colorTransform.kind === 'blue') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blue');
    if (v_colorTransform.kind === 'blueOff') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blueOff');
    if (v_colorTransform.kind === 'blueMod') writeCT_Percentage(s, v_colorTransform.value, ctx, 'blueMod');
    if (v_colorTransform.kind === 'gamma') writeCT_GammaTransform(s, v_colorTransform.value, ctx, 'gamma');
    if (v_colorTransform.kind === 'invGamma') writeCT_InverseGammaTransform(s, v_colorTransform.value, ctx, 'invGamma');
    if (v_colorTransform.kind === '$raw') { s.raw(v_colorTransform.value); }
  }
  s.endElement();
}

/** Write a `CT_Table`; the caller supplies its element local name. */
export function writeCT_Table(s: XmlSink, value: CT_Table, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['tblPr'] !== undefined) writeCT_TableProperties(s, value['tblPr'], ctx, 'tblPr');
  $q.flush(s, 0);
  if (value['tblGrid'] !== undefined) writeCT_TableGrid(s, value['tblGrid'], ctx, 'tblGrid');
  $q.flush(s, 1);
  for (let idx = 0; idx < value['tr'].length; idx++) {
    const v_tr = value['tr'][idx]!;
    writeCT_TableRow(s, v_tr, ctx, 'tr');
    $q.flush(s, 2, idx);
  }
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TableBackgroundStyle`; the caller supplies its element local name. */
export function writeCT_TableBackgroundStyle(s: XmlSink, value: CT_TableBackgroundStyle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['themeableFillStyle'] !== undefined) {
    if (value['themeableFillStyle'].kind === 'fill') writeCT_FillProperties(s, value['themeableFillStyle'].value, ctx, 'fill');
    if (value['themeableFillStyle'].kind === 'fillRef') writeCT_StyleMatrixReference(s, value['themeableFillStyle'].value, ctx, 'fillRef');
  }
  $q.flush(s, 0);
  if (value['themeableEffectStyle'] !== undefined) {
    if (value['themeableEffectStyle'].kind === 'effect') writeCT_EffectProperties(s, value['themeableEffectStyle'].value, ctx, 'effect');
    if (value['themeableEffectStyle'].kind === 'effectRef') writeCT_StyleMatrixReference(s, value['themeableEffectStyle'].value, ctx, 'effectRef');
  }
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TableCell`; the caller supplies its element local name. */
export function writeCT_TableCell(s: XmlSink, value: CT_TableCell, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['rowSpan'] !== undefined) s.attr(null, 'rowSpan', String(value['rowSpan']));
  if (value['gridSpan'] !== undefined) s.attr(null, 'gridSpan', String(value['gridSpan']));
  if (value['hMerge'] !== undefined) s.attr(null, 'hMerge', String(value['hMerge']));
  if (value['vMerge'] !== undefined) s.attr(null, 'vMerge', String(value['vMerge']));
  if (value['id'] !== undefined) s.attr(null, 'id', String(value['id']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['txBody'] !== undefined) writeCT_TextBody(s, value['txBody'], ctx, 'txBody');
  $q.flush(s, 0);
  if (value['tcPr'] !== undefined) writeCT_TableCellProperties(s, value['tcPr'], ctx, 'tcPr');
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TableCellBorderStyle`; the caller supplies its element local name. */
export function writeCT_TableCellBorderStyle(s: XmlSink, value: CT_TableCellBorderStyle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['left'] !== undefined) writeCT_ThemeableLineStyle(s, value['left'], ctx, 'left');
  $q.flush(s, 0);
  if (value['right'] !== undefined) writeCT_ThemeableLineStyle(s, value['right'], ctx, 'right');
  $q.flush(s, 1);
  if (value['top'] !== undefined) writeCT_ThemeableLineStyle(s, value['top'], ctx, 'top');
  $q.flush(s, 2);
  if (value['bottom'] !== undefined) writeCT_ThemeableLineStyle(s, value['bottom'], ctx, 'bottom');
  $q.flush(s, 3);
  if (value['insideH'] !== undefined) writeCT_ThemeableLineStyle(s, value['insideH'], ctx, 'insideH');
  $q.flush(s, 4);
  if (value['insideV'] !== undefined) writeCT_ThemeableLineStyle(s, value['insideV'], ctx, 'insideV');
  $q.flush(s, 5);
  if (value['tl2br'] !== undefined) writeCT_ThemeableLineStyle(s, value['tl2br'], ctx, 'tl2br');
  $q.flush(s, 6);
  if (value['tr2bl'] !== undefined) writeCT_ThemeableLineStyle(s, value['tr2bl'], ctx, 'tr2bl');
  $q.flush(s, 7);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 8);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TableCellProperties`; the caller supplies its element local name. */
export function writeCT_TableCellProperties(s: XmlSink, value: CT_TableCellProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['marL'] !== undefined) s.attr(null, 'marL', String(value['marL']));
  if (value['marR'] !== undefined) s.attr(null, 'marR', String(value['marR']));
  if (value['marT'] !== undefined) s.attr(null, 'marT', String(value['marT']));
  if (value['marB'] !== undefined) s.attr(null, 'marB', String(value['marB']));
  if (value['vert'] !== undefined) s.attr(null, 'vert', String(value['vert']));
  if (value['anchor'] !== undefined) s.attr(null, 'anchor', String(value['anchor']));
  if (value['anchorCtr'] !== undefined) s.attr(null, 'anchorCtr', String(value['anchorCtr']));
  if (value['horzOverflow'] !== undefined) s.attr(null, 'horzOverflow', String(value['horzOverflow']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['lnL'] !== undefined) writeCT_LineProperties(s, value['lnL'], ctx, 'lnL');
  $q.flush(s, 0);
  if (value['lnR'] !== undefined) writeCT_LineProperties(s, value['lnR'], ctx, 'lnR');
  $q.flush(s, 1);
  if (value['lnT'] !== undefined) writeCT_LineProperties(s, value['lnT'], ctx, 'lnT');
  $q.flush(s, 2);
  if (value['lnB'] !== undefined) writeCT_LineProperties(s, value['lnB'], ctx, 'lnB');
  $q.flush(s, 3);
  if (value['lnTlToBr'] !== undefined) writeCT_LineProperties(s, value['lnTlToBr'], ctx, 'lnTlToBr');
  $q.flush(s, 4);
  if (value['lnBlToTr'] !== undefined) writeCT_LineProperties(s, value['lnBlToTr'], ctx, 'lnBlToTr');
  $q.flush(s, 5);
  if (value['cell3D'] !== undefined) writeCT_Cell3D(s, value['cell3D'], ctx, 'cell3D');
  $q.flush(s, 6);
  if (value['fillProperties'] !== undefined) {
    if (value['fillProperties'].kind === 'noFill') writeCT_NoFillProperties(s, value['fillProperties'].value, ctx, 'noFill');
    if (value['fillProperties'].kind === 'solidFill') writeCT_SolidColorFillProperties(s, value['fillProperties'].value, ctx, 'solidFill');
    if (value['fillProperties'].kind === 'gradFill') writeCT_GradientFillProperties(s, value['fillProperties'].value, ctx, 'gradFill');
    if (value['fillProperties'].kind === 'blipFill') writeCT_BlipFillProperties(s, value['fillProperties'].value, ctx, 'blipFill');
    if (value['fillProperties'].kind === 'pattFill') writeCT_PatternFillProperties(s, value['fillProperties'].value, ctx, 'pattFill');
    if (value['fillProperties'].kind === 'grpFill') writeCT_GroupFillProperties(s, value['fillProperties'].value, ctx, 'grpFill');
  }
  $q.flush(s, 7);
  if (value['headers'] !== undefined) writeCT_Headers(s, value['headers'], ctx, 'headers');
  $q.flush(s, 8);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 9);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TableCol`; the caller supplies its element local name. */
export function writeCT_TableCol(s: XmlSink, value: CT_TableCol, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['w'] !== undefined) s.attr(null, 'w', String(value['w']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TableGrid`; the caller supplies its element local name. */
export function writeCT_TableGrid(s: XmlSink, value: CT_TableGrid, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['gridCol'].length; idx++) {
    const v_gridCol = value['gridCol'][idx]!;
    writeCT_TableCol(s, v_gridCol, ctx, 'gridCol');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TablePartStyle`; the caller supplies its element local name. */
export function writeCT_TablePartStyle(s: XmlSink, value: CT_TablePartStyle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['tcTxStyle'] !== undefined) writeCT_TableStyleTextStyle(s, value['tcTxStyle'], ctx, 'tcTxStyle');
  $q.flush(s, 0);
  if (value['tcStyle'] !== undefined) writeCT_TableStyleCellStyle(s, value['tcStyle'], ctx, 'tcStyle');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TableProperties`; the caller supplies its element local name. */
export function writeCT_TableProperties(s: XmlSink, value: CT_TableProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['rtl'] !== undefined) s.attr(null, 'rtl', String(value['rtl']));
  if (value['firstRow'] !== undefined) s.attr(null, 'firstRow', String(value['firstRow']));
  if (value['firstCol'] !== undefined) s.attr(null, 'firstCol', String(value['firstCol']));
  if (value['lastRow'] !== undefined) s.attr(null, 'lastRow', String(value['lastRow']));
  if (value['lastCol'] !== undefined) s.attr(null, 'lastCol', String(value['lastCol']));
  if (value['bandRow'] !== undefined) s.attr(null, 'bandRow', String(value['bandRow']));
  if (value['bandCol'] !== undefined) s.attr(null, 'bandCol', String(value['bandCol']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['fillProperties'] !== undefined) {
    if (value['fillProperties'].kind === 'noFill') writeCT_NoFillProperties(s, value['fillProperties'].value, ctx, 'noFill');
    if (value['fillProperties'].kind === 'solidFill') writeCT_SolidColorFillProperties(s, value['fillProperties'].value, ctx, 'solidFill');
    if (value['fillProperties'].kind === 'gradFill') writeCT_GradientFillProperties(s, value['fillProperties'].value, ctx, 'gradFill');
    if (value['fillProperties'].kind === 'blipFill') writeCT_BlipFillProperties(s, value['fillProperties'].value, ctx, 'blipFill');
    if (value['fillProperties'].kind === 'pattFill') writeCT_PatternFillProperties(s, value['fillProperties'].value, ctx, 'pattFill');
    if (value['fillProperties'].kind === 'grpFill') writeCT_GroupFillProperties(s, value['fillProperties'].value, ctx, 'grpFill');
  }
  $q.flush(s, 0);
  if (value['effectProperties'] !== undefined) {
    if (value['effectProperties'].kind === 'effectLst') writeCT_EffectList(s, value['effectProperties'].value, ctx, 'effectLst');
    if (value['effectProperties'].kind === 'effectDag') writeCT_EffectContainer(s, value['effectProperties'].value, ctx, 'effectDag');
  }
  $q.flush(s, 1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'tableStyle') writeCT_TableStyle(s, value['content'].value, ctx, 'tableStyle');
    if (value['content'].kind === 'tableStyleId') s.text(String(value['content'].value));
  }
  $q.flush(s, 2);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 3);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TableRow`; the caller supplies its element local name. */
export function writeCT_TableRow(s: XmlSink, value: CT_TableRow, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['h'] !== undefined) s.attr(null, 'h', String(value['h']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['tc'].length; idx++) {
    const v_tc = value['tc'][idx]!;
    writeCT_TableCell(s, v_tc, ctx, 'tc');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TableStyle`; the caller supplies its element local name. */
export function writeCT_TableStyle(s: XmlSink, value: CT_TableStyle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['styleId'] !== undefined) s.attr(null, 'styleId', String(value['styleId']));
  if (value['styleName'] !== undefined) s.attr(null, 'styleName', String(value['styleName']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['tblBg'] !== undefined) writeCT_TableBackgroundStyle(s, value['tblBg'], ctx, 'tblBg');
  $q.flush(s, 0);
  if (value['wholeTbl'] !== undefined) writeCT_TablePartStyle(s, value['wholeTbl'], ctx, 'wholeTbl');
  $q.flush(s, 1);
  if (value['band1H'] !== undefined) writeCT_TablePartStyle(s, value['band1H'], ctx, 'band1H');
  $q.flush(s, 2);
  if (value['band2H'] !== undefined) writeCT_TablePartStyle(s, value['band2H'], ctx, 'band2H');
  $q.flush(s, 3);
  if (value['band1V'] !== undefined) writeCT_TablePartStyle(s, value['band1V'], ctx, 'band1V');
  $q.flush(s, 4);
  if (value['band2V'] !== undefined) writeCT_TablePartStyle(s, value['band2V'], ctx, 'band2V');
  $q.flush(s, 5);
  if (value['lastCol'] !== undefined) writeCT_TablePartStyle(s, value['lastCol'], ctx, 'lastCol');
  $q.flush(s, 6);
  if (value['firstCol'] !== undefined) writeCT_TablePartStyle(s, value['firstCol'], ctx, 'firstCol');
  $q.flush(s, 7);
  if (value['lastRow'] !== undefined) writeCT_TablePartStyle(s, value['lastRow'], ctx, 'lastRow');
  $q.flush(s, 8);
  if (value['seCell'] !== undefined) writeCT_TablePartStyle(s, value['seCell'], ctx, 'seCell');
  $q.flush(s, 9);
  if (value['swCell'] !== undefined) writeCT_TablePartStyle(s, value['swCell'], ctx, 'swCell');
  $q.flush(s, 10);
  if (value['firstRow'] !== undefined) writeCT_TablePartStyle(s, value['firstRow'], ctx, 'firstRow');
  $q.flush(s, 11);
  if (value['neCell'] !== undefined) writeCT_TablePartStyle(s, value['neCell'], ctx, 'neCell');
  $q.flush(s, 12);
  if (value['nwCell'] !== undefined) writeCT_TablePartStyle(s, value['nwCell'], ctx, 'nwCell');
  $q.flush(s, 13);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 14);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TableStyleCellStyle`; the caller supplies its element local name. */
export function writeCT_TableStyleCellStyle(s: XmlSink, value: CT_TableStyleCellStyle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['tcBdr'] !== undefined) writeCT_TableCellBorderStyle(s, value['tcBdr'], ctx, 'tcBdr');
  $q.flush(s, 0);
  if (value['themeableFillStyle'] !== undefined) {
    if (value['themeableFillStyle'].kind === 'fill') writeCT_FillProperties(s, value['themeableFillStyle'].value, ctx, 'fill');
    if (value['themeableFillStyle'].kind === 'fillRef') writeCT_StyleMatrixReference(s, value['themeableFillStyle'].value, ctx, 'fillRef');
  }
  $q.flush(s, 1);
  if (value['cell3D'] !== undefined) writeCT_Cell3D(s, value['cell3D'], ctx, 'cell3D');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TableStyleList`; the caller supplies its element local name. */
export function writeCT_TableStyleList(s: XmlSink, value: CT_TableStyleList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['def'] !== undefined) s.attr(null, 'def', String(value['def']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['tblStyle'].length; idx++) {
    const v_tblStyle = value['tblStyle'][idx]!;
    writeCT_TableStyle(s, v_tblStyle, ctx, 'tblStyle');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TableStyleTextStyle`; the caller supplies its element local name. */
export function writeCT_TableStyleTextStyle(s: XmlSink, value: CT_TableStyleTextStyle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['b'] !== undefined) s.attr(null, 'b', String(value['b']));
  if (value['i'] !== undefined) s.attr(null, 'i', String(value['i']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['themeableFontStyles'] !== undefined) {
    if (value['themeableFontStyles'].kind === 'font') writeCT_FontCollection(s, value['themeableFontStyles'].value, ctx, 'font');
    if (value['themeableFontStyles'].kind === 'fontRef') writeCT_FontReference(s, value['themeableFontStyles'].value, ctx, 'fontRef');
  }
  $q.flush(s, 0);
  if (value['colorChoice'] !== undefined) {
    if (value['colorChoice'].kind === 'scrgbClr') writeCT_ScRgbColor(s, value['colorChoice'].value, ctx, 'scrgbClr');
    if (value['colorChoice'].kind === 'srgbClr') writeCT_SRgbColor(s, value['colorChoice'].value, ctx, 'srgbClr');
    if (value['colorChoice'].kind === 'hslClr') writeCT_HslColor(s, value['colorChoice'].value, ctx, 'hslClr');
    if (value['colorChoice'].kind === 'sysClr') writeCT_SystemColor(s, value['colorChoice'].value, ctx, 'sysClr');
    if (value['colorChoice'].kind === 'schemeClr') writeCT_SchemeColor(s, value['colorChoice'].value, ctx, 'schemeClr');
    if (value['colorChoice'].kind === 'prstClr') writeCT_PresetColor(s, value['colorChoice'].value, ctx, 'prstClr');
  }
  $q.flush(s, 1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TextAutonumberBullet`; the caller supplies its element local name. */
export function writeCT_TextAutonumberBullet(s: XmlSink, value: CT_TextAutonumberBullet, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  if (value['startAt'] !== undefined) s.attr(null, 'startAt', String(value['startAt']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextBlipBullet`; the caller supplies its element local name. */
export function writeCT_TextBlipBullet(s: XmlSink, value: CT_TextBlipBullet, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['blip'] !== undefined) writeCT_Blip(s, value['blip'], ctx, 'blip');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TextBody`; the caller supplies its element local name. */
export function writeCT_TextBody(s: XmlSink, value: CT_TextBody, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['bodyPr'] !== undefined) writeCT_TextBodyProperties(s, value['bodyPr'], ctx, 'bodyPr');
  $q.flush(s, 0);
  if (value['lstStyle'] !== undefined) writeCT_TextListStyle(s, value['lstStyle'], ctx, 'lstStyle');
  $q.flush(s, 1);
  for (let idx = 0; idx < value['p'].length; idx++) {
    const v_p = value['p'][idx]!;
    writeCT_TextParagraph(s, v_p, ctx, 'p');
    $q.flush(s, 2, idx);
  }
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TextBodyProperties`; the caller supplies its element local name. */
export function writeCT_TextBodyProperties(s: XmlSink, value: CT_TextBodyProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['rot'] !== undefined) s.attr(null, 'rot', String(value['rot']));
  if (value['spcFirstLastPara'] !== undefined) s.attr(null, 'spcFirstLastPara', String(value['spcFirstLastPara']));
  if (value['vertOverflow'] !== undefined) s.attr(null, 'vertOverflow', String(value['vertOverflow']));
  if (value['horzOverflow'] !== undefined) s.attr(null, 'horzOverflow', String(value['horzOverflow']));
  if (value['vert'] !== undefined) s.attr(null, 'vert', String(value['vert']));
  if (value['wrap'] !== undefined) s.attr(null, 'wrap', String(value['wrap']));
  if (value['lIns'] !== undefined) s.attr(null, 'lIns', String(value['lIns']));
  if (value['tIns'] !== undefined) s.attr(null, 'tIns', String(value['tIns']));
  if (value['rIns'] !== undefined) s.attr(null, 'rIns', String(value['rIns']));
  if (value['bIns'] !== undefined) s.attr(null, 'bIns', String(value['bIns']));
  if (value['numCol'] !== undefined) s.attr(null, 'numCol', String(value['numCol']));
  if (value['spcCol'] !== undefined) s.attr(null, 'spcCol', String(value['spcCol']));
  if (value['rtlCol'] !== undefined) s.attr(null, 'rtlCol', String(value['rtlCol']));
  if (value['fromWordArt'] !== undefined) s.attr(null, 'fromWordArt', String(value['fromWordArt']));
  if (value['anchor'] !== undefined) s.attr(null, 'anchor', String(value['anchor']));
  if (value['anchorCtr'] !== undefined) s.attr(null, 'anchorCtr', String(value['anchorCtr']));
  if (value['forceAA'] !== undefined) s.attr(null, 'forceAA', String(value['forceAA']));
  if (value['upright'] !== undefined) s.attr(null, 'upright', String(value['upright']));
  if (value['compatLnSpc'] !== undefined) s.attr(null, 'compatLnSpc', String(value['compatLnSpc']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['prstTxWarp'] !== undefined) writeCT_PresetTextShape(s, value['prstTxWarp'], ctx, 'prstTxWarp');
  $q.flush(s, 0);
  if (value['textAutofit'] !== undefined) {
    if (value['textAutofit'].kind === 'noAutofit') writeCT_TextNoAutofit(s, value['textAutofit'].value, ctx, 'noAutofit');
    if (value['textAutofit'].kind === 'normAutofit') writeCT_TextNormalAutofit(s, value['textAutofit'].value, ctx, 'normAutofit');
    if (value['textAutofit'].kind === 'spAutoFit') writeCT_TextShapeAutofit(s, value['textAutofit'].value, ctx, 'spAutoFit');
  }
  $q.flush(s, 1);
  if (value['scene3d'] !== undefined) writeCT_Scene3D(s, value['scene3d'], ctx, 'scene3d');
  $q.flush(s, 2);
  if (value['text3D'] !== undefined) {
    if (value['text3D'].kind === 'sp3d') writeCT_Shape3D(s, value['text3D'].value, ctx, 'sp3d');
    if (value['text3D'].kind === 'flatTx') writeCT_FlatText(s, value['text3D'].value, ctx, 'flatTx');
  }
  $q.flush(s, 3);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 4);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TextBulletColorFollowText`; the caller supplies its element local name. */
export function writeCT_TextBulletColorFollowText(s: XmlSink, value: CT_TextBulletColorFollowText, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextBulletSizeFollowText`; the caller supplies its element local name. */
export function writeCT_TextBulletSizeFollowText(s: XmlSink, value: CT_TextBulletSizeFollowText, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextBulletSizePercent`; the caller supplies its element local name. */
export function writeCT_TextBulletSizePercent(s: XmlSink, value: CT_TextBulletSizePercent, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextBulletSizePoint`; the caller supplies its element local name. */
export function writeCT_TextBulletSizePoint(s: XmlSink, value: CT_TextBulletSizePoint, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextBulletTypefaceFollowText`; the caller supplies its element local name. */
export function writeCT_TextBulletTypefaceFollowText(s: XmlSink, value: CT_TextBulletTypefaceFollowText, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextCharacterProperties`; the caller supplies its element local name. */
export function writeCT_TextCharacterProperties(s: XmlSink, value: CT_TextCharacterProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['kumimoji'] !== undefined) s.attr(null, 'kumimoji', String(value['kumimoji']));
  if (value['lang'] !== undefined) s.attr(null, 'lang', String(value['lang']));
  if (value['altLang'] !== undefined) s.attr(null, 'altLang', String(value['altLang']));
  if (value['sz'] !== undefined) s.attr(null, 'sz', String(value['sz']));
  if (value['b'] !== undefined) s.attr(null, 'b', String(value['b']));
  if (value['i'] !== undefined) s.attr(null, 'i', String(value['i']));
  if (value['u'] !== undefined) s.attr(null, 'u', String(value['u']));
  if (value['strike'] !== undefined) s.attr(null, 'strike', String(value['strike']));
  if (value['kern'] !== undefined) s.attr(null, 'kern', String(value['kern']));
  if (value['cap'] !== undefined) s.attr(null, 'cap', String(value['cap']));
  if (value['spc'] !== undefined) s.attr(null, 'spc', String(value['spc']));
  if (value['normalizeH'] !== undefined) s.attr(null, 'normalizeH', String(value['normalizeH']));
  if (value['baseline'] !== undefined) s.attr(null, 'baseline', String(value['baseline']));
  if (value['noProof'] !== undefined) s.attr(null, 'noProof', String(value['noProof']));
  if (value['dirty'] !== undefined) s.attr(null, 'dirty', String(value['dirty']));
  if (value['err'] !== undefined) s.attr(null, 'err', String(value['err']));
  if (value['smtClean'] !== undefined) s.attr(null, 'smtClean', String(value['smtClean']));
  if (value['smtId'] !== undefined) s.attr(null, 'smtId', String(value['smtId']));
  if (value['bmk'] !== undefined) s.attr(null, 'bmk', String(value['bmk']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['ln'] !== undefined) writeCT_LineProperties(s, value['ln'], ctx, 'ln');
  $q.flush(s, 0);
  if (value['fillProperties'] !== undefined) {
    if (value['fillProperties'].kind === 'noFill') writeCT_NoFillProperties(s, value['fillProperties'].value, ctx, 'noFill');
    if (value['fillProperties'].kind === 'solidFill') writeCT_SolidColorFillProperties(s, value['fillProperties'].value, ctx, 'solidFill');
    if (value['fillProperties'].kind === 'gradFill') writeCT_GradientFillProperties(s, value['fillProperties'].value, ctx, 'gradFill');
    if (value['fillProperties'].kind === 'blipFill') writeCT_BlipFillProperties(s, value['fillProperties'].value, ctx, 'blipFill');
    if (value['fillProperties'].kind === 'pattFill') writeCT_PatternFillProperties(s, value['fillProperties'].value, ctx, 'pattFill');
    if (value['fillProperties'].kind === 'grpFill') writeCT_GroupFillProperties(s, value['fillProperties'].value, ctx, 'grpFill');
  }
  $q.flush(s, 1);
  if (value['effectProperties'] !== undefined) {
    if (value['effectProperties'].kind === 'effectLst') writeCT_EffectList(s, value['effectProperties'].value, ctx, 'effectLst');
    if (value['effectProperties'].kind === 'effectDag') writeCT_EffectContainer(s, value['effectProperties'].value, ctx, 'effectDag');
  }
  $q.flush(s, 2);
  if (value['highlight'] !== undefined) writeCT_Color(s, value['highlight'], ctx, 'highlight');
  $q.flush(s, 3);
  if (value['textUnderlineLine'] !== undefined) {
    if (value['textUnderlineLine'].kind === 'uLnTx') writeCT_TextUnderlineLineFollowText(s, value['textUnderlineLine'].value, ctx, 'uLnTx');
    if (value['textUnderlineLine'].kind === 'uLn') writeCT_LineProperties(s, value['textUnderlineLine'].value, ctx, 'uLn');
  }
  $q.flush(s, 4);
  if (value['textUnderlineFill'] !== undefined) {
    if (value['textUnderlineFill'].kind === 'uFillTx') writeCT_TextUnderlineFillFollowText(s, value['textUnderlineFill'].value, ctx, 'uFillTx');
    if (value['textUnderlineFill'].kind === 'uFill') writeCT_TextUnderlineFillGroupWrapper(s, value['textUnderlineFill'].value, ctx, 'uFill');
  }
  $q.flush(s, 5);
  if (value['latin'] !== undefined) writeCT_TextFont(s, value['latin'], ctx, 'latin');
  $q.flush(s, 6);
  if (value['ea'] !== undefined) writeCT_TextFont(s, value['ea'], ctx, 'ea');
  $q.flush(s, 7);
  if (value['cs'] !== undefined) writeCT_TextFont(s, value['cs'], ctx, 'cs');
  $q.flush(s, 8);
  if (value['sym'] !== undefined) writeCT_TextFont(s, value['sym'], ctx, 'sym');
  $q.flush(s, 9);
  if (value['hlinkClick'] !== undefined) writeCT_Hyperlink(s, value['hlinkClick'], ctx, 'hlinkClick');
  $q.flush(s, 10);
  if (value['hlinkMouseOver'] !== undefined) writeCT_Hyperlink(s, value['hlinkMouseOver'], ctx, 'hlinkMouseOver');
  $q.flush(s, 11);
  if (value['rtl'] !== undefined) writeCT_Boolean(s, value['rtl'], ctx, 'rtl');
  $q.flush(s, 12);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 13);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TextCharBullet`; the caller supplies its element local name. */
export function writeCT_TextCharBullet(s: XmlSink, value: CT_TextCharBullet, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['char'] !== undefined) s.attr(null, 'char', String(value['char']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextField`; the caller supplies its element local name. */
export function writeCT_TextField(s: XmlSink, value: CT_TextField, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['id'] !== undefined) s.attr(null, 'id', String(value['id']));
  if (value['type'] !== undefined) s.attr(null, 'type', String(value['type']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['rPr'] !== undefined) writeCT_TextCharacterProperties(s, value['rPr'], ctx, 'rPr');
  $q.flush(s, 0);
  if (value['pPr'] !== undefined) writeCT_TextParagraphProperties(s, value['pPr'], ctx, 'pPr');
  $q.flush(s, 1);
  if (value['t'] !== undefined) s.text(String(value['t']));
  $q.flush(s, 2);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TextFont`; the caller supplies its element local name. */
export function writeCT_TextFont(s: XmlSink, value: CT_TextFont, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['typeface'] !== undefined) s.attr(null, 'typeface', String(value['typeface']));
  if (value['panose'] !== undefined) s.attr(null, 'panose', String(value['panose']));
  if (value['pitchFamily'] !== undefined) s.attr(null, 'pitchFamily', String(value['pitchFamily']));
  if (value['charset'] !== undefined) s.attr(null, 'charset', String(value['charset']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextLineBreak`; the caller supplies its element local name. */
export function writeCT_TextLineBreak(s: XmlSink, value: CT_TextLineBreak, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['rPr'] !== undefined) writeCT_TextCharacterProperties(s, value['rPr'], ctx, 'rPr');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TextListStyle`; the caller supplies its element local name. */
export function writeCT_TextListStyle(s: XmlSink, value: CT_TextListStyle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['defPPr'] !== undefined) writeCT_TextParagraphProperties(s, value['defPPr'], ctx, 'defPPr');
  $q.flush(s, 0);
  if (value['lvl1pPr'] !== undefined) writeCT_TextParagraphProperties(s, value['lvl1pPr'], ctx, 'lvl1pPr');
  $q.flush(s, 1);
  if (value['lvl2pPr'] !== undefined) writeCT_TextParagraphProperties(s, value['lvl2pPr'], ctx, 'lvl2pPr');
  $q.flush(s, 2);
  if (value['lvl3pPr'] !== undefined) writeCT_TextParagraphProperties(s, value['lvl3pPr'], ctx, 'lvl3pPr');
  $q.flush(s, 3);
  if (value['lvl4pPr'] !== undefined) writeCT_TextParagraphProperties(s, value['lvl4pPr'], ctx, 'lvl4pPr');
  $q.flush(s, 4);
  if (value['lvl5pPr'] !== undefined) writeCT_TextParagraphProperties(s, value['lvl5pPr'], ctx, 'lvl5pPr');
  $q.flush(s, 5);
  if (value['lvl6pPr'] !== undefined) writeCT_TextParagraphProperties(s, value['lvl6pPr'], ctx, 'lvl6pPr');
  $q.flush(s, 6);
  if (value['lvl7pPr'] !== undefined) writeCT_TextParagraphProperties(s, value['lvl7pPr'], ctx, 'lvl7pPr');
  $q.flush(s, 7);
  if (value['lvl8pPr'] !== undefined) writeCT_TextParagraphProperties(s, value['lvl8pPr'], ctx, 'lvl8pPr');
  $q.flush(s, 8);
  if (value['lvl9pPr'] !== undefined) writeCT_TextParagraphProperties(s, value['lvl9pPr'], ctx, 'lvl9pPr');
  $q.flush(s, 9);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 10);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TextNoAutofit`; the caller supplies its element local name. */
export function writeCT_TextNoAutofit(s: XmlSink, value: CT_TextNoAutofit, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextNoBullet`; the caller supplies its element local name. */
export function writeCT_TextNoBullet(s: XmlSink, value: CT_TextNoBullet, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextNormalAutofit`; the caller supplies its element local name. */
export function writeCT_TextNormalAutofit(s: XmlSink, value: CT_TextNormalAutofit, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['fontScale'] !== undefined) s.attr(null, 'fontScale', String(value['fontScale']));
  if (value['lnSpcReduction'] !== undefined) s.attr(null, 'lnSpcReduction', String(value['lnSpcReduction']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextParagraph`; the caller supplies its element local name. */
export function writeCT_TextParagraph(s: XmlSink, value: CT_TextParagraph, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  if (value['pPr'] !== undefined) writeCT_TextParagraphProperties(s, value['pPr'], ctx, 'pPr');
  for (const v_textRun of value['textRun']) {
    if (v_textRun.kind === 'r') writeCT_RegularTextRun(s, v_textRun.value, ctx, 'r');
    if (v_textRun.kind === 'br') writeCT_TextLineBreak(s, v_textRun.value, ctx, 'br');
    if (v_textRun.kind === 'fld') writeCT_TextField(s, v_textRun.value, ctx, 'fld');
    if (v_textRun.kind === '$raw') { s.raw(v_textRun.value); }
  }
  if (value['endParaRPr'] !== undefined) writeCT_TextCharacterProperties(s, value['endParaRPr'], ctx, 'endParaRPr');
  s.endElement();
}

/** Write a `CT_TextParagraphProperties`; the caller supplies its element local name. */
export function writeCT_TextParagraphProperties(s: XmlSink, value: CT_TextParagraphProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['marL'] !== undefined) s.attr(null, 'marL', String(value['marL']));
  if (value['marR'] !== undefined) s.attr(null, 'marR', String(value['marR']));
  if (value['lvl'] !== undefined) s.attr(null, 'lvl', String(value['lvl']));
  if (value['indent'] !== undefined) s.attr(null, 'indent', String(value['indent']));
  if (value['algn'] !== undefined) s.attr(null, 'algn', String(value['algn']));
  if (value['defTabSz'] !== undefined) s.attr(null, 'defTabSz', String(value['defTabSz']));
  if (value['rtl'] !== undefined) s.attr(null, 'rtl', String(value['rtl']));
  if (value['eaLnBrk'] !== undefined) s.attr(null, 'eaLnBrk', String(value['eaLnBrk']));
  if (value['fontAlgn'] !== undefined) s.attr(null, 'fontAlgn', String(value['fontAlgn']));
  if (value['latinLnBrk'] !== undefined) s.attr(null, 'latinLnBrk', String(value['latinLnBrk']));
  if (value['hangingPunct'] !== undefined) s.attr(null, 'hangingPunct', String(value['hangingPunct']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['lnSpc'] !== undefined) writeCT_TextSpacing(s, value['lnSpc'], ctx, 'lnSpc');
  $q.flush(s, 0);
  if (value['spcBef'] !== undefined) writeCT_TextSpacing(s, value['spcBef'], ctx, 'spcBef');
  $q.flush(s, 1);
  if (value['spcAft'] !== undefined) writeCT_TextSpacing(s, value['spcAft'], ctx, 'spcAft');
  $q.flush(s, 2);
  if (value['textBulletColor'] !== undefined) {
    if (value['textBulletColor'].kind === 'buClrTx') writeCT_TextBulletColorFollowText(s, value['textBulletColor'].value, ctx, 'buClrTx');
    if (value['textBulletColor'].kind === 'buClr') writeCT_Color(s, value['textBulletColor'].value, ctx, 'buClr');
  }
  $q.flush(s, 3);
  if (value['textBulletSize'] !== undefined) {
    if (value['textBulletSize'].kind === 'buSzTx') writeCT_TextBulletSizeFollowText(s, value['textBulletSize'].value, ctx, 'buSzTx');
    if (value['textBulletSize'].kind === 'buSzPct') writeCT_TextBulletSizePercent(s, value['textBulletSize'].value, ctx, 'buSzPct');
    if (value['textBulletSize'].kind === 'buSzPts') writeCT_TextBulletSizePoint(s, value['textBulletSize'].value, ctx, 'buSzPts');
  }
  $q.flush(s, 4);
  if (value['textBulletTypeface'] !== undefined) {
    if (value['textBulletTypeface'].kind === 'buFontTx') writeCT_TextBulletTypefaceFollowText(s, value['textBulletTypeface'].value, ctx, 'buFontTx');
    if (value['textBulletTypeface'].kind === 'buFont') writeCT_TextFont(s, value['textBulletTypeface'].value, ctx, 'buFont');
  }
  $q.flush(s, 5);
  if (value['textBullet'] !== undefined) {
    if (value['textBullet'].kind === 'buNone') writeCT_TextNoBullet(s, value['textBullet'].value, ctx, 'buNone');
    if (value['textBullet'].kind === 'buAutoNum') writeCT_TextAutonumberBullet(s, value['textBullet'].value, ctx, 'buAutoNum');
    if (value['textBullet'].kind === 'buChar') writeCT_TextCharBullet(s, value['textBullet'].value, ctx, 'buChar');
    if (value['textBullet'].kind === 'buBlip') writeCT_TextBlipBullet(s, value['textBullet'].value, ctx, 'buBlip');
  }
  $q.flush(s, 6);
  if (value['tabLst'] !== undefined) writeCT_TextTabStopList(s, value['tabLst'], ctx, 'tabLst');
  $q.flush(s, 7);
  if (value['defRPr'] !== undefined) writeCT_TextCharacterProperties(s, value['defRPr'], ctx, 'defRPr');
  $q.flush(s, 8);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 9);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TextShapeAutofit`; the caller supplies its element local name. */
export function writeCT_TextShapeAutofit(s: XmlSink, value: CT_TextShapeAutofit, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextSpacing`; the caller supplies its element local name. */
export function writeCT_TextSpacing(s: XmlSink, value: CT_TextSpacing, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'spcPct') writeCT_TextSpacingPercent(s, value['content'].value, ctx, 'spcPct');
    if (value['content'].kind === 'spcPts') writeCT_TextSpacingPoint(s, value['content'].value, ctx, 'spcPts');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TextSpacingPercent`; the caller supplies its element local name. */
export function writeCT_TextSpacingPercent(s: XmlSink, value: CT_TextSpacingPercent, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextSpacingPoint`; the caller supplies its element local name. */
export function writeCT_TextSpacingPoint(s: XmlSink, value: CT_TextSpacingPoint, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['val'] !== undefined) s.attr(null, 'val', String(value['val']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextTabStop`; the caller supplies its element local name. */
export function writeCT_TextTabStop(s: XmlSink, value: CT_TextTabStop, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['pos'] !== undefined) s.attr(null, 'pos', String(value['pos']));
  if (value['algn'] !== undefined) s.attr(null, 'algn', String(value['algn']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextTabStopList`; the caller supplies its element local name. */
export function writeCT_TextTabStopList(s: XmlSink, value: CT_TextTabStopList, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  for (let idx = 0; idx < value['tab'].length; idx++) {
    const v_tab = value['tab'][idx]!;
    writeCT_TextTabStop(s, v_tab, ctx, 'tab');
    $q.flush(s, 0, idx);
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TextUnderlineFillFollowText`; the caller supplies its element local name. */
export function writeCT_TextUnderlineFillFollowText(s: XmlSink, value: CT_TextUnderlineFillFollowText, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TextUnderlineFillGroupWrapper`; the caller supplies its element local name. */
export function writeCT_TextUnderlineFillGroupWrapper(s: XmlSink, value: CT_TextUnderlineFillGroupWrapper, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['fillProperties'] !== undefined) {
    if (value['fillProperties'].kind === 'noFill') writeCT_NoFillProperties(s, value['fillProperties'].value, ctx, 'noFill');
    if (value['fillProperties'].kind === 'solidFill') writeCT_SolidColorFillProperties(s, value['fillProperties'].value, ctx, 'solidFill');
    if (value['fillProperties'].kind === 'gradFill') writeCT_GradientFillProperties(s, value['fillProperties'].value, ctx, 'gradFill');
    if (value['fillProperties'].kind === 'blipFill') writeCT_BlipFillProperties(s, value['fillProperties'].value, ctx, 'blipFill');
    if (value['fillProperties'].kind === 'pattFill') writeCT_PatternFillProperties(s, value['fillProperties'].value, ctx, 'pattFill');
    if (value['fillProperties'].kind === 'grpFill') writeCT_GroupFillProperties(s, value['fillProperties'].value, ctx, 'grpFill');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TextUnderlineLineFollowText`; the caller supplies its element local name. */
export function writeCT_TextUnderlineLineFollowText(s: XmlSink, value: CT_TextUnderlineLineFollowText, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_ThemeableLineStyle`; the caller supplies its element local name. */
export function writeCT_ThemeableLineStyle(s: XmlSink, value: CT_ThemeableLineStyle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'ln') writeCT_LineProperties(s, value['content'].value, ctx, 'ln');
    if (value['content'].kind === 'lnRef') writeCT_StyleMatrixReference(s, value['content'].value, ctx, 'lnRef');
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TileInfoProperties`; the caller supplies its element local name. */
export function writeCT_TileInfoProperties(s: XmlSink, value: CT_TileInfoProperties, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['tx'] !== undefined) s.attr(null, 'tx', String(value['tx']));
  if (value['ty'] !== undefined) s.attr(null, 'ty', String(value['ty']));
  if (value['sx'] !== undefined) s.attr(null, 'sx', String(value['sx']));
  if (value['sy'] !== undefined) s.attr(null, 'sy', String(value['sy']));
  if (value['flip'] !== undefined) s.attr(null, 'flip', String(value['flip']));
  if (value['algn'] !== undefined) s.attr(null, 'algn', String(value['algn']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_TintEffect`; the caller supplies its element local name. */
export function writeCT_TintEffect(s: XmlSink, value: CT_TintEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['hue'] !== undefined) s.attr(null, 'hue', String(value['hue']));
  if (value['amt'] !== undefined) s.attr(null, 'amt', String(value['amt']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Transform2D`; the caller supplies its element local name. */
export function writeCT_Transform2D(s: XmlSink, value: CT_Transform2D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['rot'] !== undefined) s.attr(null, 'rot', String(value['rot']));
  if (value['flipH'] !== undefined) s.attr(null, 'flipH', String(value['flipH']));
  if (value['flipV'] !== undefined) s.attr(null, 'flipV', String(value['flipV']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['off'] !== undefined) writeCT_Point2D(s, value['off'], ctx, 'off');
  $q.flush(s, 0);
  if (value['ext'] !== undefined) writeCT_PositiveSize2D(s, value['ext'], ctx, 'ext');
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_TransformEffect`; the caller supplies its element local name. */
export function writeCT_TransformEffect(s: XmlSink, value: CT_TransformEffect, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['sx'] !== undefined) s.attr(null, 'sx', String(value['sx']));
  if (value['sy'] !== undefined) s.attr(null, 'sy', String(value['sy']));
  if (value['kx'] !== undefined) s.attr(null, 'kx', String(value['kx']));
  if (value['ky'] !== undefined) s.attr(null, 'ky', String(value['ky']));
  if (value['tx'] !== undefined) s.attr(null, 'tx', String(value['tx']));
  if (value['ty'] !== undefined) s.attr(null, 'ty', String(value['ty']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Vector3D`; the caller supplies its element local name. */
export function writeCT_Vector3D(s: XmlSink, value: CT_Vector3D, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['dx'] !== undefined) s.attr(null, 'dx', String(value['dx']));
  if (value['dy'] !== undefined) s.attr(null, 'dy', String(value['dy']));
  if (value['dz'] !== undefined) s.attr(null, 'dz', String(value['dz']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_VideoFile`; the caller supplies its element local name. */
export function writeCT_VideoFile(s: XmlSink, value: CT_VideoFile, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['link'] !== undefined) s.attr(uriFor(ctx, 'relationships'), 'link', String(value['link']));
  if (value['contentType'] !== undefined) s.attr(null, 'contentType', String(value['contentType']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['extLst'] !== undefined) writeCT_OfficeArtExtensionList(s, value['extLst'], ctx, 'extLst');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_WholeE2oFormatting`; the caller supplies its element local name. */
export function writeCT_WholeE2oFormatting(s: XmlSink, value: CT_WholeE2oFormatting, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['ln'] !== undefined) writeCT_LineProperties(s, value['ln'], ctx, 'ln');
  $q.flush(s, 0);
  if (value['effectProperties'] !== undefined) {
    if (value['effectProperties'].kind === 'effectLst') writeCT_EffectList(s, value['effectProperties'].value, ctx, 'effectLst');
    if (value['effectProperties'].kind === 'effectDag') writeCT_EffectContainer(s, value['effectProperties'].value, ctx, 'effectDag');
  }
  $q.flush(s, 1);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_XYAdjustHandle`; the caller supplies its element local name. */
export function writeCT_XYAdjustHandle(s: XmlSink, value: CT_XYAdjustHandle, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'dml-main'), localName);
  if (value['gdRefX'] !== undefined) s.attr(null, 'gdRefX', String(value['gdRefX']));
  if (value['minX'] !== undefined) s.attr(null, 'minX', String(value['minX']));
  if (value['maxX'] !== undefined) s.attr(null, 'maxX', String(value['maxX']));
  if (value['gdRefY'] !== undefined) s.attr(null, 'gdRefY', String(value['gdRefY']));
  if (value['minY'] !== undefined) s.attr(null, 'minY', String(value['minY']));
  if (value['maxY'] !== undefined) s.attr(null, 'maxY', String(value['maxY']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['pos'] !== undefined) writeCT_AdjPoint2D(s, value['pos'], ctx, 'pos');
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}
