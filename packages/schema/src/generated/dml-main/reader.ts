/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the dml-main namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type RawNode,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  parseInteger,
  parseXsdBoolean,
  readScalar,
  requireStart,
} from '../../runtime/index.js';
import { parseST_RelationshipId } from '../relationships/reader.js';
import type { ST_RelationshipId } from '../relationships/types.js';
import {
  parseST_FixedPercentage as shared_types_parseST_FixedPercentage,
  parseST_Guid,
  parseST_HexColorRGB,
  parseST_Lang,
  parseST_OnOff,
  parseST_Panose,
  parseST_Percentage as shared_types_parseST_Percentage,
  parseST_PositiveFixedPercentage as shared_types_parseST_PositiveFixedPercentage,
  parseST_PositivePercentage as shared_types_parseST_PositivePercentage,
  parseST_UniversalMeasure,
} from '../shared-types/reader.js';
import type {
  ST_Guid,
  ST_HexColorRGB,
  ST_Lang,
  ST_OnOff,
  ST_Panose,
} from '../shared-types/types.js';
import {
  type CT_AdjPoint2D,
  type CT_AdjustHandleList,
  type CT_AdjustHandleList_Content,
  type CT_AlphaBiLevelEffect,
  type CT_AlphaCeilingEffect,
  type CT_AlphaFloorEffect,
  type CT_AlphaInverseEffect,
  type CT_AlphaInverseEffect_ColorChoice,
  type CT_AlphaModulateEffect,
  type CT_AlphaModulateFixedEffect,
  type CT_AlphaOutsetEffect,
  type CT_AlphaReplaceEffect,
  type CT_Angle,
  type CT_AnimationChartBuildProperties,
  type CT_AnimationChartElement,
  type CT_AnimationDgmBuildProperties,
  type CT_AnimationDgmElement,
  type CT_AnimationElementChoice,
  type CT_AnimationElementChoice_Content,
  type CT_AnimationGraphicalObjectBuildProperties,
  type CT_AnimationGraphicalObjectBuildProperties_Content,
  type CT_AudioCD,
  type CT_AudioCDTime,
  type CT_AudioFile,
  type CT_Backdrop,
  type CT_BackgroundFillStyleList,
  type CT_BackgroundFillStyleList_FillProperties,
  type CT_BackgroundFormatting,
  type CT_BackgroundFormatting_EffectProperties,
  type CT_BackgroundFormatting_FillProperties,
  type CT_BaseStyles,
  type CT_BaseStylesOverride,
  type CT_Bevel,
  type CT_BiLevelEffect,
  type CT_BlendEffect,
  type CT_Blip,
  type CT_BlipFillProperties,
  type CT_BlipFillProperties_FillModeProperties,
  type CT_Blip_Content,
  type CT_BlurEffect,
  type CT_Boolean,
  type CT_Camera,
  type CT_Cell3D,
  type CT_ClipboardStyleSheet,
  type CT_Color,
  type CT_ColorChangeEffect,
  type CT_ColorMRU,
  type CT_ColorMRU_ColorChoice,
  type CT_ColorMapping,
  type CT_ColorMappingOverride,
  type CT_ColorMappingOverride_Content,
  type CT_ColorReplaceEffect,
  type CT_ColorReplaceEffect_ColorChoice,
  type CT_ColorScheme,
  type CT_ColorSchemeAndMapping,
  type CT_ColorSchemeList,
  type CT_Color_ColorChoice,
  type CT_ComplementTransform,
  type CT_Connection,
  type CT_ConnectionSite,
  type CT_ConnectionSiteList,
  type CT_ConnectorLocking,
  type CT_ContentPartLocking,
  type CT_CustomColor,
  type CT_CustomColorList,
  type CT_CustomColor_ColorChoice,
  type CT_CustomGeometry2D,
  type CT_DashStop,
  type CT_DashStopList,
  type CT_DefaultShapeDefinition,
  type CT_DuotoneEffect,
  type CT_DuotoneEffect_ColorChoice,
  type CT_EffectContainer,
  type CT_EffectContainer_Effect,
  type CT_EffectList,
  type CT_EffectProperties,
  type CT_EffectProperties_EffectProperties,
  type CT_EffectReference,
  type CT_EffectStyleItem,
  type CT_EffectStyleItem_EffectProperties,
  type CT_EffectStyleList,
  type CT_EmbeddedWAVAudioFile,
  type CT_EmptyElement,
  type CT_FillEffect,
  type CT_FillEffect_FillProperties,
  type CT_FillOverlayEffect,
  type CT_FillOverlayEffect_FillProperties,
  type CT_FillProperties,
  type CT_FillProperties_FillProperties,
  type CT_FillStyleList,
  type CT_FillStyleList_FillProperties,
  type CT_FixedPercentage,
  type CT_FlatText,
  type CT_FontCollection,
  type CT_FontReference,
  type CT_FontReference_ColorChoice,
  type CT_FontScheme,
  type CT_GammaTransform,
  type CT_GeomGuide,
  type CT_GeomGuideList,
  type CT_GeomRect,
  type CT_GlowEffect,
  type CT_GlowEffect_ColorChoice,
  type CT_GradientFillProperties,
  type CT_GradientFillProperties_ShadeProperties,
  type CT_GradientStop,
  type CT_GradientStopList,
  type CT_GradientStop_ColorChoice,
  type CT_GraphicalObject,
  type CT_GraphicalObjectData,
  type CT_GraphicalObjectFrameLocking,
  type CT_GrayscaleEffect,
  type CT_GrayscaleTransform,
  type CT_GroupFillProperties,
  type CT_GroupLocking,
  type CT_GroupShapeProperties,
  type CT_GroupShapeProperties_EffectProperties,
  type CT_GroupShapeProperties_FillProperties,
  type CT_GroupTransform2D,
  type CT_GvmlConnector,
  type CT_GvmlConnectorNonVisual,
  type CT_GvmlGraphicFrameNonVisual,
  type CT_GvmlGraphicalObjectFrame,
  type CT_GvmlGroupShape,
  type CT_GvmlGroupShapeNonVisual,
  type CT_GvmlGroupShape_Content,
  type CT_GvmlPicture,
  type CT_GvmlPictureNonVisual,
  type CT_GvmlShape,
  type CT_GvmlShapeNonVisual,
  type CT_GvmlTextShape,
  type CT_GvmlTextShape_Content,
  type CT_GvmlUseShapeRectangle,
  type CT_HSLEffect,
  type CT_Headers,
  type CT_HslColor,
  type CT_HslColor_ColorTransform,
  type CT_Hyperlink,
  type CT_InnerShadowEffect,
  type CT_InnerShadowEffect_ColorChoice,
  type CT_InverseGammaTransform,
  type CT_InverseTransform,
  type CT_LightRig,
  type CT_LineEndProperties,
  type CT_LineJoinBevel,
  type CT_LineJoinMiterProperties,
  type CT_LineJoinRound,
  type CT_LineProperties,
  type CT_LineProperties_LineDashProperties,
  type CT_LineProperties_LineFillProperties,
  type CT_LineProperties_LineJoinProperties,
  type CT_LineStyleList,
  type CT_LinearShadeProperties,
  type CT_LuminanceEffect,
  type CT_NoFillProperties,
  type CT_NonVisualConnectorProperties,
  type CT_NonVisualContentPartProperties,
  type CT_NonVisualDrawingProps,
  type CT_NonVisualDrawingShapeProps,
  type CT_NonVisualGraphicFrameProperties,
  type CT_NonVisualGroupDrawingShapeProps,
  type CT_NonVisualPictureProperties,
  type CT_ObjectStyleDefaults,
  type CT_OfficeArtExtension,
  type CT_OfficeArtExtensionList,
  type CT_OfficeStyleSheet,
  type CT_OuterShadowEffect,
  type CT_OuterShadowEffect_ColorChoice,
  type CT_Path2D,
  type CT_Path2DArcTo,
  type CT_Path2DClose,
  type CT_Path2DCubicBezierTo,
  type CT_Path2DLineTo,
  type CT_Path2DList,
  type CT_Path2DMoveTo,
  type CT_Path2DQuadBezierTo,
  type CT_Path2D_Content,
  type CT_PathShadeProperties,
  type CT_PatternFillProperties,
  type CT_Percentage,
  type CT_PictureLocking,
  type CT_Point2D,
  type CT_Point3D,
  type CT_PolarAdjustHandle,
  type CT_PositiveFixedAngle,
  type CT_PositiveFixedPercentage,
  type CT_PositivePercentage,
  type CT_PositiveSize2D,
  type CT_PresetColor,
  type CT_PresetColor_ColorTransform,
  type CT_PresetGeometry2D,
  type CT_PresetLineDashProperties,
  type CT_PresetShadowEffect,
  type CT_PresetShadowEffect_ColorChoice,
  type CT_PresetTextShape,
  type CT_QuickTimeFile,
  type CT_Ratio,
  type CT_ReflectionEffect,
  type CT_RegularTextRun,
  type CT_RelativeOffsetEffect,
  type CT_RelativeRect,
  type CT_SRgbColor,
  type CT_SRgbColor_ColorTransform,
  type CT_ScRgbColor,
  type CT_ScRgbColor_ColorTransform,
  type CT_Scale2D,
  type CT_Scene3D,
  type CT_SchemeColor,
  type CT_SchemeColor_ColorTransform,
  type CT_Shape3D,
  type CT_ShapeLocking,
  type CT_ShapeProperties,
  type CT_ShapeProperties_EffectProperties,
  type CT_ShapeProperties_FillProperties,
  type CT_ShapeProperties_Geometry,
  type CT_ShapeStyle,
  type CT_SoftEdgesEffect,
  type CT_SolidColorFillProperties,
  type CT_SolidColorFillProperties_ColorChoice,
  type CT_SphereCoords,
  type CT_StretchInfoProperties,
  type CT_StyleMatrix,
  type CT_StyleMatrixReference,
  type CT_StyleMatrixReference_ColorChoice,
  type CT_SupplementalFont,
  type CT_SystemColor,
  type CT_SystemColor_ColorTransform,
  type CT_Table,
  type CT_TableBackgroundStyle,
  type CT_TableBackgroundStyle_ThemeableEffectStyle,
  type CT_TableBackgroundStyle_ThemeableFillStyle,
  type CT_TableCell,
  type CT_TableCellBorderStyle,
  type CT_TableCellProperties,
  type CT_TableCellProperties_FillProperties,
  type CT_TableCol,
  type CT_TableGrid,
  type CT_TablePartStyle,
  type CT_TableProperties,
  type CT_TableProperties_Content,
  type CT_TableProperties_EffectProperties,
  type CT_TableProperties_FillProperties,
  type CT_TableRow,
  type CT_TableStyle,
  type CT_TableStyleCellStyle,
  type CT_TableStyleCellStyle_ThemeableFillStyle,
  type CT_TableStyleList,
  type CT_TableStyleTextStyle,
  type CT_TableStyleTextStyle_ColorChoice,
  type CT_TableStyleTextStyle_ThemeableFontStyles,
  type CT_TextAutonumberBullet,
  type CT_TextBlipBullet,
  type CT_TextBody,
  type CT_TextBodyProperties,
  type CT_TextBodyProperties_Text3D,
  type CT_TextBodyProperties_TextAutofit,
  type CT_TextBulletColorFollowText,
  type CT_TextBulletSizeFollowText,
  type CT_TextBulletSizePercent,
  type CT_TextBulletSizePoint,
  type CT_TextBulletTypefaceFollowText,
  type CT_TextCharBullet,
  type CT_TextCharacterProperties,
  type CT_TextCharacterProperties_EffectProperties,
  type CT_TextCharacterProperties_FillProperties,
  type CT_TextCharacterProperties_TextUnderlineFill,
  type CT_TextCharacterProperties_TextUnderlineLine,
  type CT_TextField,
  type CT_TextFont,
  type CT_TextLineBreak,
  type CT_TextListStyle,
  type CT_TextNoAutofit,
  type CT_TextNoBullet,
  type CT_TextNormalAutofit,
  type CT_TextParagraph,
  type CT_TextParagraphProperties,
  type CT_TextParagraphProperties_TextBullet,
  type CT_TextParagraphProperties_TextBulletColor,
  type CT_TextParagraphProperties_TextBulletSize,
  type CT_TextParagraphProperties_TextBulletTypeface,
  type CT_TextParagraph_TextRun,
  type CT_TextShapeAutofit,
  type CT_TextSpacing,
  type CT_TextSpacingPercent,
  type CT_TextSpacingPoint,
  type CT_TextSpacing_Content,
  type CT_TextTabStop,
  type CT_TextTabStopList,
  type CT_TextUnderlineFillFollowText,
  type CT_TextUnderlineFillGroupWrapper,
  type CT_TextUnderlineFillGroupWrapper_FillProperties,
  type CT_TextUnderlineLineFollowText,
  type CT_ThemeableLineStyle,
  type CT_ThemeableLineStyle_Content,
  type CT_TileInfoProperties,
  type CT_TintEffect,
  type CT_Transform2D,
  type CT_TransformEffect,
  type CT_Vector3D,
  type CT_VideoFile,
  type CT_WholeE2oFormatting,
  type CT_WholeE2oFormatting_EffectProperties,
  type CT_XYAdjustHandle,
  type ST_AdjAngle,
  type ST_AdjCoordinate,
  type ST_Angle,
  type ST_AnimationBuildType,
  ST_AnimationBuildType_VALUES,
  type ST_AnimationChartBuildType,
  type ST_AnimationChartOnlyBuildType,
  ST_AnimationChartOnlyBuildType_VALUES,
  type ST_AnimationDgmBuildType,
  type ST_AnimationDgmOnlyBuildType,
  ST_AnimationDgmOnlyBuildType_VALUES,
  type ST_BevelPresetType,
  ST_BevelPresetType_VALUES,
  type ST_BlackWhiteMode,
  ST_BlackWhiteMode_VALUES,
  type ST_BlendMode,
  ST_BlendMode_VALUES,
  type ST_BlipCompression,
  ST_BlipCompression_VALUES,
  type ST_ChartBuildStep,
  ST_ChartBuildStep_VALUES,
  type ST_ColorSchemeIndex,
  ST_ColorSchemeIndex_VALUES,
  type ST_CompoundLine,
  ST_CompoundLine_VALUES,
  type ST_Coordinate,
  type ST_Coordinate32,
  type ST_Coordinate32Unqualified,
  type ST_CoordinateUnqualified,
  type ST_DgmBuildStep,
  ST_DgmBuildStep_VALUES,
  type ST_DrawingElementId,
  type ST_EffectContainerType,
  ST_EffectContainerType_VALUES,
  type ST_FOVAngle,
  type ST_FixedAngle,
  type ST_FixedPercentage,
  type ST_FixedPercentageDecimal,
  type ST_FontCollectionIndex,
  ST_FontCollectionIndex_VALUES,
  type ST_GeomGuideFormula,
  type ST_GeomGuideName,
  type ST_LightRigDirection,
  ST_LightRigDirection_VALUES,
  type ST_LightRigType,
  ST_LightRigType_VALUES,
  type ST_LineCap,
  ST_LineCap_VALUES,
  type ST_LineEndLength,
  ST_LineEndLength_VALUES,
  type ST_LineEndType,
  ST_LineEndType_VALUES,
  type ST_LineEndWidth,
  ST_LineEndWidth_VALUES,
  type ST_LineWidth,
  type ST_OnOffStyleType,
  ST_OnOffStyleType_VALUES,
  type ST_PathFillMode,
  ST_PathFillMode_VALUES,
  type ST_PathShadeType,
  ST_PathShadeType_VALUES,
  type ST_PenAlignment,
  ST_PenAlignment_VALUES,
  type ST_Percentage,
  type ST_PercentageDecimal,
  type ST_PitchFamily,
  ST_PitchFamily_VALUES,
  type ST_PositiveCoordinate,
  type ST_PositiveCoordinate32,
  type ST_PositiveFixedAngle,
  type ST_PositiveFixedPercentage,
  type ST_PositiveFixedPercentageDecimal,
  type ST_PositivePercentage,
  type ST_PositivePercentageDecimal,
  type ST_PresetCameraType,
  ST_PresetCameraType_VALUES,
  type ST_PresetColorVal,
  ST_PresetColorVal_VALUES,
  type ST_PresetLineDashVal,
  ST_PresetLineDashVal_VALUES,
  type ST_PresetMaterialType,
  ST_PresetMaterialType_VALUES,
  type ST_PresetPatternVal,
  ST_PresetPatternVal_VALUES,
  type ST_PresetShadowVal,
  ST_PresetShadowVal_VALUES,
  type ST_RectAlignment,
  ST_RectAlignment_VALUES,
  type ST_SchemeColorVal,
  ST_SchemeColorVal_VALUES,
  type ST_ShapeID,
  type ST_ShapeType,
  ST_ShapeType_VALUES,
  type ST_StyleMatrixColumnIndex,
  type ST_SystemColorVal,
  ST_SystemColorVal_VALUES,
  type ST_TextAlignType,
  ST_TextAlignType_VALUES,
  type ST_TextAnchoringType,
  ST_TextAnchoringType_VALUES,
  type ST_TextAutonumberScheme,
  ST_TextAutonumberScheme_VALUES,
  type ST_TextBulletSize,
  type ST_TextBulletSizeDecimal,
  type ST_TextBulletSizePercent,
  type ST_TextBulletStartAtNum,
  type ST_TextCapsType,
  ST_TextCapsType_VALUES,
  type ST_TextColumnCount,
  type ST_TextFontAlignType,
  ST_TextFontAlignType_VALUES,
  type ST_TextFontScalePercent,
  type ST_TextFontScalePercentOrPercentString,
  type ST_TextFontSize,
  type ST_TextHorzOverflowType,
  ST_TextHorzOverflowType_VALUES,
  type ST_TextIndent,
  type ST_TextIndentLevelType,
  type ST_TextMargin,
  type ST_TextNonNegativePoint,
  type ST_TextPoint,
  type ST_TextPointUnqualified,
  type ST_TextShapeType,
  ST_TextShapeType_VALUES,
  type ST_TextSpacingPercent,
  type ST_TextSpacingPercentOrPercentString,
  type ST_TextSpacingPoint,
  type ST_TextStrikeType,
  ST_TextStrikeType_VALUES,
  type ST_TextTabAlignType,
  ST_TextTabAlignType_VALUES,
  type ST_TextTypeface,
  type ST_TextUnderlineType,
  ST_TextUnderlineType_VALUES,
  type ST_TextVertOverflowType,
  ST_TextVertOverflowType_VALUES,
  type ST_TextVerticalType,
  ST_TextVerticalType_VALUES,
  type ST_TextWrappingType,
  ST_TextWrappingType_VALUES,
  type ST_TileFlipMode,
  ST_TileFlipMode_VALUES,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_AdjAngle(value: string): ST_AdjAngle | undefined {
  return (parseST_Angle(value) ?? parseST_GeomGuideName(value)) as ST_AdjAngle | undefined;
}

export function parseST_AdjCoordinate(value: string): ST_AdjCoordinate | undefined {
  return (parseST_Coordinate(value) ?? parseST_GeomGuideName(value)) as ST_AdjCoordinate | undefined;
}

export function parseST_Angle(value: string): ST_Angle | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_Angle);
}

export function parseST_AnimationBuildType(value: string): ST_AnimationBuildType | undefined {
  const s = collapse(value);
  return ST_AnimationBuildType_VALUES.has(s) ? (s as ST_AnimationBuildType) : undefined;
}

export function parseST_AnimationChartBuildType(value: string): ST_AnimationChartBuildType | undefined {
  return (parseST_AnimationBuildType(value) ?? parseST_AnimationChartOnlyBuildType(value)) as ST_AnimationChartBuildType | undefined;
}

export function parseST_AnimationChartOnlyBuildType(value: string): ST_AnimationChartOnlyBuildType | undefined {
  const s = collapse(value);
  return ST_AnimationChartOnlyBuildType_VALUES.has(s) ? (s as ST_AnimationChartOnlyBuildType) : undefined;
}

export function parseST_AnimationDgmBuildType(value: string): ST_AnimationDgmBuildType | undefined {
  return (parseST_AnimationBuildType(value) ?? parseST_AnimationDgmOnlyBuildType(value)) as ST_AnimationDgmBuildType | undefined;
}

export function parseST_AnimationDgmOnlyBuildType(value: string): ST_AnimationDgmOnlyBuildType | undefined {
  const s = collapse(value);
  return ST_AnimationDgmOnlyBuildType_VALUES.has(s) ? (s as ST_AnimationDgmOnlyBuildType) : undefined;
}

export function parseST_BevelPresetType(value: string): ST_BevelPresetType | undefined {
  const s = collapse(value);
  return ST_BevelPresetType_VALUES.has(s) ? (s as ST_BevelPresetType) : undefined;
}

export function parseST_BlackWhiteMode(value: string): ST_BlackWhiteMode | undefined {
  const s = collapse(value);
  return ST_BlackWhiteMode_VALUES.has(s) ? (s as ST_BlackWhiteMode) : undefined;
}

export function parseST_BlendMode(value: string): ST_BlendMode | undefined {
  const s = collapse(value);
  return ST_BlendMode_VALUES.has(s) ? (s as ST_BlendMode) : undefined;
}

export function parseST_BlipCompression(value: string): ST_BlipCompression | undefined {
  const s = collapse(value);
  return ST_BlipCompression_VALUES.has(s) ? (s as ST_BlipCompression) : undefined;
}

export function parseST_ChartBuildStep(value: string): ST_ChartBuildStep | undefined {
  const s = collapse(value);
  return ST_ChartBuildStep_VALUES.has(s) ? (s as ST_ChartBuildStep) : undefined;
}

export function parseST_ColorSchemeIndex(value: string): ST_ColorSchemeIndex | undefined {
  const s = collapse(value);
  return ST_ColorSchemeIndex_VALUES.has(s) ? (s as ST_ColorSchemeIndex) : undefined;
}

export function parseST_CompoundLine(value: string): ST_CompoundLine | undefined {
  const s = collapse(value);
  return ST_CompoundLine_VALUES.has(s) ? (s as ST_CompoundLine) : undefined;
}

export function parseST_Coordinate(value: string): ST_Coordinate | undefined {
  return (parseST_CoordinateUnqualified(value) ?? parseST_UniversalMeasure(value)) as ST_Coordinate | undefined;
}

export function parseST_Coordinate32(value: string): ST_Coordinate32 | undefined {
  return (parseST_Coordinate32Unqualified(value) ?? parseST_UniversalMeasure(value)) as ST_Coordinate32 | undefined;
}

export function parseST_Coordinate32Unqualified(value: string): ST_Coordinate32Unqualified | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_Coordinate32Unqualified);
}

export function parseST_CoordinateUnqualified(value: string): ST_CoordinateUnqualified | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_CoordinateUnqualified);
}

export function parseST_DgmBuildStep(value: string): ST_DgmBuildStep | undefined {
  const s = collapse(value);
  return ST_DgmBuildStep_VALUES.has(s) ? (s as ST_DgmBuildStep) : undefined;
}

export function parseST_DrawingElementId(value: string): ST_DrawingElementId | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_DrawingElementId);
}

export function parseST_EffectContainerType(value: string): ST_EffectContainerType | undefined {
  const s = collapse(value);
  return ST_EffectContainerType_VALUES.has(s) ? (s as ST_EffectContainerType) : undefined;
}

export function parseST_FOVAngle(value: string): ST_FOVAngle | undefined {
  return value as ST_FOVAngle;
}

export function parseST_FixedAngle(value: string): ST_FixedAngle | undefined {
  return value as ST_FixedAngle;
}

export function parseST_FixedPercentage(value: string): ST_FixedPercentage | undefined {
  return (parseST_FixedPercentageDecimal(value) ?? shared_types_parseST_FixedPercentage(value)) as ST_FixedPercentage | undefined;
}

export function parseST_FixedPercentageDecimal(value: string): ST_FixedPercentageDecimal | undefined {
  return value as ST_FixedPercentageDecimal;
}

export function parseST_FontCollectionIndex(value: string): ST_FontCollectionIndex | undefined {
  const s = collapse(value);
  return ST_FontCollectionIndex_VALUES.has(s) ? (s as ST_FontCollectionIndex) : undefined;
}

export function parseST_GeomGuideFormula(value: string): ST_GeomGuideFormula | undefined {
  return value as ST_GeomGuideFormula;
}

export function parseST_GeomGuideName(value: string): ST_GeomGuideName | undefined {
  return value as ST_GeomGuideName;
}

export function parseST_LightRigDirection(value: string): ST_LightRigDirection | undefined {
  const s = collapse(value);
  return ST_LightRigDirection_VALUES.has(s) ? (s as ST_LightRigDirection) : undefined;
}

export function parseST_LightRigType(value: string): ST_LightRigType | undefined {
  const s = collapse(value);
  return ST_LightRigType_VALUES.has(s) ? (s as ST_LightRigType) : undefined;
}

export function parseST_LineCap(value: string): ST_LineCap | undefined {
  const s = collapse(value);
  return ST_LineCap_VALUES.has(s) ? (s as ST_LineCap) : undefined;
}

export function parseST_LineEndLength(value: string): ST_LineEndLength | undefined {
  const s = collapse(value);
  return ST_LineEndLength_VALUES.has(s) ? (s as ST_LineEndLength) : undefined;
}

export function parseST_LineEndType(value: string): ST_LineEndType | undefined {
  const s = collapse(value);
  return ST_LineEndType_VALUES.has(s) ? (s as ST_LineEndType) : undefined;
}

export function parseST_LineEndWidth(value: string): ST_LineEndWidth | undefined {
  const s = collapse(value);
  return ST_LineEndWidth_VALUES.has(s) ? (s as ST_LineEndWidth) : undefined;
}

export function parseST_LineWidth(value: string): ST_LineWidth | undefined {
  return value as ST_LineWidth;
}

export function parseST_OnOffStyleType(value: string): ST_OnOffStyleType | undefined {
  const s = collapse(value);
  return ST_OnOffStyleType_VALUES.has(s) ? (s as ST_OnOffStyleType) : undefined;
}

export function parseST_PathFillMode(value: string): ST_PathFillMode | undefined {
  const s = collapse(value);
  return ST_PathFillMode_VALUES.has(s) ? (s as ST_PathFillMode) : undefined;
}

export function parseST_PathShadeType(value: string): ST_PathShadeType | undefined {
  const s = collapse(value);
  return ST_PathShadeType_VALUES.has(s) ? (s as ST_PathShadeType) : undefined;
}

export function parseST_PenAlignment(value: string): ST_PenAlignment | undefined {
  const s = collapse(value);
  return ST_PenAlignment_VALUES.has(s) ? (s as ST_PenAlignment) : undefined;
}

export function parseST_Percentage(value: string): ST_Percentage | undefined {
  return (parseST_PercentageDecimal(value) ?? shared_types_parseST_Percentage(value)) as ST_Percentage | undefined;
}

export function parseST_PercentageDecimal(value: string): ST_PercentageDecimal | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_PercentageDecimal);
}

export function parseST_PitchFamily(value: string): ST_PitchFamily | undefined {
  const s = collapse(value);
  return ST_PitchFamily_VALUES.has(s) ? (s as ST_PitchFamily) : undefined;
}

export function parseST_PositiveCoordinate(value: string): ST_PositiveCoordinate | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_PositiveCoordinate);
}

export function parseST_PositiveCoordinate32(value: string): ST_PositiveCoordinate32 | undefined {
  return value as ST_PositiveCoordinate32;
}

export function parseST_PositiveFixedAngle(value: string): ST_PositiveFixedAngle | undefined {
  return value as ST_PositiveFixedAngle;
}

export function parseST_PositiveFixedPercentage(value: string): ST_PositiveFixedPercentage | undefined {
  return (parseST_PositiveFixedPercentageDecimal(value) ?? shared_types_parseST_PositiveFixedPercentage(value)) as ST_PositiveFixedPercentage | undefined;
}

export function parseST_PositiveFixedPercentageDecimal(value: string): ST_PositiveFixedPercentageDecimal | undefined {
  return value as ST_PositiveFixedPercentageDecimal;
}

export function parseST_PositivePercentage(value: string): ST_PositivePercentage | undefined {
  return (parseST_PositivePercentageDecimal(value) ?? shared_types_parseST_PositivePercentage(value)) as ST_PositivePercentage | undefined;
}

export function parseST_PositivePercentageDecimal(value: string): ST_PositivePercentageDecimal | undefined {
  return value as ST_PositivePercentageDecimal;
}

export function parseST_PresetCameraType(value: string): ST_PresetCameraType | undefined {
  const s = collapse(value);
  return ST_PresetCameraType_VALUES.has(s) ? (s as ST_PresetCameraType) : undefined;
}

export function parseST_PresetColorVal(value: string): ST_PresetColorVal | undefined {
  const s = collapse(value);
  return ST_PresetColorVal_VALUES.has(s) ? (s as ST_PresetColorVal) : undefined;
}

export function parseST_PresetLineDashVal(value: string): ST_PresetLineDashVal | undefined {
  const s = collapse(value);
  return ST_PresetLineDashVal_VALUES.has(s) ? (s as ST_PresetLineDashVal) : undefined;
}

export function parseST_PresetMaterialType(value: string): ST_PresetMaterialType | undefined {
  const s = collapse(value);
  return ST_PresetMaterialType_VALUES.has(s) ? (s as ST_PresetMaterialType) : undefined;
}

export function parseST_PresetPatternVal(value: string): ST_PresetPatternVal | undefined {
  const s = collapse(value);
  return ST_PresetPatternVal_VALUES.has(s) ? (s as ST_PresetPatternVal) : undefined;
}

export function parseST_PresetShadowVal(value: string): ST_PresetShadowVal | undefined {
  const s = collapse(value);
  return ST_PresetShadowVal_VALUES.has(s) ? (s as ST_PresetShadowVal) : undefined;
}

export function parseST_RectAlignment(value: string): ST_RectAlignment | undefined {
  const s = collapse(value);
  return ST_RectAlignment_VALUES.has(s) ? (s as ST_RectAlignment) : undefined;
}

export function parseST_SchemeColorVal(value: string): ST_SchemeColorVal | undefined {
  const s = collapse(value);
  return ST_SchemeColorVal_VALUES.has(s) ? (s as ST_SchemeColorVal) : undefined;
}

export function parseST_ShapeID(value: string): ST_ShapeID | undefined {
  return value as ST_ShapeID;
}

export function parseST_ShapeType(value: string): ST_ShapeType | undefined {
  const s = collapse(value);
  return ST_ShapeType_VALUES.has(s) ? (s as ST_ShapeType) : undefined;
}

export function parseST_StyleMatrixColumnIndex(value: string): ST_StyleMatrixColumnIndex | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_StyleMatrixColumnIndex);
}

export function parseST_SystemColorVal(value: string): ST_SystemColorVal | undefined {
  const s = collapse(value);
  return ST_SystemColorVal_VALUES.has(s) ? (s as ST_SystemColorVal) : undefined;
}

export function parseST_TextAlignType(value: string): ST_TextAlignType | undefined {
  const s = collapse(value);
  return ST_TextAlignType_VALUES.has(s) ? (s as ST_TextAlignType) : undefined;
}

export function parseST_TextAnchoringType(value: string): ST_TextAnchoringType | undefined {
  const s = collapse(value);
  return ST_TextAnchoringType_VALUES.has(s) ? (s as ST_TextAnchoringType) : undefined;
}

export function parseST_TextAutonumberScheme(value: string): ST_TextAutonumberScheme | undefined {
  const s = collapse(value);
  return ST_TextAutonumberScheme_VALUES.has(s) ? (s as ST_TextAutonumberScheme) : undefined;
}

export function parseST_TextBulletSize(value: string): ST_TextBulletSize | undefined {
  return (parseST_TextBulletSizePercent(value) ?? parseST_TextBulletSizeDecimal(value)) as ST_TextBulletSize | undefined;
}

export function parseST_TextBulletSizeDecimal(value: string): ST_TextBulletSizeDecimal | undefined {
  return value as ST_TextBulletSizeDecimal;
}

export function parseST_TextBulletSizePercent(value: string): ST_TextBulletSizePercent | undefined {
  return value as ST_TextBulletSizePercent;
}

export function parseST_TextBulletStartAtNum(value: string): ST_TextBulletStartAtNum | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_TextBulletStartAtNum);
}

export function parseST_TextCapsType(value: string): ST_TextCapsType | undefined {
  const s = collapse(value);
  return ST_TextCapsType_VALUES.has(s) ? (s as ST_TextCapsType) : undefined;
}

export function parseST_TextColumnCount(value: string): ST_TextColumnCount | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_TextColumnCount);
}

export function parseST_TextFontAlignType(value: string): ST_TextFontAlignType | undefined {
  const s = collapse(value);
  return ST_TextFontAlignType_VALUES.has(s) ? (s as ST_TextFontAlignType) : undefined;
}

export function parseST_TextFontScalePercent(value: string): ST_TextFontScalePercent | undefined {
  return value as ST_TextFontScalePercent;
}

export function parseST_TextFontScalePercentOrPercentString(value: string): ST_TextFontScalePercentOrPercentString | undefined {
  return (parseST_TextFontScalePercent(value) ?? shared_types_parseST_Percentage(value)) as ST_TextFontScalePercentOrPercentString | undefined;
}

export function parseST_TextFontSize(value: string): ST_TextFontSize | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_TextFontSize);
}

export function parseST_TextHorzOverflowType(value: string): ST_TextHorzOverflowType | undefined {
  const s = collapse(value);
  return ST_TextHorzOverflowType_VALUES.has(s) ? (s as ST_TextHorzOverflowType) : undefined;
}

export function parseST_TextIndent(value: string): ST_TextIndent | undefined {
  return value as ST_TextIndent;
}

export function parseST_TextIndentLevelType(value: string): ST_TextIndentLevelType | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_TextIndentLevelType);
}

export function parseST_TextMargin(value: string): ST_TextMargin | undefined {
  return value as ST_TextMargin;
}

export function parseST_TextNonNegativePoint(value: string): ST_TextNonNegativePoint | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_TextNonNegativePoint);
}

export function parseST_TextPoint(value: string): ST_TextPoint | undefined {
  return (parseST_TextPointUnqualified(value) ?? parseST_UniversalMeasure(value)) as ST_TextPoint | undefined;
}

export function parseST_TextPointUnqualified(value: string): ST_TextPointUnqualified | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_TextPointUnqualified);
}

export function parseST_TextShapeType(value: string): ST_TextShapeType | undefined {
  const s = collapse(value);
  return ST_TextShapeType_VALUES.has(s) ? (s as ST_TextShapeType) : undefined;
}

export function parseST_TextSpacingPercent(value: string): ST_TextSpacingPercent | undefined {
  return value as ST_TextSpacingPercent;
}

export function parseST_TextSpacingPercentOrPercentString(value: string): ST_TextSpacingPercentOrPercentString | undefined {
  return (parseST_TextSpacingPercent(value) ?? shared_types_parseST_Percentage(value)) as ST_TextSpacingPercentOrPercentString | undefined;
}

export function parseST_TextSpacingPoint(value: string): ST_TextSpacingPoint | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_TextSpacingPoint);
}

export function parseST_TextStrikeType(value: string): ST_TextStrikeType | undefined {
  const s = collapse(value);
  return ST_TextStrikeType_VALUES.has(s) ? (s as ST_TextStrikeType) : undefined;
}

export function parseST_TextTabAlignType(value: string): ST_TextTabAlignType | undefined {
  const s = collapse(value);
  return ST_TextTabAlignType_VALUES.has(s) ? (s as ST_TextTabAlignType) : undefined;
}

export function parseST_TextTypeface(value: string): ST_TextTypeface | undefined {
  return value as ST_TextTypeface;
}

export function parseST_TextUnderlineType(value: string): ST_TextUnderlineType | undefined {
  const s = collapse(value);
  return ST_TextUnderlineType_VALUES.has(s) ? (s as ST_TextUnderlineType) : undefined;
}

export function parseST_TextVertOverflowType(value: string): ST_TextVertOverflowType | undefined {
  const s = collapse(value);
  return ST_TextVertOverflowType_VALUES.has(s) ? (s as ST_TextVertOverflowType) : undefined;
}

export function parseST_TextVerticalType(value: string): ST_TextVerticalType | undefined {
  const s = collapse(value);
  return ST_TextVerticalType_VALUES.has(s) ? (s as ST_TextVerticalType) : undefined;
}

export function parseST_TextWrappingType(value: string): ST_TextWrappingType | undefined {
  const s = collapse(value);
  return ST_TextWrappingType_VALUES.has(s) ? (s as ST_TextWrappingType) : undefined;
}

export function parseST_TileFlipMode(value: string): ST_TileFlipMode | undefined {
  const s = collapse(value);
  return ST_TileFlipMode_VALUES.has(s) ? (s as ST_TileFlipMode) : undefined;
}

// Complex-type readers.

/** Read a `CT_AdjPoint2D`. See `transitional/dml-main.xsd:1974`. */
export function readCT_AdjPoint2D(cur: XmlCursor, ctx: ReadContext): CT_AdjPoint2D {
  const start = requireStart(cur, 'CT_AdjPoint2D');
  let x: ST_AdjCoordinate | undefined;
  let y: ST_AdjCoordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'x': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            x = v;
            continue;
          }
          ctx.invalidValue('CT_AdjPoint2D', a, 'ST_AdjCoordinate', cur);
          break;
        }
        case 'y': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            y = v;
            continue;
          }
          ctx.invalidValue('CT_AdjPoint2D', a, 'ST_AdjCoordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AdjPoint2D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (x === undefined) ctx.missingRequired('CT_AdjPoint2D', 'x', cur);
  if (y === undefined) ctx.missingRequired('CT_AdjPoint2D', 'y', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AdjPoint2D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AdjPoint2D', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AdjPoint2D', cur);
    cur.next();
  }
  return {
    x,
    y,
    $unknownAttrs,
  };
}

/** Read a `CT_AdjustHandleList`. See `transitional/dml-main.xsd:2012`. */
export function readCT_AdjustHandleList(cur: XmlCursor, ctx: ReadContext): CT_AdjustHandleList {
  const start = requireStart(cur, 'CT_AdjustHandleList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AdjustHandleList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_AdjustHandleList_Content[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AdjustHandleList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'ahXY': {
            content.push({ kind: 'ahXY', value: readCT_XYAdjustHandle(cur, ctx) });
            continue;
          }
          case 'ahPolar': {
            content.push({ kind: 'ahPolar', value: readCT_PolarAdjustHandle(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AdjustHandleList', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AdjustHandleList', cur);
    cur.next();
  }

  return {
    content,
    $unknownAttrs,
  };
}

/** Read a `CT_AlphaBiLevelEffect`. See `transitional/dml-main.xsd:1264`. */
export function readCT_AlphaBiLevelEffect(cur: XmlCursor, ctx: ReadContext): CT_AlphaBiLevelEffect {
  const start = requireStart(cur, 'CT_AlphaBiLevelEffect');
  let thresh: ST_PositiveFixedPercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'thresh': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            thresh = v;
            continue;
          }
          ctx.invalidValue('CT_AlphaBiLevelEffect', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AlphaBiLevelEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (thresh === undefined) ctx.missingRequired('CT_AlphaBiLevelEffect', 'thresh', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AlphaBiLevelEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AlphaBiLevelEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AlphaBiLevelEffect', cur);
    cur.next();
  }
  return {
    thresh,
    $unknownAttrs,
  };
}

/** Read a `CT_AlphaCeilingEffect`. See `transitional/dml-main.xsd:1267`. */
export function readCT_AlphaCeilingEffect(cur: XmlCursor, ctx: ReadContext): CT_AlphaCeilingEffect {
  const start = requireStart(cur, 'CT_AlphaCeilingEffect');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AlphaCeilingEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AlphaCeilingEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AlphaCeilingEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AlphaCeilingEffect', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_AlphaFloorEffect`. See `transitional/dml-main.xsd:1268`. */
export function readCT_AlphaFloorEffect(cur: XmlCursor, ctx: ReadContext): CT_AlphaFloorEffect {
  const start = requireStart(cur, 'CT_AlphaFloorEffect');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AlphaFloorEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AlphaFloorEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AlphaFloorEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AlphaFloorEffect', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_AlphaInverseEffect`. See `transitional/dml-main.xsd:1269`. */
export function readCT_AlphaInverseEffect(cur: XmlCursor, ctx: ReadContext): CT_AlphaInverseEffect {
  const start = requireStart(cur, 'CT_AlphaInverseEffect');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AlphaInverseEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let colorChoice: CT_AlphaInverseEffect_ColorChoice | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AlphaInverseEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AlphaInverseEffect', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AlphaInverseEffect', cur);
    cur.next();
  }

  return {
    colorChoice,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AlphaModulateEffect`. See `transitional/dml-main.xsd:1682`. */
export function readCT_AlphaModulateEffect(cur: XmlCursor, ctx: ReadContext): CT_AlphaModulateEffect {
  const start = requireStart(cur, 'CT_AlphaModulateEffect');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AlphaModulateEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cont: CT_EffectContainer | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AlphaModulateEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'cont': {
            cont = readCT_EffectContainer(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AlphaModulateEffect', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AlphaModulateEffect', cur);
    cur.next();
  }

  return {
    cont,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AlphaModulateFixedEffect`. See `transitional/dml-main.xsd:1274`. */
export function readCT_AlphaModulateFixedEffect(cur: XmlCursor, ctx: ReadContext): CT_AlphaModulateFixedEffect {
  const start = requireStart(cur, 'CT_AlphaModulateFixedEffect');
  let amt: ST_PositivePercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'amt': {
          const v = parseST_PositivePercentage(a.value);
          if (v !== undefined) {
            amt = v;
            continue;
          }
          ctx.invalidValue('CT_AlphaModulateFixedEffect', a, 'ST_PositivePercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AlphaModulateFixedEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AlphaModulateFixedEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AlphaModulateFixedEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AlphaModulateFixedEffect', cur);
    cur.next();
  }
  return {
    amt,
    $unknownAttrs,
  };
}

/** Read a `CT_AlphaOutsetEffect`. See `transitional/dml-main.xsd:1277`. */
export function readCT_AlphaOutsetEffect(cur: XmlCursor, ctx: ReadContext): CT_AlphaOutsetEffect {
  const start = requireStart(cur, 'CT_AlphaOutsetEffect');
  let rad: ST_Coordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'rad': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            rad = v;
            continue;
          }
          ctx.invalidValue('CT_AlphaOutsetEffect', a, 'ST_Coordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AlphaOutsetEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AlphaOutsetEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AlphaOutsetEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AlphaOutsetEffect', cur);
    cur.next();
  }
  return {
    rad,
    $unknownAttrs,
  };
}

/** Read a `CT_AlphaReplaceEffect`. See `transitional/dml-main.xsd:1280`. */
export function readCT_AlphaReplaceEffect(cur: XmlCursor, ctx: ReadContext): CT_AlphaReplaceEffect {
  const start = requireStart(cur, 'CT_AlphaReplaceEffect');
  let a_: ST_PositiveFixedPercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'a': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            a_ = v;
            continue;
          }
          ctx.invalidValue('CT_AlphaReplaceEffect', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AlphaReplaceEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (a_ === undefined) ctx.missingRequired('CT_AlphaReplaceEffect', 'a', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AlphaReplaceEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AlphaReplaceEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AlphaReplaceEffect', cur);
    cur.next();
  }
  return {
    a: a_,
    $unknownAttrs,
  };
}

/** Read a `CT_Angle`. See `transitional/dml-main.xsd:218`. */
export function readCT_Angle(cur: XmlCursor, ctx: ReadContext): CT_Angle {
  const start = requireStart(cur, 'CT_Angle');
  let val: ST_Angle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Angle(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Angle', a, 'ST_Angle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Angle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Angle', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Angle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Angle', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Angle', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_AnimationChartBuildProperties`. See `transitional/dml-main.xsd:938`. */
export function readCT_AnimationChartBuildProperties(cur: XmlCursor, ctx: ReadContext): CT_AnimationChartBuildProperties {
  const start = requireStart(cur, 'CT_AnimationChartBuildProperties');
  let bld: ST_AnimationChartBuildType | undefined;
  let animBg: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'bld': {
          const v = parseST_AnimationChartBuildType(a.value);
          if (v !== undefined) {
            bld = v;
            continue;
          }
          ctx.invalidValue('CT_AnimationChartBuildProperties', a, 'ST_AnimationChartBuildType', cur);
          break;
        }
        case 'animBg': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            animBg = v;
            continue;
          }
          ctx.invalidValue('CT_AnimationChartBuildProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AnimationChartBuildProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AnimationChartBuildProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AnimationChartBuildProperties', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AnimationChartBuildProperties', cur);
    cur.next();
  }
  return {
    bld,
    animBg,
    $unknownAttrs,
  };
}

/** Read a `CT_AnimationChartElement`. See `transitional/dml-main.xsd:897`. */
export function readCT_AnimationChartElement(cur: XmlCursor, ctx: ReadContext): CT_AnimationChartElement {
  const start = requireStart(cur, 'CT_AnimationChartElement');
  let seriesIdx: number | undefined;
  let categoryIdx: number | undefined;
  let bldStep: ST_ChartBuildStep | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'seriesIdx': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            seriesIdx = v;
            continue;
          }
          ctx.invalidValue('CT_AnimationChartElement', a, 'xsd:int', cur);
          break;
        }
        case 'categoryIdx': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            categoryIdx = v;
            continue;
          }
          ctx.invalidValue('CT_AnimationChartElement', a, 'xsd:int', cur);
          break;
        }
        case 'bldStep': {
          const v = parseST_ChartBuildStep(a.value);
          if (v !== undefined) {
            bldStep = v;
            continue;
          }
          ctx.invalidValue('CT_AnimationChartElement', a, 'ST_ChartBuildStep', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AnimationChartElement', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (bldStep === undefined) ctx.missingRequired('CT_AnimationChartElement', 'bldStep', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AnimationChartElement', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AnimationChartElement', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AnimationChartElement', cur);
    cur.next();
  }
  return {
    seriesIdx,
    categoryIdx,
    bldStep,
    $unknownAttrs,
  };
}

/** Read a `CT_AnimationDgmBuildProperties`. See `transitional/dml-main.xsd:923`. */
export function readCT_AnimationDgmBuildProperties(cur: XmlCursor, ctx: ReadContext): CT_AnimationDgmBuildProperties {
  const start = requireStart(cur, 'CT_AnimationDgmBuildProperties');
  let bld: ST_AnimationDgmBuildType | undefined;
  let rev: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'bld': {
          const v = parseST_AnimationDgmBuildType(a.value);
          if (v !== undefined) {
            bld = v;
            continue;
          }
          ctx.invalidValue('CT_AnimationDgmBuildProperties', a, 'ST_AnimationDgmBuildType', cur);
          break;
        }
        case 'rev': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            rev = v;
            continue;
          }
          ctx.invalidValue('CT_AnimationDgmBuildProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AnimationDgmBuildProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AnimationDgmBuildProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AnimationDgmBuildProperties', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AnimationDgmBuildProperties', cur);
    cur.next();
  }
  return {
    bld,
    rev,
    $unknownAttrs,
  };
}

/** Read a `CT_AnimationDgmElement`. See `transitional/dml-main.xsd:892`. */
export function readCT_AnimationDgmElement(cur: XmlCursor, ctx: ReadContext): CT_AnimationDgmElement {
  const start = requireStart(cur, 'CT_AnimationDgmElement');
  let id: ST_Guid | undefined;
  let bldStep: ST_DgmBuildStep | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id': {
          const v = parseST_Guid(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_AnimationDgmElement', a, 'ST_Guid', cur);
          break;
        }
        case 'bldStep': {
          const v = parseST_DgmBuildStep(a.value);
          if (v !== undefined) {
            bldStep = v;
            continue;
          }
          ctx.invalidValue('CT_AnimationDgmElement', a, 'ST_DgmBuildStep', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AnimationDgmElement', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AnimationDgmElement', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AnimationDgmElement', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AnimationDgmElement', cur);
    cur.next();
  }
  return {
    id,
    bldStep,
    $unknownAttrs,
  };
}

/** Read a `CT_AnimationElementChoice`. See `transitional/dml-main.xsd:902`. */
export function readCT_AnimationElementChoice(cur: XmlCursor, ctx: ReadContext): CT_AnimationElementChoice {
  const start = requireStart(cur, 'CT_AnimationElementChoice');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AnimationElementChoice', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_AnimationElementChoice_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AnimationElementChoice', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'dgm': {
            content = { kind: 'dgm', value: readCT_AnimationDgmElement(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'chart': {
            content = { kind: 'chart', value: readCT_AnimationChartElement(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AnimationElementChoice', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AnimationElementChoice', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AnimationGraphicalObjectBuildProperties`. See `transitional/dml-main.xsd:942`. */
export function readCT_AnimationGraphicalObjectBuildProperties(cur: XmlCursor, ctx: ReadContext): CT_AnimationGraphicalObjectBuildProperties {
  const start = requireStart(cur, 'CT_AnimationGraphicalObjectBuildProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AnimationGraphicalObjectBuildProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_AnimationGraphicalObjectBuildProperties_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AnimationGraphicalObjectBuildProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'bldDgm': {
            content = { kind: 'bldDgm', value: readCT_AnimationDgmBuildProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'bldChart': {
            content = { kind: 'bldChart', value: readCT_AnimationChartBuildProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AnimationGraphicalObjectBuildProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AnimationGraphicalObjectBuildProperties', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AudioCD`. See `transitional/dml-main.xsd:44`. */
export function readCT_AudioCD(cur: XmlCursor, ctx: ReadContext): CT_AudioCD {
  const start = requireStart(cur, 'CT_AudioCD');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AudioCD', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let st: CT_AudioCDTime | undefined;
  let end: CT_AudioCDTime | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AudioCD', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'st': {
            st = readCT_AudioCDTime(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'end': {
            end = readCT_AudioCDTime(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AudioCD', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AudioCD', cur);
    cur.next();
  }

  return {
    st,
    end,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_AudioCDTime`. See `transitional/dml-main.xsd:40`. */
export function readCT_AudioCDTime(cur: XmlCursor, ctx: ReadContext): CT_AudioCDTime {
  const start = requireStart(cur, 'CT_AudioCDTime');
  let track: number | undefined;
  let time: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'track': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            track = v;
            continue;
          }
          ctx.invalidValue('CT_AudioCDTime', a, 'xsd:unsignedByte', cur);
          break;
        }
        case 'time': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            time = v;
            continue;
          }
          ctx.invalidValue('CT_AudioCDTime', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AudioCDTime', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (track === undefined) ctx.missingRequired('CT_AudioCDTime', 'track', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AudioCDTime', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_AudioCDTime', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AudioCDTime', cur);
    cur.next();
  }
  return {
    track,
    time,
    $unknownAttrs,
  };
}

/** Read a `CT_AudioFile`. See `transitional/dml-main.xsd:20`. */
export function readCT_AudioFile(cur: XmlCursor, ctx: ReadContext): CT_AudioFile {
  const start = requireStart(cur, 'CT_AudioFile');
  let link: ST_RelationshipId | undefined;
  let contentType: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'contentType':
          contentType = a.value as string;
          continue;

      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'link': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            link = v;
            continue;
          }
          ctx.invalidValue('CT_AudioFile', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_AudioFile', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (link === undefined) ctx.missingRequired('CT_AudioFile', 'link', cur);

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AudioFile', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AudioFile', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AudioFile', cur);
    cur.next();
  }

  return {
    extLst,
    link,
    contentType,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Backdrop`. See `transitional/dml-main.xsd:1193`. */
export function readCT_Backdrop(cur: XmlCursor, ctx: ReadContext): CT_Backdrop {
  const start = requireStart(cur, 'CT_Backdrop');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Backdrop', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let anchor: CT_Point3D | undefined;
  let norm: CT_Vector3D | undefined;
  let up: CT_Vector3D | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Backdrop', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'anchor': {
            anchor = readCT_Point3D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'norm': {
            norm = readCT_Vector3D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'up': {
            up = readCT_Vector3D(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Backdrop', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Backdrop', cur);
    cur.next();
  }

  return {
    anchor,
    norm,
    up,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BackgroundFillStyleList`. See `transitional/dml-main.xsd:160`. */
export function readCT_BackgroundFillStyleList(cur: XmlCursor, ctx: ReadContext): CT_BackgroundFillStyleList {
  const start = requireStart(cur, 'CT_BackgroundFillStyleList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BackgroundFillStyleList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const fillProperties: CT_BackgroundFillStyleList_FillProperties[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BackgroundFillStyleList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'noFill': {
            fillProperties.push({ kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) });
            continue;
          }
          case 'solidFill': {
            fillProperties.push({ kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) });
            continue;
          }
          case 'gradFill': {
            fillProperties.push({ kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) });
            continue;
          }
          case 'blipFill': {
            fillProperties.push({ kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) });
            continue;
          }
          case 'pattFill': {
            fillProperties.push({ kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) });
            continue;
          }
          case 'grpFill': {
            fillProperties.push({ kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BackgroundFillStyleList', ev.localName, ev.uri, cur);
      fillProperties.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BackgroundFillStyleList', cur);
    cur.next();
  }

  return {
    fillProperties,
    $unknownAttrs,
  };
}

/** Read a `CT_BackgroundFormatting`. See `transitional/dml-main.xsd:948`. */
export function readCT_BackgroundFormatting(cur: XmlCursor, ctx: ReadContext): CT_BackgroundFormatting {
  const start = requireStart(cur, 'CT_BackgroundFormatting');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BackgroundFormatting', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let fillProperties: CT_BackgroundFormatting_FillProperties | undefined;
  let effectProperties: CT_BackgroundFormatting_EffectProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BackgroundFormatting', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'noFill': {
            fillProperties = { kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'solidFill': {
            fillProperties = { kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'gradFill': {
            fillProperties = { kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            fillProperties = { kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pattFill': {
            fillProperties = { kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'grpFill': {
            fillProperties = { kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'effectLst': {
            effectProperties = { kind: 'effectLst', value: readCT_EffectList(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'effectDag': {
            effectProperties = { kind: 'effectDag', value: readCT_EffectContainer(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BackgroundFormatting', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BackgroundFormatting', cur);
    cur.next();
  }

  return {
    fillProperties,
    effectProperties,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BaseStyles`. See `transitional/dml-main.xsd:175`. */
export function readCT_BaseStyles(cur: XmlCursor, ctx: ReadContext): CT_BaseStyles {
  const start = requireStart(cur, 'CT_BaseStyles');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BaseStyles', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let clrScheme: CT_ColorScheme | undefined;
  let fontScheme: CT_FontScheme | undefined;
  let fmtScheme: CT_StyleMatrix | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BaseStyles', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'clrScheme': {
            clrScheme = readCT_ColorScheme(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'fontScheme': {
            fontScheme = readCT_FontScheme(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'fmtScheme': {
            fmtScheme = readCT_StyleMatrix(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BaseStyles', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BaseStyles', cur);
    cur.next();
  }

  return {
    clrScheme,
    fontScheme,
    fmtScheme,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BaseStylesOverride`. See `transitional/dml-main.xsd:2353`. */
export function readCT_BaseStylesOverride(cur: XmlCursor, ctx: ReadContext): CT_BaseStylesOverride {
  const start = requireStart(cur, 'CT_BaseStylesOverride');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_BaseStylesOverride', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let clrScheme: CT_ColorScheme | undefined;
  let fontScheme: CT_FontScheme | undefined;
  let fmtScheme: CT_StyleMatrix | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BaseStylesOverride', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'clrScheme': {
            clrScheme = readCT_ColorScheme(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'fontScheme': {
            fontScheme = readCT_FontScheme(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'fmtScheme': {
            fmtScheme = readCT_StyleMatrix(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BaseStylesOverride', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BaseStylesOverride', cur);
    cur.next();
  }

  return {
    clrScheme,
    fontScheme,
    fmtScheme,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Bevel`. See `transitional/dml-main.xsd:1217`. */
export function readCT_Bevel(cur: XmlCursor, ctx: ReadContext): CT_Bevel {
  const start = requireStart(cur, 'CT_Bevel');
  let w: ST_PositiveCoordinate | undefined;
  let h: ST_PositiveCoordinate | undefined;
  let prst: ST_BevelPresetType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'w': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            w = v;
            continue;
          }
          ctx.invalidValue('CT_Bevel', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'h': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            h = v;
            continue;
          }
          ctx.invalidValue('CT_Bevel', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'prst': {
          const v = parseST_BevelPresetType(a.value);
          if (v !== undefined) {
            prst = v;
            continue;
          }
          ctx.invalidValue('CT_Bevel', a, 'ST_BevelPresetType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Bevel', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Bevel', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Bevel', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Bevel', cur);
    cur.next();
  }
  return {
    w,
    h,
    prst,
    $unknownAttrs,
  };
}

/** Read a `CT_BiLevelEffect`. See `transitional/dml-main.xsd:1283`. */
export function readCT_BiLevelEffect(cur: XmlCursor, ctx: ReadContext): CT_BiLevelEffect {
  const start = requireStart(cur, 'CT_BiLevelEffect');
  let thresh: ST_PositiveFixedPercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'thresh': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            thresh = v;
            continue;
          }
          ctx.invalidValue('CT_BiLevelEffect', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BiLevelEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (thresh === undefined) ctx.missingRequired('CT_BiLevelEffect', 'thresh', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BiLevelEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_BiLevelEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BiLevelEffect', cur);
    cur.next();
  }
  return {
    thresh,
    $unknownAttrs,
  };
}

/** Read a `CT_BlendEffect`. See `transitional/dml-main.xsd:1687`. */
export function readCT_BlendEffect(cur: XmlCursor, ctx: ReadContext): CT_BlendEffect {
  const start = requireStart(cur, 'CT_BlendEffect');
  let blend: ST_BlendMode | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'blend': {
          const v = parseST_BlendMode(a.value);
          if (v !== undefined) {
            blend = v;
            continue;
          }
          ctx.invalidValue('CT_BlendEffect', a, 'ST_BlendMode', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BlendEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (blend === undefined) ctx.missingRequired('CT_BlendEffect', 'blend', cur);

  let cont: CT_EffectContainer | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BlendEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'cont': {
            cont = readCT_EffectContainer(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BlendEffect', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BlendEffect', cur);
    cur.next();
  }

  return {
    cont,
    blend,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Blip`. See `transitional/dml-main.xsd:1497`. */
export function readCT_Blip(cur: XmlCursor, ctx: ReadContext): CT_Blip {
  const start = requireStart(cur, 'CT_Blip');
  let embed: ST_RelationshipId | undefined;
  let link: ST_RelationshipId | undefined;
  let cstate: ST_BlipCompression | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'cstate': {
          const v = parseST_BlipCompression(a.value);
          if (v !== undefined) {
            cstate = v;
            continue;
          }
          ctx.invalidValue('CT_Blip', a, 'ST_BlipCompression', cur);
          break;
        }
      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'embed': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            embed = v;
            continue;
          }
          ctx.invalidValue('CT_Blip', a, 'ST_RelationshipId', cur);
          break;
        }
        case 'link': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            link = v;
            continue;
          }
          ctx.invalidValue('CT_Blip', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Blip', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_Blip_Content[] = [];
  let extLst: CT_OfficeArtExtensionList | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Blip', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'alphaBiLevel': {
            content.push({ kind: 'alphaBiLevel', value: readCT_AlphaBiLevelEffect(cur, ctx) });
            continue;
          }
          case 'alphaCeiling': {
            content.push({ kind: 'alphaCeiling', value: readCT_AlphaCeilingEffect(cur, ctx) });
            continue;
          }
          case 'alphaFloor': {
            content.push({ kind: 'alphaFloor', value: readCT_AlphaFloorEffect(cur, ctx) });
            continue;
          }
          case 'alphaInv': {
            content.push({ kind: 'alphaInv', value: readCT_AlphaInverseEffect(cur, ctx) });
            continue;
          }
          case 'alphaMod': {
            content.push({ kind: 'alphaMod', value: readCT_AlphaModulateEffect(cur, ctx) });
            continue;
          }
          case 'alphaModFix': {
            content.push({ kind: 'alphaModFix', value: readCT_AlphaModulateFixedEffect(cur, ctx) });
            continue;
          }
          case 'alphaRepl': {
            content.push({ kind: 'alphaRepl', value: readCT_AlphaReplaceEffect(cur, ctx) });
            continue;
          }
          case 'biLevel': {
            content.push({ kind: 'biLevel', value: readCT_BiLevelEffect(cur, ctx) });
            continue;
          }
          case 'blur': {
            content.push({ kind: 'blur', value: readCT_BlurEffect(cur, ctx) });
            continue;
          }
          case 'clrChange': {
            content.push({ kind: 'clrChange', value: readCT_ColorChangeEffect(cur, ctx) });
            continue;
          }
          case 'clrRepl': {
            content.push({ kind: 'clrRepl', value: readCT_ColorReplaceEffect(cur, ctx) });
            continue;
          }
          case 'duotone': {
            content.push({ kind: 'duotone', value: readCT_DuotoneEffect(cur, ctx) });
            continue;
          }
          case 'fillOverlay': {
            content.push({ kind: 'fillOverlay', value: readCT_FillOverlayEffect(cur, ctx) });
            continue;
          }
          case 'grayscl': {
            content.push({ kind: 'grayscl', value: readCT_GrayscaleEffect(cur, ctx) });
            continue;
          }
          case 'hsl': {
            content.push({ kind: 'hsl', value: readCT_HSLEffect(cur, ctx) });
            continue;
          }
          case 'lum': {
            content.push({ kind: 'lum', value: readCT_LuminanceEffect(cur, ctx) });
            continue;
          }
          case 'tint': {
            content.push({ kind: 'tint', value: readCT_TintEffect(cur, ctx) });
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Blip', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Blip', cur);
    cur.next();
  }

  return {
    content,
    extLst,
    embed,
    link,
    cstate,
    $unknownAttrs,
  };
}

/** Read a `CT_BlipFillProperties`. See `transitional/dml-main.xsd:1524`. */
export function readCT_BlipFillProperties(cur: XmlCursor, ctx: ReadContext): CT_BlipFillProperties {
  const start = requireStart(cur, 'CT_BlipFillProperties');
  let dpi: number | undefined;
  let rotWithShape: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'dpi': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            dpi = v;
            continue;
          }
          ctx.invalidValue('CT_BlipFillProperties', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'rotWithShape': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            rotWithShape = v;
            continue;
          }
          ctx.invalidValue('CT_BlipFillProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BlipFillProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let blip: CT_Blip | undefined;
  let srcRect: CT_RelativeRect | undefined;
  let fillModeProperties: CT_BlipFillProperties_FillModeProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BlipFillProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'blip': {
            blip = readCT_Blip(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srcRect': {
            srcRect = readCT_RelativeRect(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tile': {
            fillModeProperties = { kind: 'tile', value: readCT_TileInfoProperties(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'stretch': {
            fillModeProperties = { kind: 'stretch', value: readCT_StretchInfoProperties(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_BlipFillProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BlipFillProperties', cur);
    cur.next();
  }

  return {
    blip,
    srcRect,
    fillModeProperties,
    dpi,
    rotWithShape,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_BlurEffect`. See `transitional/dml-main.xsd:1286`. */
export function readCT_BlurEffect(cur: XmlCursor, ctx: ReadContext): CT_BlurEffect {
  const start = requireStart(cur, 'CT_BlurEffect');
  let rad: ST_PositiveCoordinate | undefined;
  let grow: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'rad': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            rad = v;
            continue;
          }
          ctx.invalidValue('CT_BlurEffect', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'grow': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            grow = v;
            continue;
          }
          ctx.invalidValue('CT_BlurEffect', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_BlurEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_BlurEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_BlurEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_BlurEffect', cur);
    cur.next();
  }
  return {
    rad,
    grow,
    $unknownAttrs,
  };
}

/** Read a `CT_Boolean`. See `transitional/dml-main.xsd:2944`. */
export function readCT_Boolean(cur: XmlCursor, ctx: ReadContext): CT_Boolean {
  const start = requireStart(cur, 'CT_Boolean');
  let val: ST_OnOff | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_OnOff(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Boolean', a, 'ST_OnOff', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Boolean', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Boolean', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Boolean', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Boolean', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Camera`. See `transitional/dml-main.xsd:1127`. */
export function readCT_Camera(cur: XmlCursor, ctx: ReadContext): CT_Camera {
  const start = requireStart(cur, 'CT_Camera');
  let prst: ST_PresetCameraType | undefined;
  let fov: ST_FOVAngle | undefined;
  let zoom: ST_PositivePercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'prst': {
          const v = parseST_PresetCameraType(a.value);
          if (v !== undefined) {
            prst = v;
            continue;
          }
          ctx.invalidValue('CT_Camera', a, 'ST_PresetCameraType', cur);
          break;
        }
        case 'fov': {
          const v = parseST_FOVAngle(a.value);
          if (v !== undefined) {
            fov = v;
            continue;
          }
          ctx.invalidValue('CT_Camera', a, 'ST_FOVAngle', cur);
          break;
        }
        case 'zoom': {
          const v = parseST_PositivePercentage(a.value);
          if (v !== undefined) {
            zoom = v;
            continue;
          }
          ctx.invalidValue('CT_Camera', a, 'ST_PositivePercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Camera', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (prst === undefined) ctx.missingRequired('CT_Camera', 'prst', cur);

  let rot: CT_SphereCoords | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Camera', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'rot': {
            rot = readCT_SphereCoords(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Camera', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Camera', cur);
    cur.next();
  }

  return {
    rot,
    prst,
    fov,
    zoom,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Cell3D`. See `transitional/dml-main.xsd:2453`. */
export function readCT_Cell3D(cur: XmlCursor, ctx: ReadContext): CT_Cell3D {
  const start = requireStart(cur, 'CT_Cell3D');
  let prstMaterial: ST_PresetMaterialType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'prstMaterial': {
          const v = parseST_PresetMaterialType(a.value);
          if (v !== undefined) {
            prstMaterial = v;
            continue;
          }
          ctx.invalidValue('CT_Cell3D', a, 'ST_PresetMaterialType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Cell3D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let bevel: CT_Bevel | undefined;
  let lightRig: CT_LightRig | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Cell3D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'bevel': {
            bevel = readCT_Bevel(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lightRig': {
            lightRig = readCT_LightRig(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Cell3D', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Cell3D', cur);
    cur.next();
  }

  return {
    bevel,
    lightRig,
    extLst,
    prstMaterial,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ClipboardStyleSheet`. See `transitional/dml-main.xsd:2360`. */
export function readCT_ClipboardStyleSheet(cur: XmlCursor, ctx: ReadContext): CT_ClipboardStyleSheet {
  const start = requireStart(cur, 'CT_ClipboardStyleSheet');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ClipboardStyleSheet', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let themeElements: CT_BaseStyles | undefined;
  let clrMap: CT_ColorMapping | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ClipboardStyleSheet', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'themeElements': {
            themeElements = readCT_BaseStyles(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'clrMap': {
            clrMap = readCT_ColorMapping(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ClipboardStyleSheet', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ClipboardStyleSheet', cur);
    cur.next();
  }

  return {
    themeElements,
    clrMap,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Color`. See `transitional/dml-main.xsd:699`. */
export function readCT_Color(cur: XmlCursor, ctx: ReadContext): CT_Color {
  const start = requireStart(cur, 'CT_Color');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Color', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let colorChoice: CT_Color_ColorChoice | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Color', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Color', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Color', cur);
    cur.next();
  }

  return {
    colorChoice,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorChangeEffect`. See `transitional/dml-main.xsd:1290`. */
export function readCT_ColorChangeEffect(cur: XmlCursor, ctx: ReadContext): CT_ColorChangeEffect {
  const start = requireStart(cur, 'CT_ColorChangeEffect');
  let useA: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'useA': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            useA = v;
            continue;
          }
          ctx.invalidValue('CT_ColorChangeEffect', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ColorChangeEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let clrFrom: CT_Color | undefined;
  let clrTo: CT_Color | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorChangeEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'clrFrom': {
            clrFrom = readCT_Color(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'clrTo': {
            clrTo = readCT_Color(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ColorChangeEffect', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorChangeEffect', cur);
    cur.next();
  }

  return {
    clrFrom,
    clrTo,
    useA,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorMRU`. See `transitional/dml-main.xsd:704`. */
export function readCT_ColorMRU(cur: XmlCursor, ctx: ReadContext): CT_ColorMRU {
  const start = requireStart(cur, 'CT_ColorMRU');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ColorMRU', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const colorChoice: CT_ColorMRU_ColorChoice[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorMRU', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice.push({ kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) });
            continue;
          }
          case 'srgbClr': {
            colorChoice.push({ kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) });
            continue;
          }
          case 'hslClr': {
            colorChoice.push({ kind: 'hslClr', value: readCT_HslColor(cur, ctx) });
            continue;
          }
          case 'sysClr': {
            colorChoice.push({ kind: 'sysClr', value: readCT_SystemColor(cur, ctx) });
            continue;
          }
          case 'schemeClr': {
            colorChoice.push({ kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) });
            continue;
          }
          case 'prstClr': {
            colorChoice.push({ kind: 'prstClr', value: readCT_PresetColor(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ColorMRU', ev.localName, ev.uri, cur);
      colorChoice.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorMRU', cur);
    cur.next();
  }

  return {
    colorChoice,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorMapping`. See `transitional/dml-main.xsd:2306`. */
export function readCT_ColorMapping(cur: XmlCursor, ctx: ReadContext): CT_ColorMapping {
  const start = requireStart(cur, 'CT_ColorMapping');
  let bg1: ST_ColorSchemeIndex | undefined;
  let tx1: ST_ColorSchemeIndex | undefined;
  let bg2: ST_ColorSchemeIndex | undefined;
  let tx2: ST_ColorSchemeIndex | undefined;
  let accent1: ST_ColorSchemeIndex | undefined;
  let accent2: ST_ColorSchemeIndex | undefined;
  let accent3: ST_ColorSchemeIndex | undefined;
  let accent4: ST_ColorSchemeIndex | undefined;
  let accent5: ST_ColorSchemeIndex | undefined;
  let accent6: ST_ColorSchemeIndex | undefined;
  let hlink: ST_ColorSchemeIndex | undefined;
  let folHlink: ST_ColorSchemeIndex | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'bg1': {
          const v = parseST_ColorSchemeIndex(a.value);
          if (v !== undefined) {
            bg1 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMapping', a, 'ST_ColorSchemeIndex', cur);
          break;
        }
        case 'tx1': {
          const v = parseST_ColorSchemeIndex(a.value);
          if (v !== undefined) {
            tx1 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMapping', a, 'ST_ColorSchemeIndex', cur);
          break;
        }
        case 'bg2': {
          const v = parseST_ColorSchemeIndex(a.value);
          if (v !== undefined) {
            bg2 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMapping', a, 'ST_ColorSchemeIndex', cur);
          break;
        }
        case 'tx2': {
          const v = parseST_ColorSchemeIndex(a.value);
          if (v !== undefined) {
            tx2 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMapping', a, 'ST_ColorSchemeIndex', cur);
          break;
        }
        case 'accent1': {
          const v = parseST_ColorSchemeIndex(a.value);
          if (v !== undefined) {
            accent1 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMapping', a, 'ST_ColorSchemeIndex', cur);
          break;
        }
        case 'accent2': {
          const v = parseST_ColorSchemeIndex(a.value);
          if (v !== undefined) {
            accent2 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMapping', a, 'ST_ColorSchemeIndex', cur);
          break;
        }
        case 'accent3': {
          const v = parseST_ColorSchemeIndex(a.value);
          if (v !== undefined) {
            accent3 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMapping', a, 'ST_ColorSchemeIndex', cur);
          break;
        }
        case 'accent4': {
          const v = parseST_ColorSchemeIndex(a.value);
          if (v !== undefined) {
            accent4 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMapping', a, 'ST_ColorSchemeIndex', cur);
          break;
        }
        case 'accent5': {
          const v = parseST_ColorSchemeIndex(a.value);
          if (v !== undefined) {
            accent5 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMapping', a, 'ST_ColorSchemeIndex', cur);
          break;
        }
        case 'accent6': {
          const v = parseST_ColorSchemeIndex(a.value);
          if (v !== undefined) {
            accent6 = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMapping', a, 'ST_ColorSchemeIndex', cur);
          break;
        }
        case 'hlink': {
          const v = parseST_ColorSchemeIndex(a.value);
          if (v !== undefined) {
            hlink = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMapping', a, 'ST_ColorSchemeIndex', cur);
          break;
        }
        case 'folHlink': {
          const v = parseST_ColorSchemeIndex(a.value);
          if (v !== undefined) {
            folHlink = v;
            continue;
          }
          ctx.invalidValue('CT_ColorMapping', a, 'ST_ColorSchemeIndex', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ColorMapping', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (bg1 === undefined) ctx.missingRequired('CT_ColorMapping', 'bg1', cur);
  if (tx1 === undefined) ctx.missingRequired('CT_ColorMapping', 'tx1', cur);
  if (bg2 === undefined) ctx.missingRequired('CT_ColorMapping', 'bg2', cur);
  if (tx2 === undefined) ctx.missingRequired('CT_ColorMapping', 'tx2', cur);
  if (accent1 === undefined) ctx.missingRequired('CT_ColorMapping', 'accent1', cur);
  if (accent2 === undefined) ctx.missingRequired('CT_ColorMapping', 'accent2', cur);
  if (accent3 === undefined) ctx.missingRequired('CT_ColorMapping', 'accent3', cur);
  if (accent4 === undefined) ctx.missingRequired('CT_ColorMapping', 'accent4', cur);
  if (accent5 === undefined) ctx.missingRequired('CT_ColorMapping', 'accent5', cur);
  if (accent6 === undefined) ctx.missingRequired('CT_ColorMapping', 'accent6', cur);
  if (hlink === undefined) ctx.missingRequired('CT_ColorMapping', 'hlink', cur);
  if (folHlink === undefined) ctx.missingRequired('CT_ColorMapping', 'folHlink', cur);

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorMapping', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ColorMapping', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorMapping', cur);
    cur.next();
  }

  return {
    extLst,
    bg1,
    tx1,
    bg2,
    tx2,
    accent1,
    accent2,
    accent3,
    accent4,
    accent5,
    accent6,
    hlink,
    folHlink,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorMappingOverride`. See `transitional/dml-main.xsd:2323`. */
export function readCT_ColorMappingOverride(cur: XmlCursor, ctx: ReadContext): CT_ColorMappingOverride {
  const start = requireStart(cur, 'CT_ColorMappingOverride');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ColorMappingOverride', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_ColorMappingOverride_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorMappingOverride', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'masterClrMapping': {
            content = { kind: 'masterClrMapping', value: readCT_EmptyElement(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'overrideClrMapping': {
            content = { kind: 'overrideClrMapping', value: readCT_ColorMapping(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ColorMappingOverride', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorMappingOverride', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorReplaceEffect`. See `transitional/dml-main.xsd:1297`. */
export function readCT_ColorReplaceEffect(cur: XmlCursor, ctx: ReadContext): CT_ColorReplaceEffect {
  const start = requireStart(cur, 'CT_ColorReplaceEffect');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ColorReplaceEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let colorChoice: CT_ColorReplaceEffect_ColorChoice | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorReplaceEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ColorReplaceEffect', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorReplaceEffect', cur);
    cur.next();
  }

  return {
    colorChoice,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorScheme`. See `transitional/dml-main.xsd:87`. */
export function readCT_ColorScheme(cur: XmlCursor, ctx: ReadContext): CT_ColorScheme {
  const start = requireStart(cur, 'CT_ColorScheme');
  let name: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_ColorScheme', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_ColorScheme', 'name', cur);

  let dk1: CT_Color | undefined;
  let lt1: CT_Color | undefined;
  let dk2: CT_Color | undefined;
  let lt2: CT_Color | undefined;
  let accent1: CT_Color | undefined;
  let accent2: CT_Color | undefined;
  let accent3: CT_Color | undefined;
  let accent4: CT_Color | undefined;
  let accent5: CT_Color | undefined;
  let accent6: CT_Color | undefined;
  let hlink: CT_Color | undefined;
  let folHlink: CT_Color | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorScheme', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'dk1': {
            dk1 = readCT_Color(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lt1': {
            lt1 = readCT_Color(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'dk2': {
            dk2 = readCT_Color(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'lt2': {
            lt2 = readCT_Color(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'accent1': {
            accent1 = readCT_Color(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'accent2': {
            accent2 = readCT_Color(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'accent3': {
            accent3 = readCT_Color(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'accent4': {
            accent4 = readCT_Color(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'accent5': {
            accent5 = readCT_Color(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'accent6': {
            accent6 = readCT_Color(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'hlink': {
            hlink = readCT_Color(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'folHlink': {
            folHlink = readCT_Color(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ColorScheme', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorScheme', cur);
    cur.next();
  }

  return {
    dk1,
    lt1,
    dk2,
    lt2,
    accent1,
    accent2,
    accent3,
    accent4,
    accent5,
    accent6,
    hlink,
    folHlink,
    extLst,
    name,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorSchemeAndMapping`. See `transitional/dml-main.xsd:2331`. */
export function readCT_ColorSchemeAndMapping(cur: XmlCursor, ctx: ReadContext): CT_ColorSchemeAndMapping {
  const start = requireStart(cur, 'CT_ColorSchemeAndMapping');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ColorSchemeAndMapping', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let clrScheme: CT_ColorScheme | undefined;
  let clrMap: CT_ColorMapping | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorSchemeAndMapping', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'clrScheme': {
            clrScheme = readCT_ColorScheme(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'clrMap': {
            clrMap = readCT_ColorMapping(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ColorSchemeAndMapping', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorSchemeAndMapping', cur);
    cur.next();
  }

  return {
    clrScheme,
    clrMap,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ColorSchemeList`. See `transitional/dml-main.xsd:2337`. */
export function readCT_ColorSchemeList(cur: XmlCursor, ctx: ReadContext): CT_ColorSchemeList {
  const start = requireStart(cur, 'CT_ColorSchemeList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ColorSchemeList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const extraClrScheme: CT_ColorSchemeAndMapping[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ColorSchemeList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'extraClrScheme': {
            extraClrScheme.push(readCT_ColorSchemeAndMapping(cur, ctx));
            $slot = 0;
            $index = extraClrScheme.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ColorSchemeList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ColorSchemeList', cur);
    cur.next();
  }

  return {
    extraClrScheme,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ComplementTransform`. See `transitional/dml-main.xsd:292`. */
export function readCT_ComplementTransform(cur: XmlCursor, ctx: ReadContext): CT_ComplementTransform {
  const start = requireStart(cur, 'CT_ComplementTransform');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ComplementTransform', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ComplementTransform', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ComplementTransform', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ComplementTransform', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_Connection`. See `transitional/dml-main.xsd:2023`. */
export function readCT_Connection(cur: XmlCursor, ctx: ReadContext): CT_Connection {
  const start = requireStart(cur, 'CT_Connection');
  let id: ST_DrawingElementId | undefined;
  let idx: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DrawingElementId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Connection', a, 'ST_DrawingElementId', cur);
          break;
        }
        case 'idx': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            idx = v;
            continue;
          }
          ctx.invalidValue('CT_Connection', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Connection', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_Connection', 'id', cur);
  if (idx === undefined) ctx.missingRequired('CT_Connection', 'idx', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Connection', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Connection', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Connection', cur);
    cur.next();
  }
  return {
    id,
    idx,
    $unknownAttrs,
  };
}

/** Read a `CT_ConnectionSite`. See `transitional/dml-main.xsd:2006`. */
export function readCT_ConnectionSite(cur: XmlCursor, ctx: ReadContext): CT_ConnectionSite {
  const start = requireStart(cur, 'CT_ConnectionSite');
  let ang: ST_AdjAngle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'ang': {
          const v = parseST_AdjAngle(a.value);
          if (v !== undefined) {
            ang = v;
            continue;
          }
          ctx.invalidValue('CT_ConnectionSite', a, 'ST_AdjAngle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ConnectionSite', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (ang === undefined) ctx.missingRequired('CT_ConnectionSite', 'ang', cur);

  let pos: CT_AdjPoint2D | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ConnectionSite', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'pos': {
            pos = readCT_AdjPoint2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ConnectionSite', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ConnectionSite', cur);
    cur.next();
  }

  return {
    pos,
    ang,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ConnectionSiteList`. See `transitional/dml-main.xsd:2018`. */
export function readCT_ConnectionSiteList(cur: XmlCursor, ctx: ReadContext): CT_ConnectionSiteList {
  const start = requireStart(cur, 'CT_ConnectionSiteList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ConnectionSiteList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const cxn: CT_ConnectionSite[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ConnectionSiteList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'cxn': {
            cxn.push(readCT_ConnectionSite(cur, ctx));
            $slot = 0;
            $index = cxn.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ConnectionSiteList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ConnectionSiteList', cur);
    cur.next();
  }

  return {
    cxn,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ConnectorLocking`. See `transitional/dml-main.xsd:761`. */
export function readCT_ConnectorLocking(cur: XmlCursor, ctx: ReadContext): CT_ConnectorLocking {
  const start = requireStart(cur, 'CT_ConnectorLocking');
  let noGrp: boolean | undefined;
  let noSelect: boolean | undefined;
  let noRot: boolean | undefined;
  let noChangeAspect: boolean | undefined;
  let noMove: boolean | undefined;
  let noResize: boolean | undefined;
  let noEditPoints: boolean | undefined;
  let noAdjustHandles: boolean | undefined;
  let noChangeArrowheads: boolean | undefined;
  let noChangeShapeType: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'noGrp': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noGrp = v;
            continue;
          }
          ctx.invalidValue('CT_ConnectorLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noSelect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noSelect = v;
            continue;
          }
          ctx.invalidValue('CT_ConnectorLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noRot': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noRot = v;
            continue;
          }
          ctx.invalidValue('CT_ConnectorLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeAspect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeAspect = v;
            continue;
          }
          ctx.invalidValue('CT_ConnectorLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noMove': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noMove = v;
            continue;
          }
          ctx.invalidValue('CT_ConnectorLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noResize': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noResize = v;
            continue;
          }
          ctx.invalidValue('CT_ConnectorLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noEditPoints': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noEditPoints = v;
            continue;
          }
          ctx.invalidValue('CT_ConnectorLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noAdjustHandles': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noAdjustHandles = v;
            continue;
          }
          ctx.invalidValue('CT_ConnectorLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeArrowheads': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeArrowheads = v;
            continue;
          }
          ctx.invalidValue('CT_ConnectorLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeShapeType': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeShapeType = v;
            continue;
          }
          ctx.invalidValue('CT_ConnectorLocking', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ConnectorLocking', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ConnectorLocking', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ConnectorLocking', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ConnectorLocking', cur);
    cur.next();
  }

  return {
    extLst,
    noGrp,
    noSelect,
    noRot,
    noChangeAspect,
    noMove,
    noResize,
    noEditPoints,
    noAdjustHandles,
    noChangeArrowheads,
    noChangeShapeType,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ContentPartLocking`. See `transitional/dml-main.xsd:804`. */
export function readCT_ContentPartLocking(cur: XmlCursor, ctx: ReadContext): CT_ContentPartLocking {
  const start = requireStart(cur, 'CT_ContentPartLocking');
  let noGrp: boolean | undefined;
  let noSelect: boolean | undefined;
  let noRot: boolean | undefined;
  let noChangeAspect: boolean | undefined;
  let noMove: boolean | undefined;
  let noResize: boolean | undefined;
  let noEditPoints: boolean | undefined;
  let noAdjustHandles: boolean | undefined;
  let noChangeArrowheads: boolean | undefined;
  let noChangeShapeType: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'noGrp': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noGrp = v;
            continue;
          }
          ctx.invalidValue('CT_ContentPartLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noSelect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noSelect = v;
            continue;
          }
          ctx.invalidValue('CT_ContentPartLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noRot': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noRot = v;
            continue;
          }
          ctx.invalidValue('CT_ContentPartLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeAspect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeAspect = v;
            continue;
          }
          ctx.invalidValue('CT_ContentPartLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noMove': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noMove = v;
            continue;
          }
          ctx.invalidValue('CT_ContentPartLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noResize': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noResize = v;
            continue;
          }
          ctx.invalidValue('CT_ContentPartLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noEditPoints': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noEditPoints = v;
            continue;
          }
          ctx.invalidValue('CT_ContentPartLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noAdjustHandles': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noAdjustHandles = v;
            continue;
          }
          ctx.invalidValue('CT_ContentPartLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeArrowheads': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeArrowheads = v;
            continue;
          }
          ctx.invalidValue('CT_ContentPartLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeShapeType': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeShapeType = v;
            continue;
          }
          ctx.invalidValue('CT_ContentPartLocking', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ContentPartLocking', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ContentPartLocking', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ContentPartLocking', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ContentPartLocking', cur);
    cur.next();
  }

  return {
    extLst,
    noGrp,
    noSelect,
    noRot,
    noChangeAspect,
    noMove,
    noResize,
    noEditPoints,
    noAdjustHandles,
    noChangeArrowheads,
    noChangeShapeType,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomColor`. See `transitional/dml-main.xsd:105`. */
export function readCT_CustomColor(cur: XmlCursor, ctx: ReadContext): CT_CustomColor {
  const start = requireStart(cur, 'CT_CustomColor');
  let name: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_CustomColor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let colorChoice: CT_CustomColor_ColorChoice | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomColor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CustomColor', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomColor', cur);
    cur.next();
  }

  return {
    colorChoice,
    name,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomColorList`. See `transitional/dml-main.xsd:115`. */
export function readCT_CustomColorList(cur: XmlCursor, ctx: ReadContext): CT_CustomColorList {
  const start = requireStart(cur, 'CT_CustomColorList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_CustomColorList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const custClr: CT_CustomColor[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomColorList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'custClr': {
            custClr.push(readCT_CustomColor(cur, ctx));
            $slot = 0;
            $index = custClr.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CustomColorList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomColorList', cur);
    cur.next();
  }

  return {
    custClr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_CustomGeometry2D`. See `transitional/dml-main.xsd:2096`. */
export function readCT_CustomGeometry2D(cur: XmlCursor, ctx: ReadContext): CT_CustomGeometry2D {
  const start = requireStart(cur, 'CT_CustomGeometry2D');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_CustomGeometry2D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let avLst: CT_GeomGuideList | undefined;
  let gdLst: CT_GeomGuideList | undefined;
  let ahLst: CT_AdjustHandleList | undefined;
  let cxnLst: CT_ConnectionSiteList | undefined;
  let rect: CT_GeomRect | undefined;
  let pathLst: CT_Path2DList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_CustomGeometry2D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'avLst': {
            avLst = readCT_GeomGuideList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'gdLst': {
            gdLst = readCT_GeomGuideList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'ahLst': {
            ahLst = readCT_AdjustHandleList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'cxnLst': {
            cxnLst = readCT_ConnectionSiteList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'rect': {
            rect = readCT_GeomRect(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'pathLst': {
            pathLst = readCT_Path2DList(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_CustomGeometry2D', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_CustomGeometry2D', cur);
    cur.next();
  }

  return {
    avLst,
    gdLst,
    ahLst,
    cxnLst,
    rect,
    pathLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DashStop`. See `transitional/dml-main.xsd:2185`. */
export function readCT_DashStop(cur: XmlCursor, ctx: ReadContext): CT_DashStop {
  const start = requireStart(cur, 'CT_DashStop');
  let d: ST_PositivePercentage | undefined;
  let sp: ST_PositivePercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'd': {
          const v = parseST_PositivePercentage(a.value);
          if (v !== undefined) {
            d = v;
            continue;
          }
          ctx.invalidValue('CT_DashStop', a, 'ST_PositivePercentage', cur);
          break;
        }
        case 'sp': {
          const v = parseST_PositivePercentage(a.value);
          if (v !== undefined) {
            sp = v;
            continue;
          }
          ctx.invalidValue('CT_DashStop', a, 'ST_PositivePercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_DashStop', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (d === undefined) ctx.missingRequired('CT_DashStop', 'd', cur);
  if (sp === undefined) ctx.missingRequired('CT_DashStop', 'sp', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DashStop', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_DashStop', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DashStop', cur);
    cur.next();
  }
  return {
    d,
    sp,
    $unknownAttrs,
  };
}

/** Read a `CT_DashStopList`. See `transitional/dml-main.xsd:2189`. */
export function readCT_DashStopList(cur: XmlCursor, ctx: ReadContext): CT_DashStopList {
  const start = requireStart(cur, 'CT_DashStopList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DashStopList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const ds: CT_DashStop[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DashStopList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'ds': {
            ds.push(readCT_DashStop(cur, ctx));
            $slot = 0;
            $index = ds.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DashStopList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DashStopList', cur);
    cur.next();
  }

  return {
    ds,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DefaultShapeDefinition`. See `transitional/dml-main.xsd:2288`. */
export function readCT_DefaultShapeDefinition(cur: XmlCursor, ctx: ReadContext): CT_DefaultShapeDefinition {
  const start = requireStart(cur, 'CT_DefaultShapeDefinition');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DefaultShapeDefinition', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let spPr: CT_ShapeProperties | undefined;
  let bodyPr: CT_TextBodyProperties | undefined;
  let lstStyle: CT_TextListStyle | undefined;
  let style: CT_ShapeStyle | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DefaultShapeDefinition', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'bodyPr': {
            bodyPr = readCT_TextBodyProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'lstStyle': {
            lstStyle = readCT_TextListStyle(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DefaultShapeDefinition', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DefaultShapeDefinition', cur);
    cur.next();
  }

  return {
    spPr,
    bodyPr,
    lstStyle,
    style,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_DuotoneEffect`. See `transitional/dml-main.xsd:1302`. */
export function readCT_DuotoneEffect(cur: XmlCursor, ctx: ReadContext): CT_DuotoneEffect {
  const start = requireStart(cur, 'CT_DuotoneEffect');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_DuotoneEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const colorChoice: CT_DuotoneEffect_ColorChoice[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_DuotoneEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice.push({ kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) });
            continue;
          }
          case 'srgbClr': {
            colorChoice.push({ kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) });
            continue;
          }
          case 'hslClr': {
            colorChoice.push({ kind: 'hslClr', value: readCT_HslColor(cur, ctx) });
            continue;
          }
          case 'sysClr': {
            colorChoice.push({ kind: 'sysClr', value: readCT_SystemColor(cur, ctx) });
            continue;
          }
          case 'schemeClr': {
            colorChoice.push({ kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) });
            continue;
          }
          case 'prstClr': {
            colorChoice.push({ kind: 'prstClr', value: readCT_PresetColor(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_DuotoneEffect', ev.localName, ev.uri, cur);
      colorChoice.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_DuotoneEffect', cur);
    cur.next();
  }

  return {
    colorChoice,
    $unknownAttrs,
  };
}

/** Read a `CT_EffectContainer`. See `transitional/dml-main.xsd:1677`. */
export function readCT_EffectContainer(cur: XmlCursor, ctx: ReadContext): CT_EffectContainer {
  const start = requireStart(cur, 'CT_EffectContainer');
  let type: ST_EffectContainerType | undefined;
  let name: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_EffectContainerType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_EffectContainer', a, 'ST_EffectContainerType', cur);
          break;
        }
        case 'name':
          name = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_EffectContainer', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const effect: CT_EffectContainer_Effect[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EffectContainer', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'cont': {
            effect.push({ kind: 'cont', value: readCT_EffectContainer(cur, ctx) });
            continue;
          }
          case 'effect': {
            effect.push({ kind: 'effect', value: readCT_EffectReference(cur, ctx) });
            continue;
          }
          case 'alphaBiLevel': {
            effect.push({ kind: 'alphaBiLevel', value: readCT_AlphaBiLevelEffect(cur, ctx) });
            continue;
          }
          case 'alphaCeiling': {
            effect.push({ kind: 'alphaCeiling', value: readCT_AlphaCeilingEffect(cur, ctx) });
            continue;
          }
          case 'alphaFloor': {
            effect.push({ kind: 'alphaFloor', value: readCT_AlphaFloorEffect(cur, ctx) });
            continue;
          }
          case 'alphaInv': {
            effect.push({ kind: 'alphaInv', value: readCT_AlphaInverseEffect(cur, ctx) });
            continue;
          }
          case 'alphaMod': {
            effect.push({ kind: 'alphaMod', value: readCT_AlphaModulateEffect(cur, ctx) });
            continue;
          }
          case 'alphaModFix': {
            effect.push({ kind: 'alphaModFix', value: readCT_AlphaModulateFixedEffect(cur, ctx) });
            continue;
          }
          case 'alphaOutset': {
            effect.push({ kind: 'alphaOutset', value: readCT_AlphaOutsetEffect(cur, ctx) });
            continue;
          }
          case 'alphaRepl': {
            effect.push({ kind: 'alphaRepl', value: readCT_AlphaReplaceEffect(cur, ctx) });
            continue;
          }
          case 'biLevel': {
            effect.push({ kind: 'biLevel', value: readCT_BiLevelEffect(cur, ctx) });
            continue;
          }
          case 'blend': {
            effect.push({ kind: 'blend', value: readCT_BlendEffect(cur, ctx) });
            continue;
          }
          case 'blur': {
            effect.push({ kind: 'blur', value: readCT_BlurEffect(cur, ctx) });
            continue;
          }
          case 'clrChange': {
            effect.push({ kind: 'clrChange', value: readCT_ColorChangeEffect(cur, ctx) });
            continue;
          }
          case 'clrRepl': {
            effect.push({ kind: 'clrRepl', value: readCT_ColorReplaceEffect(cur, ctx) });
            continue;
          }
          case 'duotone': {
            effect.push({ kind: 'duotone', value: readCT_DuotoneEffect(cur, ctx) });
            continue;
          }
          case 'fill': {
            effect.push({ kind: 'fill', value: readCT_FillEffect(cur, ctx) });
            continue;
          }
          case 'fillOverlay': {
            effect.push({ kind: 'fillOverlay', value: readCT_FillOverlayEffect(cur, ctx) });
            continue;
          }
          case 'glow': {
            effect.push({ kind: 'glow', value: readCT_GlowEffect(cur, ctx) });
            continue;
          }
          case 'grayscl': {
            effect.push({ kind: 'grayscl', value: readCT_GrayscaleEffect(cur, ctx) });
            continue;
          }
          case 'hsl': {
            effect.push({ kind: 'hsl', value: readCT_HSLEffect(cur, ctx) });
            continue;
          }
          case 'innerShdw': {
            effect.push({ kind: 'innerShdw', value: readCT_InnerShadowEffect(cur, ctx) });
            continue;
          }
          case 'lum': {
            effect.push({ kind: 'lum', value: readCT_LuminanceEffect(cur, ctx) });
            continue;
          }
          case 'outerShdw': {
            effect.push({ kind: 'outerShdw', value: readCT_OuterShadowEffect(cur, ctx) });
            continue;
          }
          case 'prstShdw': {
            effect.push({ kind: 'prstShdw', value: readCT_PresetShadowEffect(cur, ctx) });
            continue;
          }
          case 'reflection': {
            effect.push({ kind: 'reflection', value: readCT_ReflectionEffect(cur, ctx) });
            continue;
          }
          case 'relOff': {
            effect.push({ kind: 'relOff', value: readCT_RelativeOffsetEffect(cur, ctx) });
            continue;
          }
          case 'softEdge': {
            effect.push({ kind: 'softEdge', value: readCT_SoftEdgesEffect(cur, ctx) });
            continue;
          }
          case 'tint': {
            effect.push({ kind: 'tint', value: readCT_TintEffect(cur, ctx) });
            continue;
          }
          case 'xfrm': {
            effect.push({ kind: 'xfrm', value: readCT_TransformEffect(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_EffectContainer', ev.localName, ev.uri, cur);
      effect.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EffectContainer', cur);
    cur.next();
  }

  return {
    effect,
    type,
    name,
    $unknownAttrs,
  };
}

/** Read a `CT_EffectList`. See `transitional/dml-main.xsd:1693`. */
export function readCT_EffectList(cur: XmlCursor, ctx: ReadContext): CT_EffectList {
  const start = requireStart(cur, 'CT_EffectList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_EffectList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let blur: CT_BlurEffect | undefined;
  let fillOverlay: CT_FillOverlayEffect | undefined;
  let glow: CT_GlowEffect | undefined;
  let innerShdw: CT_InnerShadowEffect | undefined;
  let outerShdw: CT_OuterShadowEffect | undefined;
  let prstShdw: CT_PresetShadowEffect | undefined;
  let reflection: CT_ReflectionEffect | undefined;
  let softEdge: CT_SoftEdgesEffect | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EffectList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'blur': {
            blur = readCT_BlurEffect(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'fillOverlay': {
            fillOverlay = readCT_FillOverlayEffect(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'glow': {
            glow = readCT_GlowEffect(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'innerShdw': {
            innerShdw = readCT_InnerShadowEffect(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'outerShdw': {
            outerShdw = readCT_OuterShadowEffect(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'prstShdw': {
            prstShdw = readCT_PresetShadowEffect(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'reflection': {
            reflection = readCT_ReflectionEffect(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'softEdge': {
            softEdge = readCT_SoftEdgesEffect(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_EffectList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EffectList', cur);
    cur.next();
  }

  return {
    blur,
    fillOverlay,
    glow,
    innerShdw,
    outerShdw,
    prstShdw,
    reflection,
    softEdge,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_EffectProperties`. See `transitional/dml-main.xsd:1711`. */
export function readCT_EffectProperties(cur: XmlCursor, ctx: ReadContext): CT_EffectProperties {
  const start = requireStart(cur, 'CT_EffectProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_EffectProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let effectProperties: CT_EffectProperties_EffectProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EffectProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'effectLst': {
            effectProperties = { kind: 'effectLst', value: readCT_EffectList(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'effectDag': {
            effectProperties = { kind: 'effectDag', value: readCT_EffectContainer(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_EffectProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EffectProperties', cur);
    cur.next();
  }

  return {
    effectProperties,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_EffectReference`. See `transitional/dml-main.xsd:1634`. */
export function readCT_EffectReference(cur: XmlCursor, ctx: ReadContext): CT_EffectReference {
  const start = requireStart(cur, 'CT_EffectReference');
  let ref: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'ref':
          ref = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_EffectReference', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (ref === undefined) ctx.missingRequired('CT_EffectReference', 'ref', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EffectReference', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_EffectReference', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EffectReference', cur);
    cur.next();
  }
  return {
    ref,
    $unknownAttrs,
  };
}

/** Read a `CT_EffectStyleItem`. See `transitional/dml-main.xsd:129`. */
export function readCT_EffectStyleItem(cur: XmlCursor, ctx: ReadContext): CT_EffectStyleItem {
  const start = requireStart(cur, 'CT_EffectStyleItem');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_EffectStyleItem', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let effectProperties: CT_EffectStyleItem_EffectProperties | undefined;
  let scene3d: CT_Scene3D | undefined;
  let sp3d: CT_Shape3D | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EffectStyleItem', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'effectLst': {
            effectProperties = { kind: 'effectLst', value: readCT_EffectList(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'effectDag': {
            effectProperties = { kind: 'effectDag', value: readCT_EffectContainer(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'scene3d': {
            scene3d = readCT_Scene3D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sp3d': {
            sp3d = readCT_Shape3D(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_EffectStyleItem', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EffectStyleItem', cur);
    cur.next();
  }

  return {
    effectProperties,
    scene3d,
    sp3d,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_EffectStyleList`. See `transitional/dml-main.xsd:154`. */
export function readCT_EffectStyleList(cur: XmlCursor, ctx: ReadContext): CT_EffectStyleList {
  const start = requireStart(cur, 'CT_EffectStyleList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_EffectStyleList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const effectStyle: CT_EffectStyleItem[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EffectStyleList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'effectStyle': {
            effectStyle.push(readCT_EffectStyleItem(cur, ctx));
            $slot = 0;
            $index = effectStyle.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_EffectStyleList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EffectStyleList', cur);
    cur.next();
  }

  return {
    effectStyle,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_EmbeddedWAVAudioFile`. See `transitional/dml-main.xsd:728`. */
export function readCT_EmbeddedWAVAudioFile(cur: XmlCursor, ctx: ReadContext): CT_EmbeddedWAVAudioFile {
  const start = requireStart(cur, 'CT_EmbeddedWAVAudioFile');
  let embed: ST_RelationshipId | undefined;
  let name: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'embed': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            embed = v;
            continue;
          }
          ctx.invalidValue('CT_EmbeddedWAVAudioFile', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_EmbeddedWAVAudioFile', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (embed === undefined) ctx.missingRequired('CT_EmbeddedWAVAudioFile', 'embed', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EmbeddedWAVAudioFile', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_EmbeddedWAVAudioFile', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EmbeddedWAVAudioFile', cur);
    cur.next();
  }
  return {
    embed,
    name,
    $unknownAttrs,
  };
}

/** Read a `CT_EmptyElement`. See `transitional/dml-main.xsd:2305`. */
export function readCT_EmptyElement(cur: XmlCursor, ctx: ReadContext): CT_EmptyElement {
  const start = requireStart(cur, 'CT_EmptyElement');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_EmptyElement', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EmptyElement', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_EmptyElement', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EmptyElement', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_FillEffect`. See `transitional/dml-main.xsd:1614`. */
export function readCT_FillEffect(cur: XmlCursor, ctx: ReadContext): CT_FillEffect {
  const start = requireStart(cur, 'CT_FillEffect');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FillEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let fillProperties: CT_FillEffect_FillProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FillEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'noFill': {
            fillProperties = { kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'solidFill': {
            fillProperties = { kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'gradFill': {
            fillProperties = { kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            fillProperties = { kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pattFill': {
            fillProperties = { kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'grpFill': {
            fillProperties = { kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FillEffect', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FillEffect', cur);
    cur.next();
  }

  return {
    fillProperties,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FillOverlayEffect`. See `transitional/dml-main.xsd:1628`. */
export function readCT_FillOverlayEffect(cur: XmlCursor, ctx: ReadContext): CT_FillOverlayEffect {
  const start = requireStart(cur, 'CT_FillOverlayEffect');
  let blend: ST_BlendMode | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'blend': {
          const v = parseST_BlendMode(a.value);
          if (v !== undefined) {
            blend = v;
            continue;
          }
          ctx.invalidValue('CT_FillOverlayEffect', a, 'ST_BlendMode', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FillOverlayEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (blend === undefined) ctx.missingRequired('CT_FillOverlayEffect', 'blend', cur);

  let fillProperties: CT_FillOverlayEffect_FillProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FillOverlayEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'noFill': {
            fillProperties = { kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'solidFill': {
            fillProperties = { kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'gradFill': {
            fillProperties = { kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            fillProperties = { kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pattFill': {
            fillProperties = { kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'grpFill': {
            fillProperties = { kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FillOverlayEffect', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FillOverlayEffect', cur);
    cur.next();
  }

  return {
    fillProperties,
    blend,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FillProperties`. See `transitional/dml-main.xsd:1609`. */
export function readCT_FillProperties(cur: XmlCursor, ctx: ReadContext): CT_FillProperties {
  const start = requireStart(cur, 'CT_FillProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FillProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let fillProperties: CT_FillProperties_FillProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FillProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'noFill': {
            fillProperties = { kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'solidFill': {
            fillProperties = { kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'gradFill': {
            fillProperties = { kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            fillProperties = { kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pattFill': {
            fillProperties = { kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'grpFill': {
            fillProperties = { kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FillProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FillProperties', cur);
    cur.next();
  }

  return {
    fillProperties,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FillStyleList`. See `transitional/dml-main.xsd:144`. */
export function readCT_FillStyleList(cur: XmlCursor, ctx: ReadContext): CT_FillStyleList {
  const start = requireStart(cur, 'CT_FillStyleList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FillStyleList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const fillProperties: CT_FillStyleList_FillProperties[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FillStyleList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'noFill': {
            fillProperties.push({ kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) });
            continue;
          }
          case 'solidFill': {
            fillProperties.push({ kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) });
            continue;
          }
          case 'gradFill': {
            fillProperties.push({ kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) });
            continue;
          }
          case 'blipFill': {
            fillProperties.push({ kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) });
            continue;
          }
          case 'pattFill': {
            fillProperties.push({ kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) });
            continue;
          }
          case 'grpFill': {
            fillProperties.push({ kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FillStyleList', ev.localName, ev.uri, cur);
      fillProperties.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FillStyleList', cur);
    cur.next();
  }

  return {
    fillProperties,
    $unknownAttrs,
  };
}

/** Read a `CT_FixedPercentage`. See `transitional/dml-main.xsd:265`. */
export function readCT_FixedPercentage(cur: XmlCursor, ctx: ReadContext): CT_FixedPercentage {
  const start = requireStart(cur, 'CT_FixedPercentage');
  let val: ST_FixedPercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_FixedPercentage(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_FixedPercentage', a, 'ST_FixedPercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FixedPercentage', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_FixedPercentage', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FixedPercentage', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FixedPercentage', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FixedPercentage', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_FlatText`. See `transitional/dml-main.xsd:1255`. */
export function readCT_FlatText(cur: XmlCursor, ctx: ReadContext): CT_FlatText {
  const start = requireStart(cur, 'CT_FlatText');
  let z: ST_Coordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'z': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            z = v;
            continue;
          }
          ctx.invalidValue('CT_FlatText', a, 'ST_Coordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FlatText', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FlatText', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_FlatText', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FlatText', cur);
    cur.next();
  }
  return {
    z,
    $unknownAttrs,
  };
}

/** Read a `CT_FontCollection`. See `transitional/dml-main.xsd:120`. */
export function readCT_FontCollection(cur: XmlCursor, ctx: ReadContext): CT_FontCollection {
  const start = requireStart(cur, 'CT_FontCollection');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_FontCollection', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let latin: CT_TextFont | undefined;
  let ea: CT_TextFont | undefined;
  let cs: CT_TextFont | undefined;
  const font: CT_SupplementalFont[] = [];
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FontCollection', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'latin': {
            latin = readCT_TextFont(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ea': {
            ea = readCT_TextFont(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'cs': {
            cs = readCT_TextFont(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'font': {
            font.push(readCT_SupplementalFont(cur, ctx));
            $slot = 3;
            $index = font.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FontCollection', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FontCollection', cur);
    cur.next();
  }

  return {
    latin,
    ea,
    cs,
    font,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FontReference`. See `transitional/dml-main.xsd:2274`. */
export function readCT_FontReference(cur: XmlCursor, ctx: ReadContext): CT_FontReference {
  const start = requireStart(cur, 'CT_FontReference');
  let idx: ST_FontCollectionIndex | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'idx': {
          const v = parseST_FontCollectionIndex(a.value);
          if (v !== undefined) {
            idx = v;
            continue;
          }
          ctx.invalidValue('CT_FontReference', a, 'ST_FontCollectionIndex', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_FontReference', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (idx === undefined) ctx.missingRequired('CT_FontReference', 'idx', cur);

  let colorChoice: CT_FontReference_ColorChoice | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FontReference', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FontReference', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FontReference', cur);
    cur.next();
  }

  return {
    colorChoice,
    idx,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_FontScheme`. See `transitional/dml-main.xsd:136`. */
export function readCT_FontScheme(cur: XmlCursor, ctx: ReadContext): CT_FontScheme {
  const start = requireStart(cur, 'CT_FontScheme');
  let name: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_FontScheme', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_FontScheme', 'name', cur);

  let majorFont: CT_FontCollection | undefined;
  let minorFont: CT_FontCollection | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_FontScheme', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'majorFont': {
            majorFont = readCT_FontCollection(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'minorFont': {
            minorFont = readCT_FontCollection(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_FontScheme', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_FontScheme', cur);
    cur.next();
  }

  return {
    majorFont,
    minorFont,
    extLst,
    name,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GammaTransform`. See `transitional/dml-main.xsd:295`. */
export function readCT_GammaTransform(cur: XmlCursor, ctx: ReadContext): CT_GammaTransform {
  const start = requireStart(cur, 'CT_GammaTransform');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GammaTransform', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GammaTransform', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_GammaTransform', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GammaTransform', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_GeomGuide`. See `transitional/dml-main.xsd:1959`. */
export function readCT_GeomGuide(cur: XmlCursor, ctx: ReadContext): CT_GeomGuide {
  const start = requireStart(cur, 'CT_GeomGuide');
  let name: ST_GeomGuideName | undefined;
  let fmla: ST_GeomGuideFormula | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name': {
          const v = parseST_GeomGuideName(a.value);
          if (v !== undefined) {
            name = v;
            continue;
          }
          ctx.invalidValue('CT_GeomGuide', a, 'ST_GeomGuideName', cur);
          break;
        }
        case 'fmla': {
          const v = parseST_GeomGuideFormula(a.value);
          if (v !== undefined) {
            fmla = v;
            continue;
          }
          ctx.invalidValue('CT_GeomGuide', a, 'ST_GeomGuideFormula', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GeomGuide', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (name === undefined) ctx.missingRequired('CT_GeomGuide', 'name', cur);
  if (fmla === undefined) ctx.missingRequired('CT_GeomGuide', 'fmla', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GeomGuide', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_GeomGuide', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GeomGuide', cur);
    cur.next();
  }
  return {
    name,
    fmla,
    $unknownAttrs,
  };
}

/** Read a `CT_GeomGuideList`. See `transitional/dml-main.xsd:1963`. */
export function readCT_GeomGuideList(cur: XmlCursor, ctx: ReadContext): CT_GeomGuideList {
  const start = requireStart(cur, 'CT_GeomGuideList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GeomGuideList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const gd: CT_GeomGuide[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GeomGuideList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'gd': {
            gd.push(readCT_GeomGuide(cur, ctx));
            $slot = 0;
            $index = gd.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GeomGuideList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GeomGuideList', cur);
    cur.next();
  }

  return {
    gd,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GeomRect`. See `transitional/dml-main.xsd:1978`. */
export function readCT_GeomRect(cur: XmlCursor, ctx: ReadContext): CT_GeomRect {
  const start = requireStart(cur, 'CT_GeomRect');
  let l: ST_AdjCoordinate | undefined;
  let t: ST_AdjCoordinate | undefined;
  let r: ST_AdjCoordinate | undefined;
  let b: ST_AdjCoordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'l': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            l = v;
            continue;
          }
          ctx.invalidValue('CT_GeomRect', a, 'ST_AdjCoordinate', cur);
          break;
        }
        case 't': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            t = v;
            continue;
          }
          ctx.invalidValue('CT_GeomRect', a, 'ST_AdjCoordinate', cur);
          break;
        }
        case 'r': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            r = v;
            continue;
          }
          ctx.invalidValue('CT_GeomRect', a, 'ST_AdjCoordinate', cur);
          break;
        }
        case 'b': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            b = v;
            continue;
          }
          ctx.invalidValue('CT_GeomRect', a, 'ST_AdjCoordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GeomRect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (l === undefined) ctx.missingRequired('CT_GeomRect', 'l', cur);
  if (t === undefined) ctx.missingRequired('CT_GeomRect', 't', cur);
  if (r === undefined) ctx.missingRequired('CT_GeomRect', 'r', cur);
  if (b === undefined) ctx.missingRequired('CT_GeomRect', 'b', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GeomRect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_GeomRect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GeomRect', cur);
    cur.next();
  }
  return {
    l,
    t,
    r,
    b,
    $unknownAttrs,
  };
}

/** Read a `CT_GlowEffect`. See `transitional/dml-main.xsd:1307`. */
export function readCT_GlowEffect(cur: XmlCursor, ctx: ReadContext): CT_GlowEffect {
  const start = requireStart(cur, 'CT_GlowEffect');
  let rad: ST_PositiveCoordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'rad': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            rad = v;
            continue;
          }
          ctx.invalidValue('CT_GlowEffect', a, 'ST_PositiveCoordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GlowEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let colorChoice: CT_GlowEffect_ColorChoice | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GlowEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GlowEffect', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GlowEffect', cur);
    cur.next();
  }

  return {
    colorChoice,
    rad,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GradientFillProperties`. See `transitional/dml-main.xsd:1460`. */
export function readCT_GradientFillProperties(cur: XmlCursor, ctx: ReadContext): CT_GradientFillProperties {
  const start = requireStart(cur, 'CT_GradientFillProperties');
  let flip: ST_TileFlipMode | undefined;
  let rotWithShape: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'flip': {
          const v = parseST_TileFlipMode(a.value);
          if (v !== undefined) {
            flip = v;
            continue;
          }
          ctx.invalidValue('CT_GradientFillProperties', a, 'ST_TileFlipMode', cur);
          break;
        }
        case 'rotWithShape': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            rotWithShape = v;
            continue;
          }
          ctx.invalidValue('CT_GradientFillProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GradientFillProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let gsLst: CT_GradientStopList | undefined;
  let shadeProperties: CT_GradientFillProperties_ShadeProperties | undefined;
  let tileRect: CT_RelativeRect | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GradientFillProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'gsLst': {
            gsLst = readCT_GradientStopList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lin': {
            shadeProperties = { kind: 'lin', value: readCT_LinearShadeProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'path': {
            shadeProperties = { kind: 'path', value: readCT_PathShadeProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tileRect': {
            tileRect = readCT_RelativeRect(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GradientFillProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GradientFillProperties', cur);
    cur.next();
  }

  return {
    gsLst,
    shadeProperties,
    tileRect,
    flip,
    rotWithShape,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GradientStop`. See `transitional/dml-main.xsd:1449`. */
export function readCT_GradientStop(cur: XmlCursor, ctx: ReadContext): CT_GradientStop {
  const start = requireStart(cur, 'CT_GradientStop');
  let pos: ST_PositiveFixedPercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'pos': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            pos = v;
            continue;
          }
          ctx.invalidValue('CT_GradientStop', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GradientStop', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (pos === undefined) ctx.missingRequired('CT_GradientStop', 'pos', cur);

  let colorChoice: CT_GradientStop_ColorChoice | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GradientStop', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GradientStop', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GradientStop', cur);
    cur.next();
  }

  return {
    colorChoice,
    pos,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GradientStopList`. See `transitional/dml-main.xsd:1455`. */
export function readCT_GradientStopList(cur: XmlCursor, ctx: ReadContext): CT_GradientStopList {
  const start = requireStart(cur, 'CT_GradientStopList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GradientStopList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const gs: CT_GradientStop[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GradientStopList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'gs': {
            gs.push(readCT_GradientStop(cur, ctx));
            $slot = 0;
            $index = gs.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GradientStopList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GradientStopList', cur);
    cur.next();
  }

  return {
    gs,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GraphicalObject`. See `transitional/dml-main.xsd:870`. */
export function readCT_GraphicalObject(cur: XmlCursor, ctx: ReadContext): CT_GraphicalObject {
  const start = requireStart(cur, 'CT_GraphicalObject');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GraphicalObject', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let graphicData: CT_GraphicalObjectData | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GraphicalObject', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'graphicData': {
            graphicData = readCT_GraphicalObjectData(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GraphicalObject', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GraphicalObject', cur);
    cur.next();
  }

  return {
    graphicData,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GraphicalObjectData`. See `transitional/dml-main.xsd:864`. */
export function readCT_GraphicalObjectData(cur: XmlCursor, ctx: ReadContext): CT_GraphicalObjectData {
  const start = requireStart(cur, 'CT_GraphicalObjectData');
  let uri: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'uri':
          uri = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_GraphicalObjectData', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (uri === undefined) ctx.missingRequired('CT_GraphicalObjectData', 'uri', cur);

  const any: RawNode[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GraphicalObjectData', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      any.push(cur.skipToRaw());
      $slot = 0;
      $index = any.length - 1;
      continue;
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GraphicalObjectData', cur);
    cur.next();
  }

  return {
    any,
    uri,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GraphicalObjectFrameLocking`. See `transitional/dml-main.xsd:793`. */
export function readCT_GraphicalObjectFrameLocking(cur: XmlCursor, ctx: ReadContext): CT_GraphicalObjectFrameLocking {
  const start = requireStart(cur, 'CT_GraphicalObjectFrameLocking');
  let noGrp: boolean | undefined;
  let noDrilldown: boolean | undefined;
  let noSelect: boolean | undefined;
  let noChangeAspect: boolean | undefined;
  let noMove: boolean | undefined;
  let noResize: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'noGrp': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noGrp = v;
            continue;
          }
          ctx.invalidValue('CT_GraphicalObjectFrameLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noDrilldown': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noDrilldown = v;
            continue;
          }
          ctx.invalidValue('CT_GraphicalObjectFrameLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noSelect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noSelect = v;
            continue;
          }
          ctx.invalidValue('CT_GraphicalObjectFrameLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeAspect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeAspect = v;
            continue;
          }
          ctx.invalidValue('CT_GraphicalObjectFrameLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noMove': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noMove = v;
            continue;
          }
          ctx.invalidValue('CT_GraphicalObjectFrameLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noResize': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noResize = v;
            continue;
          }
          ctx.invalidValue('CT_GraphicalObjectFrameLocking', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GraphicalObjectFrameLocking', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GraphicalObjectFrameLocking', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GraphicalObjectFrameLocking', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GraphicalObjectFrameLocking', cur);
    cur.next();
  }

  return {
    extLst,
    noGrp,
    noDrilldown,
    noSelect,
    noChangeAspect,
    noMove,
    noResize,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GrayscaleEffect`. See `transitional/dml-main.xsd:1313`. */
export function readCT_GrayscaleEffect(cur: XmlCursor, ctx: ReadContext): CT_GrayscaleEffect {
  const start = requireStart(cur, 'CT_GrayscaleEffect');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GrayscaleEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GrayscaleEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_GrayscaleEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GrayscaleEffect', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_GrayscaleTransform`. See `transitional/dml-main.xsd:294`. */
export function readCT_GrayscaleTransform(cur: XmlCursor, ctx: ReadContext): CT_GrayscaleTransform {
  const start = requireStart(cur, 'CT_GrayscaleTransform');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GrayscaleTransform', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GrayscaleTransform', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_GrayscaleTransform', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GrayscaleTransform', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_GroupFillProperties`. See `transitional/dml-main.xsd:1598`. */
export function readCT_GroupFillProperties(cur: XmlCursor, ctx: ReadContext): CT_GroupFillProperties {
  const start = requireStart(cur, 'CT_GroupFillProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GroupFillProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GroupFillProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_GroupFillProperties', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GroupFillProperties', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_GroupLocking`. See `transitional/dml-main.xsd:781`. */
export function readCT_GroupLocking(cur: XmlCursor, ctx: ReadContext): CT_GroupLocking {
  const start = requireStart(cur, 'CT_GroupLocking');
  let noGrp: boolean | undefined;
  let noUngrp: boolean | undefined;
  let noSelect: boolean | undefined;
  let noRot: boolean | undefined;
  let noChangeAspect: boolean | undefined;
  let noMove: boolean | undefined;
  let noResize: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'noGrp': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noGrp = v;
            continue;
          }
          ctx.invalidValue('CT_GroupLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noUngrp': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noUngrp = v;
            continue;
          }
          ctx.invalidValue('CT_GroupLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noSelect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noSelect = v;
            continue;
          }
          ctx.invalidValue('CT_GroupLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noRot': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noRot = v;
            continue;
          }
          ctx.invalidValue('CT_GroupLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeAspect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeAspect = v;
            continue;
          }
          ctx.invalidValue('CT_GroupLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noMove': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noMove = v;
            continue;
          }
          ctx.invalidValue('CT_GroupLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noResize': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noResize = v;
            continue;
          }
          ctx.invalidValue('CT_GroupLocking', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GroupLocking', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GroupLocking', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GroupLocking', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GroupLocking', cur);
    cur.next();
  }

  return {
    extLst,
    noGrp,
    noUngrp,
    noSelect,
    noRot,
    noChangeAspect,
    noMove,
    noResize,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GroupShapeProperties`. See `transitional/dml-main.xsd:2258`. */
export function readCT_GroupShapeProperties(cur: XmlCursor, ctx: ReadContext): CT_GroupShapeProperties {
  const start = requireStart(cur, 'CT_GroupShapeProperties');
  let bwMode: ST_BlackWhiteMode | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'bwMode': {
          const v = parseST_BlackWhiteMode(a.value);
          if (v !== undefined) {
            bwMode = v;
            continue;
          }
          ctx.invalidValue('CT_GroupShapeProperties', a, 'ST_BlackWhiteMode', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GroupShapeProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let xfrm: CT_GroupTransform2D | undefined;
  let fillProperties: CT_GroupShapeProperties_FillProperties | undefined;
  let effectProperties: CT_GroupShapeProperties_EffectProperties | undefined;
  let scene3d: CT_Scene3D | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GroupShapeProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'xfrm': {
            xfrm = readCT_GroupTransform2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'noFill': {
            fillProperties = { kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'solidFill': {
            fillProperties = { kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'gradFill': {
            fillProperties = { kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            fillProperties = { kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'pattFill': {
            fillProperties = { kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'grpFill': {
            fillProperties = { kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'effectLst': {
            effectProperties = { kind: 'effectLst', value: readCT_EffectList(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'effectDag': {
            effectProperties = { kind: 'effectDag', value: readCT_EffectContainer(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'scene3d': {
            scene3d = readCT_Scene3D(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GroupShapeProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GroupShapeProperties', cur);
    cur.next();
  }

  return {
    xfrm,
    fillProperties,
    effectProperties,
    scene3d,
    extLst,
    bwMode,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GroupTransform2D`. See `transitional/dml-main.xsd:644`. */
export function readCT_GroupTransform2D(cur: XmlCursor, ctx: ReadContext): CT_GroupTransform2D {
  const start = requireStart(cur, 'CT_GroupTransform2D');
  let rot: ST_Angle | undefined;
  let flipH: boolean | undefined;
  let flipV: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'rot': {
          const v = parseST_Angle(a.value);
          if (v !== undefined) {
            rot = v;
            continue;
          }
          ctx.invalidValue('CT_GroupTransform2D', a, 'ST_Angle', cur);
          break;
        }
        case 'flipH': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            flipH = v;
            continue;
          }
          ctx.invalidValue('CT_GroupTransform2D', a, 'xsd:boolean', cur);
          break;
        }
        case 'flipV': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            flipV = v;
            continue;
          }
          ctx.invalidValue('CT_GroupTransform2D', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GroupTransform2D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let off: CT_Point2D | undefined;
  let ext: CT_PositiveSize2D | undefined;
  let chOff: CT_Point2D | undefined;
  let chExt: CT_PositiveSize2D | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GroupTransform2D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'off': {
            off = readCT_Point2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ext': {
            ext = readCT_PositiveSize2D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'chOff': {
            chOff = readCT_Point2D(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'chExt': {
            chExt = readCT_PositiveSize2D(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GroupTransform2D', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GroupTransform2D', cur);
    cur.next();
  }

  return {
    off,
    ext,
    chOff,
    chExt,
    rot,
    flipH,
    flipV,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GvmlConnector`. See `transitional/dml-main.xsd:993`. */
export function readCT_GvmlConnector(cur: XmlCursor, ctx: ReadContext): CT_GvmlConnector {
  const start = requireStart(cur, 'CT_GvmlConnector');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GvmlConnector', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvCxnSpPr: CT_GvmlConnectorNonVisual | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let style: CT_ShapeStyle | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GvmlConnector', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'nvCxnSpPr': {
            nvCxnSpPr = readCT_GvmlConnectorNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GvmlConnector', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GvmlConnector', cur);
    cur.next();
  }

  return {
    nvCxnSpPr,
    spPr,
    style,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GvmlConnectorNonVisual`. See `transitional/dml-main.xsd:986`. */
export function readCT_GvmlConnectorNonVisual(cur: XmlCursor, ctx: ReadContext): CT_GvmlConnectorNonVisual {
  const start = requireStart(cur, 'CT_GvmlConnectorNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GvmlConnectorNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvCxnSpPr: CT_NonVisualConnectorProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GvmlConnectorNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvCxnSpPr': {
            cNvCxnSpPr = readCT_NonVisualConnectorProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GvmlConnectorNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GvmlConnectorNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvCxnSpPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GvmlGraphicFrameNonVisual`. See `transitional/dml-main.xsd:1017`. */
export function readCT_GvmlGraphicFrameNonVisual(cur: XmlCursor, ctx: ReadContext): CT_GvmlGraphicFrameNonVisual {
  const start = requireStart(cur, 'CT_GvmlGraphicFrameNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GvmlGraphicFrameNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvGraphicFramePr: CT_NonVisualGraphicFrameProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GvmlGraphicFrameNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvGraphicFramePr': {
            cNvGraphicFramePr = readCT_NonVisualGraphicFrameProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GvmlGraphicFrameNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GvmlGraphicFrameNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvGraphicFramePr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GvmlGraphicalObjectFrame`. See `transitional/dml-main.xsd:1024`. */
export function readCT_GvmlGraphicalObjectFrame(cur: XmlCursor, ctx: ReadContext): CT_GvmlGraphicalObjectFrame {
  const start = requireStart(cur, 'CT_GvmlGraphicalObjectFrame');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GvmlGraphicalObjectFrame', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvGraphicFramePr: CT_GvmlGraphicFrameNonVisual | undefined;
  let graphic: CT_GraphicalObject | undefined;
  let xfrm: CT_Transform2D | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GvmlGraphicalObjectFrame', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'nvGraphicFramePr': {
            nvGraphicFramePr = readCT_GvmlGraphicFrameNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'graphic': {
            graphic = readCT_GraphicalObject(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'xfrm': {
            xfrm = readCT_Transform2D(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GvmlGraphicalObjectFrame', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GvmlGraphicalObjectFrame', cur);
    cur.next();
  }

  return {
    nvGraphicFramePr,
    graphic,
    xfrm,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GvmlGroupShape`. See `transitional/dml-main.xsd:1040`. */
export function readCT_GvmlGroupShape(cur: XmlCursor, ctx: ReadContext): CT_GvmlGroupShape {
  const start = requireStart(cur, 'CT_GvmlGroupShape');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GvmlGroupShape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvGrpSpPr: CT_GvmlGroupShapeNonVisual | undefined;
  let grpSpPr: CT_GroupShapeProperties | undefined;
  const content: CT_GvmlGroupShape_Content[] = [];
  let extLst: CT_OfficeArtExtensionList | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GvmlGroupShape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'nvGrpSpPr': {
            nvGrpSpPr = readCT_GvmlGroupShapeNonVisual(cur, ctx);
            continue;
          }
          case 'grpSpPr': {
            grpSpPr = readCT_GroupShapeProperties(cur, ctx);
            continue;
          }
          case 'txSp': {
            content.push({ kind: 'txSp', value: readCT_GvmlTextShape(cur, ctx) });
            continue;
          }
          case 'sp': {
            content.push({ kind: 'sp', value: readCT_GvmlShape(cur, ctx) });
            continue;
          }
          case 'cxnSp': {
            content.push({ kind: 'cxnSp', value: readCT_GvmlConnector(cur, ctx) });
            continue;
          }
          case 'pic': {
            content.push({ kind: 'pic', value: readCT_GvmlPicture(cur, ctx) });
            continue;
          }
          case 'graphicFrame': {
            content.push({ kind: 'graphicFrame', value: readCT_GvmlGraphicalObjectFrame(cur, ctx) });
            continue;
          }
          case 'grpSp': {
            content.push({ kind: 'grpSp', value: readCT_GvmlGroupShape(cur, ctx) });
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GvmlGroupShape', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GvmlGroupShape', cur);
    cur.next();
  }

  return {
    nvGrpSpPr,
    grpSpPr,
    content,
    extLst,
    $unknownAttrs,
  };
}

/** Read a `CT_GvmlGroupShapeNonVisual`. See `transitional/dml-main.xsd:1033`. */
export function readCT_GvmlGroupShapeNonVisual(cur: XmlCursor, ctx: ReadContext): CT_GvmlGroupShapeNonVisual {
  const start = requireStart(cur, 'CT_GvmlGroupShapeNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GvmlGroupShapeNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvGrpSpPr: CT_NonVisualGroupDrawingShapeProps | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GvmlGroupShapeNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvGrpSpPr': {
            cNvGrpSpPr = readCT_NonVisualGroupDrawingShapeProps(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GvmlGroupShapeNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GvmlGroupShapeNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvGrpSpPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GvmlPicture`. See `transitional/dml-main.xsd:1008`. */
export function readCT_GvmlPicture(cur: XmlCursor, ctx: ReadContext): CT_GvmlPicture {
  const start = requireStart(cur, 'CT_GvmlPicture');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GvmlPicture', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvPicPr: CT_GvmlPictureNonVisual | undefined;
  let blipFill: CT_BlipFillProperties | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let style: CT_ShapeStyle | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GvmlPicture', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'nvPicPr': {
            nvPicPr = readCT_GvmlPictureNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            blipFill = readCT_BlipFillProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GvmlPicture', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GvmlPicture', cur);
    cur.next();
  }

  return {
    nvPicPr,
    blipFill,
    spPr,
    style,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GvmlPictureNonVisual`. See `transitional/dml-main.xsd:1001`. */
export function readCT_GvmlPictureNonVisual(cur: XmlCursor, ctx: ReadContext): CT_GvmlPictureNonVisual {
  const start = requireStart(cur, 'CT_GvmlPictureNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GvmlPictureNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvPicPr: CT_NonVisualPictureProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GvmlPictureNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvPicPr': {
            cNvPicPr = readCT_NonVisualPictureProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GvmlPictureNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GvmlPictureNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvPicPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GvmlShape`. See `transitional/dml-main.xsd:977`. */
export function readCT_GvmlShape(cur: XmlCursor, ctx: ReadContext): CT_GvmlShape {
  const start = requireStart(cur, 'CT_GvmlShape');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GvmlShape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvSpPr: CT_GvmlShapeNonVisual | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let txSp: CT_GvmlTextShape | undefined;
  let style: CT_ShapeStyle | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GvmlShape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'nvSpPr': {
            nvSpPr = readCT_GvmlShapeNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'txSp': {
            txSp = readCT_GvmlTextShape(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GvmlShape', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GvmlShape', cur);
    cur.next();
  }

  return {
    nvSpPr,
    spPr,
    txSp,
    style,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GvmlShapeNonVisual`. See `transitional/dml-main.xsd:971`. */
export function readCT_GvmlShapeNonVisual(cur: XmlCursor, ctx: ReadContext): CT_GvmlShapeNonVisual {
  const start = requireStart(cur, 'CT_GvmlShapeNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GvmlShapeNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvSpPr: CT_NonVisualDrawingShapeProps | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GvmlShapeNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvSpPr': {
            cNvSpPr = readCT_NonVisualDrawingShapeProps(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GvmlShapeNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GvmlShapeNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvSpPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GvmlTextShape`. See `transitional/dml-main.xsd:961`. */
export function readCT_GvmlTextShape(cur: XmlCursor, ctx: ReadContext): CT_GvmlTextShape {
  const start = requireStart(cur, 'CT_GvmlTextShape');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GvmlTextShape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let txBody: CT_TextBody | undefined;
  let content: CT_GvmlTextShape_Content | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GvmlTextShape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'txBody': {
            txBody = readCT_TextBody(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'useSpRect': {
            content = { kind: 'useSpRect', value: readCT_GvmlUseShapeRectangle(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'xfrm': {
            content = { kind: 'xfrm', value: readCT_Transform2D(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GvmlTextShape', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GvmlTextShape', cur);
    cur.next();
  }

  return {
    txBody,
    content,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GvmlUseShapeRectangle`. See `transitional/dml-main.xsd:960`. */
export function readCT_GvmlUseShapeRectangle(cur: XmlCursor, ctx: ReadContext): CT_GvmlUseShapeRectangle {
  const start = requireStart(cur, 'CT_GvmlUseShapeRectangle');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GvmlUseShapeRectangle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GvmlUseShapeRectangle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_GvmlUseShapeRectangle', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GvmlUseShapeRectangle', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_HSLEffect`. See `transitional/dml-main.xsd:1314`. */
export function readCT_HSLEffect(cur: XmlCursor, ctx: ReadContext): CT_HSLEffect {
  const start = requireStart(cur, 'CT_HSLEffect');
  let hue: ST_PositiveFixedAngle | undefined;
  let sat: ST_FixedPercentage | undefined;
  let lum: ST_FixedPercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'hue': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            hue = v;
            continue;
          }
          ctx.invalidValue('CT_HSLEffect', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
        case 'sat': {
          const v = parseST_FixedPercentage(a.value);
          if (v !== undefined) {
            sat = v;
            continue;
          }
          ctx.invalidValue('CT_HSLEffect', a, 'ST_FixedPercentage', cur);
          break;
        }
        case 'lum': {
          const v = parseST_FixedPercentage(a.value);
          if (v !== undefined) {
            lum = v;
            continue;
          }
          ctx.invalidValue('CT_HSLEffect', a, 'ST_FixedPercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_HSLEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HSLEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_HSLEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HSLEffect', cur);
    cur.next();
  }
  return {
    hue,
    sat,
    lum,
    $unknownAttrs,
  };
}

/** Read a `CT_Headers`. See `transitional/dml-main.xsd:2392`. */
export function readCT_Headers(cur: XmlCursor, ctx: ReadContext): CT_Headers {
  const start = requireStart(cur, 'CT_Headers');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Headers', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const header: string[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Headers', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'header': {
            header.push(readScalar(cur, ctx));
            $slot = 0;
            $index = header.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Headers', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Headers', cur);
    cur.next();
  }

  return {
    header,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_HslColor`. See `transitional/dml-main.xsd:343`. */
export function readCT_HslColor(cur: XmlCursor, ctx: ReadContext): CT_HslColor {
  const start = requireStart(cur, 'CT_HslColor');
  let hue: ST_PositiveFixedAngle | undefined;
  let sat: ST_Percentage | undefined;
  let lum: ST_Percentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'hue': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            hue = v;
            continue;
          }
          ctx.invalidValue('CT_HslColor', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
        case 'sat': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            sat = v;
            continue;
          }
          ctx.invalidValue('CT_HslColor', a, 'ST_Percentage', cur);
          break;
        }
        case 'lum': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            lum = v;
            continue;
          }
          ctx.invalidValue('CT_HslColor', a, 'ST_Percentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_HslColor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (hue === undefined) ctx.missingRequired('CT_HslColor', 'hue', cur);
  if (sat === undefined) ctx.missingRequired('CT_HslColor', 'sat', cur);
  if (lum === undefined) ctx.missingRequired('CT_HslColor', 'lum', cur);

  const colorTransform: CT_HslColor_ColorTransform[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_HslColor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tint': {
            colorTransform.push({ kind: 'tint', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'shade': {
            colorTransform.push({ kind: 'shade', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'comp': {
            colorTransform.push({ kind: 'comp', value: readCT_ComplementTransform(cur, ctx) });
            continue;
          }
          case 'inv': {
            colorTransform.push({ kind: 'inv', value: readCT_InverseTransform(cur, ctx) });
            continue;
          }
          case 'gray': {
            colorTransform.push({ kind: 'gray', value: readCT_GrayscaleTransform(cur, ctx) });
            continue;
          }
          case 'alpha': {
            colorTransform.push({ kind: 'alpha', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'alphaOff': {
            colorTransform.push({ kind: 'alphaOff', value: readCT_FixedPercentage(cur, ctx) });
            continue;
          }
          case 'alphaMod': {
            colorTransform.push({ kind: 'alphaMod', value: readCT_PositivePercentage(cur, ctx) });
            continue;
          }
          case 'hue': {
            colorTransform.push({ kind: 'hue', value: readCT_PositiveFixedAngle(cur, ctx) });
            continue;
          }
          case 'hueOff': {
            colorTransform.push({ kind: 'hueOff', value: readCT_Angle(cur, ctx) });
            continue;
          }
          case 'hueMod': {
            colorTransform.push({ kind: 'hueMod', value: readCT_PositivePercentage(cur, ctx) });
            continue;
          }
          case 'sat': {
            colorTransform.push({ kind: 'sat', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'satOff': {
            colorTransform.push({ kind: 'satOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'satMod': {
            colorTransform.push({ kind: 'satMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lum': {
            colorTransform.push({ kind: 'lum', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lumOff': {
            colorTransform.push({ kind: 'lumOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lumMod': {
            colorTransform.push({ kind: 'lumMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'red': {
            colorTransform.push({ kind: 'red', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'redOff': {
            colorTransform.push({ kind: 'redOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'redMod': {
            colorTransform.push({ kind: 'redMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'green': {
            colorTransform.push({ kind: 'green', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'greenOff': {
            colorTransform.push({ kind: 'greenOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'greenMod': {
            colorTransform.push({ kind: 'greenMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blue': {
            colorTransform.push({ kind: 'blue', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blueOff': {
            colorTransform.push({ kind: 'blueOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blueMod': {
            colorTransform.push({ kind: 'blueMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'gamma': {
            colorTransform.push({ kind: 'gamma', value: readCT_GammaTransform(cur, ctx) });
            continue;
          }
          case 'invGamma': {
            colorTransform.push({ kind: 'invGamma', value: readCT_InverseGammaTransform(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_HslColor', ev.localName, ev.uri, cur);
      colorTransform.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_HslColor', cur);
    cur.next();
  }

  return {
    colorTransform,
    hue,
    sat,
    lum,
    $unknownAttrs,
  };
}

/** Read a `CT_Hyperlink`. See `transitional/dml-main.xsd:732`. */
export function readCT_Hyperlink(cur: XmlCursor, ctx: ReadContext): CT_Hyperlink {
  const start = requireStart(cur, 'CT_Hyperlink');
  let id: ST_RelationshipId | undefined;
  let invalidUrl: string | undefined;
  let action: string | undefined;
  let tgtFrame: string | undefined;
  let tooltip: string | undefined;
  let history: boolean | undefined;
  let highlightClick: boolean | undefined;
  let endSnd: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'invalidUrl':
          invalidUrl = a.value as string;
          continue;

        case 'action':
          action = a.value as string;
          continue;

        case 'tgtFrame':
          tgtFrame = a.value as string;
          continue;

        case 'tooltip':
          tooltip = a.value as string;
          continue;

        case 'history': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            history = v;
            continue;
          }
          ctx.invalidValue('CT_Hyperlink', a, 'xsd:boolean', cur);
          break;
        }
        case 'highlightClick': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            highlightClick = v;
            continue;
          }
          ctx.invalidValue('CT_Hyperlink', a, 'xsd:boolean', cur);
          break;
        }
        case 'endSnd': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            endSnd = v;
            continue;
          }
          ctx.invalidValue('CT_Hyperlink', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_Hyperlink', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Hyperlink', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let snd: CT_EmbeddedWAVAudioFile | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Hyperlink', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'snd': {
            snd = readCT_EmbeddedWAVAudioFile(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Hyperlink', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Hyperlink', cur);
    cur.next();
  }

  return {
    snd,
    extLst,
    id,
    invalidUrl,
    action,
    tgtFrame,
    tooltip,
    history,
    highlightClick,
    endSnd,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_InnerShadowEffect`. See `transitional/dml-main.xsd:1319`. */
export function readCT_InnerShadowEffect(cur: XmlCursor, ctx: ReadContext): CT_InnerShadowEffect {
  const start = requireStart(cur, 'CT_InnerShadowEffect');
  let blurRad: ST_PositiveCoordinate | undefined;
  let dist: ST_PositiveCoordinate | undefined;
  let dir: ST_PositiveFixedAngle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'blurRad': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            blurRad = v;
            continue;
          }
          ctx.invalidValue('CT_InnerShadowEffect', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'dist': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            dist = v;
            continue;
          }
          ctx.invalidValue('CT_InnerShadowEffect', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'dir': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            dir = v;
            continue;
          }
          ctx.invalidValue('CT_InnerShadowEffect', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_InnerShadowEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let colorChoice: CT_InnerShadowEffect_ColorChoice | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_InnerShadowEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_InnerShadowEffect', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_InnerShadowEffect', cur);
    cur.next();
  }

  return {
    colorChoice,
    blurRad,
    dist,
    dir,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_InverseGammaTransform`. See `transitional/dml-main.xsd:296`. */
export function readCT_InverseGammaTransform(cur: XmlCursor, ctx: ReadContext): CT_InverseGammaTransform {
  const start = requireStart(cur, 'CT_InverseGammaTransform');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_InverseGammaTransform', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_InverseGammaTransform', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_InverseGammaTransform', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_InverseGammaTransform', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_InverseTransform`. See `transitional/dml-main.xsd:293`. */
export function readCT_InverseTransform(cur: XmlCursor, ctx: ReadContext): CT_InverseTransform {
  const start = requireStart(cur, 'CT_InverseTransform');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_InverseTransform', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_InverseTransform', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_InverseTransform', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_InverseTransform', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_LightRig`. See `transitional/dml-main.xsd:1178`. */
export function readCT_LightRig(cur: XmlCursor, ctx: ReadContext): CT_LightRig {
  const start = requireStart(cur, 'CT_LightRig');
  let rig: ST_LightRigType | undefined;
  let dir: ST_LightRigDirection | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'rig': {
          const v = parseST_LightRigType(a.value);
          if (v !== undefined) {
            rig = v;
            continue;
          }
          ctx.invalidValue('CT_LightRig', a, 'ST_LightRigType', cur);
          break;
        }
        case 'dir': {
          const v = parseST_LightRigDirection(a.value);
          if (v !== undefined) {
            dir = v;
            continue;
          }
          ctx.invalidValue('CT_LightRig', a, 'ST_LightRigDirection', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LightRig', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (rig === undefined) ctx.missingRequired('CT_LightRig', 'rig', cur);
  if (dir === undefined) ctx.missingRequired('CT_LightRig', 'dir', cur);

  let rot: CT_SphereCoords | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LightRig', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'rot': {
            rot = readCT_SphereCoords(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LightRig', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LightRig', cur);
    cur.next();
  }

  return {
    rot,
    rig,
    dir,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LineEndProperties`. See `transitional/dml-main.xsd:2142`. */
export function readCT_LineEndProperties(cur: XmlCursor, ctx: ReadContext): CT_LineEndProperties {
  const start = requireStart(cur, 'CT_LineEndProperties');
  let type: ST_LineEndType | undefined;
  let w: ST_LineEndWidth | undefined;
  let len: ST_LineEndLength | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_LineEndType(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_LineEndProperties', a, 'ST_LineEndType', cur);
          break;
        }
        case 'w': {
          const v = parseST_LineEndWidth(a.value);
          if (v !== undefined) {
            w = v;
            continue;
          }
          ctx.invalidValue('CT_LineEndProperties', a, 'ST_LineEndWidth', cur);
          break;
        }
        case 'len': {
          const v = parseST_LineEndLength(a.value);
          if (v !== undefined) {
            len = v;
            continue;
          }
          ctx.invalidValue('CT_LineEndProperties', a, 'ST_LineEndLength', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LineEndProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LineEndProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LineEndProperties', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LineEndProperties', cur);
    cur.next();
  }
  return {
    type,
    w,
    len,
    $unknownAttrs,
  };
}

/** Read a `CT_LineJoinBevel`. See `transitional/dml-main.xsd:2155`. */
export function readCT_LineJoinBevel(cur: XmlCursor, ctx: ReadContext): CT_LineJoinBevel {
  const start = requireStart(cur, 'CT_LineJoinBevel');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_LineJoinBevel', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LineJoinBevel', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LineJoinBevel', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LineJoinBevel', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_LineJoinMiterProperties`. See `transitional/dml-main.xsd:2157`. */
export function readCT_LineJoinMiterProperties(cur: XmlCursor, ctx: ReadContext): CT_LineJoinMiterProperties {
  const start = requireStart(cur, 'CT_LineJoinMiterProperties');
  let lim: ST_PositivePercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'lim': {
          const v = parseST_PositivePercentage(a.value);
          if (v !== undefined) {
            lim = v;
            continue;
          }
          ctx.invalidValue('CT_LineJoinMiterProperties', a, 'ST_PositivePercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LineJoinMiterProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LineJoinMiterProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LineJoinMiterProperties', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LineJoinMiterProperties', cur);
    cur.next();
  }
  return {
    lim,
    $unknownAttrs,
  };
}

/** Read a `CT_LineJoinRound`. See `transitional/dml-main.xsd:2156`. */
export function readCT_LineJoinRound(cur: XmlCursor, ctx: ReadContext): CT_LineJoinRound {
  const start = requireStart(cur, 'CT_LineJoinRound');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_LineJoinRound', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LineJoinRound', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LineJoinRound', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LineJoinRound', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_LineProperties`. See `transitional/dml-main.xsd:2228`. */
export function readCT_LineProperties(cur: XmlCursor, ctx: ReadContext): CT_LineProperties {
  const start = requireStart(cur, 'CT_LineProperties');
  let w: ST_LineWidth | undefined;
  let cap: ST_LineCap | undefined;
  let cmpd: ST_CompoundLine | undefined;
  let algn: ST_PenAlignment | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'w': {
          const v = parseST_LineWidth(a.value);
          if (v !== undefined) {
            w = v;
            continue;
          }
          ctx.invalidValue('CT_LineProperties', a, 'ST_LineWidth', cur);
          break;
        }
        case 'cap': {
          const v = parseST_LineCap(a.value);
          if (v !== undefined) {
            cap = v;
            continue;
          }
          ctx.invalidValue('CT_LineProperties', a, 'ST_LineCap', cur);
          break;
        }
        case 'cmpd': {
          const v = parseST_CompoundLine(a.value);
          if (v !== undefined) {
            cmpd = v;
            continue;
          }
          ctx.invalidValue('CT_LineProperties', a, 'ST_CompoundLine', cur);
          break;
        }
        case 'algn': {
          const v = parseST_PenAlignment(a.value);
          if (v !== undefined) {
            algn = v;
            continue;
          }
          ctx.invalidValue('CT_LineProperties', a, 'ST_PenAlignment', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LineProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let lineFillProperties: CT_LineProperties_LineFillProperties | undefined;
  let lineDashProperties: CT_LineProperties_LineDashProperties | undefined;
  let lineJoinProperties: CT_LineProperties_LineJoinProperties | undefined;
  let headEnd: CT_LineEndProperties | undefined;
  let tailEnd: CT_LineEndProperties | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LineProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'noFill': {
            lineFillProperties = { kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'solidFill': {
            lineFillProperties = { kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'gradFill': {
            lineFillProperties = { kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pattFill': {
            lineFillProperties = { kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstDash': {
            lineDashProperties = { kind: 'prstDash', value: readCT_PresetLineDashProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'custDash': {
            lineDashProperties = { kind: 'custDash', value: readCT_DashStopList(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'round': {
            lineJoinProperties = { kind: 'round', value: readCT_LineJoinRound(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'bevel': {
            lineJoinProperties = { kind: 'bevel', value: readCT_LineJoinBevel(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'miter': {
            lineJoinProperties = { kind: 'miter', value: readCT_LineJoinMiterProperties(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'headEnd': {
            headEnd = readCT_LineEndProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'tailEnd': {
            tailEnd = readCT_LineEndProperties(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LineProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LineProperties', cur);
    cur.next();
  }

  return {
    lineFillProperties,
    lineDashProperties,
    lineJoinProperties,
    headEnd,
    tailEnd,
    extLst,
    w,
    cap,
    cmpd,
    algn,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LineStyleList`. See `transitional/dml-main.xsd:149`. */
export function readCT_LineStyleList(cur: XmlCursor, ctx: ReadContext): CT_LineStyleList {
  const start = requireStart(cur, 'CT_LineStyleList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_LineStyleList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const ln: CT_LineProperties[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LineStyleList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'ln': {
            ln.push(readCT_LineProperties(cur, ctx));
            $slot = 0;
            $index = ln.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LineStyleList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LineStyleList', cur);
    cur.next();
  }

  return {
    ln,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LinearShadeProperties`. See `transitional/dml-main.xsd:1418`. */
export function readCT_LinearShadeProperties(cur: XmlCursor, ctx: ReadContext): CT_LinearShadeProperties {
  const start = requireStart(cur, 'CT_LinearShadeProperties');
  let ang: ST_PositiveFixedAngle | undefined;
  let scaled: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'ang': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            ang = v;
            continue;
          }
          ctx.invalidValue('CT_LinearShadeProperties', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
        case 'scaled': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            scaled = v;
            continue;
          }
          ctx.invalidValue('CT_LinearShadeProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LinearShadeProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LinearShadeProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LinearShadeProperties', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LinearShadeProperties', cur);
    cur.next();
  }
  return {
    ang,
    scaled,
    $unknownAttrs,
  };
}

/** Read a `CT_LuminanceEffect`. See `transitional/dml-main.xsd:1327`. */
export function readCT_LuminanceEffect(cur: XmlCursor, ctx: ReadContext): CT_LuminanceEffect {
  const start = requireStart(cur, 'CT_LuminanceEffect');
  let bright: ST_FixedPercentage | undefined;
  let contrast: ST_FixedPercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'bright': {
          const v = parseST_FixedPercentage(a.value);
          if (v !== undefined) {
            bright = v;
            continue;
          }
          ctx.invalidValue('CT_LuminanceEffect', a, 'ST_FixedPercentage', cur);
          break;
        }
        case 'contrast': {
          const v = parseST_FixedPercentage(a.value);
          if (v !== undefined) {
            contrast = v;
            continue;
          }
          ctx.invalidValue('CT_LuminanceEffect', a, 'ST_FixedPercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LuminanceEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LuminanceEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_LuminanceEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LuminanceEffect', cur);
    cur.next();
  }
  return {
    bright,
    contrast,
    $unknownAttrs,
  };
}

/** Read a `CT_NoFillProperties`. See `transitional/dml-main.xsd:1412`. */
export function readCT_NoFillProperties(cur: XmlCursor, ctx: ReadContext): CT_NoFillProperties {
  const start = requireStart(cur, 'CT_NoFillProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NoFillProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NoFillProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_NoFillProperties', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NoFillProperties', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_NonVisualConnectorProperties`. See `transitional/dml-main.xsd:829`. */
export function readCT_NonVisualConnectorProperties(cur: XmlCursor, ctx: ReadContext): CT_NonVisualConnectorProperties {
  const start = requireStart(cur, 'CT_NonVisualConnectorProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NonVisualConnectorProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cxnSpLocks: CT_ConnectorLocking | undefined;
  let stCxn: CT_Connection | undefined;
  let endCxn: CT_Connection | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NonVisualConnectorProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'cxnSpLocks': {
            cxnSpLocks = readCT_ConnectorLocking(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'stCxn': {
            stCxn = readCT_Connection(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'endCxn': {
            endCxn = readCT_Connection(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NonVisualConnectorProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NonVisualConnectorProperties', cur);
    cur.next();
  }

  return {
    cxnSpLocks,
    stCxn,
    endCxn,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NonVisualContentPartProperties`. See `transitional/dml-main.xsd:857`. */
export function readCT_NonVisualContentPartProperties(cur: XmlCursor, ctx: ReadContext): CT_NonVisualContentPartProperties {
  const start = requireStart(cur, 'CT_NonVisualContentPartProperties');
  let isComment: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'isComment': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            isComment = v;
            continue;
          }
          ctx.invalidValue('CT_NonVisualContentPartProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NonVisualContentPartProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cpLocks: CT_ContentPartLocking | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NonVisualContentPartProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'cpLocks': {
            cpLocks = readCT_ContentPartLocking(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NonVisualContentPartProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NonVisualContentPartProperties', cur);
    cur.next();
  }

  return {
    cpLocks,
    extLst,
    isComment,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NonVisualDrawingProps`. See `transitional/dml-main.xsd:810`. */
export function readCT_NonVisualDrawingProps(cur: XmlCursor, ctx: ReadContext): CT_NonVisualDrawingProps {
  const start = requireStart(cur, 'CT_NonVisualDrawingProps');
  let id: ST_DrawingElementId | undefined;
  let name: string | undefined;
  let descr: string | undefined;
  let hidden: boolean | undefined;
  let title: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id': {
          const v = parseST_DrawingElementId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_NonVisualDrawingProps', a, 'ST_DrawingElementId', cur);
          break;
        }
        case 'name':
          name = a.value as string;
          continue;

        case 'descr':
          descr = a.value as string;
          continue;

        case 'hidden': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            hidden = v;
            continue;
          }
          ctx.invalidValue('CT_NonVisualDrawingProps', a, 'xsd:boolean', cur);
          break;
        }
        case 'title':
          title = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_NonVisualDrawingProps', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_NonVisualDrawingProps', 'id', cur);
  if (name === undefined) ctx.missingRequired('CT_NonVisualDrawingProps', 'name', cur);

  let hlinkClick: CT_Hyperlink | undefined;
  let hlinkHover: CT_Hyperlink | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NonVisualDrawingProps', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'hlinkClick': {
            hlinkClick = readCT_Hyperlink(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hlinkHover': {
            hlinkHover = readCT_Hyperlink(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NonVisualDrawingProps', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NonVisualDrawingProps', cur);
    cur.next();
  }

  return {
    hlinkClick,
    hlinkHover,
    extLst,
    id,
    name,
    descr,
    hidden,
    title,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NonVisualDrawingShapeProps`. See `transitional/dml-main.xsd:822`. */
export function readCT_NonVisualDrawingShapeProps(cur: XmlCursor, ctx: ReadContext): CT_NonVisualDrawingShapeProps {
  const start = requireStart(cur, 'CT_NonVisualDrawingShapeProps');
  let txBox: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'txBox': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            txBox = v;
            continue;
          }
          ctx.invalidValue('CT_NonVisualDrawingShapeProps', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NonVisualDrawingShapeProps', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let spLocks: CT_ShapeLocking | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NonVisualDrawingShapeProps', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'spLocks': {
            spLocks = readCT_ShapeLocking(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NonVisualDrawingShapeProps', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NonVisualDrawingShapeProps', cur);
    cur.next();
  }

  return {
    spLocks,
    extLst,
    txBox,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NonVisualGraphicFrameProperties`. See `transitional/dml-main.xsd:850`. */
export function readCT_NonVisualGraphicFrameProperties(cur: XmlCursor, ctx: ReadContext): CT_NonVisualGraphicFrameProperties {
  const start = requireStart(cur, 'CT_NonVisualGraphicFrameProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NonVisualGraphicFrameProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let graphicFrameLocks: CT_GraphicalObjectFrameLocking | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NonVisualGraphicFrameProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'graphicFrameLocks': {
            graphicFrameLocks = readCT_GraphicalObjectFrameLocking(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NonVisualGraphicFrameProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NonVisualGraphicFrameProperties', cur);
    cur.next();
  }

  return {
    graphicFrameLocks,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NonVisualGroupDrawingShapeProps`. See `transitional/dml-main.xsd:844`. */
export function readCT_NonVisualGroupDrawingShapeProps(cur: XmlCursor, ctx: ReadContext): CT_NonVisualGroupDrawingShapeProps {
  const start = requireStart(cur, 'CT_NonVisualGroupDrawingShapeProps');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_NonVisualGroupDrawingShapeProps', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let grpSpLocks: CT_GroupLocking | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NonVisualGroupDrawingShapeProps', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'grpSpLocks': {
            grpSpLocks = readCT_GroupLocking(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NonVisualGroupDrawingShapeProps', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NonVisualGroupDrawingShapeProps', cur);
    cur.next();
  }

  return {
    grpSpLocks,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_NonVisualPictureProperties`. See `transitional/dml-main.xsd:837`. */
export function readCT_NonVisualPictureProperties(cur: XmlCursor, ctx: ReadContext): CT_NonVisualPictureProperties {
  const start = requireStart(cur, 'CT_NonVisualPictureProperties');
  let preferRelativeResize: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'preferRelativeResize': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            preferRelativeResize = v;
            continue;
          }
          ctx.invalidValue('CT_NonVisualPictureProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_NonVisualPictureProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let picLocks: CT_PictureLocking | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_NonVisualPictureProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'picLocks': {
            picLocks = readCT_PictureLocking(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_NonVisualPictureProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_NonVisualPictureProperties', cur);
    cur.next();
  }

  return {
    picLocks,
    extLst,
    preferRelativeResize,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ObjectStyleDefaults`. See `transitional/dml-main.xsd:2297`. */
export function readCT_ObjectStyleDefaults(cur: XmlCursor, ctx: ReadContext): CT_ObjectStyleDefaults {
  const start = requireStart(cur, 'CT_ObjectStyleDefaults');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ObjectStyleDefaults', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let spDef: CT_DefaultShapeDefinition | undefined;
  let lnDef: CT_DefaultShapeDefinition | undefined;
  let txDef: CT_DefaultShapeDefinition | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ObjectStyleDefaults', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'spDef': {
            spDef = readCT_DefaultShapeDefinition(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lnDef': {
            lnDef = readCT_DefaultShapeDefinition(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'txDef': {
            txDef = readCT_DefaultShapeDefinition(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ObjectStyleDefaults', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ObjectStyleDefaults', cur);
    cur.next();
  }

  return {
    spDef,
    lnDef,
    txDef,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OfficeArtExtension`. See `transitional/dml-main.xsd:183`. */
export function readCT_OfficeArtExtension(cur: XmlCursor, ctx: ReadContext): CT_OfficeArtExtension {
  const start = requireStart(cur, 'CT_OfficeArtExtension');
  let uri: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'uri':
          uri = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_OfficeArtExtension', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (uri === undefined) ctx.missingRequired('CT_OfficeArtExtension', 'uri', cur);

  const any: RawNode[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OfficeArtExtension', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      any.push(cur.skipToRaw());
      $slot = 0;
      $index = any.length - 1;
      continue;
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OfficeArtExtension', cur);
    cur.next();
  }

  return {
    any,
    uri,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OfficeArtExtensionList`. See `transitional/dml-main.xsd:624`. */
export function readCT_OfficeArtExtensionList(cur: XmlCursor, ctx: ReadContext): CT_OfficeArtExtensionList {
  const start = requireStart(cur, 'CT_OfficeArtExtensionList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_OfficeArtExtensionList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const ext: CT_OfficeArtExtension[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OfficeArtExtensionList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'ext': {
            ext.push(readCT_OfficeArtExtension(cur, ctx));
            $slot = 0;
            $index = ext.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OfficeArtExtensionList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OfficeArtExtensionList', cur);
    cur.next();
  }

  return {
    ext,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OfficeStyleSheet`. See `transitional/dml-main.xsd:2343`. */
export function readCT_OfficeStyleSheet(cur: XmlCursor, ctx: ReadContext): CT_OfficeStyleSheet {
  const start = requireStart(cur, 'CT_OfficeStyleSheet');
  let name: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_OfficeStyleSheet', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let themeElements: CT_BaseStyles | undefined;
  let objectDefaults: CT_ObjectStyleDefaults | undefined;
  let extraClrSchemeLst: CT_ColorSchemeList | undefined;
  let custClrLst: CT_CustomColorList | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OfficeStyleSheet', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'themeElements': {
            themeElements = readCT_BaseStyles(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'objectDefaults': {
            objectDefaults = readCT_ObjectStyleDefaults(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extraClrSchemeLst': {
            extraClrSchemeLst = readCT_ColorSchemeList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'custClrLst': {
            custClrLst = readCT_CustomColorList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OfficeStyleSheet', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OfficeStyleSheet', cur);
    cur.next();
  }

  return {
    themeElements,
    objectDefaults,
    extraClrSchemeLst,
    custClrLst,
    extLst,
    name,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_OuterShadowEffect`. See `transitional/dml-main.xsd:1331`. */
export function readCT_OuterShadowEffect(cur: XmlCursor, ctx: ReadContext): CT_OuterShadowEffect {
  const start = requireStart(cur, 'CT_OuterShadowEffect');
  let blurRad: ST_PositiveCoordinate | undefined;
  let dist: ST_PositiveCoordinate | undefined;
  let dir: ST_PositiveFixedAngle | undefined;
  let sx: ST_Percentage | undefined;
  let sy: ST_Percentage | undefined;
  let kx: ST_FixedAngle | undefined;
  let ky: ST_FixedAngle | undefined;
  let algn: ST_RectAlignment | undefined;
  let rotWithShape: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'blurRad': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            blurRad = v;
            continue;
          }
          ctx.invalidValue('CT_OuterShadowEffect', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'dist': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            dist = v;
            continue;
          }
          ctx.invalidValue('CT_OuterShadowEffect', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'dir': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            dir = v;
            continue;
          }
          ctx.invalidValue('CT_OuterShadowEffect', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
        case 'sx': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            sx = v;
            continue;
          }
          ctx.invalidValue('CT_OuterShadowEffect', a, 'ST_Percentage', cur);
          break;
        }
        case 'sy': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            sy = v;
            continue;
          }
          ctx.invalidValue('CT_OuterShadowEffect', a, 'ST_Percentage', cur);
          break;
        }
        case 'kx': {
          const v = parseST_FixedAngle(a.value);
          if (v !== undefined) {
            kx = v;
            continue;
          }
          ctx.invalidValue('CT_OuterShadowEffect', a, 'ST_FixedAngle', cur);
          break;
        }
        case 'ky': {
          const v = parseST_FixedAngle(a.value);
          if (v !== undefined) {
            ky = v;
            continue;
          }
          ctx.invalidValue('CT_OuterShadowEffect', a, 'ST_FixedAngle', cur);
          break;
        }
        case 'algn': {
          const v = parseST_RectAlignment(a.value);
          if (v !== undefined) {
            algn = v;
            continue;
          }
          ctx.invalidValue('CT_OuterShadowEffect', a, 'ST_RectAlignment', cur);
          break;
        }
        case 'rotWithShape': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            rotWithShape = v;
            continue;
          }
          ctx.invalidValue('CT_OuterShadowEffect', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_OuterShadowEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let colorChoice: CT_OuterShadowEffect_ColorChoice | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_OuterShadowEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_OuterShadowEffect', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_OuterShadowEffect', cur);
    cur.next();
  }

  return {
    colorChoice,
    blurRad,
    dist,
    dir,
    sx,
    sy,
    kx,
    ky,
    algn,
    rotWithShape,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Path2D`. See `transitional/dml-main.xsd:2064`. */
export function readCT_Path2D(cur: XmlCursor, ctx: ReadContext): CT_Path2D {
  const start = requireStart(cur, 'CT_Path2D');
  let w: ST_PositiveCoordinate | undefined;
  let h: ST_PositiveCoordinate | undefined;
  let fill: ST_PathFillMode | undefined;
  let stroke: boolean | undefined;
  let extrusionOk: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'w': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            w = v;
            continue;
          }
          ctx.invalidValue('CT_Path2D', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'h': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            h = v;
            continue;
          }
          ctx.invalidValue('CT_Path2D', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'fill': {
          const v = parseST_PathFillMode(a.value);
          if (v !== undefined) {
            fill = v;
            continue;
          }
          ctx.invalidValue('CT_Path2D', a, 'ST_PathFillMode', cur);
          break;
        }
        case 'stroke': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            stroke = v;
            continue;
          }
          ctx.invalidValue('CT_Path2D', a, 'xsd:boolean', cur);
          break;
        }
        case 'extrusionOk': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            extrusionOk = v;
            continue;
          }
          ctx.invalidValue('CT_Path2D', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Path2D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const content: CT_Path2D_Content[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Path2D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'close': {
            content.push({ kind: 'close', value: readCT_Path2DClose(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            content.push({ kind: 'moveTo', value: readCT_Path2DMoveTo(cur, ctx) });
            continue;
          }
          case 'lnTo': {
            content.push({ kind: 'lnTo', value: readCT_Path2DLineTo(cur, ctx) });
            continue;
          }
          case 'arcTo': {
            content.push({ kind: 'arcTo', value: readCT_Path2DArcTo(cur, ctx) });
            continue;
          }
          case 'quadBezTo': {
            content.push({ kind: 'quadBezTo', value: readCT_Path2DQuadBezierTo(cur, ctx) });
            continue;
          }
          case 'cubicBezTo': {
            content.push({ kind: 'cubicBezTo', value: readCT_Path2DCubicBezierTo(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Path2D', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Path2D', cur);
    cur.next();
  }

  return {
    content,
    w,
    h,
    fill,
    stroke,
    extrusionOk,
    $unknownAttrs,
  };
}

/** Read a `CT_Path2DArcTo`. See `transitional/dml-main.xsd:2037`. */
export function readCT_Path2DArcTo(cur: XmlCursor, ctx: ReadContext): CT_Path2DArcTo {
  const start = requireStart(cur, 'CT_Path2DArcTo');
  let wR: ST_AdjCoordinate | undefined;
  let hR: ST_AdjCoordinate | undefined;
  let stAng: ST_AdjAngle | undefined;
  let swAng: ST_AdjAngle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'wR': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            wR = v;
            continue;
          }
          ctx.invalidValue('CT_Path2DArcTo', a, 'ST_AdjCoordinate', cur);
          break;
        }
        case 'hR': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            hR = v;
            continue;
          }
          ctx.invalidValue('CT_Path2DArcTo', a, 'ST_AdjCoordinate', cur);
          break;
        }
        case 'stAng': {
          const v = parseST_AdjAngle(a.value);
          if (v !== undefined) {
            stAng = v;
            continue;
          }
          ctx.invalidValue('CT_Path2DArcTo', a, 'ST_AdjAngle', cur);
          break;
        }
        case 'swAng': {
          const v = parseST_AdjAngle(a.value);
          if (v !== undefined) {
            swAng = v;
            continue;
          }
          ctx.invalidValue('CT_Path2DArcTo', a, 'ST_AdjAngle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Path2DArcTo', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (wR === undefined) ctx.missingRequired('CT_Path2DArcTo', 'wR', cur);
  if (hR === undefined) ctx.missingRequired('CT_Path2DArcTo', 'hR', cur);
  if (stAng === undefined) ctx.missingRequired('CT_Path2DArcTo', 'stAng', cur);
  if (swAng === undefined) ctx.missingRequired('CT_Path2DArcTo', 'swAng', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Path2DArcTo', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Path2DArcTo', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Path2DArcTo', cur);
    cur.next();
  }
  return {
    wR,
    hR,
    stAng,
    swAng,
    $unknownAttrs,
  };
}

/** Read a `CT_Path2DClose`. See `transitional/dml-main.xsd:2053`. */
export function readCT_Path2DClose(cur: XmlCursor, ctx: ReadContext): CT_Path2DClose {
  const start = requireStart(cur, 'CT_Path2DClose');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Path2DClose', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Path2DClose', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Path2DClose', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Path2DClose', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_Path2DCubicBezierTo`. See `transitional/dml-main.xsd:2048`. */
export function readCT_Path2DCubicBezierTo(cur: XmlCursor, ctx: ReadContext): CT_Path2DCubicBezierTo {
  const start = requireStart(cur, 'CT_Path2DCubicBezierTo');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Path2DCubicBezierTo', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const pt: CT_AdjPoint2D[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Path2DCubicBezierTo', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'pt': {
            pt.push(readCT_AdjPoint2D(cur, ctx));
            $slot = 0;
            $index = pt.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Path2DCubicBezierTo', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Path2DCubicBezierTo', cur);
    cur.next();
  }

  return {
    pt,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Path2DLineTo`. See `transitional/dml-main.xsd:2032`. */
export function readCT_Path2DLineTo(cur: XmlCursor, ctx: ReadContext): CT_Path2DLineTo {
  const start = requireStart(cur, 'CT_Path2DLineTo');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Path2DLineTo', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pt: CT_AdjPoint2D | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Path2DLineTo', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'pt': {
            pt = readCT_AdjPoint2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Path2DLineTo', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Path2DLineTo', cur);
    cur.next();
  }

  return {
    pt,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Path2DList`. See `transitional/dml-main.xsd:2079`. */
export function readCT_Path2DList(cur: XmlCursor, ctx: ReadContext): CT_Path2DList {
  const start = requireStart(cur, 'CT_Path2DList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Path2DList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const path: CT_Path2D[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Path2DList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'path': {
            path.push(readCT_Path2D(cur, ctx));
            $slot = 0;
            $index = path.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Path2DList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Path2DList', cur);
    cur.next();
  }

  return {
    path,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Path2DMoveTo`. See `transitional/dml-main.xsd:2027`. */
export function readCT_Path2DMoveTo(cur: XmlCursor, ctx: ReadContext): CT_Path2DMoveTo {
  const start = requireStart(cur, 'CT_Path2DMoveTo');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Path2DMoveTo', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pt: CT_AdjPoint2D | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Path2DMoveTo', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'pt': {
            pt = readCT_AdjPoint2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Path2DMoveTo', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Path2DMoveTo', cur);
    cur.next();
  }

  return {
    pt,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Path2DQuadBezierTo`. See `transitional/dml-main.xsd:2043`. */
export function readCT_Path2DQuadBezierTo(cur: XmlCursor, ctx: ReadContext): CT_Path2DQuadBezierTo {
  const start = requireStart(cur, 'CT_Path2DQuadBezierTo');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Path2DQuadBezierTo', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const pt: CT_AdjPoint2D[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Path2DQuadBezierTo', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'pt': {
            pt.push(readCT_AdjPoint2D(cur, ctx));
            $slot = 0;
            $index = pt.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Path2DQuadBezierTo', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Path2DQuadBezierTo', cur);
    cur.next();
  }

  return {
    pt,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PathShadeProperties`. See `transitional/dml-main.xsd:1429`. */
export function readCT_PathShadeProperties(cur: XmlCursor, ctx: ReadContext): CT_PathShadeProperties {
  const start = requireStart(cur, 'CT_PathShadeProperties');
  let path: ST_PathShadeType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'path': {
          const v = parseST_PathShadeType(a.value);
          if (v !== undefined) {
            path = v;
            continue;
          }
          ctx.invalidValue('CT_PathShadeProperties', a, 'ST_PathShadeType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PathShadeProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let fillToRect: CT_RelativeRect | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PathShadeProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'fillToRect': {
            fillToRect = readCT_RelativeRect(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PathShadeProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PathShadeProperties', cur);
    cur.next();
  }

  return {
    fillToRect,
    path,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PatternFillProperties`. See `transitional/dml-main.xsd:1591`. */
export function readCT_PatternFillProperties(cur: XmlCursor, ctx: ReadContext): CT_PatternFillProperties {
  const start = requireStart(cur, 'CT_PatternFillProperties');
  let prst: ST_PresetPatternVal | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'prst': {
          const v = parseST_PresetPatternVal(a.value);
          if (v !== undefined) {
            prst = v;
            continue;
          }
          ctx.invalidValue('CT_PatternFillProperties', a, 'ST_PresetPatternVal', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PatternFillProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let fgClr: CT_Color | undefined;
  let bgClr: CT_Color | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PatternFillProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'fgClr': {
            fgClr = readCT_Color(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'bgClr': {
            bgClr = readCT_Color(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PatternFillProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PatternFillProperties', cur);
    cur.next();
  }

  return {
    fgClr,
    bgClr,
    prst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Percentage`. See `transitional/dml-main.xsd:242`. */
export function readCT_Percentage(cur: XmlCursor, ctx: ReadContext): CT_Percentage {
  const start = requireStart(cur, 'CT_Percentage');
  let val: ST_Percentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_Percentage', a, 'ST_Percentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Percentage', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_Percentage', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Percentage', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Percentage', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Percentage', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_PictureLocking`. See `transitional/dml-main.xsd:774`. */
export function readCT_PictureLocking(cur: XmlCursor, ctx: ReadContext): CT_PictureLocking {
  const start = requireStart(cur, 'CT_PictureLocking');
  let noGrp: boolean | undefined;
  let noSelect: boolean | undefined;
  let noRot: boolean | undefined;
  let noChangeAspect: boolean | undefined;
  let noMove: boolean | undefined;
  let noResize: boolean | undefined;
  let noEditPoints: boolean | undefined;
  let noAdjustHandles: boolean | undefined;
  let noChangeArrowheads: boolean | undefined;
  let noChangeShapeType: boolean | undefined;
  let noCrop: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'noGrp': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noGrp = v;
            continue;
          }
          ctx.invalidValue('CT_PictureLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noSelect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noSelect = v;
            continue;
          }
          ctx.invalidValue('CT_PictureLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noRot': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noRot = v;
            continue;
          }
          ctx.invalidValue('CT_PictureLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeAspect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeAspect = v;
            continue;
          }
          ctx.invalidValue('CT_PictureLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noMove': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noMove = v;
            continue;
          }
          ctx.invalidValue('CT_PictureLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noResize': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noResize = v;
            continue;
          }
          ctx.invalidValue('CT_PictureLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noEditPoints': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noEditPoints = v;
            continue;
          }
          ctx.invalidValue('CT_PictureLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noAdjustHandles': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noAdjustHandles = v;
            continue;
          }
          ctx.invalidValue('CT_PictureLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeArrowheads': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeArrowheads = v;
            continue;
          }
          ctx.invalidValue('CT_PictureLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeShapeType': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeShapeType = v;
            continue;
          }
          ctx.invalidValue('CT_PictureLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noCrop': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noCrop = v;
            continue;
          }
          ctx.invalidValue('CT_PictureLocking', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PictureLocking', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PictureLocking', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PictureLocking', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PictureLocking', cur);
    cur.next();
  }

  return {
    extLst,
    noGrp,
    noSelect,
    noRot,
    noChangeAspect,
    noMove,
    noResize,
    noEditPoints,
    noAdjustHandles,
    noChangeArrowheads,
    noChangeShapeType,
    noCrop,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Point2D`. See `transitional/dml-main.xsd:284`. */
export function readCT_Point2D(cur: XmlCursor, ctx: ReadContext): CT_Point2D {
  const start = requireStart(cur, 'CT_Point2D');
  let x: ST_Coordinate | undefined;
  let y: ST_Coordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'x': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            x = v;
            continue;
          }
          ctx.invalidValue('CT_Point2D', a, 'ST_Coordinate', cur);
          break;
        }
        case 'y': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            y = v;
            continue;
          }
          ctx.invalidValue('CT_Point2D', a, 'ST_Coordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Point2D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (x === undefined) ctx.missingRequired('CT_Point2D', 'x', cur);
  if (y === undefined) ctx.missingRequired('CT_Point2D', 'y', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Point2D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Point2D', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Point2D', cur);
    cur.next();
  }
  return {
    x,
    y,
    $unknownAttrs,
  };
}

/** Read a `CT_Point3D`. See `transitional/dml-main.xsd:655`. */
export function readCT_Point3D(cur: XmlCursor, ctx: ReadContext): CT_Point3D {
  const start = requireStart(cur, 'CT_Point3D');
  let x: ST_Coordinate | undefined;
  let y: ST_Coordinate | undefined;
  let z: ST_Coordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'x': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            x = v;
            continue;
          }
          ctx.invalidValue('CT_Point3D', a, 'ST_Coordinate', cur);
          break;
        }
        case 'y': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            y = v;
            continue;
          }
          ctx.invalidValue('CT_Point3D', a, 'ST_Coordinate', cur);
          break;
        }
        case 'z': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            z = v;
            continue;
          }
          ctx.invalidValue('CT_Point3D', a, 'ST_Coordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Point3D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (x === undefined) ctx.missingRequired('CT_Point3D', 'x', cur);
  if (y === undefined) ctx.missingRequired('CT_Point3D', 'y', cur);
  if (z === undefined) ctx.missingRequired('CT_Point3D', 'z', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Point3D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Point3D', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Point3D', cur);
    cur.next();
  }
  return {
    x,
    y,
    z,
    $unknownAttrs,
  };
}

/** Read a `CT_PolarAdjustHandle`. See `transitional/dml-main.xsd:1995`. */
export function readCT_PolarAdjustHandle(cur: XmlCursor, ctx: ReadContext): CT_PolarAdjustHandle {
  const start = requireStart(cur, 'CT_PolarAdjustHandle');
  let gdRefR: ST_GeomGuideName | undefined;
  let minR: ST_AdjCoordinate | undefined;
  let maxR: ST_AdjCoordinate | undefined;
  let gdRefAng: ST_GeomGuideName | undefined;
  let minAng: ST_AdjAngle | undefined;
  let maxAng: ST_AdjAngle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'gdRefR': {
          const v = parseST_GeomGuideName(a.value);
          if (v !== undefined) {
            gdRefR = v;
            continue;
          }
          ctx.invalidValue('CT_PolarAdjustHandle', a, 'ST_GeomGuideName', cur);
          break;
        }
        case 'minR': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            minR = v;
            continue;
          }
          ctx.invalidValue('CT_PolarAdjustHandle', a, 'ST_AdjCoordinate', cur);
          break;
        }
        case 'maxR': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            maxR = v;
            continue;
          }
          ctx.invalidValue('CT_PolarAdjustHandle', a, 'ST_AdjCoordinate', cur);
          break;
        }
        case 'gdRefAng': {
          const v = parseST_GeomGuideName(a.value);
          if (v !== undefined) {
            gdRefAng = v;
            continue;
          }
          ctx.invalidValue('CT_PolarAdjustHandle', a, 'ST_GeomGuideName', cur);
          break;
        }
        case 'minAng': {
          const v = parseST_AdjAngle(a.value);
          if (v !== undefined) {
            minAng = v;
            continue;
          }
          ctx.invalidValue('CT_PolarAdjustHandle', a, 'ST_AdjAngle', cur);
          break;
        }
        case 'maxAng': {
          const v = parseST_AdjAngle(a.value);
          if (v !== undefined) {
            maxAng = v;
            continue;
          }
          ctx.invalidValue('CT_PolarAdjustHandle', a, 'ST_AdjAngle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PolarAdjustHandle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pos: CT_AdjPoint2D | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PolarAdjustHandle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'pos': {
            pos = readCT_AdjPoint2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PolarAdjustHandle', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PolarAdjustHandle', cur);
    cur.next();
  }

  return {
    pos,
    gdRefR,
    minR,
    maxR,
    gdRefAng,
    minAng,
    maxAng,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PositiveFixedAngle`. See `transitional/dml-main.xsd:233`. */
export function readCT_PositiveFixedAngle(cur: XmlCursor, ctx: ReadContext): CT_PositiveFixedAngle {
  const start = requireStart(cur, 'CT_PositiveFixedAngle');
  let val: ST_PositiveFixedAngle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_PositiveFixedAngle', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PositiveFixedAngle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_PositiveFixedAngle', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PositiveFixedAngle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PositiveFixedAngle', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PositiveFixedAngle', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_PositiveFixedPercentage`. See `transitional/dml-main.xsd:277`. */
export function readCT_PositiveFixedPercentage(cur: XmlCursor, ctx: ReadContext): CT_PositiveFixedPercentage {
  const start = requireStart(cur, 'CT_PositiveFixedPercentage');
  let val: ST_PositiveFixedPercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_PositiveFixedPercentage', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PositiveFixedPercentage', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_PositiveFixedPercentage', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PositiveFixedPercentage', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PositiveFixedPercentage', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PositiveFixedPercentage', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_PositivePercentage`. See `transitional/dml-main.xsd:253`. */
export function readCT_PositivePercentage(cur: XmlCursor, ctx: ReadContext): CT_PositivePercentage {
  const start = requireStart(cur, 'CT_PositivePercentage');
  let val: ST_PositivePercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_PositivePercentage(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_PositivePercentage', a, 'ST_PositivePercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PositivePercentage', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_PositivePercentage', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PositivePercentage', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PositivePercentage', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PositivePercentage', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_PositiveSize2D`. See `transitional/dml-main.xsd:288`. */
export function readCT_PositiveSize2D(cur: XmlCursor, ctx: ReadContext): CT_PositiveSize2D {
  const start = requireStart(cur, 'CT_PositiveSize2D');
  let cx: ST_PositiveCoordinate | undefined;
  let cy: ST_PositiveCoordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'cx': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            cx = v;
            continue;
          }
          ctx.invalidValue('CT_PositiveSize2D', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'cy': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            cy = v;
            continue;
          }
          ctx.invalidValue('CT_PositiveSize2D', a, 'ST_PositiveCoordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PositiveSize2D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (cx === undefined) ctx.missingRequired('CT_PositiveSize2D', 'cx', cur);
  if (cy === undefined) ctx.missingRequired('CT_PositiveSize2D', 'cy', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PositiveSize2D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PositiveSize2D', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PositiveSize2D', cur);
    cur.next();
  }
  return {
    cx,
    cy,
    $unknownAttrs,
  };
}

/** Read a `CT_PresetColor`. See `transitional/dml-main.xsd:613`. */
export function readCT_PresetColor(cur: XmlCursor, ctx: ReadContext): CT_PresetColor {
  const start = requireStart(cur, 'CT_PresetColor');
  let val: ST_PresetColorVal | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_PresetColorVal(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_PresetColor', a, 'ST_PresetColorVal', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PresetColor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_PresetColor', 'val', cur);

  const colorTransform: CT_PresetColor_ColorTransform[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PresetColor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tint': {
            colorTransform.push({ kind: 'tint', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'shade': {
            colorTransform.push({ kind: 'shade', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'comp': {
            colorTransform.push({ kind: 'comp', value: readCT_ComplementTransform(cur, ctx) });
            continue;
          }
          case 'inv': {
            colorTransform.push({ kind: 'inv', value: readCT_InverseTransform(cur, ctx) });
            continue;
          }
          case 'gray': {
            colorTransform.push({ kind: 'gray', value: readCT_GrayscaleTransform(cur, ctx) });
            continue;
          }
          case 'alpha': {
            colorTransform.push({ kind: 'alpha', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'alphaOff': {
            colorTransform.push({ kind: 'alphaOff', value: readCT_FixedPercentage(cur, ctx) });
            continue;
          }
          case 'alphaMod': {
            colorTransform.push({ kind: 'alphaMod', value: readCT_PositivePercentage(cur, ctx) });
            continue;
          }
          case 'hue': {
            colorTransform.push({ kind: 'hue', value: readCT_PositiveFixedAngle(cur, ctx) });
            continue;
          }
          case 'hueOff': {
            colorTransform.push({ kind: 'hueOff', value: readCT_Angle(cur, ctx) });
            continue;
          }
          case 'hueMod': {
            colorTransform.push({ kind: 'hueMod', value: readCT_PositivePercentage(cur, ctx) });
            continue;
          }
          case 'sat': {
            colorTransform.push({ kind: 'sat', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'satOff': {
            colorTransform.push({ kind: 'satOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'satMod': {
            colorTransform.push({ kind: 'satMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lum': {
            colorTransform.push({ kind: 'lum', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lumOff': {
            colorTransform.push({ kind: 'lumOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lumMod': {
            colorTransform.push({ kind: 'lumMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'red': {
            colorTransform.push({ kind: 'red', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'redOff': {
            colorTransform.push({ kind: 'redOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'redMod': {
            colorTransform.push({ kind: 'redMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'green': {
            colorTransform.push({ kind: 'green', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'greenOff': {
            colorTransform.push({ kind: 'greenOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'greenMod': {
            colorTransform.push({ kind: 'greenMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blue': {
            colorTransform.push({ kind: 'blue', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blueOff': {
            colorTransform.push({ kind: 'blueOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blueMod': {
            colorTransform.push({ kind: 'blueMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'gamma': {
            colorTransform.push({ kind: 'gamma', value: readCT_GammaTransform(cur, ctx) });
            continue;
          }
          case 'invGamma': {
            colorTransform.push({ kind: 'invGamma', value: readCT_InverseGammaTransform(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PresetColor', ev.localName, ev.uri, cur);
      colorTransform.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PresetColor', cur);
    cur.next();
  }

  return {
    colorTransform,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_PresetGeometry2D`. See `transitional/dml-main.xsd:2084`. */
export function readCT_PresetGeometry2D(cur: XmlCursor, ctx: ReadContext): CT_PresetGeometry2D {
  const start = requireStart(cur, 'CT_PresetGeometry2D');
  let prst: ST_ShapeType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'prst': {
          const v = parseST_ShapeType(a.value);
          if (v !== undefined) {
            prst = v;
            continue;
          }
          ctx.invalidValue('CT_PresetGeometry2D', a, 'ST_ShapeType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PresetGeometry2D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (prst === undefined) ctx.missingRequired('CT_PresetGeometry2D', 'prst', cur);

  let avLst: CT_GeomGuideList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PresetGeometry2D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'avLst': {
            avLst = readCT_GeomGuideList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PresetGeometry2D', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PresetGeometry2D', cur);
    cur.next();
  }

  return {
    avLst,
    prst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PresetLineDashProperties`. See `transitional/dml-main.xsd:2182`. */
export function readCT_PresetLineDashProperties(cur: XmlCursor, ctx: ReadContext): CT_PresetLineDashProperties {
  const start = requireStart(cur, 'CT_PresetLineDashProperties');
  let val: ST_PresetLineDashVal | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_PresetLineDashVal(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_PresetLineDashProperties', a, 'ST_PresetLineDashVal', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PresetLineDashProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PresetLineDashProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_PresetLineDashProperties', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PresetLineDashProperties', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_PresetShadowEffect`. See `transitional/dml-main.xsd:1369`. */
export function readCT_PresetShadowEffect(cur: XmlCursor, ctx: ReadContext): CT_PresetShadowEffect {
  const start = requireStart(cur, 'CT_PresetShadowEffect');
  let prst: ST_PresetShadowVal | undefined;
  let dist: ST_PositiveCoordinate | undefined;
  let dir: ST_PositiveFixedAngle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'prst': {
          const v = parseST_PresetShadowVal(a.value);
          if (v !== undefined) {
            prst = v;
            continue;
          }
          ctx.invalidValue('CT_PresetShadowEffect', a, 'ST_PresetShadowVal', cur);
          break;
        }
        case 'dist': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            dist = v;
            continue;
          }
          ctx.invalidValue('CT_PresetShadowEffect', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'dir': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            dir = v;
            continue;
          }
          ctx.invalidValue('CT_PresetShadowEffect', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PresetShadowEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (prst === undefined) ctx.missingRequired('CT_PresetShadowEffect', 'prst', cur);

  let colorChoice: CT_PresetShadowEffect_ColorChoice | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PresetShadowEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PresetShadowEffect', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PresetShadowEffect', cur);
    cur.next();
  }

  return {
    colorChoice,
    prst,
    dist,
    dir,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PresetTextShape`. See `transitional/dml-main.xsd:2090`. */
export function readCT_PresetTextShape(cur: XmlCursor, ctx: ReadContext): CT_PresetTextShape {
  const start = requireStart(cur, 'CT_PresetTextShape');
  let prst: ST_TextShapeType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'prst': {
          const v = parseST_TextShapeType(a.value);
          if (v !== undefined) {
            prst = v;
            continue;
          }
          ctx.invalidValue('CT_PresetTextShape', a, 'ST_TextShapeType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PresetTextShape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (prst === undefined) ctx.missingRequired('CT_PresetTextShape', 'prst', cur);

  let avLst: CT_GeomGuideList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PresetTextShape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'avLst': {
            avLst = readCT_GeomGuideList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PresetTextShape', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PresetTextShape', cur);
    cur.next();
  }

  return {
    avLst,
    prst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_QuickTimeFile`. See `transitional/dml-main.xsd:34`. */
export function readCT_QuickTimeFile(cur: XmlCursor, ctx: ReadContext): CT_QuickTimeFile {
  const start = requireStart(cur, 'CT_QuickTimeFile');
  let link: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'link': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            link = v;
            continue;
          }
          ctx.invalidValue('CT_QuickTimeFile', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_QuickTimeFile', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (link === undefined) ctx.missingRequired('CT_QuickTimeFile', 'link', cur);

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_QuickTimeFile', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_QuickTimeFile', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_QuickTimeFile', cur);
    cur.next();
  }

  return {
    extLst,
    link,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Ratio`. See `transitional/dml-main.xsd:280`. */
export function readCT_Ratio(cur: XmlCursor, ctx: ReadContext): CT_Ratio {
  const start = requireStart(cur, 'CT_Ratio');
  let n: number | undefined;
  let d: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'n': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            n = v;
            continue;
          }
          ctx.invalidValue('CT_Ratio', a, 'xsd:long', cur);
          break;
        }
        case 'd': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            d = v;
            continue;
          }
          ctx.invalidValue('CT_Ratio', a, 'xsd:long', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Ratio', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (n === undefined) ctx.missingRequired('CT_Ratio', 'n', cur);
  if (d === undefined) ctx.missingRequired('CT_Ratio', 'd', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Ratio', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Ratio', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Ratio', cur);
    cur.next();
  }
  return {
    n,
    d,
    $unknownAttrs,
  };
}

/** Read a `CT_ReflectionEffect`. See `transitional/dml-main.xsd:1377`. */
export function readCT_ReflectionEffect(cur: XmlCursor, ctx: ReadContext): CT_ReflectionEffect {
  const start = requireStart(cur, 'CT_ReflectionEffect');
  let blurRad: ST_PositiveCoordinate | undefined;
  let stA: ST_PositiveFixedPercentage | undefined;
  let stPos: ST_PositiveFixedPercentage | undefined;
  let endA: ST_PositiveFixedPercentage | undefined;
  let endPos: ST_PositiveFixedPercentage | undefined;
  let dist: ST_PositiveCoordinate | undefined;
  let dir: ST_PositiveFixedAngle | undefined;
  let fadeDir: ST_PositiveFixedAngle | undefined;
  let sx: ST_Percentage | undefined;
  let sy: ST_Percentage | undefined;
  let kx: ST_FixedAngle | undefined;
  let ky: ST_FixedAngle | undefined;
  let algn: ST_RectAlignment | undefined;
  let rotWithShape: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'blurRad': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            blurRad = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'stA': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            stA = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
        case 'stPos': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            stPos = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
        case 'endA': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            endA = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
        case 'endPos': {
          const v = parseST_PositiveFixedPercentage(a.value);
          if (v !== undefined) {
            endPos = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_PositiveFixedPercentage', cur);
          break;
        }
        case 'dist': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            dist = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'dir': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            dir = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
        case 'fadeDir': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            fadeDir = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
        case 'sx': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            sx = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_Percentage', cur);
          break;
        }
        case 'sy': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            sy = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_Percentage', cur);
          break;
        }
        case 'kx': {
          const v = parseST_FixedAngle(a.value);
          if (v !== undefined) {
            kx = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_FixedAngle', cur);
          break;
        }
        case 'ky': {
          const v = parseST_FixedAngle(a.value);
          if (v !== undefined) {
            ky = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_FixedAngle', cur);
          break;
        }
        case 'algn': {
          const v = parseST_RectAlignment(a.value);
          if (v !== undefined) {
            algn = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'ST_RectAlignment', cur);
          break;
        }
        case 'rotWithShape': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            rotWithShape = v;
            continue;
          }
          ctx.invalidValue('CT_ReflectionEffect', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ReflectionEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ReflectionEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_ReflectionEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ReflectionEffect', cur);
    cur.next();
  }
  return {
    blurRad,
    stA,
    stPos,
    endA,
    endPos,
    dist,
    dir,
    fadeDir,
    sx,
    sy,
    kx,
    ky,
    algn,
    rotWithShape,
    $unknownAttrs,
  };
}

/** Read a `CT_RegularTextRun`. See `transitional/dml-main.xsd:3075`. */
export function readCT_RegularTextRun(cur: XmlCursor, ctx: ReadContext): CT_RegularTextRun {
  const start = requireStart(cur, 'CT_RegularTextRun');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_RegularTextRun', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let rPr: CT_TextCharacterProperties | undefined;
  let t: string | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RegularTextRun', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'rPr': {
            rPr = readCT_TextCharacterProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 't': {
            t = readScalar(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RegularTextRun', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RegularTextRun', cur);
    cur.next();
  }

  return {
    rPr,
    t,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_RelativeOffsetEffect`. See `transitional/dml-main.xsd:1393`. */
export function readCT_RelativeOffsetEffect(cur: XmlCursor, ctx: ReadContext): CT_RelativeOffsetEffect {
  const start = requireStart(cur, 'CT_RelativeOffsetEffect');
  let tx: ST_Percentage | undefined;
  let ty: ST_Percentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'tx': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            tx = v;
            continue;
          }
          ctx.invalidValue('CT_RelativeOffsetEffect', a, 'ST_Percentage', cur);
          break;
        }
        case 'ty': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            ty = v;
            continue;
          }
          ctx.invalidValue('CT_RelativeOffsetEffect', a, 'ST_Percentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_RelativeOffsetEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RelativeOffsetEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_RelativeOffsetEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RelativeOffsetEffect', cur);
    cur.next();
  }
  return {
    tx,
    ty,
    $unknownAttrs,
  };
}

/** Read a `CT_RelativeRect`. See `transitional/dml-main.xsd:670`. */
export function readCT_RelativeRect(cur: XmlCursor, ctx: ReadContext): CT_RelativeRect {
  const start = requireStart(cur, 'CT_RelativeRect');
  let l: ST_Percentage | undefined;
  let t: ST_Percentage | undefined;
  let r: ST_Percentage | undefined;
  let b: ST_Percentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'l': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            l = v;
            continue;
          }
          ctx.invalidValue('CT_RelativeRect', a, 'ST_Percentage', cur);
          break;
        }
        case 't': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            t = v;
            continue;
          }
          ctx.invalidValue('CT_RelativeRect', a, 'ST_Percentage', cur);
          break;
        }
        case 'r': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            r = v;
            continue;
          }
          ctx.invalidValue('CT_RelativeRect', a, 'ST_Percentage', cur);
          break;
        }
        case 'b': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            b = v;
            continue;
          }
          ctx.invalidValue('CT_RelativeRect', a, 'ST_Percentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_RelativeRect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RelativeRect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_RelativeRect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RelativeRect', cur);
    cur.next();
  }
  return {
    l,
    t,
    r,
    b,
    $unknownAttrs,
  };
}

/** Read a `CT_SRgbColor`. See `transitional/dml-main.xsd:337`. */
export function readCT_SRgbColor(cur: XmlCursor, ctx: ReadContext): CT_SRgbColor {
  const start = requireStart(cur, 'CT_SRgbColor');
  let val: ST_HexColorRGB | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_HexColorRGB(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_SRgbColor', a, 'ST_HexColorRGB', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SRgbColor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_SRgbColor', 'val', cur);

  const colorTransform: CT_SRgbColor_ColorTransform[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SRgbColor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tint': {
            colorTransform.push({ kind: 'tint', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'shade': {
            colorTransform.push({ kind: 'shade', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'comp': {
            colorTransform.push({ kind: 'comp', value: readCT_ComplementTransform(cur, ctx) });
            continue;
          }
          case 'inv': {
            colorTransform.push({ kind: 'inv', value: readCT_InverseTransform(cur, ctx) });
            continue;
          }
          case 'gray': {
            colorTransform.push({ kind: 'gray', value: readCT_GrayscaleTransform(cur, ctx) });
            continue;
          }
          case 'alpha': {
            colorTransform.push({ kind: 'alpha', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'alphaOff': {
            colorTransform.push({ kind: 'alphaOff', value: readCT_FixedPercentage(cur, ctx) });
            continue;
          }
          case 'alphaMod': {
            colorTransform.push({ kind: 'alphaMod', value: readCT_PositivePercentage(cur, ctx) });
            continue;
          }
          case 'hue': {
            colorTransform.push({ kind: 'hue', value: readCT_PositiveFixedAngle(cur, ctx) });
            continue;
          }
          case 'hueOff': {
            colorTransform.push({ kind: 'hueOff', value: readCT_Angle(cur, ctx) });
            continue;
          }
          case 'hueMod': {
            colorTransform.push({ kind: 'hueMod', value: readCT_PositivePercentage(cur, ctx) });
            continue;
          }
          case 'sat': {
            colorTransform.push({ kind: 'sat', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'satOff': {
            colorTransform.push({ kind: 'satOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'satMod': {
            colorTransform.push({ kind: 'satMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lum': {
            colorTransform.push({ kind: 'lum', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lumOff': {
            colorTransform.push({ kind: 'lumOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lumMod': {
            colorTransform.push({ kind: 'lumMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'red': {
            colorTransform.push({ kind: 'red', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'redOff': {
            colorTransform.push({ kind: 'redOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'redMod': {
            colorTransform.push({ kind: 'redMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'green': {
            colorTransform.push({ kind: 'green', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'greenOff': {
            colorTransform.push({ kind: 'greenOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'greenMod': {
            colorTransform.push({ kind: 'greenMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blue': {
            colorTransform.push({ kind: 'blue', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blueOff': {
            colorTransform.push({ kind: 'blueOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blueMod': {
            colorTransform.push({ kind: 'blueMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'gamma': {
            colorTransform.push({ kind: 'gamma', value: readCT_GammaTransform(cur, ctx) });
            continue;
          }
          case 'invGamma': {
            colorTransform.push({ kind: 'invGamma', value: readCT_InverseGammaTransform(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SRgbColor', ev.localName, ev.uri, cur);
      colorTransform.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SRgbColor', cur);
    cur.next();
  }

  return {
    colorTransform,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_ScRgbColor`. See `transitional/dml-main.xsd:329`. */
export function readCT_ScRgbColor(cur: XmlCursor, ctx: ReadContext): CT_ScRgbColor {
  const start = requireStart(cur, 'CT_ScRgbColor');
  let r: ST_Percentage | undefined;
  let g: ST_Percentage | undefined;
  let b: ST_Percentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'r': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            r = v;
            continue;
          }
          ctx.invalidValue('CT_ScRgbColor', a, 'ST_Percentage', cur);
          break;
        }
        case 'g': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            g = v;
            continue;
          }
          ctx.invalidValue('CT_ScRgbColor', a, 'ST_Percentage', cur);
          break;
        }
        case 'b': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            b = v;
            continue;
          }
          ctx.invalidValue('CT_ScRgbColor', a, 'ST_Percentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ScRgbColor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (r === undefined) ctx.missingRequired('CT_ScRgbColor', 'r', cur);
  if (g === undefined) ctx.missingRequired('CT_ScRgbColor', 'g', cur);
  if (b === undefined) ctx.missingRequired('CT_ScRgbColor', 'b', cur);

  const colorTransform: CT_ScRgbColor_ColorTransform[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ScRgbColor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tint': {
            colorTransform.push({ kind: 'tint', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'shade': {
            colorTransform.push({ kind: 'shade', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'comp': {
            colorTransform.push({ kind: 'comp', value: readCT_ComplementTransform(cur, ctx) });
            continue;
          }
          case 'inv': {
            colorTransform.push({ kind: 'inv', value: readCT_InverseTransform(cur, ctx) });
            continue;
          }
          case 'gray': {
            colorTransform.push({ kind: 'gray', value: readCT_GrayscaleTransform(cur, ctx) });
            continue;
          }
          case 'alpha': {
            colorTransform.push({ kind: 'alpha', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'alphaOff': {
            colorTransform.push({ kind: 'alphaOff', value: readCT_FixedPercentage(cur, ctx) });
            continue;
          }
          case 'alphaMod': {
            colorTransform.push({ kind: 'alphaMod', value: readCT_PositivePercentage(cur, ctx) });
            continue;
          }
          case 'hue': {
            colorTransform.push({ kind: 'hue', value: readCT_PositiveFixedAngle(cur, ctx) });
            continue;
          }
          case 'hueOff': {
            colorTransform.push({ kind: 'hueOff', value: readCT_Angle(cur, ctx) });
            continue;
          }
          case 'hueMod': {
            colorTransform.push({ kind: 'hueMod', value: readCT_PositivePercentage(cur, ctx) });
            continue;
          }
          case 'sat': {
            colorTransform.push({ kind: 'sat', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'satOff': {
            colorTransform.push({ kind: 'satOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'satMod': {
            colorTransform.push({ kind: 'satMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lum': {
            colorTransform.push({ kind: 'lum', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lumOff': {
            colorTransform.push({ kind: 'lumOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lumMod': {
            colorTransform.push({ kind: 'lumMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'red': {
            colorTransform.push({ kind: 'red', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'redOff': {
            colorTransform.push({ kind: 'redOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'redMod': {
            colorTransform.push({ kind: 'redMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'green': {
            colorTransform.push({ kind: 'green', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'greenOff': {
            colorTransform.push({ kind: 'greenOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'greenMod': {
            colorTransform.push({ kind: 'greenMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blue': {
            colorTransform.push({ kind: 'blue', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blueOff': {
            colorTransform.push({ kind: 'blueOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blueMod': {
            colorTransform.push({ kind: 'blueMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'gamma': {
            colorTransform.push({ kind: 'gamma', value: readCT_GammaTransform(cur, ctx) });
            continue;
          }
          case 'invGamma': {
            colorTransform.push({ kind: 'invGamma', value: readCT_InverseGammaTransform(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ScRgbColor', ev.localName, ev.uri, cur);
      colorTransform.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ScRgbColor', cur);
    cur.next();
  }

  return {
    colorTransform,
    r,
    g,
    b,
    $unknownAttrs,
  };
}

/** Read a `CT_Scale2D`. See `transitional/dml-main.xsd:629`. */
export function readCT_Scale2D(cur: XmlCursor, ctx: ReadContext): CT_Scale2D {
  const start = requireStart(cur, 'CT_Scale2D');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Scale2D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let sx: CT_Ratio | undefined;
  let sy: CT_Ratio | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Scale2D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'sx': {
            sx = readCT_Ratio(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sy': {
            sy = readCT_Ratio(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Scale2D', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Scale2D', cur);
    cur.next();
  }

  return {
    sx,
    sy,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Scene3D`. See `transitional/dml-main.xsd:1185`. */
export function readCT_Scene3D(cur: XmlCursor, ctx: ReadContext): CT_Scene3D {
  const start = requireStart(cur, 'CT_Scene3D');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Scene3D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let camera: CT_Camera | undefined;
  let lightRig: CT_LightRig | undefined;
  let backdrop: CT_Backdrop | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Scene3D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'camera': {
            camera = readCT_Camera(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lightRig': {
            lightRig = readCT_LightRig(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'backdrop': {
            backdrop = readCT_Backdrop(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Scene3D', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Scene3D', cur);
    cur.next();
  }

  return {
    camera,
    lightRig,
    backdrop,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SchemeColor`. See `transitional/dml-main.xsd:413`. */
export function readCT_SchemeColor(cur: XmlCursor, ctx: ReadContext): CT_SchemeColor {
  const start = requireStart(cur, 'CT_SchemeColor');
  let val: ST_SchemeColorVal | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_SchemeColorVal(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_SchemeColor', a, 'ST_SchemeColorVal', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SchemeColor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_SchemeColor', 'val', cur);

  const colorTransform: CT_SchemeColor_ColorTransform[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SchemeColor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tint': {
            colorTransform.push({ kind: 'tint', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'shade': {
            colorTransform.push({ kind: 'shade', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'comp': {
            colorTransform.push({ kind: 'comp', value: readCT_ComplementTransform(cur, ctx) });
            continue;
          }
          case 'inv': {
            colorTransform.push({ kind: 'inv', value: readCT_InverseTransform(cur, ctx) });
            continue;
          }
          case 'gray': {
            colorTransform.push({ kind: 'gray', value: readCT_GrayscaleTransform(cur, ctx) });
            continue;
          }
          case 'alpha': {
            colorTransform.push({ kind: 'alpha', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'alphaOff': {
            colorTransform.push({ kind: 'alphaOff', value: readCT_FixedPercentage(cur, ctx) });
            continue;
          }
          case 'alphaMod': {
            colorTransform.push({ kind: 'alphaMod', value: readCT_PositivePercentage(cur, ctx) });
            continue;
          }
          case 'hue': {
            colorTransform.push({ kind: 'hue', value: readCT_PositiveFixedAngle(cur, ctx) });
            continue;
          }
          case 'hueOff': {
            colorTransform.push({ kind: 'hueOff', value: readCT_Angle(cur, ctx) });
            continue;
          }
          case 'hueMod': {
            colorTransform.push({ kind: 'hueMod', value: readCT_PositivePercentage(cur, ctx) });
            continue;
          }
          case 'sat': {
            colorTransform.push({ kind: 'sat', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'satOff': {
            colorTransform.push({ kind: 'satOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'satMod': {
            colorTransform.push({ kind: 'satMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lum': {
            colorTransform.push({ kind: 'lum', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lumOff': {
            colorTransform.push({ kind: 'lumOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lumMod': {
            colorTransform.push({ kind: 'lumMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'red': {
            colorTransform.push({ kind: 'red', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'redOff': {
            colorTransform.push({ kind: 'redOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'redMod': {
            colorTransform.push({ kind: 'redMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'green': {
            colorTransform.push({ kind: 'green', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'greenOff': {
            colorTransform.push({ kind: 'greenOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'greenMod': {
            colorTransform.push({ kind: 'greenMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blue': {
            colorTransform.push({ kind: 'blue', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blueOff': {
            colorTransform.push({ kind: 'blueOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blueMod': {
            colorTransform.push({ kind: 'blueMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'gamma': {
            colorTransform.push({ kind: 'gamma', value: readCT_GammaTransform(cur, ctx) });
            continue;
          }
          case 'invGamma': {
            colorTransform.push({ kind: 'invGamma', value: readCT_InverseGammaTransform(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SchemeColor', ev.localName, ev.uri, cur);
      colorTransform.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SchemeColor', cur);
    cur.next();
  }

  return {
    colorTransform,
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_Shape3D`. See `transitional/dml-main.xsd:1241`. */
export function readCT_Shape3D(cur: XmlCursor, ctx: ReadContext): CT_Shape3D {
  const start = requireStart(cur, 'CT_Shape3D');
  let z: ST_Coordinate | undefined;
  let extrusionH: ST_PositiveCoordinate | undefined;
  let contourW: ST_PositiveCoordinate | undefined;
  let prstMaterial: ST_PresetMaterialType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'z': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            z = v;
            continue;
          }
          ctx.invalidValue('CT_Shape3D', a, 'ST_Coordinate', cur);
          break;
        }
        case 'extrusionH': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            extrusionH = v;
            continue;
          }
          ctx.invalidValue('CT_Shape3D', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'contourW': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            contourW = v;
            continue;
          }
          ctx.invalidValue('CT_Shape3D', a, 'ST_PositiveCoordinate', cur);
          break;
        }
        case 'prstMaterial': {
          const v = parseST_PresetMaterialType(a.value);
          if (v !== undefined) {
            prstMaterial = v;
            continue;
          }
          ctx.invalidValue('CT_Shape3D', a, 'ST_PresetMaterialType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Shape3D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let bevelT: CT_Bevel | undefined;
  let bevelB: CT_Bevel | undefined;
  let extrusionClr: CT_Color | undefined;
  let contourClr: CT_Color | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Shape3D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'bevelT': {
            bevelT = readCT_Bevel(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'bevelB': {
            bevelB = readCT_Bevel(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extrusionClr': {
            extrusionClr = readCT_Color(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'contourClr': {
            contourClr = readCT_Color(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Shape3D', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Shape3D', cur);
    cur.next();
  }

  return {
    bevelT,
    bevelB,
    extrusionClr,
    contourClr,
    extLst,
    z,
    extrusionH,
    contourW,
    prstMaterial,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ShapeLocking`. See `transitional/dml-main.xsd:767`. */
export function readCT_ShapeLocking(cur: XmlCursor, ctx: ReadContext): CT_ShapeLocking {
  const start = requireStart(cur, 'CT_ShapeLocking');
  let noGrp: boolean | undefined;
  let noSelect: boolean | undefined;
  let noRot: boolean | undefined;
  let noChangeAspect: boolean | undefined;
  let noMove: boolean | undefined;
  let noResize: boolean | undefined;
  let noEditPoints: boolean | undefined;
  let noAdjustHandles: boolean | undefined;
  let noChangeArrowheads: boolean | undefined;
  let noChangeShapeType: boolean | undefined;
  let noTextEdit: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'noGrp': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noGrp = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noSelect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noSelect = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noRot': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noRot = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeAspect': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeAspect = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noMove': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noMove = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noResize': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noResize = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noEditPoints': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noEditPoints = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noAdjustHandles': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noAdjustHandles = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeArrowheads': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeArrowheads = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noChangeShapeType': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noChangeShapeType = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeLocking', a, 'xsd:boolean', cur);
          break;
        }
        case 'noTextEdit': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noTextEdit = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeLocking', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ShapeLocking', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ShapeLocking', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ShapeLocking', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ShapeLocking', cur);
    cur.next();
  }

  return {
    extLst,
    noGrp,
    noSelect,
    noRot,
    noChangeAspect,
    noMove,
    noResize,
    noEditPoints,
    noAdjustHandles,
    noChangeArrowheads,
    noChangeShapeType,
    noTextEdit,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ShapeProperties`. See `transitional/dml-main.xsd:2245`. */
export function readCT_ShapeProperties(cur: XmlCursor, ctx: ReadContext): CT_ShapeProperties {
  const start = requireStart(cur, 'CT_ShapeProperties');
  let bwMode: ST_BlackWhiteMode | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'bwMode': {
          const v = parseST_BlackWhiteMode(a.value);
          if (v !== undefined) {
            bwMode = v;
            continue;
          }
          ctx.invalidValue('CT_ShapeProperties', a, 'ST_BlackWhiteMode', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_ShapeProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let xfrm: CT_Transform2D | undefined;
  let geometry: CT_ShapeProperties_Geometry | undefined;
  let fillProperties: CT_ShapeProperties_FillProperties | undefined;
  let ln: CT_LineProperties | undefined;
  let effectProperties: CT_ShapeProperties_EffectProperties | undefined;
  let scene3d: CT_Scene3D | undefined;
  let sp3d: CT_Shape3D | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ShapeProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'xfrm': {
            xfrm = readCT_Transform2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'custGeom': {
            geometry = { kind: 'custGeom', value: readCT_CustomGeometry2D(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'prstGeom': {
            geometry = { kind: 'prstGeom', value: readCT_PresetGeometry2D(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'noFill': {
            fillProperties = { kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'solidFill': {
            fillProperties = { kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'gradFill': {
            fillProperties = { kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            fillProperties = { kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'pattFill': {
            fillProperties = { kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'grpFill': {
            fillProperties = { kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'ln': {
            ln = readCT_LineProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'effectLst': {
            effectProperties = { kind: 'effectLst', value: readCT_EffectList(cur, ctx) };
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'effectDag': {
            effectProperties = { kind: 'effectDag', value: readCT_EffectContainer(cur, ctx) };
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'scene3d': {
            scene3d = readCT_Scene3D(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'sp3d': {
            sp3d = readCT_Shape3D(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ShapeProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ShapeProperties', cur);
    cur.next();
  }

  return {
    xfrm,
    geometry,
    fillProperties,
    ln,
    effectProperties,
    scene3d,
    sp3d,
    extLst,
    bwMode,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ShapeStyle`. See `transitional/dml-main.xsd:2280`. */
export function readCT_ShapeStyle(cur: XmlCursor, ctx: ReadContext): CT_ShapeStyle {
  const start = requireStart(cur, 'CT_ShapeStyle');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ShapeStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let lnRef: CT_StyleMatrixReference | undefined;
  let fillRef: CT_StyleMatrixReference | undefined;
  let effectRef: CT_StyleMatrixReference | undefined;
  let fontRef: CT_FontReference | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ShapeStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'lnRef': {
            lnRef = readCT_StyleMatrixReference(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'fillRef': {
            fillRef = readCT_StyleMatrixReference(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'effectRef': {
            effectRef = readCT_StyleMatrixReference(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'fontRef': {
            fontRef = readCT_FontReference(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ShapeStyle', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ShapeStyle', cur);
    cur.next();
  }

  return {
    lnRef,
    fillRef,
    effectRef,
    fontRef,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SoftEdgesEffect`. See `transitional/dml-main.xsd:1397`. */
export function readCT_SoftEdgesEffect(cur: XmlCursor, ctx: ReadContext): CT_SoftEdgesEffect {
  const start = requireStart(cur, 'CT_SoftEdgesEffect');
  let rad: ST_PositiveCoordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'rad': {
          const v = parseST_PositiveCoordinate(a.value);
          if (v !== undefined) {
            rad = v;
            continue;
          }
          ctx.invalidValue('CT_SoftEdgesEffect', a, 'ST_PositiveCoordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SoftEdgesEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (rad === undefined) ctx.missingRequired('CT_SoftEdgesEffect', 'rad', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SoftEdgesEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SoftEdgesEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SoftEdgesEffect', cur);
    cur.next();
  }
  return {
    rad,
    $unknownAttrs,
  };
}

/** Read a `CT_SolidColorFillProperties`. See `transitional/dml-main.xsd:1413`. */
export function readCT_SolidColorFillProperties(cur: XmlCursor, ctx: ReadContext): CT_SolidColorFillProperties {
  const start = requireStart(cur, 'CT_SolidColorFillProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_SolidColorFillProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let colorChoice: CT_SolidColorFillProperties_ColorChoice | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SolidColorFillProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SolidColorFillProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SolidColorFillProperties', cur);
    cur.next();
  }

  return {
    colorChoice,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SphereCoords`. See `transitional/dml-main.xsd:665`. */
export function readCT_SphereCoords(cur: XmlCursor, ctx: ReadContext): CT_SphereCoords {
  const start = requireStart(cur, 'CT_SphereCoords');
  let lat: ST_PositiveFixedAngle | undefined;
  let lon: ST_PositiveFixedAngle | undefined;
  let rev: ST_PositiveFixedAngle | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'lat': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            lat = v;
            continue;
          }
          ctx.invalidValue('CT_SphereCoords', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
        case 'lon': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            lon = v;
            continue;
          }
          ctx.invalidValue('CT_SphereCoords', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
        case 'rev': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            rev = v;
            continue;
          }
          ctx.invalidValue('CT_SphereCoords', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SphereCoords', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (lat === undefined) ctx.missingRequired('CT_SphereCoords', 'lat', cur);
  if (lon === undefined) ctx.missingRequired('CT_SphereCoords', 'lon', cur);
  if (rev === undefined) ctx.missingRequired('CT_SphereCoords', 'rev', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SphereCoords', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SphereCoords', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SphereCoords', cur);
    cur.next();
  }
  return {
    lat,
    lon,
    rev,
    $unknownAttrs,
  };
}

/** Read a `CT_StretchInfoProperties`. See `transitional/dml-main.xsd:1477`. */
export function readCT_StretchInfoProperties(cur: XmlCursor, ctx: ReadContext): CT_StretchInfoProperties {
  const start = requireStart(cur, 'CT_StretchInfoProperties');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_StretchInfoProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let fillRect: CT_RelativeRect | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StretchInfoProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'fillRect': {
            fillRect = readCT_RelativeRect(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_StretchInfoProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StretchInfoProperties', cur);
    cur.next();
  }

  return {
    fillRect,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_StyleMatrix`. See `transitional/dml-main.xsd:165`. */
export function readCT_StyleMatrix(cur: XmlCursor, ctx: ReadContext): CT_StyleMatrix {
  const start = requireStart(cur, 'CT_StyleMatrix');
  let name: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'name':
          name = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_StyleMatrix', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let fillStyleLst: CT_FillStyleList | undefined;
  let lnStyleLst: CT_LineStyleList | undefined;
  let effectStyleLst: CT_EffectStyleList | undefined;
  let bgFillStyleLst: CT_BackgroundFillStyleList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StyleMatrix', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'fillStyleLst': {
            fillStyleLst = readCT_FillStyleList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lnStyleLst': {
            lnStyleLst = readCT_LineStyleList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'effectStyleLst': {
            effectStyleLst = readCT_EffectStyleList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'bgFillStyleLst': {
            bgFillStyleLst = readCT_BackgroundFillStyleList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_StyleMatrix', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StyleMatrix', cur);
    cur.next();
  }

  return {
    fillStyleLst,
    lnStyleLst,
    effectStyleLst,
    bgFillStyleLst,
    name,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_StyleMatrixReference`. See `transitional/dml-main.xsd:2268`. */
export function readCT_StyleMatrixReference(cur: XmlCursor, ctx: ReadContext): CT_StyleMatrixReference {
  const start = requireStart(cur, 'CT_StyleMatrixReference');
  let idx: ST_StyleMatrixColumnIndex | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'idx': {
          const v = parseST_StyleMatrixColumnIndex(a.value);
          if (v !== undefined) {
            idx = v;
            continue;
          }
          ctx.invalidValue('CT_StyleMatrixReference', a, 'ST_StyleMatrixColumnIndex', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_StyleMatrixReference', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (idx === undefined) ctx.missingRequired('CT_StyleMatrixReference', 'idx', cur);

  let colorChoice: CT_StyleMatrixReference_ColorChoice | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_StyleMatrixReference', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_StyleMatrixReference', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_StyleMatrixReference', cur);
    cur.next();
  }

  return {
    colorChoice,
    idx,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_SupplementalFont`. See `transitional/dml-main.xsd:111`. */
export function readCT_SupplementalFont(cur: XmlCursor, ctx: ReadContext): CT_SupplementalFont {
  const start = requireStart(cur, 'CT_SupplementalFont');
  let script: string | undefined;
  let typeface: ST_TextTypeface | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'script':
          script = a.value as string;
          continue;

        case 'typeface': {
          const v = parseST_TextTypeface(a.value);
          if (v !== undefined) {
            typeface = v;
            continue;
          }
          ctx.invalidValue('CT_SupplementalFont', a, 'ST_TextTypeface', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SupplementalFont', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (script === undefined) ctx.missingRequired('CT_SupplementalFont', 'script', cur);
  if (typeface === undefined) ctx.missingRequired('CT_SupplementalFont', 'typeface', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SupplementalFont', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_SupplementalFont', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SupplementalFont', cur);
    cur.next();
  }
  return {
    script,
    typeface,
    $unknownAttrs,
  };
}

/** Read a `CT_SystemColor`. See `transitional/dml-main.xsd:385`. */
export function readCT_SystemColor(cur: XmlCursor, ctx: ReadContext): CT_SystemColor {
  const start = requireStart(cur, 'CT_SystemColor');
  let val: ST_SystemColorVal | undefined;
  let lastClr: ST_HexColorRGB | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_SystemColorVal(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_SystemColor', a, 'ST_SystemColorVal', cur);
          break;
        }
        case 'lastClr': {
          const v = parseST_HexColorRGB(a.value);
          if (v !== undefined) {
            lastClr = v;
            continue;
          }
          ctx.invalidValue('CT_SystemColor', a, 'ST_HexColorRGB', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_SystemColor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_SystemColor', 'val', cur);

  const colorTransform: CT_SystemColor_ColorTransform[] = [];

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_SystemColor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tint': {
            colorTransform.push({ kind: 'tint', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'shade': {
            colorTransform.push({ kind: 'shade', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'comp': {
            colorTransform.push({ kind: 'comp', value: readCT_ComplementTransform(cur, ctx) });
            continue;
          }
          case 'inv': {
            colorTransform.push({ kind: 'inv', value: readCT_InverseTransform(cur, ctx) });
            continue;
          }
          case 'gray': {
            colorTransform.push({ kind: 'gray', value: readCT_GrayscaleTransform(cur, ctx) });
            continue;
          }
          case 'alpha': {
            colorTransform.push({ kind: 'alpha', value: readCT_PositiveFixedPercentage(cur, ctx) });
            continue;
          }
          case 'alphaOff': {
            colorTransform.push({ kind: 'alphaOff', value: readCT_FixedPercentage(cur, ctx) });
            continue;
          }
          case 'alphaMod': {
            colorTransform.push({ kind: 'alphaMod', value: readCT_PositivePercentage(cur, ctx) });
            continue;
          }
          case 'hue': {
            colorTransform.push({ kind: 'hue', value: readCT_PositiveFixedAngle(cur, ctx) });
            continue;
          }
          case 'hueOff': {
            colorTransform.push({ kind: 'hueOff', value: readCT_Angle(cur, ctx) });
            continue;
          }
          case 'hueMod': {
            colorTransform.push({ kind: 'hueMod', value: readCT_PositivePercentage(cur, ctx) });
            continue;
          }
          case 'sat': {
            colorTransform.push({ kind: 'sat', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'satOff': {
            colorTransform.push({ kind: 'satOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'satMod': {
            colorTransform.push({ kind: 'satMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lum': {
            colorTransform.push({ kind: 'lum', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lumOff': {
            colorTransform.push({ kind: 'lumOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'lumMod': {
            colorTransform.push({ kind: 'lumMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'red': {
            colorTransform.push({ kind: 'red', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'redOff': {
            colorTransform.push({ kind: 'redOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'redMod': {
            colorTransform.push({ kind: 'redMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'green': {
            colorTransform.push({ kind: 'green', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'greenOff': {
            colorTransform.push({ kind: 'greenOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'greenMod': {
            colorTransform.push({ kind: 'greenMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blue': {
            colorTransform.push({ kind: 'blue', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blueOff': {
            colorTransform.push({ kind: 'blueOff', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'blueMod': {
            colorTransform.push({ kind: 'blueMod', value: readCT_Percentage(cur, ctx) });
            continue;
          }
          case 'gamma': {
            colorTransform.push({ kind: 'gamma', value: readCT_GammaTransform(cur, ctx) });
            continue;
          }
          case 'invGamma': {
            colorTransform.push({ kind: 'invGamma', value: readCT_InverseGammaTransform(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_SystemColor', ev.localName, ev.uri, cur);
      colorTransform.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_SystemColor', cur);
    cur.next();
  }

  return {
    colorTransform,
    val,
    lastClr,
    $unknownAttrs,
  };
}

/** Read a `CT_Table`. See `transitional/dml-main.xsd:2445`. */
export function readCT_Table(cur: XmlCursor, ctx: ReadContext): CT_Table {
  const start = requireStart(cur, 'CT_Table');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Table', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let tblPr: CT_TableProperties | undefined;
  let tblGrid: CT_TableGrid | undefined;
  const tr: CT_TableRow[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Table', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tblPr': {
            tblPr = readCT_TableProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tblGrid': {
            tblGrid = readCT_TableGrid(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tr': {
            tr.push(readCT_TableRow(cur, ctx));
            $slot = 2;
            $index = tr.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Table', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Table', cur);
    cur.next();
  }

  return {
    tblPr,
    tblGrid,
    tr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TableBackgroundStyle`. See `transitional/dml-main.xsd:2515`. */
export function readCT_TableBackgroundStyle(cur: XmlCursor, ctx: ReadContext): CT_TableBackgroundStyle {
  const start = requireStart(cur, 'CT_TableBackgroundStyle');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TableBackgroundStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let themeableFillStyle: CT_TableBackgroundStyle_ThemeableFillStyle | undefined;
  let themeableEffectStyle: CT_TableBackgroundStyle_ThemeableEffectStyle | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TableBackgroundStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'fill': {
            themeableFillStyle = { kind: 'fill', value: readCT_FillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'fillRef': {
            themeableFillStyle = { kind: 'fillRef', value: readCT_StyleMatrixReference(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'effect': {
            themeableEffectStyle = { kind: 'effect', value: readCT_EffectProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'effectRef': {
            themeableEffectStyle = { kind: 'effectRef', value: readCT_StyleMatrixReference(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TableBackgroundStyle', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TableBackgroundStyle', cur);
    cur.next();
  }

  return {
    themeableFillStyle,
    themeableEffectStyle,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TableCell`. See `transitional/dml-main.xsd:2408`. */
export function readCT_TableCell(cur: XmlCursor, ctx: ReadContext): CT_TableCell {
  const start = requireStart(cur, 'CT_TableCell');
  let rowSpan: number | undefined;
  let gridSpan: number | undefined;
  let hMerge: boolean | undefined;
  let vMerge: boolean | undefined;
  let id: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'rowSpan': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            rowSpan = v;
            continue;
          }
          ctx.invalidValue('CT_TableCell', a, 'xsd:int', cur);
          break;
        }
        case 'gridSpan': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            gridSpan = v;
            continue;
          }
          ctx.invalidValue('CT_TableCell', a, 'xsd:int', cur);
          break;
        }
        case 'hMerge': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            hMerge = v;
            continue;
          }
          ctx.invalidValue('CT_TableCell', a, 'xsd:boolean', cur);
          break;
        }
        case 'vMerge': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            vMerge = v;
            continue;
          }
          ctx.invalidValue('CT_TableCell', a, 'xsd:boolean', cur);
          break;
        }
        case 'id':
          id = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_TableCell', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let txBody: CT_TextBody | undefined;
  let tcPr: CT_TableCellProperties | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TableCell', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'txBody': {
            txBody = readCT_TextBody(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tcPr': {
            tcPr = readCT_TableCellProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TableCell', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TableCell', cur);
    cur.next();
  }

  return {
    txBody,
    tcPr,
    extLst,
    rowSpan,
    gridSpan,
    hMerge,
    vMerge,
    id,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TableCellBorderStyle`. See `transitional/dml-main.xsd:2502`. */
export function readCT_TableCellBorderStyle(cur: XmlCursor, ctx: ReadContext): CT_TableCellBorderStyle {
  const start = requireStart(cur, 'CT_TableCellBorderStyle');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TableCellBorderStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let left: CT_ThemeableLineStyle | undefined;
  let right: CT_ThemeableLineStyle | undefined;
  let top: CT_ThemeableLineStyle | undefined;
  let bottom: CT_ThemeableLineStyle | undefined;
  let insideH: CT_ThemeableLineStyle | undefined;
  let insideV: CT_ThemeableLineStyle | undefined;
  let tl2br: CT_ThemeableLineStyle | undefined;
  let tr2bl: CT_ThemeableLineStyle | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TableCellBorderStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'left': {
            left = readCT_ThemeableLineStyle(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'right': {
            right = readCT_ThemeableLineStyle(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'top': {
            top = readCT_ThemeableLineStyle(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'bottom': {
            bottom = readCT_ThemeableLineStyle(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'insideH': {
            insideH = readCT_ThemeableLineStyle(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'insideV': {
            insideV = readCT_ThemeableLineStyle(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'tl2br': {
            tl2br = readCT_ThemeableLineStyle(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'tr2bl': {
            tr2bl = readCT_ThemeableLineStyle(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TableCellBorderStyle', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TableCellBorderStyle', cur);
    cur.next();
  }

  return {
    left,
    right,
    top,
    bottom,
    insideH,
    insideV,
    tl2br,
    tr2bl,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TableCellProperties`. See `transitional/dml-main.xsd:2369`. */
export function readCT_TableCellProperties(cur: XmlCursor, ctx: ReadContext): CT_TableCellProperties {
  const start = requireStart(cur, 'CT_TableCellProperties');
  let marL: ST_Coordinate32 | undefined;
  let marR: ST_Coordinate32 | undefined;
  let marT: ST_Coordinate32 | undefined;
  let marB: ST_Coordinate32 | undefined;
  let vert: ST_TextVerticalType | undefined;
  let anchor: ST_TextAnchoringType | undefined;
  let anchorCtr: boolean | undefined;
  let horzOverflow: ST_TextHorzOverflowType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'marL': {
          const v = parseST_Coordinate32(a.value);
          if (v !== undefined) {
            marL = v;
            continue;
          }
          ctx.invalidValue('CT_TableCellProperties', a, 'ST_Coordinate32', cur);
          break;
        }
        case 'marR': {
          const v = parseST_Coordinate32(a.value);
          if (v !== undefined) {
            marR = v;
            continue;
          }
          ctx.invalidValue('CT_TableCellProperties', a, 'ST_Coordinate32', cur);
          break;
        }
        case 'marT': {
          const v = parseST_Coordinate32(a.value);
          if (v !== undefined) {
            marT = v;
            continue;
          }
          ctx.invalidValue('CT_TableCellProperties', a, 'ST_Coordinate32', cur);
          break;
        }
        case 'marB': {
          const v = parseST_Coordinate32(a.value);
          if (v !== undefined) {
            marB = v;
            continue;
          }
          ctx.invalidValue('CT_TableCellProperties', a, 'ST_Coordinate32', cur);
          break;
        }
        case 'vert': {
          const v = parseST_TextVerticalType(a.value);
          if (v !== undefined) {
            vert = v;
            continue;
          }
          ctx.invalidValue('CT_TableCellProperties', a, 'ST_TextVerticalType', cur);
          break;
        }
        case 'anchor': {
          const v = parseST_TextAnchoringType(a.value);
          if (v !== undefined) {
            anchor = v;
            continue;
          }
          ctx.invalidValue('CT_TableCellProperties', a, 'ST_TextAnchoringType', cur);
          break;
        }
        case 'anchorCtr': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            anchorCtr = v;
            continue;
          }
          ctx.invalidValue('CT_TableCellProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'horzOverflow': {
          const v = parseST_TextHorzOverflowType(a.value);
          if (v !== undefined) {
            horzOverflow = v;
            continue;
          }
          ctx.invalidValue('CT_TableCellProperties', a, 'ST_TextHorzOverflowType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TableCellProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let lnL: CT_LineProperties | undefined;
  let lnR: CT_LineProperties | undefined;
  let lnT: CT_LineProperties | undefined;
  let lnB: CT_LineProperties | undefined;
  let lnTlToBr: CT_LineProperties | undefined;
  let lnBlToTr: CT_LineProperties | undefined;
  let cell3D: CT_Cell3D | undefined;
  let fillProperties: CT_TableCellProperties_FillProperties | undefined;
  let headers: CT_Headers | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TableCellProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'lnL': {
            lnL = readCT_LineProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lnR': {
            lnR = readCT_LineProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'lnT': {
            lnT = readCT_LineProperties(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'lnB': {
            lnB = readCT_LineProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'lnTlToBr': {
            lnTlToBr = readCT_LineProperties(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'lnBlToTr': {
            lnBlToTr = readCT_LineProperties(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'cell3D': {
            cell3D = readCT_Cell3D(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'noFill': {
            fillProperties = { kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) };
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'solidFill': {
            fillProperties = { kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) };
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'gradFill': {
            fillProperties = { kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) };
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            fillProperties = { kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) };
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'pattFill': {
            fillProperties = { kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) };
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'grpFill': {
            fillProperties = { kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) };
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'headers': {
            headers = readCT_Headers(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TableCellProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TableCellProperties', cur);
    cur.next();
  }

  return {
    lnL,
    lnR,
    lnT,
    lnB,
    lnTlToBr,
    lnBlToTr,
    cell3D,
    fillProperties,
    headers,
    extLst,
    marL,
    marR,
    marT,
    marB,
    vert,
    anchor,
    anchorCtr,
    horzOverflow,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TableCol`. See `transitional/dml-main.xsd:2397`. */
export function readCT_TableCol(cur: XmlCursor, ctx: ReadContext): CT_TableCol {
  const start = requireStart(cur, 'CT_TableCol');
  let w: ST_Coordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'w': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            w = v;
            continue;
          }
          ctx.invalidValue('CT_TableCol', a, 'ST_Coordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TableCol', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (w === undefined) ctx.missingRequired('CT_TableCol', 'w', cur);

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TableCol', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TableCol', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TableCol', cur);
    cur.next();
  }

  return {
    extLst,
    w,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TableGrid`. See `transitional/dml-main.xsd:2403`. */
export function readCT_TableGrid(cur: XmlCursor, ctx: ReadContext): CT_TableGrid {
  const start = requireStart(cur, 'CT_TableGrid');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TableGrid', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const gridCol: CT_TableCol[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TableGrid', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'gridCol': {
            gridCol.push(readCT_TableCol(cur, ctx));
            $slot = 0;
            $index = gridCol.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TableGrid', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TableGrid', cur);
    cur.next();
  }

  return {
    gridCol,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TablePartStyle`. See `transitional/dml-main.xsd:2528`. */
export function readCT_TablePartStyle(cur: XmlCursor, ctx: ReadContext): CT_TablePartStyle {
  const start = requireStart(cur, 'CT_TablePartStyle');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TablePartStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let tcTxStyle: CT_TableStyleTextStyle | undefined;
  let tcStyle: CT_TableStyleCellStyle | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TablePartStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tcTxStyle': {
            tcTxStyle = readCT_TableStyleTextStyle(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'tcStyle': {
            tcStyle = readCT_TableStyleCellStyle(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TablePartStyle', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TablePartStyle', cur);
    cur.next();
  }

  return {
    tcTxStyle,
    tcStyle,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TableProperties`. See `transitional/dml-main.xsd:2427`. */
export function readCT_TableProperties(cur: XmlCursor, ctx: ReadContext): CT_TableProperties {
  const start = requireStart(cur, 'CT_TableProperties');
  let rtl: boolean | undefined;
  let firstRow: boolean | undefined;
  let firstCol: boolean | undefined;
  let lastRow: boolean | undefined;
  let lastCol: boolean | undefined;
  let bandRow: boolean | undefined;
  let bandCol: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'rtl': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            rtl = v;
            continue;
          }
          ctx.invalidValue('CT_TableProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'firstRow': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            firstRow = v;
            continue;
          }
          ctx.invalidValue('CT_TableProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'firstCol': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            firstCol = v;
            continue;
          }
          ctx.invalidValue('CT_TableProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'lastRow': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            lastRow = v;
            continue;
          }
          ctx.invalidValue('CT_TableProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'lastCol': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            lastCol = v;
            continue;
          }
          ctx.invalidValue('CT_TableProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'bandRow': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            bandRow = v;
            continue;
          }
          ctx.invalidValue('CT_TableProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'bandCol': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            bandCol = v;
            continue;
          }
          ctx.invalidValue('CT_TableProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TableProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let fillProperties: CT_TableProperties_FillProperties | undefined;
  let effectProperties: CT_TableProperties_EffectProperties | undefined;
  let content: CT_TableProperties_Content | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TableProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'noFill': {
            fillProperties = { kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'solidFill': {
            fillProperties = { kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'gradFill': {
            fillProperties = { kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            fillProperties = { kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pattFill': {
            fillProperties = { kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'grpFill': {
            fillProperties = { kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'effectLst': {
            effectProperties = { kind: 'effectLst', value: readCT_EffectList(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'effectDag': {
            effectProperties = { kind: 'effectDag', value: readCT_EffectContainer(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'tableStyle': {
            content = { kind: 'tableStyle', value: readCT_TableStyle(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'tableStyleId': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Guid($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_TableProperties', 'tableStyleId', 'ST_Guid', $s, cur);
            content = { kind: 'tableStyleId', value: ($pv ?? $s) as ST_Guid };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TableProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TableProperties', cur);
    cur.next();
  }

  return {
    fillProperties,
    effectProperties,
    content,
    extLst,
    rtl,
    firstRow,
    firstCol,
    lastRow,
    lastCol,
    bandRow,
    bandCol,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TableRow`. See `transitional/dml-main.xsd:2420`. */
export function readCT_TableRow(cur: XmlCursor, ctx: ReadContext): CT_TableRow {
  const start = requireStart(cur, 'CT_TableRow');
  let h: ST_Coordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'h': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            h = v;
            continue;
          }
          ctx.invalidValue('CT_TableRow', a, 'ST_Coordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TableRow', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (h === undefined) ctx.missingRequired('CT_TableRow', 'h', cur);

  const tc: CT_TableCell[] = [];
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TableRow', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tc': {
            tc.push(readCT_TableCell(cur, ctx));
            $slot = 0;
            $index = tc.length - 1;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TableRow', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TableRow', cur);
    cur.next();
  }

  return {
    tc,
    extLst,
    h,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TableStyle`. See `transitional/dml-main.xsd:2534`. */
export function readCT_TableStyle(cur: XmlCursor, ctx: ReadContext): CT_TableStyle {
  const start = requireStart(cur, 'CT_TableStyle');
  let styleId: ST_Guid | undefined;
  let styleName: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'styleId': {
          const v = parseST_Guid(a.value);
          if (v !== undefined) {
            styleId = v;
            continue;
          }
          ctx.invalidValue('CT_TableStyle', a, 'ST_Guid', cur);
          break;
        }
        case 'styleName':
          styleName = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_TableStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (styleId === undefined) ctx.missingRequired('CT_TableStyle', 'styleId', cur);
  if (styleName === undefined) ctx.missingRequired('CT_TableStyle', 'styleName', cur);

  let tblBg: CT_TableBackgroundStyle | undefined;
  let wholeTbl: CT_TablePartStyle | undefined;
  let band1H: CT_TablePartStyle | undefined;
  let band2H: CT_TablePartStyle | undefined;
  let band1V: CT_TablePartStyle | undefined;
  let band2V: CT_TablePartStyle | undefined;
  let lastCol: CT_TablePartStyle | undefined;
  let firstCol: CT_TablePartStyle | undefined;
  let lastRow: CT_TablePartStyle | undefined;
  let seCell: CT_TablePartStyle | undefined;
  let swCell: CT_TablePartStyle | undefined;
  let firstRow: CT_TablePartStyle | undefined;
  let neCell: CT_TablePartStyle | undefined;
  let nwCell: CT_TablePartStyle | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TableStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tblBg': {
            tblBg = readCT_TableBackgroundStyle(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'wholeTbl': {
            wholeTbl = readCT_TablePartStyle(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'band1H': {
            band1H = readCT_TablePartStyle(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'band2H': {
            band2H = readCT_TablePartStyle(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'band1V': {
            band1V = readCT_TablePartStyle(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'band2V': {
            band2V = readCT_TablePartStyle(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'lastCol': {
            lastCol = readCT_TablePartStyle(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'firstCol': {
            firstCol = readCT_TablePartStyle(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'lastRow': {
            lastRow = readCT_TablePartStyle(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'seCell': {
            seCell = readCT_TablePartStyle(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'swCell': {
            swCell = readCT_TablePartStyle(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'firstRow': {
            firstRow = readCT_TablePartStyle(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'neCell': {
            neCell = readCT_TablePartStyle(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'nwCell': {
            nwCell = readCT_TablePartStyle(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 14;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TableStyle', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TableStyle', cur);
    cur.next();
  }

  return {
    tblBg,
    wholeTbl,
    band1H,
    band2H,
    band1V,
    band2V,
    lastCol,
    firstCol,
    lastRow,
    seCell,
    swCell,
    firstRow,
    neCell,
    nwCell,
    extLst,
    styleId,
    styleName,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TableStyleCellStyle`. See `transitional/dml-main.xsd:2521`. */
export function readCT_TableStyleCellStyle(cur: XmlCursor, ctx: ReadContext): CT_TableStyleCellStyle {
  const start = requireStart(cur, 'CT_TableStyleCellStyle');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TableStyleCellStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let tcBdr: CT_TableCellBorderStyle | undefined;
  let themeableFillStyle: CT_TableStyleCellStyle_ThemeableFillStyle | undefined;
  let cell3D: CT_Cell3D | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TableStyleCellStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tcBdr': {
            tcBdr = readCT_TableCellBorderStyle(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'fill': {
            themeableFillStyle = { kind: 'fill', value: readCT_FillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'fillRef': {
            themeableFillStyle = { kind: 'fillRef', value: readCT_StyleMatrixReference(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'cell3D': {
            cell3D = readCT_Cell3D(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TableStyleCellStyle', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TableStyleCellStyle', cur);
    cur.next();
  }

  return {
    tcBdr,
    themeableFillStyle,
    cell3D,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TableStyleList`. See `transitional/dml-main.xsd:2555`. */
export function readCT_TableStyleList(cur: XmlCursor, ctx: ReadContext): CT_TableStyleList {
  const start = requireStart(cur, 'CT_TableStyleList');
  let def: ST_Guid | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'def': {
          const v = parseST_Guid(a.value);
          if (v !== undefined) {
            def = v;
            continue;
          }
          ctx.invalidValue('CT_TableStyleList', a, 'ST_Guid', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TableStyleList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (def === undefined) ctx.missingRequired('CT_TableStyleList', 'def', cur);

  const tblStyle: CT_TableStyle[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TableStyleList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tblStyle': {
            tblStyle.push(readCT_TableStyle(cur, ctx));
            $slot = 0;
            $index = tblStyle.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TableStyleList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TableStyleList', cur);
    cur.next();
  }

  return {
    tblStyle,
    def,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TableStyleTextStyle`. See `transitional/dml-main.xsd:2493`. */
export function readCT_TableStyleTextStyle(cur: XmlCursor, ctx: ReadContext): CT_TableStyleTextStyle {
  const start = requireStart(cur, 'CT_TableStyleTextStyle');
  let b: ST_OnOffStyleType | undefined;
  let i: ST_OnOffStyleType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'b': {
          const v = parseST_OnOffStyleType(a.value);
          if (v !== undefined) {
            b = v;
            continue;
          }
          ctx.invalidValue('CT_TableStyleTextStyle', a, 'ST_OnOffStyleType', cur);
          break;
        }
        case 'i': {
          const v = parseST_OnOffStyleType(a.value);
          if (v !== undefined) {
            i = v;
            continue;
          }
          ctx.invalidValue('CT_TableStyleTextStyle', a, 'ST_OnOffStyleType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TableStyleTextStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let themeableFontStyles: CT_TableStyleTextStyle_ThemeableFontStyles | undefined;
  let colorChoice: CT_TableStyleTextStyle_ColorChoice | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TableStyleTextStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'font': {
            themeableFontStyles = { kind: 'font', value: readCT_FontCollection(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'fontRef': {
            themeableFontStyles = { kind: 'fontRef', value: readCT_FontReference(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'scrgbClr': {
            colorChoice = { kind: 'scrgbClr', value: readCT_ScRgbColor(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'srgbClr': {
            colorChoice = { kind: 'srgbClr', value: readCT_SRgbColor(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'hslClr': {
            colorChoice = { kind: 'hslClr', value: readCT_HslColor(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sysClr': {
            colorChoice = { kind: 'sysClr', value: readCT_SystemColor(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'schemeClr': {
            colorChoice = { kind: 'schemeClr', value: readCT_SchemeColor(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'prstClr': {
            colorChoice = { kind: 'prstClr', value: readCT_PresetColor(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TableStyleTextStyle', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TableStyleTextStyle', cur);
    cur.next();
  }

  return {
    themeableFontStyles,
    colorChoice,
    extLst,
    b,
    i,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextAutonumberBullet`. See `transitional/dml-main.xsd:2781`. */
export function readCT_TextAutonumberBullet(cur: XmlCursor, ctx: ReadContext): CT_TextAutonumberBullet {
  const start = requireStart(cur, 'CT_TextAutonumberBullet');
  let type: ST_TextAutonumberScheme | undefined;
  let startAt: ST_TextBulletStartAtNum | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'type': {
          const v = parseST_TextAutonumberScheme(a.value);
          if (v !== undefined) {
            type = v;
            continue;
          }
          ctx.invalidValue('CT_TextAutonumberBullet', a, 'ST_TextAutonumberScheme', cur);
          break;
        }
        case 'startAt': {
          const v = parseST_TextBulletStartAtNum(a.value);
          if (v !== undefined) {
            startAt = v;
            continue;
          }
          ctx.invalidValue('CT_TextAutonumberBullet', a, 'ST_TextBulletStartAtNum', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextAutonumberBullet', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (type === undefined) ctx.missingRequired('CT_TextAutonumberBullet', 'type', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextAutonumberBullet', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextAutonumberBullet', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextAutonumberBullet', cur);
    cur.next();
  }
  return {
    type,
    startAt,
    $unknownAttrs,
  };
}

/** Read a `CT_TextBlipBullet`. See `transitional/dml-main.xsd:2788`. */
export function readCT_TextBlipBullet(cur: XmlCursor, ctx: ReadContext): CT_TextBlipBullet {
  const start = requireStart(cur, 'CT_TextBlipBullet');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextBlipBullet', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let blip: CT_Blip | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextBlipBullet', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'blip': {
            blip = readCT_Blip(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextBlipBullet', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextBlipBullet', cur);
    cur.next();
  }

  return {
    blip,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextBody`. See `transitional/dml-main.xsd:2681`. */
export function readCT_TextBody(cur: XmlCursor, ctx: ReadContext): CT_TextBody {
  const start = requireStart(cur, 'CT_TextBody');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextBody', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let bodyPr: CT_TextBodyProperties | undefined;
  let lstStyle: CT_TextListStyle | undefined;
  const p: CT_TextParagraph[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextBody', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'bodyPr': {
            bodyPr = readCT_TextBodyProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lstStyle': {
            lstStyle = readCT_TextListStyle(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'p': {
            p.push(readCT_TextParagraph(cur, ctx));
            $slot = 2;
            $index = p.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextBody', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextBody', cur);
    cur.next();
  }

  return {
    bodyPr,
    lstStyle,
    p,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextBodyProperties`. See `transitional/dml-main.xsd:2653`. */
export function readCT_TextBodyProperties(cur: XmlCursor, ctx: ReadContext): CT_TextBodyProperties {
  const start = requireStart(cur, 'CT_TextBodyProperties');
  let rot: ST_Angle | undefined;
  let spcFirstLastPara: boolean | undefined;
  let vertOverflow: ST_TextVertOverflowType | undefined;
  let horzOverflow: ST_TextHorzOverflowType | undefined;
  let vert: ST_TextVerticalType | undefined;
  let wrap: ST_TextWrappingType | undefined;
  let lIns: ST_Coordinate32 | undefined;
  let tIns: ST_Coordinate32 | undefined;
  let rIns: ST_Coordinate32 | undefined;
  let bIns: ST_Coordinate32 | undefined;
  let numCol: ST_TextColumnCount | undefined;
  let spcCol: ST_PositiveCoordinate32 | undefined;
  let rtlCol: boolean | undefined;
  let fromWordArt: boolean | undefined;
  let anchor: ST_TextAnchoringType | undefined;
  let anchorCtr: boolean | undefined;
  let forceAA: boolean | undefined;
  let upright: boolean | undefined;
  let compatLnSpc: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'rot': {
          const v = parseST_Angle(a.value);
          if (v !== undefined) {
            rot = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'ST_Angle', cur);
          break;
        }
        case 'spcFirstLastPara': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            spcFirstLastPara = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'vertOverflow': {
          const v = parseST_TextVertOverflowType(a.value);
          if (v !== undefined) {
            vertOverflow = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'ST_TextVertOverflowType', cur);
          break;
        }
        case 'horzOverflow': {
          const v = parseST_TextHorzOverflowType(a.value);
          if (v !== undefined) {
            horzOverflow = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'ST_TextHorzOverflowType', cur);
          break;
        }
        case 'vert': {
          const v = parseST_TextVerticalType(a.value);
          if (v !== undefined) {
            vert = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'ST_TextVerticalType', cur);
          break;
        }
        case 'wrap': {
          const v = parseST_TextWrappingType(a.value);
          if (v !== undefined) {
            wrap = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'ST_TextWrappingType', cur);
          break;
        }
        case 'lIns': {
          const v = parseST_Coordinate32(a.value);
          if (v !== undefined) {
            lIns = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'ST_Coordinate32', cur);
          break;
        }
        case 'tIns': {
          const v = parseST_Coordinate32(a.value);
          if (v !== undefined) {
            tIns = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'ST_Coordinate32', cur);
          break;
        }
        case 'rIns': {
          const v = parseST_Coordinate32(a.value);
          if (v !== undefined) {
            rIns = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'ST_Coordinate32', cur);
          break;
        }
        case 'bIns': {
          const v = parseST_Coordinate32(a.value);
          if (v !== undefined) {
            bIns = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'ST_Coordinate32', cur);
          break;
        }
        case 'numCol': {
          const v = parseST_TextColumnCount(a.value);
          if (v !== undefined) {
            numCol = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'ST_TextColumnCount', cur);
          break;
        }
        case 'spcCol': {
          const v = parseST_PositiveCoordinate32(a.value);
          if (v !== undefined) {
            spcCol = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'ST_PositiveCoordinate32', cur);
          break;
        }
        case 'rtlCol': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            rtlCol = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'fromWordArt': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fromWordArt = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'anchor': {
          const v = parseST_TextAnchoringType(a.value);
          if (v !== undefined) {
            anchor = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'ST_TextAnchoringType', cur);
          break;
        }
        case 'anchorCtr': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            anchorCtr = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'forceAA': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            forceAA = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'upright': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            upright = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'compatLnSpc': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            compatLnSpc = v;
            continue;
          }
          ctx.invalidValue('CT_TextBodyProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextBodyProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let prstTxWarp: CT_PresetTextShape | undefined;
  let textAutofit: CT_TextBodyProperties_TextAutofit | undefined;
  let scene3d: CT_Scene3D | undefined;
  let text3D: CT_TextBodyProperties_Text3D | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextBodyProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'prstTxWarp': {
            prstTxWarp = readCT_PresetTextShape(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'noAutofit': {
            textAutofit = { kind: 'noAutofit', value: readCT_TextNoAutofit(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'normAutofit': {
            textAutofit = { kind: 'normAutofit', value: readCT_TextNormalAutofit(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'spAutoFit': {
            textAutofit = { kind: 'spAutoFit', value: readCT_TextShapeAutofit(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'scene3d': {
            scene3d = readCT_Scene3D(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'sp3d': {
            text3D = { kind: 'sp3d', value: readCT_Shape3D(cur, ctx) };
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'flatTx': {
            text3D = { kind: 'flatTx', value: readCT_FlatText(cur, ctx) };
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextBodyProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextBodyProperties', cur);
    cur.next();
  }

  return {
    prstTxWarp,
    textAutofit,
    scene3d,
    text3D,
    extLst,
    rot,
    spcFirstLastPara,
    vertOverflow,
    horzOverflow,
    vert,
    wrap,
    lIns,
    tIns,
    rIns,
    bIns,
    numCol,
    spcCol,
    rtlCol,
    fromWordArt,
    anchor,
    anchorCtr,
    forceAA,
    upright,
    compatLnSpc,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextBulletColorFollowText`. See `transitional/dml-main.xsd:2739`. */
export function readCT_TextBulletColorFollowText(cur: XmlCursor, ctx: ReadContext): CT_TextBulletColorFollowText {
  const start = requireStart(cur, 'CT_TextBulletColorFollowText');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextBulletColorFollowText', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextBulletColorFollowText', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextBulletColorFollowText', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextBulletColorFollowText', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_TextBulletSizeFollowText`. See `transitional/dml-main.xsd:2760`. */
export function readCT_TextBulletSizeFollowText(cur: XmlCursor, ctx: ReadContext): CT_TextBulletSizeFollowText {
  const start = requireStart(cur, 'CT_TextBulletSizeFollowText');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextBulletSizeFollowText', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextBulletSizeFollowText', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextBulletSizeFollowText', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextBulletSizeFollowText', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_TextBulletSizePercent`. See `transitional/dml-main.xsd:2761`. */
export function readCT_TextBulletSizePercent(cur: XmlCursor, ctx: ReadContext): CT_TextBulletSizePercent {
  const start = requireStart(cur, 'CT_TextBulletSizePercent');
  let val: ST_TextBulletSizePercent | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TextBulletSizePercent(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TextBulletSizePercent', a, 'ST_TextBulletSizePercent', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextBulletSizePercent', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TextBulletSizePercent', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextBulletSizePercent', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextBulletSizePercent', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextBulletSizePercent', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TextBulletSizePoint`. See `transitional/dml-main.xsd:2764`. */
export function readCT_TextBulletSizePoint(cur: XmlCursor, ctx: ReadContext): CT_TextBulletSizePoint {
  const start = requireStart(cur, 'CT_TextBulletSizePoint');
  let val: ST_TextFontSize | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TextFontSize(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TextBulletSizePoint', a, 'ST_TextFontSize', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextBulletSizePoint', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TextBulletSizePoint', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextBulletSizePoint', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextBulletSizePoint', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextBulletSizePoint', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TextBulletTypefaceFollowText`. See `transitional/dml-main.xsd:2774`. */
export function readCT_TextBulletTypefaceFollowText(cur: XmlCursor, ctx: ReadContext): CT_TextBulletTypefaceFollowText {
  const start = requireStart(cur, 'CT_TextBulletTypefaceFollowText');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextBulletTypefaceFollowText', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextBulletTypefaceFollowText', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextBulletTypefaceFollowText', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextBulletTypefaceFollowText', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_TextCharBullet`. See `transitional/dml-main.xsd:2785`. */
export function readCT_TextCharBullet(cur: XmlCursor, ctx: ReadContext): CT_TextCharBullet {
  const start = requireStart(cur, 'CT_TextCharBullet');
  let char: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'char':
          char = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_TextCharBullet', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (char === undefined) ctx.missingRequired('CT_TextCharBullet', 'char', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextCharBullet', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextCharBullet', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextCharBullet', cur);
    cur.next();
  }
  return {
    char,
    $unknownAttrs,
  };
}

/** Read a `CT_TextCharacterProperties`. See `transitional/dml-main.xsd:2907`. */
export function readCT_TextCharacterProperties(cur: XmlCursor, ctx: ReadContext): CT_TextCharacterProperties {
  const start = requireStart(cur, 'CT_TextCharacterProperties');
  let kumimoji: boolean | undefined;
  let lang: ST_Lang | undefined;
  let altLang: ST_Lang | undefined;
  let sz: ST_TextFontSize | undefined;
  let b: boolean | undefined;
  let i: boolean | undefined;
  let u: ST_TextUnderlineType | undefined;
  let strike: ST_TextStrikeType | undefined;
  let kern: ST_TextNonNegativePoint | undefined;
  let cap: ST_TextCapsType | undefined;
  let spc: ST_TextPoint | undefined;
  let normalizeH: boolean | undefined;
  let baseline: ST_Percentage | undefined;
  let noProof: boolean | undefined;
  let dirty: boolean | undefined;
  let err: boolean | undefined;
  let smtClean: boolean | undefined;
  let smtId: number | undefined;
  let bmk: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'kumimoji': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            kumimoji = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'lang': {
          const v = parseST_Lang(a.value);
          if (v !== undefined) {
            lang = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'ST_Lang', cur);
          break;
        }
        case 'altLang': {
          const v = parseST_Lang(a.value);
          if (v !== undefined) {
            altLang = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'ST_Lang', cur);
          break;
        }
        case 'sz': {
          const v = parseST_TextFontSize(a.value);
          if (v !== undefined) {
            sz = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'ST_TextFontSize', cur);
          break;
        }
        case 'b': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            b = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'i': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            i = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'u': {
          const v = parseST_TextUnderlineType(a.value);
          if (v !== undefined) {
            u = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'ST_TextUnderlineType', cur);
          break;
        }
        case 'strike': {
          const v = parseST_TextStrikeType(a.value);
          if (v !== undefined) {
            strike = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'ST_TextStrikeType', cur);
          break;
        }
        case 'kern': {
          const v = parseST_TextNonNegativePoint(a.value);
          if (v !== undefined) {
            kern = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'ST_TextNonNegativePoint', cur);
          break;
        }
        case 'cap': {
          const v = parseST_TextCapsType(a.value);
          if (v !== undefined) {
            cap = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'ST_TextCapsType', cur);
          break;
        }
        case 'spc': {
          const v = parseST_TextPoint(a.value);
          if (v !== undefined) {
            spc = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'ST_TextPoint', cur);
          break;
        }
        case 'normalizeH': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            normalizeH = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'baseline': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            baseline = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'ST_Percentage', cur);
          break;
        }
        case 'noProof': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            noProof = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'dirty': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            dirty = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'err': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            err = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'smtClean': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            smtClean = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'smtId': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            smtId = v;
            continue;
          }
          ctx.invalidValue('CT_TextCharacterProperties', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'bmk':
          bmk = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_TextCharacterProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ln: CT_LineProperties | undefined;
  let fillProperties: CT_TextCharacterProperties_FillProperties | undefined;
  let effectProperties: CT_TextCharacterProperties_EffectProperties | undefined;
  let highlight: CT_Color | undefined;
  let textUnderlineLine: CT_TextCharacterProperties_TextUnderlineLine | undefined;
  let textUnderlineFill: CT_TextCharacterProperties_TextUnderlineFill | undefined;
  let latin: CT_TextFont | undefined;
  let ea: CT_TextFont | undefined;
  let cs: CT_TextFont | undefined;
  let sym: CT_TextFont | undefined;
  let hlinkClick: CT_Hyperlink | undefined;
  let hlinkMouseOver: CT_Hyperlink | undefined;
  let rtl: CT_Boolean | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextCharacterProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'ln': {
            ln = readCT_LineProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'noFill': {
            fillProperties = { kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'solidFill': {
            fillProperties = { kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'gradFill': {
            fillProperties = { kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            fillProperties = { kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'pattFill': {
            fillProperties = { kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'grpFill': {
            fillProperties = { kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'effectLst': {
            effectProperties = { kind: 'effectLst', value: readCT_EffectList(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'effectDag': {
            effectProperties = { kind: 'effectDag', value: readCT_EffectContainer(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'highlight': {
            highlight = readCT_Color(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'uLnTx': {
            textUnderlineLine = { kind: 'uLnTx', value: readCT_TextUnderlineLineFollowText(cur, ctx) };
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'uLn': {
            textUnderlineLine = { kind: 'uLn', value: readCT_LineProperties(cur, ctx) };
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'uFillTx': {
            textUnderlineFill = { kind: 'uFillTx', value: readCT_TextUnderlineFillFollowText(cur, ctx) };
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'uFill': {
            textUnderlineFill = { kind: 'uFill', value: readCT_TextUnderlineFillGroupWrapper(cur, ctx) };
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'latin': {
            latin = readCT_TextFont(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'ea': {
            ea = readCT_TextFont(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'cs': {
            cs = readCT_TextFont(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'sym': {
            sym = readCT_TextFont(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'hlinkClick': {
            hlinkClick = readCT_Hyperlink(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
          case 'hlinkMouseOver': {
            hlinkMouseOver = readCT_Hyperlink(cur, ctx);
            $slot = 11;
            $index = undefined;
            continue;
          }
          case 'rtl': {
            rtl = readCT_Boolean(cur, ctx);
            $slot = 12;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 13;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextCharacterProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextCharacterProperties', cur);
    cur.next();
  }

  return {
    ln,
    fillProperties,
    effectProperties,
    highlight,
    textUnderlineLine,
    textUnderlineFill,
    latin,
    ea,
    cs,
    sym,
    hlinkClick,
    hlinkMouseOver,
    rtl,
    extLst,
    kumimoji,
    lang,
    altLang,
    sz,
    b,
    i,
    u,
    strike,
    kern,
    cap,
    spc,
    normalizeH,
    baseline,
    noProof,
    dirty,
    err,
    smtClean,
    smtId,
    bmk,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextField`. See `transitional/dml-main.xsd:3059`. */
export function readCT_TextField(cur: XmlCursor, ctx: ReadContext): CT_TextField {
  const start = requireStart(cur, 'CT_TextField');
  let id: ST_Guid | undefined;
  let type: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id': {
          const v = parseST_Guid(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TextField', a, 'ST_Guid', cur);
          break;
        }
        case 'type':
          type = a.value as string;
          continue;

      }
    }
    ctx.unexpectedAttribute('CT_TextField', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_TextField', 'id', cur);

  let rPr: CT_TextCharacterProperties | undefined;
  let pPr: CT_TextParagraphProperties | undefined;
  let t: string | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextField', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'rPr': {
            rPr = readCT_TextCharacterProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pPr': {
            pPr = readCT_TextParagraphProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 't': {
            t = readScalar(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextField', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextField', cur);
    cur.next();
  }

  return {
    rPr,
    pPr,
    t,
    id,
    type,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextFont`. See `transitional/dml-main.xsd:2848`. */
export function readCT_TextFont(cur: XmlCursor, ctx: ReadContext): CT_TextFont {
  const start = requireStart(cur, 'CT_TextFont');
  let typeface: ST_TextTypeface | undefined;
  let panose: ST_Panose | undefined;
  let pitchFamily: ST_PitchFamily | undefined;
  let charset: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'typeface': {
          const v = parseST_TextTypeface(a.value);
          if (v !== undefined) {
            typeface = v;
            continue;
          }
          ctx.invalidValue('CT_TextFont', a, 'ST_TextTypeface', cur);
          break;
        }
        case 'panose': {
          const v = parseST_Panose(a.value);
          if (v !== undefined) {
            panose = v;
            continue;
          }
          ctx.invalidValue('CT_TextFont', a, 'ST_Panose', cur);
          break;
        }
        case 'pitchFamily': {
          const v = parseST_PitchFamily(a.value);
          if (v !== undefined) {
            pitchFamily = v;
            continue;
          }
          ctx.invalidValue('CT_TextFont', a, 'ST_PitchFamily', cur);
          break;
        }
        case 'charset': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            charset = v;
            continue;
          }
          ctx.invalidValue('CT_TextFont', a, 'xsd:byte', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextFont', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (typeface === undefined) ctx.missingRequired('CT_TextFont', 'typeface', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextFont', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextFont', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextFont', cur);
    cur.next();
  }
  return {
    typeface,
    panose,
    pitchFamily,
    charset,
    $unknownAttrs,
  };
}

/** Read a `CT_TextLineBreak`. See `transitional/dml-main.xsd:2997`. */
export function readCT_TextLineBreak(cur: XmlCursor, ctx: ReadContext): CT_TextLineBreak {
  const start = requireStart(cur, 'CT_TextLineBreak');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextLineBreak', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let rPr: CT_TextCharacterProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextLineBreak', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'rPr': {
            rPr = readCT_TextCharacterProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextLineBreak', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextLineBreak', cur);
    cur.next();
  }

  return {
    rPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextListStyle`. See `transitional/dml-main.xsd:2614`. */
export function readCT_TextListStyle(cur: XmlCursor, ctx: ReadContext): CT_TextListStyle {
  const start = requireStart(cur, 'CT_TextListStyle');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextListStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let defPPr: CT_TextParagraphProperties | undefined;
  let lvl1pPr: CT_TextParagraphProperties | undefined;
  let lvl2pPr: CT_TextParagraphProperties | undefined;
  let lvl3pPr: CT_TextParagraphProperties | undefined;
  let lvl4pPr: CT_TextParagraphProperties | undefined;
  let lvl5pPr: CT_TextParagraphProperties | undefined;
  let lvl6pPr: CT_TextParagraphProperties | undefined;
  let lvl7pPr: CT_TextParagraphProperties | undefined;
  let lvl8pPr: CT_TextParagraphProperties | undefined;
  let lvl9pPr: CT_TextParagraphProperties | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextListStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'defPPr': {
            defPPr = readCT_TextParagraphProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lvl1pPr': {
            lvl1pPr = readCT_TextParagraphProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'lvl2pPr': {
            lvl2pPr = readCT_TextParagraphProperties(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'lvl3pPr': {
            lvl3pPr = readCT_TextParagraphProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'lvl4pPr': {
            lvl4pPr = readCT_TextParagraphProperties(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'lvl5pPr': {
            lvl5pPr = readCT_TextParagraphProperties(cur, ctx);
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'lvl6pPr': {
            lvl6pPr = readCT_TextParagraphProperties(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'lvl7pPr': {
            lvl7pPr = readCT_TextParagraphProperties(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'lvl8pPr': {
            lvl8pPr = readCT_TextParagraphProperties(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'lvl9pPr': {
            lvl9pPr = readCT_TextParagraphProperties(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 10;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextListStyle', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextListStyle', cur);
    cur.next();
  }

  return {
    defPPr,
    lvl1pPr,
    lvl2pPr,
    lvl3pPr,
    lvl4pPr,
    lvl5pPr,
    lvl6pPr,
    lvl7pPr,
    lvl8pPr,
    lvl9pPr,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextNoAutofit`. See `transitional/dml-main.xsd:2645`. */
export function readCT_TextNoAutofit(cur: XmlCursor, ctx: ReadContext): CT_TextNoAutofit {
  const start = requireStart(cur, 'CT_TextNoAutofit');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextNoAutofit', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextNoAutofit', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextNoAutofit', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextNoAutofit', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_TextNoBullet`. See `transitional/dml-main.xsd:2793`. */
export function readCT_TextNoBullet(cur: XmlCursor, ctx: ReadContext): CT_TextNoBullet {
  const start = requireStart(cur, 'CT_TextNoBullet');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextNoBullet', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextNoBullet', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextNoBullet', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextNoBullet', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_TextNormalAutofit`. See `transitional/dml-main.xsd:2638`. */
export function readCT_TextNormalAutofit(cur: XmlCursor, ctx: ReadContext): CT_TextNormalAutofit {
  const start = requireStart(cur, 'CT_TextNormalAutofit');
  let fontScale: ST_TextFontScalePercentOrPercentString | undefined;
  let lnSpcReduction: ST_TextSpacingPercentOrPercentString | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'fontScale': {
          const v = parseST_TextFontScalePercentOrPercentString(a.value);
          if (v !== undefined) {
            fontScale = v;
            continue;
          }
          ctx.invalidValue('CT_TextNormalAutofit', a, 'ST_TextFontScalePercentOrPercentString', cur);
          break;
        }
        case 'lnSpcReduction': {
          const v = parseST_TextSpacingPercentOrPercentString(a.value);
          if (v !== undefined) {
            lnSpcReduction = v;
            continue;
          }
          ctx.invalidValue('CT_TextNormalAutofit', a, 'ST_TextSpacingPercentOrPercentString', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextNormalAutofit', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextNormalAutofit', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextNormalAutofit', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextNormalAutofit', cur);
    cur.next();
  }
  return {
    fontScale,
    lnSpcReduction,
    $unknownAttrs,
  };
}

/** Read a `CT_TextParagraph`. See `transitional/dml-main.xsd:2562`. */
export function readCT_TextParagraph(cur: XmlCursor, ctx: ReadContext): CT_TextParagraph {
  const start = requireStart(cur, 'CT_TextParagraph');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextParagraph', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pPr: CT_TextParagraphProperties | undefined;
  const textRun: CT_TextParagraph_TextRun[] = [];
  let endParaRPr: CT_TextCharacterProperties | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextParagraph', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'pPr': {
            pPr = readCT_TextParagraphProperties(cur, ctx);
            continue;
          }
          case 'r': {
            textRun.push({ kind: 'r', value: readCT_RegularTextRun(cur, ctx) });
            continue;
          }
          case 'br': {
            textRun.push({ kind: 'br', value: readCT_TextLineBreak(cur, ctx) });
            continue;
          }
          case 'fld': {
            textRun.push({ kind: 'fld', value: readCT_TextField(cur, ctx) });
            continue;
          }
          case 'endParaRPr': {
            endParaRPr = readCT_TextCharacterProperties(cur, ctx);
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextParagraph', ev.localName, ev.uri, cur);
      textRun.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextParagraph', cur);
    cur.next();
  }

  return {
    pPr,
    textRun,
    endParaRPr,
    $unknownAttrs,
  };
}

/** Read a `CT_TextParagraphProperties`. See `transitional/dml-main.xsd:3034`. */
export function readCT_TextParagraphProperties(cur: XmlCursor, ctx: ReadContext): CT_TextParagraphProperties {
  const start = requireStart(cur, 'CT_TextParagraphProperties');
  let marL: ST_TextMargin | undefined;
  let marR: ST_TextMargin | undefined;
  let lvl: ST_TextIndentLevelType | undefined;
  let indent: ST_TextIndent | undefined;
  let algn: ST_TextAlignType | undefined;
  let defTabSz: ST_Coordinate32 | undefined;
  let rtl: boolean | undefined;
  let eaLnBrk: boolean | undefined;
  let fontAlgn: ST_TextFontAlignType | undefined;
  let latinLnBrk: boolean | undefined;
  let hangingPunct: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'marL': {
          const v = parseST_TextMargin(a.value);
          if (v !== undefined) {
            marL = v;
            continue;
          }
          ctx.invalidValue('CT_TextParagraphProperties', a, 'ST_TextMargin', cur);
          break;
        }
        case 'marR': {
          const v = parseST_TextMargin(a.value);
          if (v !== undefined) {
            marR = v;
            continue;
          }
          ctx.invalidValue('CT_TextParagraphProperties', a, 'ST_TextMargin', cur);
          break;
        }
        case 'lvl': {
          const v = parseST_TextIndentLevelType(a.value);
          if (v !== undefined) {
            lvl = v;
            continue;
          }
          ctx.invalidValue('CT_TextParagraphProperties', a, 'ST_TextIndentLevelType', cur);
          break;
        }
        case 'indent': {
          const v = parseST_TextIndent(a.value);
          if (v !== undefined) {
            indent = v;
            continue;
          }
          ctx.invalidValue('CT_TextParagraphProperties', a, 'ST_TextIndent', cur);
          break;
        }
        case 'algn': {
          const v = parseST_TextAlignType(a.value);
          if (v !== undefined) {
            algn = v;
            continue;
          }
          ctx.invalidValue('CT_TextParagraphProperties', a, 'ST_TextAlignType', cur);
          break;
        }
        case 'defTabSz': {
          const v = parseST_Coordinate32(a.value);
          if (v !== undefined) {
            defTabSz = v;
            continue;
          }
          ctx.invalidValue('CT_TextParagraphProperties', a, 'ST_Coordinate32', cur);
          break;
        }
        case 'rtl': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            rtl = v;
            continue;
          }
          ctx.invalidValue('CT_TextParagraphProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'eaLnBrk': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            eaLnBrk = v;
            continue;
          }
          ctx.invalidValue('CT_TextParagraphProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'fontAlgn': {
          const v = parseST_TextFontAlignType(a.value);
          if (v !== undefined) {
            fontAlgn = v;
            continue;
          }
          ctx.invalidValue('CT_TextParagraphProperties', a, 'ST_TextFontAlignType', cur);
          break;
        }
        case 'latinLnBrk': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            latinLnBrk = v;
            continue;
          }
          ctx.invalidValue('CT_TextParagraphProperties', a, 'xsd:boolean', cur);
          break;
        }
        case 'hangingPunct': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            hangingPunct = v;
            continue;
          }
          ctx.invalidValue('CT_TextParagraphProperties', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextParagraphProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let lnSpc: CT_TextSpacing | undefined;
  let spcBef: CT_TextSpacing | undefined;
  let spcAft: CT_TextSpacing | undefined;
  let textBulletColor: CT_TextParagraphProperties_TextBulletColor | undefined;
  let textBulletSize: CT_TextParagraphProperties_TextBulletSize | undefined;
  let textBulletTypeface: CT_TextParagraphProperties_TextBulletTypeface | undefined;
  let textBullet: CT_TextParagraphProperties_TextBullet | undefined;
  let tabLst: CT_TextTabStopList | undefined;
  let defRPr: CT_TextCharacterProperties | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextParagraphProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'lnSpc': {
            lnSpc = readCT_TextSpacing(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spcBef': {
            spcBef = readCT_TextSpacing(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'spcAft': {
            spcAft = readCT_TextSpacing(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'buClrTx': {
            textBulletColor = { kind: 'buClrTx', value: readCT_TextBulletColorFollowText(cur, ctx) };
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'buClr': {
            textBulletColor = { kind: 'buClr', value: readCT_Color(cur, ctx) };
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'buSzTx': {
            textBulletSize = { kind: 'buSzTx', value: readCT_TextBulletSizeFollowText(cur, ctx) };
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'buSzPct': {
            textBulletSize = { kind: 'buSzPct', value: readCT_TextBulletSizePercent(cur, ctx) };
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'buSzPts': {
            textBulletSize = { kind: 'buSzPts', value: readCT_TextBulletSizePoint(cur, ctx) };
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'buFontTx': {
            textBulletTypeface = { kind: 'buFontTx', value: readCT_TextBulletTypefaceFollowText(cur, ctx) };
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'buFont': {
            textBulletTypeface = { kind: 'buFont', value: readCT_TextFont(cur, ctx) };
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'buNone': {
            textBullet = { kind: 'buNone', value: readCT_TextNoBullet(cur, ctx) };
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'buAutoNum': {
            textBullet = { kind: 'buAutoNum', value: readCT_TextAutonumberBullet(cur, ctx) };
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'buChar': {
            textBullet = { kind: 'buChar', value: readCT_TextCharBullet(cur, ctx) };
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'buBlip': {
            textBullet = { kind: 'buBlip', value: readCT_TextBlipBullet(cur, ctx) };
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'tabLst': {
            tabLst = readCT_TextTabStopList(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
          case 'defRPr': {
            defRPr = readCT_TextCharacterProperties(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 9;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextParagraphProperties', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextParagraphProperties', cur);
    cur.next();
  }

  return {
    lnSpc,
    spcBef,
    spcAft,
    textBulletColor,
    textBulletSize,
    textBulletTypeface,
    textBullet,
    tabLst,
    defRPr,
    extLst,
    marL,
    marR,
    lvl,
    indent,
    algn,
    defTabSz,
    rtl,
    eaLnBrk,
    fontAlgn,
    latinLnBrk,
    hangingPunct,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextShapeAutofit`. See `transitional/dml-main.xsd:2644`. */
export function readCT_TextShapeAutofit(cur: XmlCursor, ctx: ReadContext): CT_TextShapeAutofit {
  const start = requireStart(cur, 'CT_TextShapeAutofit');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextShapeAutofit', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextShapeAutofit', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextShapeAutofit', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextShapeAutofit', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_TextSpacing`. See `transitional/dml-main.xsd:3002`. */
export function readCT_TextSpacing(cur: XmlCursor, ctx: ReadContext): CT_TextSpacing {
  const start = requireStart(cur, 'CT_TextSpacing');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextSpacing', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_TextSpacing_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextSpacing', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'spcPct': {
            content = { kind: 'spcPct', value: readCT_TextSpacingPercent(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spcPts': {
            content = { kind: 'spcPts', value: readCT_TextSpacingPoint(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextSpacing', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextSpacing', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextSpacingPercent`. See `transitional/dml-main.xsd:2962`. */
export function readCT_TextSpacingPercent(cur: XmlCursor, ctx: ReadContext): CT_TextSpacingPercent {
  const start = requireStart(cur, 'CT_TextSpacingPercent');
  let val: ST_TextSpacingPercentOrPercentString | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TextSpacingPercentOrPercentString(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TextSpacingPercent', a, 'ST_TextSpacingPercentOrPercentString', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextSpacingPercent', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TextSpacingPercent', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextSpacingPercent', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextSpacingPercent', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextSpacingPercent', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TextSpacingPoint`. See `transitional/dml-main.xsd:2965`. */
export function readCT_TextSpacingPoint(cur: XmlCursor, ctx: ReadContext): CT_TextSpacingPoint {
  const start = requireStart(cur, 'CT_TextSpacingPoint');
  let val: ST_TextSpacingPoint | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'val': {
          const v = parseST_TextSpacingPoint(a.value);
          if (v !== undefined) {
            val = v;
            continue;
          }
          ctx.invalidValue('CT_TextSpacingPoint', a, 'ST_TextSpacingPoint', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextSpacingPoint', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (val === undefined) ctx.missingRequired('CT_TextSpacingPoint', 'val', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextSpacingPoint', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextSpacingPoint', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextSpacingPoint', cur);
    cur.next();
  }
  return {
    val,
    $unknownAttrs,
  };
}

/** Read a `CT_TextTabStop`. See `transitional/dml-main.xsd:2988`. */
export function readCT_TextTabStop(cur: XmlCursor, ctx: ReadContext): CT_TextTabStop {
  const start = requireStart(cur, 'CT_TextTabStop');
  let pos: ST_Coordinate32 | undefined;
  let algn: ST_TextTabAlignType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'pos': {
          const v = parseST_Coordinate32(a.value);
          if (v !== undefined) {
            pos = v;
            continue;
          }
          ctx.invalidValue('CT_TextTabStop', a, 'ST_Coordinate32', cur);
          break;
        }
        case 'algn': {
          const v = parseST_TextTabAlignType(a.value);
          if (v !== undefined) {
            algn = v;
            continue;
          }
          ctx.invalidValue('CT_TextTabStop', a, 'ST_TextTabAlignType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextTabStop', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextTabStop', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextTabStop', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextTabStop', cur);
    cur.next();
  }
  return {
    pos,
    algn,
    $unknownAttrs,
  };
}

/** Read a `CT_TextTabStopList`. See `transitional/dml-main.xsd:2992`. */
export function readCT_TextTabStopList(cur: XmlCursor, ctx: ReadContext): CT_TextTabStopList {
  const start = requireStart(cur, 'CT_TextTabStopList');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextTabStopList', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const tab: CT_TextTabStop[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextTabStopList', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'tab': {
            tab.push(readCT_TextTabStop(cur, ctx));
            $slot = 0;
            $index = tab.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextTabStopList', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextTabStopList', cur);
    cur.next();
  }

  return {
    tab,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextUnderlineFillFollowText`. See `transitional/dml-main.xsd:2877`. */
export function readCT_TextUnderlineFillFollowText(cur: XmlCursor, ctx: ReadContext): CT_TextUnderlineFillFollowText {
  const start = requireStart(cur, 'CT_TextUnderlineFillFollowText');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextUnderlineFillFollowText', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextUnderlineFillFollowText', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextUnderlineFillFollowText', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextUnderlineFillFollowText', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_TextUnderlineFillGroupWrapper`. See `transitional/dml-main.xsd:2878`. */
export function readCT_TextUnderlineFillGroupWrapper(cur: XmlCursor, ctx: ReadContext): CT_TextUnderlineFillGroupWrapper {
  const start = requireStart(cur, 'CT_TextUnderlineFillGroupWrapper');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextUnderlineFillGroupWrapper', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let fillProperties: CT_TextUnderlineFillGroupWrapper_FillProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextUnderlineFillGroupWrapper', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'noFill': {
            fillProperties = { kind: 'noFill', value: readCT_NoFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'solidFill': {
            fillProperties = { kind: 'solidFill', value: readCT_SolidColorFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'gradFill': {
            fillProperties = { kind: 'gradFill', value: readCT_GradientFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            fillProperties = { kind: 'blipFill', value: readCT_BlipFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'pattFill': {
            fillProperties = { kind: 'pattFill', value: readCT_PatternFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'grpFill': {
            fillProperties = { kind: 'grpFill', value: readCT_GroupFillProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextUnderlineFillGroupWrapper', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextUnderlineFillGroupWrapper', cur);
    cur.next();
  }

  return {
    fillProperties,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextUnderlineLineFollowText`. See `transitional/dml-main.xsd:2876`. */
export function readCT_TextUnderlineLineFollowText(cur: XmlCursor, ctx: ReadContext): CT_TextUnderlineLineFollowText {
  const start = requireStart(cur, 'CT_TextUnderlineLineFollowText');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TextUnderlineLineFollowText', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextUnderlineLineFollowText', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TextUnderlineLineFollowText', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextUnderlineLineFollowText', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_ThemeableLineStyle`. See `transitional/dml-main.xsd:2468`. */
export function readCT_ThemeableLineStyle(cur: XmlCursor, ctx: ReadContext): CT_ThemeableLineStyle {
  const start = requireStart(cur, 'CT_ThemeableLineStyle');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ThemeableLineStyle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_ThemeableLineStyle_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ThemeableLineStyle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'ln': {
            content = { kind: 'ln', value: readCT_LineProperties(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lnRef': {
            content = { kind: 'lnRef', value: readCT_StyleMatrixReference(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ThemeableLineStyle', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ThemeableLineStyle', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TileInfoProperties`. See `transitional/dml-main.xsd:1469`. */
export function readCT_TileInfoProperties(cur: XmlCursor, ctx: ReadContext): CT_TileInfoProperties {
  const start = requireStart(cur, 'CT_TileInfoProperties');
  let tx: ST_Coordinate | undefined;
  let ty: ST_Coordinate | undefined;
  let sx: ST_Percentage | undefined;
  let sy: ST_Percentage | undefined;
  let flip: ST_TileFlipMode | undefined;
  let algn: ST_RectAlignment | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'tx': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            tx = v;
            continue;
          }
          ctx.invalidValue('CT_TileInfoProperties', a, 'ST_Coordinate', cur);
          break;
        }
        case 'ty': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            ty = v;
            continue;
          }
          ctx.invalidValue('CT_TileInfoProperties', a, 'ST_Coordinate', cur);
          break;
        }
        case 'sx': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            sx = v;
            continue;
          }
          ctx.invalidValue('CT_TileInfoProperties', a, 'ST_Percentage', cur);
          break;
        }
        case 'sy': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            sy = v;
            continue;
          }
          ctx.invalidValue('CT_TileInfoProperties', a, 'ST_Percentage', cur);
          break;
        }
        case 'flip': {
          const v = parseST_TileFlipMode(a.value);
          if (v !== undefined) {
            flip = v;
            continue;
          }
          ctx.invalidValue('CT_TileInfoProperties', a, 'ST_TileFlipMode', cur);
          break;
        }
        case 'algn': {
          const v = parseST_RectAlignment(a.value);
          if (v !== undefined) {
            algn = v;
            continue;
          }
          ctx.invalidValue('CT_TileInfoProperties', a, 'ST_RectAlignment', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TileInfoProperties', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TileInfoProperties', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TileInfoProperties', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TileInfoProperties', cur);
    cur.next();
  }
  return {
    tx,
    ty,
    sx,
    sy,
    flip,
    algn,
    $unknownAttrs,
  };
}

/** Read a `CT_TintEffect`. See `transitional/dml-main.xsd:1400`. */
export function readCT_TintEffect(cur: XmlCursor, ctx: ReadContext): CT_TintEffect {
  const start = requireStart(cur, 'CT_TintEffect');
  let hue: ST_PositiveFixedAngle | undefined;
  let amt: ST_FixedPercentage | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'hue': {
          const v = parseST_PositiveFixedAngle(a.value);
          if (v !== undefined) {
            hue = v;
            continue;
          }
          ctx.invalidValue('CT_TintEffect', a, 'ST_PositiveFixedAngle', cur);
          break;
        }
        case 'amt': {
          const v = parseST_FixedPercentage(a.value);
          if (v !== undefined) {
            amt = v;
            continue;
          }
          ctx.invalidValue('CT_TintEffect', a, 'ST_FixedPercentage', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TintEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TintEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TintEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TintEffect', cur);
    cur.next();
  }
  return {
    hue,
    amt,
    $unknownAttrs,
  };
}

/** Read a `CT_Transform2D`. See `transitional/dml-main.xsd:635`. */
export function readCT_Transform2D(cur: XmlCursor, ctx: ReadContext): CT_Transform2D {
  const start = requireStart(cur, 'CT_Transform2D');
  let rot: ST_Angle | undefined;
  let flipH: boolean | undefined;
  let flipV: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'rot': {
          const v = parseST_Angle(a.value);
          if (v !== undefined) {
            rot = v;
            continue;
          }
          ctx.invalidValue('CT_Transform2D', a, 'ST_Angle', cur);
          break;
        }
        case 'flipH': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            flipH = v;
            continue;
          }
          ctx.invalidValue('CT_Transform2D', a, 'xsd:boolean', cur);
          break;
        }
        case 'flipV': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            flipV = v;
            continue;
          }
          ctx.invalidValue('CT_Transform2D', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Transform2D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let off: CT_Point2D | undefined;
  let ext: CT_PositiveSize2D | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Transform2D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'off': {
            off = readCT_Point2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ext': {
            ext = readCT_PositiveSize2D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Transform2D', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Transform2D', cur);
    cur.next();
  }

  return {
    off,
    ext,
    rot,
    flipH,
    flipV,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TransformEffect`. See `transitional/dml-main.xsd:1404`. */
export function readCT_TransformEffect(cur: XmlCursor, ctx: ReadContext): CT_TransformEffect {
  const start = requireStart(cur, 'CT_TransformEffect');
  let sx: ST_Percentage | undefined;
  let sy: ST_Percentage | undefined;
  let kx: ST_FixedAngle | undefined;
  let ky: ST_FixedAngle | undefined;
  let tx: ST_Coordinate | undefined;
  let ty: ST_Coordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'sx': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            sx = v;
            continue;
          }
          ctx.invalidValue('CT_TransformEffect', a, 'ST_Percentage', cur);
          break;
        }
        case 'sy': {
          const v = parseST_Percentage(a.value);
          if (v !== undefined) {
            sy = v;
            continue;
          }
          ctx.invalidValue('CT_TransformEffect', a, 'ST_Percentage', cur);
          break;
        }
        case 'kx': {
          const v = parseST_FixedAngle(a.value);
          if (v !== undefined) {
            kx = v;
            continue;
          }
          ctx.invalidValue('CT_TransformEffect', a, 'ST_FixedAngle', cur);
          break;
        }
        case 'ky': {
          const v = parseST_FixedAngle(a.value);
          if (v !== undefined) {
            ky = v;
            continue;
          }
          ctx.invalidValue('CT_TransformEffect', a, 'ST_FixedAngle', cur);
          break;
        }
        case 'tx': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            tx = v;
            continue;
          }
          ctx.invalidValue('CT_TransformEffect', a, 'ST_Coordinate', cur);
          break;
        }
        case 'ty': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            ty = v;
            continue;
          }
          ctx.invalidValue('CT_TransformEffect', a, 'ST_Coordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TransformEffect', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TransformEffect', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_TransformEffect', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TransformEffect', cur);
    cur.next();
  }
  return {
    sx,
    sy,
    kx,
    ky,
    tx,
    ty,
    $unknownAttrs,
  };
}

/** Read a `CT_Vector3D`. See `transitional/dml-main.xsd:660`. */
export function readCT_Vector3D(cur: XmlCursor, ctx: ReadContext): CT_Vector3D {
  const start = requireStart(cur, 'CT_Vector3D');
  let dx: ST_Coordinate | undefined;
  let dy: ST_Coordinate | undefined;
  let dz: ST_Coordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'dx': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            dx = v;
            continue;
          }
          ctx.invalidValue('CT_Vector3D', a, 'ST_Coordinate', cur);
          break;
        }
        case 'dy': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            dy = v;
            continue;
          }
          ctx.invalidValue('CT_Vector3D', a, 'ST_Coordinate', cur);
          break;
        }
        case 'dz': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            dz = v;
            continue;
          }
          ctx.invalidValue('CT_Vector3D', a, 'ST_Coordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Vector3D', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (dx === undefined) ctx.missingRequired('CT_Vector3D', 'dx', cur);
  if (dy === undefined) ctx.missingRequired('CT_Vector3D', 'dy', cur);
  if (dz === undefined) ctx.missingRequired('CT_Vector3D', 'dz', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Vector3D', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Vector3D', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Vector3D', cur);
    cur.next();
  }
  return {
    dx,
    dy,
    dz,
    $unknownAttrs,
  };
}

/** Read a `CT_VideoFile`. See `transitional/dml-main.xsd:27`. */
export function readCT_VideoFile(cur: XmlCursor, ctx: ReadContext): CT_VideoFile {
  const start = requireStart(cur, 'CT_VideoFile');
  let link: ST_RelationshipId | undefined;
  let contentType: string | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'contentType':
          contentType = a.value as string;
          continue;

      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'link': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            link = v;
            continue;
          }
          ctx.invalidValue('CT_VideoFile', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_VideoFile', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (link === undefined) ctx.missingRequired('CT_VideoFile', 'link', cur);

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_VideoFile', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_VideoFile', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_VideoFile', cur);
    cur.next();
  }

  return {
    extLst,
    link,
    contentType,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_WholeE2oFormatting`. See `transitional/dml-main.xsd:954`. */
export function readCT_WholeE2oFormatting(cur: XmlCursor, ctx: ReadContext): CT_WholeE2oFormatting {
  const start = requireStart(cur, 'CT_WholeE2oFormatting');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_WholeE2oFormatting', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let ln: CT_LineProperties | undefined;
  let effectProperties: CT_WholeE2oFormatting_EffectProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WholeE2oFormatting', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'ln': {
            ln = readCT_LineProperties(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'effectLst': {
            effectProperties = { kind: 'effectLst', value: readCT_EffectList(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'effectDag': {
            effectProperties = { kind: 'effectDag', value: readCT_EffectContainer(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WholeE2oFormatting', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WholeE2oFormatting', cur);
    cur.next();
  }

  return {
    ln,
    effectProperties,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_XYAdjustHandle`. See `transitional/dml-main.xsd:1984`. */
export function readCT_XYAdjustHandle(cur: XmlCursor, ctx: ReadContext): CT_XYAdjustHandle {
  const start = requireStart(cur, 'CT_XYAdjustHandle');
  let gdRefX: ST_GeomGuideName | undefined;
  let minX: ST_AdjCoordinate | undefined;
  let maxX: ST_AdjCoordinate | undefined;
  let gdRefY: ST_GeomGuideName | undefined;
  let minY: ST_AdjCoordinate | undefined;
  let maxY: ST_AdjCoordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'gdRefX': {
          const v = parseST_GeomGuideName(a.value);
          if (v !== undefined) {
            gdRefX = v;
            continue;
          }
          ctx.invalidValue('CT_XYAdjustHandle', a, 'ST_GeomGuideName', cur);
          break;
        }
        case 'minX': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            minX = v;
            continue;
          }
          ctx.invalidValue('CT_XYAdjustHandle', a, 'ST_AdjCoordinate', cur);
          break;
        }
        case 'maxX': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            maxX = v;
            continue;
          }
          ctx.invalidValue('CT_XYAdjustHandle', a, 'ST_AdjCoordinate', cur);
          break;
        }
        case 'gdRefY': {
          const v = parseST_GeomGuideName(a.value);
          if (v !== undefined) {
            gdRefY = v;
            continue;
          }
          ctx.invalidValue('CT_XYAdjustHandle', a, 'ST_GeomGuideName', cur);
          break;
        }
        case 'minY': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            minY = v;
            continue;
          }
          ctx.invalidValue('CT_XYAdjustHandle', a, 'ST_AdjCoordinate', cur);
          break;
        }
        case 'maxY': {
          const v = parseST_AdjCoordinate(a.value);
          if (v !== undefined) {
            maxY = v;
            continue;
          }
          ctx.invalidValue('CT_XYAdjustHandle', a, 'ST_AdjCoordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_XYAdjustHandle', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let pos: CT_AdjPoint2D | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_XYAdjustHandle', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'pos': {
            pos = readCT_AdjPoint2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_XYAdjustHandle', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_XYAdjustHandle', cur);
    cur.next();
  }

  return {
    pos,
    gdRefX,
    minX,
    maxX,
    gdRefY,
    minY,
    maxY,
    $unknown,
    $unknownAttrs,
  };
}
