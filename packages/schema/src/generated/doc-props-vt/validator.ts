/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: validators for the doc-props-vt namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import { validateST_Guid } from '../shared-types/validator.js';
import { ST_ArrayBaseType_VALUES, ST_VectorBaseType_VALUES } from './types.js';

export type ValidationCode = 'missing-required' | 'invalid-value' | 'facet-violation';
export interface ValidationIssue { readonly path: string; readonly code: ValidationCode; readonly message: string; }
export type ValidationResult = readonly ValidationIssue[];
function issue(path: string, code: ValidationCode, message: string): ValidationIssue { return { path, code, message }; }
function invalidEnum(path: string, value: unknown, values: ReadonlySet<string>): ValidationIssue { return issue(path, 'invalid-value', `value ${String(value)} is not in the enumeration {${[...values].join(', ')}}`); }
const BUILTIN_GROUPS = {
  string: new Set(['xsd:ID', 'xsd:IDREF', 'xsd:NCName', 'xsd:NMTOKEN', 'xsd:anyURI', 'xsd:base64Binary', 'xsd:date', 'xsd:dateTime', 'xsd:duration', 'xsd:hexBinary', 'xsd:language', 'xsd:string', 'xsd:time', 'xsd:token']),
  boolean: new Set(['xsd:boolean']),
  number: new Set(['xsd:byte', 'xsd:decimal', 'xsd:double', 'xsd:float', 'xsd:int', 'xsd:integer', 'xsd:long', 'xsd:nonNegativeInteger', 'xsd:positiveInteger', 'xsd:unsignedByte', 'xsd:unsignedInt', 'xsd:unsignedLong', 'xsd:unsignedShort']),
};
function validBuiltin(value: unknown, name: string): boolean { if (BUILTIN_GROUPS.string.has(name)) return typeof value === 'string'; if (BUILTIN_GROUPS.boolean.has(name)) return typeof value === 'boolean'; if (BUILTIN_GROUPS.number.has(name)) return typeof value === 'number' && Number.isFinite(value); return false; }

export function validateST_ArrayBaseType(value: unknown, path = 'ST_ArrayBaseType'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_ArrayBaseType_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_ArrayBaseType_VALUES));
  }
  return issues;
}

export function validateST_Cy(value: unknown, path = 'ST_Cy'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !(new RegExp("\\s*[0-9]*\\.[0-9]{4}\\s*")).test(value)) issues.push(issue(path, 'facet-violation', 'pattern mismatch'));
  return issues;
}

export function validateST_Error(value: unknown, path = 'ST_Error'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !(new RegExp("\\s*0x[0-9A-Za-z]{8}\\s*")).test(value)) issues.push(issue(path, 'facet-violation', 'pattern mismatch'));
  return issues;
}

export function validateST_VectorBaseType(value: unknown, path = 'ST_VectorBaseType'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_VectorBaseType_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_VectorBaseType_VALUES));
  }
  return issues;
}

export function validateCT_Array(value: unknown, path = 'CT_Array'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (!Array.isArray(v['content'])) {
  } else {
    for (let i = 0; i < (v['content'] as readonly unknown[]).length; i += 1) {
      if ((v['content'] as readonly unknown[])[i] && typeof (v['content'] as readonly unknown[])[i] === 'object') {
        const item = (v['content'] as readonly unknown[])[i] as { kind?: unknown; value?: unknown };
        if (item.kind !== '$raw') {
          if (item.kind === 'variant') issues.push(...validateCT_Variant(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'error') issues.push(...validateST_Error(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'cy') issues.push(...validateST_Cy(item.value, path + '.content[' + i + ']' + '.value'));
          if ('variant' !== item.kind && 'i1' !== item.kind && 'i2' !== item.kind && 'i4' !== item.kind && 'int' !== item.kind && 'ui1' !== item.kind && 'ui2' !== item.kind && 'ui4' !== item.kind && 'uint' !== item.kind && 'r4' !== item.kind && 'r8' !== item.kind && 'decimal' !== item.kind && 'bstr' !== item.kind && 'date' !== item.kind && 'bool' !== item.kind && 'error' !== item.kind && 'cy' !== item.kind) issues.push(issue(path + '.content[' + i + ']', 'invalid-value', 'unknown choice alternative'));
        }
      }
    }
  }
  if (v['lBounds'] === undefined) {
    issues.push(issue(path + '.lBounds', 'missing-required', 'required attribute is absent'));
  } else {
    if (!validBuiltin(v['lBounds'], 'xsd:int')) issues.push(issue(path + '.lBounds', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['uBounds'] === undefined) {
    issues.push(issue(path + '.uBounds', 'missing-required', 'required attribute is absent'));
  } else {
    if (!validBuiltin(v['uBounds'], 'xsd:int')) issues.push(issue(path + '.uBounds', 'invalid-value', 'invalid xsd:int'));
  }
  if (v['baseType'] === undefined) {
    issues.push(issue(path + '.baseType', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_ArrayBaseType(v['baseType'], path + '.baseType'));
  }
  return issues;
}

export function validateCT_Empty(value: unknown, path = 'CT_Empty'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  return issues;
}

export function validateCT_Null(value: unknown, path = 'CT_Null'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  return issues;
}

export function validateCT_Variant(value: unknown, path = 'CT_Variant'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['content'] === undefined) {
    issues.push(issue(path + '.content', 'missing-required', 'required slot is absent'));
  } else {
    if (v['content'] && typeof v['content'] === 'object') {
      const item = v['content'] as { kind?: unknown; value?: unknown };
      if (item.kind !== '$raw') {
        if (item.kind === 'variant') issues.push(...validateCT_Variant(item.value, path + '.content' + '.value'));
        if (item.kind === 'vector') issues.push(...validateCT_Vector(item.value, path + '.content' + '.value'));
        if (item.kind === 'array') issues.push(...validateCT_Array(item.value, path + '.content' + '.value'));
        if (item.kind === 'empty') issues.push(...validateCT_Empty(item.value, path + '.content' + '.value'));
        if (item.kind === 'null') issues.push(...validateCT_Null(item.value, path + '.content' + '.value'));
        if (item.kind === 'cy') issues.push(...validateST_Cy(item.value, path + '.content' + '.value'));
        if (item.kind === 'error') issues.push(...validateST_Error(item.value, path + '.content' + '.value'));
        if (item.kind === 'vstream') issues.push(...validateCT_Vstream(item.value, path + '.content' + '.value'));
        if (item.kind === 'clsid') issues.push(...validateST_Guid(item.value, path + '.content' + '.value'));
        if ('variant' !== item.kind && 'vector' !== item.kind && 'array' !== item.kind && 'blob' !== item.kind && 'oblob' !== item.kind && 'empty' !== item.kind && 'null' !== item.kind && 'i1' !== item.kind && 'i2' !== item.kind && 'i4' !== item.kind && 'i8' !== item.kind && 'int' !== item.kind && 'ui1' !== item.kind && 'ui2' !== item.kind && 'ui4' !== item.kind && 'ui8' !== item.kind && 'uint' !== item.kind && 'r4' !== item.kind && 'r8' !== item.kind && 'decimal' !== item.kind && 'lpstr' !== item.kind && 'lpwstr' !== item.kind && 'bstr' !== item.kind && 'date' !== item.kind && 'filetime' !== item.kind && 'bool' !== item.kind && 'cy' !== item.kind && 'error' !== item.kind && 'stream' !== item.kind && 'ostream' !== item.kind && 'storage' !== item.kind && 'ostorage' !== item.kind && 'vstream' !== item.kind && 'clsid' !== item.kind) issues.push(issue(path + '.content', 'invalid-value', 'unknown choice alternative'));
      }
    }
  }
  return issues;
}

export function validateCT_Vector(value: unknown, path = 'CT_Vector'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (!Array.isArray(v['content'])) {
  } else {
    for (let i = 0; i < (v['content'] as readonly unknown[]).length; i += 1) {
      if ((v['content'] as readonly unknown[])[i] && typeof (v['content'] as readonly unknown[])[i] === 'object') {
        const item = (v['content'] as readonly unknown[])[i] as { kind?: unknown; value?: unknown };
        if (item.kind !== '$raw') {
          if (item.kind === 'variant') issues.push(...validateCT_Variant(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'cy') issues.push(...validateST_Cy(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'error') issues.push(...validateST_Error(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'clsid') issues.push(...validateST_Guid(item.value, path + '.content[' + i + ']' + '.value'));
          if ('variant' !== item.kind && 'i1' !== item.kind && 'i2' !== item.kind && 'i4' !== item.kind && 'i8' !== item.kind && 'ui1' !== item.kind && 'ui2' !== item.kind && 'ui4' !== item.kind && 'ui8' !== item.kind && 'r4' !== item.kind && 'r8' !== item.kind && 'lpstr' !== item.kind && 'lpwstr' !== item.kind && 'bstr' !== item.kind && 'date' !== item.kind && 'filetime' !== item.kind && 'bool' !== item.kind && 'cy' !== item.kind && 'error' !== item.kind && 'clsid' !== item.kind) issues.push(issue(path + '.content[' + i + ']', 'invalid-value', 'unknown choice alternative'));
        }
      }
    }
  }
  if (v['baseType'] === undefined) {
    issues.push(issue(path + '.baseType', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_VectorBaseType(v['baseType'], path + '.baseType'));
  }
  if (v['size'] === undefined) {
    issues.push(issue(path + '.size', 'missing-required', 'required attribute is absent'));
  } else {
    if (!validBuiltin(v['size'], 'xsd:unsignedInt')) issues.push(issue(path + '.size', 'invalid-value', 'invalid xsd:unsignedInt'));
  }
  return issues;
}

export function validateCT_Vstream(value: unknown, path = 'CT_Vstream'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (!validBuiltin(v['$value'], 'xsd:base64Binary')) issues.push(issue(path + '.$value', 'invalid-value', 'invalid xsd:base64Binary'));
  if (v['version'] === undefined) {
  } else {
    issues.push(...validateST_Guid(v['version'], path + '.version'));
  }
  return issues;
}
