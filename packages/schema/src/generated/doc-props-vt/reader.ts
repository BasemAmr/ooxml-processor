/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the doc-props-vt namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  parseDecimal,
  parseDouble,
  parseInteger,
  parseXsdBoolean,
  readScalar,
  requireStart,
} from '../../runtime/index.js';
import { parseST_Guid } from '../shared-types/reader.js';
import type { ST_Guid } from '../shared-types/types.js';
import {
  type CT_Array,
  type CT_Array_Content,
  type CT_Empty,
  type CT_Null,
  type CT_Variant,
  type CT_Variant_Content,
  type CT_Vector,
  type CT_Vector_Content,
  type CT_Vstream,
  type ST_ArrayBaseType,
  ST_ArrayBaseType_VALUES,
  type ST_Cy,
  type ST_Error,
  type ST_VectorBaseType,
  ST_VectorBaseType_VALUES,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_ArrayBaseType(value: string): ST_ArrayBaseType | undefined {
  const s = collapse(value);
  return ST_ArrayBaseType_VALUES.has(s) ? (s as ST_ArrayBaseType) : undefined;
}

export function parseST_Cy(value: string): ST_Cy | undefined {
  return value as ST_Cy;
}

export function parseST_Error(value: string): ST_Error | undefined {
  return value as ST_Error;
}

export function parseST_VectorBaseType(value: string): ST_VectorBaseType | undefined {
  const s = collapse(value);
  return ST_VectorBaseType_VALUES.has(s) ? (s as ST_VectorBaseType) : undefined;
}

// Complex-type readers.

/** Read a `CT_Array`. See `transitional/shared-documentPropertiesVariantTypes.xsd:92`. */
export function readCT_Array(cur: XmlCursor, ctx: ReadContext): CT_Array {
  const start = requireStart(cur, 'CT_Array');
  let lBounds: number | undefined;
  let uBounds: number | undefined;
  let baseType: ST_ArrayBaseType | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'lBounds': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            lBounds = v;
            continue;
          }
          ctx.invalidValue('CT_Array', a, 'xsd:int', cur);
          break;
        }
        case 'uBounds': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            uBounds = v;
            continue;
          }
          ctx.invalidValue('CT_Array', a, 'xsd:int', cur);
          break;
        }
        case 'baseType': {
          const v = parseST_ArrayBaseType(a.value);
          if (v !== undefined) {
            baseType = v;
            continue;
          }
          ctx.invalidValue('CT_Array', a, 'ST_ArrayBaseType', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Array', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (lBounds === undefined) ctx.missingRequired('CT_Array', 'lBounds', cur);
  if (uBounds === undefined) ctx.missingRequired('CT_Array', 'uBounds', cur);
  if (baseType === undefined) ctx.missingRequired('CT_Array', 'baseType', cur);

  const content: CT_Array_Content[] = [];

  const NS_doc_props_vt = ctx.uris['doc-props-vt'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Array', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_doc_props_vt) {
        switch (ev.localName) {
          case 'variant': {
            content.push({ kind: 'variant', value: readCT_Variant(cur, ctx) });
            continue;
          }
          case 'i1': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'i1', 'xsd:byte', $s, cur);
            content.push({ kind: 'i1', value: ($pv ?? $s) as number });
            continue;
          }
          case 'i2': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'i2', 'xsd:int', $s, cur);
            content.push({ kind: 'i2', value: ($pv ?? $s) as number });
            continue;
          }
          case 'i4': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'i4', 'xsd:int', $s, cur);
            content.push({ kind: 'i4', value: ($pv ?? $s) as number });
            continue;
          }
          case 'int': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'int', 'xsd:int', $s, cur);
            content.push({ kind: 'int', value: ($pv ?? $s) as number });
            continue;
          }
          case 'ui1': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'ui1', 'xsd:unsignedByte', $s, cur);
            content.push({ kind: 'ui1', value: ($pv ?? $s) as number });
            continue;
          }
          case 'ui2': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'ui2', 'xsd:unsignedShort', $s, cur);
            content.push({ kind: 'ui2', value: ($pv ?? $s) as number });
            continue;
          }
          case 'ui4': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'ui4', 'xsd:unsignedInt', $s, cur);
            content.push({ kind: 'ui4', value: ($pv ?? $s) as number });
            continue;
          }
          case 'uint': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'uint', 'xsd:unsignedInt', $s, cur);
            content.push({ kind: 'uint', value: ($pv ?? $s) as number });
            continue;
          }
          case 'r4': {
            const $s = readScalar(cur, ctx);
            const $pv = parseDouble($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'r4', 'xsd:float', $s, cur);
            content.push({ kind: 'r4', value: ($pv ?? $s) as number });
            continue;
          }
          case 'r8': {
            const $s = readScalar(cur, ctx);
            const $pv = parseDouble($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'r8', 'xsd:double', $s, cur);
            content.push({ kind: 'r8', value: ($pv ?? $s) as number });
            continue;
          }
          case 'decimal': {
            const $s = readScalar(cur, ctx);
            const $pv = parseDecimal($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'decimal', 'xsd:decimal', $s, cur);
            content.push({ kind: 'decimal', value: ($pv ?? $s) as number });
            continue;
          }
          case 'bstr': {
            content.push({ kind: 'bstr', value: readScalar(cur, ctx) });
            continue;
          }
          case 'date': {
            content.push({ kind: 'date', value: readScalar(cur, ctx) });
            continue;
          }
          case 'bool': {
            const $s = readScalar(cur, ctx);
            const $pv = parseXsdBoolean($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'bool', 'xsd:boolean', $s, cur);
            content.push({ kind: 'bool', value: ($pv ?? $s) as boolean });
            continue;
          }
          case 'error': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Error($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'error', 'ST_Error', $s, cur);
            content.push({ kind: 'error', value: ($pv ?? $s) as ST_Error });
            continue;
          }
          case 'cy': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Cy($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Array', 'cy', 'ST_Cy', $s, cur);
            content.push({ kind: 'cy', value: ($pv ?? $s) as ST_Cy });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Array', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Array', cur);
    cur.next();
  }

  return {
    content,
    lBounds,
    uBounds,
    baseType,
    $unknownAttrs,
  };
}

/** Read a `CT_Empty`. See `transitional/shared-documentPropertiesVariantTypes.xsd:64`. */
export function readCT_Empty(cur: XmlCursor, ctx: ReadContext): CT_Empty {
  const start = requireStart(cur, 'CT_Empty');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Empty', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Empty', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Empty', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Empty', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_Null`. See `transitional/shared-documentPropertiesVariantTypes.xsd:65`. */
export function readCT_Null(cur: XmlCursor, ctx: ReadContext): CT_Null {
  const start = requireStart(cur, 'CT_Null');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Null', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Null', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Null', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Null', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_Variant`. See `transitional/shared-documentPropertiesVariantTypes.xsd:116`. */
export function readCT_Variant(cur: XmlCursor, ctx: ReadContext): CT_Variant {
  const start = requireStart(cur, 'CT_Variant');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Variant', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let content: CT_Variant_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_doc_props_vt = ctx.uris['doc-props-vt'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Variant', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_doc_props_vt) {
        switch (ev.localName) {
          case 'variant': {
            content = { kind: 'variant', value: readCT_Variant(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'vector': {
            content = { kind: 'vector', value: readCT_Vector(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'array': {
            content = { kind: 'array', value: readCT_Array(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'blob': {
            content = { kind: 'blob', value: readScalar(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'oblob': {
            content = { kind: 'oblob', value: readScalar(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'empty': {
            content = { kind: 'empty', value: readCT_Empty(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'null': {
            content = { kind: 'null', value: readCT_Null(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'i1': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'i1', 'xsd:byte', $s, cur);
            content = { kind: 'i1', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'i2': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'i2', 'xsd:int', $s, cur);
            content = { kind: 'i2', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'i4': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'i4', 'xsd:int', $s, cur);
            content = { kind: 'i4', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'i8': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'i8', 'xsd:long', $s, cur);
            content = { kind: 'i8', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'int': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'int', 'xsd:int', $s, cur);
            content = { kind: 'int', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ui1': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'ui1', 'xsd:unsignedByte', $s, cur);
            content = { kind: 'ui1', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ui2': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'ui2', 'xsd:unsignedShort', $s, cur);
            content = { kind: 'ui2', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ui4': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'ui4', 'xsd:unsignedInt', $s, cur);
            content = { kind: 'ui4', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ui8': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'ui8', 'xsd:unsignedLong', $s, cur);
            content = { kind: 'ui8', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'uint': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'uint', 'xsd:unsignedInt', $s, cur);
            content = { kind: 'uint', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'r4': {
            const $s = readScalar(cur, ctx);
            const $pv = parseDouble($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'r4', 'xsd:float', $s, cur);
            content = { kind: 'r4', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'r8': {
            const $s = readScalar(cur, ctx);
            const $pv = parseDouble($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'r8', 'xsd:double', $s, cur);
            content = { kind: 'r8', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'decimal': {
            const $s = readScalar(cur, ctx);
            const $pv = parseDecimal($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'decimal', 'xsd:decimal', $s, cur);
            content = { kind: 'decimal', value: ($pv ?? $s) as number };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lpstr': {
            content = { kind: 'lpstr', value: readScalar(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lpwstr': {
            content = { kind: 'lpwstr', value: readScalar(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'bstr': {
            content = { kind: 'bstr', value: readScalar(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'date': {
            content = { kind: 'date', value: readScalar(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'filetime': {
            content = { kind: 'filetime', value: readScalar(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'bool': {
            const $s = readScalar(cur, ctx);
            const $pv = parseXsdBoolean($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'bool', 'xsd:boolean', $s, cur);
            content = { kind: 'bool', value: ($pv ?? $s) as boolean };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cy': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Cy($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'cy', 'ST_Cy', $s, cur);
            content = { kind: 'cy', value: ($pv ?? $s) as ST_Cy };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'error': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Error($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'error', 'ST_Error', $s, cur);
            content = { kind: 'error', value: ($pv ?? $s) as ST_Error };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'stream': {
            content = { kind: 'stream', value: readScalar(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ostream': {
            content = { kind: 'ostream', value: readScalar(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'storage': {
            content = { kind: 'storage', value: readScalar(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ostorage': {
            content = { kind: 'ostorage', value: readScalar(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'vstream': {
            content = { kind: 'vstream', value: readCT_Vstream(cur, ctx) };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'clsid': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Guid($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Variant', 'clsid', 'ST_Guid', $s, cur);
            content = { kind: 'clsid', value: ($pv ?? $s) as ST_Guid };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Variant', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Variant', cur);
    cur.next();
  }

  return {
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Vector`. See `transitional/shared-documentPropertiesVariantTypes.xsd:66`. */
export function readCT_Vector(cur: XmlCursor, ctx: ReadContext): CT_Vector {
  const start = requireStart(cur, 'CT_Vector');
  let baseType: ST_VectorBaseType | undefined;
  let size: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'baseType': {
          const v = parseST_VectorBaseType(a.value);
          if (v !== undefined) {
            baseType = v;
            continue;
          }
          ctx.invalidValue('CT_Vector', a, 'ST_VectorBaseType', cur);
          break;
        }
        case 'size': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            size = v;
            continue;
          }
          ctx.invalidValue('CT_Vector', a, 'xsd:unsignedInt', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Vector', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (baseType === undefined) ctx.missingRequired('CT_Vector', 'baseType', cur);
  if (size === undefined) ctx.missingRequired('CT_Vector', 'size', cur);

  const content: CT_Vector_Content[] = [];

  const NS_doc_props_vt = ctx.uris['doc-props-vt'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Vector', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_doc_props_vt) {
        switch (ev.localName) {
          case 'variant': {
            content.push({ kind: 'variant', value: readCT_Variant(cur, ctx) });
            continue;
          }
          case 'i1': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'i1', 'xsd:byte', $s, cur);
            content.push({ kind: 'i1', value: ($pv ?? $s) as number });
            continue;
          }
          case 'i2': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'i2', 'xsd:int', $s, cur);
            content.push({ kind: 'i2', value: ($pv ?? $s) as number });
            continue;
          }
          case 'i4': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'i4', 'xsd:int', $s, cur);
            content.push({ kind: 'i4', value: ($pv ?? $s) as number });
            continue;
          }
          case 'i8': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'i8', 'xsd:long', $s, cur);
            content.push({ kind: 'i8', value: ($pv ?? $s) as number });
            continue;
          }
          case 'ui1': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'ui1', 'xsd:unsignedByte', $s, cur);
            content.push({ kind: 'ui1', value: ($pv ?? $s) as number });
            continue;
          }
          case 'ui2': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'ui2', 'xsd:unsignedShort', $s, cur);
            content.push({ kind: 'ui2', value: ($pv ?? $s) as number });
            continue;
          }
          case 'ui4': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'ui4', 'xsd:unsignedInt', $s, cur);
            content.push({ kind: 'ui4', value: ($pv ?? $s) as number });
            continue;
          }
          case 'ui8': {
            const $s = readScalar(cur, ctx);
            const $pv = parseInteger($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'ui8', 'xsd:unsignedLong', $s, cur);
            content.push({ kind: 'ui8', value: ($pv ?? $s) as number });
            continue;
          }
          case 'r4': {
            const $s = readScalar(cur, ctx);
            const $pv = parseDouble($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'r4', 'xsd:float', $s, cur);
            content.push({ kind: 'r4', value: ($pv ?? $s) as number });
            continue;
          }
          case 'r8': {
            const $s = readScalar(cur, ctx);
            const $pv = parseDouble($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'r8', 'xsd:double', $s, cur);
            content.push({ kind: 'r8', value: ($pv ?? $s) as number });
            continue;
          }
          case 'lpstr': {
            content.push({ kind: 'lpstr', value: readScalar(cur, ctx) });
            continue;
          }
          case 'lpwstr': {
            content.push({ kind: 'lpwstr', value: readScalar(cur, ctx) });
            continue;
          }
          case 'bstr': {
            content.push({ kind: 'bstr', value: readScalar(cur, ctx) });
            continue;
          }
          case 'date': {
            content.push({ kind: 'date', value: readScalar(cur, ctx) });
            continue;
          }
          case 'filetime': {
            content.push({ kind: 'filetime', value: readScalar(cur, ctx) });
            continue;
          }
          case 'bool': {
            const $s = readScalar(cur, ctx);
            const $pv = parseXsdBoolean($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'bool', 'xsd:boolean', $s, cur);
            content.push({ kind: 'bool', value: ($pv ?? $s) as boolean });
            continue;
          }
          case 'cy': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Cy($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'cy', 'ST_Cy', $s, cur);
            content.push({ kind: 'cy', value: ($pv ?? $s) as ST_Cy });
            continue;
          }
          case 'error': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Error($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'error', 'ST_Error', $s, cur);
            content.push({ kind: 'error', value: ($pv ?? $s) as ST_Error });
            continue;
          }
          case 'clsid': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_Guid($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Vector', 'clsid', 'ST_Guid', $s, cur);
            content.push({ kind: 'clsid', value: ($pv ?? $s) as ST_Guid });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Vector', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Vector', cur);
    cur.next();
  }

  return {
    content,
    baseType,
    size,
    $unknownAttrs,
  };
}

/** Read a `CT_Vstream`. See `transitional/shared-documentPropertiesVariantTypes.xsd:154`. */
export function readCT_Vstream(cur: XmlCursor, ctx: ReadContext): CT_Vstream {
  const start = requireStart(cur, 'CT_Vstream');
  let version: ST_Guid | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'version': {
          const v = parseST_Guid(a.value);
          if (v !== undefined) {
            version = v;
            continue;
          }
          ctx.invalidValue('CT_Vstream', a, 'ST_Guid', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Vstream', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let $value = '';
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Vstream', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'text') { $value += ev.value; cur.next(); continue; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_Vstream', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    cur.next();
  }

  return {
    $value: $value,
    version,
    $unknownAttrs,
  };
}
