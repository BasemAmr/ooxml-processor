/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: writers for the doc-props-vt namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  PositionedRawQueue,
  type WriteContext,
  type XmlSink,
  uriFor,
} from '../../runtime/index.js';
import type { CT_Array, CT_Empty, CT_Null, CT_Variant, CT_Vector, CT_Vstream } from './types.js';

// XML writers. Element names are supplied by the owning particle.

/** Write a `CT_Array`; the caller supplies its element local name. */
export function writeCT_Array(s: XmlSink, value: CT_Array, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'doc-props-vt'), localName);
  if (value['lBounds'] !== undefined) s.attr(null, 'lBounds', String(value['lBounds']));
  if (value['uBounds'] !== undefined) s.attr(null, 'uBounds', String(value['uBounds']));
  if (value['baseType'] !== undefined) s.attr(null, 'baseType', String(value['baseType']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_content of value['content']) {
    if (v_content.kind === 'variant') writeCT_Variant(s, v_content.value, ctx, 'variant');
    if (v_content.kind === 'i1') s.text(String(v_content.value));
    if (v_content.kind === 'i2') s.text(String(v_content.value));
    if (v_content.kind === 'i4') s.text(String(v_content.value));
    if (v_content.kind === 'int') s.text(String(v_content.value));
    if (v_content.kind === 'ui1') s.text(String(v_content.value));
    if (v_content.kind === 'ui2') s.text(String(v_content.value));
    if (v_content.kind === 'ui4') s.text(String(v_content.value));
    if (v_content.kind === 'uint') s.text(String(v_content.value));
    if (v_content.kind === 'r4') s.text(String(v_content.value));
    if (v_content.kind === 'r8') s.text(String(v_content.value));
    if (v_content.kind === 'decimal') s.text(String(v_content.value));
    if (v_content.kind === 'bstr') s.text(String(v_content.value));
    if (v_content.kind === 'date') s.text(String(v_content.value));
    if (v_content.kind === 'bool') s.text(String(v_content.value));
    if (v_content.kind === 'error') s.text(String(v_content.value));
    if (v_content.kind === 'cy') s.text(String(v_content.value));
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  s.endElement();
}

/** Write a `CT_Empty`; the caller supplies its element local name. */
export function writeCT_Empty(s: XmlSink, value: CT_Empty, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'doc-props-vt'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Null`; the caller supplies its element local name. */
export function writeCT_Null(s: XmlSink, value: CT_Null, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'doc-props-vt'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.endElement();
}

/** Write a `CT_Variant`; the caller supplies its element local name. */
export function writeCT_Variant(s: XmlSink, value: CT_Variant, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'doc-props-vt'), localName);
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  const $q = new PositionedRawQueue(value.$unknown);
  $q.flush(s, -1);
  if (value['content'] !== undefined) {
    if (value['content'].kind === 'variant') writeCT_Variant(s, value['content'].value, ctx, 'variant');
    if (value['content'].kind === 'vector') writeCT_Vector(s, value['content'].value, ctx, 'vector');
    if (value['content'].kind === 'array') writeCT_Array(s, value['content'].value, ctx, 'array');
    if (value['content'].kind === 'blob') s.text(String(value['content'].value));
    if (value['content'].kind === 'oblob') s.text(String(value['content'].value));
    if (value['content'].kind === 'empty') writeCT_Empty(s, value['content'].value, ctx, 'empty');
    if (value['content'].kind === 'null') writeCT_Null(s, value['content'].value, ctx, 'null');
    if (value['content'].kind === 'i1') s.text(String(value['content'].value));
    if (value['content'].kind === 'i2') s.text(String(value['content'].value));
    if (value['content'].kind === 'i4') s.text(String(value['content'].value));
    if (value['content'].kind === 'i8') s.text(String(value['content'].value));
    if (value['content'].kind === 'int') s.text(String(value['content'].value));
    if (value['content'].kind === 'ui1') s.text(String(value['content'].value));
    if (value['content'].kind === 'ui2') s.text(String(value['content'].value));
    if (value['content'].kind === 'ui4') s.text(String(value['content'].value));
    if (value['content'].kind === 'ui8') s.text(String(value['content'].value));
    if (value['content'].kind === 'uint') s.text(String(value['content'].value));
    if (value['content'].kind === 'r4') s.text(String(value['content'].value));
    if (value['content'].kind === 'r8') s.text(String(value['content'].value));
    if (value['content'].kind === 'decimal') s.text(String(value['content'].value));
    if (value['content'].kind === 'lpstr') s.text(String(value['content'].value));
    if (value['content'].kind === 'lpwstr') s.text(String(value['content'].value));
    if (value['content'].kind === 'bstr') s.text(String(value['content'].value));
    if (value['content'].kind === 'date') s.text(String(value['content'].value));
    if (value['content'].kind === 'filetime') s.text(String(value['content'].value));
    if (value['content'].kind === 'bool') s.text(String(value['content'].value));
    if (value['content'].kind === 'cy') s.text(String(value['content'].value));
    if (value['content'].kind === 'error') s.text(String(value['content'].value));
    if (value['content'].kind === 'stream') s.text(String(value['content'].value));
    if (value['content'].kind === 'ostream') s.text(String(value['content'].value));
    if (value['content'].kind === 'storage') s.text(String(value['content'].value));
    if (value['content'].kind === 'ostorage') s.text(String(value['content'].value));
    if (value['content'].kind === 'vstream') writeCT_Vstream(s, value['content'].value, ctx, 'vstream');
    if (value['content'].kind === 'clsid') s.text(String(value['content'].value));
  }
  $q.flush(s, 0);
  $q.flushRemaining(s);
  s.endElement();
}

/** Write a `CT_Vector`; the caller supplies its element local name. */
export function writeCT_Vector(s: XmlSink, value: CT_Vector, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'doc-props-vt'), localName);
  if (value['baseType'] !== undefined) s.attr(null, 'baseType', String(value['baseType']));
  if (value['size'] !== undefined) s.attr(null, 'size', String(value['size']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  for (const v_content of value['content']) {
    if (v_content.kind === 'variant') writeCT_Variant(s, v_content.value, ctx, 'variant');
    if (v_content.kind === 'i1') s.text(String(v_content.value));
    if (v_content.kind === 'i2') s.text(String(v_content.value));
    if (v_content.kind === 'i4') s.text(String(v_content.value));
    if (v_content.kind === 'i8') s.text(String(v_content.value));
    if (v_content.kind === 'ui1') s.text(String(v_content.value));
    if (v_content.kind === 'ui2') s.text(String(v_content.value));
    if (v_content.kind === 'ui4') s.text(String(v_content.value));
    if (v_content.kind === 'ui8') s.text(String(v_content.value));
    if (v_content.kind === 'r4') s.text(String(v_content.value));
    if (v_content.kind === 'r8') s.text(String(v_content.value));
    if (v_content.kind === 'lpstr') s.text(String(v_content.value));
    if (v_content.kind === 'lpwstr') s.text(String(v_content.value));
    if (v_content.kind === 'bstr') s.text(String(v_content.value));
    if (v_content.kind === 'date') s.text(String(v_content.value));
    if (v_content.kind === 'filetime') s.text(String(v_content.value));
    if (v_content.kind === 'bool') s.text(String(v_content.value));
    if (v_content.kind === 'cy') s.text(String(v_content.value));
    if (v_content.kind === 'error') s.text(String(v_content.value));
    if (v_content.kind === 'clsid') s.text(String(v_content.value));
    if (v_content.kind === '$raw') { s.raw(v_content.value); }
  }
  s.endElement();
}

/** Write a `CT_Vstream`; the caller supplies its element local name. */
export function writeCT_Vstream(s: XmlSink, value: CT_Vstream, ctx: WriteContext, localName: string): void {
  s.startElement(uriFor(ctx, 'doc-props-vt'), localName);
  if (value['version'] !== undefined) s.attr(null, 'version', String(value['version']));
  for (const a of value.$unknownAttrs ?? []) s.attr(a.uri || null, a.localName, a.value);
  s.text(String(value.$value));
  s.endElement();
}
