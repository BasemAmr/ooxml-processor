/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: validators for the dml-wordprocessingDrawing namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  validateCT_BackgroundFormatting,
  validateCT_GraphicalObject,
  validateCT_GroupShapeProperties,
  validateCT_NonVisualConnectorProperties,
  validateCT_NonVisualContentPartProperties,
  validateCT_NonVisualDrawingProps,
  validateCT_NonVisualDrawingShapeProps,
  validateCT_NonVisualGraphicFrameProperties,
  validateCT_NonVisualGroupDrawingShapeProps,
  validateCT_OfficeArtExtensionList,
  validateCT_Point2D,
  validateCT_PositiveSize2D,
  validateCT_ShapeProperties,
  validateCT_ShapeStyle,
  validateCT_TextBodyProperties,
  validateCT_Transform2D,
  validateCT_WholeE2oFormatting,
  validateST_BlackWhiteMode,
  validateST_Coordinate,
} from '../dml-main/validator.js';
import { validateCT_Picture } from '../dml-picture/validator.js';
import { validateCT_OMath, validateCT_OMathPara } from '../math/validator.js';
import { validateST_RelationshipId } from '../relationships/validator.js';
import {
  validateCT_AltChunk,
  validateCT_Bookmark,
  validateCT_CustomXmlBlock,
  validateCT_Markup,
  validateCT_MarkupRange,
  validateCT_MoveBookmark,
  validateCT_P,
  validateCT_Perm,
  validateCT_PermStart,
  validateCT_ProofErr,
  validateCT_RunTrackChange,
  validateCT_SdtBlock,
  validateCT_Tbl,
  validateCT_TrackChange,
} from '../wml/validator.js';
import {
  ST_AlignH_VALUES,
  ST_AlignV_VALUES,
  ST_RelFromH_VALUES,
  ST_RelFromV_VALUES,
  ST_WrapText_VALUES,
} from './types.js';

export type ValidationCode = 'missing-required' | 'invalid-value' | 'facet-violation';
export interface ValidationIssue { readonly path: string; readonly code: ValidationCode; readonly message: string; }
export type ValidationResult = readonly ValidationIssue[];
function issue(path: string, code: ValidationCode, message: string): ValidationIssue { return { path, code, message }; }
function invalidEnum(path: string, value: unknown, values: ReadonlySet<string>): ValidationIssue { return issue(path, 'invalid-value', `value ${String(value)} is not in the enumeration {${[...values].join(', ')}}`); }
const BUILTIN_GROUPS = {
  string: new Set(['xsd:ID', 'xsd:IDREF', 'xsd:NCName', 'xsd:NMTOKEN', 'xsd:anyURI', 'xsd:base64Binary', 'xsd:date', 'xsd:dateTime', 'xsd:duration', 'xsd:hexBinary', 'xsd:language', 'xsd:string', 'xsd:time', 'xsd:token']),
  boolean: new Set(['xsd:boolean']),
  number: new Set(['xsd:byte', 'xsd:decimal', 'xsd:double', 'xsd:float', 'xsd:int', 'xsd:integer', 'xsd:long', 'xsd:nonNegativeInteger', 'xsd:positiveInteger', 'xsd:unsignedByte', 'xsd:unsignedInt', 'xsd:unsignedLong', 'xsd:unsignedShort']),
};
function validBuiltin(value: unknown, name: string): boolean { if (BUILTIN_GROUPS.string.has(name)) return typeof value === 'string'; if (BUILTIN_GROUPS.boolean.has(name)) return typeof value === 'boolean'; if (BUILTIN_GROUPS.number.has(name)) return typeof value === 'number' && Number.isFinite(value); return false; }

export function validateST_AlignH(value: unknown, path = 'ST_AlignH'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_AlignH_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_AlignH_VALUES));
  }
  return issues;
}

export function validateST_AlignV(value: unknown, path = 'ST_AlignV'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_AlignV_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_AlignV_VALUES));
  }
  return issues;
}

export function validateST_PositionOffset(value: unknown, path = 'ST_PositionOffset'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'number' || !Number.isFinite(value)) issues.push(issue(path, 'invalid-value', 'expected a finite number'));
  return issues;
}

export function validateST_RelFromH(value: unknown, path = 'ST_RelFromH'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_RelFromH_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_RelFromH_VALUES));
  }
  return issues;
}

export function validateST_RelFromV(value: unknown, path = 'ST_RelFromV'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_RelFromV_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_RelFromV_VALUES));
  }
  return issues;
}

export function validateST_WrapDistance(value: unknown, path = 'ST_WrapDistance'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'number' || !Number.isFinite(value)) issues.push(issue(path, 'invalid-value', 'expected a finite number'));
  return issues;
}

export function validateST_WrapText(value: unknown, path = 'ST_WrapText'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (typeof value !== 'string') issues.push(issue(path, 'invalid-value', 'expected a string'));
  if (typeof value === 'string' && !ST_WrapText_VALUES.has(value)) {
    issues.push(invalidEnum(path, value, ST_WrapText_VALUES));
  }
  return issues;
}

export function validateCT_Anchor(value: unknown, path = 'CT_Anchor'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['simplePos'] === undefined) {
    issues.push(issue(path + '.simplePos', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_Point2D(v['simplePos'], path + '.simplePos'));
  }
  if (v['positionH'] === undefined) {
    issues.push(issue(path + '.positionH', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_PosH(v['positionH'], path + '.positionH'));
  }
  if (v['positionV'] === undefined) {
    issues.push(issue(path + '.positionV', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_PosV(v['positionV'], path + '.positionV'));
  }
  if (v['extent'] === undefined) {
    issues.push(issue(path + '.extent', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_PositiveSize2D(v['extent'], path + '.extent'));
  }
  if (v['effectExtent'] === undefined) {
  } else {
    issues.push(...validateCT_EffectExtent(v['effectExtent'], path + '.effectExtent'));
  }
  if (v['content'] === undefined) {
    issues.push(issue(path + '.content', 'missing-required', 'required slot is absent'));
  } else {
    if (v['content'] && typeof v['content'] === 'object') {
      const item = v['content'] as { kind?: unknown; value?: unknown };
      if (item.kind !== '$raw') {
        if (item.kind === 'wrapNone') issues.push(...validateCT_WrapNone(item.value, path + '.content' + '.value'));
        if (item.kind === 'wrapSquare') issues.push(...validateCT_WrapSquare(item.value, path + '.content' + '.value'));
        if (item.kind === 'wrapTight') issues.push(...validateCT_WrapTight(item.value, path + '.content' + '.value'));
        if (item.kind === 'wrapThrough') issues.push(...validateCT_WrapThrough(item.value, path + '.content' + '.value'));
        if (item.kind === 'wrapTopAndBottom') issues.push(...validateCT_WrapTopBottom(item.value, path + '.content' + '.value'));
        if ('wrapNone' !== item.kind && 'wrapSquare' !== item.kind && 'wrapTight' !== item.kind && 'wrapThrough' !== item.kind && 'wrapTopAndBottom' !== item.kind) issues.push(issue(path + '.content', 'invalid-value', 'unknown choice alternative'));
      }
    }
  }
  if (v['docPr'] === undefined) {
    issues.push(issue(path + '.docPr', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_NonVisualDrawingProps(v['docPr'], path + '.docPr'));
  }
  if (v['cNvGraphicFramePr'] === undefined) {
  } else {
    issues.push(...validateCT_NonVisualGraphicFrameProperties(v['cNvGraphicFramePr'], path + '.cNvGraphicFramePr'));
  }
  if (v['graphic'] === undefined) {
    issues.push(issue(path + '.graphic', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_GraphicalObject(v['graphic'], path + '.graphic'));
  }
  if (v['distT'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distT'], path + '.distT'));
  }
  if (v['distB'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distB'], path + '.distB'));
  }
  if (v['distL'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distL'], path + '.distL'));
  }
  if (v['distR'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distR'], path + '.distR'));
  }
  if (v['simplePosAttr'] === undefined) {
  } else {
    if (!validBuiltin(v['simplePosAttr'], 'xsd:boolean')) issues.push(issue(path + '.simplePosAttr', 'invalid-value', 'invalid xsd:boolean'));
  }
  if (v['relativeHeight'] === undefined) {
    issues.push(issue(path + '.relativeHeight', 'missing-required', 'required attribute is absent'));
  } else {
    if (!validBuiltin(v['relativeHeight'], 'xsd:unsignedInt')) issues.push(issue(path + '.relativeHeight', 'invalid-value', 'invalid xsd:unsignedInt'));
  }
  if (v['behindDoc'] === undefined) {
    issues.push(issue(path + '.behindDoc', 'missing-required', 'required attribute is absent'));
  } else {
    if (!validBuiltin(v['behindDoc'], 'xsd:boolean')) issues.push(issue(path + '.behindDoc', 'invalid-value', 'invalid xsd:boolean'));
  }
  if (v['locked'] === undefined) {
    issues.push(issue(path + '.locked', 'missing-required', 'required attribute is absent'));
  } else {
    if (!validBuiltin(v['locked'], 'xsd:boolean')) issues.push(issue(path + '.locked', 'invalid-value', 'invalid xsd:boolean'));
  }
  if (v['layoutInCell'] === undefined) {
    issues.push(issue(path + '.layoutInCell', 'missing-required', 'required attribute is absent'));
  } else {
    if (!validBuiltin(v['layoutInCell'], 'xsd:boolean')) issues.push(issue(path + '.layoutInCell', 'invalid-value', 'invalid xsd:boolean'));
  }
  if (v['hidden'] === undefined) {
  } else {
    if (!validBuiltin(v['hidden'], 'xsd:boolean')) issues.push(issue(path + '.hidden', 'invalid-value', 'invalid xsd:boolean'));
  }
  if (v['allowOverlap'] === undefined) {
    issues.push(issue(path + '.allowOverlap', 'missing-required', 'required attribute is absent'));
  } else {
    if (!validBuiltin(v['allowOverlap'], 'xsd:boolean')) issues.push(issue(path + '.allowOverlap', 'invalid-value', 'invalid xsd:boolean'));
  }
  return issues;
}

export function validateCT_EffectExtent(value: unknown, path = 'CT_EffectExtent'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['l'] === undefined) {
    issues.push(issue(path + '.l', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_Coordinate(v['l'], path + '.l'));
  }
  if (v['t'] === undefined) {
    issues.push(issue(path + '.t', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_Coordinate(v['t'], path + '.t'));
  }
  if (v['r'] === undefined) {
    issues.push(issue(path + '.r', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_Coordinate(v['r'], path + '.r'));
  }
  if (v['b'] === undefined) {
    issues.push(issue(path + '.b', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_Coordinate(v['b'], path + '.b'));
  }
  return issues;
}

export function validateCT_GraphicFrame(value: unknown, path = 'CT_GraphicFrame'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['cNvPr'] === undefined) {
    issues.push(issue(path + '.cNvPr', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_NonVisualDrawingProps(v['cNvPr'], path + '.cNvPr'));
  }
  if (v['cNvFrPr'] === undefined) {
    issues.push(issue(path + '.cNvFrPr', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_NonVisualGraphicFrameProperties(v['cNvFrPr'], path + '.cNvFrPr'));
  }
  if (v['xfrm'] === undefined) {
    issues.push(issue(path + '.xfrm', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_Transform2D(v['xfrm'], path + '.xfrm'));
  }
  if (v['graphic'] === undefined) {
    issues.push(issue(path + '.graphic', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_GraphicalObject(v['graphic'], path + '.graphic'));
  }
  if (v['extLst'] === undefined) {
  } else {
    issues.push(...validateCT_OfficeArtExtensionList(v['extLst'], path + '.extLst'));
  }
  return issues;
}

export function validateCT_Inline(value: unknown, path = 'CT_Inline'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['extent'] === undefined) {
    issues.push(issue(path + '.extent', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_PositiveSize2D(v['extent'], path + '.extent'));
  }
  if (v['effectExtent'] === undefined) {
  } else {
    issues.push(...validateCT_EffectExtent(v['effectExtent'], path + '.effectExtent'));
  }
  if (v['docPr'] === undefined) {
    issues.push(issue(path + '.docPr', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_NonVisualDrawingProps(v['docPr'], path + '.docPr'));
  }
  if (v['cNvGraphicFramePr'] === undefined) {
  } else {
    issues.push(...validateCT_NonVisualGraphicFrameProperties(v['cNvGraphicFramePr'], path + '.cNvGraphicFramePr'));
  }
  if (v['graphic'] === undefined) {
    issues.push(issue(path + '.graphic', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_GraphicalObject(v['graphic'], path + '.graphic'));
  }
  if (v['distT'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distT'], path + '.distT'));
  }
  if (v['distB'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distB'], path + '.distB'));
  }
  if (v['distL'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distL'], path + '.distL'));
  }
  if (v['distR'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distR'], path + '.distR'));
  }
  return issues;
}

export function validateCT_LinkedTextboxInformation(value: unknown, path = 'CT_LinkedTextboxInformation'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['extLst'] === undefined) {
  } else {
    issues.push(...validateCT_OfficeArtExtensionList(v['extLst'], path + '.extLst'));
  }
  if (v['id'] === undefined) {
    issues.push(issue(path + '.id', 'missing-required', 'required attribute is absent'));
  } else {
    if (!validBuiltin(v['id'], 'xsd:unsignedShort')) issues.push(issue(path + '.id', 'invalid-value', 'invalid xsd:unsignedShort'));
  }
  if (v['seq'] === undefined) {
    issues.push(issue(path + '.seq', 'missing-required', 'required attribute is absent'));
  } else {
    if (!validBuiltin(v['seq'], 'xsd:unsignedShort')) issues.push(issue(path + '.seq', 'invalid-value', 'invalid xsd:unsignedShort'));
  }
  return issues;
}

export function validateCT_PosH(value: unknown, path = 'CT_PosH'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['content'] === undefined) {
    issues.push(issue(path + '.content', 'missing-required', 'required slot is absent'));
  } else {
    if (v['content'] && typeof v['content'] === 'object') {
      const item = v['content'] as { kind?: unknown; value?: unknown };
      if (item.kind !== '$raw') {
        if (item.kind === 'align') issues.push(...validateST_AlignH(item.value, path + '.content' + '.value'));
        if (item.kind === 'posOffset') issues.push(...validateST_PositionOffset(item.value, path + '.content' + '.value'));
        if ('align' !== item.kind && 'posOffset' !== item.kind) issues.push(issue(path + '.content', 'invalid-value', 'unknown choice alternative'));
      }
    }
  }
  if (v['relativeFrom'] === undefined) {
    issues.push(issue(path + '.relativeFrom', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_RelFromH(v['relativeFrom'], path + '.relativeFrom'));
  }
  return issues;
}

export function validateCT_PosV(value: unknown, path = 'CT_PosV'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['content'] === undefined) {
    issues.push(issue(path + '.content', 'missing-required', 'required slot is absent'));
  } else {
    if (v['content'] && typeof v['content'] === 'object') {
      const item = v['content'] as { kind?: unknown; value?: unknown };
      if (item.kind !== '$raw') {
        if (item.kind === 'align') issues.push(...validateST_AlignV(item.value, path + '.content' + '.value'));
        if (item.kind === 'posOffset') issues.push(...validateST_PositionOffset(item.value, path + '.content' + '.value'));
        if ('align' !== item.kind && 'posOffset' !== item.kind) issues.push(issue(path + '.content', 'invalid-value', 'unknown choice alternative'));
      }
    }
  }
  if (v['relativeFrom'] === undefined) {
    issues.push(issue(path + '.relativeFrom', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_RelFromV(v['relativeFrom'], path + '.relativeFrom'));
  }
  return issues;
}

export function validateCT_TextboxInfo(value: unknown, path = 'CT_TextboxInfo'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['txbxContent'] === undefined) {
    issues.push(issue(path + '.txbxContent', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_TxbxContent(v['txbxContent'], path + '.txbxContent'));
  }
  if (v['extLst'] === undefined) {
  } else {
    issues.push(...validateCT_OfficeArtExtensionList(v['extLst'], path + '.extLst'));
  }
  if (v['id'] === undefined) {
  } else {
    if (!validBuiltin(v['id'], 'xsd:unsignedShort')) issues.push(issue(path + '.id', 'invalid-value', 'invalid xsd:unsignedShort'));
  }
  return issues;
}

export function validateCT_TxbxContent(value: unknown, path = 'CT_TxbxContent'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (!Array.isArray(v['blockLevelElts'])) {
  } else {
    for (let i = 0; i < (v['blockLevelElts'] as readonly unknown[]).length; i += 1) {
      if ((v['blockLevelElts'] as readonly unknown[])[i] && typeof (v['blockLevelElts'] as readonly unknown[])[i] === 'object') {
        const item = (v['blockLevelElts'] as readonly unknown[])[i] as { kind?: unknown; value?: unknown };
        if (item.kind !== '$raw') {
          if (item.kind === 'customXml') issues.push(...validateCT_CustomXmlBlock(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'sdt') issues.push(...validateCT_SdtBlock(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'p') issues.push(...validateCT_P(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'tbl') issues.push(...validateCT_Tbl(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'proofErr') issues.push(...validateCT_ProofErr(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'permStart') issues.push(...validateCT_PermStart(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'permEnd') issues.push(...validateCT_Perm(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'bookmarkStart') issues.push(...validateCT_Bookmark(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'bookmarkEnd') issues.push(...validateCT_MarkupRange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'moveFromRangeStart') issues.push(...validateCT_MoveBookmark(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'moveFromRangeEnd') issues.push(...validateCT_MarkupRange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'moveToRangeStart') issues.push(...validateCT_MoveBookmark(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'moveToRangeEnd') issues.push(...validateCT_MarkupRange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'commentRangeStart') issues.push(...validateCT_MarkupRange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'commentRangeEnd') issues.push(...validateCT_MarkupRange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'customXmlInsRangeStart') issues.push(...validateCT_TrackChange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'customXmlInsRangeEnd') issues.push(...validateCT_Markup(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'customXmlDelRangeStart') issues.push(...validateCT_TrackChange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'customXmlDelRangeEnd') issues.push(...validateCT_Markup(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'customXmlMoveFromRangeStart') issues.push(...validateCT_TrackChange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'customXmlMoveFromRangeEnd') issues.push(...validateCT_Markup(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'customXmlMoveToRangeStart') issues.push(...validateCT_TrackChange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'customXmlMoveToRangeEnd') issues.push(...validateCT_Markup(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'ins') issues.push(...validateCT_RunTrackChange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'del') issues.push(...validateCT_RunTrackChange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'moveFrom') issues.push(...validateCT_RunTrackChange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'moveTo') issues.push(...validateCT_RunTrackChange(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'oMathPara') issues.push(...validateCT_OMathPara(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'oMath') issues.push(...validateCT_OMath(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if (item.kind === 'altChunk') issues.push(...validateCT_AltChunk(item.value, path + '.blockLevelElts[' + i + ']' + '.value'));
          if ('customXml' !== item.kind && 'sdt' !== item.kind && 'p' !== item.kind && 'tbl' !== item.kind && 'proofErr' !== item.kind && 'permStart' !== item.kind && 'permEnd' !== item.kind && 'bookmarkStart' !== item.kind && 'bookmarkEnd' !== item.kind && 'moveFromRangeStart' !== item.kind && 'moveFromRangeEnd' !== item.kind && 'moveToRangeStart' !== item.kind && 'moveToRangeEnd' !== item.kind && 'commentRangeStart' !== item.kind && 'commentRangeEnd' !== item.kind && 'customXmlInsRangeStart' !== item.kind && 'customXmlInsRangeEnd' !== item.kind && 'customXmlDelRangeStart' !== item.kind && 'customXmlDelRangeEnd' !== item.kind && 'customXmlMoveFromRangeStart' !== item.kind && 'customXmlMoveFromRangeEnd' !== item.kind && 'customXmlMoveToRangeStart' !== item.kind && 'customXmlMoveToRangeEnd' !== item.kind && 'ins' !== item.kind && 'del' !== item.kind && 'moveFrom' !== item.kind && 'moveTo' !== item.kind && 'oMathPara' !== item.kind && 'oMath' !== item.kind && 'altChunk' !== item.kind) issues.push(issue(path + '.blockLevelElts[' + i + ']', 'invalid-value', 'unknown choice alternative'));
        }
      }
    }
  }
  return issues;
}

export function validateCT_WordprocessingCanvas(value: unknown, path = 'CT_WordprocessingCanvas'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['bg'] === undefined) {
  } else {
    issues.push(...validateCT_BackgroundFormatting(v['bg'], path + '.bg'));
  }
  if (v['whole'] === undefined) {
  } else {
    issues.push(...validateCT_WholeE2oFormatting(v['whole'], path + '.whole'));
  }
  if (!Array.isArray(v['content'])) {
  } else {
    for (let i = 0; i < (v['content'] as readonly unknown[]).length; i += 1) {
      if ((v['content'] as readonly unknown[])[i] && typeof (v['content'] as readonly unknown[])[i] === 'object') {
        const item = (v['content'] as readonly unknown[])[i] as { kind?: unknown; value?: unknown };
        if (item.kind !== '$raw') {
          if (item.kind === 'wsp') issues.push(...validateCT_WordprocessingShape(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'pic') issues.push(...validateCT_Picture(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'contentPart') issues.push(...validateCT_WordprocessingContentPart(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'wgp') issues.push(...validateCT_WordprocessingGroup(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'graphicFrame') issues.push(...validateCT_GraphicFrame(item.value, path + '.content[' + i + ']' + '.value'));
          if ('wsp' !== item.kind && 'pic' !== item.kind && 'contentPart' !== item.kind && 'wgp' !== item.kind && 'graphicFrame' !== item.kind) issues.push(issue(path + '.content[' + i + ']', 'invalid-value', 'unknown choice alternative'));
        }
      }
    }
  }
  if (v['extLst'] === undefined) {
  } else {
    issues.push(...validateCT_OfficeArtExtensionList(v['extLst'], path + '.extLst'));
  }
  return issues;
}

export function validateCT_WordprocessingContentPart(value: unknown, path = 'CT_WordprocessingContentPart'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['nvContentPartPr'] === undefined) {
  } else {
    issues.push(...validateCT_WordprocessingContentPartNonVisual(v['nvContentPartPr'], path + '.nvContentPartPr'));
  }
  if (v['xfrm'] === undefined) {
  } else {
    issues.push(...validateCT_Transform2D(v['xfrm'], path + '.xfrm'));
  }
  if (v['extLst'] === undefined) {
  } else {
    issues.push(...validateCT_OfficeArtExtensionList(v['extLst'], path + '.extLst'));
  }
  if (v['bwMode'] === undefined) {
  } else {
    issues.push(...validateST_BlackWhiteMode(v['bwMode'], path + '.bwMode'));
  }
  if (v['id'] === undefined) {
    issues.push(issue(path + '.id', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_RelationshipId(v['id'], path + '.id'));
  }
  return issues;
}

export function validateCT_WordprocessingContentPartNonVisual(value: unknown, path = 'CT_WordprocessingContentPartNonVisual'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['cNvPr'] === undefined) {
  } else {
    issues.push(...validateCT_NonVisualDrawingProps(v['cNvPr'], path + '.cNvPr'));
  }
  if (v['cNvContentPartPr'] === undefined) {
  } else {
    issues.push(...validateCT_NonVisualContentPartProperties(v['cNvContentPartPr'], path + '.cNvContentPartPr'));
  }
  return issues;
}

export function validateCT_WordprocessingGroup(value: unknown, path = 'CT_WordprocessingGroup'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['cNvPr'] === undefined) {
  } else {
    issues.push(...validateCT_NonVisualDrawingProps(v['cNvPr'], path + '.cNvPr'));
  }
  if (v['cNvGrpSpPr'] === undefined) {
    issues.push(issue(path + '.cNvGrpSpPr', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_NonVisualGroupDrawingShapeProps(v['cNvGrpSpPr'], path + '.cNvGrpSpPr'));
  }
  if (v['grpSpPr'] === undefined) {
    issues.push(issue(path + '.grpSpPr', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_GroupShapeProperties(v['grpSpPr'], path + '.grpSpPr'));
  }
  if (!Array.isArray(v['content'])) {
  } else {
    for (let i = 0; i < (v['content'] as readonly unknown[]).length; i += 1) {
      if ((v['content'] as readonly unknown[])[i] && typeof (v['content'] as readonly unknown[])[i] === 'object') {
        const item = (v['content'] as readonly unknown[])[i] as { kind?: unknown; value?: unknown };
        if (item.kind !== '$raw') {
          if (item.kind === 'wsp') issues.push(...validateCT_WordprocessingShape(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'grpSp') issues.push(...validateCT_WordprocessingGroup(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'graphicFrame') issues.push(...validateCT_GraphicFrame(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'pic') issues.push(...validateCT_Picture(item.value, path + '.content[' + i + ']' + '.value'));
          if (item.kind === 'contentPart') issues.push(...validateCT_WordprocessingContentPart(item.value, path + '.content[' + i + ']' + '.value'));
          if ('wsp' !== item.kind && 'grpSp' !== item.kind && 'graphicFrame' !== item.kind && 'pic' !== item.kind && 'contentPart' !== item.kind) issues.push(issue(path + '.content[' + i + ']', 'invalid-value', 'unknown choice alternative'));
        }
      }
    }
  }
  if (v['extLst'] === undefined) {
  } else {
    issues.push(...validateCT_OfficeArtExtensionList(v['extLst'], path + '.extLst'));
  }
  return issues;
}

export function validateCT_WordprocessingShape(value: unknown, path = 'CT_WordprocessingShape'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['cNvPr'] === undefined) {
  } else {
    issues.push(...validateCT_NonVisualDrawingProps(v['cNvPr'], path + '.cNvPr'));
  }
  if (v['content'] === undefined) {
    issues.push(issue(path + '.content', 'missing-required', 'required slot is absent'));
  } else {
    if (v['content'] && typeof v['content'] === 'object') {
      const item = v['content'] as { kind?: unknown; value?: unknown };
      if (item.kind !== '$raw') {
        if (item.kind === 'cNvSpPr') issues.push(...validateCT_NonVisualDrawingShapeProps(item.value, path + '.content' + '.value'));
        if (item.kind === 'cNvCnPr') issues.push(...validateCT_NonVisualConnectorProperties(item.value, path + '.content' + '.value'));
        if ('cNvSpPr' !== item.kind && 'cNvCnPr' !== item.kind) issues.push(issue(path + '.content', 'invalid-value', 'unknown choice alternative'));
      }
    }
  }
  if (v['spPr'] === undefined) {
    issues.push(issue(path + '.spPr', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_ShapeProperties(v['spPr'], path + '.spPr'));
  }
  if (v['style'] === undefined) {
  } else {
    issues.push(...validateCT_ShapeStyle(v['style'], path + '.style'));
  }
  if (v['extLst'] === undefined) {
  } else {
    issues.push(...validateCT_OfficeArtExtensionList(v['extLst'], path + '.extLst'));
  }
  if (v['content2'] === undefined) {
  } else {
    if (v['content2'] && typeof v['content2'] === 'object') {
      const item = v['content2'] as { kind?: unknown; value?: unknown };
      if (item.kind !== '$raw') {
        if (item.kind === 'txbx') issues.push(...validateCT_TextboxInfo(item.value, path + '.content2' + '.value'));
        if (item.kind === 'linkedTxbx') issues.push(...validateCT_LinkedTextboxInformation(item.value, path + '.content2' + '.value'));
        if ('txbx' !== item.kind && 'linkedTxbx' !== item.kind) issues.push(issue(path + '.content2', 'invalid-value', 'unknown choice alternative'));
      }
    }
  }
  if (v['bodyPr'] === undefined) {
    issues.push(issue(path + '.bodyPr', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_TextBodyProperties(v['bodyPr'], path + '.bodyPr'));
  }
  if (v['normalEastAsianFlow'] === undefined) {
  } else {
    if (!validBuiltin(v['normalEastAsianFlow'], 'xsd:boolean')) issues.push(issue(path + '.normalEastAsianFlow', 'invalid-value', 'invalid xsd:boolean'));
  }
  return issues;
}

export function validateCT_WrapNone(value: unknown, path = 'CT_WrapNone'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  return issues;
}

export function validateCT_WrapPath(value: unknown, path = 'CT_WrapPath'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['start'] === undefined) {
    issues.push(issue(path + '.start', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_Point2D(v['start'], path + '.start'));
  }
  if (!Array.isArray(v['lineTo'])) {
  } else {
    for (let i = 0; i < (v['lineTo'] as readonly unknown[]).length; i += 1) {
      issues.push(...validateCT_Point2D((v['lineTo'] as readonly unknown[])[i], path + '.lineTo[' + i + ']'));
    }
  }
  if (v['edited'] === undefined) {
  } else {
    if (!validBuiltin(v['edited'], 'xsd:boolean')) issues.push(issue(path + '.edited', 'invalid-value', 'invalid xsd:boolean'));
  }
  return issues;
}

export function validateCT_WrapSquare(value: unknown, path = 'CT_WrapSquare'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['effectExtent'] === undefined) {
  } else {
    issues.push(...validateCT_EffectExtent(v['effectExtent'], path + '.effectExtent'));
  }
  if (v['wrapText'] === undefined) {
    issues.push(issue(path + '.wrapText', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_WrapText(v['wrapText'], path + '.wrapText'));
  }
  if (v['distT'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distT'], path + '.distT'));
  }
  if (v['distB'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distB'], path + '.distB'));
  }
  if (v['distL'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distL'], path + '.distL'));
  }
  if (v['distR'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distR'], path + '.distR'));
  }
  return issues;
}

export function validateCT_WrapThrough(value: unknown, path = 'CT_WrapThrough'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['wrapPolygon'] === undefined) {
    issues.push(issue(path + '.wrapPolygon', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_WrapPath(v['wrapPolygon'], path + '.wrapPolygon'));
  }
  if (v['wrapText'] === undefined) {
    issues.push(issue(path + '.wrapText', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_WrapText(v['wrapText'], path + '.wrapText'));
  }
  if (v['distL'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distL'], path + '.distL'));
  }
  if (v['distR'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distR'], path + '.distR'));
  }
  return issues;
}

export function validateCT_WrapTight(value: unknown, path = 'CT_WrapTight'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['wrapPolygon'] === undefined) {
    issues.push(issue(path + '.wrapPolygon', 'missing-required', 'required slot is absent'));
  } else {
    issues.push(...validateCT_WrapPath(v['wrapPolygon'], path + '.wrapPolygon'));
  }
  if (v['wrapText'] === undefined) {
    issues.push(issue(path + '.wrapText', 'missing-required', 'required attribute is absent'));
  } else {
    issues.push(...validateST_WrapText(v['wrapText'], path + '.wrapText'));
  }
  if (v['distL'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distL'], path + '.distL'));
  }
  if (v['distR'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distR'], path + '.distR'));
  }
  return issues;
}

export function validateCT_WrapTopBottom(value: unknown, path = 'CT_WrapTopBottom'): ValidationResult {
  const issues: ValidationIssue[] = [];
  if (value === null || typeof value !== 'object') return [issue(path, 'invalid-value', 'expected an object')];
  const v = value as Record<string, unknown>;
  if (v['effectExtent'] === undefined) {
  } else {
    issues.push(...validateCT_EffectExtent(v['effectExtent'], path + '.effectExtent'));
  }
  if (v['distT'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distT'], path + '.distT'));
  }
  if (v['distB'] === undefined) {
  } else {
    issues.push(...validateST_WrapDistance(v['distB'], path + '.distB'));
  }
  return issues;
}
