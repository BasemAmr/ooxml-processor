/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the dml-wordprocessingDrawing namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  collapse,
  isIgnorableWhitespace,
  parseInteger,
  parseXsdBoolean,
  readScalar,
  requireStart,
} from '../../runtime/index.js';
import {
  parseST_BlackWhiteMode,
  parseST_Coordinate,
  readCT_BackgroundFormatting,
  readCT_GraphicalObject,
  readCT_GroupShapeProperties,
  readCT_NonVisualConnectorProperties,
  readCT_NonVisualContentPartProperties,
  readCT_NonVisualDrawingProps,
  readCT_NonVisualDrawingShapeProps,
  readCT_NonVisualGraphicFrameProperties,
  readCT_NonVisualGroupDrawingShapeProps,
  readCT_OfficeArtExtensionList,
  readCT_Point2D,
  readCT_PositiveSize2D,
  readCT_ShapeProperties,
  readCT_ShapeStyle,
  readCT_TextBodyProperties,
  readCT_Transform2D,
  readCT_WholeE2oFormatting,
} from '../dml-main/reader.js';
import type {
  CT_BackgroundFormatting,
  CT_GraphicalObject,
  CT_GroupShapeProperties,
  CT_NonVisualContentPartProperties,
  CT_NonVisualDrawingProps,
  CT_NonVisualGraphicFrameProperties,
  CT_NonVisualGroupDrawingShapeProps,
  CT_OfficeArtExtensionList,
  CT_Point2D,
  CT_PositiveSize2D,
  CT_ShapeProperties,
  CT_ShapeStyle,
  CT_TextBodyProperties,
  CT_Transform2D,
  CT_WholeE2oFormatting,
  ST_BlackWhiteMode,
  ST_Coordinate,
} from '../dml-main/types.js';
import { readCT_Picture } from '../dml-picture/reader.js';
import { readCT_OMath, readCT_OMathPara } from '../math/reader.js';
import { parseST_RelationshipId } from '../relationships/reader.js';
import type { ST_RelationshipId } from '../relationships/types.js';
import {
  readCT_AltChunk,
  readCT_Bookmark,
  readCT_CustomXmlBlock,
  readCT_Markup,
  readCT_MarkupRange,
  readCT_MoveBookmark,
  readCT_P,
  readCT_Perm,
  readCT_PermStart,
  readCT_ProofErr,
  readCT_RunTrackChange,
  readCT_SdtBlock,
  readCT_Tbl,
  readCT_TrackChange,
} from '../wml/reader.js';
import {
  type CT_Anchor,
  type CT_Anchor_Content,
  type CT_EffectExtent,
  type CT_GraphicFrame,
  type CT_Inline,
  type CT_LinkedTextboxInformation,
  type CT_PosH,
  type CT_PosH_Content,
  type CT_PosV,
  type CT_PosV_Content,
  type CT_TextboxInfo,
  type CT_TxbxContent,
  type CT_TxbxContent_BlockLevelElts,
  type CT_WordprocessingCanvas,
  type CT_WordprocessingCanvas_Content,
  type CT_WordprocessingContentPart,
  type CT_WordprocessingContentPartNonVisual,
  type CT_WordprocessingGroup,
  type CT_WordprocessingGroup_Content,
  type CT_WordprocessingShape,
  type CT_WordprocessingShape_Content,
  type CT_WordprocessingShape_Content2,
  type CT_WrapNone,
  type CT_WrapPath,
  type CT_WrapSquare,
  type CT_WrapThrough,
  type CT_WrapTight,
  type CT_WrapTopBottom,
  type ST_AlignH,
  ST_AlignH_VALUES,
  type ST_AlignV,
  ST_AlignV_VALUES,
  type ST_PositionOffset,
  type ST_RelFromH,
  ST_RelFromH_VALUES,
  type ST_RelFromV,
  ST_RelFromV_VALUES,
  type ST_WrapDistance,
  type ST_WrapText,
  ST_WrapText_VALUES,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_AlignH(value: string): ST_AlignH | undefined {
  const s = collapse(value);
  return ST_AlignH_VALUES.has(s) ? (s as ST_AlignH) : undefined;
}

export function parseST_AlignV(value: string): ST_AlignV | undefined {
  const s = collapse(value);
  return ST_AlignV_VALUES.has(s) ? (s as ST_AlignV) : undefined;
}

export function parseST_PositionOffset(value: string): ST_PositionOffset | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_PositionOffset);
}

export function parseST_RelFromH(value: string): ST_RelFromH | undefined {
  const s = collapse(value);
  return ST_RelFromH_VALUES.has(s) ? (s as ST_RelFromH) : undefined;
}

export function parseST_RelFromV(value: string): ST_RelFromV | undefined {
  const s = collapse(value);
  return ST_RelFromV_VALUES.has(s) ? (s as ST_RelFromV) : undefined;
}

export function parseST_WrapDistance(value: string): ST_WrapDistance | undefined {
  const n = parseInteger(value);
  return n === undefined ? undefined : (n as ST_WrapDistance);
}

export function parseST_WrapText(value: string): ST_WrapText | undefined {
  const s = collapse(value);
  return ST_WrapText_VALUES.has(s) ? (s as ST_WrapText) : undefined;
}

// Complex-type readers.

/** Read a `CT_Anchor`. See `transitional/dml-wordprocessingDrawing.xsd:164`. */
export function readCT_Anchor(cur: XmlCursor, ctx: ReadContext): CT_Anchor {
  const start = requireStart(cur, 'CT_Anchor');
  let distT: ST_WrapDistance | undefined;
  let distB: ST_WrapDistance | undefined;
  let distL: ST_WrapDistance | undefined;
  let distR: ST_WrapDistance | undefined;
  let simplePosAttr: boolean | undefined;
  let relativeHeight: number | undefined;
  let behindDoc: boolean | undefined;
  let locked: boolean | undefined;
  let layoutInCell: boolean | undefined;
  let hidden: boolean | undefined;
  let allowOverlap: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'distT': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distT = v;
            continue;
          }
          ctx.invalidValue('CT_Anchor', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'distB': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distB = v;
            continue;
          }
          ctx.invalidValue('CT_Anchor', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'distL': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distL = v;
            continue;
          }
          ctx.invalidValue('CT_Anchor', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'distR': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distR = v;
            continue;
          }
          ctx.invalidValue('CT_Anchor', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'simplePos': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            simplePosAttr = v;
            continue;
          }
          ctx.invalidValue('CT_Anchor', a, 'xsd:boolean', cur);
          break;
        }
        case 'relativeHeight': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            relativeHeight = v;
            continue;
          }
          ctx.invalidValue('CT_Anchor', a, 'xsd:unsignedInt', cur);
          break;
        }
        case 'behindDoc': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            behindDoc = v;
            continue;
          }
          ctx.invalidValue('CT_Anchor', a, 'xsd:boolean', cur);
          break;
        }
        case 'locked': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            locked = v;
            continue;
          }
          ctx.invalidValue('CT_Anchor', a, 'xsd:boolean', cur);
          break;
        }
        case 'layoutInCell': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            layoutInCell = v;
            continue;
          }
          ctx.invalidValue('CT_Anchor', a, 'xsd:boolean', cur);
          break;
        }
        case 'hidden': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            hidden = v;
            continue;
          }
          ctx.invalidValue('CT_Anchor', a, 'xsd:boolean', cur);
          break;
        }
        case 'allowOverlap': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            allowOverlap = v;
            continue;
          }
          ctx.invalidValue('CT_Anchor', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Anchor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (relativeHeight === undefined) ctx.missingRequired('CT_Anchor', 'relativeHeight', cur);
  if (behindDoc === undefined) ctx.missingRequired('CT_Anchor', 'behindDoc', cur);
  if (locked === undefined) ctx.missingRequired('CT_Anchor', 'locked', cur);
  if (layoutInCell === undefined) ctx.missingRequired('CT_Anchor', 'layoutInCell', cur);
  if (allowOverlap === undefined) ctx.missingRequired('CT_Anchor', 'allowOverlap', cur);

  let simplePos: CT_Point2D | undefined;
  let positionH: CT_PosH | undefined;
  let positionV: CT_PosV | undefined;
  let extent: CT_PositiveSize2D | undefined;
  let effectExtent: CT_EffectExtent | undefined;
  let content: CT_Anchor_Content | undefined;
  let docPr: CT_NonVisualDrawingProps | undefined;
  let cNvGraphicFramePr: CT_NonVisualGraphicFrameProperties | undefined;
  let graphic: CT_GraphicalObject | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Anchor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'graphic': {
            graphic = readCT_GraphicalObject(cur, ctx);
            $slot = 8;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'simplePos': {
            simplePos = readCT_Point2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'positionH': {
            positionH = readCT_PosH(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'positionV': {
            positionV = readCT_PosV(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extent': {
            extent = readCT_PositiveSize2D(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'effectExtent': {
            effectExtent = readCT_EffectExtent(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'wrapNone': {
            content = { kind: 'wrapNone', value: readCT_WrapNone(cur, ctx) };
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'wrapSquare': {
            content = { kind: 'wrapSquare', value: readCT_WrapSquare(cur, ctx) };
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'wrapTight': {
            content = { kind: 'wrapTight', value: readCT_WrapTight(cur, ctx) };
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'wrapThrough': {
            content = { kind: 'wrapThrough', value: readCT_WrapThrough(cur, ctx) };
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'wrapTopAndBottom': {
            content = { kind: 'wrapTopAndBottom', value: readCT_WrapTopBottom(cur, ctx) };
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'docPr': {
            docPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
          case 'cNvGraphicFramePr': {
            cNvGraphicFramePr = readCT_NonVisualGraphicFrameProperties(cur, ctx);
            $slot = 7;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Anchor', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Anchor', cur);
    cur.next();
  }

  return {
    simplePos,
    positionH,
    positionV,
    extent,
    effectExtent,
    content,
    docPr,
    cNvGraphicFramePr,
    graphic,
    distT,
    distB,
    distL,
    distR,
    simplePosAttr,
    relativeHeight,
    behindDoc,
    locked,
    layoutInCell,
    hidden,
    allowOverlap,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_EffectExtent`. See `transitional/dml-wordprocessingDrawing.xsd:18`. */
export function readCT_EffectExtent(cur: XmlCursor, ctx: ReadContext): CT_EffectExtent {
  const start = requireStart(cur, 'CT_EffectExtent');
  let l: ST_Coordinate | undefined;
  let t: ST_Coordinate | undefined;
  let r: ST_Coordinate | undefined;
  let b: ST_Coordinate | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'l': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            l = v;
            continue;
          }
          ctx.invalidValue('CT_EffectExtent', a, 'ST_Coordinate', cur);
          break;
        }
        case 't': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            t = v;
            continue;
          }
          ctx.invalidValue('CT_EffectExtent', a, 'ST_Coordinate', cur);
          break;
        }
        case 'r': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            r = v;
            continue;
          }
          ctx.invalidValue('CT_EffectExtent', a, 'ST_Coordinate', cur);
          break;
        }
        case 'b': {
          const v = parseST_Coordinate(a.value);
          if (v !== undefined) {
            b = v;
            continue;
          }
          ctx.invalidValue('CT_EffectExtent', a, 'ST_Coordinate', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_EffectExtent', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (l === undefined) ctx.missingRequired('CT_EffectExtent', 'l', cur);
  if (t === undefined) ctx.missingRequired('CT_EffectExtent', 't', cur);
  if (r === undefined) ctx.missingRequired('CT_EffectExtent', 'r', cur);
  if (b === undefined) ctx.missingRequired('CT_EffectExtent', 'b', cur);

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_EffectExtent', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_EffectExtent', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_EffectExtent', cur);
    cur.next();
  }
  return {
    l,
    t,
    r,
    b,
    $unknownAttrs,
  };
}

/** Read a `CT_GraphicFrame`. See `transitional/dml-wordprocessingDrawing.xsd:227`. */
export function readCT_GraphicFrame(cur: XmlCursor, ctx: ReadContext): CT_GraphicFrame {
  const start = requireStart(cur, 'CT_GraphicFrame');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GraphicFrame', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvFrPr: CT_NonVisualGraphicFrameProperties | undefined;
  let xfrm: CT_Transform2D | undefined;
  let graphic: CT_GraphicalObject | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GraphicFrame', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'graphic': {
            graphic = readCT_GraphicalObject(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvFrPr': {
            cNvFrPr = readCT_NonVisualGraphicFrameProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'xfrm': {
            xfrm = readCT_Transform2D(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GraphicFrame', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GraphicFrame', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvFrPr,
    xfrm,
    graphic,
    extLst,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Inline`. See `transitional/dml-wordprocessingDrawing.xsd:27`. */
export function readCT_Inline(cur: XmlCursor, ctx: ReadContext): CT_Inline {
  const start = requireStart(cur, 'CT_Inline');
  let distT: ST_WrapDistance | undefined;
  let distB: ST_WrapDistance | undefined;
  let distL: ST_WrapDistance | undefined;
  let distR: ST_WrapDistance | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'distT': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distT = v;
            continue;
          }
          ctx.invalidValue('CT_Inline', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'distB': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distB = v;
            continue;
          }
          ctx.invalidValue('CT_Inline', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'distL': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distL = v;
            continue;
          }
          ctx.invalidValue('CT_Inline', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'distR': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distR = v;
            continue;
          }
          ctx.invalidValue('CT_Inline', a, 'ST_WrapDistance', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Inline', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let extent: CT_PositiveSize2D | undefined;
  let effectExtent: CT_EffectExtent | undefined;
  let docPr: CT_NonVisualDrawingProps | undefined;
  let cNvGraphicFramePr: CT_NonVisualGraphicFrameProperties | undefined;
  let graphic: CT_GraphicalObject | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_main = ctx.uris['dml-main'];
  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Inline', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'graphic': {
            graphic = readCT_GraphicalObject(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'extent': {
            extent = readCT_PositiveSize2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'effectExtent': {
            effectExtent = readCT_EffectExtent(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'docPr': {
            docPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'cNvGraphicFramePr': {
            cNvGraphicFramePr = readCT_NonVisualGraphicFrameProperties(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Inline', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Inline', cur);
    cur.next();
  }

  return {
    extent,
    effectExtent,
    docPr,
    cNvGraphicFramePr,
    graphic,
    distT,
    distB,
    distL,
    distR,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_LinkedTextboxInformation`. See `transitional/dml-wordprocessingDrawing.xsd:199`. */
export function readCT_LinkedTextboxInformation(cur: XmlCursor, ctx: ReadContext): CT_LinkedTextboxInformation {
  const start = requireStart(cur, 'CT_LinkedTextboxInformation');
  let id: number | undefined;
  let seq: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_LinkedTextboxInformation', a, 'xsd:unsignedShort', cur);
          break;
        }
        case 'seq': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            seq = v;
            continue;
          }
          ctx.invalidValue('CT_LinkedTextboxInformation', a, 'xsd:unsignedShort', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_LinkedTextboxInformation', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_LinkedTextboxInformation', 'id', cur);
  if (seq === undefined) ctx.missingRequired('CT_LinkedTextboxInformation', 'seq', cur);

  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_LinkedTextboxInformation', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_LinkedTextboxInformation', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_LinkedTextboxInformation', cur);
    cur.next();
  }

  return {
    extLst,
    id,
    seq,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PosH`. See `transitional/dml-wordprocessingDrawing.xsd:125`. */
export function readCT_PosH(cur: XmlCursor, ctx: ReadContext): CT_PosH {
  const start = requireStart(cur, 'CT_PosH');
  let relativeFrom: ST_RelFromH | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'relativeFrom': {
          const v = parseST_RelFromH(a.value);
          if (v !== undefined) {
            relativeFrom = v;
            continue;
          }
          ctx.invalidValue('CT_PosH', a, 'ST_RelFromH', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PosH', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (relativeFrom === undefined) ctx.missingRequired('CT_PosH', 'relativeFrom', cur);

  let content: CT_PosH_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PosH', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'align': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_AlignH($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_PosH', 'align', 'ST_AlignH', $s, cur);
            content = { kind: 'align', value: ($pv ?? $s) as ST_AlignH };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'posOffset': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_PositionOffset($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_PosH', 'posOffset', 'ST_PositionOffset', $s, cur);
            content = { kind: 'posOffset', value: ($pv ?? $s) as ST_PositionOffset };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PosH', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PosH', cur);
    cur.next();
  }

  return {
    content,
    relativeFrom,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PosV`. See `transitional/dml-wordprocessingDrawing.xsd:155`. */
export function readCT_PosV(cur: XmlCursor, ctx: ReadContext): CT_PosV {
  const start = requireStart(cur, 'CT_PosV');
  let relativeFrom: ST_RelFromV | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'relativeFrom': {
          const v = parseST_RelFromV(a.value);
          if (v !== undefined) {
            relativeFrom = v;
            continue;
          }
          ctx.invalidValue('CT_PosV', a, 'ST_RelFromV', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_PosV', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (relativeFrom === undefined) ctx.missingRequired('CT_PosV', 'relativeFrom', cur);

  let content: CT_PosV_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PosV', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'align': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_AlignV($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_PosV', 'align', 'ST_AlignV', $s, cur);
            content = { kind: 'align', value: ($pv ?? $s) as ST_AlignV };
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'posOffset': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_PositionOffset($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_PosV', 'posOffset', 'ST_PositionOffset', $s, cur);
            content = { kind: 'posOffset', value: ($pv ?? $s) as ST_PositionOffset };
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PosV', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PosV', cur);
    cur.next();
  }

  return {
    content,
    relativeFrom,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TextboxInfo`. See `transitional/dml-wordprocessingDrawing.xsd:192`. */
export function readCT_TextboxInfo(cur: XmlCursor, ctx: ReadContext): CT_TextboxInfo {
  const start = requireStart(cur, 'CT_TextboxInfo');
  let id: number | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'id': {
          const v = parseInteger(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_TextboxInfo', a, 'xsd:unsignedShort', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_TextboxInfo', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let txbxContent: CT_TxbxContent | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TextboxInfo', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'txbxContent': {
            txbxContent = readCT_TxbxContent(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TextboxInfo', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TextboxInfo', cur);
    cur.next();
  }

  return {
    txbxContent,
    extLst,
    id,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_TxbxContent`. See `transitional/dml-wordprocessingDrawing.xsd:189`. */
export function readCT_TxbxContent(cur: XmlCursor, ctx: ReadContext): CT_TxbxContent {
  const start = requireStart(cur, 'CT_TxbxContent');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_TxbxContent', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const blockLevelElts: CT_TxbxContent_BlockLevelElts[] = [];

  const NS_math = ctx.uris['math'];
  const NS_wml = ctx.uris['wml'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_TxbxContent', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_math) {
        switch (ev.localName) {
          case 'oMathPara': {
            blockLevelElts.push({ kind: 'oMathPara', value: readCT_OMathPara(cur, ctx) });
            continue;
          }
          case 'oMath': {
            blockLevelElts.push({ kind: 'oMath', value: readCT_OMath(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_wml) {
        switch (ev.localName) {
          case 'customXml': {
            blockLevelElts.push({ kind: 'customXml', value: readCT_CustomXmlBlock(cur, ctx) });
            continue;
          }
          case 'sdt': {
            blockLevelElts.push({ kind: 'sdt', value: readCT_SdtBlock(cur, ctx) });
            continue;
          }
          case 'p': {
            blockLevelElts.push({ kind: 'p', value: readCT_P(cur, ctx) });
            continue;
          }
          case 'tbl': {
            blockLevelElts.push({ kind: 'tbl', value: readCT_Tbl(cur, ctx) });
            continue;
          }
          case 'proofErr': {
            blockLevelElts.push({ kind: 'proofErr', value: readCT_ProofErr(cur, ctx) });
            continue;
          }
          case 'permStart': {
            blockLevelElts.push({ kind: 'permStart', value: readCT_PermStart(cur, ctx) });
            continue;
          }
          case 'permEnd': {
            blockLevelElts.push({ kind: 'permEnd', value: readCT_Perm(cur, ctx) });
            continue;
          }
          case 'bookmarkStart': {
            blockLevelElts.push({ kind: 'bookmarkStart', value: readCT_Bookmark(cur, ctx) });
            continue;
          }
          case 'bookmarkEnd': {
            blockLevelElts.push({ kind: 'bookmarkEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveFromRangeStart': {
            blockLevelElts.push({ kind: 'moveFromRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveFromRangeEnd': {
            blockLevelElts.push({ kind: 'moveFromRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'moveToRangeStart': {
            blockLevelElts.push({ kind: 'moveToRangeStart', value: readCT_MoveBookmark(cur, ctx) });
            continue;
          }
          case 'moveToRangeEnd': {
            blockLevelElts.push({ kind: 'moveToRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeStart': {
            blockLevelElts.push({ kind: 'commentRangeStart', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'commentRangeEnd': {
            blockLevelElts.push({ kind: 'commentRangeEnd', value: readCT_MarkupRange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeStart': {
            blockLevelElts.push({ kind: 'customXmlInsRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlInsRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlInsRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeStart': {
            blockLevelElts.push({ kind: 'customXmlDelRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlDelRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlDelRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveFromRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveFromRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeStart': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeStart', value: readCT_TrackChange(cur, ctx) });
            continue;
          }
          case 'customXmlMoveToRangeEnd': {
            blockLevelElts.push({ kind: 'customXmlMoveToRangeEnd', value: readCT_Markup(cur, ctx) });
            continue;
          }
          case 'ins': {
            blockLevelElts.push({ kind: 'ins', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'del': {
            blockLevelElts.push({ kind: 'del', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveFrom': {
            blockLevelElts.push({ kind: 'moveFrom', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'moveTo': {
            blockLevelElts.push({ kind: 'moveTo', value: readCT_RunTrackChange(cur, ctx) });
            continue;
          }
          case 'altChunk': {
            blockLevelElts.push({ kind: 'altChunk', value: readCT_AltChunk(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_TxbxContent', ev.localName, ev.uri, cur);
      blockLevelElts.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_TxbxContent', cur);
    cur.next();
  }

  return {
    blockLevelElts,
    $unknownAttrs,
  };
}

/** Read a `CT_WordprocessingCanvas`. See `transitional/dml-wordprocessingDrawing.xsd:268`. */
export function readCT_WordprocessingCanvas(cur: XmlCursor, ctx: ReadContext): CT_WordprocessingCanvas {
  const start = requireStart(cur, 'CT_WordprocessingCanvas');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_WordprocessingCanvas', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let bg: CT_BackgroundFormatting | undefined;
  let whole: CT_WholeE2oFormatting | undefined;
  const content: CT_WordprocessingCanvas_Content[] = [];
  let extLst: CT_OfficeArtExtensionList | undefined;

  const NS_dml_picture = ctx.uris['dml-picture'];
  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WordprocessingCanvas', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_picture) {
        switch (ev.localName) {
          case 'pic': {
            content.push({ kind: 'pic', value: readCT_Picture(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'bg': {
            bg = readCT_BackgroundFormatting(cur, ctx);
            continue;
          }
          case 'whole': {
            whole = readCT_WholeE2oFormatting(cur, ctx);
            continue;
          }
          case 'wsp': {
            content.push({ kind: 'wsp', value: readCT_WordprocessingShape(cur, ctx) });
            continue;
          }
          case 'contentPart': {
            content.push({ kind: 'contentPart', value: readCT_WordprocessingContentPart(cur, ctx) });
            continue;
          }
          case 'wgp': {
            content.push({ kind: 'wgp', value: readCT_WordprocessingGroup(cur, ctx) });
            continue;
          }
          case 'graphicFrame': {
            content.push({ kind: 'graphicFrame', value: readCT_GraphicFrame(cur, ctx) });
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WordprocessingCanvas', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WordprocessingCanvas', cur);
    cur.next();
  }

  return {
    bg,
    whole,
    content,
    extLst,
    $unknownAttrs,
  };
}

/** Read a `CT_WordprocessingContentPart`. See `transitional/dml-wordprocessingDrawing.xsd:243`. */
export function readCT_WordprocessingContentPart(cur: XmlCursor, ctx: ReadContext): CT_WordprocessingContentPart {
  const start = requireStart(cur, 'CT_WordprocessingContentPart');
  let bwMode: ST_BlackWhiteMode | undefined;
  let id: ST_RelationshipId | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  const NS_relationships_A = ctx.uris['relationships'];
  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'bwMode': {
          const v = parseST_BlackWhiteMode(a.value);
          if (v !== undefined) {
            bwMode = v;
            continue;
          }
          ctx.invalidValue('CT_WordprocessingContentPart', a, 'ST_BlackWhiteMode', cur);
          break;
        }
      }
    }
    if (a.uri === NS_relationships_A) {
      switch (a.localName) {
        case 'id': {
          const v = parseST_RelationshipId(a.value);
          if (v !== undefined) {
            id = v;
            continue;
          }
          ctx.invalidValue('CT_WordprocessingContentPart', a, 'ST_RelationshipId', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_WordprocessingContentPart', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (id === undefined) ctx.missingRequired('CT_WordprocessingContentPart', 'id', cur);

  let nvContentPartPr: CT_WordprocessingContentPartNonVisual | undefined;
  let xfrm: CT_Transform2D | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WordprocessingContentPart', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'nvContentPartPr': {
            nvContentPartPr = readCT_WordprocessingContentPartNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'xfrm': {
            xfrm = readCT_Transform2D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WordprocessingContentPart', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WordprocessingContentPart', cur);
    cur.next();
  }

  return {
    nvContentPartPr,
    xfrm,
    extLst,
    bwMode,
    id,
    $unknown,
    $unknownAttrs,
  };
}

/**
 * Read a `CT_WordprocessingContentPartNonVisual`. See
 * `transitional/dml-wordprocessingDrawing.xsd:237`.
 */
export function readCT_WordprocessingContentPartNonVisual(cur: XmlCursor, ctx: ReadContext): CT_WordprocessingContentPartNonVisual {
  const start = requireStart(cur, 'CT_WordprocessingContentPartNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_WordprocessingContentPartNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvContentPartPr: CT_NonVisualContentPartProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WordprocessingContentPartNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvContentPartPr': {
            cNvContentPartPr = readCT_NonVisualContentPartProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WordprocessingContentPartNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WordprocessingContentPartNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvContentPartPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_WordprocessingGroup`. See `transitional/dml-wordprocessingDrawing.xsd:252`. */
export function readCT_WordprocessingGroup(cur: XmlCursor, ctx: ReadContext): CT_WordprocessingGroup {
  const start = requireStart(cur, 'CT_WordprocessingGroup');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_WordprocessingGroup', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvGrpSpPr: CT_NonVisualGroupDrawingShapeProps | undefined;
  let grpSpPr: CT_GroupShapeProperties | undefined;
  const content: CT_WordprocessingGroup_Content[] = [];
  let extLst: CT_OfficeArtExtensionList | undefined;

  const NS_dml_picture = ctx.uris['dml-picture'];
  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WordprocessingGroup', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_picture) {
        switch (ev.localName) {
          case 'pic': {
            content.push({ kind: 'pic', value: readCT_Picture(cur, ctx) });
            continue;
          }
        }
      }
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            continue;
          }
          case 'cNvGrpSpPr': {
            cNvGrpSpPr = readCT_NonVisualGroupDrawingShapeProps(cur, ctx);
            continue;
          }
          case 'grpSpPr': {
            grpSpPr = readCT_GroupShapeProperties(cur, ctx);
            continue;
          }
          case 'wsp': {
            content.push({ kind: 'wsp', value: readCT_WordprocessingShape(cur, ctx) });
            continue;
          }
          case 'grpSp': {
            content.push({ kind: 'grpSp', value: readCT_WordprocessingGroup(cur, ctx) });
            continue;
          }
          case 'graphicFrame': {
            content.push({ kind: 'graphicFrame', value: readCT_GraphicFrame(cur, ctx) });
            continue;
          }
          case 'contentPart': {
            content.push({ kind: 'contentPart', value: readCT_WordprocessingContentPart(cur, ctx) });
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WordprocessingGroup', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WordprocessingGroup', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvGrpSpPr,
    grpSpPr,
    content,
    extLst,
    $unknownAttrs,
  };
}

/** Read a `CT_WordprocessingShape`. See `transitional/dml-wordprocessingDrawing.xsd:206`. */
export function readCT_WordprocessingShape(cur: XmlCursor, ctx: ReadContext): CT_WordprocessingShape {
  const start = requireStart(cur, 'CT_WordprocessingShape');
  let normalEastAsianFlow: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'normalEastAsianFlow': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            normalEastAsianFlow = v;
            continue;
          }
          ctx.invalidValue('CT_WordprocessingShape', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_WordprocessingShape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let content: CT_WordprocessingShape_Content | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let style: CT_ShapeStyle | undefined;
  let extLst: CT_OfficeArtExtensionList | undefined;
  let content2: CT_WordprocessingShape_Content2 | undefined;
  let bodyPr: CT_TextBodyProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WordprocessingShape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvSpPr': {
            content = { kind: 'cNvSpPr', value: readCT_NonVisualDrawingShapeProps(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'cNvCnPr': {
            content = { kind: 'cNvCnPr', value: readCT_NonVisualConnectorProperties(cur, ctx) };
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
          case 'extLst': {
            extLst = readCT_OfficeArtExtensionList(cur, ctx);
            $slot = 4;
            $index = undefined;
            continue;
          }
          case 'txbx': {
            content2 = { kind: 'txbx', value: readCT_TextboxInfo(cur, ctx) };
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'linkedTxbx': {
            content2 = { kind: 'linkedTxbx', value: readCT_LinkedTextboxInformation(cur, ctx) };
            $slot = 5;
            $index = undefined;
            continue;
          }
          case 'bodyPr': {
            bodyPr = readCT_TextBodyProperties(cur, ctx);
            $slot = 6;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WordprocessingShape', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WordprocessingShape', cur);
    cur.next();
  }

  return {
    cNvPr,
    content,
    spPr,
    style,
    extLst,
    content2,
    bodyPr,
    normalEastAsianFlow,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_WrapNone`. See `transitional/dml-wordprocessingDrawing.xsd:56`. */
export function readCT_WrapNone(cur: XmlCursor, ctx: ReadContext): CT_WrapNone {
  const start = requireStart(cur, 'CT_WrapNone');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_WrapNone', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WrapNone', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      ctx.unexpectedElement('CT_WrapNone', ev.localName, ev.uri, cur);
      cur.skip();
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WrapNone', cur);
    cur.next();
  }
  return {
    $unknownAttrs,
  };
}

/** Read a `CT_WrapPath`. See `transitional/dml-wordprocessingDrawing.xsd:49`. */
export function readCT_WrapPath(cur: XmlCursor, ctx: ReadContext): CT_WrapPath {
  const start = requireStart(cur, 'CT_WrapPath');
  let edited: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'edited': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            edited = v;
            continue;
          }
          ctx.invalidValue('CT_WrapPath', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_WrapPath', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let start_: CT_Point2D | undefined;
  const lineTo: CT_Point2D[] = [];
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WrapPath', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'start': {
            start_ = readCT_Point2D(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'lineTo': {
            lineTo.push(readCT_Point2D(cur, ctx));
            $slot = 1;
            $index = lineTo.length - 1;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WrapPath', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WrapPath', cur);
    cur.next();
  }

  return {
    start: start_,
    lineTo,
    edited,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_WrapSquare`. See `transitional/dml-wordprocessingDrawing.xsd:57`. */
export function readCT_WrapSquare(cur: XmlCursor, ctx: ReadContext): CT_WrapSquare {
  const start = requireStart(cur, 'CT_WrapSquare');
  let wrapText: ST_WrapText | undefined;
  let distT: ST_WrapDistance | undefined;
  let distB: ST_WrapDistance | undefined;
  let distL: ST_WrapDistance | undefined;
  let distR: ST_WrapDistance | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'wrapText': {
          const v = parseST_WrapText(a.value);
          if (v !== undefined) {
            wrapText = v;
            continue;
          }
          ctx.invalidValue('CT_WrapSquare', a, 'ST_WrapText', cur);
          break;
        }
        case 'distT': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distT = v;
            continue;
          }
          ctx.invalidValue('CT_WrapSquare', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'distB': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distB = v;
            continue;
          }
          ctx.invalidValue('CT_WrapSquare', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'distL': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distL = v;
            continue;
          }
          ctx.invalidValue('CT_WrapSquare', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'distR': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distR = v;
            continue;
          }
          ctx.invalidValue('CT_WrapSquare', a, 'ST_WrapDistance', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_WrapSquare', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (wrapText === undefined) ctx.missingRequired('CT_WrapSquare', 'wrapText', cur);

  let effectExtent: CT_EffectExtent | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WrapSquare', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'effectExtent': {
            effectExtent = readCT_EffectExtent(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WrapSquare', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WrapSquare', cur);
    cur.next();
  }

  return {
    effectExtent,
    wrapText,
    distT,
    distB,
    distL,
    distR,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_WrapThrough`. See `transitional/dml-wordprocessingDrawing.xsd:75`. */
export function readCT_WrapThrough(cur: XmlCursor, ctx: ReadContext): CT_WrapThrough {
  const start = requireStart(cur, 'CT_WrapThrough');
  let wrapText: ST_WrapText | undefined;
  let distL: ST_WrapDistance | undefined;
  let distR: ST_WrapDistance | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'wrapText': {
          const v = parseST_WrapText(a.value);
          if (v !== undefined) {
            wrapText = v;
            continue;
          }
          ctx.invalidValue('CT_WrapThrough', a, 'ST_WrapText', cur);
          break;
        }
        case 'distL': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distL = v;
            continue;
          }
          ctx.invalidValue('CT_WrapThrough', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'distR': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distR = v;
            continue;
          }
          ctx.invalidValue('CT_WrapThrough', a, 'ST_WrapDistance', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_WrapThrough', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (wrapText === undefined) ctx.missingRequired('CT_WrapThrough', 'wrapText', cur);

  let wrapPolygon: CT_WrapPath | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WrapThrough', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'wrapPolygon': {
            wrapPolygon = readCT_WrapPath(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WrapThrough', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WrapThrough', cur);
    cur.next();
  }

  return {
    wrapPolygon,
    wrapText,
    distL,
    distR,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_WrapTight`. See `transitional/dml-wordprocessingDrawing.xsd:67`. */
export function readCT_WrapTight(cur: XmlCursor, ctx: ReadContext): CT_WrapTight {
  const start = requireStart(cur, 'CT_WrapTight');
  let wrapText: ST_WrapText | undefined;
  let distL: ST_WrapDistance | undefined;
  let distR: ST_WrapDistance | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'wrapText': {
          const v = parseST_WrapText(a.value);
          if (v !== undefined) {
            wrapText = v;
            continue;
          }
          ctx.invalidValue('CT_WrapTight', a, 'ST_WrapText', cur);
          break;
        }
        case 'distL': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distL = v;
            continue;
          }
          ctx.invalidValue('CT_WrapTight', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'distR': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distR = v;
            continue;
          }
          ctx.invalidValue('CT_WrapTight', a, 'ST_WrapDistance', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_WrapTight', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  if (wrapText === undefined) ctx.missingRequired('CT_WrapTight', 'wrapText', cur);

  let wrapPolygon: CT_WrapPath | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WrapTight', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'wrapPolygon': {
            wrapPolygon = readCT_WrapPath(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WrapTight', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WrapTight', cur);
    cur.next();
  }

  return {
    wrapPolygon,
    wrapText,
    distL,
    distR,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_WrapTopBottom`. See `transitional/dml-wordprocessingDrawing.xsd:83`. */
export function readCT_WrapTopBottom(cur: XmlCursor, ctx: ReadContext): CT_WrapTopBottom {
  const start = requireStart(cur, 'CT_WrapTopBottom');
  let distT: ST_WrapDistance | undefined;
  let distB: ST_WrapDistance | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'distT': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distT = v;
            continue;
          }
          ctx.invalidValue('CT_WrapTopBottom', a, 'ST_WrapDistance', cur);
          break;
        }
        case 'distB': {
          const v = parseST_WrapDistance(a.value);
          if (v !== undefined) {
            distB = v;
            continue;
          }
          ctx.invalidValue('CT_WrapTopBottom', a, 'ST_WrapDistance', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_WrapTopBottom', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let effectExtent: CT_EffectExtent | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_wordprocessingDrawing = ctx.uris['dml-wordprocessingDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_WrapTopBottom', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_wordprocessingDrawing) {
        switch (ev.localName) {
          case 'effectExtent': {
            effectExtent = readCT_EffectExtent(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_WrapTopBottom', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_WrapTopBottom', cur);
    cur.next();
  }

  return {
    effectExtent,
    distT,
    distB,
    $unknown,
    $unknownAttrs,
  };
}
