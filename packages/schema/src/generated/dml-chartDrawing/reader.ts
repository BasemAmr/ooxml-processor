/**
 * @generated by @ooxml/codegen — do not edit.
 *
 * Source: readers for the dml-chartDrawing namespace of the ECMA-376 5th edition schema set
 *
 * Regenerate with `pnpm gen`. CI fails if this file differs from a fresh run,
 * so hand edits will be reverted by the next build.
 */

import {
  type PositionedRaw,
  type ReadContext,
  type XmlAttr,
  type XmlCursor,
  isIgnorableWhitespace,
  parseDouble,
  parseXsdBoolean,
  readScalar,
  requireStart,
} from '../../runtime/index.js';
import {
  readCT_BlipFillProperties,
  readCT_GraphicalObject,
  readCT_GroupShapeProperties,
  readCT_NonVisualConnectorProperties,
  readCT_NonVisualDrawingProps,
  readCT_NonVisualDrawingShapeProps,
  readCT_NonVisualGraphicFrameProperties,
  readCT_NonVisualGroupDrawingShapeProps,
  readCT_NonVisualPictureProperties,
  readCT_PositiveSize2D,
  readCT_ShapeProperties,
  readCT_ShapeStyle,
  readCT_TextBody,
  readCT_Transform2D,
} from '../dml-main/reader.js';
import type {
  CT_BlipFillProperties,
  CT_GraphicalObject,
  CT_GroupShapeProperties,
  CT_NonVisualConnectorProperties,
  CT_NonVisualDrawingProps,
  CT_NonVisualDrawingShapeProps,
  CT_NonVisualGraphicFrameProperties,
  CT_NonVisualGroupDrawingShapeProps,
  CT_NonVisualPictureProperties,
  CT_PositiveSize2D,
  CT_ShapeProperties,
  CT_ShapeStyle,
  CT_TextBody,
  CT_Transform2D,
} from '../dml-main/types.js';
import type {
  CT_AbsSizeAnchor,
  CT_AbsSizeAnchor_Content,
  CT_Connector,
  CT_ConnectorNonVisual,
  CT_Drawing,
  CT_Drawing_Anchor,
  CT_GraphicFrame,
  CT_GraphicFrameNonVisual,
  CT_GroupShape,
  CT_GroupShapeNonVisual,
  CT_GroupShape_Content,
  CT_Marker,
  CT_Picture,
  CT_PictureNonVisual,
  CT_RelSizeAnchor,
  CT_RelSizeAnchor_Content,
  CT_Shape,
  CT_ShapeNonVisual,
  ST_MarkerCoordinate,
} from './types.js';

// Simple-type parsers. `undefined` means "outside the lexical space".

export function parseST_MarkerCoordinate(value: string): ST_MarkerCoordinate | undefined {
  const n = parseDouble(value);
  return n === undefined ? undefined : (n as ST_MarkerCoordinate);
}

// Complex-type readers.

/** Read a `CT_AbsSizeAnchor`. See `transitional/dml-chartDrawing.xsd:128`. */
export function readCT_AbsSizeAnchor(cur: XmlCursor, ctx: ReadContext): CT_AbsSizeAnchor {
  const start = requireStart(cur, 'CT_AbsSizeAnchor');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_AbsSizeAnchor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let from: CT_Marker | undefined;
  let ext: CT_PositiveSize2D | undefined;
  let content: CT_AbsSizeAnchor_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_AbsSizeAnchor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'from': {
            from = readCT_Marker(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'ext': {
            ext = readCT_PositiveSize2D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sp': {
            content = { kind: 'sp', value: readCT_Shape(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'grpSp': {
            content = { kind: 'grpSp', value: readCT_GroupShape(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'graphicFrame': {
            content = { kind: 'graphicFrame', value: readCT_GraphicFrame(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'cxnSp': {
            content = { kind: 'cxnSp', value: readCT_Connector(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'pic': {
            content = { kind: 'pic', value: readCT_Picture(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_AbsSizeAnchor', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_AbsSizeAnchor', cur);
    cur.next();
  }

  return {
    from,
    ext,
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Connector`. See `transitional/dml-chartDrawing.xsd:35`. */
export function readCT_Connector(cur: XmlCursor, ctx: ReadContext): CT_Connector {
  const start = requireStart(cur, 'CT_Connector');
  let macro: string | undefined;
  let fPublished: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'macro':
          macro = a.value as string;
          continue;

        case 'fPublished': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fPublished = v;
            continue;
          }
          ctx.invalidValue('CT_Connector', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Connector', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvCxnSpPr: CT_ConnectorNonVisual | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let style: CT_ShapeStyle | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Connector', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'nvCxnSpPr': {
            nvCxnSpPr = readCT_ConnectorNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Connector', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Connector', cur);
    cur.next();
  }

  return {
    nvCxnSpPr,
    spPr,
    style,
    macro,
    fPublished,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ConnectorNonVisual`. See `transitional/dml-chartDrawing.xsd:28`. */
export function readCT_ConnectorNonVisual(cur: XmlCursor, ctx: ReadContext): CT_ConnectorNonVisual {
  const start = requireStart(cur, 'CT_ConnectorNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ConnectorNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvCxnSpPr: CT_NonVisualConnectorProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ConnectorNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvCxnSpPr': {
            cNvCxnSpPr = readCT_NonVisualConnectorProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ConnectorNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ConnectorNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvCxnSpPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Drawing`. See `transitional/dml-chartDrawing.xsd:141`. */
export function readCT_Drawing(cur: XmlCursor, ctx: ReadContext): CT_Drawing {
  const start = requireStart(cur, 'CT_Drawing');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Drawing', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  const anchor: CT_Drawing_Anchor[] = [];

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Drawing', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'relSizeAnchor': {
            anchor.push({ kind: 'relSizeAnchor', value: readCT_RelSizeAnchor(cur, ctx) });
            continue;
          }
          case 'absSizeAnchor': {
            anchor.push({ kind: 'absSizeAnchor', value: readCT_AbsSizeAnchor(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Drawing', ev.localName, ev.uri, cur);
      anchor.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Drawing', cur);
    cur.next();
  }

  return {
    anchor,
    $unknownAttrs,
  };
}

/** Read a `CT_GraphicFrame`. See `transitional/dml-chartDrawing.xsd:68`. */
export function readCT_GraphicFrame(cur: XmlCursor, ctx: ReadContext): CT_GraphicFrame {
  const start = requireStart(cur, 'CT_GraphicFrame');
  let macro: string | undefined;
  let fPublished: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'macro':
          macro = a.value as string;
          continue;

        case 'fPublished': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fPublished = v;
            continue;
          }
          ctx.invalidValue('CT_GraphicFrame', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_GraphicFrame', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvGraphicFramePr: CT_GraphicFrameNonVisual | undefined;
  let xfrm: CT_Transform2D | undefined;
  let graphic: CT_GraphicalObject | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  const NS_dml_main = ctx.uris['dml-main'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GraphicFrame', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'nvGraphicFramePr': {
            nvGraphicFramePr = readCT_GraphicFrameNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'xfrm': {
            xfrm = readCT_Transform2D(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      if (ev.uri === NS_dml_main) {
        switch (ev.localName) {
          case 'graphic': {
            graphic = readCT_GraphicalObject(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GraphicFrame', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GraphicFrame', cur);
    cur.next();
  }

  return {
    nvGraphicFramePr,
    xfrm,
    graphic,
    macro,
    fPublished,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GraphicFrameNonVisual`. See `transitional/dml-chartDrawing.xsd:61`. */
export function readCT_GraphicFrameNonVisual(cur: XmlCursor, ctx: ReadContext): CT_GraphicFrameNonVisual {
  const start = requireStart(cur, 'CT_GraphicFrameNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GraphicFrameNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvGraphicFramePr: CT_NonVisualGraphicFrameProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GraphicFrameNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvGraphicFramePr': {
            cNvGraphicFramePr = readCT_NonVisualGraphicFrameProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GraphicFrameNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GraphicFrameNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvGraphicFramePr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_GroupShape`. See `transitional/dml-chartDrawing.xsd:85`. */
export function readCT_GroupShape(cur: XmlCursor, ctx: ReadContext): CT_GroupShape {
  const start = requireStart(cur, 'CT_GroupShape');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GroupShape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvGrpSpPr: CT_GroupShapeNonVisual | undefined;
  let grpSpPr: CT_GroupShapeProperties | undefined;
  const content: CT_GroupShape_Content[] = [];

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GroupShape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'nvGrpSpPr': {
            nvGrpSpPr = readCT_GroupShapeNonVisual(cur, ctx);
            continue;
          }
          case 'grpSpPr': {
            grpSpPr = readCT_GroupShapeProperties(cur, ctx);
            continue;
          }
          case 'sp': {
            content.push({ kind: 'sp', value: readCT_Shape(cur, ctx) });
            continue;
          }
          case 'grpSp': {
            content.push({ kind: 'grpSp', value: readCT_GroupShape(cur, ctx) });
            continue;
          }
          case 'graphicFrame': {
            content.push({ kind: 'graphicFrame', value: readCT_GraphicFrame(cur, ctx) });
            continue;
          }
          case 'cxnSp': {
            content.push({ kind: 'cxnSp', value: readCT_Connector(cur, ctx) });
            continue;
          }
          case 'pic': {
            content.push({ kind: 'pic', value: readCT_Picture(cur, ctx) });
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GroupShape', ev.localName, ev.uri, cur);
      content.push({ kind: '$raw', value: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GroupShape', cur);
    cur.next();
  }

  return {
    nvGrpSpPr,
    grpSpPr,
    content,
    $unknownAttrs,
  };
}

/** Read a `CT_GroupShapeNonVisual`. See `transitional/dml-chartDrawing.xsd:78`. */
export function readCT_GroupShapeNonVisual(cur: XmlCursor, ctx: ReadContext): CT_GroupShapeNonVisual {
  const start = requireStart(cur, 'CT_GroupShapeNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_GroupShapeNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvGrpSpPr: CT_NonVisualGroupDrawingShapeProps | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_GroupShapeNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvGrpSpPr': {
            cNvGrpSpPr = readCT_NonVisualGroupDrawingShapeProps(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_GroupShapeNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_GroupShapeNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvGrpSpPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Marker`. See `transitional/dml-chartDrawing.xsd:115`. */
export function readCT_Marker(cur: XmlCursor, ctx: ReadContext): CT_Marker {
  const start = requireStart(cur, 'CT_Marker');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_Marker', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let x: ST_MarkerCoordinate | undefined;
  let y: ST_MarkerCoordinate | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Marker', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'x': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_MarkerCoordinate($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Marker', 'x', 'ST_MarkerCoordinate', $s, cur);
            x = ($pv ?? $s) as ST_MarkerCoordinate;
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'y': {
            const $s = readScalar(cur, ctx);
            const $pv = parseST_MarkerCoordinate($s);
            if ($pv === undefined) ctx.invalidElementValue('CT_Marker', 'y', 'ST_MarkerCoordinate', $s, cur);
            y = ($pv ?? $s) as ST_MarkerCoordinate;
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Marker', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Marker', cur);
    cur.next();
  }

  return {
    x,
    y,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Picture`. See `transitional/dml-chartDrawing.xsd:51`. */
export function readCT_Picture(cur: XmlCursor, ctx: ReadContext): CT_Picture {
  const start = requireStart(cur, 'CT_Picture');
  let macro: string | undefined;
  let fPublished: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'macro':
          macro = a.value as string;
          continue;

        case 'fPublished': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fPublished = v;
            continue;
          }
          ctx.invalidValue('CT_Picture', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Picture', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvPicPr: CT_PictureNonVisual | undefined;
  let blipFill: CT_BlipFillProperties | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let style: CT_ShapeStyle | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Picture', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'nvPicPr': {
            nvPicPr = readCT_PictureNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'blipFill': {
            blipFill = readCT_BlipFillProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Picture', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Picture', cur);
    cur.next();
  }

  return {
    nvPicPr,
    blipFill,
    spPr,
    style,
    macro,
    fPublished,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_PictureNonVisual`. See `transitional/dml-chartDrawing.xsd:44`. */
export function readCT_PictureNonVisual(cur: XmlCursor, ctx: ReadContext): CT_PictureNonVisual {
  const start = requireStart(cur, 'CT_PictureNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_PictureNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvPicPr: CT_NonVisualPictureProperties | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_PictureNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvPicPr': {
            cNvPicPr = readCT_NonVisualPictureProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_PictureNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_PictureNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvPicPr,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_RelSizeAnchor`. See `transitional/dml-chartDrawing.xsd:121`. */
export function readCT_RelSizeAnchor(cur: XmlCursor, ctx: ReadContext): CT_RelSizeAnchor {
  const start = requireStart(cur, 'CT_RelSizeAnchor');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_RelSizeAnchor', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let from: CT_Marker | undefined;
  let to: CT_Marker | undefined;
  let content: CT_RelSizeAnchor_Content | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_RelSizeAnchor', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'from': {
            from = readCT_Marker(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'to': {
            to = readCT_Marker(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'sp': {
            content = { kind: 'sp', value: readCT_Shape(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'grpSp': {
            content = { kind: 'grpSp', value: readCT_GroupShape(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'graphicFrame': {
            content = { kind: 'graphicFrame', value: readCT_GraphicFrame(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'cxnSp': {
            content = { kind: 'cxnSp', value: readCT_Connector(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'pic': {
            content = { kind: 'pic', value: readCT_Picture(cur, ctx) };
            $slot = 2;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_RelSizeAnchor', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_RelSizeAnchor', cur);
    cur.next();
  }

  return {
    from,
    to,
    content,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_Shape`. See `transitional/dml-chartDrawing.xsd:16`. */
export function readCT_Shape(cur: XmlCursor, ctx: ReadContext): CT_Shape {
  const start = requireStart(cur, 'CT_Shape');
  let macro: string | undefined;
  let textlink: string | undefined;
  let fLocksText: boolean | undefined;
  let fPublished: boolean | undefined;
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    if (a.uri === '') {
      switch (a.localName) {
        case 'macro':
          macro = a.value as string;
          continue;

        case 'textlink':
          textlink = a.value as string;
          continue;

        case 'fLocksText': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fLocksText = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'xsd:boolean', cur);
          break;
        }
        case 'fPublished': {
          const v = parseXsdBoolean(a.value);
          if (v !== undefined) {
            fPublished = v;
            continue;
          }
          ctx.invalidValue('CT_Shape', a, 'xsd:boolean', cur);
          break;
        }
      }
    }
    ctx.unexpectedAttribute('CT_Shape', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let nvSpPr: CT_ShapeNonVisual | undefined;
  let spPr: CT_ShapeProperties | undefined;
  let style: CT_ShapeStyle | undefined;
  let txBody: CT_TextBody | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_Shape', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'nvSpPr': {
            nvSpPr = readCT_ShapeNonVisual(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'spPr': {
            spPr = readCT_ShapeProperties(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
          case 'style': {
            style = readCT_ShapeStyle(cur, ctx);
            $slot = 2;
            $index = undefined;
            continue;
          }
          case 'txBody': {
            txBody = readCT_TextBody(cur, ctx);
            $slot = 3;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_Shape', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_Shape', cur);
    cur.next();
  }

  return {
    nvSpPr,
    spPr,
    style,
    txBody,
    macro,
    textlink,
    fLocksText,
    fPublished,
    $unknown,
    $unknownAttrs,
  };
}

/** Read a `CT_ShapeNonVisual`. See `transitional/dml-chartDrawing.xsd:9`. */
export function readCT_ShapeNonVisual(cur: XmlCursor, ctx: ReadContext): CT_ShapeNonVisual {
  const start = requireStart(cur, 'CT_ShapeNonVisual');
  let $unknownAttrs: XmlAttr[] | undefined;

  for (const a of start.attrs) {
    ctx.unexpectedAttribute('CT_ShapeNonVisual', a, cur);
    ($unknownAttrs ??= []).push(a);
  }

  let cNvPr: CT_NonVisualDrawingProps | undefined;
  let cNvSpPr: CT_NonVisualDrawingShapeProps | undefined;
  let $unknown: PositionedRaw[] | undefined;
  let $slot = -1;
  let $index: number | undefined;

  const NS_dml_chartDrawing = ctx.uris['dml-chartDrawing'];
  cur.next();
  for (;;) {
    const ev = cur.current;
    if (ev === undefined) { ctx.unexpectedEof('CT_ShapeNonVisual', cur); break; }
    if (ev.type === 'endElement') { cur.next(); break; }
    if (ev.type === 'startElement') {
      if (ev.uri === NS_dml_chartDrawing) {
        switch (ev.localName) {
          case 'cNvPr': {
            cNvPr = readCT_NonVisualDrawingProps(cur, ctx);
            $slot = 0;
            $index = undefined;
            continue;
          }
          case 'cNvSpPr': {
            cNvSpPr = readCT_NonVisualDrawingShapeProps(cur, ctx);
            $slot = 1;
            $index = undefined;
            continue;
          }
        }
      }
      ctx.unexpectedElement('CT_ShapeNonVisual', ev.localName, ev.uri, cur);
      ($unknown ??= []).push({ afterSlot: $slot, afterIndex: $index, node: cur.skipToRaw() });
      continue;
    }
    if (ev.type === 'text' && !isIgnorableWhitespace(ev.value)) ctx.unexpectedText('CT_ShapeNonVisual', cur);
    cur.next();
  }

  return {
    cNvPr,
    cNvSpPr,
    $unknown,
    $unknownAttrs,
  };
}
