import { isToggleProperty, toggleCombine, foldNumbering, formatNumber } from '@ooxml/wml';
export function probeFormat(): void { void isToggleProperty; void toggleCombine; void foldNumbering; void formatNumber; }
