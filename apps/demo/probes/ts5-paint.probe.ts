import { DisplayList, batchDisplayList, CanvasPainter, PageBitmapCache, ViewportManager } from '@ooxml/paint';
export function probePaint(): void { const list = new DisplayList(); void batchDisplayList(list); void CanvasPainter; void new PageBitmapCache(); void new ViewportManager(); }
