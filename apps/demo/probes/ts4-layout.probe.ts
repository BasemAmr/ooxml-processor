import { pageGeometry, textArea, enumerateSections, ExclusionStore, layoutMath, fixpoint } from '@ooxml/layout';
export function probeLayout(): void { const g = pageGeometry(undefined); void textArea(g); void enumerateSections; void new ExclusionStore(); void layoutMath; void fixpoint; }
